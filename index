<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>ezOne PMS — App Shell</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
<style>
/* =========================================================================
   LOGO ezOne PMS — VECTOR GỐC TỪ FIGMA
   Nguồn: EzOne.DesignLibrary node 21274:1436
   (Module=PMS, Style=Horizontal, Mode=Light Mode) — export SVG.
   Wordmark đã outline sẵn nên KHÔNG cần nhúng font logo. Mọi fill được
   ánh xạ sang biến --logo-* để design token vẫn điều khiển được màu.
   ========================================================================= */
:root{
  /* Theme */
  --theme-action-hover:#2F2B3D0F;
  --theme-action-selected:#2F2B3D14;
  --theme-divider:#2F2B3D1F;
  --theme-input-border:#2F2B3D38;
  --theme-text-primary:#2F2B3DE5;
  --theme-text-secondary:#2F2B3DB2;
  --theme-text-disabled:#2F2B3D66;

  /* Palette */
  --color-palette-primary-main:#186FF4;
  --color-palette-primary-dark:#1562D7;
  --color-palette-error-main:#FF4C51;

  /* Logo ezOne PMS (EzOne.DesignLibrary 21274:1436) */
  --logo-emblem:#345B82;   /* accent — vòng tròn emblem + chữ "PMS" */
  --logo-bar-1:#8A9BB2;    /* accent-4 — vạch trên */
  --logo-bar-2:#BAC4D1;    /* accent-5 — vạch giữa */
  --logo-bar-3:#FFFFFF;    /* background — vạch dưới */
  --logo-ez:#00A2E1;       /* accent-3 — chữ "ez" */
  --logo-one:#004BA5;      /* accent-alt — chữ "One" */
  --logo-pms:#345B82;      /* accent — chữ "PMS" */

  /* Surfaces */
  --misc-paper:#FFFFFF;
  /* v42: --theme-table-header trước đây CHỈ được khai báo trong khối [data-theme="dark"],
     nên ở giao diện sáng nó rỗng -> nền <th> dính bị TRONG SUỐT và dòng dữ liệu
     cuộn xuyên qua tiêu đề cột. Khai báo mặc định sáng đúng token bảng của dự án. */
  --theme-table-header:#F6F7FB;
  --misc-body-bg:#F8F7FA;
  --misc-bg-white-text:#FFFFFF;
  --misc-avatar-bg:#EEEDF0;
  --misc-card-header:4px;

  /* Radius */
  --border-radius-sm:4px;
  --border-radius-md:6px;
  --border-round:500px;

  /* Padding scale */
  --padding-1:4px; --padding-2:8px; --padding-3:12px; --padding-4:16px;

  /* Gap scale */
  --gap-2:8px; --gap-3:12px;

  /* Type */
  --text-size-md:15px;

  /* Elevation */
  --shadow-sm:0px 2px 8px 0px rgba(47,43,61,.12);
  --shadow-md:0px 3px 12px 0px rgba(47,43,61,.14);
  --shadow-rail:0px 2px 4px 0px rgba(47,43,61,.12);

  /* Layout constants */
  --sidebar-width:260px;
  --rail-width:70px;
  --group-gap:4px;
  --select-width:300px;
  --field-px:14px;
  --avatar-size:38px;
  --icon-size:22px;
}

*{box-sizing:border-box}
html,body{margin:0;padding:0;height:100%}
body{
  font-family:'Roboto',-apple-system,BlinkMacSystemFont,'Segoe UI',Helvetica,Arial,sans-serif;
  background:var(--misc-body-bg);
  color:var(--theme-text-primary);
  -webkit-font-smoothing:antialiased;
}
button{font-family:inherit;background:none;border:0;padding:0;cursor:pointer;color:inherit}
:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:2px}

/* ================= APP ================= */
.app{display:flex;flex-direction:row;height:100vh;overflow:hidden}

/* ================= SIDEBAR ================= */
.sidebar{
  width:var(--sidebar-width);
  flex:0 0 auto;
  background:var(--misc-paper);
  box-shadow:var(--shadow-sm);
  display:flex;flex-direction:column;
  overflow:hidden;
  transition:width .22s ease;
  z-index:3;
}
.logo-row{
  display:flex;align-items:center;justify-content:space-between;
  padding:12px 16px 8px;
  flex:0 0 auto;
}
.logo-btn{display:flex;align-items:center;gap:var(--gap-2);border-radius:var(--border-radius-md);min-width:0}
.logo-full{height:36px;width:auto;display:block;flex:0 0 auto}
.logo-mark{height:48px;width:48px;display:none;flex:0 0 auto}
.pin-btn{
  width:20px;height:20px;flex:0 0 auto;
  display:flex;align-items:center;justify-content:center;
  color:var(--theme-text-secondary);border-radius:var(--border-round);
}
.pin-btn:hover{color:var(--color-palette-primary-main)}
.pin-btn svg{width:20px;height:20px}

.menu-drawer{flex:1;min-height:0;padding:4px 10px var(--padding-3);display:flex}
.pages{flex:1;min-height:0;display:flex}
.pages-scroll{
  flex:1;min-height:0;overflow-y:auto;overflow-x:hidden;
  display:flex;flex-direction:column;gap:var(--group-gap);
  padding-right:2px;
}
.pages-scroll::-webkit-scrollbar{width:6px}
.pages-scroll::-webkit-scrollbar-thumb{background:var(--theme-input-border);border-radius:var(--border-round)}
.pages-scroll::-webkit-scrollbar-track{background:transparent}

.nav-item{
  display:flex;align-items:center;gap:var(--gap-2);
  width:100%;min-height:38px;padding:8px 10px;
  border-radius:var(--border-radius-sm);
  color:var(--theme-text-primary);text-align:left;
}
.nav-item:hover{background:var(--theme-action-hover)}
.nav-icon{width:var(--icon-size);height:var(--icon-size);flex:0 0 auto;display:block;color:var(--theme-text-secondary)}
.nav-label{flex:1;min-width:0;font-size:var(--text-size-md);line-height:22px;font-weight:400;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.nav-caret{
  width:20px;height:20px;flex:0 0 auto;display:flex;align-items:center;justify-content:center;
  color:var(--theme-text-secondary);transition:transform .18s ease;
}
.nav-caret svg{width:18px;height:18px}
.nav-group.is-closed .nav-caret{transform:rotate(-90deg)}
.nav-group.is-closed .nav-children{display:none}
.nav-children{display:flex;flex-direction:column;gap:var(--group-gap);margin-top:var(--group-gap)}
.nav-child{
  display:flex;align-items:center;gap:var(--gap-2);
  width:100%;min-height:38px;padding:8px 10px;
  border-radius:var(--border-radius-sm);
  color:var(--theme-text-primary);text-align:left;
}
.nav-child:hover{background:var(--theme-action-hover)}
.nav-bullet{width:var(--icon-size);height:var(--icon-size);flex:0 0 auto;display:flex;align-items:center;justify-content:center;color:var(--theme-text-secondary)}
.nav-bullet svg{width:12px;height:12px}
.nav-child.is-selected{background:var(--color-palette-primary-main);color:var(--misc-bg-white-text)}
.nav-child.is-selected .nav-icon{color:var(--misc-bg-white-text)}
.nav-child.is-selected:hover{background:var(--color-palette-primary-dark)}
.nav-child.has-dot{padding-left:14px}
.nav-dot{width:var(--icon-size);height:var(--icon-size);color:var(--theme-text-secondary);opacity:.55}
.nav-dot circle{r:3.2}
.nav-child.is-selected .nav-dot{color:var(--misc-bg-white-text);opacity:1}

/* ================= CONTAINER ================= */
.container{display:flex;flex-direction:column;flex:1;min-width:0}
.nav-wrap{padding:var(--misc-card-header);flex:0 0 auto}
.nav-header{
  display:flex;align-items:center;gap:var(--gap-2);
  padding:8px 16px;
  background:var(--misc-paper);
  border-radius:var(--border-radius-sm);
  box-shadow:var(--shadow-sm);
}
.search{display:flex;align-items:center;gap:var(--padding-1);min-width:0}
.search-placeholder{font-size:var(--text-size-md);color:var(--theme-text-disabled);white-space:nowrap}
.actions{flex:1;display:flex;align-items:center;justify-content:flex-end;gap:var(--padding-3);min-width:0}
.select{width:var(--select-width);max-width:100%;min-width:0}
.select-field{
  display:flex;align-items:center;gap:var(--gap-2);width:100%;
  border:1px solid var(--theme-input-border);
  border-radius:var(--border-radius-md);
  padding:8px var(--field-px);
}
.select-field:hover{border-color:var(--color-palette-primary-main)}
.select-value{flex:1;min-width:0;font-size:14px;color:var(--theme-text-primary);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;text-align:left}
.select-chevron{width:16px;height:16px;flex:0 0 auto;color:var(--theme-text-secondary)}
.tools{display:flex;align-items:center;gap:var(--padding-1)}
.icon-btn{
  display:flex;align-items:center;justify-content:center;
  padding:8px;border-radius:var(--border-round);
  color:var(--theme-text-secondary);
}
.icon-btn:hover{background:var(--theme-action-hover);color:var(--color-palette-primary-main)}
.icon-btn svg{width:22px;height:22px;display:block}
.bell-wrap{position:relative;display:flex}
.badge{
  position:absolute;top:6px;right:6px;
  width:8px;height:8px;border-radius:var(--border-round);
  background:var(--color-palette-error-main);
  border:1px solid var(--misc-paper);
}
.avatar{
  width:var(--avatar-size);height:var(--avatar-size);flex:0 0 auto;
  border-radius:var(--border-round);overflow:hidden;background:var(--misc-avatar-bg);
}
.avatar img{width:100%;height:100%;object-fit:cover;display:block}

.body{flex:1;min-height:0;padding:var(--misc-card-header);display:flex}
.card{
  flex:1;min-height:0;
  background:var(--misc-paper);
  border-radius:var(--border-radius-md);
  box-shadow:var(--shadow-md);
  overflow:auto;
}

/* ================= RAIL (thu gọn) ================= */
.app.is-collapsed .sidebar{width:var(--rail-width);box-shadow:var(--shadow-rail)}
.app.is-collapsed .logo-row{justify-content:center;height:70px;padding:0}
.app.is-collapsed .logo-full{display:none}
.app.is-collapsed .logo-mark{display:block}
.app.is-collapsed .pin-btn,
.app.is-collapsed .nav-label,
.app.is-collapsed .nav-caret,
.app.is-collapsed .nav-children{display:none}
.app.is-collapsed .menu-drawer{padding:0 var(--padding-2) var(--padding-3)}
.app.is-collapsed .nav-item{justify-content:center;padding:10px 0}
.app.is-collapsed .nav-item:hover .nav-icon{color:var(--color-palette-primary-main)}

  /* ============================================================
     QUICK SETUP — Global Setup Progress Bar + Panel
     SRS: ezAdmin_SRS Thanh tiến trình điều hướng — Quick Setup
     Figma mockup: node 57580:30490
     ============================================================ */
  :root {
    --qs-track: var(--theme-action-selected);
    --qs-done: #28C76F;              /* Color/Success/success-500 */
    --qs-done-soft: #28C76F14;       /* Opacity Color/Success/success-8 */
    --qs-primary-soft: #186FF414;    /* Opacity Color/Primary/primary-8 */
    --qs-primary-16: #186FF429;      /* Opacity Color/Primary/primary-16 */
    --qs-grey-light: #FAFAFA;        /* Misc/grey-light */
    --qs-panel-w: 480px;   /* chiều ngang dropdown */
    --qs-panel-h: 720px;   /* chiều cao dropdown — vượt quá thì cuộn dọc */
    --qs-trigger-h: 38px;
  }

  .qs { position: relative; flex-shrink: 0; }
  .nav-header { overflow: visible; }   /* cho popover tràn ra ngoài */
  [hidden] { display: none !important; }  /* để hidden thắng display của component */
  /* cho phép cụm search co lại, nhường chỗ cho trigger tiến độ */
  .search { min-width: 0; overflow: hidden; }
  .search-placeholder { overflow: hidden; text-overflow: ellipsis; }

  /* ---------- Trigger (khung chung) ---------- */
  .qs-trigger {
    display: inline-flex;
    align-items: center;
    gap: var(--gap-2);
    height: var(--qs-trigger-h);
    padding: 0 var(--padding-3);
    border: 1px solid var(--theme-input-border);
    border-radius: var(--border-round);
    background: var(--misc-paper);
    color: var(--theme-text-primary);
    font: inherit;
    cursor: pointer;
    white-space: nowrap;
    transition: border-color .15s ease, background .15s ease;
  }
  .qs-trigger:hover { border-color: var(--color-palette-primary-main); background: var(--qs-primary-soft); }
  .qs-trigger:focus-visible { outline: 2px solid var(--color-palette-primary-main); outline-offset: 2px; }
  .qs-trigger.is-open { border-color: var(--color-palette-primary-main); background: var(--qs-primary-soft); }

  .qs-t-label { font-size: 13px; line-height: 1; color: var(--theme-text-secondary); }
  .qs-t-count {
    font-size: 13px; font-weight: 700; line-height: 1;
    color: var(--color-palette-primary-main);
    font-variant-numeric: tabular-nums;
  }

  /* ---------- PA1: vòng tròn % ---------- */
  .qs-ring { width: 26px; height: 26px; flex-shrink: 0; display: block; }
  .qs-ring circle { fill: none; stroke-width: 3.2; }
  .qs-ring .qs-ring-bg { stroke: var(--qs-track); }
  .qs-ring .qs-ring-fg {
    stroke: var(--color-palette-primary-main);
    stroke-linecap: round;
    transform: rotate(-90deg);
    transform-origin: 50% 50%;
    transition: stroke-dashoffset .45s cubic-bezier(.4,0,.2,1);
  }
  .qs-ring-text {
    font-size: 9px; font-weight: 700;
    fill: var(--theme-text-primary);
    text-anchor: middle; dominant-baseline: central;
  }

  /* ---------- PA2: thanh ngang ---------- */
  .qs-bar-wrap { display: flex; flex-direction: column; gap: 3px; min-width: 140px; }
  .qs-bar-top { display: flex; align-items: baseline; justify-content: space-between; gap: var(--gap-2); }
  .qs-track {
    position: relative; height: 6px; width: 100%;
    border-radius: var(--border-round);
    background: var(--qs-track); overflow: hidden;
  }
  .qs-fill {
    position: absolute; inset: 0 auto 0 0; width: 0%;
    border-radius: var(--border-round);
    background: linear-gradient(90deg, var(--color-palette-primary-main), var(--logo-ez));
    transition: width .45s cubic-bezier(.4,0,.2,1);
  }
  .qs[data-variant="2"] .qs-trigger { border-radius: var(--border-radius-md); }

  /* ---------- PA3: nút mở drawer ---------- */
  .qs-spark { display: inline-flex; width: 20px; height: 20px; color: var(--color-palette-primary-main); flex-shrink: 0; }
  .qs-spark svg { width: 100%; height: 100%; display: block; }
  .qs[data-variant="3"] .qs-trigger { background: var(--qs-primary-soft); border-color: var(--qs-primary-16); }
  .qs[data-variant="3"] .qs-trigger:hover { background: var(--qs-primary-16); }
  .qs-mini-track {
    width: 54px; height: 5px; flex-shrink: 0; position: relative; overflow: hidden;
    border-radius: var(--border-round); background: var(--qs-track);
  }

  /* ---------- Backdrop (drawer) ---------- */
  .qs-backdrop {
    position: fixed; inset: 0;
    background: rgba(47, 43, 61, 0.5);
    opacity: 0; pointer-events: none;
    transition: opacity .22s ease;
    z-index: 40;
  }
  .qs-backdrop.is-open { opacity: 1; pointer-events: auto; }

  /* ---------- PANEL (dùng chung 3 PA) ---------- */
  .qs-panel {
    position: fixed; z-index: 50;
    width: var(--qs-panel-w);
    max-width: calc(100vw - 2 * var(--padding-4));
    background: var(--misc-paper);
    border-radius: var(--border-radius-md);
    box-shadow: var(--shadow-md), 0 12px 32px rgba(47, 43, 61, 0.16);
    display: none; flex-direction: column; overflow: hidden;
  }
  .qs-panel.is-open { display: flex; }

  .qs-panel[data-mode="popover"] {
    /* cao 720px; nếu viewport thấp hơn thì bám theo viewport — phần thân tự cuộn */
    height: min(var(--qs-panel-h), calc(100vh - 90px));
    max-height: calc(100vh - 90px);
    animation: qsPop .18s cubic-bezier(.2,.8,.3,1);
  }
  @keyframes qsPop {
    from { opacity: 0; transform: translateY(-6px) scale(.985); }
    to   { opacity: 1; transform: none; }
  }

  .qs-panel[data-mode="drawer"] {
    top: 0; right: 0; bottom: 0;
    width: min(var(--qs-panel-w), 100vw); max-width: 100vw;
    height: 100vh; max-height: 100vh;
    border-radius: 0;
    transform: translateX(100%);
    transition: transform .26s cubic-bezier(.2,.8,.3,1);
  }
  .qs-panel[data-mode="drawer"].is-open { transform: translateX(0); }

  /* ---------- Panel header ---------- */
  .qs-head {
    display: flex; align-items: flex-start; gap: var(--gap-3);
    padding: var(--padding-4);
    border-bottom: 1px solid var(--theme-divider);
    flex-shrink: 0;
  }
  /* gap nhỏ (4px) để title — dòng đếm — progress bám sát nhau thành một cụm */
  .qs-head-main { flex: 1 0 0; min-width: 0; display: flex; flex-direction: column; gap: var(--padding-1); }
  .qs-title { margin: 0; font-size: 16px; font-weight: 500; line-height: 22px; color: var(--theme-text-primary); }
  .qs-sub {
    display: flex; align-items: center; gap: var(--gap-2);
    margin: 0;
    font-size: 13px; line-height: 18px; color: var(--theme-text-secondary);
  }
  .qs-sub b { color: var(--color-palette-primary-main); font-variant-numeric: tabular-nums; }
  .qs-head-track {
    position: relative; height: 6px; overflow: hidden;
    border-radius: var(--border-round); background: var(--qs-track);
  }
  .qs-close {
    display: inline-flex; align-items: center; justify-content: center;
    width: 32px; height: 32px; flex-shrink: 0;
    border: 0; background: transparent; cursor: pointer;
    border-radius: var(--border-round); color: var(--theme-text-secondary);
  }
  .qs-close:hover { background: var(--theme-action-hover); color: var(--theme-text-primary); }
  .qs-close:focus-visible { outline: 2px solid var(--color-palette-primary-main); outline-offset: -2px; }
  .qs-close svg { width: 20px; height: 20px; display: block; }

  /* ---------- Panel body ---------- */
  .qs-body {
    flex: 1 1 auto; min-height: 0; overflow-y: auto;
    padding: var(--padding-3) var(--padding-4) var(--padding-4);
    display: flex; flex-direction: column; gap: var(--padding-3);
  }
  .qs-body::-webkit-scrollbar { width: 6px; }
  .qs-body::-webkit-scrollbar-thumb { background: var(--theme-divider); border-radius: var(--border-round); }
  .qs-body { scrollbar-width: thin; scrollbar-color: var(--theme-divider) transparent; }

  /* ---------- Nhóm (accordion) ---------- */
  /* flex-shrink:0 — nếu không, các nhóm bị nén lại và overflow:hidden cắt mất nội dung */
  .qs-group {
    flex: 0 0 auto;
    border: 1px solid var(--theme-divider);
    border-radius: var(--border-radius-md);
    overflow: hidden;
  }

  .qs-g-head {
    display: flex; align-items: center; gap: var(--gap-2);
    width: 100%; padding: var(--padding-3);
    border: 0; background: var(--qs-grey-light);
    cursor: pointer; font: inherit; text-align: left;
    transition: background .15s ease;
  }
  .qs-g-head:hover { background: var(--theme-action-hover); }
  .qs-g-head:focus-visible { outline: 2px solid var(--color-palette-primary-main); outline-offset: -2px; }

  .qs-g-caret { width: 18px; height: 18px; flex-shrink: 0; color: var(--theme-text-secondary); transition: transform .2s ease; }
  .qs-g-caret svg { width: 100%; height: 100%; display: block; }
  .qs-group.is-closed .qs-g-caret { transform: rotate(-90deg); }

  .qs-g-name { flex: 1 0 0; min-width: 0; font-size: 14px; font-weight: 500; line-height: 20px; color: var(--theme-text-primary); }
  .qs-g-count {
    flex-shrink: 0; display: inline-flex; align-items: center; gap: 6px;
    font-size: 12px; line-height: 18px; color: var(--theme-text-secondary);
    font-variant-numeric: tabular-nums;
  }
  .qs-g-count b { color: var(--theme-text-primary); font-weight: 500; }
  .qs-group.is-done .qs-g-count, .qs-group.is-done .qs-g-count b { color: var(--qs-done); }
  .qs-group.is-done .qs-g-head { background: var(--qs-done-soft); }

  .qs-g-body { display: flex; flex-direction: column; }
  .qs-group.is-closed .qs-g-body { display: none; }

  /* ---------- Bước ---------- */
  .qs-step {
    display: flex; align-items: flex-start; gap: var(--gap-3);
    padding: var(--padding-3);
    border-top: 1px solid var(--theme-divider);
    background: var(--misc-paper);
    cursor: pointer; transition: background .15s ease;
  }
  .qs-g-body > .qs-step:first-child { border-top: 0; }
  .qs-step:hover { background: var(--qs-primary-soft); }
  .qs-step:focus-visible { outline: 2px solid var(--color-palette-primary-main); outline-offset: -2px; }

  .qs-mark {
    width: 24px; height: 24px; flex-shrink: 0;
    border-radius: var(--border-round);
    display: inline-flex; align-items: center; justify-content: center;
    font-size: 12px; font-weight: 500;
    background: var(--theme-action-selected);
    color: var(--theme-text-secondary);
    transition: background .2s ease, color .2s ease;
  }
  .qs-mark svg { width: 15px; height: 15px; display: block; }
  .qs-step.is-done .qs-mark { background: var(--qs-done); color: var(--misc-bg-white-text); }

  .qs-s-main { flex: 1 0 0; min-width: 0; display: flex; flex-direction: column; gap: 2px; }
  /* Tên chức năng: một style duy nhất cho mọi trạng thái — không gạch chân, không mũi tên */
  .qs-s-name {
    font-size: 14px; line-height: 20px; font-weight: 500;
    color: var(--theme-text-primary);
    text-decoration: none;
  }
  /* Dòng điều kiện hoàn thành — chỉ render ở bước CHƯA hoàn thành */
  .qs-s-cond { font-size: 12px; line-height: 18px; color: var(--theme-text-disabled); }

  /* Icon điều hướng — chỉ hiện khi hover / focus vào bước */
  .qs-go {
    flex-shrink: 0; align-self: center;
    display: inline-flex; align-items: center; justify-content: center;
    width: 28px; height: 28px;
    border: 0; padding: 0;
    border-radius: var(--border-round);
    background: var(--qs-primary-16);
    color: var(--color-palette-primary-main);
    cursor: pointer;
    opacity: 0;
    transform: translateX(-4px);
    transition: opacity .16s ease, transform .16s ease, background .15s ease;
    pointer-events: none;
  }
  .qs-go svg { width: 16px; height: 16px; display: block; }
  .qs-step:hover .qs-go,
  .qs-step:focus-visible .qs-go,
  .qs-go:focus-visible {
    opacity: 1; transform: none; pointer-events: auto;
  }
  .qs-go:hover { background: var(--color-palette-primary-main); color: var(--misc-bg-white-text); }
  .qs-go:focus-visible { outline: 2px solid var(--color-palette-primary-main); outline-offset: 2px; }
  /* Thiết bị cảm ứng / người dùng giảm chuyển động: luôn hiện icon */
  @media (hover: none) {
    .qs-go { opacity: 1; transform: none; pointer-events: auto; }
  }
  @media (prefers-reduced-motion: reduce) {
    .qs-go { transition: none; }
  }

  /* ---------- Panel footer ---------- */
  .qs-foot {
    display: flex; align-items: center; justify-content: space-between; gap: var(--gap-2);
    padding: var(--padding-3) var(--padding-4);
    border-top: 1px solid var(--theme-divider);
    background: var(--qs-grey-light);
    flex-shrink: 0;
  }
  .qs-foot-note { font-size: 12px; line-height: 18px; color: var(--theme-text-secondary); }
  .qs-foot-note b { color: var(--theme-text-primary); }

/* ================= DIALOG CHỌN MODULE =================
   Figma "EzOne Products" (110:4547) — popover neo dưới nút Ứng dụng.
   Cấu trúc port nguyên từ EzOne_Admin_QuickSetup_v8.html. */
.prod-backdrop{position:fixed;inset:0;background:transparent;z-index:45;display:none}
.prod-backdrop.is-open{display:block}

.prod-pop{
  position:fixed;z-index:55;
  width:650px;max-width:calc(100vw - 16px);
  max-height:min(500px,calc(100vh - 90px));
  background:var(--misc-paper);
  border-radius:var(--border-radius-md);
  box-shadow:var(--shadow-md),0 12px 32px rgba(47,43,61,.16);
  display:none;flex-direction:column;overflow:hidden;
  transform-origin:top right;
}
.prod-pop.is-open{display:flex;animation:prodPop .16s cubic-bezier(.2,.8,.3,1)}
@keyframes prodPop{
  from{opacity:0;transform:translateY(-6px) scale(.985)}
  to{opacity:1;transform:none}
}

.prod-head{
  display:flex;align-items:center;gap:var(--padding-3);
  padding:var(--padding-4);flex-shrink:0;
  border-bottom:1px solid var(--theme-divider);
}
.prod-title{flex:1 0 0;margin:0;font-size:18px;font-weight:500;line-height:28px;color:var(--theme-text-primary)}
.prod-search{position:relative;width:320px;flex-shrink:0}
.prod-search input{
  width:100%;height:30px;padding:0 var(--padding-3) 0 34px;
  font:inherit;font-size:13px;color:var(--theme-text-primary);
  background:var(--misc-paper);
  border:1px solid var(--theme-input-border);border-radius:var(--border-radius-md);outline:none;
}
.prod-search input::placeholder{color:var(--theme-text-disabled)}
.prod-search input:focus{border-color:var(--color-palette-primary-main);box-shadow:0 0 0 3px var(--qs-primary-soft)}
.prod-search svg{position:absolute;left:10px;top:50%;transform:translateY(-50%);width:16px;height:16px;color:var(--theme-text-disabled);pointer-events:none}
.prod-close{
  display:inline-flex;align-items:center;justify-content:center;
  width:32px;height:32px;flex-shrink:0;
  border-radius:var(--border-round);color:var(--theme-text-secondary);
}
.prod-close:hover{background:var(--theme-action-hover);color:var(--theme-text-primary)}
.prod-close svg{width:20px;height:20px;display:block}

.prod-body{
  flex:1 1 auto;min-height:0;overflow-y:auto;
  padding:var(--padding-3);display:flex;flex-direction:column;gap:var(--padding-3);
}
.prod-body::-webkit-scrollbar{width:8px}
.prod-body::-webkit-scrollbar-thumb{background:var(--theme-input-border);border-radius:var(--border-round)}

.prod-group{display:flex;flex-direction:column}
.prod-group + .prod-group{border-top:1px solid var(--theme-divider);padding-top:var(--padding-3)}
.prod-g-head{
  display:flex;align-items:center;gap:var(--padding-1);
  width:100%;padding:0 0 var(--padding-2);
  font:inherit;font-size:15px;font-weight:500;color:var(--theme-text-primary);
  text-align:left;
}
.prod-caret{width:20px;height:20px;flex-shrink:0;color:var(--theme-text-secondary);transition:transform .18s ease}
.prod-caret svg{width:100%;height:100%;display:block}
.prod-group.is-closed .prod-caret{transform:rotate(-90deg)}
.prod-group.is-closed .prod-grid{display:none}

.prod-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:var(--padding-2)}
.prod-item{
  display:flex;flex-direction:column;align-items:center;gap:6px;
  padding:var(--padding-3) var(--padding-2);
  border-radius:var(--border-radius-md);
  background:transparent;font:inherit;text-align:center;
  transition:background .15s ease;
}
.prod-item:hover{background:var(--qs-primary-soft)}
.prod-item:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:-2px}
.prod-item.is-current{background:var(--qs-primary-soft)}
.prod-ico{
  width:48px;height:48px;flex-shrink:0;border-radius:var(--border-round);
  display:inline-flex;align-items:center;justify-content:center;
  color:var(--misc-bg-white-text);
}
.prod-ico svg{width:26px;height:26px;display:block}
/* Logo module thật đã có nền tròn riêng → không tô nền, hiển thị nguyên khổ */
.prod-ico.is-logo{background:none}
.prod-ico.is-logo svg{width:48px;height:48px}
.prod-name{font-size:13px;font-weight:500;line-height:20px;color:var(--theme-text-primary)}
.prod-desc{font-size:12px;line-height:18px;color:var(--theme-text-disabled)}
.prod-empty{padding:var(--padding-4);text-align:center;font-size:13px;color:var(--theme-text-disabled)}

/* ================= RESPONSIVE ================= */
@media (max-width:768px){
  :root{--sidebar-width:240px}
  .select{width:260px}
}
@media (max-width:600px){
  .select{width:180px}
  .search-placeholder{display:none}
  .actions{gap:var(--padding-2)}
  .prod-head{flex-wrap:wrap}
  .prod-search{width:100%;order:3}
  .prod-grid{grid-template-columns:repeat(2,1fr)}
}
</style>
<style id="tl-style">
/* ================================================================
   Sơ đồ phòng — Timeline  ·  token trích từ Figma 32146:44587
   Tien to --tl-* cho token moi; ke thua cac token --theme, --color-palette, --misc cua shell v7
   ================================================================ */
:root{
  /* --- geometry: đo từ DOM production (b-grid-row 31px, header 60px, event 26px) --- */
  --tl-room-col: 180px;
  --tl-cell-w: 48px;
  --tl-row-h: 31px;
  --tl-head-h: 60px;
  --tl-bar-h: 26px;

  /* --- surface --- */
  --tl-divider: #EBEDF2;                /* border grid thật (.b-grid-cell) */
  --tl-outline: rgba(47,43,61,.24);
  /* Cuối tuần dùng hệ Error theo token: nền error-8, chữ error-500.
     Phải là màu ĐẶC (blend sẵn trên nền trắng) vì ô header/cột phòng là sticky. */
  --tl-weekend-bg: #FFF1F1;             /* = --color-palette-error-opacity-lighter #FF4C5114 trên trắng */
  --tl-weekend-hover: #FFE2E3;          /* = --color-palette-error-opacity-light   #FF4C5129 trên trắng */
  --tl-weekend-fg: #FF4C51;             /* = --color-palette-error-main */
  --tl-cell-hover: #F1F1F3;             /* #80839024 */
  --tl-sum-bg: #F1F1F3;                 /* SUM_CAPACITY  #8083901a */
  --tl-grp-bg: #FAFAFB;                 /* SUM_CAPACITY_* #8083900a */
  --tl-hover-bg: rgba(47,43,61,.04);

  /* --- màu chữ legend (.status-style-* production) --- */
  --tl-st-booked:   #186FF4;
  --tl-st-unassign: #D46B08;
  --tl-st-inhouse:  #DA4343;
  --tl-st-checkout: #9C27B0;
  --tl-st-repair:   rgba(47,43,61,.7);
  --tl-st-dirty:    #B56613;
  --tl-st-light:    #BFA210;
  --tl-st-available:#309F61;

  /* --- nền booking bar (theme "room-*" production) --- */
  --tl-bar-booked:   #4983DC;
  --tl-bar-unassign: #D46B08;
  --tl-bar-inhouse:  #D96262;
  --tl-bar-checkout: #EFBFF0;
  --tl-bar-repair:   #808390;

  /* --- thanh toán (theme "room-payment-*") --- */
  --tl-pay-paid:    #24B364;
  --tl-pay-partial: #FF9F43;
  --tl-pay-none:    #DA4343;

  /* --- typography (đo từ DOM production) --- */
  --tl-fs-room: 14px;      --tl-lh-room: 22px;    /* tên phòng */
  --tl-fs-head: 14px;                             /* .weekday-name / .day-num  */
  --tl-fs-bar: 14px;                              /* nhãn booking bar (tên khách) */
  --tl-fs-occ: 14px;                              /* ô công suất phòng          */
  --tl-c-strong: #2F2B3DE6;                       /* high-emphasis .9          */
  --tl-c-muted:  #2F2B3DB3;                       /* medium-emphasis .7        */
  --tl-c-occ:    rgba(47,43,61,.78);

  --tl-primary-o-light: rgba(24,111,244,.08);
  --tl-primary-o-main:  rgba(24,111,244,.16);
  --tl-radius-sm: var(--border-radius-sm);   /* 4px  */
  --tl-radius-md: var(--border-radius-md);   /* 6px  */
  --tl-radius-bar: 20px;
  --tl-radius-group: 8px;                  /* radius.md — buttongroup.md */
  --tl-btn-h-sm: 30px;                     /* Size Small — buttongroup.md */
  --tl-dur-fast: 100ms;                    /* motion.duration.fast */
  --tl-snackbar: #2F2B3D;                  /* --misc-snackbar — nền Tooltip */
  --tl-ease: cubic-bezier(.4,0,.2,1);      /* motion.easing.standard */
  --tl-radius-round: var(--border-round);    /* 500px */
  --tl-z-bar: 5;
  --tl-z-rowhead: 20;
  --tl-z-colhead: 30;
  --tl-z-corner: 40;
  --tl-z-overlay: 1200;
}

/* card chứa timeline: chuyển sang flex-column, tự quản scroll bên trong */
#contentCard.tl-host{display:flex;flex-direction:column;overflow:hidden}
.tl{display:flex;flex-direction:column;min-height:0;flex:1;font-family:'Roboto',-apple-system,BlinkMacSystemFont,'Segoe UI',Helvetica,Arial,sans-serif;color:var(--theme-text-primary)}
.tl *,.tl *::before,.tl *::after{box-sizing:border-box}

/* ================= HEADER ================= */
.tl-headwrap{display:contents}
.tl-head{
  display:flex;flex-direction:column;gap:20px;justify-content:center;
  padding:16px;background:var(--misc-paper);
  border-bottom:1px solid var(--tl-divider);flex:0 0 auto;
  font-size:13px;
}
.tl-toolbar{display:flex;align-items:center;justify-content:space-between;gap:16px;min-height:30px}
.tl-toolbar-l{display:flex;align-items:center;gap:16px;flex:1 1 auto;min-width:0;flex-wrap:wrap}
.tl-toolbar-r{display:flex;align-items:center;gap:12px;flex:0 0 auto}

/* Date navigator — cùng khung Button Group Outline/Small */
.tl-btngroup{display:inline-flex;align-items:center;border:0;background:none;overflow:visible}
.tl-btngroup>*+*{margin-left:-1px}
.tl-btngroup>*:first-child{border-top-left-radius:var(--tl-radius-group);border-bottom-left-radius:var(--tl-radius-group)}
.tl-btngroup>*:last-child{border-top-right-radius:var(--tl-radius-group);border-bottom-right-radius:var(--tl-radius-group)}
.tl-navbtn{
  height:var(--tl-btn-h-sm);position:relative;border-radius:0;
  display:inline-flex;align-items:center;justify-content:center;gap:6px;
  padding:0 14px;background:var(--misc-paper);border:1px solid var(--tl-divider);cursor:pointer;
  font-weight:500;font-size:13px;line-height:18px;color:var(--theme-text-secondary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease),color var(--tl-dur-fast) var(--tl-ease);
}
.tl-navbtn:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px;z-index:3}
.tl-navbtn:hover:not(:disabled){background:var(--tl-hover-bg)}
.tl-navbtn:disabled{color:var(--theme-text-disabled);cursor:not-allowed}
.tl-navbtn svg{width:14px;height:14px;flex:0 0 auto}
.tl-datefield{
  position:relative;display:inline-flex;align-items:center;justify-content:center;gap:6px;
  height:var(--tl-btn-h-sm);min-width:120px;padding:0 14px;border-radius:0;
  border:1px solid var(--tl-divider);background:var(--misc-paper);cursor:pointer;
  font-weight:500;font-size:13px;line-height:18px;color:var(--theme-text-secondary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease);
}
.tl-datefield:hover{background:var(--tl-hover-bg)}
.tl-datefield:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px;z-index:3}
.tl-datefield[aria-expanded="true"]{background:var(--tl-primary-o-light);color:var(--color-palette-primary-main);border-color:var(--color-palette-primary-main);z-index:2}
.tl-datefield svg{width:16px;height:16px;flex:0 0 auto}

/* ===== Date picker — Vuexy AppDateTimePicker (flatpickr) ===== */
.tl-dp{
  position:fixed;z-index:1206;display:none;width:296px;
  background:var(--misc-paper);border-radius:var(--tl-radius-md);
  box-shadow:0 4px 18px rgba(19,17,32,.22);
  padding:8px;color:var(--theme-text-primary);
  font-weight:400;font-size:13px;line-height:20px;
}
.tl-dp.is-open{display:block}
.tl-dp-head{display:flex;align-items:center;gap:4px;padding:2px 0 8px}
.tl-dp-nav{
  width:30px;height:30px;flex:0 0 auto;display:inline-flex;align-items:center;justify-content:center;
  border:0;border-radius:var(--tl-radius-round);background:none;cursor:pointer;color:var(--theme-text-secondary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease);
}
.tl-dp-nav:hover{background:var(--theme-action-hover)}
.tl-dp-nav:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:-2px}
.tl-dp-nav svg{width:18px;height:18px}
.tl-dp-title{flex:1 1 auto;display:flex;align-items:center;justify-content:center;gap:6px;min-width:0}
.tl-dp-title select,.tl-dp-title input{
  border:1px solid transparent;border-radius:var(--tl-radius-sm);background:none;cursor:pointer;
  font-family:inherit;font-weight:500;font-size:15px;line-height:24px;color:var(--theme-text-primary);
  padding:2px 4px;
}
.tl-dp-title select:hover,.tl-dp-title input:hover{background:var(--theme-action-hover)}
.tl-dp-title select:focus-visible,.tl-dp-title input:focus-visible{outline:none;border-color:var(--color-palette-primary-main)}
.tl-dp-title input{width:64px;text-align:center;cursor:text}
.tl-dp-wd,.tl-dp-days{display:grid;grid-template-columns:repeat(7,1fr)}
.tl-dp-wd span{
  height:28px;display:inline-flex;align-items:center;justify-content:center;
  font-weight:500;font-size:12px;line-height:16px;color:var(--theme-text-secondary);
}
.tl-dp-wd span.we{color:var(--tl-weekend-fg)}
.tl-dp-days button{
  height:38px;display:inline-flex;align-items:center;justify-content:center;
  border:1px solid transparent;border-radius:var(--tl-radius-round);background:none;cursor:pointer;
  font-family:inherit;font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-primary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease),color var(--tl-dur-fast) var(--tl-ease);
}
.tl-dp-days button:hover{background:var(--theme-action-hover)}
.tl-dp-days button.out{color:var(--theme-text-disabled)}
.tl-dp-days button.we{color:var(--tl-weekend-fg)}
.tl-dp-days button.out.we{color:rgba(255,76,81,.4)}
.tl-dp-days button.today{border-color:var(--color-palette-primary-main);color:var(--color-palette-primary-main);font-weight:500}
.tl-dp-days button.sel{background:var(--color-palette-primary-main);border-color:var(--color-palette-primary-main);color:#fff;font-weight:500}
.tl-dp-days button.sel:hover{background:var(--color-palette-primary-dark)}
.tl-dp-days button:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:-3px}
.tl-dp-foot{display:flex;align-items:center;justify-content:space-between;gap:8px;padding-top:8px;margin-top:4px;border-top:1px solid var(--tl-divider)}
.tl-dp-foot button{
  height:30px;padding:0 12px;border:0;border-radius:var(--tl-radius-sm);background:none;cursor:pointer;
  font-family:inherit;font-weight:500;font-size:13px;line-height:18px;color:var(--color-palette-primary-main);
}
.tl-dp-foot button:hover{background:var(--tl-primary-o-light)}
.tl-dp-foot button.ghost{color:var(--theme-text-secondary)}
.tl-dp-foot button.ghost:hover{background:var(--theme-action-hover)}
.tl-datefield input::-webkit-calendar-picker-indicator{opacity:0;position:absolute;inset:0;width:100%;height:100%;cursor:pointer}

/* nút outline độc lập (Hôm nay) */
.tl-btn-outline{
  display:inline-flex;align-items:center;justify-content:center;
  padding:8px 16px;border:1px solid var(--tl-divider);border-radius:var(--tl-radius-md);
  background:none;cursor:pointer;font-weight:500;font-size:12px;line-height:14px;color:var(--theme-text-secondary);
  transition:background-color .2s ease,color .2s ease,border-color .2s ease;
}
.tl-btn-outline:hover:not(:disabled){background:var(--tl-hover-bg)}
.tl-btn-outline:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px}
/* đang ở đúng hôm nay → Active dạng Outline primary (không disable) */
.tl-btn-outline[aria-pressed="true"]{
  color:var(--color-palette-primary-main);
  border-color:var(--color-palette-primary-main);
  background:var(--tl-primary-o-light);
}
.tl-btn-outline[aria-pressed="true"]:hover{background:rgba(24,111,244,.14)}
.tl-btn-outline:disabled{color:var(--theme-text-disabled);border-color:var(--theme-action-hover);cursor:not-allowed}

/* ===== Button Group — Outline · Size Small (buttongroup.md · Figma 19188:26216) =====
   · gap 0, border dùng chung tại điểm tiếp giáp (margin-left:-1px) → không bị viền 2px
   · radius.md 8px CHỈ ở góc ngoài của item đầu / item cuối
   · KHÔNG overflow:hidden trên container → focus ring không bị clip                     */
.tl-seg{display:inline-flex;align-items:center;border:0;background:none;overflow:visible}
.tl-seg button{
  height:var(--tl-btn-h-sm);
  display:inline-flex;align-items:center;justify-content:center;
  padding:0 14px;border:1px solid var(--tl-divider);background:var(--misc-paper);
  cursor:pointer;white-space:nowrap;position:relative;border-radius:0;
  font-weight:500;font-size:13px;line-height:18px;color:var(--theme-text-secondary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease),
             color var(--tl-dur-fast) var(--tl-ease),
             border-color var(--tl-dur-fast) var(--tl-ease);
}
.tl-seg button+button{margin-left:-1px}
.tl-seg button:first-child{border-top-left-radius:var(--tl-radius-group);border-bottom-left-radius:var(--tl-radius-group)}
.tl-seg button:last-child{border-top-right-radius:var(--tl-radius-group);border-bottom-right-radius:var(--tl-radius-group)}
.tl-seg button:hover:not(:disabled){background:var(--tl-hover-bg);z-index:1}
.tl-seg button:active:not(:disabled){background:rgba(24,111,244,.14);z-index:2}
.tl-seg button[aria-pressed="true"]{
  background:var(--tl-primary-o-light);
  color:var(--color-palette-primary-main);
  border-color:var(--color-palette-primary-main);
  z-index:2;
}
.tl-seg button:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px;z-index:3}
.tl-seg button:disabled{background:var(--misc-avatar-bg);color:var(--theme-text-disabled);cursor:not-allowed}

/* checkbox Xem ngày */
.tl-check{display:inline-flex;align-items:center;gap:4px;height:30px;cursor:pointer;user-select:none}
.tl-check .box,.tl-check span.lb{pointer-events:none}
.tl-check input{position:absolute;opacity:0;width:0;height:0}
.tl-check .box{
  width:18px;height:18px;border:2px solid var(--theme-text-disabled);
  border-radius:var(--tl-radius-sm);display:inline-flex;align-items:center;justify-content:center;
  margin:6px;transition:background-color .2s ease,border-color .2s ease;
}
.tl-check .box svg{width:12px;height:12px;color:#fff;opacity:0}
.tl-check input:checked+.box{background:var(--color-palette-primary-main);border-color:var(--color-palette-primary-main)}
.tl-check input:checked+.box svg{opacity:1}
.tl-check input:focus-visible+.box{outline:2px solid var(--color-palette-primary-main);outline-offset:2px}
.tl-check span.lb{font-weight:400;font-size:15px;line-height:24px;color:var(--theme-text-primary)}

/* nút primary + icon button */
.tl-btn-primary{
  display:inline-flex;align-items:center;justify-content:center;gap:6px;
  padding:6px 14px;border:0;border-radius:var(--tl-radius-sm);
  background:var(--color-palette-primary-main);color:#fff;cursor:pointer;
  font-weight:500;font-size:13px;line-height:18px;transition:background-color .2s ease;
}
.tl-btn-primary:hover{background:var(--color-palette-primary-dark)}
.tl-btn-primary svg{width:16px;height:16px}
.tl-iconbtn{
  display:inline-flex;align-items:center;justify-content:center;padding:5px;
  border:1px solid var(--tl-outline);border-radius:var(--tl-radius-sm);
  background:none;cursor:pointer;color:var(--theme-text-secondary);
  transition:background-color .2s ease,color .2s ease;
}
.tl-iconbtn:hover{background:var(--tl-hover-bg);color:var(--color-palette-primary-main)}
.tl-iconbtn svg{width:20px;height:20px}

/* filter bar / legend */
.tl-filterbar{display:flex;align-items:center;justify-content:space-between;gap:16px}
.tl-legend{display:flex;align-items:flex-start;gap:4px;flex:1 1 auto;min-width:0;flex-wrap:wrap}
.tl-legend button{
  display:inline-flex;align-items:center;gap:6px;padding:6px 14px;
  border:0;border-radius:var(--tl-radius-sm);background:none;cursor:pointer;
  font-weight:500;font-size:13px;line-height:18px;transition:background-color .2s ease;
}
.tl-legend button{border-radius:var(--tl-radius-md)}
/* hover: chỉ đổi NỀN sang xám --theme-action-hover, giữ nguyên màu chữ trạng thái */
.tl-legend button:hover{background:var(--theme-action-hover)}
.tl-legend button[aria-pressed="true"]{background:var(--tl-legend-active,var(--tl-primary-o-light))}
.tl-legend button[aria-pressed="true"] .lbl{font-weight:600}
.tl-legend button svg{width:16px;height:16px}
.tl-legend .cnt{opacity:.85}
.tl-legend .dot{width:10px;height:10px;border-radius:var(--tl-radius-round);flex:0 0 auto}
.tl-legend button[aria-pressed="true"]{box-shadow:inset 0 0 0 1px currentColor}
.tl-filter-note{
  display:none;align-items:center;gap:6px;padding:0 8px 0 10px;height:26px;
  border-radius:var(--tl-radius-round);background:var(--tl-primary-o-light);
  color:var(--color-palette-primary-main);font-weight:500;font-size:12px;line-height:26px;white-space:nowrap;
}
.tl-filter-note.is-on{display:inline-flex}
.tl-filter-note button{
  display:inline-flex;align-items:center;justify-content:center;
  width:18px;height:18px;padding:0;border:0;border-radius:var(--tl-radius-round);
  background:none;cursor:pointer;color:inherit;
}
.tl-filter-note button:hover{background:rgba(24,111,244,.16)}
.tl-filter-note svg{width:12px;height:12px}

/* switcher loại sơ đồ */
.tl-viewswitch{display:inline-flex;align-items:center;border:0;background:none;overflow:visible;flex:0 0 auto}
.tl-viewswitch button{
  height:var(--tl-btn-h-sm);position:relative;border-radius:0;
  display:inline-flex;align-items:center;justify-content:center;
  padding:0 16px;border:1px solid var(--tl-divider);background:var(--misc-paper);
  cursor:pointer;color:var(--theme-text-secondary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease),color var(--tl-dur-fast) var(--tl-ease);
}
.tl-viewswitch button+button{margin-left:-1px}
.tl-viewswitch button:first-child{border-top-left-radius:var(--tl-radius-group);border-bottom-left-radius:var(--tl-radius-group)}
.tl-viewswitch button:last-child{border-top-right-radius:var(--tl-radius-group);border-bottom-right-radius:var(--tl-radius-group)}
.tl-viewswitch button:hover:not(:disabled){background:var(--tl-hover-bg);z-index:1}
.tl-viewswitch button[aria-pressed="true"]{background:var(--tl-primary-o-main);color:var(--color-palette-primary-main);border-color:var(--color-palette-primary-main);z-index:2}
.tl-viewswitch button:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px;z-index:3}
.tl-viewswitch svg{width:24px;height:24px}

/* ================= GRID ================= */
.tl-scroll{flex:1 1 auto;min-height:0;overflow:auto;position:relative;background:var(--misc-paper);
  scrollbar-width:thin;scrollbar-color:#D5D6DB transparent}
.tl-scroll::-webkit-scrollbar{width:10px;height:10px}
.tl-scroll::-webkit-scrollbar-track{background:transparent}
.tl-scroll::-webkit-scrollbar-thumb{background:#D5D6DB;border-radius:500px;border:3px solid var(--misc-paper)}
.tl-scroll::-webkit-scrollbar-thumb:hover{background:#B9BAC1}
.tl-scroll::-webkit-scrollbar-corner{background:transparent}
.tl-table{
  border-collapse:separate;   /* BẮT BUỘC cho sticky column trên Chrome */
  border-spacing:0;
  table-layout:fixed;
  width:max-content;
  font-weight:400;font-size:13px;line-height:20px;
}
.tl-table th,.tl-table td{
  height:var(--tl-row-h);padding:0;margin:0;
  border-right:1px solid var(--tl-divider);
  border-bottom:1px solid var(--tl-divider);
  background:var(--misc-paper);
  font-weight:inherit;text-align:center;vertical-align:middle;
}
/* cột tên phòng — sticky trái */
.tl-table .rc{
  position:sticky;left:0;z-index:var(--tl-z-rowhead);
  width:var(--tl-room-col);min-width:var(--tl-room-col);max-width:var(--tl-room-col);
  text-align:left;padding:0 8px;
}
/* header ngày — sticky trên */
.tl-table thead th{position:sticky;top:0;z-index:var(--tl-z-colhead);background:var(--misc-paper)}
/* Cuối tuần: nền đỏ nhạt CHỈ ở header lịch, ô dữ liệu bên dưới giữ nền trắng */
.tl-table thead th.tl-weekend{background:var(--tl-weekend-bg)!important}
.tl-table thead th.tl-daycell.tl-weekend .dw,
.tl-table thead th.tl-daycell.tl-weekend .dn{color:var(--tl-weekend-fg)}
.tl-table thead th.rc{left:0;top:0;z-index:var(--tl-z-corner)}
.tl-table thead tr:nth-child(2) th{top:var(--tl-row-h)}

.tl-daycell{position:relative;padding:0;transition:background-color .2s ease}
.tl-daycell .sq{display:flex;flex-direction:column;align-items:center;justify-content:flex-start;
  width:100%;height:100%;gap:0;padding:6px 0 0}
.tl-daycell .dw{display:block;font-weight:400;font-size:var(--tl-fs-head);line-height:normal;color:var(--tl-c-muted);transform:translateY(2px)}
.tl-daycell .dn{display:block;font-weight:500;font-size:var(--tl-fs-head);line-height:normal;color:var(--tl-c-strong);transform:translateY(2px)}
.tl-daycell.is-today .dw,
.tl-daycell.is-today .dn{color:var(--color-palette-primary-main)}
.tl-table thead th.tl-daycell:hover{background:var(--tl-cell-hover)}
.tl-table thead th.tl-daycell.tl-weekend:hover{background:var(--tl-weekend-hover)!important}
/* chế độ Ngày: header xếp ngang như .is-day-view .calendar-square */
.tl-table thead th.tl-daycell.is-hour .sq{flex-direction:row;justify-content:center;align-items:center;gap:8px;padding:6px 12px}
.tl-holiday{box-shadow:inset 0 4px 0 0 #FF9F43}

/* badge phòng chưa gán */
.tl-unassign{
  display:inline-flex;align-items:center;justify-content:center;
  min-width:18px;height:18px;padding:2px 4px;margin-top:2px;border:0;cursor:pointer;
  border-radius:16px;background:#FF9F43;
  color:#fff;font-weight:400;font-size:12px;line-height:14px;
}
.tl-unassign:hover{background:#E68F3C}
.tl-unassign:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:2px}

/* hàng Tổng + hàng loại phòng */
.tl-row-sum>th,.tl-row-sum>td{background:var(--tl-sum-bg)!important}
.tl-row-grp>th,.tl-row-grp>td{background:var(--tl-grp-bg)!important}
.tl-row-sum>td,.tl-row-grp>td{font-size:var(--tl-fs-occ);line-height:1.3;color:var(--tl-c-occ)}
.tl-row-sum>td,.tl-row-sum>.rc{font-weight:500}
.tl-row-grp>td{font-weight:400}
.tl-row-grp .rc{cursor:pointer;display:flex;align-items:center;gap:6px;padding:0 10px}
.tl-row-grp .tl-roomcell{font-weight:500;padding:0 10px;gap:6px}
.tl-row-sum>.rc{background:var(--tl-sum-bg)!important}
.tl-row-grp>.rc{background:var(--tl-grp-bg)!important}
.tl-row-room>.rc{background:var(--misc-paper)}
.tl-grp-caret{width:22px;height:22px;min-width:22px;flex:0 0 auto;opacity:.7;transition:transform .2s ease}
.tl-row-grp.is-collapsed .tl-grp-caret{transform:rotate(-90deg)}
.tl-grp-name{flex:0 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-weight:500;font-size:var(--tl-fs-room);line-height:var(--tl-lh-room);color:var(--tl-c-strong)}

/* ô tên phòng */
/* .timeline-resource-label: 15px/22px · parent 500 · child 400 · color #2f2b3de6 */
.tl-roomcell{display:flex;align-items:center;gap:4px;width:100%;height:100%;border:0;background:none;
  padding:0 10px 0 12px;cursor:pointer;text-align:left;
  font-weight:400;font-size:var(--tl-fs-room);line-height:var(--tl-lh-room);color:var(--tl-c-strong)}
.tl-roomcell:hover{background:var(--tl-hover-bg)}
.tl-roomcell .rn{flex:0 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.tl-roomcell .flags{display:inline-flex;align-items:center;gap:2px;flex:0 0 auto;margin-left:auto;padding-left:4px}
.tl-roomcell .flags svg{width:16px;height:16px}
.tl-flag-light{color:var(--tl-st-light)}
.tl-flag-dirty{color:var(--tl-st-dirty)}

/* ô ngày trong body: neo cho booking bar */
.tl-table tbody td.dc{position:relative;overflow:visible}
/* td có bar phải nổi lên trên các td kế tiếp, nếu không bar bị td sau chặn hit-test */
.tl-table tbody td.dc.has-bar{z-index:6}
.tl-table tbody td.dc:hover{background:var(--tl-hover-bg)}

/* ================= BOOKING BAR ================= */
.tl-bar{
  /* .b-sch-event production: h26 · radius 20 · padding 0 · nhãn 11px/500 */
  position:absolute;top:2px;height:var(--tl-bar-h);
  display:flex;align-items:center;gap:0;
  padding:0 0 0 2px;border:0;cursor:pointer;
  border-radius:var(--tl-radius-bar);
  z-index:var(--tl-z-bar);overflow:visible;
  font-weight:500;font-size:var(--tl-fs-bar);line-height:1;color:#fff;text-align:left;
  transition:filter .2s ease,box-shadow .2s ease;
}
.tl-bar>.body{overflow:hidden;border-radius:inherit}
.tl-bar:hover{filter:brightness(1.06);box-shadow:0 2px 6px rgba(47,43,61,.24)}
.tl-bar:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px}
.tl-bar.clip-l{border-top-left-radius:0;border-bottom-left-radius:0}
.tl-bar.clip-r{border-top-right-radius:0;border-bottom-right-radius:0}
.tl-bar .body{display:flex;align-items:center;gap:4px;flex:1 1 0;min-width:0;padding-right:8px}
.tl-bar .label{flex:1 1 0;min-width:0;margin-left:4px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.tl-bar .grp{
  width:22px;height:22px;flex:0 0 auto;margin-right:-8px;
  border-radius:var(--tl-radius-round);background:rgba(0,0,0,.5);
  display:inline-flex;align-items:center;justify-content:center;
}
.tl-bar .grp svg{width:15px;height:15px}
.tl-bar .ava{width:22px;height:22px;flex:0 0 auto;border-radius:500px;overflow:hidden;background:#F2F2F2;
  display:inline-flex;align-items:center;justify-content:center}
.tl-bar .ava img,.tl-bar .ava svg{width:100%;height:100%;display:block}
.tl-bar .pay{position:absolute;right:2px;top:50%;transform:translateY(-50%);
  width:7px;height:7px;border:1px solid #fff;border-radius:500px;box-sizing:content-box}
.tl-bar[data-st="booked"]{background:var(--tl-bar-booked)}
.tl-bar[data-st="unassigned"]{background:var(--tl-bar-unassign)}
.tl-bar[data-st="inhouse"]{background:var(--tl-bar-inhouse)}
.tl-bar[data-st="checkout"]{background:var(--tl-bar-checkout);color:rgba(47,43,61,.9)}
.tl-bar[data-st="repair"]{background:var(--tl-bar-repair)}
.tl-bar.is-narrow>.body{display:none}
.tl-bar.is-narrow .pay{right:1px}
.tl-bar.is-drag{opacity:.5;animation:none}
/* Vào một ngày/khoảng mới: booking hiện dần theo thứ tự dòng từ trên xuống, ease-in */
@keyframes tl-bar-in{from{opacity:0;transform:translateY(-6px)}to{opacity:1;transform:none}}
.tl-anim .tl-bar{animation:tl-bar-in .26s ease-in both;animation-delay:var(--tl-d,0ms)}
.tl-bar-ghost{position:absolute;top:1px;height:var(--tl-bar-h);border-radius:var(--tl-radius-bar);border:2px dashed var(--color-palette-primary-main);background:var(--tl-primary-o-light);z-index:6;pointer-events:none}

/* đường giờ hiện tại */
.tl-now{position:absolute;top:0;width:0;border-left:1.5px dashed #FF4C51;z-index:8;pointer-events:none;
  transition:left .6s linear}
@media (prefers-reduced-motion:reduce){.tl-now,.tl-now-chip{transition:none}}
.tl-now-chip{
  position:absolute;transform:translateX(-50%);z-index:21;pointer-events:none;
  min-width:24px;padding:0 4px;border-radius:var(--tl-radius-sm);
  background:#FF4C51;color:#fff;
  font-weight:500;font-size:13px;line-height:16px;text-align:center;transition:left .6s linear;
}
/* chip ngày lễ */
.tl-holiday-chip{
  position:fixed;z-index:1206;pointer-events:none;transform:translate(-50%,-100%);
  box-shadow:0 4px 8px #00000026;
  padding:6px 12px;border-radius:var(--tl-radius-sm);
  background:#2F2B3D;color:#fff;font-weight:400;font-size:13px;line-height:20px;white-space:nowrap;
}

/* ================= EMPTY STATE ================= */
.tl-empty{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;color:var(--theme-text-secondary);padding:48px}
.tl-empty svg{width:48px;height:48px;opacity:.4}
.tl-empty p{margin:0;font-weight:400;font-size:15px;line-height:24px}

/* ================= POPUP XEM NHANH BOOKING (Figma 49851:170574 · 49820:169614) ================= */
.tl-pop{
  position:fixed;z-index:var(--tl-z-overlay);display:none;
  background:var(--misc-paper);border:1px solid rgba(47,43,61,.12);
  border-radius:var(--tl-radius-md);box-shadow:0 4px 18px rgba(19,17,32,.22);
  overflow:hidden;color:var(--theme-text-primary);
  font-weight:400;font-size:13px;line-height:20px;
}
.tl-pop.is-open{display:flex;flex-direction:column}
.tl-pop[data-kind="single"],.tl-pop[data-kind="repair"]{width:636px;max-width:calc(100vw - 32px)}
.tl-pop[data-kind="group"]{width:936px;max-width:calc(100vw - 32px)}

/* --- Header --- */
.tl-pop-head{
  display:flex;align-items:center;gap:12px;padding:12px;
  border-bottom:1px solid rgba(47,43,61,.12);flex:0 0 auto;
}
.tl-pop-head .l{display:flex;align-items:center;gap:10px;flex:1 1 0;min-width:0}
.tl-pop-title{
  display:flex;align-items:center;gap:8px;min-width:0;
  font-weight:500;font-size:15px;line-height:24px;color:#2F2B3D;
}
.tl-pop-title>span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
/* mọi SVG trong popup phải có kích thước cố định, nếu không flex sẽ kéo giãn nó ra */
.tl-pop svg{flex:0 0 auto}
.tl-pop-title>svg{width:20px;height:20px}
.tl-pop-code{
  display:inline-flex;align-items:center;gap:4px;border:0;background:none;padding:0;cursor:pointer;
  font-weight:400;font-size:13px;line-height:20px;color:#186FF4;
}
.tl-pop-code svg{width:20px;height:20px;flex:0 0 auto}
.tl-pop-chip{
  display:inline-flex;align-items:center;justify-content:center;min-width:24px;padding:0 6px;
  border-radius:var(--tl-radius-sm);
  font-weight:500;font-size:13px;line-height:20px;white-space:nowrap;
}
.tl-pop-chip.ok{background:rgba(40,199,111,.16);color:#28C76F}
.tl-pop-chip.grey{background:rgba(128,131,144,.16);color:var(--theme-text-secondary)}
.tl-pop-chip.green{background:rgba(40,199,111,.16);color:#28C76F}
.tl-pop-x{
  display:inline-flex;align-items:center;justify-content:center;padding:5px;flex:0 0 auto;
  border:0;background:none;border-radius:var(--tl-radius-round);cursor:pointer;color:var(--theme-text-secondary);
}
.tl-pop-x:hover{background:var(--tl-hover-bg)}
.tl-pop-x svg{width:20px;height:20px}

/* --- Body --- */
.tl-pop-body{display:flex;background:var(--misc-body-bg);padding:4px;gap:4px;flex:1 1 auto;min-height:0;overflow:auto}
.tl-pop-card{
  background:var(--misc-paper);border-radius:var(--tl-radius-md);
  padding:12px 16px;display:flex;flex-direction:column;gap:16px;
  box-shadow:0 1px 3px rgba(47,43,61,.1);flex:1 1 0;min-width:0;
}
.tl-pop-card.side{flex:0 0 300px;gap:12px}
.tl-pop-sect{font-weight:500;font-size:15px;line-height:24px;color:#2F2B3D;margin:0}

/* hàng "Phòng" + nhóm nút hành động */
.tl-pop-room{display:flex;align-items:center;gap:12px}
.tl-pop-room .ico{width:24px;height:24px;flex:0 0 auto;color:#186FF4}
.tl-pop-room .meta{display:flex;flex-direction:column;gap:4px;flex:1 1 0;min-width:0}
.tl-pop-roomname{display:flex;align-items:center;gap:4px;min-width:0;
  font-weight:500;font-size:15px;line-height:24px;color:#2F2B3D}
.tl-pop-roomname>span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.tl-pop-roomname svg{width:20px;height:20px;flex:0 0 auto;color:#FF4C51}
.tl-pop-dates{display:flex;align-items:center;gap:4px;flex-wrap:wrap;
  font-weight:400;font-size:12px;line-height:16px;color:var(--theme-text-primary)}
.tl-pop-dates .sep,.tl-pop-dates .n{color:var(--theme-text-secondary)}
.tl-pop-acts{display:flex;align-items:center;gap:8px;flex:0 0 auto}
.tl-pop-btn{
  height:30px;display:inline-flex;align-items:center;justify-content:center;gap:8px;
  padding:0 14px;border:0;border-radius:var(--tl-radius-sm);cursor:pointer;color:#fff;
  font-weight:400;font-size:13px;line-height:18px;white-space:nowrap;
  transition:filter var(--tl-dur-fast) var(--tl-ease);
}
.tl-pop-btn:hover{filter:brightness(.93)}
.tl-pop-btn svg{width:20px;height:20px}
.tl-pop-btn.checkin{background:#FF4C51}      /* room-checkin action */
.tl-pop-btn.checkout{background:#D761F2}     /* checkout-action */
.tl-pop-btn.primary{background:#186FF4}
.tl-pop-btn2{
  height:30px;display:inline-flex;align-items:center;justify-content:center;
  padding:0 14px;border:1px solid var(--tl-outline);border-radius:var(--tl-radius-sm);
  background:var(--misc-paper);cursor:pointer;color:var(--theme-text-secondary);
  font-weight:500;font-size:13px;line-height:18px;white-space:nowrap;
}
.tl-pop-btn2:hover{background:var(--tl-hover-bg)}
.tl-pop-ib{
  height:30px;width:30px;display:inline-flex;align-items:center;justify-content:center;
  border:1px solid var(--tl-outline);border-radius:var(--tl-radius-sm);
  background:var(--misc-paper);cursor:pointer;color:var(--theme-text-secondary);
}
.tl-pop-ib:hover{background:var(--tl-hover-bg)}
.tl-pop-ib[aria-expanded="true"]{background:var(--tl-primary-o-light);border-color:var(--color-palette-primary-main);color:var(--color-palette-primary-main)}
/* menu mo tu nut "..." — bam sat nut, do bong dam hon de noi khoi popup */
.tl-menu.is-beside{box-shadow:0 8px 28px rgba(47,43,61,.28)}
.tl-pop-ib.danger{border-color:#FF4C51;color:#FF4C51}
.tl-pop-ib.danger:hover{background:rgba(255,76,81,.08)}
.tl-pop-ib svg{width:20px;height:20px}

/* --- Field list --- */
.tl-pop-fields{display:flex;flex-direction:column;gap:12px}
.tl-pop-f{display:flex;align-items:flex-start;gap:12px}
.tl-pop-f>.k{flex:0 0 120px;width:120px;color:var(--theme-text-secondary)}
.tl-pop-card.side .tl-pop-f>.k{flex:0 0 96px;width:96px}
.tl-pop-f>.v{flex:1 1 0;min-width:0;display:flex;flex-direction:column;gap:4px}
.tl-pop-f .strong{font-weight:500;color:#2F2B3D}
.tl-pop-f .muted{color:var(--theme-text-secondary)}
.tl-pop-f .empty{color:var(--theme-text-disabled)}
.tl-pop-f .link{color:#186FF4}
.tl-pop-code2{color:var(--color-palette-primary-main);font-weight:500}
.tl-pop-more{border:0;background:none;padding:0;cursor:pointer;color:#186FF4;font-weight:500;font-size:13px;line-height:18px}
.tl-pop-guests{display:flex;align-items:center;gap:4px;flex-wrap:wrap}
.tl-pop-guests .nm{font-weight:500;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;max-width:220px}
.tl-pop-guests svg{width:16px;height:16px;flex:0 0 auto}
.tl-pop-phone{display:flex;align-items:center;gap:4px;color:var(--theme-text-secondary)}
.tl-pop-phone svg{width:16px;height:16px;flex:0 0 auto}
.tl-pop-count{display:flex;align-items:center;height:24px}
.tl-pop-count>span{display:inline-flex;align-items:center;gap:4px;width:40px}
.tl-pop-count svg{width:20px;height:20px;flex:0 0 auto;color:var(--theme-text-secondary)}
.tl-flag{width:17px;height:13px;flex:0 0 auto;overflow:hidden}
.tl-pop-avatar{width:20px;height:20px;border-radius:500px;overflow:hidden;flex:0 0 auto}

/* --- Khối thanh toán --- */
.tl-pop-pay{
  background:rgba(128,131,144,.08);border-radius:8px;padding:8px 12px;
  display:flex;flex-direction:column;gap:2px;
}
.tl-pop-pay .r{display:flex;align-items:center;justify-content:flex-end;gap:10px;text-align:right}
.tl-pop-pay .r b{font-weight:500}
.tl-pop-pay .amt{display:flex;align-items:center;justify-content:flex-end;gap:4px;width:180px}
.tl-pop-pay .amt i{flex:1 1 0;min-width:0;font-style:normal;text-align:right}
.tl-pop-pay .amt u{text-decoration:none;color:rgba(47,43,61,.55)}
.tl-pop-pay .r.left i{color:#FF4C51;font-weight:500}
.tl-pop-pay .dot{width:12px;height:12px;border:1px solid #fff;border-radius:20px;flex:0 0 auto}

@media (max-width:1024px){
  .tl-pop[data-kind="group"]{width:calc(100vw - 32px)}
  .tl-pop-body{flex-direction:column}
  .tl-pop-card.side{flex:0 0 auto}
}

/* ================= MENU THAO TÁC NHANH ================= */
.tl-menu{
  position:fixed;z-index:1208;min-width:232px;padding:8px 0;
  background:var(--misc-paper);border-radius:var(--tl-radius-md);
  box-shadow:0 4px 24px rgba(47,43,61,.24);display:none;
}
.tl-menu.is-open{display:block}
.tl-menu button{
  display:flex;align-items:center;gap:12px;width:100%;padding:8px 16px;
  border:0;background:none;cursor:pointer;text-align:left;
  font-weight:400;font-size:14px;line-height:20px;color:var(--theme-text-primary);
  transition:background-color .2s ease;
}
.tl-menu button:hover{background:var(--tl-hover-bg)}
.tl-menu button svg{width:20px;height:20px;flex:0 0 auto;color:var(--theme-text-secondary)}
.tl-menu button.danger,.tl-menu button.danger svg{color:var(--color-palette-error-main)}
.tl-menu hr{border:0;border-top:1px solid var(--tl-divider);margin:8px 16px}

/* ================= DIALOG ================= */
.tl-backdrop{position:fixed;inset:0;background:rgba(47,43,61,.5);z-index:1210;display:none}
.tl-backdrop.is-open{display:block}
.tl-dialog{
  position:fixed;top:50%;left:50%;transform:translate(-50%,-50%);
  z-index:1220;width:560px;max-width:calc(100vw - 32px);
  background:var(--misc-paper);border-radius:var(--tl-radius-md);
  box-shadow:0 8px 32px rgba(47,43,61,.32);display:none;flex-direction:column;
  font-weight:400;font-size:14px;line-height:20px;color:var(--theme-text-primary);
}
.tl-dialog.is-open{display:flex}
.tl-dialog header{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:16px 20px;border-bottom:1px solid var(--tl-divider)}
.tl-dialog h2{margin:0;font-weight:500;font-size:18px;line-height:26px}
.tl-dialog .bd{padding:20px;display:flex;flex-direction:column;gap:16px;max-height:60vh;overflow:auto}
.tl-dialog footer{display:flex;align-items:center;justify-content:flex-end;gap:12px;padding:16px 20px;border-top:1px solid var(--tl-divider)}
.tl-dialog .close{border:0;background:none;cursor:pointer;color:var(--theme-text-secondary);display:inline-flex;padding:4px;border-radius:var(--tl-radius-sm)}
.tl-dialog .close:hover{background:var(--tl-hover-bg)}
.tl-dialog .close svg{width:20px;height:20px}

.tl-switchrow{display:flex;align-items:center;gap:48px}
.tl-switch{display:inline-flex;align-items:center;gap:12px;cursor:pointer;user-select:none;
  border:0;background:none;padding:0;font:inherit;color:inherit}
.tl-switch>*{pointer-events:none}
.tl-switch .track{
  width:40px;height:22px;border-radius:var(--tl-radius-round);background:rgba(47,43,61,.26);
  position:relative;transition:background-color .2s ease;flex:0 0 auto;
}
.tl-switch .track::after{
  content:"";position:absolute;top:2px;left:2px;width:18px;height:18px;
  border-radius:var(--tl-radius-round);background:#fff;
  box-shadow:0 1px 3px rgba(47,43,61,.3);transition:transform .2s ease;
}
.tl-switch[aria-checked="true"] .track{background:var(--color-palette-primary-main)}
.tl-switch[aria-checked="true"] .track::after{transform:translateX(18px)}
.tl-switch:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:2px;border-radius:var(--tl-radius-sm)}

.tl-field{display:flex;flex-direction:column;gap:6px}
.tl-field label{font-weight:400;font-size:13px;line-height:20px;color:var(--color-palette-primary-main)}
.tl-field label .req{color:var(--color-palette-error-main)}
.tl-field textarea{
  width:100%;min-height:88px;resize:vertical;padding:10px 12px;
  border:1px solid var(--color-palette-primary-main);border-radius:var(--tl-radius-md);
  font-weight:400;font-size:14px;line-height:20px;color:var(--theme-text-primary);background:var(--misc-paper);
}
.tl-field textarea:focus{outline:none;border-color:var(--color-palette-primary-dark)}
.tl-field.is-error textarea{border-color:var(--color-palette-error-main)}
.tl-field .err{display:none;font-weight:400;font-size:12px;line-height:16px;color:var(--color-palette-error-main)}
.tl-field.is-error .err{display:block}
.tl-btn-ghost{
  padding:8px 20px;border:0;border-radius:var(--tl-radius-sm);cursor:pointer;
  background:var(--misc-avatar-bg);color:var(--theme-text-secondary);font-weight:500;font-size:14px;line-height:20px;
}
.tl-btn-ghost:hover{background:var(--theme-action-selected)}
.tl-dialog .tl-btn-primary{padding:8px 20px;font-weight:500;font-size:14px;line-height:20px}

/* danh sách phòng chưa gán / đặt phòng trong dialog */
.tl-list{display:flex;flex-direction:column;gap:8px}
.tl-list-item{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:10px 12px;border:1px solid var(--tl-divider);border-radius:var(--tl-radius-md)}
.tl-list-item .l{display:flex;flex-direction:column;gap:2px;min-width:0}
.tl-list-item .l b{font-weight:500;font-size:14px;line-height:20px}
.tl-list-item .l span{font-weight:400;font-size:12px;line-height:16px;color:var(--theme-text-secondary)}
.tl-badge-st{display:inline-flex;align-items:center;gap:6px;padding:2px 10px;border-radius:var(--tl-radius-round);font-weight:500;font-size:12px;line-height:18px}

/* ================= TOAST ================= */
.tl-toasts{position:fixed;right:16px;top:16px;z-index:1230;display:flex;flex-direction:column;gap:8px}
.tl-toast{
  display:flex;align-items:center;gap:10px;min-width:280px;max-width:420px;
  padding:12px 16px;border-radius:var(--tl-radius-md);background:var(--misc-paper);
  box-shadow:0 4px 20px rgba(47,43,61,.24);font-weight:400;font-size:14px;line-height:20px;
  border-left:4px solid var(--tl-pay-paid);
  animation:tl-toast-in .2s ease;
}
.tl-toast.err{border-left-color:var(--color-palette-error-main)}
.tl-toast svg{width:20px;height:20px;flex:0 0 auto;color:var(--tl-pay-paid)}
.tl-toast.err svg{color:var(--color-palette-error-main)}
@keyframes tl-toast-in{from{opacity:0;transform:translateX(16px)}to{opacity:1;transform:none}}

/* ===== Tooltip — EzOne.DesignLibrary 216:135965 (tooltip.md) =====
   Surface --misc-snackbar #2F2B3D · label trắng Roboto Medium 13/20
   padding 5px/12px · radius 4px · arrow 12×6 · gap trigger→tooltip 8px       */
.tl-tt{
  position:fixed;z-index:1205;display:none;
  padding:5px 12px;border-radius:var(--tl-radius-sm);
  background:var(--tl-snackbar);color:#fff;
  font-weight:500;font-size:13px;line-height:20px;
  max-width:240px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;
  pointer-events:none;
}
.tl-tt.is-open{display:block}
.tl-tt::after{content:"";position:absolute;width:12px;height:6px;background:var(--tl-snackbar)}
/* Direction=Down: khối nằm TRÊN trigger, mũi tên chỉ xuống */
.tl-tt[data-dir="down"]::after{bottom:-6px;left:var(--tt-ax,50%);transform:translateX(-50%);clip-path:polygon(0 0,100% 0,50% 100%)}
/* Direction=Up: khối nằm DƯỚI trigger, mũi tên chỉ lên */
.tl-tt[data-dir="up"]::after{top:-6px;left:var(--tt-ax,50%);transform:translateX(-50%);clip-path:polygon(50% 0,100% 100%,0 100%)}

/* sr-only */
.tl-sr{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap;border:0}

@media (prefers-reduced-motion:reduce){
  .tl *,.tl *::before,.tl *::after{transition-duration:.01ms!important;animation-duration:.01ms!important}
  .tl-anim .tl-bar{animation:none!important}
}
@media (max-width:1024px){
  .tl-head{gap:12px;padding:12px}
  .tl-toolbar{flex-wrap:wrap}
}


/* ================================================================
   ĐẶT PHÒNG NHANH (Quick Booking) — right drawer 800px
   Figma: 46683:170162 · 46701:175379 · 47924:231992
          47787:87196  (goi y khach da co du lieu)
          45163:286669 (ReactDatePicker + TimePicker)
          55583:60835  (Loading khi he thong tao dat phong)
   Ke thua bien --tl, --theme, --color-palette cua shell v8.
   z-index dung so nguyen literal (Chrome tra "auto" neu dung calc voi var).
   ================================================================ */
:root{
  --qb-w: max(720px, 60vw);      /* sidebar luon chiem 60% man hinh, tu scale khi man lon */
  --qb-sugg-w: 465px;
  --qb-ctrl-h: 38px;
  --qb-radius: 8px;          /* control — radius discipline 8/12/9999 */
  --qb-radius-card: 12px;    /* card */
  --qb-border: rgba(47,43,61,.22);
  --qb-border-soft: rgba(47,43,61,.12);
  --qb-ph: rgba(47,43,61,.4);
  --qb-body-bg: #F8F7FA;
  --qb-fs: 13px; --qb-lh: 20px;
  --qb-fs-md: 15px; --qb-lh-md: 22px;
  --qb-hover: rgba(47,43,61,.06);
  --qb-selected: rgba(47,43,61,.08);
  --qb-success: #28C76F;
  --qb-success-dk: #24B364;
  --qb-warn: #FF9F43;
  --qb-mark: rgba(255,159,67,.45);
  --qb-gap-x: 8px;
  --qb-gap-y: 12px;
}

.qb-backdrop{position:fixed;inset:0;background:rgba(47,43,61,.5);z-index:1240;display:none}
.qb-qrbd{position:fixed;inset:0;background:rgba(47,43,61,.5);z-index:1294;display:none}
.qb-qrbd.is-open{display:block}
.qb-backdrop.is-open{display:block}

.qb{
  position:fixed;top:0;right:0;height:100%;width:var(--qb-w);max-width:100vw;
  z-index:1250;background:var(--qb-body-bg);
  display:none;flex-direction:column;
  box-shadow:-8px 0 32px rgba(47,43,61,.24);
  font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);
  color:var(--theme-text-primary);
  transform:translateX(24px);opacity:0;
  transition:transform .18s var(--tl-ease),opacity .18s var(--tl-ease);
}
.qb.is-open{display:flex;transform:translateX(0);opacity:1}
@media (prefers-reduced-motion: reduce){ .qb{transition:none} }

/* ---------- Header ---------- */
.qb-head{
  display:flex;align-items:center;justify-content:space-between;gap:12px;
  padding:16px 20px;background:var(--misc-paper);
  border-bottom:1px solid var(--qb-border-soft);flex:0 0 auto;
}
.qb-head h2{margin:0;font-weight:500;font-size:18px;line-height:28px;color:var(--theme-text-primary)}
.qb-x{
  border:0;background:none;cursor:pointer;color:var(--theme-text-secondary);
  display:inline-flex;padding:4px;border-radius:var(--qb-radius);
  transition:background-color var(--tl-dur-fast) var(--tl-ease);
}
.qb-x:hover{background:var(--qb-hover)}
.qb-x svg{width:20px;height:20px;flex:0 0 auto}

/* ---------- Tabs lon ---------- */
.qb-tabwrap{background:var(--misc-paper);padding:16px;flex:0 0 auto;border-bottom:1px solid var(--qb-border-soft)}
.qb-tabs{display:flex;gap:16px}
.qb-tab{
  flex:1 1 0;min-width:0;height:54px;
  display:inline-flex;align-items:center;justify-content:center;gap:8px;
  border:1px solid var(--qb-border);border-radius:var(--qb-radius);
  background:var(--misc-paper);cursor:pointer;
  font-weight:500;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);
  color:var(--theme-text-primary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease),border-color var(--tl-dur-fast) var(--tl-ease),color var(--tl-dur-fast) var(--tl-ease);
}
.qb-tab svg{width:20px;height:20px;flex:0 0 auto}
.qb-tab:hover{background:var(--qb-hover)}
.qb-tab[aria-selected="true"][data-qbtab="prebook"]{
  background:var(--tl-primary-o-light);border-color:var(--color-palette-primary-main);color:var(--color-palette-primary-main)
}
.qb-tab[aria-selected="true"][data-qbtab="checkin"]{
  background:rgba(255,76,81,.08);border-color:var(--tl-weekend-fg);color:var(--tl-weekend-fg)
}
.qb-tab:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:2px}

/* ---------- Body + card ---------- */
.qb-body{flex:1 1 auto;overflow:auto;padding:16px;display:flex;flex-direction:column;gap:12px}
.qb-2col{display:grid;grid-template-columns:1fr 1fr;gap:12px;align-items:start}

.qb-card{
  background:var(--misc-paper);border:1px solid var(--qb-border-soft);
  border-radius:var(--qb-radius-card);padding:12px;
  display:flex;flex-direction:column;gap:12px;
}
.qb-cardhead{display:flex;align-items:center;gap:8px;min-height:30px}
.qb-cardhead .qb-mark{width:3px;height:16px;border-radius:9999px;background:var(--color-palette-primary-main);flex:0 0 auto}
.qb-cardhead h3{
  margin:0;font-weight:500;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);
  color:var(--theme-text-primary);flex:1 1 auto;
}

/* Chip trang thai dat phong */
.qb-chips{display:inline-flex;gap:8px;flex:0 0 auto}
.qb-chip{
  height:30px;padding:0 12px;border-radius:var(--qb-radius);
  border:1px solid var(--qb-border);background:var(--misc-paper);cursor:pointer;
  font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-secondary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease),border-color var(--tl-dur-fast) var(--tl-ease),color var(--tl-dur-fast) var(--tl-ease);
}
.qb-chip:hover{background:var(--qb-hover)}
.qb-chip[aria-pressed="true"]{
  background:rgba(40,199,111,.08);border-color:var(--qb-success);color:var(--qb-success-dk);font-weight:500
}
.qb-chip:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px}

/* ================================================================
   LƯỚI 12 CỘT — mọi ô nhập cùng chiều cao, cùng baseline, thẳng cột
   giữa các hàng (fix phản hồi của PO về việc các ô lệch kích thước).
   ================================================================ */
.qb-grid{display:grid;grid-template-columns:repeat(12,1fr);gap:var(--qb-gap-y) var(--qb-gap-x);align-items:start}
.qb-c1{grid-column:span 1}   .qb-c2{grid-column:span 2}   .qb-c3{grid-column:span 3}
.qb-c4{grid-column:span 4}   .qb-c5{grid-column:span 5}   .qb-c6{grid-column:span 6}
.qb-c7{grid-column:span 7}   .qb-c8{grid-column:span 8}   .qb-c9{grid-column:span 9}
.qb-c10{grid-column:span 10} .qb-c11{grid-column:span 11} .qb-c12{grid-column:span 12}

.qb-f{display:flex;flex-direction:column;gap:4px;position:relative;min-width:0}
.qb-lb{
  display:flex;align-items:center;gap:2px;min-height:20px;
  font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);
  color:var(--theme-text-secondary);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;
}
.qb-lb .req{color:var(--tl-weekend-fg);font-weight:500}
.qb-msg{display:none;font-weight:400;font-size:12px;line-height:16px;color:var(--tl-weekend-fg)}
.qb-f.is-err .qb-msg{display:block}

/* ---------- Control chung ---------- */
.qb-ctrl{
  width:100%;height:var(--qb-ctrl-h);box-sizing:border-box;
  padding:0 12px;border:1px solid var(--qb-border);border-radius:var(--qb-radius);
  background:var(--misc-paper);color:var(--theme-text-primary);
  font-family:inherit;font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);
  transition:border-color var(--tl-dur-fast) var(--tl-ease),box-shadow var(--tl-dur-fast) var(--tl-ease);
}
.qb-ctrl::placeholder{color:var(--qb-ph)}
.qb-ctrl:hover:not(:disabled){border-color:rgba(47,43,61,.38)}
.qb-ctrl:focus{outline:none;border-color:var(--color-palette-primary-main);box-shadow:0 0 0 2px var(--tl-primary-o-light)}
.qb-ctrl:disabled,.qb-ctrl[readonly]{background:rgba(47,43,61,.04);color:var(--theme-text-secondary);cursor:not-allowed}
textarea.qb-ctrl{height:auto;min-height:70px;padding:9px 12px;resize:vertical}
.qb-f.is-err .qb-ctrl,.qb-f.is-err .qb-vs-btn{border-color:var(--tl-weekend-fg)}

.qb-inwrap{position:relative;display:block;width:100%}
.qb-inwrap .qb-suffix{
  position:absolute;right:4px;top:50%;transform:translateY(-50%);
  color:var(--theme-text-secondary);display:inline-flex;align-items:center;justify-content:center;
  width:26px;height:26px;border:0;background:none;border-radius:var(--qb-radius);cursor:pointer;padding:0;
}
.qb-inwrap .qb-suffix:hover{background:var(--qb-hover)}
.qb-inwrap .qb-suffix svg{width:16px;height:16px;flex:0 0 auto}
.qb-inwrap.has-suffix .qb-ctrl{padding-right:34px}

.qb-ibtn{
  width:var(--qb-ctrl-h);height:var(--qb-ctrl-h);flex:0 0 auto;
  display:inline-flex;align-items:center;justify-content:center;
  border:1px solid var(--qb-border);border-radius:var(--qb-radius);
  background:var(--misc-paper);cursor:pointer;color:var(--theme-text-secondary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease);
}
.qb-ibtn:hover{background:var(--qb-hover)}
.qb-ibtn svg{width:18px;height:18px;flex:0 0 auto}
.qb-ibtn:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px}
.qb-inline{display:flex;gap:8px;align-items:flex-start}
.qb-inline>.qb-vs,.qb-inline>.qb-inwrap{flex:1 1 auto;min-width:0}

/* ---------- Select kiểu Vuexy (VSelect / AppSelect, variant outlined) ---------- */
.qb-vs{position:relative;display:block;width:100%}
.qb-vs-btn{
  width:100%;height:var(--qb-ctrl-h);box-sizing:border-box;
  display:flex;align-items:center;gap:8px;text-align:left;
  padding:0 34px 0 12px;border:1px solid var(--qb-border);border-radius:var(--qb-radius);
  background:var(--misc-paper);color:var(--theme-text-primary);cursor:pointer;
  font-family:inherit;font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);
  transition:border-color var(--tl-dur-fast) var(--tl-ease),box-shadow var(--tl-dur-fast) var(--tl-ease);
}
.qb-vs-btn:hover{border-color:rgba(47,43,61,.38)}
.qb-vs-btn[aria-expanded="true"]{border-color:var(--color-palette-primary-main);box-shadow:0 0 0 2px var(--tl-primary-o-light)}
.qb-vs-btn:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px}
.qb-vs-btn .qb-vs-val{flex:1 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.qb-vs-btn.is-ph .qb-vs-val{color:var(--qb-ph)}
.qb-vs-btn .qb-lead{display:inline-flex;flex:0 0 auto}
.qb-vs-btn .qb-lead svg{width:18px;height:14px;flex:0 0 auto;border-radius:2px}
.qb-vs .qb-cv{
  position:absolute;right:11px;top:calc(var(--qb-ctrl-h) / 2);transform:translateY(-50%);
  width:16px;height:16px;pointer-events:none;color:var(--theme-text-secondary);
  transition:transform var(--tl-dur-fast) var(--tl-ease);
}
.qb-vs .qb-cv svg{width:16px;height:16px;display:block;flex:0 0 auto}
.qb-vs.is-open .qb-cv{transform:translateY(-50%) rotate(180deg)}

/* menu noi (Vuexy v-menu) — render ngoai .qb-body de khong bi cat boi overflow */
.qb-vsmenu{
  position:fixed;z-index:1272;display:none;
  min-width:120px;max-height:280px;overflow:auto;
  background:var(--misc-paper);border-radius:var(--qb-radius);
  box-shadow:0 4px 18px rgba(47,43,61,.22);padding:4px;
  transform-origin:top center;
}
.qb-vsmenu.is-open{display:block;animation:qb-menu-in .12s var(--tl-ease)}
@keyframes qb-menu-in{from{opacity:0;transform:scaleY(.92)}to{opacity:1;transform:scaleY(1)}}
@media (prefers-reduced-motion: reduce){ .qb-vsmenu.is-open{animation:none} }
.qb-vsmenu button{
  display:flex;align-items:center;gap:8px;width:100%;text-align:left;
  min-height:var(--qb-ctrl-h);padding:8px 12px;border:0;border-radius:var(--qb-radius);
  background:none;cursor:pointer;font-family:inherit;
  font-weight:400;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);color:var(--theme-text-primary);
}
.qb-vsmenu button svg{width:18px;height:14px;flex:0 0 auto;border-radius:2px}
.qb-vsmenu button:hover,.qb-vsmenu button.is-act{background:var(--qb-hover)}
.qb-vsmenu button[aria-selected="true"]{
  background:var(--tl-primary-o-main);color:var(--color-palette-primary-main);font-weight:500
}
.qb-vsmenu button[aria-selected="true"].is-act{background:rgba(24,111,244,.24)}

/* ---------- Thời gian lưu trú ---------- */
.qb-stay{
  display:flex;align-items:center;gap:12px;
  background:var(--tl-primary-o-light);border-radius:var(--qb-radius);
  padding:8px 12px;min-height:54px;box-sizing:border-box;
}
.qb-dt{
  flex:1 1 0;min-width:0;display:flex;flex-direction:column;gap:2px;
  border:0;background:none;padding:0;text-align:left;cursor:pointer;font-family:inherit;
  border-radius:var(--qb-radius);
}
.qb-dt .d{font-weight:500;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);color:var(--color-palette-primary-main)}
.qb-dt .t{font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-secondary)}
.qb-dt:hover:not(:disabled) .d{text-decoration:underline}
.qb-dt:disabled{cursor:default}
.qb-dt:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:2px}
.qb-arrow{flex:0 0 auto;color:var(--theme-text-secondary);display:inline-flex}
.qb-arrow svg{width:18px;height:18px;flex:0 0 auto}
.qb-stay .qb-vr{width:1px;align-self:stretch;background:var(--qb-border-soft);flex:0 0 auto}

.qb-step{display:inline-flex;align-items:center;gap:8px;flex:0 0 auto}
.qb-step button{
  width:26px;height:26px;flex:0 0 auto;display:inline-flex;align-items:center;justify-content:center;
  border:1px solid var(--qb-border);border-radius:var(--qb-radius);background:var(--misc-paper);
  cursor:pointer;color:var(--theme-text-secondary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease);
}
.qb-step button:hover:not(:disabled){background:var(--qb-hover)}
.qb-step button:disabled{opacity:.38;cursor:not-allowed}
.qb-step button svg{width:14px;height:14px;flex:0 0 auto}
.qb-step .qb-nights{
  min-width:52px;text-align:center;font-weight:500;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);
  color:var(--color-palette-primary-main);
}

/* Tạm tính / Tổng tiền */
.qb-readout{
  display:flex;align-items:center;justify-content:space-between;gap:8px;
  height:var(--qb-ctrl-h);box-sizing:border-box;padding:0 12px;
  background:var(--qb-hover);border-radius:var(--qb-radius);
}
.qb-readout .v{font-weight:500;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);color:var(--tl-weekend-fg)}
.qb-readout .u{font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-secondary)}
.qb-readout.is-total .v{font-size:18px;line-height:24px}

/* Số lượng khách */
.qb-paxbox{
  display:flex;align-items:center;gap:8px;height:var(--qb-ctrl-h);box-sizing:border-box;
  padding:0 6px 0 10px;border:1px solid var(--qb-border);border-radius:var(--qb-radius);background:var(--misc-paper);
}
.qb-paxbox .ic{color:var(--theme-text-secondary);display:inline-flex;flex:0 0 auto}
.qb-paxbox .ic svg{width:18px;height:18px;flex:0 0 auto}
.qb-paxbox input{
  flex:1 1 auto;min-width:0;width:100%;border:0;background:none;padding:0;text-align:center;
  font-family:inherit;font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-primary);
  -moz-appearance:textfield;appearance:textfield;
}
.qb-paxbox input::-webkit-outer-spin-button,.qb-paxbox input::-webkit-inner-spin-button{-webkit-appearance:none;margin:0}
.qb-paxbox input:focus{outline:none}
.qb-spin{display:flex;flex-direction:column;gap:1px;flex:0 0 auto}
.qb-spin button{
  width:16px;height:12px;display:inline-flex;align-items:center;justify-content:center;
  border:0;background:none;cursor:pointer;color:var(--theme-text-secondary);padding:0;border-radius:2px;
}
.qb-spin button:hover{background:var(--qb-hover)}
.qb-spin button svg{width:10px;height:10px;flex:0 0 auto}

/* ---------- Thanh toán ---------- */
.qb-paymode{display:flex;gap:12px}
.qb-paymode button{
  flex:1 1 0;height:var(--qb-ctrl-h);border-radius:var(--qb-radius);
  border:1px solid var(--qb-border);background:var(--misc-paper);cursor:pointer;
  font-family:inherit;font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-primary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease),border-color var(--tl-dur-fast) var(--tl-ease),color var(--tl-dur-fast) var(--tl-ease);
}
.qb-paymode button:hover{background:var(--qb-hover)}
.qb-paymode button[aria-pressed="true"]{
  background:var(--tl-primary-o-light);border-color:var(--color-palette-primary-main);
  color:var(--color-palette-primary-main);font-weight:500;
}
.qb-paymode button:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px}

.qb-quick{display:flex;align-items:center;justify-content:flex-end;gap:8px;flex-wrap:wrap;min-height:var(--qb-ctrl-h)}
.qb-quick .k{font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-secondary)}
.qb-quick button{
  height:26px;min-width:42px;padding:0 8px;border-radius:var(--qb-radius);
  border:1px solid var(--qb-border);background:var(--misc-paper);cursor:pointer;
  font-family:inherit;font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-primary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease);
}
.qb-quick button:hover{background:var(--qb-hover)}
.qb-quick button[aria-pressed="true"]{
  background:var(--tl-primary-o-main);border-color:var(--color-palette-primary-main);color:var(--color-palette-primary-main)
}
.qb-quick button:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px}

/* ================================================================
   Gợi ý khách đã có dữ liệu — panel trái, Figma 47787:87196
   ================================================================ */
.qb-sugg{
  position:fixed;top:0;right:var(--qb-w);height:100%;width:var(--qb-sugg-w);
  max-width:calc(100vw - var(--qb-w));
  z-index:1248;display:none;flex-direction:column;
  background:var(--misc-paper);border-right:1px solid var(--qb-border-soft);
  box-shadow:-8px 0 24px rgba(47,43,61,.14);
}
.qb-sugg.is-open{display:flex}
.qb-sugg-head{
  flex:0 0 auto;padding:16px 20px;border-bottom:1px solid var(--qb-border-soft);
  font-weight:500;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);color:var(--theme-text-primary);
}
.qb-sugg-list{flex:1 1 auto;overflow:auto;padding:0}
.qb-sugg-list button{
  display:flex;flex-direction:column;gap:4px;width:100%;text-align:left;
  border:0;border-bottom:1px solid var(--qb-border-soft);background:none;cursor:pointer;
  padding:12px 16px;font-family:inherit;
  transition:background-color var(--tl-dur-fast) var(--tl-ease);
}
.qb-sugg-list button:hover,.qb-sugg-list button.is-act{background:var(--tl-primary-o-light)}
.qb-sugg-list .n{display:flex;align-items:center;gap:6px;font-weight:400;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);color:var(--theme-text-primary)}
.qb-sugg-list .n svg{width:20px;height:14px;flex:0 0 auto;border-radius:2px}
.qb-sugg-list .n .who{width:14px;height:14px;color:var(--theme-text-secondary)}
.qb-sugg-list .m{display:flex;align-items:center;gap:5px;flex-wrap:nowrap;white-space:nowrap;font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-secondary)}
.qb-sugg-list .m svg{width:13px;height:13px;flex:0 0 auto}
.qb-sugg-list .m i{font-style:normal;color:var(--qb-border)}
.qb-sugg-list mark{background:var(--qb-mark);color:inherit;border-radius:2px;padding:0 1px}
.qb-sugg-empty{padding:24px 20px;color:var(--theme-text-secondary)}

/* nút bỏ gán hồ sơ khách trong ô "Nhập tên khách" */
.qb-f .qb-clear{
  position:absolute;right:8px;bottom:8px;
  width:22px;height:22px;display:inline-flex;align-items:center;justify-content:center;
  border:0;border-radius:9999px;background:rgba(47,43,61,.08);color:var(--theme-text-secondary);cursor:pointer;padding:0;
}
.qb-f .qb-clear:hover{background:rgba(47,43,61,.16)}
.qb-f .qb-clear svg{width:12px;height:12px;flex:0 0 auto}
.qb-ctrl.is-linked{padding-right:36px;border-color:var(--color-palette-primary-main)}

/* ================================================================
   Bộ lịch ngày + giờ — Figma 45163:286669 (ReactDatePicker)
   ================================================================ */
.qb-dtp{
  position:fixed;z-index:1274;display:none;width:294px;
  background:var(--misc-paper);border-radius:var(--qb-radius);
  box-shadow:0 3px 12px rgba(47,43,61,.14),0 2px 8px rgba(47,43,61,.12);
  overflow:hidden;
}
.qb-dtp.is-open{display:block}
.qb-dtp-head{display:flex;align-items:center;gap:12px;padding:12px 16px}
.qb-dtp-title{
  flex:1 1 auto;display:flex;align-items:center;gap:12px;min-width:0;
  font-weight:400;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);color:var(--theme-text-primary);
}
.qb-dtp-title select{
  flex:1 1 0;min-width:0;border:0;background:none;padding:2px 4px;border-radius:var(--qb-radius);
  font-family:inherit;font-weight:400;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);
  color:var(--theme-text-primary);cursor:pointer;
}
.qb-dtp-title select:hover{background:var(--qb-hover)}
.qb-dtp-title select:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:0}
.qb-dtp-nav{display:inline-flex;gap:12px;flex:0 0 auto}
.qb-dtp-nav button{
  width:30px;height:30px;display:inline-flex;align-items:center;justify-content:center;
  border:0;border-radius:9999px;background:var(--qb-selected);color:var(--theme-text-primary);cursor:pointer;padding:0;
  transition:background-color var(--tl-dur-fast) var(--tl-ease);
}
.qb-dtp-nav button:hover{background:rgba(47,43,61,.16)}
.qb-dtp-nav button svg{width:20px;height:20px;flex:0 0 auto}
.qb-dtp-nav button:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px}

.qb-dtp-wd{display:grid;grid-template-columns:repeat(7,36px);justify-content:center;padding:0 8px}
.qb-dtp-wd span{
  height:20px;display:flex;align-items:center;justify-content:center;
  font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-primary);
}
.qb-dtp-days{display:grid;grid-template-columns:repeat(7,36px);justify-content:center;padding:12px 8px 8px}
.qb-dtp-days button{
  width:36px;height:36px;display:inline-flex;align-items:center;justify-content:center;
  border:0;border-radius:9999px;background:none;cursor:pointer;padding:0;font-family:inherit;
  font-weight:400;font-size:var(--qb-fs-md);line-height:24px;color:var(--theme-text-primary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease);
}
.qb-dtp-days button:hover{background:var(--qb-hover)}
.qb-dtp-days button.out{color:var(--qb-ph)}
.qb-dtp-days button.today{background:var(--tl-primary-o-main);color:var(--color-palette-primary-main)}
.qb-dtp-days button.sel{
  background:var(--color-palette-primary-main);color:#fff;
  box-shadow:0 2px 6px rgba(0,131,202,.3);
}
.qb-dtp-days button:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px}

.qb-dtp-time{display:flex;align-items:stretch;border-top:1px solid var(--qb-border-soft)}
.qb-dtp-time .cell{
  flex:1 1 0;min-width:0;position:relative;display:flex;align-items:center;justify-content:center;padding:12px;
}
.qb-dtp-time .cell.h{background:var(--qb-hover)}
.qb-dtp-time input{
  width:100%;border:0;background:none;padding:0;text-align:center;
  font-family:inherit;font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-primary);
  -moz-appearance:textfield;appearance:textfield;
}
.qb-dtp-time input::-webkit-outer-spin-button,.qb-dtp-time input::-webkit-inner-spin-button{-webkit-appearance:none;margin:0}
.qb-dtp-time input:focus{outline:none}
.qb-dtp-time .dots{
  flex:0 0 auto;display:flex;align-items:center;justify-content:center;padding:12px 0;
  font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-primary);
}
.qb-dtp-spin{
  position:absolute;right:0;top:50%;transform:translateY(-50%);
  width:16px;height:46px;display:flex;flex-direction:column;
  border:.6px solid var(--qb-border-soft);overflow:hidden;
}
.qb-dtp-spin button{
  flex:1 1 0;display:flex;align-items:center;justify-content:center;
  border:0;background:none;cursor:pointer;padding:2px 3px;color:var(--theme-text-primary);
}
.qb-dtp-spin button:first-child{background:var(--qb-hover)}
.qb-dtp-spin button:hover{background:rgba(47,43,61,.12)}
.qb-dtp-spin svg{width:5px;height:4px;flex:0 0 auto;display:block}
.qb-dtp-foot{display:flex;align-items:center;justify-content:flex-end;gap:8px;padding:8px 12px;border-top:1px solid var(--qb-border-soft)}
.qb-dtp-foot button{
  height:30px;padding:0 12px;border-radius:var(--qb-radius);cursor:pointer;font-family:inherit;
  font-weight:500;font-size:var(--qb-fs);line-height:var(--qb-lh);
  border:1px solid var(--color-palette-primary-main);background:var(--color-palette-primary-main);color:#fff;
}
.qb-dtp-foot button.ghost{background:none;color:var(--theme-text-secondary);border-color:var(--qb-border)}
.qb-dtp-foot button.ghost:hover{background:var(--qb-hover)}

/* ---------- Footer ---------- */
.qb-foot{
  flex:0 0 auto;background:var(--misc-paper);border-top:1px solid var(--qb-border-soft);
  padding:12px 16px 16px;display:flex;flex-direction:column;gap:12px;
}
.qb-remain{
  display:flex;align-items:center;justify-content:space-between;gap:12px;
  padding:12px 16px;border-radius:var(--qb-radius);
  background:rgba(255,76,81,.08);border:1px solid rgba(255,76,81,.24);
}
.qb-remain .k{font-weight:400;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);color:var(--theme-text-primary)}
.qb-remain .v{display:inline-flex;align-items:baseline;gap:6px}
.qb-remain .dot{width:8px;height:8px;border-radius:9999px;background:var(--tl-weekend-fg);align-self:center;flex:0 0 auto}
.qb-remain .amt{font-weight:500;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);color:var(--tl-weekend-fg)}
.qb-remain .u{font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-secondary)}
.qb-remain.is-warn{background:rgba(255,159,67,.08);border-color:rgba(255,159,67,.24)}
.qb-remain.is-warn .dot{background:var(--qb-warn)}
.qb-remain.is-warn .amt{color:var(--qb-warn)}
.qb-remain.is-ok{background:rgba(40,199,111,.08);border-color:rgba(40,199,111,.24)}
.qb-remain.is-ok .dot{background:var(--qb-success)}
.qb-remain.is-ok .amt{color:var(--qb-success-dk)}

.qb-actions{display:grid;grid-template-columns:1fr 1fr;gap:12px}
.qb-actions button{
  height:48px;border-radius:var(--qb-radius);cursor:pointer;
  font-family:inherit;font-weight:500;font-size:var(--qb-fs-md);line-height:var(--qb-lh-md);
  transition:background-color var(--tl-dur-fast) var(--tl-ease);
}
.qb-cancel{border:1px solid var(--qb-border-soft);background:var(--qb-hover);color:var(--theme-text-primary)}
.qb-cancel:hover{background:rgba(47,43,61,.1)}
.qb-ok{border:0;background:var(--color-palette-primary-main);color:#fff}
.qb-ok:hover{background:#1560D8}
.qb.is-checkin .qb-ok{background:var(--tl-weekend-fg)}
.qb.is-checkin .qb-ok:hover{background:#E8434A}
.qb-actions button:disabled{opacity:.6;cursor:not-allowed}
.qb-actions button:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:2px}

/* ================================================================
   Loading khi hệ thống tạo đặt phòng — Figma 55583:60835
   ================================================================ */
.qb-load{
  position:fixed;inset:0;z-index:1290;display:none;
  align-items:center;justify-content:center;background:rgba(47,43,61,.5);
}
.qb-load.is-open{display:flex}
.qb-load .box{
  width:270px;padding:24px 20px 20px;border-radius:var(--qb-radius-card);
  background:var(--misc-paper);box-shadow:0 8px 32px rgba(47,43,61,.24);
  display:flex;flex-direction:column;align-items:center;gap:12px;text-align:center;
}
.qb-ring{position:relative;width:76px;height:76px;flex:0 0 auto}
.qb-ring svg{width:76px;height:76px;display:block;flex:0 0 auto;transform:rotate(-90deg)}
.qb-ring .trk{stroke:rgba(47,43,61,.08)}
.qb-ring .bar{
  stroke:var(--color-palette-primary-main);stroke-linecap:round;
  transition:stroke-dashoffset .18s linear;
}
.qb-ring .pct{
  position:absolute;inset:0;display:flex;align-items:center;justify-content:center;
  font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--color-palette-primary-main);
}
.qb-load .t1{font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);color:var(--theme-text-primary)}
.qb-load .t2{font-weight:400;font-size:12px;line-height:16px;color:var(--theme-text-secondary)}

/* Toast thành công (Figma 55583:60835) — dùng chung container .tl-toasts */
.qb-toast{
  display:flex;align-items:center;gap:8px;min-width:200px;max-width:360px;
  padding:10px 12px;border-radius:var(--qb-radius);
  background:var(--qb-success);color:#fff;
  box-shadow:0 4px 16px rgba(40,199,111,.32);
  font-weight:400;font-size:var(--qb-fs);line-height:var(--qb-lh);
  animation:qb-toast-in .18s var(--tl-ease);
}
@keyframes qb-toast-in{from{opacity:0;transform:translateX(16px)}to{opacity:1;transform:translateX(0)}}
@media (prefers-reduced-motion: reduce){ .qb-toast{animation:none} }
.qb-toast .ic{
  width:22px;height:22px;flex:0 0 auto;display:inline-flex;align-items:center;justify-content:center;
  border-radius:var(--qb-radius);background:rgba(255,255,255,.22);
}
.qb-toast .ic svg{width:14px;height:14px;flex:0 0 auto}
.qb-toast .tx{flex:1 1 auto;min-width:0}
.qb-toast .x{
  width:20px;height:20px;flex:0 0 auto;display:inline-flex;align-items:center;justify-content:center;
  border:0;background:none;color:#fff;cursor:pointer;padding:0;border-radius:var(--qb-radius);opacity:.9;
}
.qb-toast .x:hover{background:rgba(255,255,255,.22)}
.qb-toast .x svg{width:12px;height:12px;flex:0 0 auto}

/* ---------- Vùng kéo chọn ô trống trên lưới ---------- */
.tl-sel-ghost{
  position:absolute;height:var(--tl-bar-h);top:1px;border-radius:var(--tl-radius-bar);
  border:2px dashed var(--color-palette-primary-main);background:var(--tl-primary-o-light);
  z-index:7;pointer-events:none;
}
.tl-table tbody td.dc.is-selecting{background:var(--tl-primary-o-light)}

/* --- sua nho --- */
.qb [hidden],.qb-dtp[hidden],.qb-dtp-time[hidden],#qbCompanyDebtWrap[hidden]{display:none !important}
.qb-vs-btn .qb-vs-lead:empty{display:none}
.qb-vs[data-qbjoin="l"] .qb-vs-btn{border-top-right-radius:0;border-bottom-right-radius:0}
.qb-vs-btn>*{pointer-events:none}

/* ================================================================
   SƠ ĐỒ CHI TIẾT + SƠ ĐỒ ĐƠN GIẢN
   Figma: 55588:67065 (chi tiet) · 55588:67066 (don gian)
          32146:44832 (Room card — 5 status x 2 variant)
   ================================================================ */
:root{
  --dv-card-h: 86px;
  --dv-tile-w: 80px;
  --dv-gap: 8px;
  --dv-card-min: 272px;
  --dv-empty: #309F61;          /* = --tl-st-available */
  --dv-booking: #186FF4;        /* primary */
  --dv-checkin: #D96262;        /* = --tl-bar-inhouse */
  --dv-repair-bg: #E9E9EB;
  --dv-repair-tile: #DEDEE1;
  --dv-badge: #FF9F43;
  --dv-body-bg: #FAFAFB;
}

/* nhóm 4 lựa chọn + ô tìm kiếm — chỉ hiện ở sơ đồ chi tiết / đơn giản */
.tl-seg.tl-seg4 button{padding:0 12px}
.tl-search{
  display:inline-flex;align-items:center;gap:8px;height:var(--tl-btn-h-sm);
  padding:0 12px;min-width:240px;border:1px solid var(--tl-divider);border-radius:var(--tl-radius-group);
  background:var(--misc-paper);color:var(--theme-text-secondary);
}
.tl-search svg{width:16px;height:16px;flex:0 0 auto}
.tl-search input{
  flex:1 1 auto;min-width:0;border:0;background:none;padding:0;
  font-family:inherit;font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-primary);
}
.tl-search input:focus{outline:none}
.tl-search:focus-within{border-color:var(--color-palette-primary-main);box-shadow:0 0 0 2px var(--tl-primary-o-light)}

/* vùng cuộn của 2 view mới */
.tl-cards{
  flex:1 1 auto;min-height:0;overflow:auto;background:var(--dv-body-bg);
  padding:12px 16px 24px;
}
.tl-cards[hidden]{display:none !important}
.tl-cards-grp{margin:0 0 16px}
.tl-cards-grp:last-child{margin-bottom:0}
.tl-cards-h{
  margin:0 0 8px;font-weight:500;font-size:15px;line-height:24px;color:var(--theme-text-primary);
}
.tl-cards-body{display:grid;gap:var(--dv-gap)}
.tl-cards.is-detail .tl-cards-body{grid-template-columns:repeat(auto-fill,minmax(var(--dv-card-min),1fr))}
.tl-cards.is-simple .tl-cards-body{grid-template-columns:repeat(auto-fill,var(--dv-tile-w));justify-content:start}

/* ---------- Room card ---------- */
.tl-card{
  display:flex;align-items:stretch;height:var(--dv-card-h);box-sizing:border-box;
  border:1px solid var(--tl-divider);border-radius:var(--tl-radius-group);
  background:var(--misc-paper);overflow:hidden;cursor:pointer;text-align:left;padding:0;
  font-family:inherit;color:var(--theme-text-primary);
  transition:box-shadow var(--tl-dur-fast) var(--tl-ease),border-color var(--tl-dur-fast) var(--tl-ease);
}
.tl-card:hover{border-color:rgba(47,43,61,.28);box-shadow:0 2px 8px rgba(47,43,61,.12)}
.tl-card:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px}
.tl-cards.is-simple .tl-card{width:var(--dv-tile-w);border:0}

.tl-card .tile{
  position:relative;flex:0 0 var(--dv-tile-w);width:var(--dv-tile-w);
  display:flex;flex-direction:column;align-items:center;justify-content:center;gap:2px;
  color:#fff;padding:6px 4px;box-sizing:border-box;
}
.tl-card .tile .tp{font-weight:400;font-size:13px;line-height:18px;opacity:.92}
.tl-card .tile .rn{font-weight:500;font-size:15px;line-height:20px}
.tl-card .tile .ics{display:flex;align-items:center;gap:3px;margin-top:2px}
.tl-card .tile .ics span{
  width:16px;height:16px;border-radius:9999px;display:inline-flex;align-items:center;justify-content:center;
  background:rgba(255,255,255,.22);
}
.tl-card .tile .ics svg{width:10px;height:10px;flex:0 0 auto}
.tl-card .tile .ok{
  width:20px;height:20px;border-radius:9999px;background:rgba(255,255,255,.28);
  display:inline-flex;align-items:center;justify-content:center;margin-top:2px;
}
.tl-card .tile .ok svg{width:12px;height:12px;flex:0 0 auto}

.tl-card[data-st="empty"]   .tile{background:var(--dv-empty)}
.tl-card[data-st="booked"]  .tile{background:var(--dv-booking)}
.tl-card[data-st="inhouse"] .tile{background:var(--dv-checkin)}
.tl-card[data-st="checkout"].tile,
.tl-card[data-st="checkout"] .tile{background:#B36BC0}
.tl-card[data-st="repair"]  .tile{background:var(--dv-repair-tile);color:var(--theme-text-secondary)}
.tl-card[data-st="repair"]  .tile .ok{background:rgba(47,43,61,.1)}
.tl-card[data-st="repair"]{background:var(--dv-repair-bg);border-color:transparent}

.tl-card .bd{flex:1 1 auto;min-width:0;padding:8px 10px;display:flex;flex-direction:column;justify-content:center;gap:1px}
.tl-card .bd .nm{display:flex;align-items:center;gap:6px;min-width:0}
.tl-card .bd .nm svg{width:16px;height:11px;flex:0 0 auto;border-radius:2px}
.tl-card .bd .nm b{font-weight:500;font-size:13px;line-height:20px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.tl-card .bd .dt{font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.tl-card .bd .sr{font-style:italic;font-weight:400;font-size:12px;line-height:16px;color:rgba(47,43,61,.55);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.tl-card .bd .pay{display:flex;align-items:center;justify-content:flex-end;gap:6px;margin-top:auto;font-weight:400;font-size:12px;line-height:16px;color:var(--theme-text-secondary)}
.tl-card .bd .pay .dot{width:8px;height:8px;border-radius:9999px;flex:0 0 auto}
.tl-card .bd .free{
  flex:1 1 auto;display:flex;align-items:center;justify-content:center;
  font-weight:500;font-size:15px;line-height:22px;color:var(--dv-empty);
}
.tl-card[data-st="repair"] .bd{background:var(--dv-repair-bg)}
.tl-card[data-st="repair"] .bd .nm b{color:var(--theme-text-primary)}

.tl-cards-empty{
  display:flex;flex-direction:column;align-items:center;justify-content:center;gap:12px;
  padding:64px 16px;color:var(--theme-text-secondary);
}
.tl-cards-empty svg{width:44px;height:44px;flex:0 0 auto;opacity:.5}

/* ================================================================
   Popup Thanh toán QR — Figma 48500:303200
   ================================================================ */
.qb-qrdlg{
  position:fixed;top:50%;left:50%;transform:translate(-50%,-50%);
  z-index:1296;width:771px;max-width:calc(100vw - 32px);max-height:calc(100vh - 32px);overflow:auto;
  background:var(--misc-paper);border-radius:var(--qb-radius);
  box-shadow:0 4px 18px rgba(47,43,61,.16);
  display:none;flex-direction:column;
  font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-primary);
}
.qb-qrdlg.is-open{display:flex}
.qb-qrdlg header{display:flex;align-items:center;gap:12px;padding:16px;border-bottom:1px solid var(--qb-border-soft)}
.qb-qrdlg header h2{margin:0;flex:1 1 auto;font-weight:500;font-size:18px;line-height:28px}
.qb-qrdlg .bd{padding:16px;display:flex;flex-direction:column;gap:12px}
.qb-qrdlg .ok-bar{
  display:flex;align-items:flex-start;gap:12px;padding:12px;border-radius:var(--qb-radius);
  background:rgba(40,199,111,.16);
}
.qb-qrdlg .ok-bar svg{width:24px;height:24px;flex:0 0 auto;color:var(--qb-success-dk)}
.qb-qrdlg .ok-bar span{flex:1 1 auto;font-weight:500;font-size:15px;line-height:22px}
.qb-qrdlg .qr-row{display:flex;align-items:center;justify-content:center;gap:24px;padding:12px}
.qb-qrdlg .qr-box{
  width:240px;height:240px;flex:0 0 240px;box-sizing:border-box;
  background:#fff;border:1px solid var(--qb-border-soft);border-radius:var(--qb-radius);
  display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;
}
.qb-qrdlg .qr-box .brand{display:flex;align-items:center;gap:0;letter-spacing:.2px;font-weight:700;font-size:11px;line-height:13px;color:#E11B22}
.qb-qrdlg .qr-box .brand i{font-style:normal;color:#0A3D8F}
.qb-qrdlg .qr-box svg{width:180px;height:180px;display:block;flex:0 0 auto}
.qb-qrdlg .qr-info{flex:1 1 auto;min-width:0;display:flex;flex-direction:column;gap:8px}
.qb-qrdlg .qr-info .r{display:flex;align-items:flex-start;gap:12px}
.qb-qrdlg .qr-info .r .k{width:100px;flex:0 0 100px;font-weight:400;font-size:15px;line-height:24px;color:var(--theme-text-secondary)}
.qb-qrdlg .qr-info .r .v{flex:1 1 auto;min-width:0;font-weight:500;font-size:15px;line-height:24px;color:var(--theme-text-primary);word-break:break-word}
.qb-qrdlg .qr-info hr{border:0;border-top:1px solid var(--qb-border-soft);margin:0;width:100%}
.qb-qrdlg .qr-info .note{font-weight:400;font-size:13px;line-height:20px;color:rgba(47,43,61,.55)}
.qb-qrdlg .hint{font-weight:400;font-size:13px;line-height:20px;color:var(--color-palette-primary-main);text-align:center}
.qb-qrdlg footer{display:flex;align-items:center;justify-content:flex-end;gap:12px;padding:16px;border-top:1px solid var(--qb-border-soft)}
.qb-qrdlg footer button{
  height:38px;display:inline-flex;align-items:center;justify-content:center;gap:8px;
  padding:0 20px;border-radius:var(--qb-radius);cursor:pointer;font-family:inherit;
  font-weight:500;font-size:15px;line-height:22px;
}
.qb-qrdlg footer button svg{width:20px;height:20px;flex:0 0 auto}
.qb-qrdlg footer .ghost{border:1px solid rgba(47,43,61,.24);background:var(--misc-paper);color:var(--theme-text-secondary)}
.qb-qrdlg footer .ghost:hover{background:var(--qb-hover)}
.qb-qrdlg footer .primary{border:0;background:var(--color-palette-primary-main);color:#fff;box-shadow:0 2px 6px rgba(0,131,202,.3)}
.qb-qrdlg footer .primary:hover{background:#1560D8}
.qb-qrdlg footer button:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:2px}

.tl-card .tile .dv-fl{display:inline-flex;align-items:center;justify-content:center;width:16px;height:16px;flex:0 0 auto}
.tl-card .tile .dv-fl svg{width:14px;height:14px}
.tl-card .tile .dv-fl.grp{color:#7c4dff}
.tl-card .tile .dv-fl.light{color:#ff9f43}
.tl-card .tile .dv-fl.dirty{color:#ea5455}

/* ================================================================
   DANH SÁCH ĐẶT PHÒNG (R01.03 — List booking)
   Figma: 31443:41077 (man hinh) · 33311:18510 (body)
          33422:114094 (popup Cot hien thi)
   SRS:   R01.03 Danh sach dat phong v0.1
   Dung chung du lieu BOOKINGS/UNASSIGNED voi so do Timeline.
   ================================================================ */
:root{
  --lb-row-h: 40px;
  --lb-head-bg: #F6F7FB;
  --lb-border: var(--tl-divider);
  --lb-fs: 13px; --lb-lh: 20px;
  --lb-fs-md: 15px; --lb-lh-md: 22px;
}

.card[hidden], .lb-host[hidden]{display:none !important}

.lb-host{display:flex;flex-direction:column;min-height:0;overflow:hidden;padding:0;gap:0}
.lb{display:flex;flex-direction:column;min-height:0;flex:1 1 auto;font-weight:400;font-size:var(--lb-fs);line-height:var(--lb-lh);color:var(--theme-text-primary)}

/* ---------- Bộ lọc inline ---------- */
.lb-filters{flex:0 0 auto;padding:16px 20px;border-bottom:1px solid var(--lb-border);display:flex;flex-direction:column;gap:12px}
.lb-title{display:flex;align-items:center;gap:12px;flex-wrap:wrap}
.lb-titlebtn{
  display:inline-flex;align-items:center;gap:8px;border:0;background:none;cursor:pointer;padding:2px 6px;
  border-radius:var(--tl-radius-group);font-family:inherit;
  font-weight:500;font-size:18px;line-height:26px;color:var(--theme-text-primary);
}
.lb-titlebtn:hover{background:var(--tl-hover-bg)}
.lb-titlebtn svg{width:20px;height:20px;flex:0 0 auto;color:var(--theme-text-secondary);transition:transform var(--tl-dur-fast) var(--tl-ease)}
.lb-titlebtn[aria-expanded="true"] svg{transform:rotate(180deg)}
.lb-titlebtn:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:1px}
.lb-desc{font-weight:400;font-size:var(--lb-fs-md);line-height:var(--lb-lh-md);color:var(--theme-text-secondary)}

.lb-frow{display:flex;align-items:flex-end;gap:16px;flex-wrap:wrap}
.lb-f{display:flex;flex-direction:column;gap:4px;min-width:0}
.lb-f > .lb-lb{font-weight:400;font-size:var(--lb-fs);line-height:var(--lb-lh);color:var(--theme-text-secondary)}
.lb-f.w-s{width:270px}.lb-f.w-m{width:270px}.lb-f.w-l{width:270px}
.lb-ctrl{
  width:100%;height:38px;box-sizing:border-box;padding:0 12px;
  border:1px solid rgba(47,43,61,.22);border-radius:var(--tl-radius-group);
  background:var(--misc-paper);color:var(--theme-text-primary);
  font-family:inherit;font-weight:400;font-size:var(--lb-fs);line-height:var(--lb-lh);
}
.lb-ctrl::placeholder{color:rgba(47,43,61,.4)}
.lb-ctrl:focus{outline:none;border-color:var(--color-palette-primary-main);box-shadow:0 0 0 2px var(--tl-primary-o-light)}
.lb-inwrap{position:relative;display:block;width:100%}
.lb-inwrap .pre,.lb-inwrap .suf{
  position:absolute;top:50%;transform:translateY(-50%);display:inline-flex;color:var(--theme-text-secondary);
}
.lb-inwrap .pre{left:11px}
.lb-inwrap .suf{right:8px;border:0;background:none;cursor:pointer;padding:4px;border-radius:var(--tl-radius-group)}
.lb-inwrap .suf:hover{background:var(--tl-hover-bg)}
.lb-inwrap svg{width:16px;height:16px;flex:0 0 auto}
.lb-inwrap.has-pre .lb-ctrl{padding-left:34px}
.lb-inwrap.has-suf .lb-ctrl{padding-right:36px}

.lb-addf{
  display:inline-flex;align-items:center;gap:6px;height:38px;padding:0 8px;
  border:0;background:none;cursor:pointer;border-radius:var(--tl-radius-group);font-family:inherit;
  font-weight:500;font-size:var(--lb-fs-md);line-height:var(--lb-lh-md);color:var(--color-palette-primary-main);
}
.lb-addf:hover{background:var(--tl-primary-o-light)}
.lb-addf svg{width:18px;height:18px;flex:0 0 auto}

.lb-arow{display:flex;align-items:center;gap:12px}
.lb-btn{
  display:inline-flex;align-items:center;justify-content:center;gap:8px;height:38px;padding:0 16px;
  border-radius:var(--tl-radius-group);cursor:pointer;font-family:inherit;
  font-weight:500;font-size:var(--lb-fs);line-height:var(--lb-lh);
  border:1px solid rgba(47,43,61,.24);background:var(--misc-paper);color:var(--theme-text-primary);
  transition:background-color var(--tl-dur-fast) var(--tl-ease);
}
.lb-btn:hover{background:var(--tl-hover-bg)}
.lb-btn.primary{border:0;background:var(--color-palette-primary-main);color:#fff;box-shadow:0 2px 6px rgba(0,131,202,.3)}
.lb-btn.primary:hover{background:#1560D8}
.lb-btn svg{width:18px;height:18px;flex:0 0 auto}
.lb-btn:focus-visible{outline:2px solid var(--color-palette-primary-main);outline-offset:2px}

/* ---------- Thanh công cụ bảng ---------- */
.lb-tbar{flex:0 0 auto;display:flex;align-items:center;gap:12px;padding:12px 20px}
.lb-tbar .sp{flex:1 1 auto}
.lb-tbtns{display:inline-flex;gap:12px;flex:0 0 auto}
.lb-bulk{
  display:none;align-items:center;gap:12px;padding:6px 12px;border-radius:var(--tl-radius-group);
  background:var(--tl-primary-o-light);color:var(--color-palette-primary-main);font-weight:500;
}
.lb-bulk.is-on{display:inline-flex}
.lb-bulk .lb-btn{height:30px;padding:0 12px}

/* ---------- Bảng ---------- */
.lb-scroll{flex:1 1 auto;min-height:0;overflow:auto;border-top:1px solid var(--lb-border)}
.lb-table{width:100%;border-collapse:separate;border-spacing:0;table-layout:auto}
.lb-table th,.lb-table td{
  padding:0 12px;height:var(--lb-row-h);box-sizing:border-box;
  border-bottom:1px solid var(--lb-border);white-space:nowrap;text-align:left;vertical-align:middle;
}
.lb-table thead th{
  position:sticky;top:0;z-index:3;background:var(--lb-head-bg);
  font-weight:500;font-size:12px;line-height:16px;letter-spacing:.3px;text-transform:uppercase;
  color:var(--theme-text-secondary);user-select:none;
}
.lb-table thead th.sortable{cursor:pointer}
.lb-table thead th.sortable:hover{color:var(--theme-text-primary)}
.lb-table thead th .sortic{display:inline-flex;margin-left:4px;vertical-align:-2px;opacity:.4}
.lb-table thead th.is-sorted .sortic{opacity:1;color:var(--color-palette-primary-main)}
.lb-table thead th .sortic svg{width:12px;height:12px;flex:0 0 auto}
.lb-table td{font-weight:400;font-size:var(--lb-fs);line-height:var(--lb-lh);color:var(--theme-text-primary)}
.lb-table td.num{text-align:right;font-variant-numeric:tabular-nums}
.lb-table th.num{text-align:right}
.lb-table tbody tr.lb-row{cursor:pointer;transition:background-color var(--tl-dur-fast) var(--tl-ease)}
.lb-table tbody tr.lb-row:hover{background:var(--tl-hover-bg)}
.lb-table tbody tr.lb-row.is-sel{background:var(--tl-primary-o-light)}
.lb-table tbody tr.lb-grp{background:#F1F1F3;font-weight:500}
.lb-table tbody tr.lb-grp td{color:var(--theme-text-primary);font-weight:500}
.lb-table tbody tr.lb-grp .cr{
  display:inline-flex;align-items:center;justify-content:center;width:24px;height:24px;
  border:0;background:none;cursor:pointer;color:var(--theme-text-secondary);border-radius:var(--tl-radius-group);
}
.lb-table tbody tr.lb-grp .cr svg{width:16px;height:16px;flex:0 0 auto;transition:transform var(--tl-dur-fast) var(--tl-ease)}
.lb-table tbody tr.lb-grp.is-closed .cr svg{transform:rotate(-90deg)}
.lb-table td.tag-cell{position:relative}
.lb-table tbody tr.lb-row td:first-child{position:relative}
.lb-table tbody tr.lb-row.in-grp td:first-child::before{
  content:"";position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--lb-tag,transparent);
}
.lb-ck{width:16px;height:16px;accent-color:var(--color-palette-primary-main);cursor:pointer;margin:0;vertical-align:middle}
.lb-code{color:var(--color-palette-primary-main);font-weight:500}
.lb-chip{
  display:inline-flex;align-items:center;gap:6px;height:24px;padding:0 10px;border-radius:9999px;
  font-weight:500;font-size:12px;line-height:16px;
}
.lb-chip .dot{width:6px;height:6px;border-radius:9999px;flex:0 0 auto;background:currentColor}
.lb-empty{padding:56px 20px;text-align:center;color:var(--theme-text-secondary)}

/* ---------- Footer / phân trang ---------- */
.lb-foot{
  flex:0 0 auto;display:flex;align-items:center;gap:12px;flex-wrap:wrap;
  padding:12px 20px;border-top:1px solid var(--lb-border);
}
.lb-foot .k{color:var(--theme-text-secondary)}
.lb-foot .sp{flex:1 1 auto}
.lb-pager{display:inline-flex;gap:4px}
.lb-pager button{
  min-width:32px;height:32px;padding:0 6px;border-radius:var(--tl-radius-group);
  border:1px solid transparent;background:none;cursor:pointer;color:var(--theme-text-primary);
  font-family:inherit;font-weight:400;font-size:var(--lb-fs);line-height:var(--lb-lh);
}
.lb-pager button:hover:not(:disabled){background:var(--tl-hover-bg)}
.lb-pager button[aria-current="page"]{background:var(--color-palette-primary-main);color:#fff;font-weight:500}
.lb-pager button:disabled{opacity:.38;cursor:not-allowed}
.lb-pager button svg{width:14px;height:14px;flex:0 0 auto}

/* ---------- Panel Cột hiển thị (Figma 33422:114094) ---------- */
.lb-drawer{
  position:fixed;top:0;right:0;height:100%;width:480px;max-width:100vw;z-index:1266;
  background:var(--misc-paper);box-shadow:-8px 0 32px rgba(47,43,61,.24);
  display:none;flex-direction:column;
  font-weight:400;font-size:var(--lb-fs);line-height:var(--lb-lh);color:var(--theme-text-primary);
}
.lb-drawer.is-open{display:flex}
.lb-drawer header{padding:16px;display:flex;flex-direction:column;gap:12px;border-bottom:1px solid var(--lb-border)}
.lb-drawer header .hd{display:flex;align-items:center;gap:12px}
.lb-drawer header h2{margin:0;flex:1 1 auto;font-weight:500;font-size:18px;line-height:28px}
.lb-drawer .bd{flex:1 1 auto;overflow:auto;padding:0 0 8px}
.lb-drawer .grp{padding:16px}
.lb-drawer .grp > h3{margin:0 0 8px;font-weight:500;font-size:var(--lb-fs-md);line-height:var(--lb-lh-md)}
.lb-drawer .list{display:grid;grid-template-columns:1fr 1fr;gap:4px 12px}
.lb-drawer label.opt{
  display:flex;align-items:center;gap:8px;min-height:36px;padding:0 8px;border-radius:var(--tl-radius-group);cursor:pointer;
}
.lb-drawer label.opt:hover{background:var(--tl-hover-bg)}
.lb-drawer label.opt.is-lock{opacity:.6;cursor:not-allowed}
.lb-drawer footer{display:flex;align-items:center;justify-content:flex-end;gap:12px;padding:16px;border-top:1px solid var(--lb-border)}
.lb-bd{position:fixed;inset:0;background:rgba(47,43,61,.5);z-index:1264;display:none}
.lb-bd.is-open{display:block}

/* ---------- Menu chọn danh sách mẫu ---------- */
.lb-lists{
  position:fixed;z-index:1268;display:none;min-width:320px;max-height:360px;overflow:auto;
  background:var(--misc-paper);border-radius:var(--tl-radius-group);
  box-shadow:0 4px 18px rgba(47,43,61,.22);padding:8px;
}
.lb-lists.is-open{display:block}
.lb-lists .h{padding:6px 10px;font-weight:500;color:var(--theme-text-secondary)}
.lb-lists button{
  display:flex;flex-direction:column;gap:2px;width:100%;text-align:left;border:0;background:none;cursor:pointer;
  padding:8px 10px;border-radius:var(--tl-radius-group);font-family:inherit;color:var(--theme-text-primary);
}
.lb-lists button:hover{background:var(--tl-hover-bg)}
.lb-lists button[aria-selected="true"]{background:var(--tl-primary-o-main);color:var(--color-palette-primary-main)}
.lb-lists button b{font-weight:500;font-size:var(--lb-fs-md);line-height:var(--lb-lh-md)}
.lb-lists button span{font-weight:400;font-size:var(--lb-fs);line-height:var(--lb-lh);color:var(--theme-text-secondary)}
.lb-lists button[aria-selected="true"] span{color:var(--color-palette-primary-main);opacity:.8}

/* ================================================================
   17. MAN CAU HINH SO DO PHONG
   Figma: 41697:138310 (menu + cau hinh), 41697:167156 (layout),
          41765:97267 (RowTable/Setting)
   Ke thua bien --tl, --qb, --theme cua shell v8.
   ================================================================ */
.cfg{
  --cfg-head-bg:#f8f7fa;
  --cfg-div:rgba(47,43,61,.12);
  display:flex;flex-direction:column;min-height:0;overflow:hidden;
}
.cfg[hidden]{display:none !important}

.cfg-head{
  display:flex;align-items:flex-start;gap:16px;
  padding:16px 20px 12px;border-bottom:1px solid var(--cfg-div);
}
.cfg-ttl{flex:1;min-width:0}
.cfg-ttl h1{margin:0;font-size:18px;line-height:26px;font-weight:600;color:var(--theme-text-primary);letter-spacing:.1px}
.cfg-ttl p{margin:2px 0 0;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.cfg-acts{display:flex;align-items:center;gap:8px;flex:0 0 auto}
.cfg-btn{
  height:38px;padding:0 20px;border:0;border-radius:var(--tl-radius-sm,8px);
  background:var(--misc-avatar-bg);color:var(--theme-text-secondary);
  font-size:14px;line-height:20px;font-weight:500;cursor:pointer;
}
.cfg-btn:hover{background:var(--theme-action-selected)}
.cfg-btn.primary{background:var(--tl-brand,#186FF4);color:#fff;border-color:var(--tl-brand,#186FF4)}
.cfg-btn.primary:hover{background:#125fd6;border-color:#125fd6}

.cfg-tabs{display:flex;align-items:stretch;gap:2px;padding:0 20px;border-bottom:1px solid var(--cfg-div);overflow-x:auto}
.cfg-tab{
  position:relative;flex:0 0 auto;height:44px;padding:0 16px;background:transparent;border:0;
  font-size:14px;line-height:22px;font-weight:500;color:var(--theme-text-secondary);cursor:pointer;
  border-radius:8px 8px 0 0;white-space:nowrap;
}
.cfg-tab:hover{background:rgba(24,111,244,.06);color:var(--tl-brand,#186FF4)}
.cfg-tab.is-on{color:var(--tl-brand,#186FF4)}
.cfg-tab.is-on::after{content:"";position:absolute;left:8px;right:8px;bottom:-1px;height:2px;background:var(--tl-brand,#186FF4);border-radius:9999px}

.cfg-body{flex:1;min-height:0;display:flex;align-items:stretch;gap:0}
.cfg-main{flex:1;min-width:0;padding:16px 20px 24px;overflow:auto}
.cfg-prev{
  flex:0 0 360px;width:360px;border-left:1px solid var(--cfg-div);
  padding:16px;display:flex;flex-direction:column;gap:12px;overflow:auto;background:#fbfcfe;
}
.cfg-prev-h{display:flex;align-items:center;justify-content:space-between;gap:8px}
.cfg-prev-h h3{margin:0;font-size:13px;line-height:24px;font-weight:600;text-transform:uppercase;letter-spacing:.2px;color:var(--theme-text-secondary)}
.cfg-prev-tag{font-size:12px;line-height:20px;padding:0 10px;border-radius:9999px;background:rgba(24,111,244,.1);color:var(--tl-brand,#186FF4);font-weight:500}
.cfg-prev-bd{display:flex;flex-direction:column;gap:10px}
.cfg-prev-note{margin:0;font-size:12px;line-height:18px;color:var(--theme-text-secondary)}

.cfg-filters{display:flex;align-items:flex-end;gap:12px;flex-wrap:wrap;margin-bottom:16px}
.cfg-f{flex:1 1 240px;min-width:200px;display:flex;flex-direction:column;gap:4px}
.cfg-lb{font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.cfg-clear{
  height:38px;padding:0 12px;border-radius:8px;border:1px solid var(--theme-input-border,#dbdade);
  background:#fff;color:var(--theme-text-secondary);font-size:13px;cursor:pointer;flex:0 0 auto;
}
.cfg-clear:hover{color:var(--tl-brand,#186FF4);border-color:var(--tl-brand,#186FF4)}

/* ---- Nhom (accordion) ---- */
.cfg-grp{border:1px solid var(--cfg-div);border-radius:12px;overflow:hidden;margin-bottom:12px;background:#fff}
.cfg-grp-h{
  display:flex;align-items:center;gap:10px;width:100%;
  padding:10px 14px;background:var(--cfg-head-bg);border:0;cursor:pointer;text-align:left;
}
.cfg-grp-h .nm{flex:1;min-width:0;font-size:14px;line-height:22px;font-weight:600;color:var(--theme-text-primary)}
.cfg-grp-h .cnt{font-size:12px;line-height:20px;color:var(--theme-text-secondary)}
.cfg-grp-h .cv{width:18px;height:18px;flex:0 0 auto;color:var(--theme-text-secondary);transition:transform .18s ease}
.cfg-grp.is-closed .cfg-grp-h .cv{transform:rotate(-90deg)}
.cfg-grp.is-closed .cfg-tbl-wrap{display:none}
.cfg-tbl-wrap{overflow-x:auto}
.cfg-tbl{width:100%;border-collapse:separate;border-spacing:0;font-size:13px}
.cfg-tbl th{
  padding:8px 12px;text-align:left;font-size:13px;line-height:24px;font-weight:500;
  text-transform:uppercase;letter-spacing:.2px;color:var(--theme-text-secondary);
  background:var(--cfg-head-bg);border-bottom:1px solid var(--cfg-div);white-space:nowrap;
}
.cfg-tbl td{padding:8px 12px;border-bottom:1px solid var(--cfg-div);line-height:22px;color:var(--theme-text-primary);vertical-align:middle}
.cfg-tbl tr:last-child td{border-bottom:0}
.cfg-tbl .c-tog{width:90px;text-align:center}
.cfg-tbl .c-def{width:240px}
.cfg-tbl .fname{display:flex;align-items:center;gap:6px}
.cfg-lock{
  font-size:11px;line-height:18px;padding:0 8px;border-radius:9999px;
  background:rgba(47,43,61,.08);color:var(--theme-text-secondary);
}
.cfg-hint{display:block;font-size:12px;line-height:18px;color:var(--theme-text-secondary)}

/* ---- Checkbox theo Figma RowTable/Setting ---- */
.cfg-ck{display:inline-flex;align-items:center;justify-content:center;cursor:pointer}
.cfg-ck input{position:absolute;opacity:0;width:0;height:0}
.cfg-ck .bx{
  width:18px;height:18px;border-radius:4px;border:2px solid rgba(47,43,61,.45);
  display:flex;align-items:center;justify-content:center;color:#fff;transition:.15s ease;
}
.cfg-ck .bx svg{width:12px;height:12px;opacity:0}
.cfg-ck input:checked + .bx{background:var(--tl-brand,#186FF4);border-color:var(--tl-brand,#186FF4)}
.cfg-ck input:checked + .bx svg{opacity:1}
.cfg-ck input:focus-visible + .bx{box-shadow:0 0 0 3px rgba(24,111,244,.25)}
.cfg-ck input:disabled + .bx{background:#808390;border-color:#808390;opacity:.45}
.cfg-ck input:disabled{cursor:not-allowed}

.cfg-sel{
  width:100%;height:34px;padding:0 30px 0 10px;border-radius:8px;
  border:1px solid rgba(47,43,61,.22);background:#fff;color:var(--theme-text-primary);
  font-family:inherit;font-size:13px;line-height:22px;cursor:pointer;
  appearance:none;-webkit-appearance:none;
  background-image:url("data:image/svg+xml;charset=utf-8,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%232f2b3d' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3E%3Cpath d='M6 9l6 6 6-6'/%3E%3C/svg%3E");
  background-repeat:no-repeat;background-position:right 8px center;background-size:16px 16px;
}
.cfg-sel:focus{outline:none;border-color:var(--tl-brand,#186FF4);box-shadow:0 0 0 3px rgba(24,111,244,.15)}
.cfg-sel:disabled{background-color:rgba(47,43,61,.04);color:var(--theme-text-secondary);cursor:not-allowed}
.cfg-dash{color:var(--theme-text-secondary)}

.cfg-empty{padding:32px 12px;text-align:center;color:var(--theme-text-secondary);font-size:13px}

/* ---- Xem truoc ---- */
/* ---- Xem trước sơ đồ timeline: bản thu nhỏ đúng cấu trúc lưới thật ---- */
.cfg-tl{
  --cfg-tl-room:88px;      /* bề rộng cột phòng */
  --cfg-tl-col:47px;       /* bề rộng mỗi cột ngày */
  --cfg-tl-row:28px;       /* chiều cao mỗi hàng */
  border:1px solid var(--cfg-div);border-radius:8px;background:#fff;overflow:hidden;
}
.cfg-tl table{width:100%;border-collapse:separate;border-spacing:0;table-layout:fixed;font-size:11px;line-height:16px}
.cfg-tl col.rc{width:var(--cfg-tl-room)}
.cfg-tl col.dc{width:var(--cfg-tl-col)}
.cfg-tl th,.cfg-tl td{
  height:var(--cfg-tl-row);padding:0 6px;
  border-right:1px solid var(--cfg-div);border-bottom:1px solid var(--cfg-div);
  color:var(--theme-text-primary);font-weight:400;text-align:center;vertical-align:middle;
}
.cfg-tl tr:last-child td,.cfg-tl tr:last-child th{border-bottom:0}
.cfg-tl th:last-child,.cfg-tl td:last-child{border-right:0}
.cfg-tl th.rc,.cfg-tl td.rc{text-align:left;font-weight:400}

/* Hàng đầu: thứ + ngày */
.cfg-tl .hd th{height:48px;padding:4px 2px;vertical-align:top}
.cfg-tl .hd .wd{display:block;font-size:10px;line-height:14px;color:var(--theme-text-secondary)}
.cfg-tl .hd .dn{display:block;font-size:12px;line-height:16px;font-weight:600;color:var(--theme-text-primary)}
.cfg-tl .hd .ua{
  display:inline-flex;align-items:center;justify-content:center;width:15px;height:15px;margin-top:2px;
  border-radius:9999px;background:var(--tl-st-unassign,#D46B08);color:#fff;font-size:9px;font-weight:600;line-height:1;
}
.cfg-tl .hd th.wknd,.cfg-tl td.wknd{background:#fff8f8}

/* Hàng Tổng + hàng nhóm loại phòng */
.cfg-tl .sum td,.cfg-tl .sum th{background:#f2f2f4;color:var(--theme-text-secondary)}
.cfg-tl .sum th.rc{color:var(--theme-text-primary)}
.cfg-tl .grp td,.cfg-tl .grp th{background:#fafafb;color:var(--theme-text-secondary)}
.cfg-tl .grp th.rc{display:flex;align-items:center;gap:2px;height:var(--cfg-tl-row);color:var(--theme-text-primary)}
.cfg-tl .grp th.rc svg{width:12px;height:12px;flex:0 0 auto;color:var(--theme-text-secondary)}

/* Hàng phòng */
.cfg-tl .rm th.rc{display:flex;align-items:center;gap:3px;height:var(--cfg-tl-row)}
.cfg-tl .rm th.rc .nm{flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.cfg-tl .rm th.rc svg{width:11px;height:11px;flex:0 0 auto}
.cfg-tl .fl-light{color:var(--tl-st-light,#BFA210)}
.cfg-tl .fl-dirty{color:var(--tl-st-dirty,#B56613)}
.cfg-tl td.dc{position:relative;padding:0}

/* Thanh booking thu nhỏ */
.cfg-tl .bar{
  position:absolute;top:4px;height:20px;display:flex;align-items:center;gap:3px;
  padding:0 4px;border-radius:9999px;color:#fff;overflow:hidden;z-index:1;
}
.cfg-tl .bar[data-st="inhouse"]{background:var(--tl-bar-inhouse,#D96262)}
.cfg-tl .bar[data-st="booked"]{background:var(--tl-bar-booked,#4983DC)}
.cfg-tl .bar[data-st="checkout"]{background:var(--tl-bar-checkout,#EFBFF0);color:rgba(47,43,61,.9)}
.cfg-tl .bar .grp{width:14px;height:14px;border-radius:9999px;flex:0 0 auto;
  display:flex;align-items:center;justify-content:center;background:rgba(0,0,0,.22)}
.cfg-tl .bar .grp svg{width:9px;height:9px;color:#fff}
.cfg-tl .bar .ava{width:15px;height:15px;border-radius:9999px;flex:0 0 auto;background:#2f5fa8;color:#fff;
  display:flex;align-items:center;justify-content:center;font-size:8px;font-weight:600;letter-spacing:0}
.cfg-tl .bar[data-st="checkout"] .ava{background:#7B3E9D}
.cfg-tl .bar .lbl{flex:1;min-width:0;font-size:10px;line-height:14px;
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.cfg-tl .bar .pay{width:11px;height:11px;flex:0 0 auto;border-radius:9999px;border:2px solid #fff;box-sizing:border-box}
.cfg-tl .bar[data-st="checkout"] .pay{border-color:rgba(47,43,61,.55)}

.cfg-pv-row{display:flex;align-items:center;gap:8px;font-size:12px;line-height:20px;color:var(--theme-text-secondary)}
.cfg-pv-row b{font-weight:500;color:var(--theme-text-primary)}

.cfg-pv-card{border:1px solid var(--cfg-div);border-radius:12px;overflow:hidden;background:#fff}
.cfg-pv-card .tile{
  display:flex;align-items:center;gap:6px;padding:8px 10px;background:#e8f0ff;
  font-size:12px;line-height:20px;color:var(--theme-text-primary);
}
.cfg-pv-card .tile .rn{font-weight:600;font-size:14px}
.cfg-pv-card .tile .tp{color:var(--theme-text-secondary)}
.cfg-pv-card .tile .fl{margin-left:auto;display:flex;gap:4px}
.cfg-pv-card .bd{padding:8px 10px;display:flex;flex-direction:column;gap:2px;font-size:12px;line-height:19px}
.cfg-pv-card .bd .nm{font-weight:600;color:var(--theme-text-primary);display:flex;align-items:center;gap:5px}
.cfg-pv-card .bd .mt{color:var(--theme-text-secondary)}
.cfg-pv-flag{width:16px;height:12px;border-radius:2px;flex:0 0 auto}
.cfg-pv-off{color:var(--theme-text-secondary);font-size:12px;font-style:italic}

@media (max-width:1180px){
  .cfg-body{flex-direction:column}
  .cfg-prev{flex:0 0 auto;width:auto;border-left:0;border-top:1px solid var(--cfg-div)}
}

/* ================= Xem trước: SƠ ĐỒ CHI TIẾT (Figma 41789:95419) ================= */
.cfg-dv{
  display:flex;align-items:stretch;background:#fff;border-radius:8px;overflow:hidden;
  box-shadow:0 1px 4px rgba(47,43,61,.14);
}
.cfg-dv .tile{
  position:relative;flex:0 0 80px;width:80px;min-height:84px;
  display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;
  color:#fff;padding:8px 4px;
}
.cfg-dv .tile[data-st="booked"]{background:var(--tl-st-booked,#186FF4)}
.cfg-dv .tile[data-st="inhouse"]{background:var(--tl-st-inhouse,#DA4343)}
.cfg-dv .tile[data-st="empty"]{background:var(--tl-st-available,#309F61)}
.cfg-dv .tile[data-st="repair"]{background:#dedee1;color:rgba(47,43,61,.85)}
.cfg-dv .tile .tp{font-size:11px;line-height:16px;opacity:.92}
.cfg-dv .tile .rn{font-size:14px;line-height:20px;font-weight:600}
.cfg-dv .badge{
  position:absolute;top:0;left:0;min-width:20px;height:20px;padding:0 5px;
  display:flex;align-items:center;justify-content:center;
  background:#FF9F43;color:#fff;font-size:11px;font-weight:600;border-radius:0 0 8px 0;
}
.cfg-dv .ics,.cfg-sv .ics{display:flex;align-items:center;gap:3px;margin-top:3px}
.cfg-dv .ics span,.cfg-sv .ics span{
  width:16px;height:16px;border-radius:9999px;background:rgba(0,0,0,.22);
  display:flex;align-items:center;justify-content:center;
}
.cfg-dv .ics svg,.cfg-sv .ics svg{width:10px;height:10px;color:#fff}
.cfg-dv .ics .ok,.cfg-sv .ics .ok{background:rgba(255,255,255,.9)}
.cfg-dv .ics .ok svg,.cfg-sv .ics .ok svg{color:var(--tl-st-available,#309F61)}
.cfg-sv .tile[data-st="repair"] .ics .ok svg{color:var(--tl-st-available,#309F61)}
.cfg-dv .bd{flex:1;min-width:0;display:flex;flex-direction:column;gap:1px;padding:8px 10px}
.cfg-dv .bd .nm{display:flex;align-items:center;gap:5px;font-size:13px;line-height:20px;color:var(--theme-text-primary)}
.cfg-dv .bd .nm b{font-weight:600;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.cfg-dv .bd .fl{width:16px;height:12px;border-radius:2px;flex:0 0 auto}
.cfg-dv .bd .dt{font-size:12px;line-height:18px;color:var(--theme-text-secondary)}
.cfg-dv .bd .sr{font-size:12px;line-height:18px;color:var(--theme-text-secondary);font-style:italic}
.cfg-dv .bd .pay{
  display:flex;align-items:center;justify-content:flex-end;gap:5px;margin-top:4px;
  font-size:12px;line-height:18px;color:var(--theme-text-secondary);
}
.cfg-dv .bd .pay b{font-weight:600;color:var(--theme-text-primary)}
.cfg-dv .bd .pay u{text-decoration:none;color:var(--theme-text-secondary)}
.cfg-dv .bd .pay .dot{width:8px;height:8px;border-radius:9999px;background:var(--tl-pay-paid,#24B364);flex:0 0 auto}

/* ================= Xem trước: SƠ ĐỒ ĐƠN GIẢN (Figma 41789:98064) ================= */
.cfg-sv{display:flex;flex-wrap:wrap;gap:10px}
.cfg-sv .tile{
  position:relative;flex:0 0 74px;width:74px;min-height:78px;border-radius:8px;overflow:hidden;
  display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;
  color:#fff;padding:8px 4px;box-shadow:0 1px 4px rgba(47,43,61,.14);
}
.cfg-sv .tile[data-st="booked"]{background:var(--tl-st-booked,#186FF4)}
.cfg-sv .tile[data-st="inhouse"]{background:var(--tl-st-inhouse,#DA4343)}
.cfg-sv .tile[data-st="empty"]{background:var(--tl-st-available,#309F61)}
.cfg-sv .tile[data-st="repair"]{background:#dedee1;color:rgba(47,43,61,.85)}
.cfg-sv .tile .tp{font-size:11px;line-height:16px;opacity:.92}
.cfg-sv .tile .rn{font-size:14px;line-height:20px;font-weight:600}
.cfg-sv .badge{
  position:absolute;top:0;left:0;min-width:20px;height:20px;padding:0 5px;
  display:flex;align-items:center;justify-content:center;
  background:#FF9F43;color:#fff;font-size:11px;font-weight:600;border-radius:0 0 8px 0;
}

/* ================= Xem trước: THẺ XEM NHANH (Figma 41834:47500) ================= */
.cfg-qv{
  background:#fff;border-radius:12px;overflow:hidden;
  box-shadow:0 2px 10px rgba(47,43,61,.16);font-size:12px;line-height:18px;
}
.cfg-qv .hd{
  display:flex;align-items:center;gap:6px;padding:10px 12px;
  border-bottom:1px solid var(--cfg-div);
}
.cfg-qv .hd .gi{width:16px;height:16px;flex:0 0 auto;color:var(--tl-brand,#186FF4)}
.cfg-qv .hd .gi svg{width:16px;height:16px}
.cfg-qv .hd .nm{font-size:13px;line-height:20px;font-weight:600;color:var(--theme-text-primary);
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.cfg-qv .hd .x{margin-left:auto;width:16px;height:16px;flex:0 0 auto;color:var(--theme-text-secondary)}
.cfg-qv .hd .x svg{width:16px;height:16px}
.cfg-qv .lk{color:var(--tl-brand,#186FF4);font-weight:500;text-decoration:none;flex:0 0 auto}
.cfg-qv .chip{
  flex:0 0 auto;padding:1px 8px;border-radius:9999px;font-size:11px;line-height:18px;font-weight:500;
  background:rgba(40,199,111,.14);color:#1F9254;
}
.cfg-qv .bd{display:flex;align-items:stretch}
.cfg-qv .col{flex:1 1 50%;min-width:0;padding:10px 12px;display:flex;flex-direction:column;gap:7px}
.cfg-qv .col.cr{border-left:1px solid var(--cfg-div)}
.cfg-qv .col > .r{display:flex;align-items:center;gap:6px;min-width:0}
.cfg-qv .col > .r .ic{width:15px;height:15px;flex:0 0 auto;color:var(--theme-text-secondary);display:flex;align-items:center}
.cfg-qv .col > .r .ic svg{width:15px;height:15px}
.cfg-qv .col > .r .ic .fl{width:15px;height:11px;border-radius:2px}
.cfg-qv .col > .r .tx{flex:0 1 auto;min-width:0;display:flex;align-items:center;gap:4px;color:var(--theme-text-primary);
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.cfg-qv .col > .r .tx > span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.cfg-qv .col > .r .tx svg{width:12px;height:12px;flex:0 0 auto}
.cfg-qv .col > .r .rt{flex:0 0 auto;margin-left:auto;display:flex;align-items:center;gap:4px;color:var(--theme-text-secondary)}
.cfg-qv .col > .r .rt svg{width:13px;height:13px;flex:0 0 auto}
.cfg-qv .rm{display:flex;align-items:flex-start;gap:6px}
.cfg-qv .rm > svg{width:16px;height:16px;flex:0 0 auto;color:var(--tl-brand,#186FF4);margin-top:1px}
.cfg-qv .rm .t{display:flex;align-items:center;gap:5px;font-size:13px;line-height:20px}
.cfg-qv .rm .t b{font-weight:600;color:var(--theme-text-primary)}
.cfg-qv .lk-red{width:14px;height:14px;color:var(--tl-st-inhouse,#DA4343);display:flex}
.cfg-qv .lk-red svg{width:14px;height:14px}
.cfg-qv .rm .s{color:var(--theme-text-secondary)}
.cfg-qv .rm .s i{font-style:normal;opacity:.85}
.cfg-qv .pax{display:flex;align-items:center;gap:14px;color:var(--theme-text-secondary)}
.cfg-qv .pax span{display:flex;align-items:center;gap:4px}
.cfg-qv .pax svg{width:15px;height:15px}
.cfg-qv .money{margin-top:4px;background:#f4f4f6;border-radius:8px;padding:8px 10px;
  display:flex;flex-direction:column;gap:3px}
.cfg-qv .money .m{display:flex;align-items:center;justify-content:flex-end;gap:6px}
.cfg-qv .money .m b{font-weight:400;color:var(--theme-text-secondary)}
.cfg-qv .money .m span{font-weight:500;color:var(--theme-text-primary)}
.cfg-qv .money .m u{text-decoration:none;font-weight:400;color:var(--theme-text-secondary)}
.cfg-qv .money .m .red{color:var(--tl-st-inhouse,#DA4343)}
.cfg-qv .money .dot{width:8px;height:8px;border-radius:9999px;background:var(--tl-pay-partial,#FF9F43);flex:0 0 auto}

/* Thẻ xem nhanh rộng hơn cột Xem trước mặc định */
.cfg-prev.is-wide{flex:0 0 600px;width:600px}
@media (max-width:1400px){ .cfg-prev.is-wide{flex:0 0 460px;width:460px} }

/* ================================================================
   20. MAN CHI TIET DAT PHONG — FOOR03
   SRS: [FOOR03] Chi tiet dat phong v0.1 (3.1.4 / 3.1.5)
   Figma: 26641:35144 (booking doan) · 34214:112087 (booking le)
          26641:37082 (Thong tin chung) · 26641:38147 (Card phong)
          26641:36076 (Bulk action bar)
   Token ke thua: --ez-*, --tl-*, --theme-*, --misc-*
   ================================================================ */
.bd-backdrop{position:fixed;inset:0;background:rgba(47,43,61,.5);z-index:1240;display:none}
.bd-backdrop.is-open{display:block}

/* Slide-over chiem 90% man hinh, truot tu phai sang */
.bdw{
  --bd-div: rgba(47,43,61,.12);
  --bd-left: 400px;
  --bd-w: 90vw;
  position:fixed;top:0;right:0;height:100%;width:var(--bd-w);max-width:100vw;
  z-index:1250;background:var(--misc-paper);
  display:none;flex-direction:column;min-height:0;overflow:hidden;
  box-shadow:-8px 0 32px rgba(47,43,61,.24);
  transform:translateX(24px);opacity:0;
  transition:transform .18s var(--tl-ease),opacity .18s var(--tl-ease);
}
.bdw.is-open{display:flex;transform:translateX(0);opacity:1}
@media (prefers-reduced-motion: reduce){ .bdw{transition:none} }
.bdw[hidden]{display:none !important}
.bdw [hidden]{display:none !important}

/* ---------------- Header / Overview bar (SRS 3.1.4.2 muc 1-2) ---------------- */
.bd-head{flex:0 0 auto;padding:12px 20px 0;border-bottom:1px solid var(--bd-div)}
.bd-h1{display:flex;align-items:center;gap:10px;min-height:32px}
.bd-grpic{width:24px;height:24px;flex:0 0 auto;color:var(--ez-warning,#FF9F43)}
.bd-grpic svg{width:24px;height:24px}
.bd-name{
  font-size:18px;line-height:28px;font-weight:600;color:var(--theme-text-primary);
  max-width:460px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;
  border-radius:6px;padding:0 4px;margin-left:-4px;cursor:text;
}
.bd-name:hover{background:var(--ez-primary-soft)}
.bd-code{
  display:inline-flex;align-items:center;gap:6px;flex:0 0 auto;
  font-size:18px;line-height:28px;font-weight:600;color:var(--ez-primary);
  background:none;border:0;padding:0 4px;border-radius:6px;cursor:pointer;font-family:inherit;
}
.bd-code:hover{background:var(--ez-primary-soft)}
.bd-code svg{width:16px;height:16px;flex:0 0 auto;opacity:.75}
.bd-code.is-copied{color:var(--ez-success,#28C76F)}
.bd-chip{
  flex:0 0 auto;height:24px;padding:0 10px;border-radius:6px;border:0;cursor:default;
  display:inline-flex;align-items:center;gap:4px;
  font-family:inherit;font-size:13px;line-height:24px;font-weight:500;
}
.bd-chip svg{width:14px;height:14px;flex:0 0 auto}
.bd-chip[data-st="confirmed"]{background:rgba(40,199,111,.16);color:#1F9254}
.bd-chip[data-st="tentative"]{background:rgba(255,159,67,.18);color:#B54708}
.bd-chip[data-st="cancelled"]{background:rgba(255,76,81,.16);color:#C0343A}
.bd-chip.is-editable{cursor:pointer}
.bd-chip.is-editable:hover{filter:brightness(.95)}
.bd-h1 .sp{flex:1 1 auto;min-width:8px}

/* Hang 2: so khach · OTA · nguoi dat · con lai */
.bd-h2{display:flex;align-items:center;gap:16px;flex-wrap:wrap;min-height:28px;padding-bottom:8px;
  font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.bd-pax{display:inline-flex;align-items:center;gap:12px;flex:0 0 auto}
.bd-pax span{display:inline-flex;align-items:center;gap:4px;color:var(--theme-text-primary)}
.bd-pax svg{width:16px;height:16px;color:var(--theme-text-secondary)}
.bd-ota{display:inline-flex;align-items:center;gap:6px;flex:0 0 auto}
.bd-ota .lk{color:var(--ez-primary);font-weight:500;background:none;border:0;padding:0 2px;
  border-radius:4px;cursor:pointer;font-family:inherit;font-size:13px}
.bd-ota .lk:hover{background:var(--ez-primary-soft)}
.bd-ota .lk.is-copied{color:var(--ez-success,#28C76F)}
.bd-ota .nm{color:var(--theme-text-primary)}
.bd-booker{display:inline-flex;align-items:center;gap:6px;flex:0 0 auto;color:var(--theme-text-primary);position:relative}
.bd-booker .fl{width:17px;height:13px;border-radius:2px;flex:0 0 auto}
.bd-booker svg.lead{width:14px;height:14px;color:var(--ez-success,#28C76F)}
.bd-sep{color:var(--bd-div)}
.bd-left-amt{flex:0 0 auto;color:var(--theme-text-secondary)}
.bd-left-amt b{font-weight:500;color:var(--ez-error,#FF4C51)}

/* Thanh tinh nang ben phai header */
.bd-tools{display:flex;align-items:center;gap:8px;flex:0 0 auto}
.bd-search{
  display:inline-flex;align-items:center;gap:8px;height:var(--ez-tf-h-md,38px);width:230px;
  padding:0 var(--ez-tf-px-md,14px);border:1px solid var(--ez-tf-border,rgba(47,43,61,.22));
  border-radius:var(--ez-tf-r-md,6px);background:var(--misc-paper);color:var(--theme-text-secondary);cursor:pointer;
  font-family:inherit;font-size:14px;line-height:24px;
}
.bd-search{transition:border-color var(--ez-dur,100ms) var(--ez-ease),box-shadow var(--ez-dur,100ms) var(--ez-ease)}
.bd-search:hover{border-color:var(--ez-tf-hover,rgba(47,43,61,.6))}
.bd-search svg{width:16px;height:16px;flex:0 0 auto}
.bd-todo{
  position:relative;display:inline-flex;align-items:center;gap:6px;height:var(--ez-h-md,38px);
  padding:0 12px;border-radius:var(--ez-r,8px);border:0;cursor:pointer;font-family:inherit;
  background:rgba(40,199,111,.14);color:#1F9254;font-size:14px;line-height:22px;font-weight:500;
}
.bd-todo{transition:background-color var(--ez-dur,100ms) var(--ez-ease)}
.bd-todo:hover{background:rgba(40,199,111,.22)}
.bd-todo svg{width:18px;height:18px;flex:0 0 auto}
.bd-todo .badge{
  position:absolute;top:-6px;right:-6px;min-width:18px;height:18px;padding:0 5px;border-radius:9999px;
  background:var(--ez-error,#FF4C51);color:#fff;font-size:11px;line-height:18px;font-weight:600;
}
.bd-ib{
  height:var(--ez-h-md,38px);width:var(--ez-h-md,38px);padding:0;flex:0 0 auto;
  display:inline-flex;align-items:center;justify-content:center;
  border:0;background:none;border-radius:var(--ez-ib-r-md,6px);
  color:var(--theme-text-secondary);cursor:pointer;
  transition:background-color var(--ez-dur,100ms) var(--ez-ease),color var(--ez-dur,100ms) var(--ez-ease);
}
.bd-ib svg{width:var(--ez-ib-ic-md,20px);height:var(--ez-ib-ic-md,20px)}
.bd-ib:hover{background:var(--ez-primary-soft);color:var(--ez-primary)}
.bd-ib:focus-visible{outline:2px solid var(--ez-primary);outline-offset:2px}
.bd-ib[aria-expanded="true"]{background:var(--ez-primary-soft);color:var(--ez-primary)}

/* ---------------- Sub tabs (SRS 3.1.4.2 muc 2.8) ---------------- */
.bd-tabs{display:flex;align-items:stretch;gap:4px;padding:0 20px}
.bd-tab{
  position:relative;flex:0 0 auto;height:40px;padding:0 12px;background:transparent;border:0;
  font-family:inherit;font-size:14px;line-height:22px;font-weight:500;
  color:var(--theme-text-secondary);cursor:pointer;border-radius:8px 8px 0 0;white-space:nowrap;
}
.bd-tab:hover{background:var(--ez-primary-soft);color:var(--ez-primary)}
.bd-tab.is-on{color:var(--ez-primary)}
.bd-tab.is-on::after{content:"";position:absolute;left:8px;right:8px;bottom:-1px;height:2px;
  background:var(--ez-primary);border-radius:9999px}
.bd-tab:focus-visible{outline:2px solid var(--ez-primary);outline-offset:-2px}

/* ---------------- Bulk action bar (Figma 26641:36076) ---------------- */
.bd-bulk{
  display:none;align-items:center;gap:8px;flex-wrap:wrap;
  padding:10px 20px;border-bottom:1px solid var(--bd-div);background:var(--misc-paper);
}
.bd-bulk.is-on{display:flex}
.bd-bulk .lb{font-size:14px;line-height:22px;font-weight:500;color:var(--theme-text-primary);flex:0 0 auto}
.bd-bulk .x{
  width:28px;height:28px;flex:0 0 auto;display:inline-flex;align-items:center;justify-content:center;
  border:0;background:none;border-radius:6px;color:var(--theme-text-secondary);cursor:pointer;
}
.bd-bulk .x:hover{background:var(--tl-hover-bg)}
.bd-bulk .x svg{width:18px;height:18px}
.bd-bulk .sp{flex:1 1 auto}
.bd-btn-ok{background:var(--ez-success,#28C76F) !important;color:#fff !important;border:0 !important}
.bd-btn-ok:hover:not(:disabled){background:#22A75E !important}

/* ---------------- Body 2 cot ---------------- */
.bd-body{flex:1 1 auto;min-height:0;display:flex;align-items:stretch}
.bd-col-l{
  flex:0 0 var(--bd-left);width:var(--bd-left);min-width:0;
  border-right:1px solid var(--bd-div);display:flex;flex-direction:column;min-height:0;
}
.bd-col-r{flex:1 1 auto;min-width:0;display:flex;flex-direction:column;min-height:0;overflow:auto;background:var(--misc-body-bg)}
.bdw.is-single .bd-col-l{display:none}

/* Bo loc cot trai */
.bd-lfilter{flex:0 0 auto;display:flex;align-items:center;gap:8px;padding:12px}
.bd-lfilter .bd-tf{flex:1 1 auto;min-width:0}
.bd-tf{
  display:inline-flex;align-items:center;gap:8px;height:var(--ez-tf-h-md,38px);
  padding:0 var(--ez-tf-px-md,14px);border:1px solid var(--ez-tf-border,rgba(47,43,61,.22));
  border-radius:var(--ez-tf-r-md,6px);background:var(--misc-paper);color:var(--theme-text-secondary);
}
.bd-tf:hover{border-color:var(--ez-tf-hover,rgba(47,43,61,.6))}
.bd-tf:focus-within{border:2px solid var(--ez-primary);padding:0 calc(var(--ez-tf-px-md,14px) - 1px);
  box-shadow:var(--ez-tf-shadow,0 2px 6px rgba(0,131,202,.3))}
.bd-tf svg{width:var(--ez-tf-ic-md,16px);height:var(--ez-tf-ic-md,16px);flex:0 0 auto}
.bd-tf input{
  flex:1 1 auto;min-width:0;border:0;background:none;padding:0;font-family:inherit;
  font-size:var(--ez-tf-fs-md,14px);line-height:24px;color:var(--theme-text-primary);
}
.bd-tf input:focus{outline:none}
.bd-tf input::placeholder{color:rgba(47,43,61,.4)}
.bd-lfilter .qb-vs{flex:0 0 152px;width:152px}

/* Danh sach phong cot trai */
.bd-list{flex:1 1 auto;min-height:0;overflow:auto;border-top:1px solid var(--bd-div)}
.bd-allrow,.bd-grprow{
  display:flex;align-items:center;gap:10px;width:100%;
  padding:8px 12px;border-bottom:1px solid var(--bd-div);background:var(--misc-paper);
  font-family:inherit;text-align:left;
}
.bd-allrow{font-size:14px;line-height:22px;font-weight:500;color:var(--theme-text-primary)}
.bd-grprow{cursor:pointer;border:0;border-bottom:1px solid var(--bd-div);
  font-size:14px;line-height:22px;font-weight:500;color:var(--theme-text-primary)}
.bd-grprow .nm{flex:1 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.bd-grprow .cv{width:18px;height:18px;flex:0 0 auto;color:var(--theme-text-secondary);transition:transform .18s ease}
.bd-grp.is-closed .bd-grprow .cv{transform:rotate(-90deg)}
.bd-grp.is-closed .bd-grp-body{display:none}

/* Card phong — Figma 26641:38147 */
.bd-card{
  display:flex;align-items:flex-start;gap:4px;width:100%;
  padding:12px;border:0;border-bottom:1px solid var(--bd-div);
  background:var(--misc-paper);text-align:left;font-family:inherit;cursor:pointer;
  transition:background-color var(--ez-dur,100ms) var(--ez-ease);
}
.bd-card:hover{background:var(--theme-action-hover,rgba(47,43,61,.06))}
.bd-card.is-focus{background:var(--ez-primary-soft)}
.bd-card:focus-visible{outline:2px solid var(--ez-primary);outline-offset:-2px}
.bd-card .sticon{width:22px;height:22px;flex:0 0 auto;margin-top:1px}
.bd-card .sticon svg{width:22px;height:22px}
.bd-card .bd-cbody{flex:1 1 auto;min-width:0;display:flex;flex-direction:column}
.bd-card .rn{
  font-size:15px;line-height:24px;font-weight:500;color:var(--theme-text-primary);
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap;
}
.bd-card .tm{display:flex;align-items:center;gap:4px;height:22px;
  font-size:13px;line-height:20px;color:var(--theme-text-primary);white-space:nowrap}
.bd-card .tm .sep{color:var(--theme-text-secondary)}
.bd-card .tm .n{color:rgba(47,43,61,.55)}
.bd-card .gu{display:flex;align-items:center;gap:4px;min-width:0}
.bd-card .gu .fl{width:17px;height:13px;border-radius:2px;flex:0 0 auto}
.bd-card .gu .nm{font-size:13px;line-height:20px;color:var(--theme-text-primary);
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.bd-card .gu svg.lead{width:16px;height:16px;flex:0 0 auto;color:var(--ez-success,#28C76F)}
.bd-empty{padding:28px 12px;text-align:center;color:var(--theme-text-secondary);font-size:13px}

/* Checkbox dung chung — Figma checkbox 18px radius 4 */
.bd-ck{display:inline-flex;align-items:center;justify-content:center;flex:0 0 auto;cursor:pointer}
.bd-ck input{position:absolute;opacity:0;width:0;height:0}
.bd-ck .bx{
  width:18px;height:18px;border-radius:4px;border:2px solid rgba(47,43,61,.4);
  display:flex;align-items:center;justify-content:center;color:#fff;
  transition:background-color var(--ez-dur,100ms) var(--ez-ease),border-color var(--ez-dur,100ms) var(--ez-ease);
}
.bd-ck .bx svg{width:12px;height:12px;opacity:0}
.bd-ck input:checked + .bx,.bd-ck input:indeterminate + .bx{
  background:var(--ez-primary);border-color:var(--ez-primary);box-shadow:0 2px 3px rgba(24,111,244,.3);
}
.bd-ck input:checked + .bx svg.ck,.bd-ck input:indeterminate + .bx svg.mi{opacity:1}
.bd-ck .bx svg.mi{opacity:0}
.bd-ck input:indeterminate + .bx svg.ck{opacity:0}
.bd-ck input:focus-visible + .bx{box-shadow:0 0 0 3px rgba(24,111,244,.25)}

/* ---------------- Cot phai: khoi Phong ---------------- */
.bd-pane{padding:4px;display:flex;flex-direction:column;gap:4px}
.bd-block{background:var(--misc-paper);border:1px solid var(--bd-div);border-radius:12px;overflow:hidden}
.bd-rhead{display:flex;align-items:center;gap:10px;padding:14px 16px}
.bd-rhead .cal{width:22px;height:22px;flex:0 0 auto;color:var(--ez-primary)}
.bd-rhead .cal svg{width:22px;height:22px}
.bd-rhead h2{margin:0;font-size:18px;line-height:26px;font-weight:600;color:var(--theme-text-primary);
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.bd-lock{width:22px;height:22px;flex:0 0 auto;border:0;background:none;padding:0;cursor:pointer;
  color:var(--ez-error,#FF4C51);border-radius:4px}
.bd-lock svg{width:20px;height:20px}
.bd-lock:hover{background:rgba(255,76,81,.1)}
.bd-lock.is-locked{color:var(--ez-error,#FF4C51)}
.bd-rhead .sp{flex:1 1 auto}

/* Bang thong tin phong */
.bd-fields{padding:0 16px 14px;display:flex;flex-direction:column;gap:10px}
.bd-f{display:flex;align-items:flex-start;gap:12px;font-size:14px;line-height:22px}
.bd-f > .k{flex:0 0 150px;width:150px;color:var(--theme-text-secondary)}
.bd-f > .v{flex:1 1 auto;min-width:0;color:var(--theme-text-primary);word-break:break-word}
.bd-f .v .muted{color:var(--theme-text-secondary)}
.bd-f .v .lk{color:var(--ez-primary);font-weight:500;background:none;border:0;padding:0;
  cursor:pointer;font-family:inherit;font-size:14px}
.bd-f .v .lk:hover{text-decoration:underline}
.bd-note{max-height:96px;overflow:auto}

/* Khoi khach */
.bd-guests{border-top:1px dashed var(--bd-div);padding:12px 16px 8px}
.bd-guests-h{display:flex;align-items:center;gap:14px;margin-bottom:8px}
.bd-guests-h h3{margin:0;font-size:14px;line-height:22px;font-weight:600;color:var(--theme-text-primary)}
.bd-guests-h .sp{flex:1 1 auto}
.bd-addg{display:inline-flex;align-items:center;gap:6px;border:0;background:none;padding:0 4px;height:28px;
  color:var(--ez-primary);font-family:inherit;font-size:14px;font-weight:500;cursor:pointer;border-radius:6px}
.bd-addg:hover{background:var(--ez-primary-soft)}
.bd-addg svg{width:16px;height:16px}
.bd-g{display:flex;align-items:flex-start;gap:10px;padding:8px 0;border-bottom:1px solid var(--bd-div)}
.bd-g:last-child{border-bottom:0}
.bd-g .gi{flex:1 1 auto;min-width:0;display:flex;flex-direction:column;gap:2px}
.bd-g .gn{display:flex;align-items:center;gap:6px;font-size:14px;line-height:22px;font-weight:500;
  color:var(--theme-text-primary)}
.bd-g .gn .fl{width:17px;height:13px;border-radius:2px;flex:0 0 auto}
.bd-g .gn svg.mk{width:15px;height:15px;flex:0 0 auto}
.bd-g .gm{display:flex;align-items:center;gap:6px;flex-wrap:wrap;
  font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.bd-g .gm span{display:inline-flex;align-items:center;gap:4px}
.bd-g .gm svg{width:14px;height:14px}
.bd-g .ga{display:flex;align-items:center;gap:2px;flex:0 0 auto}
.bd-gib{width:30px;height:30px;border:0;background:none;border-radius:4px;cursor:pointer;
  display:inline-flex;align-items:center;justify-content:center;color:var(--theme-text-secondary)}
.bd-gib svg{width:16px;height:16px}
.bd-gib:hover{background:var(--ez-primary-soft);color:var(--ez-primary)}
.bd-gib:focus-visible{outline:2px solid var(--ez-primary);outline-offset:1px}

/* ---------------- Tab Thong tin chung (Figma 26641:37082) ---------------- */
.bd-gen{padding:4px}
.bd-gen h2{margin:0 0 14px;font-size:18px;line-height:26px;font-weight:600;color:var(--theme-text-primary)}
.bd-gen-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px 40px}
.bd-gen-grid .bd-f{align-items:flex-start}
.bd-gen-grid .full{grid-column:1 / -1}
.bd-booker-line{display:flex;flex-direction:column;gap:2px}
.bd-booker-line .sub{font-size:13px;line-height:20px;color:var(--theme-text-secondary);
  display:flex;align-items:center;gap:6px}
.bd-booker-line .sub svg{width:14px;height:14px}

/* Edit inline */
.bd-ed{
  display:inline-flex;align-items:center;gap:6px;max-width:100%;
  border-radius:6px;padding:0 4px;margin-left:-4px;cursor:pointer;
  transition:background-color var(--ez-dur,100ms) var(--ez-ease);
}
.bd-ed:hover{background:var(--ez-primary-soft)}
.bd-ed .pen{width:15px;height:15px;flex:0 0 auto;color:var(--ez-primary);opacity:0}
.bd-ed:hover .pen{opacity:1}
.bd-ed .tx{min-width:0;overflow:hidden;text-overflow:ellipsis}
.bd-edin{
  width:100%;height:var(--ez-tf-h-md,38px);padding:0 var(--ez-tf-px-md,14px);
  border:2px solid var(--ez-primary);border-radius:var(--ez-tf-r-md,6px);
  background:var(--misc-paper);font-family:inherit;font-size:14px;line-height:24px;
  color:var(--theme-text-primary);box-shadow:var(--ez-tf-shadow,0 2px 6px rgba(0,131,202,.3));
}
.bd-edin:focus{outline:none}
textarea.bd-edin{height:auto;min-height:76px;padding:8px 14px;resize:vertical;line-height:22px}

/* Menu dung chung cua man chi tiet */
.bd-menu{
  position:fixed;z-index:1270;display:none;min-width:220px;padding:6px;
  background:var(--misc-paper);border-radius:8px;box-shadow:0 8px 28px rgba(47,43,61,.24);
}
.bd-menu.is-open{display:block}
.bd-menu button{
  display:flex;align-items:center;gap:10px;width:100%;padding:8px 10px;border:0;background:none;
  border-radius:6px;cursor:pointer;text-align:left;font-family:inherit;
  font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.bd-menu button:hover{background:var(--ez-primary-soft);color:var(--ez-primary)}
.bd-menu button:focus-visible{outline:2px solid var(--ez-primary);outline-offset:-2px}
.bd-menu button svg{width:18px;height:18px;flex:0 0 auto;color:var(--theme-text-secondary)}
.bd-menu button:hover svg{color:var(--ez-primary)}
.bd-menu button.danger,.bd-menu button.danger svg{color:var(--ez-error,#FF4C51)}
.bd-menu button.danger:hover{background:rgba(255,76,81,.08);color:var(--ez-error,#FF4C51)}
.bd-menu .sep{height:1px;margin:4px 6px;background:var(--bd-div)}

@media (max-width:1280px){
  .bdw{--bd-left:340px}
  .bd-gen-grid{grid-template-columns:1fr}
}

/* Dialog + toast phai noi len tren slide-over Chi tiet dat phong */
.bd-open .tl-backdrop{z-index:1260}
.bd-open .tl-dialog{z-index:1265}
.bd-open .tl-toasts{z-index:1280}

/* =====================================================================
   22. SỬA PHÒNG — Thiết lập / Điều chỉnh / Xoá  (Figma 42502:133780)
   Dialog 480px theo Figma; dùng lại nền .tl-dialog + tokens --ez-*.
   ===================================================================== */
.rp-dlg{width:480px;z-index:1216}   /* dưới #tlGenDlg (1220) để dialog Xung đột booking nổi lên trên */
.rp-dlg header{padding:16px}
.rp-dlg header h2{font-weight:500;font-size:18px;line-height:28px}
.rp-bd{padding:16px;gap:12px;max-height:none}
.rp-foot{padding:16px;gap:12px}
.rp-foot .rp-sp{flex:1 1 auto}

.rp-f{display:flex;flex-direction:column;gap:4px;min-width:0;flex:1 1 0}
.rp-lb{font-weight:400;font-size:13px;line-height:15px;color:var(--theme-text-primary)}
.rp-lb .req{color:var(--ez-error);font-style:normal}
.rp-row{display:flex;gap:10px;align-items:flex-start}
.rp-msg{font-weight:400;font-size:12px;line-height:16px;color:var(--ez-error)}
.rp-hr{border:0;border-top:1px solid var(--tl-divider);margin:4px 0 0}

/* --- Toggle Loại sửa (Toggle Button Colors, cao 30px) --- */
.rp-toggle{display:flex;width:100%;border:1px solid var(--ez-tf-border);border-radius:var(--ez-r);overflow:hidden}
.rp-toggle button{
  flex:1 1 0;min-width:0;height:30px;border:0;background:var(--misc-paper);cursor:pointer;
  font-family:inherit;font-weight:400;font-size:13px;line-height:18px;
  color:var(--theme-text-secondary);padding:0 8px;
  transition:background-color var(--ez-dur) var(--ez-ease),color var(--ez-dur) var(--ez-ease);
}
.rp-toggle button+button{border-left:1px solid var(--ez-tf-border)}
.rp-toggle button:hover{background:var(--tl-hover-bg)}
.rp-toggle button[aria-pressed="true"]{background:var(--ez-primary-soft);color:var(--ez-primary);font-weight:500}
.rp-toggle button:focus-visible{outline:2px solid var(--ez-primary);outline-offset:-2px}

/* --- Ô ngày giờ (select-date) --- */
.rp-dt{
  display:flex;align-items:center;justify-content:space-between;gap:8px;width:100%;
  height:var(--ez-tf-h-md);padding:0 var(--ez-tf-px-md);
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);
  background:var(--misc-paper);cursor:pointer;font-family:inherit;
  font-weight:400;font-size:var(--ez-tf-fs-md);line-height:22px;color:var(--theme-text-primary);
  transition:border-color var(--ez-dur) var(--ez-ease);
}
.rp-dt:hover{border-color:var(--ez-tf-hover)}
.rp-dt:focus-visible{outline:none;border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.rp-dt .v{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.rp-dt .ic{display:inline-flex;flex:0 0 auto;color:var(--theme-text-secondary)}
.rp-dt .ic svg{width:var(--ez-tf-ic-md);height:var(--ez-tf-ic-md)}
.rp-dlg.is-dateerr .rp-dt{border-color:var(--ez-error)}

/* --- Checkbox Vẫn mở bán phòng --- */
.rp-ck{display:inline-flex;align-items:center;gap:8px;cursor:pointer;user-select:none;align-self:flex-start}
.rp-ck input{position:absolute;opacity:0;width:0;height:0}
.rp-ck .box{
  width:18px;height:18px;flex:0 0 auto;border:2px solid var(--ez-tf-hover);border-radius:2px;
  display:inline-flex;align-items:center;justify-content:center;color:#fff;background:var(--misc-paper);
  transition:background-color var(--ez-dur) var(--ez-ease),border-color var(--ez-dur) var(--ez-ease);
}
.rp-ck .box svg{width:12px;height:12px;opacity:0;transition:opacity var(--ez-dur) var(--ez-ease)}
.rp-ck input:checked+.box{background:var(--ez-primary);border-color:var(--ez-primary)}
.rp-ck input:checked+.box svg{opacity:1}
.rp-ck input:focus-visible+.box{outline:2px solid var(--ez-primary);outline-offset:2px}
.rp-ck .lb{font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary)}

/* --- Textarea Lý do khác / Lý do xoá --- */
.rp-bd textarea{
  width:100%;min-height:100px;resize:vertical;padding:10px var(--ez-tf-px-md);
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);
  font-family:inherit;font-weight:400;font-size:var(--ez-tf-fs-md);line-height:22px;
  color:var(--theme-text-primary);background:var(--misc-paper);
}
.rp-bd textarea::placeholder{color:var(--theme-text-disabled)}
.rp-bd textarea:hover{border-color:var(--ez-tf-hover)}
.rp-bd textarea:focus{outline:none;border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.rp-bd textarea.is-error{border-color:var(--ez-error)}

/* --- Bảng thông tin trong popup Xoá --- */
.rp-info{margin:0;display:grid;grid-template-columns:120px 1fr;gap:10px 12px}
.rp-info dt{font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-secondary)}
.rp-info dd{margin:0;font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary)}

/* --- Nút Xoá sửa phòng (tonal error) + Xác nhận (error) --- */
.rp-del{
  height:var(--ez-h-md);padding:0 var(--ez-px-md);border:0;border-radius:var(--ez-r);cursor:pointer;
  font-family:inherit;font-weight:500;font-size:var(--ez-fs-md);line-height:var(--ez-lh-md);
  background:rgba(255,76,81,.08);color:var(--ez-error);
  transition:background-color var(--ez-dur) var(--ez-ease);
}
.rp-del:hover{background:rgba(255,76,81,.16)}
.rp-del:focus-visible{outline:2px solid var(--ez-error);outline-offset:2px}
.rp-danger{
  height:var(--ez-h-md);padding:0 var(--ez-px-md);border:0;border-radius:var(--ez-r);cursor:pointer;
  font-family:inherit;font-weight:500;font-size:var(--ez-fs-md);line-height:var(--ez-lh-md);
  background:var(--ez-error);color:#fff;
  transition:background-color var(--ez-dur) var(--ez-ease);
}
.rp-danger:hover{background:#E84348}
.rp-danger:focus-visible{outline:2px solid var(--ez-error);outline-offset:2px}

/* nút Hủy / Lưu trong 2 popup này dùng chuẩn Medium 38px */
.rp-foot .tl-btn-ghost,.rp-foot .tl-btn-primary{
  height:var(--ez-h-md);padding:0 var(--ez-px-md);border-radius:var(--ez-r);
  font-weight:500;font-size:var(--ez-fs-md);line-height:var(--ez-lh-md);
  display:inline-flex;align-items:center;justify-content:center;
}

/* select Lý do dùng lại engine Vuexy của Đặt phòng nhanh */
.rp-dlg .qb-vs{position:relative}

/* =====================================================================
   23. ĐỔI NGÀY Ở (R01.07) — Figma 50775:132643
   Modal 1400px + 2 popup con (Đổi đồng loạt 500px, Chi tiết thay đổi 600px)
   z-index: backdrop 1214 · dialog 1216 · sub-backdrop 1217 · sub 1218
   (đều dưới #tlGenDlg 1220 để các MSG_xx nổi lên trên)
   ===================================================================== */
.cd-bd{position:fixed;inset:0;background:rgba(47,43,61,.5);z-index:1214;display:none}
.cd-bd.is-open{display:block}
.cd-dlg{
  position:fixed;top:0;right:0;z-index:1216;
  width:90vw;max-width:100vw;height:100%;
  background:var(--misc-paper);border-radius:0;
  box-shadow:-8px 0 32px rgba(47,43,61,.24);
  display:none;flex-direction:column;overflow:hidden;
  transform:translateX(24px);opacity:0;
  transition:transform .18s var(--tl-ease),opacity .18s var(--tl-ease);
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.cd-dlg.is-open{display:flex;transform:translateX(0);opacity:1}

.cd-head{display:flex;align-items:center;gap:12px;padding:16px;border-bottom:1px solid var(--tl-divider);flex:0 0 auto}
.cd-head h2{margin:0;flex:1 1 auto;font-weight:500;font-size:18px;line-height:28px;color:var(--theme-text-primary)}
.cd-headacts{display:flex;align-items:center;gap:12px}
.cd-x{border:0;background:none;cursor:pointer;color:var(--theme-text-secondary);display:inline-flex;padding:4px;border-radius:var(--ez-r);flex:0 0 auto}
.cd-x:hover{background:var(--tl-hover-bg)}
.cd-x svg{width:20px;height:20px}

.cd-btn-out{
  height:var(--ez-h-md);padding:0 var(--ez-px-md);cursor:pointer;
  border:1px solid var(--ez-primary);border-radius:var(--ez-r);background:var(--misc-paper);
  font-family:inherit;font-weight:500;font-size:var(--ez-fs-md);line-height:var(--ez-lh-md);color:var(--ez-primary);
  transition:background-color var(--ez-dur) var(--ez-ease);
}
.cd-btn-out:hover:not(:disabled){background:var(--ez-primary-soft)}
.cd-btn-out:disabled{border-color:rgba(47,43,61,.22);color:var(--theme-text-disabled);cursor:default}
.cd-btn-out:focus-visible{outline:2px solid var(--ez-primary);outline-offset:2px}

/* --- thanh chọn nhiều dòng --- */
.cd-selbar{display:flex;align-items:center;gap:12px;padding:12px 16px;border-bottom:1px solid var(--tl-divider);flex:0 0 auto}
.cd-selbar[hidden]{display:none}
.cd-selbar span{font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.cd-selx{border:0;background:none;cursor:pointer;display:inline-flex;padding:2px;color:var(--theme-text-secondary);border-radius:var(--ez-ib-r-sm)}
.cd-selx:hover{background:var(--tl-hover-bg)}
.cd-selx svg{width:18px;height:18px}

/* --- băng cảnh báo xung đột --- */
.cd-errbar{display:flex;align-items:center;gap:8px;margin:12px 16px 0;padding:8px 12px;flex:0 0 auto;
  background:rgba(255,76,81,.08);border-radius:var(--ez-r);
  font-weight:500;font-size:14px;line-height:22px;color:var(--ez-error)}
.cd-errbar[hidden]{display:none}
.cd-errbar svg{width:20px;height:20px;flex:0 0 auto}

/* --- bảng --- */
.cd-scroll{flex:1 1 auto;overflow:auto;min-height:0}
.cd-tbl{width:100%;border-collapse:separate;border-spacing:0;table-layout:fixed}
.cd-tbl thead th{
  position:sticky;top:0;z-index:2;background:var(--misc-table-header-bg,#f6f7fb);
  padding:12px 8px;text-align:left;vertical-align:middle;
  font-weight:500;font-size:13px;line-height:18px;letter-spacing:.4px;text-transform:uppercase;
  color:var(--theme-text-secondary);border-bottom:1px solid var(--tl-divider);
}
.cd-tbl th.ck{width:44px;padding-left:16px}
.cd-tbl th.rm{width:150px}
.cd-tbl th.gt{width:170px}
.cd-tbl th.px{width:80px}
.cd-tbl th.dt{width:190px}
.cd-tbl th.nt{width:86px}
.cd-tbl th.pa{width:98px;text-align:center}
.cd-tbl th.mo{width:118px;text-align:right}
.cd-tbl th.ed{width:44px}
.cd-tbl td{padding:10px 8px;vertical-align:middle;border-bottom:1px solid var(--tl-divider);font-size:14px;line-height:22px}
.cd-tbl td.ck{padding-left:16px}
.cd-tbl td.mo{text-align:right;white-space:nowrap}
.cd-tbl td.pa{text-align:center}
.cd-tbl tr.cd-grp td{background:var(--misc-table-header-bg,#f6f7fb);font-weight:500;padding:8px}
.cd-tbl tr.cd-row:hover td{background:var(--tl-hover-bg)}
.cd-tbl tr.cd-row.is-sel td{background:var(--ez-primary-soft)}
.cd-tbl tr.cd-row.is-conf td{background:rgba(255,76,81,.04)}

.cd-grpname{display:inline-flex;align-items:center;gap:8px;border:0;background:none;cursor:pointer;font:inherit;font-weight:500;color:var(--theme-text-primary);padding:0}
.cd-grpname svg{width:18px;height:18px;transition:transform var(--ez-dur) var(--ez-ease)}
.cd-grpname.is-closed svg{transform:rotate(-90deg)}

.cd-room{display:inline-flex;align-items:center;gap:8px;min-width:0}
.cd-room .bed{width:20px;height:20px;flex:0 0 auto}
.cd-guestcell{display:inline-flex;align-items:center;gap:8px;min-width:0}
.cd-guestcell .tl-flag{width:20px;height:14px;border-radius:2px;flex:0 0 auto}
.cd-orig{display:block;font-size:12px;line-height:18px;color:var(--theme-text-secondary);margin-top:2px;white-space:nowrap}
.cd-nd{display:block;font-size:12px;line-height:18px;color:var(--ez-error);margin-top:2px}

/* ô ngày trong bảng */
.cd-cell-dt{
  display:flex;align-items:center;justify-content:space-between;gap:6px;width:100%;
  height:var(--ez-tf-h-md);padding:0 8px;cursor:pointer;white-space:nowrap;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);
  background:var(--misc-paper);font-family:inherit;font-size:14px;line-height:22px;color:var(--theme-text-primary);
  transition:border-color var(--ez-dur) var(--ez-ease);
}
.cd-cell-dt>span:first-child{white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.cd-cell-dt:hover:not(:disabled){border-color:var(--ez-tf-hover)}
.cd-cell-dt:disabled{background:var(--theme-action-selected);color:var(--theme-text-disabled);cursor:default}
.cd-cell-dt:focus-visible{outline:none;border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.cd-cell-dt svg{width:16px;height:16px;flex:0 0 auto;color:var(--theme-text-secondary)}

/* stepper số đêm */
.cd-num{display:inline-flex;align-items:center;gap:4px;width:64px;height:var(--ez-tf-h-md);padding:0 6px 0 10px;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);background:var(--misc-paper)}
.cd-num input{width:100%;min-width:0;border:0;background:none;outline:none;font-family:inherit;font-size:14px;line-height:22px;color:var(--theme-text-primary);padding:0}
.cd-num .sp{display:inline-flex;flex-direction:column;flex:0 0 auto}
.cd-num .sp button{border:0;background:none;cursor:pointer;padding:0;display:inline-flex;color:var(--theme-text-secondary);height:11px;align-items:center}
.cd-num .sp button:hover{color:var(--ez-primary)}
.cd-num .sp svg{width:12px;height:12px}
.cd-nwrap{display:flex;flex-direction:column;gap:4px;align-items:flex-start}
.cd-delta{font-size:14px;line-height:22px;color:var(--ez-error)}
.cd-delta.up{color:var(--ez-success)}

/* checkbox */
.cd-ck{display:inline-flex;align-items:center;cursor:pointer}
.cd-ck input{position:absolute;opacity:0;width:0;height:0}
.cd-ck .box{width:18px;height:18px;flex:0 0 auto;border:2px solid var(--ez-tf-hover);border-radius:2px;
  display:inline-flex;align-items:center;justify-content:center;color:#fff;background:var(--misc-paper);
  transition:background-color var(--ez-dur) var(--ez-ease),border-color var(--ez-dur) var(--ez-ease)}
.cd-ck .box svg{width:12px;height:12px;opacity:0}
.cd-ck input:checked+.box{background:var(--ez-primary);border-color:var(--ez-primary)}
.cd-ck input:checked+.box svg{opacity:1}
.cd-ck input:indeterminate+.box{background:var(--ez-primary);border-color:var(--ez-primary)}
.cd-ck input:indeterminate+.box svg{opacity:0}
.cd-ck input:indeterminate+.box::after{content:"";width:10px;height:2px;border-radius:1px;background:#fff}
.cd-ck input:disabled+.box{border-color:rgba(47,43,61,.16);background:var(--theme-action-selected);cursor:default}
.cd-ck input:focus-visible+.box{outline:2px solid var(--ez-primary);outline-offset:2px}

.cd-up{color:var(--ez-success)}
.cd-down{color:var(--ez-error)}
.cd-mute{color:var(--theme-text-secondary)}

.cd-edit{border:0;background:none;cursor:pointer;display:inline-flex;padding:4px;border-radius:var(--ez-ib-r-md);color:var(--theme-text-secondary)}
.cd-edit:hover{background:var(--tl-hover-bg);color:var(--ez-primary)}
.cd-edit svg{width:18px;height:18px}

/* dòng xung đột */
.cd-conf{display:flex;align-items:center;gap:8px;padding:8px 12px;border-radius:var(--ez-r);background:rgba(255,76,81,.08)}
.cd-conf svg{width:18px;height:18px;flex:0 0 auto;color:var(--ez-error)}
.cd-conf .tx{flex:1 1 auto;font-weight:500;font-size:14px;line-height:22px;color:var(--ez-error)}
.cd-conf .go{height:30px;padding:0 12px;display:inline-flex;align-items:center;gap:6px;cursor:pointer;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-r);background:var(--misc-paper);
  font-family:inherit;font-weight:500;font-size:12px;line-height:18px;color:var(--theme-text-primary)}
.cd-conf .go:hover{background:var(--tl-hover-bg)}
.cd-conf .go svg{width:14px;height:14px;color:inherit}

/* dòng tổng + phân trang + footer */
.cd-sum{display:flex;align-items:center;justify-content:flex-end;gap:12px;flex:0 0 auto;
  padding:14px 16px;background:var(--misc-table-header-bg,#f6f7fb);border-top:1px solid var(--tl-divider)}
.cd-sum .lb{font-weight:500;font-size:13px;line-height:18px;letter-spacing:.4px;text-transform:uppercase;color:var(--theme-text-secondary)}
.cd-sum .v{min-width:110px;text-align:right;font-size:14px;line-height:22px}
.cd-sum .ar{display:inline-flex;color:var(--theme-text-secondary)}
.cd-sum .ar svg{width:18px;height:18px}
.cd-sum .d{min-width:110px;text-align:right;font-size:14px;line-height:22px;color:var(--theme-text-secondary)}

.cd-pg{display:flex;align-items:center;gap:12px;flex:0 0 auto;padding:10px 16px;border-top:1px solid var(--tl-divider)}
.cd-pg[hidden]{display:none}
.cd-pg .l{font-size:13px;line-height:18px;color:var(--theme-text-secondary)}
.cd-pg .r{margin-left:auto;font-size:13px;line-height:18px;color:var(--theme-text-secondary)}
.cd-pg .cd-vs{width:88px;flex:0 0 auto}
#cdDtDlg .cd-vs{flex:1 1 auto;min-width:0}
.cd-pager{display:inline-flex;align-items:center;gap:4px}
.cd-pager button{min-width:30px;height:30px;padding:0 6px;cursor:pointer;border:1px solid var(--ez-tf-border);
  border-radius:var(--ez-r);background:var(--misc-paper);font-family:inherit;font-size:13px;line-height:18px;color:var(--theme-text-primary)}
.cd-pager button:hover:not(:disabled){background:var(--tl-hover-bg)}
.cd-pager button[aria-current="true"]{background:var(--ez-primary);border-color:var(--ez-primary);color:#fff}
.cd-pager button:disabled{color:var(--theme-text-disabled);cursor:default}
.cd-pager svg{width:14px;height:14px}

.cd-foot{display:flex;align-items:center;justify-content:flex-end;gap:12px;flex:0 0 auto;padding:16px;border-top:1px solid var(--tl-divider)}
.cd-foot .tl-btn-ghost,.cd-foot .tl-btn-primary,
.cd-sub footer .tl-btn-ghost,.cd-sub footer .tl-btn-primary{
  height:var(--ez-h-md);padding:0 var(--ez-px-md);border-radius:var(--ez-r);
  font-weight:500;font-size:var(--ez-fs-md);line-height:var(--ez-lh-md);
  display:inline-flex;align-items:center;justify-content:center;
}

/* ================= POPUP CON ================= */
.cd-subbd{position:fixed;inset:0;background:rgba(47,43,61,.5);z-index:1217;display:none}
.cd-subbd.is-open{display:block}
.cd-sub{
  position:fixed;top:50%;left:50%;transform:translate(-50%,-50%);
  z-index:1218;width:500px;max-width:calc(100vw - 32px);max-height:calc(100vh - 32px);
  background:var(--misc-paper);border-radius:var(--tl-radius-md);
  box-shadow:0 8px 32px rgba(47,43,61,.32);display:none;flex-direction:column;overflow:hidden;
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.cd-sub.is-open{display:flex}
.cd-sub header{display:flex;align-items:center;gap:12px;padding:20px;border-bottom:1px solid var(--tl-divider);flex:0 0 auto}
.cd-sub header h3{margin:0;flex:1 1 auto;font-weight:600;font-size:16px;line-height:24px}
.cd-subbody{padding:16px 24px;display:flex;flex-direction:column;gap:16px;overflow:auto;min-height:0}
.cd-sub footer{display:flex;align-items:center;justify-content:flex-end;gap:12px;padding:16px;border-top:1px solid var(--tl-divider);flex:0 0 auto}

.cd-frow{display:flex;align-items:center;gap:4px}
.cd-frow.top{align-items:flex-start}
.cd-frow .lb{width:100px;flex:0 0 auto;font-weight:500;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.cd-frow.bulkrate .lb{width:200px}
.cd-dt{
  display:flex;align-items:center;justify-content:space-between;gap:8px;flex:1 1 auto;min-width:0;
  height:var(--ez-tf-h-md);padding:0 var(--ez-tf-px-md);cursor:pointer;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);background:var(--misc-paper);
  font-family:inherit;font-size:14px;line-height:22px;color:var(--theme-text-primary);
  transition:border-color var(--ez-dur) var(--ez-ease);
}
.cd-dt:hover{border-color:var(--ez-tf-hover)}
.cd-dt:focus-visible{outline:none;border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.cd-dt .ic{display:inline-flex;color:var(--theme-text-secondary)}
.cd-dt .ic svg{width:16px;height:16px}

/* ---- Chi tiết thay đổi ---- */
.cd-dt-dlg{width:600px}
.cd-guest{display:inline-flex;align-items:center;gap:8px;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.cd-guest .tl-flag{width:20px;height:14px;border-radius:2px}
.cd-dtinfo{margin:0;display:grid;grid-template-columns:auto 1fr;gap:6px 12px}
.cd-dtinfo dt{font-size:14px;line-height:22px;color:var(--theme-text-secondary)}
.cd-dtinfo dd{margin:0;text-align:right;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.cd-dtinfo dd.strong{font-weight:500}
.cd-tabs{display:flex;gap:12px}
.cd-tabs button{
  height:var(--ez-h-md);padding:0 var(--ez-px-md);cursor:pointer;border-radius:var(--ez-r);
  border:1px solid var(--ez-tf-border);background:var(--misc-paper);
  font-family:inherit;font-weight:500;font-size:var(--ez-fs-md);line-height:var(--ez-lh-md);color:var(--theme-text-primary);
  transition:background-color var(--ez-dur) var(--ez-ease);
}
.cd-tabs button:hover{background:var(--tl-hover-bg)}
.cd-tabs button[aria-selected="true"]{background:var(--ez-primary);border-color:var(--ez-primary);color:#fff}
.cd-tabs button:focus-visible{outline:2px solid var(--ez-primary);outline-offset:2px}
.cd-dtscroll{max-height:300px;overflow:auto;border-top:1px solid var(--tl-divider)}
.cd-dttbl{width:100%;border-collapse:separate;border-spacing:0;table-layout:fixed}
.cd-dttbl thead th{position:sticky;top:0;z-index:2;background:var(--misc-table-header-bg,#f6f7fb);
  padding:10px 8px;text-align:left;font-weight:500;font-size:13px;line-height:18px;color:var(--theme-text-secondary);
  border-bottom:1px solid var(--tl-divider)}
.cd-dttbl th.tm{width:96px}
.cd-dttbl th.rt{width:150px}
.cd-dttbl th.am{width:110px;text-align:right}
.cd-dttbl td{padding:8px;border-bottom:1px solid var(--tl-divider);font-size:14px;line-height:22px;vertical-align:middle}
.cd-dttbl td.am{text-align:right;white-space:nowrap}
.cd-dttbl td.am input{width:100%;height:var(--ez-tf-h-md);padding:0 10px;text-align:right;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);background:var(--misc-paper);
  font-family:inherit;font-size:14px;line-height:22px}
.cd-dttbl td.am input:focus{outline:none;border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.cd-dtpg{font-size:13px;line-height:18px;color:var(--theme-text-secondary)}
.cd-dtsum{margin:0;display:grid;grid-template-columns:1fr auto;gap:6px 12px;padding-top:12px;border-top:1px solid var(--tl-divider)}
.cd-dtsum dt{font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.cd-dtsum dd{margin:0;text-align:right;font-size:14px;line-height:22px;font-weight:500}

/* select chính sách giá trong bảng Chi tiết thay đổi (native select, style theo DS) */
.cd-ratesel{
  width:100%;height:var(--ez-tf-h-md);padding:0 28px 0 10px;cursor:pointer;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);background:var(--misc-paper);
  font-family:inherit;font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
  appearance:none;-webkit-appearance:none;
  background-image:url("data:image/svg+xml;charset=utf-8,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%232f2b3db3' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3E%3Cpath d='M6 9l6 6 6-6'/%3E%3C/svg%3E");
  background-repeat:no-repeat;background-position:right 8px center;background-size:16px 16px;
  transition:border-color var(--ez-dur) var(--ez-ease);
}
.cd-ratesel:hover{border-color:var(--ez-tf-hover)}
.cd-ratesel:focus{outline:none;border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}

/* =====================================================================
   24. CHUYỂN PHÒNG (R01.08) — Figma 48831:92306
   1 modal, 2 trạng thái: danh sách (1 cột) ⇄ thực hiện (3 cột).
   z-index: backdrop 1214 · dialog 1216 (dưới #tlGenDlg 1220 để MSG nổi lên)
   ===================================================================== */
.mv-bd{position:fixed;inset:0;background:rgba(47,43,61,.5);z-index:1214;display:none}
.mv-bd.is-open{display:block}
.mv-dlg{
  position:fixed;top:0;right:0;z-index:1216;
  width:90vw;max-width:100vw;height:100%;
  background:var(--misc-paper);border-radius:0;
  box-shadow:-8px 0 32px rgba(47,43,61,.24);
  display:none;overflow:hidden;
  transform:translateX(24px);opacity:0;
  transition:transform .18s var(--tl-ease),opacity .18s var(--tl-ease);
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.mv-dlg.is-open{display:flex;transform:translateX(0);opacity:1}
.mv-left{display:flex;flex-direction:column;flex:1 1 auto;min-width:0}
.mv-mid,.mv-right{display:none}
.mv-dlg.is-side .mv-left{flex:0 0 280px;width:280px;border-right:1px solid var(--tl-divider)}
.mv-dlg.is-side .mv-mid{display:flex;flex-direction:column;flex:1 1 auto;min-width:0;border-right:1px solid var(--tl-divider)}
.mv-dlg.is-side .mv-right{display:flex;flex-direction:column;flex:0 0 520px;width:520px}
.mv-dlg.is-side .mv-tbl,
.mv-dlg.is-side .mv-pg .r,
.mv-dlg.is-side .mv-pg .mv-pager,
.mv-dlg.is-side #mvFoot,
.mv-dlg.is-side #mvX{display:none}
.mv-dlg:not(.is-side) .mv-tree{display:none}

.mv-head{display:flex;align-items:center;gap:12px;padding:16px;border-bottom:1px solid var(--tl-divider);flex:0 0 auto}
.mv-head h2{margin:0;flex:1 1 auto;font-weight:500;font-size:18px;line-height:28px}
.mv-x{border:0;background:none;cursor:pointer;color:var(--theme-text-secondary);display:inline-flex;padding:4px;border-radius:var(--ez-r);flex:0 0 auto}
.mv-x:hover{background:var(--tl-hover-bg)}
.mv-x svg{width:20px;height:20px}

/* ---------- bảng danh sách ---------- */
.mv-scroll{flex:1 1 auto;overflow:auto;min-height:0}
.mv-tbl{width:100%;border-collapse:separate;border-spacing:0;table-layout:fixed}
.mv-tbl thead th{position:sticky;top:0;z-index:2;background:var(--misc-table-header-bg,#f6f7fb);
  padding:12px 8px;text-align:left;font-weight:500;font-size:13px;line-height:18px;letter-spacing:.4px;
  text-transform:uppercase;color:var(--theme-text-secondary);border-bottom:1px solid var(--tl-divider)}
.mv-tbl th.rm{width:190px;padding-left:16px}
.mv-tbl th.lk{width:70px;text-align:center}
.mv-tbl th.gt{width:170px}
.mv-tbl th.px{width:90px}
.mv-tbl th.dt{width:220px}
.mv-tbl th.rt{width:140px}
.mv-tbl th.mo{width:120px;text-align:right}
.mv-tbl td{padding:10px 8px;border-bottom:1px solid var(--tl-divider);font-size:14px;line-height:22px;vertical-align:middle}
.mv-tbl td.dt{white-space:nowrap}
.mv-tbl td.rm{padding-left:16px}
.mv-tbl td.lk{text-align:center}
.mv-tbl td.mo{text-align:right;white-space:nowrap}
.mv-tbl tr.mv-grp td{background:var(--misc-table-header-bg,#f6f7fb);font-weight:500;padding:8px 8px 8px 16px}
.mv-tbl tr.mv-row{cursor:pointer}
.mv-tbl tr.mv-row:hover td{background:var(--tl-hover-bg)}
.mv-tbl tr.mv-row.is-cur td{background:var(--ez-primary-soft)}

.mv-grpname{display:inline-flex;align-items:center;gap:8px;border:0;background:none;cursor:pointer;font:inherit;font-weight:500;color:var(--theme-text-primary);padding:0}
.mv-grpname svg{width:18px;height:18px;transition:transform var(--ez-dur) var(--ez-ease)}
.mv-grpname.is-closed svg{transform:rotate(-90deg)}
.mv-roomcell{display:inline-flex;align-items:center;gap:8px;min-width:0;color:var(--ez-primary);font-weight:500}
.mv-roomcell svg{width:18px;height:18px;flex:0 0 auto}
.mv-roomcell.done{color:var(--ez-success)}
.mv-guest{display:inline-flex;align-items:center;gap:8px;min-width:0}
.mv-guest .tl-flag{width:20px;height:14px;border-radius:2px;flex:0 0 auto}
.mv-lockic{display:inline-flex;color:var(--ez-error);cursor:help}
.mv-lockic svg{width:18px;height:18px}
.mv-up{color:var(--ez-success)}
.mv-down{color:var(--ez-error)}
.mv-mute{color:var(--theme-text-secondary)}

/* ---------- cây phòng (trạng thái 3 cột) ---------- */
.mv-tree{display:flex;flex-direction:column}
.mv-tree .g{padding:10px 16px;background:var(--misc-table-header-bg,#f6f7fb);border-bottom:1px solid var(--tl-divider)}
.mv-tree .g button{display:inline-flex;align-items:center;gap:8px;border:0;background:none;cursor:pointer;font:inherit;font-weight:500;color:var(--theme-text-primary);padding:0}
.mv-tree .g svg{width:18px;height:18px}
.mv-tree .it{display:flex;align-items:center;gap:8px;width:100%;padding:12px 16px;border:0;border-bottom:1px solid var(--tl-divider);
  background:none;cursor:pointer;font:inherit;text-align:left;color:var(--ez-primary);font-weight:500;
  transition:background-color var(--ez-dur) var(--ez-ease)}
.mv-tree .it:hover{background:var(--tl-hover-bg)}
.mv-tree .it.is-cur{background:var(--ez-primary-soft)}
.mv-tree .it svg{width:18px;height:18px;flex:0 0 auto}
.mv-tree .it.done svg{color:var(--ez-success)}

/* ---------- tổng + phân trang + footer ---------- */
.mv-tot{display:flex;align-items:center;gap:8px;flex:0 0 auto;padding:14px 16px;
  background:var(--misc-table-header-bg,#f6f7fb);border-top:1px solid var(--tl-divider)}
.mv-tot .lb{flex:1 1 auto;font-weight:500;font-size:13px;line-height:18px;letter-spacing:.4px;text-transform:uppercase;color:var(--theme-text-secondary)}
.mv-tot .mo{width:120px;text-align:right;font-size:14px;line-height:22px}
.mv-dlg.is-side .mv-tot .mo{display:none}

.mv-pg,.mv-rpg{display:flex;align-items:center;gap:12px;flex:0 0 auto;padding:10px 16px;border-top:1px solid var(--tl-divider)}
.mv-pg .l,.mv-rpg .l{font-size:13px;line-height:18px;color:var(--theme-text-secondary)}
.mv-pg .r{margin-left:auto;font-size:13px;line-height:18px;color:var(--theme-text-secondary)}
.mv-vs-per{width:88px;flex:0 0 auto}
.mv-rpg .mv-pager{margin-left:auto}
.mv-pager{display:inline-flex;align-items:center;gap:4px}
.mv-pager button{min-width:30px;height:30px;padding:0 6px;cursor:pointer;border:1px solid var(--ez-tf-border);
  border-radius:var(--ez-r);background:var(--misc-paper);font-family:inherit;font-size:13px;line-height:18px;color:var(--theme-text-primary)}
.mv-pager button:hover:not(:disabled){background:var(--tl-hover-bg)}
.mv-pager button[aria-current="true"]{background:var(--ez-primary);border-color:var(--ez-primary);color:#fff}
.mv-pager button:disabled{color:var(--theme-text-disabled);cursor:default}
.mv-pager svg{width:14px;height:14px}

.mv-foot,.mv-midfoot{display:flex;align-items:center;gap:12px;flex:0 0 auto;padding:16px;border-top:1px solid var(--tl-divider)}
.mv-foot{justify-content:flex-end}
.mv-midfoot{justify-content:space-between}
.mv-foot .tl-btn-ghost,.mv-foot .tl-btn-primary,
.mv-midfoot .tl-btn-ghost,.mv-midfoot .tl-btn-primary{
  height:var(--ez-h-md);padding:0 var(--ez-px-md);border-radius:var(--ez-r);gap:8px;
  font-weight:500;font-size:var(--ez-fs-md);line-height:var(--ez-lh-md);
  display:inline-flex;align-items:center;justify-content:center;
}
.mv-midfoot button svg{width:18px;height:18px}
.mv-midfoot button:disabled{opacity:.5;cursor:default}

/* ---------- cột giữa ---------- */
.mv-midbody{flex:1 1 auto;overflow:auto;min-height:0;padding:16px;display:flex;flex-direction:column;gap:16px}
.mv-sect{margin:0;font-weight:500;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.mv-banner{display:flex;align-items:center;gap:16px;padding:16px;border-radius:var(--ez-r);background:var(--misc-table-header-bg,#f6f7fb)}
.mv-banner .c{flex:1 1 0;display:flex;flex-direction:column;align-items:center;gap:2px;min-width:0}
.mv-banner .k{font-size:13px;line-height:18px;color:var(--theme-text-secondary);display:inline-flex;align-items:center;gap:6px}
.mv-banner .k .lk{display:inline-flex;color:var(--ez-error)}
.mv-banner .k .lk svg{width:16px;height:16px}
.mv-banner .v{font-weight:500;font-size:18px;line-height:28px;color:var(--ez-primary)}
.mv-banner .v.none{color:var(--ez-warning)}
.mv-banner .c:first-child .v{color:var(--theme-text-primary)}
.mv-banner .ar{display:inline-flex;flex:0 0 auto;color:var(--theme-text-secondary)}
.mv-banner .ar svg{width:22px;height:22px}

.mv-info{margin:0;display:grid;grid-template-columns:130px 1fr;gap:10px 12px}
.mv-info dt{font-size:14px;line-height:22px;color:var(--theme-text-secondary)}
.mv-info dd{margin:0;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.mv-info dd .tl-flag{width:20px;height:14px;border-radius:2px;vertical-align:-2px;margin-right:6px}
.mv-info dd .sub{color:var(--theme-text-secondary)}

.mv-f{display:flex;flex-direction:column;gap:4px;min-width:0;flex:1 1 0}
.mv-lb{font-weight:400;font-size:13px;line-height:18px;color:var(--theme-text-primary)}
.mv-lb .req{color:var(--ez-error);font-style:normal}
.mv-msg{font-weight:400;font-size:12px;line-height:16px;color:var(--ez-error)}
.mv-2col{display:flex;gap:12px}
.mv-midbody textarea{width:100%;min-height:80px;resize:vertical;padding:10px var(--ez-tf-px-md);
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);background:var(--misc-paper);
  font-family:inherit;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.mv-midbody textarea:hover{border-color:var(--ez-tf-hover)}
.mv-midbody textarea:focus{outline:none;border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.mv-midbody textarea.is-error{border-color:var(--ez-error)}

.mv-tasks{display:flex;flex-direction:column;gap:12px;padding:16px;border:1px solid var(--tl-divider);border-radius:var(--ez-r)}
.mv-tasks[hidden]{display:none}
.mv-rate{display:flex;flex-direction:column;gap:12px}
.mv-rate[hidden]{display:none}
.mv-fee{display:flex;align-items:center;gap:8px;height:var(--ez-tf-h-md);padding:0 var(--ez-tf-px-md);
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);background:var(--misc-paper)}
.mv-fee .u{flex:0 0 auto;color:var(--theme-text-disabled);font-size:14px}
.mv-fee input{flex:1 1 auto;min-width:0;border:0;background:none;outline:none;text-align:right;font-family:inherit;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.mv-calc{margin:0;display:grid;grid-template-columns:1fr auto;gap:8px 12px}
.mv-calc dt{font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.mv-calc dd{margin:0;text-align:right;font-size:14px;line-height:22px}

/* checkbox dùng chung */
.mv-ck{display:inline-flex;align-items:center;gap:8px;cursor:pointer;user-select:none;align-self:flex-start}
.mv-ck input{position:absolute;opacity:0;width:0;height:0}
.mv-ck .box{width:18px;height:18px;flex:0 0 auto;border:2px solid var(--ez-tf-hover);border-radius:2px;
  display:inline-flex;align-items:center;justify-content:center;color:#fff;background:var(--misc-paper);
  transition:background-color var(--ez-dur) var(--ez-ease),border-color var(--ez-dur) var(--ez-ease)}
.mv-ck .box svg{width:12px;height:12px;opacity:0}
.mv-ck input:checked+.box{background:var(--ez-primary);border-color:var(--ez-primary)}
.mv-ck input:checked+.box svg{opacity:1}
.mv-ck input:focus-visible+.box{outline:2px solid var(--ez-primary);outline-offset:2px}
.mv-ck .lb{font-size:14px;line-height:22px;color:var(--theme-text-primary)}

/* ---------- cột phải ---------- */
.mv-rightbody{flex:1 1 auto;overflow:auto;min-height:0;padding:16px;display:flex;flex-direction:column;gap:12px}
.mv-filters{display:flex;gap:12px}
.mv-fcks{display:flex;flex-wrap:wrap;gap:12px 20px}
.mv-rscroll{flex:1 1 auto;min-height:0;overflow:auto;border-top:1px solid var(--tl-divider)}
.mv-rtbl{width:100%;border-collapse:separate;border-spacing:0;table-layout:fixed}
.mv-rtbl thead th{position:sticky;top:0;z-index:2;background:var(--misc-table-header-bg,#f6f7fb);
  padding:10px 8px;text-align:left;font-weight:500;font-size:13px;line-height:18px;color:var(--theme-text-secondary);
  border-bottom:1px solid var(--tl-divider)}
.mv-rtbl th.ac{width:130px}
.mv-rtbl td{padding:8px;border-bottom:1px solid var(--tl-divider);font-size:14px;line-height:22px;vertical-align:middle}
.mv-rtbl td.ac{text-align:right}
.mv-rtbl tr:hover td{background:var(--tl-hover-bg)}
.mv-godo{height:30px;padding:0 12px;cursor:pointer;border:0;border-radius:var(--ez-r);background:none;
  font-family:inherit;font-weight:500;font-size:13px;line-height:18px;color:var(--ez-primary);
  transition:background-color var(--ez-dur) var(--ez-ease)}
.mv-godo:hover{background:var(--ez-primary-soft)}
.mv-godo:focus-visible{outline:2px solid var(--ez-primary);outline-offset:2px}

.mv-locked{flex:1 1 auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:6px;padding:24px;text-align:center}
.mv-locked[hidden]{display:none}
.mv-locked svg{width:56px;height:56px;color:var(--theme-text-secondary);margin-bottom:6px}
.mv-locked .t{margin:0;font-weight:500;font-size:18px;line-height:28px;color:var(--theme-text-primary)}
.mv-locked .s{margin:0;font-size:14px;line-height:22px;color:var(--theme-text-secondary)}
.mv-locked .w{margin:6px 0 0;font-size:14px;line-height:22px;color:var(--theme-text-primary)}

/* =====================================================================
   25. DANH SÁCH BÁO CÁO (RR00) — Figma 35977:4589
   Màn toàn trang: cột trái 289px (danh sách) + cột phải (xem trước A4).
   ===================================================================== */
.rl{display:flex;gap:8px;padding:0;overflow:hidden;background:transparent;box-shadow:none;border:0}
.rl[hidden]{display:none}

/* mục "Báo cáo" trên menu trái là .nav-item (single) — bổ sung state chọn */
.nav-item.is-selected{background:var(--color-palette-primary-main);color:var(--misc-bg-white-text)}
.nav-item.is-selected .nav-icon{color:var(--misc-bg-white-text)}
.nav-item.is-selected:hover{background:var(--color-palette-primary-dark)}

/* ---------- Cột trái ---------- */
.rl-side{
  flex:0 0 289px;width:289px;display:flex;flex-direction:column;min-height:0;
  background:var(--misc-paper);border-radius:var(--tl-radius-md);overflow:hidden;
}
.rl-sidehead{padding:16px;display:flex;flex-direction:column;gap:8px;flex:0 0 auto}
.rl-title{margin:0;font-weight:500;font-size:20px;line-height:28px;color:var(--theme-text-primary)}
.rl-search{
  display:flex;align-items:center;gap:8px;height:var(--ez-tf-h-md);padding:0 var(--ez-tf-px-md);
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);background:var(--misc-paper);
  transition:border-color var(--ez-dur) var(--ez-ease);
}
.rl-search:hover{border-color:var(--ez-tf-hover)}
.rl-search:focus-within{border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.rl-search svg{width:16px;height:16px;flex:0 0 auto;color:var(--theme-text-secondary)}
.rl-search input{
  flex:1 1 auto;min-width:0;border:0;background:none;outline:none;padding:0;
  font-family:inherit;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.rl-search input::placeholder{color:var(--theme-text-disabled)}
.rl-search input::-webkit-search-cancel-button{-webkit-appearance:none;appearance:none}

.rl-tabs{display:flex;height:var(--ez-tf-h-md);border-bottom:1px solid var(--tl-divider)}
.rl-tabs button{
  flex:1 1 0;border:0;background:none;cursor:pointer;padding:0 8px;
  font-family:inherit;font-weight:500;font-size:14px;line-height:22px;color:var(--theme-text-secondary);
  border-bottom:2px solid transparent;margin-bottom:-1px;
  transition:color var(--ez-dur) var(--ez-ease),border-color var(--ez-dur) var(--ez-ease);
}
.rl-tabs button:hover{color:var(--theme-text-primary)}
.rl-tabs button[aria-selected="true"]{color:var(--ez-primary);border-bottom-color:var(--ez-primary)}
.rl-tabs button:focus-visible{outline:2px solid var(--ez-primary);outline-offset:-2px}

.rl-list{flex:1 1 auto;min-height:0;overflow-y:auto;overflow-x:hidden}
.rl-list::-webkit-scrollbar{width:8px}
.rl-list::-webkit-scrollbar-thumb{background:rgba(47,43,61,.22);border-radius:9999px}
.rl-list::-webkit-scrollbar-track{background:transparent}

.rl-grp{
  display:flex;align-items:center;gap:8px;width:100%;height:38px;padding:0 16px;
  border:0;border-bottom:1px solid var(--tl-divider);cursor:pointer;text-align:left;
  background:var(--misc-table-header-bg,#f6f7fb);font-family:inherit;
  font-weight:500;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.rl-grp span{flex:1 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.rl-grp svg{width:18px;height:18px;flex:0 0 auto;color:var(--theme-text-secondary);transition:transform var(--ez-dur) var(--ez-ease)}
.rl-grp.is-closed svg{transform:rotate(-90deg)}
.rl-grp:hover{background:var(--theme-action-selected)}

.rl-item{
  display:flex;align-items:center;gap:8px;width:100%;height:46px;padding:0 12px;box-sizing:border-box;
  border:0;border-bottom:1px solid var(--tl-divider);cursor:pointer;text-align:left;
  background:var(--misc-paper);font-family:inherit;
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
  transition:background-color var(--ez-dur) var(--ez-ease);
}
.rl-item .nm{flex:1 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.rl-item:hover{background:var(--theme-action-hover,rgba(47,43,61,.06))}
.rl-item.is-sel{background:var(--ez-primary-soft);color:var(--ez-primary)}
.rl-item:focus-visible{outline:2px solid var(--ez-primary);outline-offset:-2px}

.rl-star{
  width:20px;height:20px;flex:0 0 auto;display:inline-flex;align-items:center;justify-content:center;
  border:0;background:none;cursor:pointer;padding:0;color:var(--ez-warning);
  opacity:0;transition:opacity var(--ez-dur) var(--ez-ease);
}
.rl-star svg{width:20px;height:20px}
.rl-star.is-on{opacity:1}
.rl-item:hover .rl-star,
.rl-star:focus-visible{opacity:1}
.rl-star:focus-visible{outline:2px solid var(--ez-primary);outline-offset:2px;border-radius:var(--ez-ib-r-sm)}

.rl-noresult{padding:24px 16px;text-align:center;font-size:14px;line-height:22px;color:var(--theme-text-secondary)}

/* ---------- Cột phải ---------- */
.rl-main{flex:1 1 auto;min-width:0;display:flex;flex-direction:column;background:var(--misc-paper);border-radius:var(--tl-radius-md);overflow:hidden}
.rl-preview{flex:1 1 auto;min-height:0;overflow:auto;padding:16px}

.rl-sheet{display:flex;flex-direction:column;min-height:100%}
.rl-sheet[hidden]{display:none}
.rl-sheet-top{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}
.rl-sheet-top .hotel,.rl-sheet-top .code{font-weight:400;font-size:14px;line-height:20px;color:var(--theme-text-primary)}
.rl-sheet-title{margin:8px 0 0;text-align:center;font-weight:500;font-size:20px;line-height:28px;color:var(--theme-text-primary)}
.rl-sheet-sub{margin:4px 0 0;text-align:center;font-weight:400;font-size:14px;line-height:20px;color:var(--theme-text-primary)}

.rl-sheet-tblwrap{margin-top:16px;flex:1 1 auto;min-height:0;overflow:auto;border:1px solid var(--tl-divider);border-radius:var(--ez-r)}
.rl-sheet-tbl{width:100%;border-collapse:separate;border-spacing:0}
.rl-sheet-tbl thead th{
  position:sticky;top:0;z-index:2;height:40px;padding:0 12px;white-space:nowrap;text-align:left;
  background:var(--misc-table-header-bg,#f6f7fb);border-bottom:1px solid var(--tl-divider);
  font-weight:500;font-size:13px;line-height:18px;letter-spacing:.4px;text-transform:uppercase;color:var(--theme-text-secondary);
}
.rl-sheet-tbl th.stt{width:56px}
.rl-sheet-tbl td{height:36px;padding:0 12px;white-space:nowrap;border-bottom:1px solid var(--tl-divider);font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.rl-sheet-tbl tbody tr:last-child td{border-bottom:0}

.rl-sheet-foot{display:flex;align-items:center;gap:24px;margin-top:16px;padding-top:12px;border-top:1px solid var(--tl-divider);flex:0 0 auto}
.rl-sheet-foot .pw{display:inline-flex;align-items:center;gap:6px;font-size:14px;line-height:20px;color:var(--theme-text-secondary)}
.rl-sheet-foot .logo{display:inline-flex;width:24px;height:24px}
.rl-sheet-foot .logo svg{width:24px;height:24px}
.rl-sheet-foot .nm{color:var(--ez-primary)}
.rl-sheet-foot .it{font-size:14px;line-height:20px;color:var(--theme-text-primary)}
.rl-sheet-foot .it b{font-weight:500}
.rl-sheet-foot .pg{margin-left:auto;font-size:14px;line-height:20px;color:var(--theme-text-primary)}

.rl-empty{flex:1 1 auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:6px;padding:32px;text-align:center}
.rl-empty[hidden]{display:none}
.rl-empty svg{width:56px;height:56px;color:var(--theme-text-secondary);margin-bottom:6px}
.rl-empty .t{margin:0;font-weight:500;font-size:18px;line-height:28px;color:var(--theme-text-primary)}
.rl-empty .s{margin:0;max-width:420px;font-size:14px;line-height:22px;color:var(--theme-text-secondary)}

.rl-cta{display:flex;align-items:center;justify-content:flex-end;gap:12px;flex:0 0 auto;padding:12px 16px;border-top:1px solid var(--tl-divider)}
.rl-btn-out,.rl-btn-primary{
  height:var(--ez-h-md);padding:0 var(--ez-px-md);cursor:pointer;border-radius:var(--ez-r);
  display:inline-flex;align-items:center;justify-content:center;gap:8px;
  font-family:inherit;font-weight:500;font-size:var(--ez-fs-md);line-height:var(--ez-lh-md);
  transition:background-color var(--ez-dur) var(--ez-ease);
}
.rl-btn-out{border:1px solid var(--ez-primary);background:var(--misc-paper);color:var(--ez-primary)}
.rl-btn-out:hover{background:var(--ez-primary-soft)}
.rl-btn-primary{border:0;background:var(--ez-primary);color:#fff;box-shadow:var(--ez-tf-shadow)}
.rl-btn-primary:hover{background:var(--ez-primary-hover)}
.rl-btn-out svg,.rl-btn-primary svg{width:18px;height:18px;flex:0 0 auto}
.rl-btn-out:focus-visible,.rl-btn-primary:focus-visible{outline:2px solid var(--ez-primary);outline-offset:2px}

/* ---- Tooltip tên báo cáo bị cắt ---- */
.rl-tip{
  position:fixed;left:0;top:0;z-index:1272;
  max-width:420px;padding:6px 10px;box-sizing:border-box;
  border-radius:6px;background:rgba(47,43,61,.9);color:#fff;
  font-family:inherit;font-weight:400;font-size:13px;line-height:18px;letter-spacing:.16px;
  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;
  pointer-events:none;opacity:0;visibility:hidden;
  transition:opacity var(--ez-dur,.15s) var(--ez-ease,ease);
}
.rl-tip.is-open{opacity:1;visibility:visible}

/* =====================================================================
   26. KHAI BÁO CÔNG AN / HẢI QUAN (RR06) — Figma 35977:17486
   Lớp phủ toàn màn: toolbar 54px + trang A4 + panel "Lọc dữ liệu" 303px.
   z-index: backdrop 1214 · màn 1216 · menu multi-select 1272 · popup cột 1218
   ===================================================================== */
.ca-bd{position:fixed;inset:0;background:rgba(47,43,61,.5);z-index:1214;display:none}
.ca-bd.is-open{display:block}
.ca{
  position:fixed;top:0;right:0;z-index:1216;
  width:80vw;max-width:100vw;height:100%;
  display:none;flex-direction:column;overflow:hidden;
  background:var(--tl-page-bg,#f4f5fa);border-radius:0;
  box-shadow:-8px 0 32px rgba(47,43,61,.24);
  transform:translateX(24px);opacity:0;
  transition:transform .18s var(--tl-ease,cubic-bezier(.4,0,.2,1)),opacity .18s var(--tl-ease,cubic-bezier(.4,0,.2,1));
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.ca.is-open{display:flex;transform:translateX(0);opacity:1}

/* ---------- Toolbar ---------- */
.ca-nav{display:flex;align-items:center;gap:12px;flex:0 0 auto;height:54px;padding:0 16px;
  background:var(--misc-paper);border-bottom:1px solid var(--tl-divider)}
.ca-tools{display:flex;align-items:center;gap:4px;flex:1 1 auto;min-width:0}
.ca-navr{display:flex;align-items:center;gap:12px;flex:0 0 auto}
.ca-vr{width:1px;height:24px;background:var(--tl-divider)}

.ca-ib{
  width:34px;height:34px;flex:0 0 auto;display:inline-flex;align-items:center;justify-content:center;
  border:0;background:none;cursor:pointer;border-radius:var(--ez-ib-r-md);color:var(--theme-text-secondary);
  transition:background-color var(--ez-dur) var(--ez-ease),color var(--ez-dur) var(--ez-ease);
}
.ca-ib:hover{background:var(--tl-hover-bg);color:var(--ez-primary)}
.ca-ib:disabled{color:var(--theme-text-disabled);cursor:default;background:none}
.ca-ib svg{width:20px;height:20px}
.ca-ib:focus-visible{outline:2px solid var(--ez-primary);outline-offset:2px}

.ca-pgbox{display:inline-flex;align-items:center;gap:6px;margin:0 4px}
.ca-pgbox input{
  width:48px;height:30px;padding:0 8px;text-align:center;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);background:var(--misc-paper);
  font-family:inherit;font-size:13px;line-height:22px;color:var(--theme-text-primary);
}
.ca-pgbox input:focus{outline:none;border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.ca-pgbox .of{font-size:14px;line-height:24px;color:var(--theme-text-secondary)}

.ca-btn-out,.ca-btn-primary{
  height:var(--ez-h-sm);padding:0 var(--ez-px-md);cursor:pointer;border-radius:var(--ez-r);
  display:inline-flex;align-items:center;justify-content:center;gap:8px;
  font-family:inherit;font-weight:500;font-size:var(--ez-fs-md);line-height:var(--ez-lh-md);
  transition:background-color var(--ez-dur) var(--ez-ease);
}
.ca-btn-out{border:1px solid var(--ez-primary);background:var(--misc-paper);color:var(--ez-primary)}
.ca-btn-out:hover{background:var(--ez-primary-soft)}
.ca-btn-primary{height:var(--ez-h-md);border:0;background:var(--ez-primary);color:#fff;box-shadow:var(--ez-tf-shadow)}
.ca-btn-primary:hover{background:var(--ez-primary-hover)}
.ca-btn-out svg,.ca-btn-primary svg{width:16px;height:16px;flex:0 0 auto}
.ca-btn-out .cv{width:14px;height:14px}
.ca-btn-out:focus-visible,.ca-btn-primary:focus-visible{outline:2px solid var(--ez-primary);outline-offset:2px}

.ca-dl{position:relative}
.ca-dlmenu{
  position:absolute;top:calc(100% + 6px);right:0;z-index:5;display:none;min-width:180px;padding:4px;
  background:var(--misc-paper);border-radius:var(--ez-r);box-shadow:0 4px 18px rgba(47,43,61,.22);
}
.ca-dlmenu.is-open{display:block}
.ca-dlmenu button{
  display:block;width:100%;padding:8px 12px;border:0;background:none;cursor:pointer;text-align:left;
  border-radius:var(--ez-tf-r-md);font-family:inherit;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.ca-dlmenu button:hover{background:var(--tl-hover-bg)}

/* ---------- Thân ---------- */
.ca-body{flex:1 1 auto;min-height:0;display:flex;gap:6px;padding:6px}
.ca-viewer{flex:1 1 auto;min-width:0;overflow:auto;background:var(--misc-paper);border-radius:var(--tl-radius-md);padding:8px}
/* Trang báo cáo giãn hết bề ngang khung xem (không còn khoá cứng 793px) */
.ca-a4{
  width:100%;margin:0 auto;min-height:842px;box-sizing:border-box;
  background:#fff;border:1px solid var(--tl-divider);border-radius:var(--ez-r);
  padding:16px 20px;display:flex;flex-direction:column;
}
.ca-a4-top{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}
.ca-a4-top .brand{font-weight:500;font-size:14px;line-height:20px;color:var(--theme-text-primary)}
.ca-a4-top .code{font-size:14px;line-height:20px;color:var(--theme-text-primary)}
.ca-a4-title{margin:6px 0 0;text-align:center;font-weight:600;font-size:18px;line-height:28px;color:var(--theme-text-primary)}
.ca-a4-range{margin:2px 0 0;text-align:center;font-size:13px;line-height:20px;color:var(--theme-text-primary)}
.ca-a4-cond{margin:12px 0 0;font-size:12px;line-height:20px;color:var(--theme-text-primary)}
.ca-a4-cond b{font-weight:500;font-style:normal}
.ca-a4-cond i{font-style:italic}

.ca-a4-tblwrap{margin-top:12px;flex:1 1 auto;min-height:0}
.ca-a4-tbl{width:100%;border-collapse:collapse;table-layout:auto}
/* Dữ liệu giữ trên 1 dòng (số/ngày không bị cắt giữa chừng);
   chỉ NHÃN CỘT được phép xuống dòng để bảng vừa bề ngang, không phải cuộn ngang. */
.ca-a4-tbl th,.ca-a4-tbl td{
  border:1px solid rgba(47,43,61,.5);padding:4px 6px;font-size:12px;line-height:18px;
  text-align:left;vertical-align:middle;white-space:nowrap;
}
.ca-a4-tbl thead th{text-align:center;white-space:normal;overflow-wrap:normal}
.ca-a4-tbl th.stt,.ca-a4-tbl td.stt{white-space:nowrap}
.ca-a4-tbl thead th{background:#fff;font-weight:500;color:var(--theme-text-primary)}
.ca-a4-tbl td{color:var(--theme-text-primary)}
.ca-a4-tbl th.stt,.ca-a4-tbl td.stt{width:34px;text-align:center}

.ca-a4-foot{display:flex;align-items:center;gap:20px;margin-top:12px;padding-top:8px;border-top:1px solid rgba(47,43,61,.5);flex:0 0 auto}
.ca-a4-foot .it{font-size:12px;line-height:18px;color:var(--theme-text-primary)}
.ca-a4-foot .it b{font-weight:500}
.ca-a4-foot .pg{margin-left:auto;font-size:12px;line-height:18px}

/* ---------- Panel lọc ---------- */
.ca-filter{
  flex:0 0 303px;width:303px;display:flex;flex-direction:column;min-height:0;
  background:var(--misc-paper);border-radius:var(--ez-tf-r-md);box-shadow:0 2px 8px rgba(19,17,32,.18);overflow:hidden;
}
.ca-fhead{padding:12px 16px 0;flex:0 0 auto}
.ca-fhead h3{margin:0;font-weight:500;font-size:18px;line-height:28px;color:var(--theme-text-primary)}
.ca-fbody{flex:1 1 auto;min-height:0;overflow-y:auto;padding:16px;display:flex;flex-direction:column;gap:12px}
.ca-fbody::-webkit-scrollbar{width:8px}
.ca-fbody::-webkit-scrollbar-thumb{background:rgba(47,43,61,.22);border-radius:9999px}
.ca-fbody::-webkit-scrollbar-track{background:transparent}
.ca-ffoot{display:flex;align-items:center;justify-content:flex-end;padding:12px 16px;border-top:1px solid var(--tl-divider);flex:0 0 auto}

.ca-f{display:flex;flex-direction:column;gap:4px;min-width:0;flex:1 1 auto}
.ca-f-last{flex:0 0 92px;width:92px}
.ca-frow{display:flex;gap:8px;align-items:flex-start}
.ca-lb{font-weight:400;font-size:13px;line-height:15px;color:var(--theme-text-primary)}
.ca-fbody input[type="text"],
.ca-dt,.ca-msbtn{
  display:flex;align-items:center;gap:8px;width:100%;box-sizing:border-box;
  height:30px;padding:0 12px;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-ib-r-sm);background:var(--misc-paper);
  font-family:inherit;font-weight:400;font-size:13px;line-height:22px;color:var(--theme-text-primary);
  transition:border-color var(--ez-dur) var(--ez-ease);
}
.ca-fbody input[type="text"]::placeholder{color:var(--theme-text-disabled)}
.ca-fbody input[type="text"]:hover,.ca-dt:hover,.ca-msbtn:hover{border-color:var(--ez-tf-hover)}
.ca-fbody input[type="text"]:focus,.ca-dt:focus-visible,.ca-msbtn:focus-visible{outline:none;border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.ca-fbody input.is-error{border-color:var(--ez-error)}
.ca-dt,.ca-msbtn{cursor:pointer;justify-content:space-between;text-align:left}
.ca-dt .v,.ca-msbtn .v{flex:1 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.ca-dt .v.ph,.ca-msbtn .v.ph{color:var(--theme-text-disabled)}
.ca-dt .ic,.ca-msbtn .ic{display:inline-flex;flex:0 0 auto;color:var(--theme-text-secondary)}
.ca-dt .ic svg,.ca-msbtn .ic svg{width:14px;height:14px}

/* menu multi-select */
.ca-msmenu{
  position:fixed;z-index:1272;display:none;min-width:200px;max-height:300px;overflow:auto;padding:4px;
  background:var(--misc-paper);border-radius:var(--ez-tf-r-md);box-shadow:0 4px 18px rgba(47,43,61,.22);
}
.ca-msmenu.is-open{display:block}
.ca-msmenu label{
  display:flex;align-items:center;gap:8px;padding:6px 10px;cursor:pointer;border-radius:var(--ez-tf-r-md);
  font-size:13px;line-height:22px;color:var(--theme-text-primary);
}
.ca-msmenu label:hover{background:var(--tl-hover-bg)}
.ca-msmenu label.is-on{background:var(--ez-primary-soft);color:var(--ez-primary)}
.ca-msmenu input{position:absolute;opacity:0;width:0;height:0}
.ca-msmenu .box{
  width:16px;height:16px;flex:0 0 auto;border:2px solid var(--ez-tf-hover);border-radius:2px;
  display:inline-flex;align-items:center;justify-content:center;color:#fff;background:var(--misc-paper);
}
.ca-msmenu .box svg{width:11px;height:11px;opacity:0}
.ca-msmenu input:checked+.box{background:var(--ez-primary);border-color:var(--ez-primary)}
.ca-msmenu input:checked+.box svg{opacity:1}
.ca-msmenu hr{border:0;border-top:1px solid var(--tl-divider);margin:4px 8px}

/* ---------- Chế độ in: chỉ giữ trang A4 ---------- */
@media print{
  body *{visibility:hidden !important}
  .ca.is-open,.ca.is-open .ca-body,.ca.is-open .ca-viewer,.ca.is-open .ca-a4,.ca.is-open .ca-a4 *{visibility:visible !important}
  .ca.is-open{position:absolute;inset:0;width:100%;box-shadow:none;transform:none;opacity:1;background:#fff;display:block}
  .ca.is-open .ca-nav,.ca.is-open .ca-filter{display:none !important}
  .ca.is-open .ca-body{display:block;padding:0}
  .ca.is-open .ca-viewer{overflow:visible;padding:0;background:#fff}
  .ca.is-open .ca-a4{border:0;border-radius:0;margin:0;width:100%;min-height:0;padding:0}
  .ca-bd,.ca-msmenu{display:none !important}
}

/* ================= BÁO CÁO DÙNG CHUNG (RR03 bữa ăn · RR04 công nợ) =================
   #rvCard mượn lại toàn bộ chrome của .ca (drawer 80vw, toolbar, filter panel, @media print)
   nên ở đây chỉ khai báo phần TRANG BÁO CÁO A4 và 2 bảng đặc thù.
   ================================================================================== */

/* ---------- Trang A4 dùng chung ---------- */
.rv-a4-sec{margin-top:14px}
.rv-a4-sec>h3{margin:0 0 6px;font-weight:600;font-size:13px;line-height:20px;color:var(--theme-text-primary)}

/* Lưới 2 cột cho 4 bảng tổng kết của báo cáo bữa ăn */
.rv-sum{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}
.rv-sum>table{width:100%}

/* Bảng báo cáo: dùng chung khung với .ca-a4-tbl nhưng cho phép xuống dòng ở ô văn bản */
.rv-tbl{width:100%;border-collapse:collapse;table-layout:auto}
.rv-tbl th,.rv-tbl td{
  border:1px solid rgba(47,43,61,.5);padding:4px 6px;
  font-size:11px;line-height:16px;text-align:left;vertical-align:middle;color:var(--theme-text-primary);
  white-space:nowrap;
}
.rv-tbl thead th{background:#fff;font-weight:500;text-align:center}
.rv-tbl thead th.l{text-align:left}
.rv-tbl td.c,.rv-tbl th.c{text-align:center}
.rv-tbl td.r,.rv-tbl th.r{text-align:right;white-space:nowrap}
.rv-tbl td.wrap{white-space:normal}
.rv-tbl tr.rv-grp>td{font-weight:600;background:var(--tl-page-bg,#f4f5fa)}
.rv-tbl tr.rv-note>td{font-style:italic;color:var(--theme-text-secondary);white-space:normal}
.rv-tbl tr.rv-total>td{font-weight:600}
.rv-tbl tr.rv-meal>td{font-weight:600}

/* Bảng tổng kết: mỗi nhóm 2 cột (số lượng · tỉ lệ) */
.rv-tbl td.n{text-align:right;white-space:nowrap;width:1%}
.rv-tbl td.p{text-align:right;white-space:nowrap;width:1%;color:var(--theme-text-secondary)}

/* ---------- Khung xem trước trong màn Danh sách báo cáo ---------- */
/* Trang A4 rộng 793px được thu nhỏ vừa khung bằng transform: scale() */
.rl-pvwrap{
  flex:1 1 auto;min-height:0;overflow:auto;
  padding:16px;background:var(--tl-page-bg,#f4f5fa);
  display:flex;justify-content:center;align-items:flex-start;
}
.rl-pv .ca-a4-tblwrap{overflow:visible}
/* v42 — SỬA LỖI CẮT TRANG XEM TRƯỚC:
   trước đây transform:scale() đặt trên chính .rl-pvscale nên hộp bọc bị thu nhỏ
   theo (812px -> 578px thực tế) rồi overflow:hidden cắt mất phần bên phải.
   Nay: .rl-pvscale chỉ giữ KÍCH THƯỚC SAU KHI THU NHỎ, còn scale đặt trên .rl-pv. */
.rl-pvscale{overflow:hidden;flex:0 0 auto}
/* Bề ngang do rlPvFit() tính lại theo nội dung — không cắt, không cuộn */
.rl-pv{
  width:793px;box-sizing:border-box;transform-origin:top left;
  background:var(--misc-paper,#fff);border:1px solid var(--tl-divider);
  padding:24px;display:flex;flex-direction:column;
}

/* ---------- Checkbox trong bộ lọc (RR04) ---------- */
/* Bộ lọc của #rvCard chỉ có vài trường → không cho các trường giãn đều toàn cột */
#rvFBody{justify-content:flex-start}
#rvFBody .ca-f{flex:0 0 auto}

.ca-f-ck{
  position:relative;display:flex;flex-direction:row;align-items:center;
  gap:8px;cursor:pointer;padding:2px 0;min-width:0;flex:0 0 auto;
}
.ca-f-ck input{position:absolute;opacity:0;width:0;height:0}
.ca-f-ck .box{
  flex:0 0 auto;width:18px;height:18px;border-radius:2px;
  border:2px solid rgba(47,43,61,.4);background:transparent;
  display:inline-flex;align-items:center;justify-content:center;
  transition:background-color var(--ez-dur,.15s) var(--ez-ease,ease),border-color var(--ez-dur,.15s) var(--ez-ease,ease);
}
.ca-f-ck .box svg{width:12px;height:12px;opacity:0;color:#fff}
.ca-f-ck input:checked+.box{background:var(--ez-primary);border-color:var(--ez-primary)}
.ca-f-ck input:checked+.box svg{opacity:1}
.ca-f-ck input:focus-visible+.box{outline:2px solid var(--ez-primary);outline-offset:2px}
.ca-f-ck .lb{
  flex:1 1 auto;min-width:0;
  font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-primary);
}

/* ---------- Cụm thu phóng trên thanh công cụ ---------- */
.ca-zoombox{display:flex;align-items:center;gap:2px;flex:0 0 auto}
.ca-zoomlb{
  min-width:52px;height:30px;padding:0 8px;box-sizing:border-box;
  border:1px solid var(--ez-tf-border,rgba(47,43,61,.22));border-radius:var(--ez-tf-r-sm,4px);
  background:var(--misc-paper,#fff);cursor:pointer;
  font-family:inherit;font-weight:400;font-size:13px;line-height:20px;
  color:var(--theme-text-primary);text-align:center;
  transition:border-color var(--ez-dur,.15s) var(--ez-ease,ease),background-color var(--ez-dur,.15s) var(--ez-ease,ease);
}
.ca-zoomlb:hover{border-color:var(--ez-tf-hover,rgba(47,43,61,.4));background:var(--tl-hover-bg)}
.ca-zoomlb:focus-visible{outline:2px solid var(--ez-primary);outline-offset:2px}

/* Lớp bọc thu phóng: scale từ mép trên-trái để thanh cuộn tính đúng bề rộng */
.ca-zoom{transform-origin:top left;width:100%}
.ca-zoom.is-zoomed{position:relative}

/* Khi phóng to, khung xem mới cần cuộn ngang */
.ca-viewer{overflow:auto}

/* Bảng bữa ăn / công nợ: nhãn cột được xuống dòng, ô dữ liệu chữ dài cũng vậy;
   số tiền · ngày · tỉ lệ luôn giữ 1 dòng */
.rv-tbl thead th{white-space:normal;overflow-wrap:normal}
.rv-tbl td.r,.rv-tbl th.r,.rv-tbl td.n,.rv-tbl td.p,.rv-tbl td.c{white-space:nowrap}

/* ================= POPUP HỒ SƠ NGƯỜI DÙNG — Figma EzOne.DesignLibrary 22255:55976 ================= */

.pf-pop{
  position:fixed;z-index:1290;width:300px;box-sizing:border-box;
  display:none;flex-direction:column;padding:8px 0;
  background:var(--misc-paper,#fff);border-radius:var(--tl-radius-md,6px);
  box-shadow:0 4px 18px 0 rgba(47,43,61,.16);
  font-weight:400;font-size:15px;line-height:24px;color:var(--theme-text-primary);
  opacity:0;transform:translateY(-4px);
  transition:opacity .14s var(--tl-ease,cubic-bezier(.4,0,.2,1)),transform .14s var(--tl-ease,cubic-bezier(.4,0,.2,1));
}
.pf-pop.is-open{display:flex;opacity:1;transform:translateY(0)}

/* ---------- Đầu: ảnh đại diện + tên + email ---------- */
.pf-head{display:flex;align-items:center;gap:12px;padding:8px 16px}
.pf-ava{
  position:relative;width:40px;height:40px;flex:0 0 auto;
  border-radius:500px;overflow:hidden;background:var(--misc-avatar-bg,#EEEDF0);
}
.pf-ava img{width:100%;height:100%;object-fit:cover;display:block}
.pf-ava .flag{
  position:absolute;right:0;bottom:0;width:15px;height:15px;border-radius:500px;
  overflow:hidden;box-shadow:0 0 0 1.5px var(--misc-paper,#fff);
}
.pf-ava .flag svg{width:100%;height:100%;display:block}
.pf-id{flex:1 1 auto;min-width:0;display:flex;flex-direction:column}
.pf-id .nm{
  font-weight:500;font-size:15px;line-height:22px;color:var(--theme-text-primary);
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap;
}
.pf-id .em{
  font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary,rgba(47,43,61,.55));
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap;
}

.pf-div{height:1px;background:var(--tl-divider);flex:0 0 auto}

/* ---------- Nhóm dòng ---------- */
.pf-grp{display:flex;flex-direction:column;padding:4px 8px}
.pf-row{
  display:flex;align-items:center;gap:12px;width:100%;
  padding:8px;box-sizing:border-box;border:0;background:none;cursor:pointer;
  border-radius:var(--tl-radius-md,6px);text-align:left;font-family:inherit;
  font-weight:400;font-size:15px;line-height:24px;color:var(--theme-text-primary);
  transition:background-color var(--ez-dur,.15s) var(--ez-ease,ease);
}
.pf-row:hover{background:var(--tl-hover-bg,rgba(47,43,61,.06))}
.pf-row:focus-visible{outline:2px solid var(--ez-primary);outline-offset:-2px}
.pf-row>.ic{width:22px;height:22px;flex:0 0 auto;color:var(--theme-text-primary)}
.pf-row>.ic svg{width:100%;height:100%;display:block}
.pf-row>.pf-lb{flex:1 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.pf-row>.pf-lb b{font-weight:700}

/* Cụm giá trị bên phải (Ngôn ngữ / Giao diện) */
.pf-opt{display:flex;align-items:center;gap:4px;flex:0 0 auto;color:var(--theme-text-primary)}
.pf-opt .fg{width:22px;height:22px;flex:0 0 auto;border-radius:2px;overflow:hidden}
.pf-opt .fg svg{width:100%;height:100%;display:block}
.pf-opt .sun{width:22px;height:22px;flex:0 0 auto}
.pf-opt .sun svg{width:100%;height:100%;display:block}
.pf-opt .v{font-weight:400;font-size:12px;line-height:18px;white-space:nowrap}
.pf-opt .cv{width:16px;height:16px;flex:0 0 auto}
.pf-opt .cv svg{width:100%;height:100%;display:block}

/* ---------- Đăng xuất + phiên bản ---------- */
.pf-act{padding:8px 16px}
.pf-logout{
  display:flex;align-items:center;justify-content:center;gap:6px;
  width:100%;height:30px;padding:0 14px;box-sizing:border-box;
  border:0;border-radius:var(--tl-radius-md,6px);cursor:pointer;
  background:var(--ez-error,#ff4c51);box-shadow:0 2px 6px 0 rgba(255,76,81,.3);
  font-family:inherit;font-weight:500;font-size:13px;line-height:18px;color:#fff;
  transition:filter var(--ez-dur,.15s) var(--ez-ease,ease);
}
.pf-logout:hover{filter:brightness(.94)}
.pf-logout:focus-visible{outline:2px solid var(--ez-error,#ff4c51);outline-offset:2px}
.pf-logout svg{width:16px;height:16px;flex:0 0 auto;display:block}
.pf-ver{
  display:flex;align-items:center;justify-content:center;padding:4px 12px;
  font-weight:400;font-size:12px;line-height:18px;color:var(--theme-text-secondary,rgba(47,43,61,.7));
}

/* Ảnh đại diện trên thanh tiêu đề trở thành nút bấm được */
.avatar[role="button"]{cursor:pointer}
.avatar[role="button"]:focus-visible{outline:2px solid var(--ez-primary);outline-offset:2px}

/* ---------- Menu con: Ngôn ngữ · Giao diện (Figma 19385:5570 / 19385:5067) ---------- */
.pf-menu{
  position:fixed;z-index:1292;width:150px;box-sizing:border-box;
  display:none;flex-direction:column;gap:2px;padding:8px;
  background:var(--misc-paper,#fff);border-radius:var(--tl-radius-md,6px);
  box-shadow:0 4px 18px 0 rgba(47,43,61,.16);
}
.pf-menu.is-open{display:flex}
.pf-mi{
  display:flex;align-items:center;gap:8px;width:134px;height:38px;
  padding:0 10px;box-sizing:border-box;border:0;cursor:pointer;
  background:none;border-radius:var(--tl-radius-md,6px);text-align:left;
  font-family:inherit;font-weight:400;font-size:14px;line-height:22px;
  color:var(--theme-text-primary);
  transition:background-color var(--ez-dur,.15s) var(--ez-ease,ease);
}
.pf-mi:hover{background:var(--tl-hover-bg,rgba(47,43,61,.06))}
.pf-mi:focus-visible{outline:2px solid var(--ez-primary);outline-offset:-2px}
.pf-mi.is-sel{background:var(--ez-primary-soft,rgba(24,111,244,.08));color:var(--ez-primary)}
.pf-mi>.mi-ic{width:22px;height:22px;flex:0 0 auto;display:block}
.pf-mi>.mi-ic svg{width:100%;height:100%;display:block}
.pf-mi>.mi-tx{flex:1 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}

/* ================= TẠO ĐẶT PHÒNG ĐẦY ĐỦ (Reservation) — Figma 47216:315103 ================= */

.rs{display:flex;flex-direction:column;min-height:0;overflow:hidden;padding:0}
.rs[hidden]{display:none !important}

/* Mỗi bước là một cột co giãn: bộ lọc + thân cuộn được + chân màn GHIM ĐÁY */
.rs-pane{flex:1 1 auto;min-height:0;display:flex;flex-direction:column;overflow:hidden}
.rs-pane[hidden]{display:none !important}

/* ---------- Stepper ---------- */
.rs-steps{
  display:flex;align-items:center;gap:8px;flex:0 0 auto;
  padding:12px 20px;border-bottom:1px solid var(--tl-divider);
}
.rs-step{
  display:flex;align-items:center;gap:12px;border:0;background:none;cursor:pointer;
  padding:4px 8px;border-radius:var(--tl-radius-md,6px);font-family:inherit;
  transition:background-color var(--ez-dur,.15s) var(--ez-ease,ease);
}
.rs-step:disabled{cursor:not-allowed}
.rs-step:hover:not(:disabled){background:var(--tl-hover-bg)}
.rs-step:focus-visible{outline:2px solid var(--ez-primary);outline-offset:-2px}
.rs-step .no{
  width:38px;height:38px;flex:0 0 auto;border-radius:var(--tl-radius-md,6px);
  display:inline-flex;align-items:center;justify-content:center;
  background:var(--theme-action-selected);color:var(--theme-text-secondary);
  font-weight:500;font-size:16px;line-height:24px;
  transition:background-color var(--ez-dur,.15s) var(--ez-ease,ease),color var(--ez-dur,.15s) var(--ez-ease,ease);
}
.rs-step .no svg{width:20px;height:20px;display:block}
.rs-step .tx{font-weight:400;font-size:15px;line-height:24px;color:var(--theme-text-secondary)}
.rs-step.is-cur .no{background:var(--ez-primary);color:var(--misc-bg-white-text)}
.rs-step.is-cur .tx{font-weight:500;color:var(--theme-text-primary)}
.rs-step.is-done .no{background:var(--color-palette-success-main,#28C76F);color:var(--misc-bg-white-text)}
.rs-step.is-done .tx{color:var(--theme-text-primary)}
.rs-step:disabled .tx{color:var(--theme-text-disabled)}
.rs-sep{width:20px;height:20px;flex:0 0 auto;color:var(--theme-text-disabled)}
.rs-sep svg{width:100%;height:100%;display:block}

/* ---------- Bộ lọc bước 1 ---------- */
.rs-filter{
  flex:0 0 auto;padding:16px 20px;border-bottom:1px solid var(--tl-divider);
  display:flex;flex-direction:column;gap:12px;
}
/* Màn thấp: thu gọn khoảng đệm để chân màn không bị đẩy khuất */
@media (max-height:820px){
  .rs-filter{padding:12px 20px;gap:8px}
  .rs-main{padding:12px 20px}
  .rs-steps{padding:8px 20px}
  .rs-foot{padding:10px 20px}
}
.rs-frow{display:grid;grid-template-columns:repeat(12,minmax(0,1fr));gap:12px}
.rs-f{display:flex;flex-direction:column;gap:4px;min-width:0}
.rs-f>.lbl{
  font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary);
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap;
}
.rs-f>.lbl .req{color:var(--ez-error,#FF4C51)}
.rs-c3{grid-column:span 3}
.rs-c2{grid-column:span 2}
.rs-c1{grid-column:span 1}

.rs-f input[type="text"],
.rs-f input[type="number"]{
  width:100%;height:var(--ez-tf-h-md,38px);padding:0 12px;box-sizing:border-box;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md,6px);
  background:var(--misc-paper);font-family:inherit;font-weight:400;font-size:14px;
  line-height:22px;color:var(--theme-text-primary);
}
.rs-f input::placeholder{color:var(--theme-text-disabled)}
.rs-f input:hover{border-color:var(--ez-tf-hover)}
.rs-f input:focus{outline:none;border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.rs-dt{
  display:flex;align-items:center;gap:8px;width:100%;height:var(--ez-tf-h-md,38px);
  padding:0 12px;box-sizing:border-box;cursor:pointer;text-align:left;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md,6px);
  background:var(--misc-paper);font-family:inherit;font-weight:400;font-size:14px;
  line-height:22px;color:var(--theme-text-primary);
}
.rs-dt:hover{border-color:var(--ez-tf-hover)}
.rs-dt .v{flex:1 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.rs-dt .ic{width:18px;height:18px;flex:0 0 auto;color:var(--theme-text-secondary)}
.rs-dt .ic svg{width:100%;height:100%;display:block}

/* Ô nhiều lựa chọn (Loại phòng) */
.rs-chips{
  display:flex;align-items:center;gap:6px;flex-wrap:nowrap;overflow:hidden;
  width:100%;height:var(--ez-tf-h-md,38px);padding:0 8px;box-sizing:border-box;cursor:pointer;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md,6px);background:var(--misc-paper);
}
.rs-chips:hover{border-color:var(--ez-tf-hover)}
.rs-chip{
  display:inline-flex;align-items:center;gap:4px;flex:0 0 auto;height:24px;padding:0 4px 0 8px;
  border-radius:var(--tl-radius-sm,4px);background:var(--theme-chip-background);
  font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-primary);
}
.rs-chip .x{
  width:16px;height:16px;flex:0 0 auto;border:0;background:none;cursor:pointer;padding:0;
  display:inline-flex;align-items:center;justify-content:center;color:var(--theme-text-secondary);
}
.rs-chip .x svg{width:12px;height:12px;display:block}
.rs-chips .more{
  flex:0 0 auto;font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary);
}
.rs-chips .cv{margin-left:auto;width:18px;height:18px;flex:0 0 auto;color:var(--theme-text-secondary)}
.rs-chips .cv svg{width:100%;height:100%;display:block}

/* ---------- Thân bước 1: bảng + giỏ hàng ---------- */
.rs-body{flex:1 1 auto;min-height:0;display:flex;gap:0;overflow:hidden}
.rs-main{flex:1 1 auto;min-width:0;display:flex;flex-direction:column;padding:16px 20px;overflow:auto}

/* v42 — Bước 1 "Tìm và báo giá phòng": KHÔNG ghim bộ lọc.
   Cả bước là một vùng cuộn duy nhất: kéo xuống thì bộ lọc trôi theo,
   bảng "Danh sách phòng trống" trải hết chiều cao (không cuộn trong bảng).
   Tiêu đề bảng + cột phòng vẫn dính nhờ cuộn nằm ở chính #rsP1. */
#rsP1{overflow:auto}
#rsP1 .rs-filter{flex:0 0 auto}
#rsP1 .rs-body{flex:0 0 auto;min-height:0;overflow:visible}
#rsP1 .rs-main{flex:1 1 auto;overflow:visible}
#rsP1 .rs-tblwrap{flex:0 0 auto;min-height:0;overflow:visible}
#rsP1 .rs-cart{overflow:visible}
/* Chân màn vẫn bám đáy khung nhìn dù nội dung dài */
#rsP1 .rs-foot{position:sticky;bottom:0;z-index:4;background:var(--misc-paper)}
/* Tiêu đề khối + tiêu đề cột cùng dính theo vùng cuộn của cả bước.
   Khoá chiều cao .rs-mhead = 44px để đặt chính xác offset cho hàng <th>. */
#rsP1 .rs-mhead{
  position:sticky;top:0;z-index:6;background:var(--misc-paper);
  height:44px;box-sizing:border-box;margin-bottom:0;padding:4px 0;
}
#rsP1 .rs-tbl th{top:44px}
#rsP1 .rs-tbl th:first-child{z-index:4}
#rsP1 .rs-note{position:relative;z-index:0}
.rs-cart{
  flex:0 0 360px;width:360px;border-left:1px solid var(--tl-divider);
  display:flex;flex-direction:column;padding:16px;gap:12px;overflow:auto;
}
.rs-mhead{display:flex;align-items:center;gap:16px;flex:0 0 auto;margin-bottom:12px}
.rs-mhead h3{margin:0;flex:0 0 auto;font-weight:500;font-size:16px;line-height:24px;color:var(--theme-text-primary)}
.rs-heat{display:flex;align-items:center;gap:8px;cursor:pointer;position:relative;flex:0 0 auto}
.rs-heat input{position:absolute;opacity:0;width:0;height:0}
.rs-heat .box{
  width:18px;height:18px;flex:0 0 auto;border-radius:2px;border:2px solid rgba(47,43,61,.4);
  display:inline-flex;align-items:center;justify-content:center;
}
.rs-heat .box svg{width:12px;height:12px;opacity:0;color:#fff}
.rs-heat input:checked+.box{background:var(--ez-primary);border-color:var(--ez-primary)}
.rs-heat input:checked+.box svg{opacity:1}
.rs-heat .t{font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.rs-seg{display:flex;flex:0 0 auto;border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md,6px);overflow:hidden}
.rs-seg button{
  height:34px;padding:0 16px;border:0;background:var(--misc-paper);cursor:pointer;font-family:inherit;
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-secondary);
}
.rs-seg button[aria-pressed="true"]{background:var(--ez-primary-soft);color:var(--ez-primary);font-weight:500}

/* Bảng phòng trống */
.rs-tblwrap{flex:1 1 auto;min-height:0;overflow:auto}
.rs-tbl{border-collapse:separate;border-spacing:0;table-layout:auto;width:100%}
.rs-tbl th,.rs-tbl td{
  border-bottom:1px solid var(--tl-divider);border-right:1px solid var(--tl-divider);
  padding:0 12px;height:44px;box-sizing:border-box;white-space:nowrap;
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);text-align:center;
}
.rs-tbl th{
  position:sticky;top:0;z-index:2;background:var(--theme-table-header);
  font-weight:500;font-size:13px;line-height:20px;color:var(--theme-text-secondary);
}
.rs-tbl th:first-child,.rs-tbl td:first-child{
  position:sticky;left:0;z-index:1;background:var(--misc-paper);text-align:left;
  border-right:1px solid var(--tl-divider);
}
.rs-tbl th:first-child{z-index:3;background:var(--theme-table-header)}
.rs-tbl tr.rs-sum td{font-weight:500;background:var(--misc-grey-light,#FAFAFA)}
.rs-tbl tr.rs-sum td:first-child{background:var(--misc-grey-light,#FAFAFA)}
.rs-tbl td .low{color:var(--ez-error,#FF4C51)}
.rs-tbl td.rate{cursor:pointer;transition:background-color var(--ez-dur,.15s) var(--ez-ease,ease)}
.rs-tbl td.rate:hover{background:var(--ez-primary-soft)}
.rs-tbl td.rate.is-off{color:var(--theme-text-disabled);background:var(--theme-action-disabled-bg);cursor:not-allowed}
.rs-tbl td.rate.is-sel{outline:2px solid var(--ez-primary);outline-offset:-2px;font-weight:500;color:var(--ez-primary)}
/* Bản đồ nhiệt — 4 mức theo giá */
.rs-tbl td.rate.h1{background:rgba(24,111,244,.06)}
.rs-tbl td.rate.h2{background:rgba(24,111,244,.12)}
.rs-tbl td.rate.h3{background:rgba(24,111,244,.20)}
.rs-tbl td.rate.h4{background:rgba(24,111,244,.30)}
.rs-tbl .rcname{display:flex;align-items:center;gap:6px}
.rs-tbl .rcname .i{width:16px;height:16px;flex:0 0 auto;color:var(--theme-text-secondary)}
.rs-tbl .rcname .i svg{width:100%;height:100%;display:block}

.rs-note{
  flex:0 0 auto;margin-top:12px;
  font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary);
}
.rs-note b{color:var(--ez-error,#FF4C51);font-weight:400}

/* ---------- Giỏ hàng ---------- */
.rs-cart h3{margin:0;font-weight:500;font-size:16px;line-height:24px;color:var(--theme-text-primary)}
.rs-empty{
  flex:1 1 auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:12px;
  color:var(--theme-text-secondary);
}
.rs-empty svg{width:88px;height:88px;color:var(--ez-primary);opacity:.5}
.rs-empty p{margin:0;font-weight:500;font-size:15px;line-height:24px}
.rs-item{
  border:1px solid var(--tl-divider);border-radius:var(--tl-radius-lg,8px);
  padding:12px;display:flex;flex-direction:column;gap:8px;background:var(--misc-paper);
}
.rs-item .top{display:flex;align-items:flex-start;gap:8px}
.rs-item .top .nm{flex:1 1 auto;min-width:0;font-weight:500;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.rs-item .top .rc{font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.rs-del{
  width:28px;height:28px;flex:0 0 auto;border:0;background:none;cursor:pointer;border-radius:var(--tl-radius-sm,4px);
  display:inline-flex;align-items:center;justify-content:center;color:var(--ez-error,#FF4C51);
}
.rs-del:hover{background:var(--color-palette-error-opacity-lighter)}
.rs-del svg{width:16px;height:16px;display:block}
.rs-row{display:flex;align-items:center;gap:8px;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.rs-row .k{flex:1 1 auto;min-width:0}
.rs-row .v{flex:0 0 auto;color:var(--theme-text-primary);font-weight:500}
.rs-spin{display:inline-flex;align-items:center;border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-sm,4px);overflow:hidden}
.rs-spin button{
  width:26px;height:26px;border:0;background:var(--misc-paper);cursor:pointer;
  display:inline-flex;align-items:center;justify-content:center;color:var(--theme-text-primary);font-size:15px;
}
.rs-spin button:hover:not(:disabled){background:var(--tl-hover-bg)}
.rs-spin button:disabled{color:var(--theme-text-disabled);cursor:not-allowed}
.rs-spin .n{min-width:30px;text-align:center;font-weight:500;font-size:13px;color:var(--theme-text-primary)}
.rs-total{
  border-top:1px solid var(--tl-divider);padding-top:12px;display:flex;flex-direction:column;gap:6px;flex:0 0 auto;
}
.rs-total .r{display:flex;align-items:center;gap:8px;font-size:14px;line-height:22px;color:var(--theme-text-secondary)}
.rs-total .r .v{margin-left:auto;color:var(--theme-text-primary);font-weight:500}
.rs-total .r.big{font-size:16px;line-height:24px;color:var(--theme-text-primary);font-weight:500}
.rs-total .r.big .v{color:var(--ez-primary);font-weight:600}

/* ---------- Bước 2 · 3 ---------- */
.rs-cols{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}
.rs-card{
  border:1px solid var(--tl-divider);border-radius:var(--tl-radius-lg,8px);
  background:var(--misc-paper);padding:16px;display:flex;flex-direction:column;gap:12px;
}
.rs-card>h4{margin:0;font-weight:500;font-size:15px;line-height:24px;color:var(--theme-text-primary)}
.rs-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}
.rs-grid.one{grid-template-columns:1fr}
.rs-ro{
  display:flex;align-items:center;gap:8px;min-height:38px;padding:0 12px;box-sizing:border-box;
  border:1px solid var(--tl-divider);border-radius:var(--ez-tf-r-md,6px);
  background:var(--theme-action-disabled-bg);
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-secondary);
}
.rs-pay .r{display:flex;align-items:center;gap:8px;font-size:14px;line-height:22px;color:var(--theme-text-secondary)}
.rs-pay .r .v{margin-left:auto;color:var(--theme-text-primary);font-weight:500}
.rs-pay .r.due{border-top:1px solid var(--tl-divider);padding-top:10px;margin-top:4px;font-size:16px;line-height:24px}
.rs-pay .r.due .v{color:var(--ez-primary);font-weight:600;font-size:18px}
.rs-methods{display:flex;flex-wrap:wrap;gap:8px}
.rs-method{
  height:38px;padding:0 14px;border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md,6px);
  background:var(--misc-paper);cursor:pointer;font-family:inherit;
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.rs-method[aria-pressed="true"]{border-color:var(--ez-primary);background:var(--ez-primary-soft);color:var(--ez-primary);font-weight:500}

/* ---------- Chân màn: luôn ghim đáy, không cần cuộn hay thu nhỏ mới thấy ---------- */
.rs-foot{
  flex:0 0 auto;display:flex;align-items:center;gap:12px;
  padding:12px 20px;border-top:1px solid var(--tl-divider);
  background:var(--misc-paper);
}
.rs-foot .sp{flex:1 1 auto}

/* ---------- Menu nhiều lựa chọn ---------- */
.rs-msmenu{
  position:fixed;z-index:1272;min-width:220px;max-height:320px;overflow:auto;
  display:none;flex-direction:column;padding:6px;
  background:var(--misc-paper);border-radius:var(--tl-radius-md,6px);
  box-shadow:0 4px 18px 0 rgba(47,43,61,.16);
}
.rs-msmenu.is-open{display:flex}
.rs-msmenu label{
  display:flex;align-items:center;gap:8px;padding:6px 8px;cursor:pointer;position:relative;
  border-radius:var(--tl-radius-sm,4px);font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.rs-msmenu label:hover{background:var(--tl-hover-bg)}
.rs-msmenu input{position:absolute;opacity:0;width:0;height:0}
.rs-msmenu .box{
  width:18px;height:18px;flex:0 0 auto;border-radius:2px;border:2px solid rgba(47,43,61,.4);
  display:inline-flex;align-items:center;justify-content:center;
}
.rs-msmenu .box svg{width:12px;height:12px;opacity:0;color:#fff}
.rs-msmenu input:checked+.box{background:var(--ez-primary);border-color:var(--ez-primary)}
.rs-msmenu input:checked+.box svg{opacity:1}

@media (max-width:1280px){
  .rs-cart{flex:0 0 300px;width:300px}
  .rs-cols{grid-template-columns:1fr}
}

/* ---------- Trạng thái cột / ô theo SRS mục 17 · 22-25 ---------- */
.rs-tbl th.is-off,
.rs-tbl td.is-off:first-child{color:var(--theme-text-disabled)}
.rs-tbl td.is-off{
  color:var(--theme-text-disabled);
  background:var(--theme-action-disabled-bg);
  cursor:not-allowed;
}
.rs-tbl th .tname{
  display:inline-block;max-width:120px;overflow:hidden;text-overflow:ellipsis;
  white-space:nowrap;vertical-align:bottom;
}
.rs-tbl td.rate .warn{
  display:inline-flex;width:14px;height:14px;margin-left:4px;vertical-align:-2px;
  color:var(--color-palette-warning-main,#FF9F43);
}
.rs-tbl td.rate .warn svg{width:100%;height:100%;display:block}
.rs-tbl .rcname .i{
  width:18px;height:18px;flex:0 0 auto;padding:0;border:0;background:none;cursor:pointer;
  display:inline-flex;align-items:center;justify-content:center;color:var(--ez-primary);
}
.rs-tbl .rcname .i svg{width:16px;height:16px;display:block}
.rs-hint{height:120px;text-align:center;color:var(--theme-text-secondary)}

/* ---------- Giỏ hàng: đầu mục + chặng ---------- */
.rs-carth{display:flex;align-items:center;gap:8px;flex:0 0 auto}
.rs-carth h3{flex:1 1 auto;min-width:0}
.rs-clear{
  display:inline-flex;align-items:center;gap:6px;flex:0 0 auto;height:30px;padding:0 10px;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md,6px);
  background:var(--misc-paper);cursor:pointer;font-family:inherit;
  font-weight:400;font-size:13px;line-height:20px;color:var(--ez-error,#FF4C51);
}
.rs-clear:hover{background:var(--color-palette-error-opacity-lighter);border-color:var(--ez-error,#FF4C51)}
.rs-clear svg{width:14px;height:14px;flex:0 0 auto;display:block}

.rs-seg-card{
  border:1px solid var(--tl-divider);border-radius:var(--tl-radius-lg,8px);
  overflow:hidden;margin-bottom:12px;background:var(--misc-paper);
}
.rs-seg-h{
  display:flex;flex-direction:column;gap:2px;padding:8px 12px;
  background:var(--theme-table-header);border-bottom:1px solid var(--tl-divider);
}
.rs-seg-h .dt{font-weight:500;font-size:13px;line-height:20px;color:var(--theme-text-primary)}
.rs-seg-h .dt .nt{font-weight:400;color:var(--theme-text-secondary)}
.rs-seg-h .gs{font-weight:400;font-size:12px;line-height:18px;color:var(--theme-text-secondary)}
.rs-seg-card .rs-item{border:0;border-radius:0;border-bottom:1px solid var(--tl-divider)}
.rs-seg-card .rs-item:last-child{border-bottom:0}

/* ================= THÊM DỊCH VỤ (R01.14 Order dịch vụ) — Figma 43142:300070 ================= */

.sv-bd{position:fixed;inset:0;background:var(--theme-backdrop-overlay,rgba(47,43,61,.5));z-index:1214;display:none}
.sv-bd.is-open{display:block}

.sv{
  position:fixed;top:50%;left:50%;z-index:1216;
  width:min(1400px,94vw);height:min(900px,92vh);box-sizing:border-box;
  display:none;flex-direction:column;overflow:hidden;
  background:var(--misc-paper);border-radius:var(--tl-radius-lg,8px);
  box-shadow:0 8px 32px rgba(47,43,61,.24);
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
  transform:translate(-50%,-50%) scale(.98);opacity:0;
  transition:transform .16s var(--tl-ease,cubic-bezier(.4,0,.2,1)),opacity .16s var(--tl-ease,cubic-bezier(.4,0,.2,1));
}
.sv.is-open{display:flex;transform:translate(-50%,-50%) scale(1);opacity:1}

/* ---------- Đầu ---------- */
.sv-head{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;height:60px;padding:0 16px;
  border-bottom:1px solid var(--tl-divider);
}
.sv-head h2{margin:0;flex:1 1 auto;font-weight:500;font-size:18px;line-height:28px;color:var(--theme-text-primary)}
.sv-x{
  width:24px;height:24px;flex:0 0 auto;padding:0;border:0;background:none;cursor:pointer;
  display:inline-flex;align-items:center;justify-content:center;color:var(--theme-text-secondary);
}
.sv-x svg{width:20px;height:20px;display:block}
.sv-x:hover{color:var(--theme-text-primary)}

.sv-body{flex:1 1 auto;min-height:0;display:flex}

/* ---------- Cột trái: menu dịch vụ ---------- */
.sv-left{
  flex:0 0 620px;width:620px;min-width:0;display:flex;flex-direction:column;
  border-right:1px solid var(--tl-divider);
}
.sv-lhead{flex:0 0 auto;padding:16px 16px 0;display:flex;flex-direction:column;gap:8px}
.sv-lrow{display:flex;gap:8px}
.sv-lrow>*{flex:1 1 0;min-width:0}
.sv-search{
  display:flex;align-items:center;gap:8px;height:var(--ez-tf-h-md,38px);padding:0 12px;box-sizing:border-box;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md,6px);background:var(--misc-paper);
}
.sv-search:focus-within{border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.sv-search svg{width:16px;height:16px;flex:0 0 auto;color:var(--theme-text-secondary)}
.sv-search input{
  flex:1 1 auto;min-width:0;border:0;background:none;outline:none;font-family:inherit;
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.sv-search input::placeholder{color:var(--theme-text-disabled)}

/* Tabs danh mục + 2 nút cuộn */
.sv-tabs{display:flex;align-items:center;gap:4px;height:38px;border-bottom:1px solid var(--tl-divider)}
.sv-tarrow{
  width:30px;height:30px;flex:0 0 auto;padding:0;border:0;background:none;cursor:pointer;border-radius:var(--tl-radius-sm,4px);
  display:inline-flex;align-items:center;justify-content:center;color:var(--theme-text-secondary);
}
.sv-tarrow:hover{background:var(--tl-hover-bg)}
.sv-tarrow svg{width:18px;height:18px;display:block}
.sv-tstrip{flex:1 1 auto;min-width:0;display:flex;gap:0;overflow:hidden;scroll-behavior:smooth}
.sv-tab{
  flex:0 0 auto;height:38px;padding:0 14px;border:0;border-bottom:2px solid transparent;background:none;cursor:pointer;
  font-family:inherit;font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-secondary);white-space:nowrap;
  transition:color var(--ez-dur,.15s) var(--ez-ease,ease),border-color var(--ez-dur,.15s) var(--ez-ease,ease);
}
.sv-tab:hover{color:var(--theme-text-primary)}
.sv-tab[aria-selected="true"]{color:var(--ez-primary);border-bottom-color:var(--ez-primary);font-weight:500}

/* Lưới thẻ sản phẩm — 4 cột, thẻ 150×188 theo Figma */
.sv-grid{
  flex:1 1 auto;min-height:0;overflow:auto;padding:4px;
  display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:4px;align-content:start;
}
.sv-card{
  position:relative;height:188px;box-sizing:border-box;padding:0;cursor:pointer;text-align:left;
  border:1px solid var(--tl-divider);border-radius:var(--tl-radius-sm,4px);background:var(--misc-paper);
  display:flex;flex-direction:column;overflow:hidden;font-family:inherit;
  transition:border-color var(--ez-dur,.15s) var(--ez-ease,ease),box-shadow var(--ez-dur,.15s) var(--ez-ease,ease);
}
.sv-card:hover{border-color:var(--ez-primary);box-shadow:0 2px 8px rgba(47,43,61,.12)}
.sv-card:focus-visible{outline:2px solid var(--ez-primary);outline-offset:-2px}
.sv-card .img{
  flex:0 0 118px;height:118px;background:var(--misc-grey-light,#FAFAFA);
  display:flex;align-items:center;justify-content:center;overflow:hidden;
}
.sv-card .img svg{width:52px;height:52px;color:var(--theme-text-disabled)}
.sv-card .bd{flex:1 1 auto;min-height:0;display:flex;flex-direction:column;gap:2px;padding:6px 8px}
.sv-card .nm{
  font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-primary);
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap;
}
.sv-card .cd{font-weight:400;font-size:12px;line-height:18px;color:var(--theme-text-secondary)}
.sv-card .pr{margin-top:auto;text-align:right;font-weight:500;font-size:13px;line-height:20px;color:var(--ez-error,#FF4C51)}
.sv-card .pr .old{
  margin-right:6px;font-weight:400;color:var(--theme-text-disabled);text-decoration:line-through;
}
.sv-card .off{
  position:absolute;top:0;left:0;z-index:1;height:22px;padding:0 8px;
  border-radius:var(--tl-radius-sm,4px) 0 var(--tl-radius-sm,4px) 0;
  background:var(--ez-error,#FF4C51);color:#fff;
  display:inline-flex;align-items:center;font-weight:500;font-size:12px;line-height:18px;
}

/* Phân trang */
.sv-page{
  flex:0 0 auto;height:46px;display:flex;align-items:center;gap:8px;padding:0 12px;
  border-top:1px solid var(--tl-divider);
}
.sv-page .lb{font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.sv-page .nav{margin-left:auto;display:flex;align-items:center;gap:4px}
.sv-pbtn{
  min-width:30px;height:30px;padding:0 6px;box-sizing:border-box;cursor:pointer;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-sm,4px);background:var(--misc-paper);
  font-family:inherit;font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-primary);
  display:inline-flex;align-items:center;justify-content:center;
}
.sv-pbtn:hover:not(:disabled){border-color:var(--ez-tf-hover)}
.sv-pbtn:disabled{color:var(--theme-text-disabled);cursor:not-allowed}
.sv-pbtn.is-cur{background:var(--ez-primary);border-color:var(--ez-primary);color:#fff;font-weight:500}
.sv-pbtn svg{width:14px;height:14px;display:block}

/* ---------- Cột phải: dịch vụ đã chọn ---------- */
.sv-right{flex:1 1 auto;min-width:0;display:flex;flex-direction:column;padding:16px}
.sv-rhead{display:flex;align-items:center;gap:12px;flex:0 0 auto;margin-bottom:12px}
.sv-rhead h3{margin:0;flex:1 1 auto;font-weight:500;font-size:18px;line-height:28px;color:var(--theme-text-primary)}
.sv-clear{
  display:inline-flex;align-items:center;gap:6px;flex:0 0 auto;height:30px;padding:0 8px;
  border:0;background:none;cursor:pointer;border-radius:var(--tl-radius-sm,4px);font-family:inherit;
  font-weight:400;font-size:14px;line-height:22px;color:var(--ez-error,#FF4C51);
}
.sv-clear:hover{background:var(--color-palette-error-opacity-lighter)}
.sv-clear svg{width:16px;height:16px;flex:0 0 auto;display:block}

.sv-tblwrap{flex:1 1 auto;min-height:0;overflow:auto}
.sv-tbl{width:100%;border-collapse:separate;border-spacing:0;table-layout:auto}
.sv-tbl th{
  position:sticky;top:0;z-index:1;height:48px;padding:0 12px;box-sizing:border-box;
  background:var(--theme-table-header);border-bottom:1px solid var(--tl-divider);
  font-weight:500;font-size:12px;line-height:16px;letter-spacing:.3px;text-transform:uppercase;
  color:var(--theme-text-secondary);text-align:left;white-space:nowrap;
}
.sv-tbl th.num,.sv-tbl td.num{text-align:right}
.sv-tbl th.c,.sv-tbl td.c{text-align:center}
.sv-tbl td{
  padding:10px 12px;border-bottom:1px solid var(--tl-divider);vertical-align:middle;
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.sv-tbl td.num{white-space:nowrap}
.sv-tbl .nm{font-weight:500;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.sv-tbl .sub{display:block;font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.sv-tbl .sub.pro{color:var(--ez-error,#FF4C51)}
.sv-acts{display:flex;align-items:center;gap:4px;justify-content:flex-end}
.sv-ib{
  width:28px;height:28px;flex:0 0 auto;padding:0;border:0;background:none;cursor:pointer;
  border-radius:var(--tl-radius-sm,4px);display:inline-flex;align-items:center;justify-content:center;
  color:var(--theme-text-secondary);
}
.sv-ib:hover{background:var(--tl-hover-bg);color:var(--theme-text-primary)}
.sv-ib.danger:hover{background:var(--color-palette-error-opacity-lighter);color:var(--ez-error,#FF4C51)}
.sv-ib svg{width:16px;height:16px;display:block}

/* Spinner số lượng */
.sv-spin{
  display:inline-flex;align-items:center;height:var(--ez-tf-h-sm,30px);
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-sm,4px);overflow:hidden;background:var(--misc-paper);
}
.sv-spin .n{
  min-width:34px;text-align:center;font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.sv-spin button{
  width:22px;height:28px;border:0;background:none;cursor:pointer;color:var(--theme-text-secondary);
  font-family:inherit;font-size:14px;line-height:1;
}
.sv-spin button:hover:not(:disabled){background:var(--tl-hover-bg);color:var(--theme-text-primary)}
.sv-spin button:disabled{color:var(--theme-text-disabled);cursor:not-allowed}

.sv-empty{
  display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;
  padding:60px 20px;color:var(--theme-text-secondary);
}
.sv-empty svg{width:56px;height:56px;color:var(--theme-text-disabled)}
.sv-empty p{margin:0;font-weight:500;font-size:15px;line-height:24px;color:var(--theme-text-primary)}
.sv-empty .s{font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}

/* Tổng kết */
.sv-sum{flex:0 0 auto;padding:12px 0 0;display:flex;flex-direction:column;gap:6px;align-items:flex-end}
.sv-sum .r{display:flex;align-items:baseline;gap:12px;font-weight:400;font-size:14px;line-height:22px}
.sv-sum .r .k{color:var(--theme-text-secondary)}
.sv-sum .r .v{min-width:150px;text-align:right;color:var(--theme-text-primary)}
.sv-sum .r.big{font-weight:500;font-size:18px;line-height:28px}
.sv-sum .r.big .v{color:var(--ez-error,#FF4C51)}

/* ---------- Chân ---------- */
.sv-foot{
  flex:0 0 auto;height:70px;display:flex;align-items:center;justify-content:flex-end;gap:12px;
  padding:0 16px;border-top:1px solid var(--tl-divider);
}

/* ---------- Popup Chi tiết dịch vụ ---------- */
.sv-dbd{position:fixed;inset:0;background:var(--theme-backdrop-overlay,rgba(47,43,61,.5));z-index:1217;display:none}
.sv-dbd.is-open{display:block}
.sv-dlg{
  position:fixed;top:50%;left:50%;z-index:1218;
  width:min(732px,94vw);max-height:92vh;box-sizing:border-box;
  display:none;flex-direction:column;overflow:hidden;
  background:var(--misc-paper);border-radius:var(--tl-radius-lg,8px);
  box-shadow:0 8px 32px rgba(47,43,61,.24);
  transform:translate(-50%,-50%);
}
.sv-dlg.is-open{display:flex}
.sv-dlg>header{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;height:64px;padding:0 20px;
  border-bottom:1px solid var(--tl-divider);
}
.sv-dlg>header h3{margin:0;flex:1 1 auto;font-weight:500;font-size:16px;line-height:24px;color:var(--theme-text-primary)}
.sv-dbody{flex:1 1 auto;min-height:0;overflow:auto;padding:16px 20px;display:flex;flex-direction:column;gap:16px}
.sv-drow{display:flex;align-items:center;gap:12px}
.sv-drow>.k{flex:0 0 110px;font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.sv-drow>.f{flex:1 1 auto;min-width:0;max-width:257px}
.sv-dlg input[type="text"],.sv-dlg input[type="number"]{
  width:100%;height:var(--ez-tf-h-md,38px);padding:0 12px;box-sizing:border-box;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md,6px);background:var(--misc-paper);
  font-family:inherit;font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.sv-dlg input::placeholder{color:var(--theme-text-disabled)}
.sv-dlg input:focus{outline:none;border-color:var(--ez-primary);box-shadow:var(--ez-tf-shadow)}
.sv-dck{display:flex;flex-direction:column;gap:10px}
.sv-ck{position:relative;display:flex;align-items:center;gap:8px;cursor:pointer}
.sv-ck input{position:absolute;opacity:0;width:0;height:0}
.sv-ck .box{
  width:18px;height:18px;flex:0 0 auto;border-radius:2px;border:2px solid var(--theme-input-border);
  display:inline-flex;align-items:center;justify-content:center;
}
.sv-ck .box svg{width:12px;height:12px;opacity:0;color:#fff}
.sv-ck input:checked+.box{background:var(--ez-primary);border-color:var(--ez-primary)}
.sv-ck input:checked+.box svg{opacity:1}
.sv-ck .t{font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.sv-disc{display:flex;flex-direction:column;gap:10px;padding:12px;border-radius:var(--tl-radius-lg,8px);background:var(--theme-table-header)}
.sv-disc .two{display:flex;gap:8px}
.sv-disc .two>*{flex:1 1 0;min-width:0}
.sv-dfoot{
  flex:0 0 auto;height:70px;display:flex;align-items:center;justify-content:flex-end;gap:12px;
  padding:0 20px;border-top:1px solid var(--tl-divider);
}

/* ================= QUY TRÌNH TRẢ PHÒNG (R01.21 Checkout) — Figma 45799:283821 / 45799:281120 ================= */

.co-bd{position:fixed;inset:0;background:var(--theme-backdrop-overlay,rgba(47,43,61,.5));z-index:1214;display:none}
.co-bd.is-open{display:block}

/* v41: check-out chuyển từ modal giữa màn -> slide-over phải 85% bề ngang màn hình
   (đồng bộ với check-in .ci, theo yêu cầu "checkin/checkout dạng slidebar 85%") */
.co{
  position:fixed;top:0;right:0;z-index:1216;
  width:85vw;max-width:100vw;height:100%;box-sizing:border-box;
  display:none;flex-direction:column;overflow:hidden;
  background:var(--misc-paper);border-radius:0;
  box-shadow:-8px 0 32px rgba(47,43,61,.24);
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
  transform:translateX(24px);opacity:0;
  transition:transform .16s var(--tl-ease,cubic-bezier(.4,0,.2,1)),opacity .16s var(--tl-ease,cubic-bezier(.4,0,.2,1));
}
.co.is-open{display:flex;transform:translateX(0);opacity:1}

/* ---------- Đầu ---------- */
.co-head{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;height:60px;padding:0 16px;
  border-bottom:1px solid var(--tl-divider);
}
.co-head h2{margin:0;flex:1 1 auto;font-weight:500;font-size:18px;line-height:28px;color:var(--theme-text-primary)}
.co-chip{
  position:relative;display:inline-flex;align-items:center;gap:6px;flex:0 0 auto;height:24px;padding:0 10px;
  border-radius:9999px;background:var(--color-palette-success-opacity-lighter,rgba(40,199,111,.14));
  font-weight:500;font-size:13px;line-height:20px;color:var(--color-palette-success-dark,#24B364);
}
.co-chip svg{width:14px;height:14px;flex:0 0 auto;display:block}
.co-head .co-chip{margin-right:14px}
.co-chip .n{
  position:absolute;top:-8px;right:-8px;min-width:20px;height:20px;padding:0 5px;box-sizing:border-box;
  border-radius:9999px;background:var(--ez-error,#FF4C51);color:#fff;
  display:inline-flex;align-items:center;justify-content:center;
  font-weight:500;font-size:12px;line-height:18px;
}
.co-x{
  width:24px;height:24px;flex:0 0 auto;padding:0;border:0;background:none;cursor:pointer;
  display:inline-flex;align-items:center;justify-content:center;color:var(--theme-text-secondary);
}
.co-x svg{width:20px;height:20px;display:block}
.co-x:hover{color:var(--theme-text-primary)}

/* ---------- Stepper ---------- */
.co-steps{
  display:flex;align-items:center;gap:8px;flex:0 0 auto;height:66px;padding:0 20px;
  border-bottom:1px solid var(--tl-divider);
}
.co-step{
  display:flex;align-items:center;gap:12px;border:0;background:none;cursor:pointer;
  padding:2px 6px;border-radius:var(--tl-radius-md,6px);font-family:inherit;
  transition:background-color var(--ez-dur,.15s) var(--ez-ease,ease);
}
.co-step:hover:not(:disabled){background:var(--tl-hover-bg)}
.co-step:disabled{cursor:not-allowed}
.co-step .no{
  width:30px;height:30px;flex:0 0 auto;border-radius:var(--tl-radius-md,6px);
  display:inline-flex;align-items:center;justify-content:center;
  background:var(--theme-action-selected);color:var(--theme-text-secondary);
  font-weight:500;font-size:14px;line-height:22px;
}
.co-step .tx{font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-secondary);white-space:nowrap}
.co-step.is-cur .no{background:var(--ez-primary);color:#fff}
.co-step.is-cur .tx{font-weight:500;color:var(--theme-text-primary)}
.co-step.is-done .no{background:var(--color-palette-primary-opacity-light,rgba(24,111,244,.16));color:var(--ez-primary)}
.co-step.is-done .tx{color:var(--theme-text-primary)}
.co-step:disabled .tx{color:var(--theme-text-disabled)}
.co-sep{width:18px;height:18px;flex:0 0 auto;color:var(--theme-text-disabled)}
.co-sep svg{width:100%;height:100%;display:block}

/* ---------- Thanh công cụ ---------- */
.co-tools{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;padding:12px 20px;
}
.co-hint{
  display:inline-flex;align-items:center;gap:8px;flex:1 1 auto;min-width:0;
  font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary);
}
.co-hint svg{width:16px;height:16px;flex:0 0 auto;color:var(--theme-text-secondary)}
.co-tabs{display:flex;align-items:center;gap:8px;flex:1 1 auto;min-width:0}
.co-tab{
  height:38px;padding:0 16px;border:0;border-radius:var(--tl-radius-md,6px);background:none;cursor:pointer;
  font-family:inherit;font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-secondary);white-space:nowrap;
}
.co-tab:hover{background:var(--tl-hover-bg)}
.co-tab[aria-selected="true"]{background:var(--ez-primary);color:#fff;font-weight:500}

/* ---------- Bảng phòng ---------- */
.co-body{flex:1 1 auto;min-height:0;display:flex;flex-direction:column;overflow:hidden}
.co-tblwrap{flex:1 1 auto;min-height:0;overflow:auto}
.co-tbl{width:100%;border-collapse:separate;border-spacing:0;table-layout:auto}
.co-tbl th{
  position:sticky;top:0;z-index:2;height:44px;padding:0 12px;box-sizing:border-box;
  background:var(--theme-table-header);border-bottom:1px solid var(--tl-divider);
  font-weight:500;font-size:12px;line-height:16px;letter-spacing:.3px;text-transform:uppercase;
  color:var(--theme-text-secondary);text-align:left;white-space:nowrap;
}
.co-tbl th.num,.co-tbl td.num{text-align:right}
.co-tbl th.c,.co-tbl td.c{text-align:center}
.co-tbl td{
  height:48px;padding:0 12px;border-bottom:1px solid var(--tl-divider);vertical-align:middle;
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);white-space:nowrap;
}
.co-tbl tr.co-grp>td{background:var(--misc-grey-light);font-weight:500}
.co-tbl tr.co-row:hover>td{background:var(--tl-hover-bg)}
.co-tbl .rm{display:inline-flex;align-items:center;gap:8px}
.co-tbl .rm .ic{width:18px;height:18px;flex:0 0 auto;color:var(--tl-st-inhouse,#DA4343)}
.co-tbl .rm .ic svg{width:100%;height:100%;display:block}
.co-tbl .gu{display:inline-flex;align-items:center;gap:8px;min-width:0}
.co-tbl .gu .fg{width:20px;height:14px;flex:0 0 auto;border-radius:2px;overflow:hidden}
.co-tbl .gu .fg svg{width:100%;height:100%;display:block}
.co-tbl .gu .nm{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;max-width:150px}
.co-tbl .stay{white-space:normal;line-height:20px;font-size:13px}
.co-tbl .stay .out{color:var(--ez-primary)}
.co-tbl .lock{display:inline-flex;align-items:center;gap:4px;justify-content:flex-end}
.co-tbl .lock svg{width:14px;height:14px;flex:0 0 auto;color:var(--theme-text-secondary)}
.co-tbl .left0{color:var(--ez-error,#FF4C51)}
.co-tbl .cr{
  width:24px;height:24px;padding:0;border:0;background:none;cursor:pointer;color:var(--theme-text-secondary);
  display:inline-flex;align-items:center;justify-content:center;
}
.co-tbl .cr svg{width:16px;height:16px;display:block;transition:transform var(--ez-dur,.15s) var(--ez-ease,ease)}
.co-tbl .cr.is-closed svg{transform:rotate(-90deg)}
.co-btn-pay{
  height:30px;padding:0 12px;border:0;border-radius:var(--tl-radius-md,6px);cursor:pointer;font-family:inherit;
  background:var(--color-palette-secondary-opacity-light,rgba(128,131,144,.16));color:#9C27B0;
  font-weight:500;font-size:13px;line-height:20px;
}
.co-btn-pay:hover{background:rgba(156,39,176,.16)}

/* Ô chọn */
.co-ck{position:relative;display:inline-flex;align-items:center;cursor:pointer}
.co-ck input{position:absolute;opacity:0;width:0;height:0}
.co-ck .box{
  width:18px;height:18px;border-radius:2px;border:2px solid var(--theme-input-border);
  display:inline-flex;align-items:center;justify-content:center;
}
.co-ck .box svg{width:12px;height:12px;opacity:0;color:#fff}
.co-ck input:checked+.box{background:var(--ez-primary);border-color:var(--ez-primary)}
.co-ck input:checked+.box svg{opacity:1}

/* Dòng tổng cộng */
.co-sum{
  flex:0 0 auto;display:flex;align-items:center;gap:0;height:56px;padding:0 12px;
  border-top:1px solid var(--tl-divider);background:var(--theme-table-header);
  font-weight:500;font-size:14px;line-height:22px;
}
.co-sum .lb{flex:1 1 auto;text-align:right;padding-right:16px;color:var(--theme-text-primary)}
.co-sum .v{width:150px;text-align:right;padding-right:12px;color:var(--theme-text-primary)}
.co-sum .v.left0{color:var(--ez-error,#FF4C51)}

/* ---------- Chân ---------- */
.co-foot{
  flex:0 0 auto;display:flex;align-items:center;gap:12px;height:70px;padding:0 16px;
  border-top:1px solid var(--tl-divider);
}
.co-foot .sp{flex:1 1 auto}
.co-note{
  display:inline-flex;align-items:center;gap:8px;
  font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary);
}
.co-note svg{width:16px;height:16px;flex:0 0 auto}
.co-btn-all{
  height:38px;padding:0 16px;border:0;border-radius:var(--tl-radius-md,6px);cursor:pointer;font-family:inherit;
  background:var(--color-palette-success-main,#28C76F);color:#fff;font-weight:500;font-size:14px;line-height:22px;
}
.co-btn-all:hover{background:var(--color-palette-success-dark,#24B364)}
.co-btn-all:disabled{background:var(--theme-action-disabled-bg);color:var(--theme-text-disabled);cursor:not-allowed}

/* ---------- Bước 3: hoá đơn điện tử ---------- */
.co-done{
  flex:1 1 auto;min-height:0;display:flex;flex-direction:column;align-items:center;justify-content:center;
  gap:12px;padding:40px;text-align:center;
}
.co-done .ic{
  width:72px;height:72px;border-radius:9999px;display:flex;align-items:center;justify-content:center;
  background:var(--color-palette-success-opacity-lighter,rgba(40,199,111,.14));color:var(--color-palette-success-main,#28C76F);
}
.co-done .ic svg{width:40px;height:40px}
.co-done h3{margin:0;font-weight:500;font-size:20px;line-height:30px;color:var(--theme-text-primary)}
.co-done p{margin:0;font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-secondary);max-width:520px}
.co-inv{
  width:100%;max-width:520px;margin-top:8px;padding:16px;box-sizing:border-box;
  border:1px solid var(--tl-divider);border-radius:var(--tl-radius-lg,8px);background:var(--theme-table-header);
  display:flex;flex-direction:column;gap:8px;
}
.co-inv .r{display:flex;align-items:baseline;gap:12px;font-weight:400;font-size:14px;line-height:22px}
.co-inv .r .k{flex:1 1 auto;text-align:left;color:var(--theme-text-secondary)}
.co-inv .r .v{color:var(--theme-text-primary)}
.co-inv .r.big{font-weight:500;font-size:16px;line-height:24px}
.co-inv .r.big .v{color:var(--color-palette-success-dark,#24B364)}

/* ---------- Popup Cập nhật thời gian trả phòng ---------- */
.co-dbd{position:fixed;inset:0;background:var(--theme-backdrop-overlay,rgba(47,43,61,.5));z-index:1217;display:none}
.co-dbd.is-open{display:block}
.co-dlg{
  position:fixed;top:50%;left:50%;z-index:1218;width:min(453px,94vw);box-sizing:border-box;
  display:none;flex-direction:column;overflow:hidden;transform:translate(-50%,-50%);
  background:var(--misc-paper);border-radius:var(--tl-radius-lg,8px);box-shadow:0 8px 32px rgba(47,43,61,.24);
}
.co-dlg.is-open{display:flex}
.co-dlg>header{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;height:60px;padding:0 16px;
  border-bottom:1px solid var(--tl-divider);
}
.co-dlg>header h3{margin:0;flex:1 1 auto;font-weight:500;font-size:16px;line-height:24px}
.co-dbody{flex:1 1 auto;padding:16px;display:flex;flex-direction:column;gap:12px}
.co-dbody .f{display:flex;flex-direction:column;gap:4px}
.co-dbody .f>.k{font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.co-dfoot{
  flex:0 0 auto;display:flex;align-items:center;justify-content:flex-end;gap:12px;height:70px;padding:0 16px;
  border-top:1px solid var(--tl-divider);
}

/* ================= KIỂM TOÁN ĐÊM (FD01) — Figma 43747:47069 ================= */

.na{
  display:flex;flex-direction:column;gap:12px;padding:0;background:none;border:0;box-shadow:none;
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);overflow:auto;
}
.na[hidden]{display:none}

/* ---------- Thanh đầu: ngày kinh doanh + hành động ---------- */
.na-top{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;
  padding:16px 20px;box-sizing:border-box;
  background:var(--misc-paper);border:1px solid var(--tl-divider);border-radius:var(--tl-radius-lg,8px);
}
.na-bd-lb{font-weight:500;font-size:16px;line-height:24px;color:var(--theme-text-primary)}
.na-bd-val{font-weight:500;font-size:16px;line-height:24px;color:var(--ez-primary,#186FF4)}
.na-bd-val.is-off{color:var(--theme-text-disabled)}
.na-top .sp{flex:1 1 auto}
.na-edit{
  width:24px;height:24px;flex:0 0 auto;padding:0;border:0;background:none;cursor:pointer;
  display:inline-flex;align-items:center;justify-content:center;color:var(--ez-primary,#186FF4);
}
.na-edit svg{width:18px;height:18px;display:block}
.na-edit:disabled{color:var(--theme-text-disabled);cursor:not-allowed}

/* ---------- Thẻ quy trình ---------- */
.na-card{
  flex:1 1 auto;min-height:0;display:flex;flex-direction:column;gap:16px;
  padding:16px 20px 20px;box-sizing:border-box;
  background:var(--misc-paper);border:1px solid var(--tl-divider);border-radius:var(--tl-radius-lg,8px);
}
.na-cardhead{display:flex;align-items:center;gap:12px;flex:0 0 auto}
.na-cardhead h2{margin:0;font-weight:500;font-size:16px;line-height:24px;color:var(--theme-text-primary)}
.na-chip{
  display:inline-flex;align-items:center;height:24px;padding:0 10px;border-radius:9999px;
  font-weight:500;font-size:13px;line-height:20px;
  background:var(--theme-action-selected);color:var(--theme-text-secondary);
}
.na-chip.is-run{background:rgba(24,111,244,.12);color:var(--ez-primary,#186FF4)}
.na-chip.is-done{background:rgba(40,199,111,.14);color:#24B364}

/* ---------- Danh sách bước ---------- */
.na-steps{display:flex;flex-direction:column;gap:12px;flex:0 0 auto}
.na-step{display:flex;align-items:flex-start;gap:16px;min-height:48px}
.na-no{
  width:38px;height:38px;flex:0 0 auto;border-radius:var(--tl-radius-md,6px);
  display:inline-flex;align-items:center;justify-content:center;
  background:var(--theme-action-selected);color:var(--theme-text-secondary);
  font-weight:500;font-size:16px;line-height:24px;
}
.na-step.is-done .na-no{background:rgba(40,199,111,.14);color:#24B364}
.na-step.is-run  .na-no{background:var(--ez-primary,#186FF4);color:#fff}
.na-step.is-err  .na-no{background:rgba(255,76,81,.14);color:var(--ez-error,#FF4C51)}
.na-txt{display:flex;flex-direction:column;gap:4px;padding-top:6px;min-width:0}
.na-nm{font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.na-step.is-idle .na-nm{color:var(--theme-text-primary)}
.na-sub{font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.na-sub .ok{color:#24B364}
.na-sub .run{color:var(--ez-primary,#186FF4)}
.na-sub .err{color:var(--ez-error,#FF4C51)}
.na-link{
  border:0;background:none;padding:0;margin:0;cursor:pointer;font:inherit;
  color:var(--ez-error,#FF4C51);text-decoration:underline;text-underline-offset:2px;
}
.na-fix{align-self:flex-start;margin-top:4px}

/* ---------- Cảnh báo ---------- */
.na-warn{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;
  padding:12px 16px;box-sizing:border-box;border-radius:var(--tl-radius-lg,8px);
  background:rgba(255,159,67,.12);color:var(--theme-text-primary);
  font-weight:400;font-size:14px;line-height:22px;
}
.na-warn svg{width:20px;height:20px;flex:0 0 auto;display:block;color:var(--ez-warning,#FF9F43)}

/* ================= CẤU HÌNH KIỂM TOÁN ĐÊM ================= */
.na-cfg{display:flex;flex-direction:column;gap:20px;flex:1 1 auto;min-height:0}
.na-cfgrow{display:flex;align-items:center;gap:12px}
.na-cfgrow .k{font-weight:500;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.na-times{display:flex;gap:16px;flex-wrap:wrap}
.na-f{display:flex;flex-direction:column;gap:4px;width:240px}
.na-f .k{font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.na-f .k i{color:var(--ez-error,#FF4C51);font-style:normal}
.na-f input[type="time"]{
  height:38px;box-sizing:border-box;padding:0 12px;font-family:inherit;font-size:14px;line-height:22px;
  color:var(--theme-text-primary);background:var(--misc-paper);
  border:1px solid var(--ez-tf-border,rgba(47,43,61,.22));border-radius:var(--tl-radius-lg,8px);
}
.na-f input[type="time"]:focus{outline:none;border-color:var(--ez-primary,#186FF4)}
.na-cfglist{display:flex;flex-direction:column;gap:4px}
.na-cfgitem{
  display:flex;align-items:center;gap:12px;min-height:44px;padding:0 12px;box-sizing:border-box;
  border-radius:var(--tl-radius-lg,8px);
}
.na-cfgitem:nth-child(odd){background:var(--theme-action-hover,rgba(47,43,61,.04))}
.na-cfgitem .n{
  width:28px;height:28px;flex:0 0 auto;border-radius:var(--tl-radius-md,6px);
  display:inline-flex;align-items:center;justify-content:center;
  background:var(--theme-action-selected);color:var(--theme-text-secondary);
  font-weight:500;font-size:13px;line-height:20px;
}
.na-cfgitem .t{flex:1 1 auto;min-width:0}
.na-cfgitem .req{
  flex:0 0 auto;font-weight:400;font-size:12px;line-height:18px;color:var(--theme-text-disabled);
}
.na-cfgfoot{display:flex;align-items:center;gap:12px;flex:0 0 auto;padding-top:4px}
.na-cfgfoot .sp{flex:1 1 auto}

/* ================= MODAL DANH SÁCH PHÒNG ================= */
.na-bd{position:fixed;inset:0;background:var(--theme-backdrop-overlay,rgba(47,43,61,.5));z-index:1214;display:none}
.na-bd.is-open{display:block}

.na-dlg{
  position:fixed;top:50%;left:50%;z-index:1216;
  width:min(1353px,94vw);height:min(924px,92vh);box-sizing:border-box;
  display:none;flex-direction:column;overflow:hidden;
  background:var(--misc-paper);border-radius:var(--tl-radius-lg,8px);
  box-shadow:0 8px 32px rgba(47,43,61,.24);
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
  transform:translate(-50%,-50%) scale(.98);opacity:0;
  transition:transform .16s var(--tl-ease,cubic-bezier(.4,0,.2,1)),opacity .16s var(--tl-ease,cubic-bezier(.4,0,.2,1));
}
.na-dlg.is-open{display:flex;transform:translate(-50%,-50%) scale(1);opacity:1}
.na-dhead{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;height:60px;padding:0 20px;
  border-bottom:1px solid var(--tl-divider);
}
.na-dhead h3{margin:0;flex:1 1 auto;font-weight:500;font-size:18px;line-height:28px}
.na-x{
  width:24px;height:24px;flex:0 0 auto;padding:0;border:0;background:none;cursor:pointer;
  display:inline-flex;align-items:center;justify-content:center;color:var(--theme-text-secondary);
}
.na-x svg{width:20px;height:20px;display:block}
.na-x:hover{color:var(--theme-text-primary)}
.na-dtools{display:flex;align-items:center;justify-content:flex-end;gap:12px;flex:0 0 auto;padding:12px 20px}
.na-dbody{flex:1 1 auto;min-height:0;overflow:auto;padding:0 20px}

.na-tbl{width:100%;border-collapse:separate;border-spacing:0;font-size:14px;line-height:22px}
.na-tbl th{
  position:sticky;top:0;z-index:1;text-align:left;white-space:nowrap;
  padding:10px 12px;background:var(--theme-action-hover,rgba(47,43,61,.04));
  border-bottom:1px solid var(--tl-divider);
  font-weight:500;font-size:12px;line-height:18px;letter-spacing:.4px;text-transform:uppercase;
  color:var(--theme-text-secondary);
}
.na-tbl td{
  padding:10px 12px;border-bottom:1px solid var(--tl-divider);white-space:nowrap;
  color:var(--theme-text-primary);
}
.na-tbl td.num,.na-tbl th.num{text-align:right}
.na-tbl tr.na-grp td{
  background:var(--theme-action-hover,rgba(47,43,61,.04));
  font-weight:500;font-size:13px;line-height:20px;text-transform:uppercase;letter-spacing:.4px;
}
.na-tbl td.na-mk{position:relative;padding-left:16px}
.na-tbl td.na-mk::before{content:"";position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--gc,transparent)}
.na-ck{width:36px}
.na-dfoot{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;height:64px;padding:0 20px;
  border-top:1px solid var(--tl-divider);background:var(--misc-paper);
}
.na-dfoot .sp{flex:1 1 auto}
.na-dnote{display:inline-flex;align-items:center;gap:8px;color:var(--theme-text-secondary);font-size:13px;line-height:20px}
.na-dnote svg{width:16px;height:16px;flex:0 0 auto;display:block}
.na-empty{
  display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;
  padding:80px 20px;color:var(--theme-text-secondary);text-align:center;
}

/* ================= DIALOG NHỎ ================= */
.na-sdbd{position:fixed;inset:0;background:var(--theme-backdrop-overlay,rgba(47,43,61,.5));z-index:1217;display:none}
.na-sdbd.is-open{display:block}
.na-sdlg{
  position:fixed;top:50%;left:50%;z-index:1218;width:min(480px,94vw);box-sizing:border-box;
  display:none;flex-direction:column;overflow:hidden;
  background:var(--misc-paper);border-radius:var(--tl-radius-lg,8px);
  box-shadow:0 8px 32px rgba(47,43,61,.24);
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
  transform:translate(-50%,-50%) scale(.98);opacity:0;
  transition:transform .16s var(--tl-ease,cubic-bezier(.4,0,.2,1)),opacity .16s var(--tl-ease,cubic-bezier(.4,0,.2,1));
}
.na-sdlg.is-open{display:flex;transform:translate(-50%,-50%) scale(1);opacity:1}
.na-sdlg > header{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;height:60px;padding:0 20px;
  border-bottom:1px solid var(--tl-divider);
}
.na-sdlg > header h3{margin:0;flex:1 1 auto;font-weight:500;font-size:18px;line-height:28px}
.na-sdbody{padding:20px;display:flex;flex-direction:column;gap:16px}
.na-sdbody b{font-weight:500}
.na-sdfoot{display:flex;align-items:center;justify-content:flex-end;gap:12px;padding:0 20px 20px}
.na-sdf{display:flex;flex-direction:column;gap:4px}
.na-sdf .k{font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}

/* ---------- Toggle switch (Tự động kiểm toán đêm) ---------- */
.na-sw{position:relative;display:inline-flex;align-items:center;gap:12px;cursor:pointer}
.na-sw input{position:absolute;opacity:0;width:0;height:0}
.na-sw .tr{
  width:40px;height:22px;flex:0 0 auto;box-sizing:border-box;border-radius:9999px;
  background:var(--theme-action-disabled-background,rgba(47,43,61,.26));
  transition:background-color var(--ez-dur,.15s) var(--ez-ease,ease);
}
.na-sw .tr::after{
  content:"";display:block;width:18px;height:18px;margin:2px;border-radius:9999px;background:#fff;
  box-shadow:0 1px 3px rgba(47,43,61,.24);
  transition:transform var(--ez-dur,.15s) var(--ez-ease,ease);
}
.na-sw input:checked + .tr{background:var(--ez-primary,#186FF4)}
.na-sw input:checked + .tr::after{transform:translateX(18px)}
.na-sw input:focus-visible + .tr{outline:2px solid var(--ez-primary,#186FF4);outline-offset:2px}

/* Checkbox trong cấu hình: dùng lại .sv-ck, bổ sung trạng thái vô hiệu */
.na-cfgitem .sv-ck input:disabled + .box{opacity:.38;cursor:not-allowed}
.na-cfgitem .sv-ck input:disabled ~ .t{color:var(--theme-text-disabled)}

/* Nút biểu tượng vuông (cấu hình) — iconbutton.md medium */
.na-ib{width:38px;min-width:38px;padding:0;justify-content:center}
.na-ib svg{width:20px;height:20px;flex:0 0 auto}

/* ================================================================
   18. BUTTON — CHUAN HOA THEO button.md v1.0.0
   Figma: EzOne.DesignLibrary node 19195:47371
   Lop nay nap SAU cung nen la nguon su that duy nhat cho moi nut:
   radius / spacing / border / mau / transition / focus / disabled.
   ================================================================ */
:root{
  /* radius.md — MOI nut deu 8px */
  --ez-r: 8px;

  /* Sizing Spec — Large / Medium / Small */
  --ez-h-lg: 48px;  --ez-px-lg: 20px; --ez-fs-lg: 16px; --ez-lh-lg: 24px; --ez-ic-lg: 20px;
  --ez-h-md: 38px;  --ez-px-md: 16px; --ez-fs-md: 14px; --ez-lh-md: 22px; --ez-ic-md: 18px;
  --ez-h-sm: 30px;  --ez-px-sm: 12px; --ez-fs-sm: 12px; --ez-lh-sm: 18px; --ez-ic-sm: 16px;
  --ez-gap: 8px;

  /* Icon Button — iconbutton.md v1.1.0 (radius nho hon Button thuong) */
  --ez-ib-r-lg: 8px; --ez-ib-r-md: 6px; --ez-ib-r-sm: 4px;
  --ez-ib-ic-lg: 24px; --ez-ib-ic-md: 20px; --ez-ib-ic-sm: 16px;

  /* Text Field — textfield.md v1.0.0 */
  --ez-tf-h-lg: 48px; --ez-tf-h-md: 38px; --ez-tf-h-sm: 30px;
  --ez-tf-r-lg: 8px;  --ez-tf-r-md: 6px;  --ez-tf-r-sm: 4px;
  --ez-tf-px-lg: 16px; --ez-tf-px-md: 14px; --ez-tf-px-sm: 12px;
  --ez-tf-fs-lg: 17px; --ez-tf-fs-md: 14px; --ez-tf-fs-sm: 13px;
  --ez-tf-ic-lg: 18px; --ez-tf-ic-md: 16px; --ez-tf-ic-sm: 14px;
  --ez-tf-border: rgba(47,43,61,.22);
  --ez-tf-hover:  rgba(47,43,61,.6);
  --ez-tf-shadow: 0 2px 6px rgba(0,131,202,.3);

  /* Border — Outline Button */
  --ez-bw: 1.5px;          /* Large + Medium */
  --ez-bw-sm: 1px;         /* Small */

  /* motion.duration.fast + motion.easing.standard */
  --ez-dur: 100ms;
  --ez-ease: cubic-bezier(.4,0,.2,1);

  /* Colors — semantic intent */
  --ez-primary:        #186FF4;
  --ez-primary-hover:  #1560D8;
  --ez-primary-active: #1155B8;
  --ez-primary-soft:   rgba(24,111,244,.08);
  --ez-error:          #FF4C51;
  --ez-error-hover:    #E8434A;
  --ez-success:        #28C76F;
  --ez-warning:        #FF9F43;

  --ez-on-fill:        #ffffff;
  --ez-text:           var(--theme-text-primary);
  --ez-text-2:         var(--theme-text-secondary);
  --ez-border:         rgba(47,43,61,.24);
  --ez-label-bg:       var(--misc-avatar-bg);
  --ez-label-hover:    var(--theme-action-selected);
  --ez-hover:          var(--tl-hover-bg);

  --ez-disabled-bg:    rgba(47,43,61,.12);
  --ez-disabled-fg:    rgba(47,43,61,.38);
}

/* ---------------- Base chung cho MOI nut ---------------- */
.tl-btn-primary,.tl-btn-outline,.tl-btn-ghost,.tl-navbtn,
.tl-pop-btn,.tl-pop-btn2,.tl-pop-ib,.tl-iconbtn,
.qb-actions button,.qb-ibtn,
.lb-btn,.cfg-btn,.cfg-clear{
  display:inline-flex;align-items:center;justify-content:center;gap:var(--ez-gap);
  border-radius:var(--ez-r);
  font-family:inherit;font-weight:500;
  cursor:pointer;white-space:nowrap;
  transition:background-color var(--ez-dur) var(--ez-ease),
             border-color var(--ez-dur) var(--ez-ease),
             color var(--ez-dur) var(--ez-ease),
             transform var(--ez-dur) var(--ez-ease);
}
/* Focus visible — bat buoc, khong duoc outline:none */
.tl-btn-primary:focus-visible,.tl-btn-outline:focus-visible,.tl-btn-ghost:focus-visible,
.tl-navbtn:focus-visible,.tl-pop-btn:focus-visible,.tl-pop-btn2:focus-visible,
.tl-pop-ib:focus-visible,.tl-iconbtn:focus-visible,.qb-actions button:focus-visible,
.qb-ibtn:focus-visible,.lb-btn:focus-visible,.cfg-btn:focus-visible,.cfg-clear:focus-visible{
  outline:2px solid var(--ez-primary);outline-offset:2px;
}
/* Active — lun xuong 1px */
.tl-btn-primary:active:not(:disabled),.tl-btn-outline:active:not(:disabled),
.tl-btn-ghost:active:not(:disabled),.tl-pop-btn:active:not(:disabled),
.tl-pop-btn2:active:not(:disabled),.qb-actions button:active:not(:disabled),
.lb-btn:active:not(:disabled),.cfg-btn:active:not(:disabled),.cfg-clear:active:not(:disabled){
  transform:translateY(1px);
}
/* Disabled */
.tl-btn-primary:disabled,.tl-btn-outline:disabled,.tl-btn-ghost:disabled,
.tl-navbtn:disabled,.tl-pop-btn:disabled,.tl-pop-btn2:disabled,.tl-pop-ib:disabled,
.tl-iconbtn:disabled,.qb-actions button:disabled,.qb-ibtn:disabled,
.lb-btn:disabled,.cfg-btn:disabled,.cfg-clear:disabled{
  background:var(--ez-disabled-bg);border-color:transparent;color:var(--ez-disabled-fg);
  cursor:not-allowed;transform:none;box-shadow:none;
}

/* ---------------- SIZE: Large 48px — dialog / modal / form chinh ---------------- */
.qb-actions button,
.tl-dialog footer .tl-btn-primary,
.tl-dialog footer .tl-btn-ghost{
  height:var(--ez-h-lg);padding:0 var(--ez-px-lg);
  font-size:var(--ez-fs-lg);line-height:var(--ez-lh-lg);
}
.qb-actions button svg,
.tl-dialog footer .tl-btn-primary svg,
.tl-dialog footer .tl-btn-ghost svg{width:var(--ez-ic-lg);height:var(--ez-ic-lg);flex:0 0 auto}

/* ---------------- SIZE: Medium 38px — toolbar / filter bar / inline ---------------- */
.tl-btn-primary,.tl-btn-outline,.tl-btn-ghost,.lb-btn,.cfg-btn,.cfg-clear{
  height:var(--ez-h-md);padding:0 var(--ez-px-md);
  font-size:var(--ez-fs-md);line-height:var(--ez-lh-md);
}
.tl-btn-primary svg,.tl-btn-outline svg,.tl-btn-ghost svg,
.lb-btn svg,.cfg-btn svg,.cfg-clear svg{width:var(--ez-ic-md);height:var(--ez-ic-md);flex:0 0 auto}

/* ---------------- SIZE: Small 30px — compact / popup action / button group ---------------- */
.tl-navbtn,.tl-pop-btn,.tl-pop-btn2{
  height:var(--ez-h-sm);padding:0 var(--ez-px-sm);
  font-size:var(--ez-fs-sm);line-height:var(--ez-lh-sm);
}
.tl-navbtn svg,.tl-pop-btn svg,.tl-pop-btn2 svg{width:var(--ez-ic-sm);height:var(--ez-ic-sm);flex:0 0 auto}
/* Nut chi co icon — o vuong, canh = chieu cao size */
.tl-pop-ib,.tl-iconbtn{
  height:var(--ez-h-sm);width:var(--ez-h-sm);padding:0;
}
.tl-pop-ib svg,.tl-iconbtn svg{width:var(--ez-ic-md);height:var(--ez-ic-md);flex:0 0 auto}
.tl-pop-ib,.tl-iconbtn{font-size:var(--ez-fs-sm);line-height:var(--ez-lh-sm)}
.qb-ibtn{height:var(--ez-h-md);width:var(--ez-h-md);padding:0;border-radius:var(--ez-r)}
.qb-ibtn svg{width:var(--ez-ic-md);height:var(--ez-ic-md);flex:0 0 auto}
.qb-ibtn{font-size:var(--ez-fs-md);line-height:var(--ez-lh-md)}

/* ---------------- TYPE: Default Button (Solid / Filled) ---------------- */
.tl-btn-primary,.qb-ok,.lb-btn.primary,.cfg-btn.primary,.tl-pop-btn.primary{
  border:0;background:var(--ez-primary);color:var(--ez-on-fill);box-shadow:none;
}
.tl-btn-primary:hover:not(:disabled),.qb-ok:hover:not(:disabled),
.lb-btn.primary:hover:not(:disabled),.cfg-btn.primary:hover:not(:disabled),
.tl-pop-btn.primary:hover:not(:disabled){background:var(--ez-primary-hover)}
.tl-btn-primary:active:not(:disabled),.qb-ok:active:not(:disabled),
.lb-btn.primary:active:not(:disabled),.cfg-btn.primary:active:not(:disabled){background:var(--ez-primary-active)}

/* Default / Error — hanh dong khong the hoan tac */
.tl-pop-btn.checkin,.qb.is-checkin .qb-ok{border:0;background:var(--ez-error);color:var(--ez-on-fill)}
.tl-pop-btn.checkin:hover:not(:disabled),.qb.is-checkin .qb-ok:hover:not(:disabled){background:var(--ez-error-hover)}
/* Default / Info — tra phong */
.tl-pop-btn.checkout{border:0;background:#D761F2;color:var(--ez-on-fill)}
.tl-pop-btn.checkout:hover:not(:disabled){background:#C64BE2}
.tl-pop-btn:hover:not(:disabled){filter:none}

/* ---------------- TYPE: Outline Button (Bordered) ---------------- */
.tl-btn-outline,.lb-btn,.cfg-clear,.qb-ibtn{
  border:var(--ez-bw) solid var(--ez-border);background:var(--misc-paper);color:var(--ez-text);
}
.tl-btn-outline{color:var(--ez-text-2)}
.tl-btn-outline:hover:not(:disabled),.lb-btn:hover:not(:disabled),
.cfg-clear:hover:not(:disabled),.qb-ibtn:hover:not(:disabled){
  background:var(--ez-primary-soft);border-color:var(--ez-primary);color:var(--ez-primary);
}
.tl-btn-outline[aria-pressed="true"]{border-color:var(--ez-primary);color:var(--ez-primary);background:var(--ez-primary-soft)}
/* Outline size Small → border 1px */
.tl-pop-btn2,.tl-pop-ib,.tl-iconbtn,.tl-navbtn{
  border:var(--ez-bw-sm) solid var(--ez-border);background:var(--misc-paper);color:var(--ez-text-2);
}
.tl-pop-btn2:hover:not(:disabled),.tl-pop-ib:hover:not(:disabled),
.tl-iconbtn:hover:not(:disabled),.tl-navbtn:hover:not(:disabled){
  background:var(--ez-primary-soft);border-color:var(--ez-primary);color:var(--ez-primary);
}
.tl-pop-ib[aria-expanded="true"]{background:var(--ez-primary-soft);border-color:var(--ez-primary);color:var(--ez-primary)}
.tl-pop-ib.danger{border-color:var(--ez-error);color:var(--ez-error)}
.tl-pop-ib.danger:hover:not(:disabled){background:rgba(255,76,81,.08);border-color:var(--ez-error);color:var(--ez-error)}

/* ---------------- TYPE: Label Button — Huy / Dat lai / Dong ---------------- */
.tl-btn-ghost,.qb-cancel,.cfg-btn:not(.primary){
  border:0;background:var(--ez-label-bg);color:var(--ez-text-2);
}
.tl-btn-ghost:hover:not(:disabled),.qb-cancel:hover:not(:disabled),
.cfg-btn:not(.primary):hover:not(:disabled){background:var(--ez-label-hover);color:var(--ez-text)}

/* ---------------- Button group — bo goc o 2 dau, giua vuong ---------------- */
.tl-btngroup{--tl-radius-group:var(--ez-r)}
.tl-btngroup>*{border-radius:0}
.tl-btngroup>*:first-child{border-top-left-radius:var(--ez-r);border-bottom-left-radius:var(--ez-r)}
.tl-btngroup>*:last-child{border-top-right-radius:var(--ez-r);border-bottom-right-radius:var(--ez-r)}
.tl-btngroup>*:only-child{border-radius:var(--ez-r)}
.tl-navbtn[aria-pressed="true"],.tl-navbtn.is-on{
  background:var(--ez-primary-soft);border-color:var(--ez-primary);color:var(--ez-primary);z-index:2;
}
.tl-datefield{
  height:var(--ez-h-sm);font-size:var(--ez-fs-sm);line-height:var(--ez-lh-sm);
  border-width:var(--ez-bw-sm);border-radius:0;
  transition:background-color var(--ez-dur) var(--ez-ease),border-color var(--ez-dur) var(--ez-ease),color var(--ez-dur) var(--ez-ease);
}
.tl-datefield:focus-visible{outline:2px solid var(--ez-primary);outline-offset:2px}

/* ---------------- Bo bong dong o nut (khong dung resting shadow) ---------------- */
.lb-btn.primary,.lb-btn.primary:hover,.tl-btn-primary,.cfg-btn.primary,.qb-ok{box-shadow:none}

@media (prefers-reduced-motion: reduce){
  .tl-btn-primary,.tl-btn-outline,.tl-btn-ghost,.tl-navbtn,.tl-pop-btn,.tl-pop-btn2,
  .tl-pop-ib,.tl-iconbtn,.qb-actions button,.qb-ibtn,.lb-btn,.cfg-btn,.cfg-clear{transition:none}
  .tl-btn-primary:active,.lb-btn:active,.cfg-btn:active,.qb-actions button:active{transform:none}
}


/* ================================================================
   19. THANH CONG CU SO DO TIMELINE — nang len size Medium (38px)
   buttongroup.md 1.0.0 · iconbutton.md 1.1.0 · textfield.md 1.0.0
   ================================================================ */

/* --- Button Group Medium: chon ngay + chon che do xem --- */
.tl-btngroup .tl-navbtn,
.tl-btngroup .tl-datefield,
.tl-seg button,
.tl-viewswitch button{
  height:var(--ez-h-md);
  padding:0 var(--ez-px-md);
  font-family:inherit;font-weight:500;
  font-size:var(--ez-fs-md);line-height:var(--ez-lh-md);
  border-width:var(--ez-bw-sm);border-style:solid;border-color:var(--ez-border);
  background:var(--misc-paper);color:var(--ez-text-2);
  transition:background-color var(--ez-dur) var(--ez-ease),
             border-color var(--ez-dur) var(--ez-ease),
             color var(--ez-dur) var(--ez-ease);
}
.tl-btngroup .tl-navbtn{padding:0 12px}
.tl-btngroup .tl-navbtn svg{width:var(--ez-ic-md);height:var(--ez-ic-md)}
.tl-btngroup .tl-datefield{min-width:148px}
.tl-btngroup .tl-datefield svg{width:var(--ez-ic-md);height:var(--ez-ic-md)}

/* Bo goc nhom: dau trai 8px, cuoi phai 8px, giua vuong */
.tl-seg,.tl-viewswitch{--tl-radius-group:var(--ez-r)}
.tl-seg button,.tl-viewswitch button{border-radius:0}
.tl-seg button:first-child,.tl-viewswitch button:first-child{
  border-top-left-radius:var(--ez-r);border-bottom-left-radius:var(--ez-r);
}
.tl-seg button:last-child,.tl-viewswitch button:last-child{
  border-top-right-radius:var(--ez-r);border-bottom-right-radius:var(--ez-r);
}
.tl-seg button:only-child,.tl-viewswitch button:only-child{border-radius:var(--ez-r)}

/* Icon Button trong nhom chuyen kieu so do — o vuong 38px */
.tl-viewswitch button{width:var(--ez-h-md);padding:0}
.tl-viewswitch svg{width:var(--ez-ib-ic-md);height:var(--ez-ib-ic-md)}

.tl-seg button:hover:not(:disabled),
.tl-viewswitch button:hover:not(:disabled),
.tl-btngroup .tl-navbtn:hover:not(:disabled),
.tl-btngroup .tl-datefield:hover:not(:disabled){
  background:var(--ez-primary-soft);border-color:var(--ez-primary);color:var(--ez-primary);z-index:1;
}
.tl-seg button[aria-pressed="true"],
.tl-viewswitch button[aria-pressed="true"],
.tl-btngroup .tl-datefield[aria-expanded="true"]{
  background:var(--ez-primary-soft);color:var(--ez-primary);border-color:var(--ez-primary);z-index:2;
}
.tl-seg button:focus-visible,.tl-viewswitch button:focus-visible{
  outline:2px solid var(--ez-primary);outline-offset:2px;z-index:3;
}

/* --- Icon Button Medium: nut cai dat --- */
#tlConfig{
  height:var(--ez-h-md);width:var(--ez-h-md);padding:0;
  border-radius:var(--ez-ib-r-md);
  border:var(--ez-bw-sm) solid var(--ez-border);
  background:var(--misc-paper);color:var(--ez-text-2);
}
#tlConfig svg{width:var(--ez-ib-ic-md);height:var(--ez-ib-ic-md)}
#tlConfig:hover:not(:disabled){background:var(--ez-primary-soft);border-color:var(--ez-primary);color:var(--ez-primary)}

/* --- Text Field Medium: o tim kiem ten phong --- */
.tl-search{
  height:var(--ez-tf-h-md);
  padding:0 var(--ez-tf-px-md);
  gap:8px;min-width:240px;
  border:1px solid var(--ez-tf-border);border-radius:var(--ez-tf-r-md);
  background:var(--misc-paper);color:var(--ez-text-2);
  transition:border-color var(--ez-dur) var(--ez-ease),box-shadow var(--ez-dur) var(--ez-ease);
}
.tl-search:hover{border-color:var(--ez-tf-hover)}
.tl-search svg{width:var(--ez-tf-ic-md);height:var(--ez-tf-ic-md)}
.tl-search input{
  font-size:var(--ez-tf-fs-md);line-height:24px;
  color:var(--theme-text-primary);
}
.tl-search input::placeholder{color:rgba(47,43,61,.4)}
.tl-search:focus-within{
  border:2px solid var(--ez-primary);
  padding:0 calc(var(--ez-tf-px-md) - 1px);
  box-shadow:var(--ez-tf-shadow);
}


/* ================================================================
   21. BO SUNG
   · .lb-btn-sm — nut Outline size Small (30px) cho toolbar bang
   · Trang thai "dang duoc chon" cua dropdown/select: nen xanh 8%
   ================================================================ */
.lb-btn.lb-btn-sm{
  height:var(--ez-h-sm);padding:0 var(--ez-px-sm);
  font-size:var(--ez-fs-sm);line-height:var(--ez-lh-sm);
  border-width:var(--ez-bw-sm);
}
.lb-btn.lb-btn-sm svg{width:var(--ez-ic-sm);height:var(--ez-ic-sm)}

/* Tuy chon dang duoc chon trong moi dropdown/select — opacity 8% */
.qb-vsmenu button[aria-selected="true"],
.lb-lists button[aria-selected="true"],
.tl-menu button[aria-selected="true"],
.bd-menu button[aria-selected="true"]{
  background:var(--ez-primary-soft);color:var(--ez-primary);
}
/* tuy chon dang chon + dang duoc dieu huong ban phim: giu 8%, phan biet bang vien */
.qb-vsmenu button[aria-selected="true"].is-act,
.lb-lists button[aria-selected="true"].is-act{
  background:var(--ez-primary-soft);box-shadow:inset 0 0 0 1px rgba(24,111,244,.35);
}
.qb-vs-btn[aria-expanded="true"],
.qb-dtp-days button.today{background:var(--ez-primary-soft);color:var(--ez-primary)}

/* ================= DARK MODE =================
   Giá trị lấy đúng từ EzOne_DesignTokens.md — cột "Dark Value".
   Bật bằng thuộc tính [data-theme="dark"] trên <html>.
   Nguyên tắc: chỉ ghi đè TOKEN, không sửa từng component.
   ============================================= */

[data-theme="dark"]{
  /* ---------- Collection: Variables › Theme ---------- */
  --theme-action-active:#E1DEF599;
  --theme-action-disabled:#E1DEF54D;
  --theme-action-disabled-bg:#E1DEF50F;
  --theme-action-focus:#E1DEF51A;
  --theme-action-hover:#E1DEF50F;
  --theme-action-selected:#E1DEF514;
  --theme-backdrop-overlay:#17192599;
  --theme-chip-background:#E1DEF514;
  --theme-divider:#E1DEF51F;
  --theme-filled-input-bg:#E1DEF50F;
  --theme-input-border:#E1DEF538;
  --theme-outline-border:#E1DEF53D;
  --theme-table-header:#25293C;
  --theme-table-header-2:#31364F;
  --theme-text-disabled:#E1DEF566;
  --theme-text-primary:#E1DEF5E5;
  --theme-text-secondary:#E1DEF5B2;
  --theme-text-subtitle:#E1DEF58C;

  /* ---------- Collection: Variables › Misc ---------- */
  --misc-avatar-bg:#373B50;
  --misc-bg-black-text:#2F2B3D;
  --misc-body-bg:#25293C;
  --misc-chat-bg:#202534;
  --misc-grey-light:#353A52;
  --misc-paper:#2F3349;
  --misc-snackbar:#F7F4FF;
  --misc-track-bg:#3A3F57;

  /* ---------- Primitives › Color/Gray (adaptive) ---------- */
  --color-gray-100:#535876; --color-gray-200:#636784; --color-gray-300:#727692;
  --color-gray-400:#8285A0; --color-gray-500:#9293AE; --color-gray-600:#A2A2BD;
  --color-gray-700:#B2B1CB; --color-gray-800:#C1C0D9; --color-gray-900:#CFCCE4;

  /* ---------- Primitives › Opacity Color/Gray (adaptive) ---------- */
  --opacity-color-gray-8:#E7E3FC14;  --opacity-color-gray-16:#E7E3FC29;
  --opacity-color-gray-24:#E7E3FC3D; --opacity-color-gray-32:#E7E3FC52;
  --opacity-color-gray-38:#E7E3FC61;

  /* ---------- Token nội bộ của prototype (suy ra từ bảng Dark) ---------- */
  --tl-page-bg:#25293C;
  --tl-divider:#E1DEF51F;
  --tl-outline:rgba(225,222,245,.24);
  --tl-hover-bg:rgba(225,222,245,.06);
  --tl-cell-hover:#353A52;
  --tl-sum-bg:#353A52;
  --tl-grp-bg:#31364F;
  --tl-weekend-bg:#3A2A32;
  --tl-weekend-hover:#4A323B;
  --tl-c-strong:#E1DEF5E5;
  --tl-c-muted:#E1DEF5B2;
  --tl-c-occ:rgba(225,222,245,.78);

  --ez-tf-border:#E1DEF538;
  --ez-tf-hover:rgba(225,222,245,.5);

  --shadow-sm:0 2px 8px 0 rgba(0,0,0,.4);
  --shadow-md:0 3px 12px 0 rgba(0,0,0,.45);
  --shadow-rail:0 2px 4px 0 rgba(0,0,0,.4);

  /* Vạch dưới của logo vốn là màu nền trang */
  --logo-bar-3:#2F3349;

  color-scheme:dark;
}

/* ---------- Bề mặt bị hard-code #fff trong các module cũ ---------- */
[data-theme="dark"] .ca-a4,
[data-theme="dark"] .rl-pv,
[data-theme="dark"] .ca-a4-tbl thead th,
[data-theme="dark"] .rv-tbl thead th{
  background:var(--misc-paper);
  color:var(--theme-text-primary);
}
[data-theme="dark"] .ca-a4{border-color:var(--theme-divider)}
[data-theme="dark"] .ca-a4-tbl th,
[data-theme="dark"] .ca-a4-tbl td,
[data-theme="dark"] .rv-tbl th,
[data-theme="dark"] .rv-tbl td{border-color:rgba(225,222,245,.35);color:var(--theme-text-primary)}
[data-theme="dark"] .ca-a4-foot{border-top-color:rgba(225,222,245,.35)}
[data-theme="dark"] .rv-tbl tr.rv-grp>td,
[data-theme="dark"] .rv-tbl tr.rv-meal>td{background:var(--theme-table-header-2)}

/* Các nền / viền sáng còn hard-code trong module cũ — quy về token EzOne */
[data-theme="dark"]{
  --misc-table-header-bg:#31364F;   /* .rl-grp */

  /* Đặt phòng nhanh (qb) — viền & placeholder vốn là màu mực tối nên tàng hình trên nền tối */
  --qb-body-bg:#25293C;
  --qb-border:#E1DEF538;
  --qb-border-soft:#E1DEF51F;
  --qb-ph:#E1DEF566;
  --qb-hover:#E1DEF50F;
  --qb-selected:#E1DEF514;

  /* Thẻ xem nhanh / thẻ chi tiết (dv) */
  --dv-body-bg:#25293C;
  --dv-repair-bg:#3A3F57;
  --dv-repair-tile:#454A63;
}
[data-theme="dark"] .qb-qrdlg .qr-box{background:var(--misc-paper)}
[data-theme="dark"] .qb-qrdlg .qr-box svg [fill="#091135"],
[data-theme="dark"] .qb-qrdlg .qr-box svg [stroke="#091135"]{fill:var(--theme-text-primary);stroke:var(--theme-text-primary)}

/* Thẻ xem nhanh trong Cấu hình — thân thẻ vốn hard-code #fff nên chữ sáng chìm hẳn */
[data-theme="dark"] .cfg-qv,
[data-theme="dark"] .cfg-dv,
[data-theme="dark"] .cfg-sv{background:var(--misc-paper);border-color:var(--theme-divider)}
[data-theme="dark"] .cfg-qv .money{background:var(--theme-table-header-2)}
[data-theme="dark"] .cfg-dv .tile[data-st="repair"]{background:#454A63;color:var(--theme-text-primary)}
[data-theme="dark"] .cfg-dv .ics .ok,
[data-theme="dark"] .cfg-sv .ics .ok{background:rgba(225,222,245,.14)}

/* Chú giải trạng thái trên thanh lọc sơ đồ: "Sửa" dùng màu mực tối nên tàng hình */
[data-theme="dark"]{
  --tl-st-repair:rgba(225,222,245,.7);
  --tl-st-dirty:#E8A33D;
  --tl-st-light:#E3C93B;
}
/* Màu chú giải do JS gán inline (STATUS[k].color) → phải dùng !important mới đè được */
[data-theme="dark"] .tl-legend [data-legend="repair"]{color:rgba(225,222,245,.75) !important}
[data-theme="dark"] .tl-legend [data-legend="repair"] .dot{background:rgba(225,222,245,.75) !important}
[data-theme="dark"] .tl-legend [data-legend="dirty"]{color:#E8A33D !important}
[data-theme="dark"] .tl-legend [data-legend="dirty"] .dot{background:#E8A33D !important}

/* Bộ lọc / popover trên sơ đồ timeline */
[data-theme="dark"] .tl-pop,
[data-theme="dark"] .tl-menu,
[data-theme="dark"] .tl-dialog,
[data-theme="dark"] .qb-vs-menu,
[data-theme="dark"] #qbVsMenu,
[data-theme="dark"] #qbDtp{background:var(--misc-paper);color:var(--theme-text-primary)}
[data-theme="dark"] .tl-pop-body{background:var(--misc-body-bg)}
[data-theme="dark"] .rl-grp{background:var(--theme-table-header-2)}
[data-theme="dark"] .rl-pvwrap{background:var(--misc-body-bg)}
[data-theme="dark"] .ca,
[data-theme="dark"] .ca-viewer{background:var(--misc-paper)}
[data-theme="dark"] .ca-body{background:var(--misc-body-bg)}

/* ---------- Cấu hình sơ đồ (cfg) — đưa màu hard-code về token EzOne ---------- */
[data-theme="dark"] .cfg{
  --cfg-head-bg:var(--theme-table-header);      /* #25293C */
  --cfg-div:var(--theme-divider);
}
[data-theme="dark"] .cfg-main{background:var(--misc-body-bg)}
[data-theme="dark"] .cfg-prev{background:var(--misc-paper);border-left-color:var(--theme-divider)}
[data-theme="dark"] .cfg-grp,
[data-theme="dark"] .cfg-pv-card,
[data-theme="dark"] .cfg-tl,
[data-theme="dark"] .cfg-sel,
[data-theme="dark"] .cfg-btn-out{background:var(--misc-paper)}
[data-theme="dark"] .cfg-pv-card .tile{background:var(--color-palette-primary-opacity-lighter)}
[data-theme="dark"] .cfg-prev-tag{background:var(--color-palette-primary-opacity-light)}
[data-theme="dark"] .cfg-tl .sum td,[data-theme="dark"] .cfg-tl .sum th{background:var(--theme-table-header-2)}
[data-theme="dark"] .cfg-tl .grp td,[data-theme="dark"] .cfg-tl .grp th{background:var(--misc-grey-light)}
[data-theme="dark"] .cfg-tl .hd th.wknd,[data-theme="dark"] .cfg-tl td.wknd{background:var(--color-palette-error-opacity-lighter)}
/* Nhãn nhóm / tiêu đề cột trong bảng cấu hình bị chìm trên nền tối */
[data-theme="dark"] .cfg-grp-h .nm{color:var(--theme-text-primary)}
[data-theme="dark"] .cfg-grp-h .cnt{color:var(--theme-text-secondary)}

/* ---------- Danh sách đặt phòng (lb) ---------- */
[data-theme="dark"] .lb{
  --lb-head-bg:var(--theme-table-header);
  --lb-border:var(--theme-divider);
}
[data-theme="dark"] .lb-table thead th{
  background:var(--theme-table-header);
  color:var(--theme-text-secondary);
}
[data-theme="dark"] .lb-table tbody tr.lb-grp{background:var(--theme-table-header-2)}

/* Thanh cuộn tuỳ biến */
[data-theme="dark"] ::-webkit-scrollbar-thumb{background:rgba(225,222,245,.24)}

/* Ảnh đại diện placeholder (SVG nền sáng) — làm dịu lại cho nền tối */
[data-theme="dark"] .avatar img,
[data-theme="dark"] .pf-ava img{filter:brightness(.82)}

/* Trang in luôn nền trắng dù đang ở chế độ tối */
@media print{
  [data-theme="dark"] .ca-a4,
  [data-theme="dark"] .ca-a4 *{background:#fff !important;color:#000 !important}
  [data-theme="dark"] .ca-a4 th,
  [data-theme="dark"] .ca-a4 td{border-color:rgba(47,43,61,.5) !important}
}

/* Kiểm toán đêm — bảng chọn giờ gốc của trình duyệt phải theo nền tối */
[data-theme="dark"] .na-f input[type="time"]{color-scheme:dark}


/* ============================================================================
   SEARCH TỔNG (Header common — SRS 3.1) + MENU IN NHANH HOÁ ĐƠN (check-out)
   Figma: zjVU9MyBEO0VxQkdZFtWeU / 20470:2849 (PMS · Search)
   ========================================================================= */

/* ---- Trigger trên header ---- */
.search{cursor:pointer}
.search-kbd{
  margin-left:auto;display:inline-flex;align-items:center;gap:2px;
  padding:1px 6px;border:1px solid var(--theme-divider);border-radius:4px;
  font-size:12px;line-height:18px;color:var(--theme-text-disabled);
  font-family:inherit;white-space:nowrap;flex:0 0 auto;
}
@media (max-width:900px){.search-kbd{display:none}}

/* ---- Overlay ---- */
.gs-bd{position:fixed;inset:0;background:rgba(47,43,61,.5);z-index:1300;display:none}
.gs-bd.is-open{display:block}
.gs{
  position:fixed;left:50%;top:50px;transform:translateX(-50%);
  width:800px;max-width:calc(100vw - 32px);
  max-height:calc(100vh - 100px);
  display:none;flex-direction:column;overflow:hidden;
  background:var(--misc-paper);border-radius:var(--border-radius-md);
  box-shadow:0 12px 40px rgba(47,43,61,.28);z-index:1301;
}
.gs.is-open{display:flex}

/* Header 56px — padding 16, gap 12, divider dưới */
.gs-head{
  display:flex;align-items:center;gap:var(--gap-3);
  padding:var(--padding-4);flex:0 0 auto;
  border-bottom:1px solid var(--theme-divider);background:var(--misc-paper);
}
.gs-head .ic{width:24px;height:24px;flex:0 0 auto;color:var(--theme-text-primary)}
.gs-head .ic svg{width:100%;height:100%;display:block}
.gs-q{
  flex:1 1 auto;min-width:0;border:0;outline:0;background:none;padding:0;
  font-family:inherit;font-weight:400;font-size:15px;line-height:24px;
  color:var(--theme-text-primary);
}
.gs-q::placeholder{color:var(--theme-text-disabled)}
.gs-esc{
  font-size:15px;line-height:24px;color:#2F2B3D8C;flex:0 0 auto;user-select:none;
}
.gs-x{
  width:24px;height:24px;flex:0 0 auto;border:0;background:none;padding:0;cursor:pointer;
  color:var(--theme-text-primary);display:flex;align-items:center;justify-content:center;
}
.gs-x svg{width:24px;height:24px;display:block}

/* Tabs — segmented 38px, radius 8 */
.gs-tabs{padding:var(--padding-4);flex:0 0 auto;background:var(--misc-paper)}
.gs-seg{
  display:flex;align-items:stretch;width:100%;height:38px;box-sizing:border-box;
  border:1px solid var(--theme-divider);border-radius:8px;overflow:hidden;
}
.gs-seg button{
  flex:1 1 0;min-width:0;border:0;background:none;cursor:pointer;
  display:flex;align-items:center;justify-content:center;
  padding:0 20px;font-family:inherit;font-weight:500;font-size:15px;line-height:22px;
  color:var(--theme-text-secondary);transition:background-color .2s ease,color .2s ease;
}
.gs-seg button + button{border-left:1px solid var(--theme-divider)}
.gs-seg button:hover{background:var(--tl-hover-bg)}
.gs-seg button[aria-selected="true"]{
  background:rgba(24,111,244,.08);color:var(--color-palette-primary-main);
}

/* Body */
.gs-body{
  flex:1 1 auto;min-height:0;overflow-y:auto;overflow-x:hidden;
  padding:0 var(--padding-2) var(--padding-2);
  display:flex;flex-direction:column;gap:var(--padding-4);
}
.gs-body::-webkit-scrollbar{width:8px}
.gs-body::-webkit-scrollbar-track{background:rgba(128,131,144,.16);border-radius:var(--border-round)}
.gs-body::-webkit-scrollbar-thumb{background:#808390;border-radius:var(--border-round)}
.gs-body{scrollbar-width:thin;scrollbar-color:#808390 rgba(128,131,144,.16)}

.gs-sec{display:flex;flex-direction:column;gap:var(--gap-2);padding:0 var(--padding-2)}
.gs-sec-t{
  font-weight:400;font-size:15px;line-height:24px;color:#2F2B3D8C;
  text-transform:uppercase;
}
.gs-list{display:flex;flex-direction:column}

/* Item đặt phòng — padding 12, radius 4, gap 16 */
.gs-bk{
  display:flex;align-items:flex-start;gap:16px;width:100%;
  padding:var(--padding-3);border:0;background:none;cursor:pointer;text-align:left;
  border-radius:var(--border-radius-sm);font-family:inherit;
  transition:background-color .15s ease;
}
.gs-bk:hover,.gs-bk.is-cur{background:var(--theme-action-hover)}
.gs-bk .st{width:24px;height:24px;flex:0 0 auto}
.gs-bk .st svg{width:100%;height:100%;display:block}
.gs-bk .st[data-st="inhouse"]{color:#DA4343}
.gs-bk .st[data-st="booked"]{color:var(--color-palette-primary-main)}
.gs-bk .st[data-st="checkout"]{color:#D761F2}
.gs-bk .st[data-st="noshow"]{color:#FF9F43}
.gs-bk .st[data-st="cancel"]{color:var(--theme-text-secondary)}
.gs-bk .dt{flex:1 1 auto;min-width:0;display:flex;flex-direction:column;gap:4px}
.gs-bk .rm{
  font-weight:500;font-size:15px;line-height:24px;color:var(--theme-text-primary);
  overflow:hidden;text-overflow:ellipsis;white-space:nowrap;
}
.gs-bk .gu{
  font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-primary);
  display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;
}
.gs-bk .tm{font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.gs-bk .id{
  flex:0 0 auto;display:flex;flex-direction:column;align-items:flex-end;gap:4px;
  font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary);
}
.gs-bk .id .bk{color:var(--theme-text-primary)}
.gs-bk .id .ota{display:flex;align-items:center;gap:8px;justify-content:flex-end}
.gs-bk .id .ota .lg{
  width:24px;height:24px;flex:0 0 auto;border:1px solid var(--theme-divider);
  border-radius:var(--border-round);display:flex;align-items:center;justify-content:center;
  font-size:9px;font-weight:500;color:#fff;overflow:hidden;
}

/* Item chức năng — px 12 / py 8, gap 8 */
.gs-fn{
  display:flex;align-items:center;gap:var(--gap-2);width:100%;
  padding:8px var(--padding-3);border:0;background:none;cursor:pointer;text-align:left;
  border-radius:var(--border-radius-sm);font-family:inherit;
  font-weight:400;font-size:15px;line-height:22px;color:var(--theme-text-primary);
  transition:background-color .15s ease;
}
.gs-fn:hover,.gs-fn.is-cur{background:var(--theme-action-hover)}
.gs-fn svg{width:24px;height:24px;flex:0 0 auto;color:var(--theme-text-primary)}
.gs-fn .tx{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}

/* Từ khoá khớp — SRS 3.1.4 mục 4 */
.gs-hl{background:#FFB269;border-radius:2px}

/* Empty state — MSG_01 */
.gs-empty{
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  gap:8px;padding:56px 16px 24px;text-align:center;
}
.gs-empty svg{width:48px;height:48px;color:var(--theme-text-primary)}
.gs-empty h4{margin:0;font-weight:500;font-size:15px;line-height:24px;color:var(--theme-text-primary)}
.gs-empty p{margin:0;font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}

@media (max-width:840px){
  .gs{width:calc(100vw - 32px);top:24px;max-height:calc(100vh - 48px)}
}

/* ============ MENU IN NHANH (check-out) ============ */
.co-print{display:inline-flex;align-items:stretch}
.co-print .tl-btn-outline{border-top-right-radius:0;border-bottom-right-radius:0}
.co-print .co-print-tg{
  display:inline-flex;align-items:center;justify-content:center;
  width:34px;padding:0;margin-left:-1px;cursor:pointer;
  border:1px solid var(--theme-input-border);background:var(--misc-paper);
  border-top-left-radius:0;border-bottom-left-radius:0;
  border-top-right-radius:var(--border-radius-md);border-bottom-right-radius:var(--border-radius-md);
  color:var(--theme-text-secondary);
}
.co-print .co-print-tg:hover{background:var(--tl-hover-bg)}
.co-print .co-print-tg svg{width:18px;height:18px;display:block;transition:transform .2s ease}
.co-print .co-print-tg[aria-expanded="true"] svg{transform:rotate(180deg)}
.co-pm{min-width:256px;z-index:1217}
.co-pm .cap{
  padding:8px 16px 6px;font-weight:400;font-size:13px;line-height:20px;
  color:var(--theme-text-disabled);cursor:default;
}

/* ============================================================================
   R01.22 — QUY TRÌNH NHẬN PHÒNG (CHECK-IN)
   SRS: R01.22_Checkin_v2.0 · Figma: eaPQ9ueUW2YmRpjucaNEBo / 32496:70835
   Slide-over phải, chiếm 85% bề ngang màn hình (yêu cầu người dùng;
   Figma vẽ 1380/1440 = 95.8% — xem mục Sai lệch).
   ========================================================================= */
.ci-bd{position:fixed;inset:0;background:var(--theme-backdrop-overlay,rgba(47,43,61,.5));z-index:1214;display:none}
.ci-bd.is-open{display:block}

.ci{
  position:fixed;top:0;right:0;z-index:1216;
  width:85vw;max-width:100vw;height:100%;box-sizing:border-box;
  display:none;flex-direction:column;overflow:hidden;
  background:var(--misc-paper);
  box-shadow:-8px 0 32px rgba(47,43,61,.24);
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);
  transform:translateX(24px);opacity:0;
  transition:transform .16s var(--tl-ease,cubic-bezier(.4,0,.2,1)),opacity .16s var(--tl-ease,cubic-bezier(.4,0,.2,1));
}
.ci.is-open{display:flex;transform:translateX(0);opacity:1}

/* ---------- Header 70px (Figma 32496:71775) ---------- */
.ci-head{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;height:70px;padding:0 16px;
  border-bottom:1px solid var(--tl-divider);
}
.ci-head h2{margin:0;flex:1 1 auto;font-weight:500;font-size:20px;line-height:28px;color:var(--theme-text-primary)}
.ci-x{
  width:24px;height:24px;flex:0 0 auto;padding:0;border:0;background:none;cursor:pointer;
  display:inline-flex;align-items:center;justify-content:center;color:var(--theme-text-secondary);
}
.ci-x svg{width:20px;height:20px;display:block}
.ci-x:hover{color:var(--theme-text-primary)}

/* ---------- Stepper 66px (Figma 32496:71781) ---------- */
.ci-steps{
  display:flex;align-items:center;gap:8px;flex:0 0 auto;height:66px;padding:0 20px;
  border-bottom:1px solid var(--tl-divider);
}
.ci-step{display:flex;align-items:center;gap:12px;padding:2px 6px;border-radius:var(--tl-radius-md,6px)}
.ci-step .no{
  width:30px;height:30px;flex:0 0 auto;border-radius:var(--tl-radius-md,6px);
  display:inline-flex;align-items:center;justify-content:center;
  background:var(--theme-action-selected);color:var(--theme-text-secondary);
  font-weight:500;font-size:14px;line-height:22px;
}
.ci-step .tx{font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-disabled);white-space:nowrap}
.ci-step.is-cur .no{background:var(--ez-primary);color:#fff}
.ci-step.is-cur .tx{font-weight:500;color:var(--theme-text-primary)}
.ci-step.is-done .no{background:var(--color-palette-primary-opacity-light,rgba(24,111,244,.16));color:var(--ez-primary)}
.ci-step.is-done .tx{color:var(--theme-text-primary)}
.ci-sep{width:18px;height:18px;flex:0 0 auto;color:var(--theme-text-disabled)}
.ci-sep svg{width:100%;height:100%;display:block}

/* ---------- Tabs bước 2 ---------- */
.ci-tools{display:flex;align-items:center;gap:8px;flex:0 0 auto;padding:12px 20px;border-bottom:1px solid var(--tl-divider)}
.ci-tab{
  height:38px;padding:0 16px;border:0;border-radius:var(--tl-radius-md,6px);background:none;cursor:pointer;
  font-family:inherit;font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-secondary);white-space:nowrap;
}
.ci-tab:hover{background:var(--tl-hover-bg)}
.ci-tab[aria-selected="true"]{background:var(--ez-primary);color:#fff;font-weight:500}
.ci-tools .sp{flex:1 1 auto}
.ci-selbar{
  display:none;align-items:center;gap:12px;flex:0 0 auto;padding:8px 20px;
  background:var(--color-palette-primary-opacity-light,rgba(24,111,244,.08));border-bottom:1px solid var(--tl-divider);
}
.ci-selbar.is-on{display:flex}
.ci-selbar .lb{font-weight:500;font-size:14px;line-height:22px;color:var(--theme-text-primary)}
.ci-selbar .sp{flex:1 1 auto}

/* ---------- Bảng ---------- */
.ci-body{flex:1 1 auto;min-height:0;display:flex;flex-direction:column;overflow:hidden}
.ci-tblwrap{flex:1 1 auto;min-height:0;overflow:auto}
.ci-tbl{width:100%;border-collapse:separate;border-spacing:0;table-layout:auto}
.ci-tbl th{
  position:sticky;top:0;z-index:2;height:48px;padding:0 12px;box-sizing:border-box;
  background:var(--theme-table-header);border-bottom:1px solid var(--tl-divider);
  font-weight:500;font-size:12px;line-height:16px;letter-spacing:.3px;text-transform:uppercase;
  color:var(--theme-text-secondary);text-align:left;white-space:nowrap;
}
.ci-tbl th.num,.ci-tbl td.num{text-align:right}
.ci-tbl th.c,.ci-tbl td.c{text-align:center}
.ci-tbl td{
  height:48px;padding:0 12px;border-bottom:1px solid var(--tl-divider);vertical-align:middle;
  font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-primary);white-space:nowrap;
}
.ci-tbl tr.ci-grp>td{background:var(--misc-grey-light);font-weight:500;cursor:pointer}
.ci-tbl tr.ci-row:hover>td{background:var(--tl-hover-bg)}
.ci-tbl .gcar{width:16px;height:16px;flex:0 0 auto;color:var(--theme-text-secondary);transition:transform .15s ease}
.ci-tbl tr.ci-grp.is-closed .gcar{transform:rotate(-90deg)}
.ci-grpn{display:inline-flex;align-items:center;gap:8px}

.ci-rm{display:inline-flex;align-items:center;gap:8px}
.ci-rm .ic{width:20px;height:20px;flex:0 0 auto}
.ci-rm .ic svg{width:100%;height:100%;display:block}
.ci-rm .ic[data-st="booked"]{color:var(--ez-primary)}
.ci-rm .ic[data-st="inhouse"]{color:var(--tl-st-inhouse,#DA4343)}
.ci-gu{display:inline-flex;align-items:center;gap:8px;min-width:0}
.ci-gu .fg{width:20px;height:14px;flex:0 0 auto;border-radius:2px;overflow:hidden;background:#DA251D;position:relative}
.ci-gu .fg svg{width:100%;height:100%;display:block}
.ci-gu .nm{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;max-width:150px}
.ci-gu .lead{width:14px;height:14px;flex:0 0 auto;color:var(--color-palette-success-main,#28C76F)}
.ci-stay{white-space:normal;line-height:20px;font-size:13px}

/* Radio thời điểm tính tiền (SRS 3.1.3.2 mục 10) */
.ci-rg{display:flex;align-items:center;gap:16px}
.ci-rd{display:inline-flex;align-items:center;gap:6px;cursor:pointer;white-space:nowrap;font-size:13px;line-height:20px}
.ci-rd input{position:absolute;opacity:0;width:0;height:0}
.ci-rd .dot{
  width:16px;height:16px;flex:0 0 auto;border-radius:9999px;box-sizing:border-box;
  border:2px solid var(--theme-input-border);display:inline-flex;align-items:center;justify-content:center;
}
.ci-rd .dot::after{content:'';width:8px;height:8px;border-radius:9999px;background:var(--ez-primary);opacity:0;transition:opacity .12s ease}
.ci-rd input:checked+.dot{border-color:var(--ez-primary)}
.ci-rd input:checked+.dot::after{opacity:1}
.ci-rd input:disabled+.dot{border-color:var(--theme-action-disabled-bg);cursor:not-allowed}
.ci-billlb{font-size:13px;line-height:20px;color:var(--theme-text-primary)}

.ci-n{display:flex;flex-direction:column;align-items:center;line-height:18px}
.ci-n .d{font-size:12px;color:var(--ez-error,#FF4C51)}
.ci-diff.up{color:var(--color-palette-success-main,#28C76F)}
.ci-diff.dn{color:var(--ez-error,#FF4C51)}

/* Ô chọn */
.ci-ck{position:relative;display:inline-flex;align-items:center;cursor:pointer}
.ci-ck input{position:absolute;opacity:0;width:0;height:0}
.ci-ck .box{
  width:18px;height:18px;border-radius:2px;box-sizing:border-box;
  border:2px solid var(--theme-input-border);display:inline-flex;align-items:center;justify-content:center;
}
.ci-ck .box svg{width:12px;height:12px;opacity:0;color:#fff}
.ci-ck input:checked+.box{background:var(--ez-primary);border-color:var(--ez-primary)}
.ci-ck input:checked+.box svg{opacity:1}
.ci-ck input:disabled+.box{border-color:var(--theme-action-disabled-bg);cursor:not-allowed}

/* Tag trạng thái thẻ (SRS 3.2.3.2 mục 10) */
.ci-tag{
  display:inline-flex;align-items:center;height:24px;padding:0 10px;border-radius:var(--tl-radius-sm,4px);
  font-weight:500;font-size:13px;line-height:20px;
}
.ci-tag[data-t="none"]{background:var(--theme-action-selected);color:var(--theme-text-secondary)}
.ci-tag[data-t="part"]{background:var(--color-palette-warning-opacity-light,rgba(255,159,67,.16));color:#CC7A33}
.ci-tag[data-t="done"]{background:var(--color-palette-success-opacity-lighter,rgba(40,199,111,.14));color:var(--color-palette-success-dark,#24B364)}

.ci-linkbtn{
  height:30px;padding:0 12px;border:0;border-radius:var(--tl-radius-md,6px);cursor:pointer;font-family:inherit;
  background:var(--color-palette-primary-opacity-light,rgba(24,111,244,.08));color:var(--ez-primary);
  font-weight:500;font-size:13px;line-height:20px;
}
.ci-linkbtn:hover:not(:disabled){background:rgba(24,111,244,.16)}
.ci-linkbtn:disabled{background:var(--theme-action-disabled-bg);color:var(--theme-text-disabled);cursor:not-allowed}
.ci-user{display:inline-flex;align-items:center;gap:8px}
.ci-user .av{
  width:26px;height:26px;flex:0 0 auto;border-radius:9999px;background:var(--misc-avatar-bg);
  display:inline-flex;align-items:center;justify-content:center;
  font-weight:500;font-size:11px;color:var(--theme-text-secondary);
}

/* Dòng tổng cộng (SRS 3.1.3.2 mục 16) */
.ci-sum{
  flex:0 0 auto;display:flex;align-items:center;height:56px;padding:0 12px;
  border-top:1px solid var(--tl-divider);background:var(--theme-table-header);
  font-weight:500;font-size:14px;line-height:22px;
}
.ci-sum .lb{flex:1 1 auto;text-align:right;padding-right:16px;text-transform:uppercase;letter-spacing:.3px}
.ci-sum .v{width:150px;text-align:right;padding-right:12px}

/* Empty state bước 2 */
.ci-empty{
  flex:1 1 auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:12px;padding:40px;
}
.ci-empty svg{width:96px;height:96px;color:var(--ez-primary)}
.ci-empty p{margin:0;font-weight:500;font-size:15px;line-height:24px;color:var(--theme-text-primary)}

/* ---------- Chân 70px ---------- */
.ci-foot{
  flex:0 0 auto;display:flex;align-items:center;gap:12px;height:70px;padding:0 16px;
  border-top:1px solid var(--tl-divider);
}
.ci-foot .sp{flex:1 1 auto}
.ci-btn-later{
  height:38px;padding:0 16px;border:0;border-radius:var(--tl-radius-md,6px);cursor:pointer;font-family:inherit;
  background:var(--color-palette-warning-main,#FF9F43);color:#fff;font-weight:500;font-size:14px;line-height:22px;
}
.ci-btn-later:hover{background:#E8912F}
.ci-btn-done{
  display:inline-flex;align-items:center;gap:8px;
  height:38px;padding:0 16px;border:0;border-radius:var(--tl-radius-md,6px);cursor:pointer;font-family:inherit;
  background:var(--color-palette-success-main,#28C76F);color:#fff;font-weight:500;font-size:14px;line-height:22px;
}
.ci-btn-done:hover{background:var(--color-palette-success-dark,#24B364)}
.ci-btn-done svg{width:18px;height:18px}

/* ==========================================================================
   POPUP TẠO THẺ TỪ (SRS 3.2.4 · Figma 32496:72226)
   ====================================================================== */
.ci-cbd{position:fixed;inset:0;background:rgba(47,43,61,.5);z-index:1218;display:none}
.ci-cbd.is-open{display:block}
.ci-card{
  position:fixed;top:50%;left:50%;transform:translate(-50%,-50%);z-index:1219;
  width:min(1000px,92vw);height:min(700px,88vh);box-sizing:border-box;
  display:none;flex-direction:column;overflow:hidden;
  background:var(--misc-paper);border-radius:var(--tl-radius-lg,8px);
  box-shadow:0 8px 32px rgba(47,43,61,.28);
}
.ci-card.is-open{display:flex}
.ci-card-h{
  display:flex;align-items:center;gap:12px;flex:0 0 auto;height:60px;padding:0 20px;
  border-bottom:1px solid var(--tl-divider);
}
.ci-card-h h3{margin:0;flex:1 1 auto;font-weight:500;font-size:18px;line-height:28px}
.ci-card-b{flex:1 1 auto;min-height:0;display:flex;overflow:hidden}
.ci-card-l{width:44%;flex:0 0 auto;min-width:0;overflow:auto;border-right:1px solid var(--tl-divider)}
.ci-card-r{flex:1 1 auto;min-width:0;display:flex;flex-direction:column;padding:20px;overflow:auto}
.ci-card-l table{width:100%;border-collapse:separate;border-spacing:0}
.ci-card-l th{
  position:sticky;top:0;z-index:2;height:44px;padding:0 12px;background:var(--theme-table-header);
  border-bottom:1px solid var(--tl-divider);text-align:left;white-space:nowrap;
  font-weight:500;font-size:12px;line-height:16px;letter-spacing:.3px;text-transform:uppercase;color:var(--theme-text-secondary);
}
.ci-card-l td{
  height:44px;padding:0 12px;border-bottom:1px solid var(--tl-divider);white-space:nowrap;
  font-weight:400;font-size:13px;line-height:20px;
}
.ci-card-l tr{cursor:pointer}
.ci-card-l tr:hover>td{background:var(--tl-hover-bg)}
.ci-card-l tr.is-cur>td{background:var(--color-palette-primary-opacity-light,rgba(24,111,244,.08))}
.ci-card-r .t{display:flex;align-items:center;gap:10px;font-weight:500;font-size:16px;line-height:24px;margin-bottom:16px}
.ci-card-r .t b{color:var(--ez-primary);font-weight:500}
.ci-card-r label.fl{display:block;font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary);margin-bottom:6px}
.ci-num{
  display:inline-flex;align-items:center;width:150px;height:38px;box-sizing:border-box;
  border:1px solid var(--theme-input-border);border-radius:var(--tl-radius-md,6px);overflow:hidden;
}
.ci-num input{
  flex:1 1 auto;min-width:0;height:100%;border:0;outline:0;background:none;padding:0 12px;
  font-family:inherit;font-size:14px;line-height:22px;color:var(--theme-text-primary);
}
.ci-num .sp2{display:flex;flex-direction:column;width:26px;flex:0 0 auto;border-left:1px solid var(--tl-divider)}
.ci-num .sp2 button{
  flex:1 1 auto;border:0;background:none;cursor:pointer;color:var(--theme-text-secondary);
  display:flex;align-items:center;justify-content:center;padding:0;
}
.ci-num .sp2 button:hover:not(:disabled){background:var(--tl-hover-bg);color:var(--theme-text-primary)}
.ci-num .sp2 button:disabled{color:var(--theme-action-disabled-bg);cursor:not-allowed}
.ci-num .sp2 button + button{border-top:1px solid var(--tl-divider)}
.ci-num .sp2 svg{width:12px;height:12px}
.ci-info{
  display:flex;gap:40px;margin:16px 0;padding:12px 16px;box-sizing:border-box;
  border-radius:var(--tl-radius-md,6px);background:var(--theme-table-header);
}
.ci-info .k{font-weight:400;font-size:13px;line-height:20px;color:var(--theme-text-secondary)}
.ci-info .v{font-weight:500;font-size:14px;line-height:22px;color:var(--theme-text-primary);margin-top:2px}
.ci-guide{
  flex:1 1 auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:16px;
  padding:16px;text-align:center;border-top:1px solid var(--tl-divider);
}
.ci-guide svg{width:200px;height:110px}
.ci-guide p{margin:0;font-weight:400;font-size:14px;line-height:22px;color:var(--theme-text-secondary)}
.ci-guide p b{font-weight:500;color:var(--theme-text-primary)}
.ci-write{
  display:flex;align-items:center;justify-content:center;gap:10px;position:relative;
  width:100%;height:48px;margin-top:16px;flex:0 0 auto;
  border:0;border-radius:var(--tl-radius-md,6px);cursor:pointer;font-family:inherit;
  background:var(--ez-primary);color:#fff;font-weight:500;font-size:15px;line-height:24px;
}
.ci-write:hover:not(:disabled){background:var(--color-palette-primary-dark,#1562D7)}
.ci-write:disabled{background:rgba(24,111,244,.45);cursor:progress}
.ci-write kbd{
  position:absolute;right:12px;padding:1px 6px;border-radius:4px;border:1px solid rgba(255,255,255,.6);
  font-family:inherit;font-size:12px;line-height:18px;
}
</style>
</head>
<body>

<div class="app" id="app">

  <!-- ============ SIDEBAR ============ -->
  <aside class="sidebar">
    <div class="logo-row">
      <button class="logo-btn" id="logoBtn" type="button" aria-label="ezOne PMS — bung menu">
        <svg class="logo-full" viewBox="72.87 67.6 951.91 204.8" fill="none" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="ezOne PMS">
          <path d="M175.27 272.4C231.824 272.4 277.67 226.554 277.67 170C277.67 113.446 231.824 67.6 175.27 67.6C118.716 67.6 72.87 113.446 72.87 170C72.87 226.554 118.716 272.4 175.27 272.4Z" fill="var(--logo-emblem)"/>
          <path d="M200.26 124.86H150.29C144.138 124.86 139.15 129.848 139.15 136V136.01C139.15 142.162 144.138 147.15 150.29 147.15H200.26C206.412 147.15 211.4 142.162 211.4 136.01V136C211.4 129.848 206.412 124.86 200.26 124.86Z" fill="var(--logo-bar-1)"/>
          <path d="M210.14 159.93H140.4C134.248 159.93 129.26 164.918 129.26 171.07V171.08C129.26 177.232 134.248 182.22 140.4 182.22H210.14C216.292 182.22 221.28 177.232 221.28 171.08V171.07C221.28 164.918 216.292 159.93 210.14 159.93Z" fill="var(--logo-bar-2)"/>
          <path d="M219.47 194.99H131.07C124.918 194.99 119.93 199.978 119.93 206.13V206.14C119.93 212.292 124.918 217.28 131.07 217.28H219.47C225.622 217.28 230.61 212.292 230.61 206.14V206.13C230.61 199.978 225.622 194.99 219.47 194.99Z" fill="var(--logo-bar-3)"/>
          <path d="M366.49 214.16C353.76 214.16 344.47 210.77 338.62 203.98C332.77 197.19 329.84 188.14 329.84 176.81C329.84 167.54 331.29 160.1 334.19 154.48C337.09 148.86 341.12 144.79 346.27 142.26C351.42 139.73 357.31 138.47 363.96 138.47C371.35 138.47 377.25 140.13 381.65 143.45C386.05 146.77 389.25 151.17 391.27 156.65C393.28 162.13 394.29 168.14 394.29 174.69C394.29 175.44 393.82 176.49 392.89 177.85C391.95 179.21 390.92 180.4 389.8 181.43C388.68 182.46 387.79 182.97 387.13 182.97H351.32C351.79 188.59 353.45 192.45 356.3 194.55C359.15 196.66 362.88 197.71 367.46 197.71C370.45 197.71 373.1 197.34 375.39 196.59C377.68 195.84 379.6 195.12 381.15 194.41C382.7 193.7 383.79 193.36 384.45 193.36C385.48 193.36 386.56 194.06 387.68 195.47C388.8 196.87 389.76 198.42 390.56 200.1C391.35 201.78 391.75 203.1 391.75 204.03C391.75 205.81 390.84 207.33 389.01 208.59C387.18 209.85 384.89 210.91 382.13 211.75C379.37 212.59 376.56 213.2 373.71 213.58C370.85 213.95 368.44 214.14 366.48 214.14L366.49 214.16ZM351.32 169.09H374.07C374.07 164.97 373.27 161.58 371.68 158.91C370.09 156.24 367.51 154.91 363.96 154.91C361.06 154.91 358.44 155.92 356.1 157.93C353.76 159.94 352.17 163.66 351.33 169.09H351.32Z" fill="var(--logo-ez)"/>
          <path d="M407.49 213.04C406.93 213.04 406.32 212.41 405.66 211.14C405 209.88 404.47 208.4 404.05 206.72C403.63 205.04 403.42 203.58 403.42 202.37C403.42 201.43 403.56 200.78 403.84 200.4L436 156.03H409.88C407.07 156.03 405.67 153.69 405.67 149.01V146.62C405.67 141.94 407.07 139.6 409.88 139.6H459.59C460.15 139.6 460.74 140.23 461.35 141.5C461.96 142.76 462.5 144.24 462.96 145.92C463.43 147.61 463.66 149.1 463.66 150.41C463.66 151.35 463.52 152.05 463.24 152.52L431.23 196.61H460.02C462.83 196.61 464.23 198.95 464.23 203.63V206.02C464.23 210.7 462.83 213.04 460.02 213.04H407.5H407.49Z" fill="var(--logo-ez)"/>
          <path d="M519.68 214.16C505.26 214.16 494.15 209.83 486.33 201.17C478.51 192.51 474.61 180.09 474.61 163.89C474.61 147.69 478.52 135.27 486.33 126.61C494.14 117.95 505.26 113.62 519.68 113.62C534.1 113.62 545.31 117.95 553.03 126.61C560.75 135.27 564.61 147.7 564.61 163.89C564.61 180.08 560.75 192.51 553.03 201.17C545.31 209.83 534.19 214.16 519.68 214.16ZM519.68 195.77C527.17 195.77 532.83 193.03 536.67 187.56C540.51 182.08 542.43 174.2 542.43 163.9C542.43 153.6 540.51 145.74 536.67 140.31C532.83 134.88 527.17 132.17 519.68 132.17C512.19 132.17 506.5 134.89 502.62 140.31C498.73 145.74 496.79 153.6 496.79 163.9C496.79 174.2 498.73 182.08 502.62 187.56C506.5 193.04 512.19 195.77 519.68 195.77Z" fill="var(--logo-one)"/>
          <path d="M588.06 213.32C582.44 213.32 579.64 211.82 579.64 208.83V164.32C579.64 159.64 579.5 156.04 579.22 153.51C578.94 150.98 578.66 149.09 578.38 147.82C578.1 146.56 577.96 145.46 577.96 144.52C577.96 143.68 578.59 142.98 579.86 142.41C581.13 141.84 582.67 141.38 584.49 141.01C586.32 140.64 588.09 140.38 589.83 140.24C591.56 140.1 592.85 140.03 593.69 140.03C595.19 140.03 596.29 140.55 596.99 141.57C597.69 142.6 598.18 143.82 598.46 145.22C598.74 146.62 599.02 147.94 599.3 149.15C601.36 146.44 604.21 143.98 607.87 141.78C611.52 139.58 615.64 138.48 620.23 138.48C627.25 138.48 632.45 140.33 635.82 144.03C639.19 147.73 640.87 153.46 640.87 161.23V208.83C640.87 211.83 638.06 213.32 632.45 213.32H628.52C622.9 213.32 620.1 211.82 620.1 208.83V164.32C620.1 161.61 619.47 159.48 618.2 157.93C616.94 156.39 614.76 155.61 611.67 155.61C608.77 155.61 606.38 156.57 604.51 158.49C602.64 160.41 601.28 162.16 600.44 163.76V208.83C600.44 211.83 597.63 213.32 592.02 213.32H588.09H588.06Z" fill="var(--logo-one)"/>
          <path d="M691.68 214.16C678.95 214.16 669.66 210.77 663.81 203.98C657.96 197.19 655.03 188.14 655.03 176.81C655.03 167.54 656.48 160.1 659.38 154.48C662.28 148.86 666.31 144.79 671.46 142.26C676.61 139.73 682.5 138.47 689.15 138.47C696.54 138.47 702.44 140.13 706.84 143.45C711.24 146.77 714.44 151.17 716.46 156.65C718.47 162.13 719.48 168.14 719.48 174.69C719.48 175.44 719.01 176.49 718.08 177.85C717.14 179.21 716.11 180.4 714.99 181.43C713.87 182.46 712.98 182.97 712.32 182.97H676.51C676.98 188.59 678.64 192.45 681.49 194.55C684.34 196.66 688.07 197.71 692.65 197.71C695.64 197.71 698.29 197.34 700.58 196.59C702.87 195.84 704.79 195.12 706.34 194.41C707.89 193.7 708.98 193.36 709.64 193.36C710.67 193.36 711.75 194.06 712.87 195.47C713.99 196.87 714.95 198.42 715.75 200.1C716.54 201.78 716.94 203.1 716.94 204.03C716.94 205.81 716.03 207.33 714.2 208.59C712.37 209.85 710.08 210.91 707.32 211.75C704.56 212.59 701.75 213.2 698.9 213.58C696.04 213.95 693.63 214.14 691.67 214.14L691.68 214.16ZM676.52 169.09H699.27C699.27 164.97 698.47 161.58 696.88 158.91C695.29 156.24 692.71 154.91 689.16 154.91C686.26 154.91 683.64 155.92 681.3 157.93C678.96 159.94 677.37 163.66 676.53 169.09H676.52Z" fill="var(--logo-one)"/>
          <path d="M776.63 213.32C771.01 213.32 768.21 211.82 768.21 208.83V120.37C768.21 116.63 770.08 114.75 773.83 114.75H803.32C806.41 114.75 809.87 115.1 813.71 115.8C817.55 116.5 821.22 117.88 824.73 119.94C828.24 122 831.12 125.09 833.37 129.21C835.62 133.33 836.74 138.76 836.74 145.5C836.74 150.93 835.9 155.47 834.21 159.12C832.53 162.77 830.35 165.74 827.68 168.04C825.01 170.33 822.2 172.07 819.26 173.24C816.31 174.41 813.52 175.21 810.91 175.63C808.29 176.05 806.23 176.26 804.73 176.26H789.85V208.84C789.85 211.84 787.04 213.33 781.43 213.33H776.66L776.63 213.32ZM789.83 157.71H802.33C803.64 157.71 805.02 157.55 806.47 157.22C807.92 156.89 809.25 156.29 810.47 155.39C811.69 154.5 812.67 153.26 813.42 151.67C814.17 150.08 814.54 148.02 814.54 145.49C814.54 142.12 813.88 139.55 812.57 137.77C811.26 135.99 809.67 134.78 807.8 134.12C805.93 133.47 804.1 133.14 802.32 133.14H789.82V157.71H789.83Z" fill="var(--logo-pms)"/>
          <path d="M895.7 189.59C893.26 189.59 891.39 188.7 890.08 186.92L872.81 156.03C872.25 155.1 871.71 154.18 871.2 153.29C870.68 152.4 870.19 151.49 869.73 150.55L869.17 150.69V208.82C869.17 211.82 866.36 213.31 860.75 213.31H856.68C851.06 213.31 848.26 211.81 848.26 208.82V118.95C848.26 115.95 851.07 114.46 856.68 114.46H864.12C866.18 114.46 867.98 114.74 869.53 115.3C871.07 115.86 872.22 116.84 872.97 118.25L896.28 159.81C896.47 160.19 896.7 160.56 896.98 160.93C897.26 161.31 897.49 161.73 897.68 162.19C897.87 161.72 898.08 161.3 898.31 160.93C898.54 160.56 898.75 160.18 898.94 159.81L922.67 118.25C923.42 116.85 924.57 115.86 926.11 115.3C927.65 114.74 929.46 114.46 931.52 114.46H938.82C944.44 114.46 947.24 115.96 947.24 118.95V208.82C947.24 211.82 944.43 213.31 938.82 213.31H934.75C929.13 213.31 926.33 211.81 926.33 208.82V150.69L925.77 150.55C925.3 151.49 924.81 152.4 924.3 153.29C923.78 154.18 923.25 155.09 922.69 156.03L905.42 186.92C904.11 188.7 902.24 189.59 899.8 189.59H895.73H895.7Z" fill="var(--logo-pms)"/>
          <path d="M993.01 214.16C985.89 214.16 979.9 213.43 975.04 211.98C970.17 210.53 966.47 208.92 963.95 207.14C961.33 205.36 960.02 203.58 960.02 201.8C960.02 200.3 960.53 198.59 961.56 196.68C962.59 194.76 963.81 193.05 965.21 191.56C966.61 190.06 967.78 189.31 968.72 189.31C969.19 189.31 969.72 189.47 970.33 189.8C970.94 190.13 971.71 190.53 972.65 190.99C974.52 191.93 976.84 192.96 979.6 194.08C982.36 195.2 985.61 195.77 989.36 195.77C991.42 195.77 993.45 195.51 995.47 195C997.48 194.49 999.17 193.5 1000.52 192.05C1001.88 190.6 1002.56 188.56 1002.56 185.94C1002.56 183.13 1001.48 180.88 999.33 179.2C997.18 177.52 994.44 176.02 991.12 174.71C987.8 173.4 984.4 171.9 980.94 170.22C978.5 169.1 976.17 167.76 973.92 166.22C971.67 164.68 969.64 162.83 967.81 160.67C965.98 158.52 964.56 155.92 963.53 152.88C962.5 149.84 961.99 146.21 961.99 142C961.99 135.26 963.63 129.81 966.9 125.64C970.18 121.48 974.36 118.43 979.47 116.51C984.57 114.59 989.84 113.63 995.27 113.63C998.64 113.63 1001.73 113.86 1004.54 114.33C1007.35 114.8 1009.85 115.43 1012.05 116.23C1014.25 117.03 1016.05 117.94 1017.46 118.97C1019.52 120.47 1020.55 121.97 1020.55 123.46C1020.55 124.68 1020.15 126.27 1019.36 128.23C1018.56 130.2 1017.56 131.93 1016.34 133.43C1015.12 134.93 1013.95 135.68 1012.83 135.68C1012.36 135.68 1011.73 135.49 1010.93 135.12C1010.13 134.75 1009.08 134.33 1007.77 133.86C1006.55 133.39 1005.08 133 1003.35 132.67C1001.62 132.34 999.58 132.18 997.24 132.18C993.59 132.18 990.83 132.6 988.96 133.44C987.09 134.28 985.82 135.31 985.17 136.53C984.51 137.75 984.19 138.92 984.19 140.04C984.19 142.57 984.89 144.65 986.3 146.29C987.7 147.93 989.53 149.31 991.78 150.43C994.03 151.55 996.51 152.68 999.22 153.8C1002.12 155.02 1005.07 156.35 1008.07 157.8C1011.06 159.25 1013.83 161.05 1016.35 163.21C1018.88 165.36 1020.91 168.08 1022.46 171.35C1024 174.63 1024.78 178.65 1024.78 183.43C1024.78 189.14 1023.87 193.96 1022.04 197.89C1020.21 201.82 1017.76 204.98 1014.67 207.37C1011.58 209.76 1008.16 211.49 1004.42 212.57C1000.67 213.64 996.88 214.18 993.05 214.18L993.01 214.16Z" fill="var(--logo-pms)"/>
        </svg>
        <svg class="logo-mark" viewBox="72.87 67.6 204.8 204.8" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <path d="M175.27 272.4C231.824 272.4 277.67 226.554 277.67 170C277.67 113.446 231.824 67.6 175.27 67.6C118.716 67.6 72.87 113.446 72.87 170C72.87 226.554 118.716 272.4 175.27 272.4Z" fill="var(--logo-emblem)"/>
          <path d="M200.26 124.86H150.29C144.138 124.86 139.15 129.848 139.15 136V136.01C139.15 142.162 144.138 147.15 150.29 147.15H200.26C206.412 147.15 211.4 142.162 211.4 136.01V136C211.4 129.848 206.412 124.86 200.26 124.86Z" fill="var(--logo-bar-1)"/>
          <path d="M210.14 159.93H140.4C134.248 159.93 129.26 164.918 129.26 171.07V171.08C129.26 177.232 134.248 182.22 140.4 182.22H210.14C216.292 182.22 221.28 177.232 221.28 171.08V171.07C221.28 164.918 216.292 159.93 210.14 159.93Z" fill="var(--logo-bar-2)"/>
          <path d="M219.47 194.99H131.07C124.918 194.99 119.93 199.978 119.93 206.13V206.14C119.93 212.292 124.918 217.28 131.07 217.28H219.47C225.622 217.28 230.61 212.292 230.61 206.14V206.13C230.61 199.978 225.622 194.99 219.47 194.99Z" fill="var(--logo-bar-3)"/>
        </svg>
      </button>
      <button class="pin-btn" id="pinBtn" type="button" aria-label="Thu gọn menu">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M15 4.5l-4 4l-4 1.5l-1.5 1.5l7 7l1.5 -1.5l1.5 -4l4 -4"/><path d="M9 15l-4.5 4.5"/><path d="M14.5 4l5.5 5.5"/>
        </svg>
      </button>
    </div>

    <nav class="menu-drawer" aria-label="Menu chính">
      <div class="pages">
        <div class="pages-scroll" id="pagesScroll"><!-- render bằng JS --></div>
      </div>
    </nav>
  </aside>

  <!-- ============ CONTAINER ============ -->
  <div class="container">
    <div class="nav-wrap">
      <header class="nav-header">
        <div class="search" id="gsTrigger" role="button" tabindex="0"
             aria-haspopup="dialog" aria-expanded="false" aria-controls="gsCard"
             aria-label="Tìm kiếm theo đặt phòng, chức năng">
          <button class="icon-btn" type="button" tabindex="-1" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M10 10m-7 0a7 7 0 1 0 14 0a7 7 0 1 0 -14 0"/><path d="M21 21l-6 -6"/>
            </svg>
          </button>
          <span class="search-placeholder">Tìm kiếm theo đặt phòng, chức năng</span>
          <kbd class="search-kbd" id="gsKbd">Ctrl K</kbd>
        </div>

        <div class="actions">
          <div class="select">
            <button class="select-field" type="button" aria-label="Chọn chi nhánh" aria-haspopup="listbox">
              <span class="select-value">Chi nhánh Hà Nội</span>
              <svg class="select-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6l6 -6"/></svg>
            </button>
          </div>

          <!-- ===== Global Setup Progress Bar (Quick Setup) ===== -->
          <div class="qs" id="qs" data-variant="1">
            <button class="qs-trigger" type="button" id="qsTrigger"
                    aria-haspopup="dialog" aria-expanded="false" aria-controls="qsPanel"
                    aria-label="Thiết lập khách sạn — xem tiến độ hoàn thành thông tin bắt buộc">
              <svg class="qs-ring" viewBox="0 0 36 36" aria-hidden="true">
                <circle class="qs-ring-bg" cx="18" cy="18" r="15.5"></circle>
                <circle class="qs-ring-fg" id="qsRing" cx="18" cy="18" r="15.5"
                        stroke-dasharray="97.39" stroke-dashoffset="97.39"></circle>
                <text class="qs-ring-text" id="qsRingPct" x="18" y="18">0%</text>
              </svg>
              <span class="qs-t-label">Thiết lập khách sạn</span>
              <span class="qs-t-count" id="qsCountPill">0/0</span>
            </button>
          </div>

          <div class="tools">
            <button class="icon-btn" type="button" id="btnApps" aria-label="Ứng dụng" aria-haspopup="dialog" aria-expanded="false">
              <!-- Icon Ứng dụng — EzOne.DesignLibrary "btn-icon / btn-secondary".
                   viewBox cắt về vùng icon 22px (offset 8 = padding của nút 38px)
                   để giữ đúng tỷ lệ 1:1 và stroke-width 1.5 như bản gốc. -->
              <svg viewBox="8 8 22 22" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <rect x="11.668" y="11.6665" width="5.5" height="5.5" rx="1"/>
                <rect x="20.832" y="11.6665" width="5.5" height="5.5" rx="1"/>
                <rect x="11.668" y="20.8335" width="5.5" height="5.5" rx="1"/>
                <rect x="20.832" y="20.8335" width="5.5" height="5.5" rx="1"/>
              </svg>
            </button>
            <div class="bell-wrap">
              <button class="icon-btn" type="button" aria-label="Thông báo">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                  <path d="M10 5a2 2 0 1 1 4 0a7 7 0 0 1 4 6v3a4 4 0 0 0 2 3h-16a4 4 0 0 0 2 -3v-3a7 7 0 0 1 4 -6"/><path d="M9 17v1a3 3 0 0 0 6 0v-1"/>
                </svg>
              </button>
              <span class="badge" aria-label="Có thông báo mới"></span>
            </div>
          </div>

          <div class="avatar">
            <img src="data:image/svg+xml;utf8,%3Csvg xmlns='http://www.w3.org/2000/svg' width='76' height='76' viewBox='0 0 76 76'%3E%3Crect width='76' height='76' fill='%23EEEDF0'/%3E%3Ccircle cx='38' cy='30' r='13' fill='%23B7B4C0'/%3E%3Cpath d='M12 76c0-14.36 11.64-26 26-26s26 11.64 26 26z' fill='%23B7B4C0'/%3E%3C/svg%3E" alt="Ảnh đại diện người dùng">
          </div>
        </div>
      </header>
    </div>

    <main class="body">
      <section class="card tl-host" id="contentCard" aria-label="Nội dung màn hình">
        <!-- ============ SƠ ĐỒ PHÒNG — TIMELINE ============ -->
        <div class="tl" id="tl">

          <!-- ---------- Searchbar / Header ---------- -->
          <div class="tl-headwrap">
          <div class="tl-head">
            <div class="tl-toolbar">
              <div class="tl-toolbar-l">

                <!-- 1.1 Date navigator -->
                <div class="tl-btngroup" role="group" aria-label="Ngày xem">
                  <button class="tl-navbtn" type="button" id="tlPrev" aria-label="Ngày trước">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 6l-6 6 6 6"/></svg>
                  </button>
                  <button class="tl-datefield" type="button" id="tlDateBtn"
                          aria-haspopup="dialog" aria-expanded="false" aria-label="Ô hiển thị ngày">
                    <span class="tl-datetext" id="tlDateText">28/02/2025</span>
                  </button>
                  <button class="tl-navbtn" type="button" id="tlNext" aria-label="Ngày tiếp theo">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 6l6 6-6 6"/></svg>
                  </button>
                </div>

                <!-- 1.2 Hôm nay -->
                <button class="tl-btn-outline" type="button" id="tlToday" aria-pressed="false">Hôm nay</button>

                <!-- 1.3 Chế độ thời gian -->
                <div class="tl-seg" role="group" aria-label="Chế độ hiển thị thời gian">
                  <button type="button" data-range="day" aria-pressed="false">Ngày</button>
                  <button type="button" data-range="week" aria-pressed="true">Tuần</button>
                  <button type="button" data-range="month" aria-pressed="false">Tháng</button>
                </div>

                <!-- 1.4 Nhóm phòng -->
                <div class="tl-seg" role="group" aria-label="Chế độ hiển thị nhóm phòng">
                  <button type="button" data-group="type" aria-pressed="true">Loại</button>
                  <button type="button" data-group="room" aria-pressed="false">Phòng</button>
                </div>

                <!-- 1.4b Nhóm phòng cho Sơ đồ chi tiết / đơn giản (Figma 55588:67065) -->
                <div class="tl-seg tl-seg4" id="tlGroup4" role="group" aria-label="Chế độ nhóm phòng" hidden>
                  <button type="button" data-group4="status" aria-pressed="true">Đặt phòng</button>
                  <button type="button" data-group4="type" aria-pressed="false">Loại</button>
                  <button type="button" data-group4="floor" aria-pressed="false">Tầng</button>
                  <button type="button" data-group4="room" aria-pressed="false">Phòng</button>
                </div>

                <!-- Checkbox Xem ngày (Figma) -->
                <label class="tl-check">
                  <input type="checkbox" id="tlShowDate">
                  <span class="box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg></span>
                  <span class="lb">Xem ngày</span>
                </label>
              </div>

              <div class="tl-toolbar-r">
                <!-- 1.4c Tìm kiếm tên phòng (sơ đồ chi tiết / đơn giản) -->
                <label class="tl-search" id="tlSearchWrap" hidden>
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><circle cx="11" cy="11" r="7"/><path d="M20 20l-3.5-3.5"/></svg>
                  <input type="search" id="tlSearch" placeholder="Tìm kiếm tên phòng" aria-label="Tìm kiếm tên phòng">
                </label>
                <!-- 1.5 Đọc thẻ -->
                <button class="tl-btn-primary" type="button" id="tlCardReader">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="2" y="6" width="14" height="12" rx="2"/><path d="M18 9c1.5 1 1.5 5 0 6M21 7c2.5 2 2.5 8 0 10"/></svg>
                  Đọc thẻ
                </button>
                <!-- 1.6 Cấu hình hiển thị -->
                <button class="tl-iconbtn" type="button" id="tlConfig" aria-label="Cấu hình hiển thị">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M10.3 3.6a1.8 1.8 0 013.4 0l.2.7a1.8 1.8 0 002.5 1l.7-.3a1.8 1.8 0 012.3 2.4l-.3.6a1.8 1.8 0 001 2.6l.7.2a1.8 1.8 0 010 3.4l-.7.2a1.8 1.8 0 00-1 2.6l.3.6a1.8 1.8 0 01-2.3 2.4l-.7-.3a1.8 1.8 0 00-2.5 1l-.2.7a1.8 1.8 0 01-3.4 0l-.2-.7a1.8 1.8 0 00-2.5-1l-.7.3a1.8 1.8 0 01-2.3-2.4l.3-.6a1.8 1.8 0 00-1-2.6l-.7-.2a1.8 1.8 0 010-3.4l.7-.2a1.8 1.8 0 001-2.6l-.3-.6a1.8 1.8 0 012.3-2.4l.7.3a1.8 1.8 0 002.5-1z"/><circle cx="12" cy="12" r="3"/></svg>
                </button>
              </div>
            </div>

            <!-- 1.7 Ghi chú trạng thái + 1.8 Loại sơ đồ -->
            <div class="tl-filterbar">
              <div class="tl-legend" id="tlLegend" role="group" aria-label="Bộ lọc trạng thái phòng"></div>
              <span class="tl-filter-note" id="tlFilterNote" aria-live="polite">
                <span id="tlFilterNoteText"></span>
                <button type="button" id="tlFilterClear" aria-label="Bỏ lọc">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
                </button>
              </span>
              <div class="tl-viewswitch" role="group" aria-label="Chế độ hiển thị sơ đồ">
                <button type="button" data-chart="timeline" aria-pressed="true" aria-label="Sơ đồ Timeline" title="Sơ đồ Timeline">
                  <svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><rect x="2" y="5" width="9" height="4" rx="1" fill="currentColor"/><rect x="12" y="5" width="10" height="4" rx="1" fill="currentColor" opacity=".28"/><rect x="6" y="15" width="9" height="4" rx="1" fill="currentColor"/><rect x="16" y="15" width="6" height="4" rx="1" fill="currentColor" opacity=".28"/><path d="M13 2v20" stroke="#FF4C51" stroke-width="1.2" stroke-dasharray="3 3"/></svg>
                </button>
                <button type="button" data-chart="detail" aria-pressed="false" aria-label="Sơ đồ Chi tiết" title="Sơ đồ Chi tiết">
                  <svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><rect x="3" y="5" width="6" height="5" rx="1" fill="currentColor"/><rect x="10" y="5" width="11" height="5" rx="1" fill="currentColor" opacity=".28"/><rect x="3" y="14" width="6" height="5" rx="1" fill="currentColor"/><rect x="10" y="14" width="11" height="5" rx="1" fill="currentColor" opacity=".28"/></svg>
                </button>
                <button type="button" data-chart="simple" aria-pressed="false" aria-label="Sơ đồ Đơn giản" title="Sơ đồ Đơn giản">
                  <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="6" cy="6" r="2"/><circle cx="12" cy="6" r="2"/><circle cx="18" cy="6" r="2"/><circle cx="6" cy="12" r="2"/><circle cx="12" cy="12" r="2"/><circle cx="18" cy="12" r="2"/><circle cx="6" cy="18" r="2"/><circle cx="12" cy="18" r="2"/><circle cx="18" cy="18" r="2"/></svg>
                </button>
              </div>
            </div>
          </div>
          </div>

          <!-- ---------- Grid ---------- -->
          <div class="tl-scroll" id="tlScroll">
            <table class="tl-table" id="tlTable">
              <thead id="tlHead"></thead>
              <tbody id="tlBody"></tbody>
            </table>
            <div class="tl-now" id="tlNowLine" hidden></div>
            <div class="tl-now-chip" id="tlNowChip" hidden></div>
            <div class="tl-holiday-chip" id="tlHolidayChip" hidden></div>
          </div>

          <!-- ---------- Sơ đồ chi tiết / đơn giản (Figma 55588:67065 · 55588:67066) ---------- -->
          <div class="tl-cards" id="tlCards" hidden></div>

          <!-- ---------- Empty state dự phòng ---------- -->
          <div class="tl-empty" id="tlEmpty" hidden>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="16" rx="2"/><path d="M3 10h18M9 10v10"/></svg>
            <p id="tlEmptyText">Đang phát triển</p>
          </div>
        </div>
      </section>
      <!-- ============ DANH SÁCH ĐẶT PHÒNG (R01.03) ============ -->
      <section class="card lb-host" id="lbCard" aria-label="Danh sách đặt phòng" hidden>
        <div class="lb">

          <!-- Bộ lọc inline -->
          <div class="lb-filters">
            <div class="lb-title">
              <button class="lb-titlebtn" type="button" id="lbListBtn" aria-haspopup="listbox" aria-expanded="false">
                <span id="lbListName">Khách đang ở</span>
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>
              </button>
              <span class="lb-desc" id="lbListDesc">Danh sách khách đang lưu trú</span>
            </div>

            <div class="lb-frow">
              <div class="lb-f w-s">
                <label class="lb-lb" for="lbKeyword">Từ khoá tìm kiếm</label>
                <span class="lb-inwrap has-pre">
                  <span class="pre" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"><circle cx="11" cy="11" r="7"/><path d="M20 20l-3.5-3.5"/></svg></span>
                  <input class="lb-ctrl" id="lbKeyword" placeholder="Nhập từ khoá" maxlength="200" autocomplete="off">
                </span>
              </div>
              <div class="lb-f w-m">
                <label class="lb-lb" for="lbRange">Khoảng ngày đến</label>
                <span class="lb-inwrap has-suf">
                  <input class="lb-ctrl" id="lbRange" placeholder="Từ ngày - Đến ngày" readonly>
                  <button class="suf" type="button" id="lbRangeBtn" aria-label="Chọn khoảng ngày">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M3 10h18M8 3v4M16 3v4"/></svg>
                  </button>
                </span>
              </div>
              <div class="lb-f w-l">
                <span class="lb-lb">Trạng thái</span>
                <div class="qb-vs" data-qbsel="lbStatus" data-qblabel="Trạng thái"></div>
              </div>
              <button class="lb-addf" type="button" id="lbAddFilter">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"><path d="M12 5v14M5 12h14"/></svg>
                Thêm bộ lọc
              </button>
            </div>

            <div class="lb-arow">
              <button class="lb-btn primary" type="button" id="lbSearchBtn">Tìm kiếm</button>
              <button class="lb-btn" type="button" id="lbSaveTpl">Lưu mẫu mới</button>
            </div>
          </div>

          <!-- Thanh công cụ bảng -->
          <div class="lb-tbar">
            <span class="lb-bulk" id="lbBulk">
              <span id="lbBulkTxt">Đã chọn 0</span>
              <button class="lb-btn" type="button" data-lbbulk="checkin">Nhận phòng</button>
              <button class="lb-btn" type="button" data-lbbulk="card">Tạo thẻ</button>
              <button class="lb-btn" type="button" data-lbbulk="cancel">Huỷ đặt phòng</button>
            </span>
            <span class="sp"></span>
            <span class="lb-tbtns">
              <button class="lb-btn lb-btn-sm" type="button" id="lbDownload">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 4v11M8 11l4 4 4-4M4 19h16"/></svg>
                Tải danh sách
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>
              </button>
              <button class="lb-btn lb-btn-sm" type="button" id="lbPrint">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9V4h12v5"/><path d="M6 18H4a1 1 0 01-1-1v-6a1 1 0 011-1h16a1 1 0 011 1v6a1 1 0 01-1 1h-2"/><rect x="7" y="14" width="10" height="7" rx="1"/></svg>
                In danh sách
              </button>
              <button class="lb-btn lb-btn-sm" type="button" id="lbColsBtn">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="16" rx="2"/><path d="M10 4v16"/></svg>
                Cột hiển thị
              </button>
            </span>
          </div>

          <!-- Bảng -->
          <div class="lb-scroll">
            <table class="lb-table" id="lbTable">
              <thead id="lbHead"></thead>
              <tbody id="lbBody"></tbody>
            </table>
            <div class="lb-empty" id="lbEmpty" hidden>Không có bản ghi nào khớp điều kiện lọc</div>
          </div>

          <!-- Phân trang -->
          <div class="lb-foot">
            <span class="k">Số bản ghi trên trang</span>
            <div class="qb-vs" data-qbsel="lbPageSize" data-qblabel="Số bản ghi trên trang" style="width:88px"></div>
            <span class="sp"></span>
            <span class="k" id="lbCount">Hiển thị 0 - 0 trên 0 bản ghi</span>
            <nav class="lb-pager" id="lbPager" aria-label="Phân trang"></nav>
          </div>
        </div>
      </section>

      <section class="card cfg" id="cfgCard" hidden aria-label="Cấu hình sơ đồ phòng">
        <header class="cfg-head">
          <div class="cfg-ttl">
            <h1 id="cfgTitle">Cấu hình sơ đồ phòng</h1>
            <p id="cfgSub">Chọn thông tin sẽ hiển thị trên sơ đồ phòng. Bỏ chọn để ẩn thông tin đó khỏi màn hình.</p>
          </div>
          <div class="cfg-acts">
            <button class="cfg-btn" type="button" id="cfgReset">Đặt lại</button>
            <button class="cfg-btn primary" type="button" id="cfgSave">Lưu</button>
          </div>
        </header>

        <div class="cfg-tabs" id="cfgTabs" role="tablist" aria-label="Loại sơ đồ">
          <button class="cfg-tab is-on" type="button" role="tab" aria-selected="true" data-cfgtab="timeline">Sơ đồ timeline</button>
          <button class="cfg-tab" type="button" role="tab" aria-selected="false" data-cfgtab="detail">Sơ đồ chi tiết</button>
          <button class="cfg-tab" type="button" role="tab" aria-selected="false" data-cfgtab="simple">Sơ đồ đơn giản</button>
          <button class="cfg-tab" type="button" role="tab" aria-selected="false" data-cfgtab="card">Thẻ xem nhanh</button>
        </div>

        <div class="cfg-body">
          <div class="cfg-main">
            <div class="cfg-filters">
              <div class="cfg-f">
                <span class="cfg-lb">Lọc nhóm thông tin</span>
                <div class="qb-vs" data-qbsel="cfgFGroup" data-qblabel="Lọc nhóm thông tin"></div>
              </div>
              <div class="cfg-f">
                <span class="cfg-lb">Lọc trường thông tin</span>
                <div class="qb-vs" data-qbsel="cfgFField" data-qblabel="Lọc trường thông tin"></div>
              </div>
            </div>
            <div id="cfgGroups"></div>
          </div>

          <aside class="cfg-prev" aria-label="Xem trước">
            <div class="cfg-prev-h">
              <h3>Xem trước</h3>
              <span class="cfg-prev-tag" id="cfgPrevTag">Sơ đồ timeline</span>
            </div>
            <div class="cfg-prev-bd" id="cfgPrev"></div>
            <p class="cfg-prev-note">Bản xem trước dùng dữ liệu mẫu. Thay đổi được áp dụng ngay khi bấm Lưu.</p>
          </aside>
        </div>
      </section>

      <!-- ============ DANH SÁCH BÁO CÁO (RR00) — Figma 35977:4589 ============ -->
      <section class="card rl" id="rlCard" hidden aria-label="Danh sách báo cáo">

        <!-- ---------- Cột trái: danh sách báo cáo ---------- -->
        <aside class="rl-side" aria-label="Danh sách báo cáo">
          <div class="rl-sidehead">
            <h1 class="rl-title">Danh sách báo cáo</h1>
            <label class="rl-search">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><circle cx="11" cy="11" r="7"/><path d="M20 20l-3.5-3.5"/></svg>
              <input type="search" id="rlSearch" maxlength="50" placeholder="Nhập tên, mã báo cáo" aria-label="Tìm kiếm báo cáo">
            </label>
            <div class="rl-tabs" role="tablist" aria-label="Chế độ xem báo cáo">
              <button type="button" role="tab" data-rltab="all" aria-selected="true">Tất cả</button>
              <button type="button" role="tab" data-rltab="fav" aria-selected="false">Yêu thích</button>
            </div>
          </div>
          <div class="rl-list" id="rlList"></div>
        </aside>

        <!-- ---------- Cột phải: xem trước báo cáo ---------- -->
        <section class="rl-main" aria-label="Xem trước báo cáo">
          <div class="rl-preview" id="rlPreview">

            <!-- Khung giấy A4 xem trước — dùng chung renderer với màn xem báo cáo -->
            <div class="rl-pvwrap" id="rlSheet">
              <div class="rl-pvscale" id="rlPvScale">
                <article class="rl-pv ca-a4" id="rlPvPage"></article>
              </div>
            </div>

            <!-- Trạng thái rỗng cho báo cáo chưa có mẫu xem trước -->
            <div class="rl-empty" id="rlEmpty" hidden>
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14 3H7a2 2 0 00-2 2v14a2 2 0 002 2h10a2 2 0 002-2V8z"/><path d="M14 3v5h5M9 13h6M9 17h4"/></svg>
              <p class="t" id="rlEmptyTitle">Chưa có mẫu xem trước</p>
              <p class="s">Báo cáo này chưa có bản xem trước. Bấm <b>Sử dụng báo cáo</b> để mở màn hình báo cáo.</p>
            </div>
          </div>

          <footer class="rl-cta">
            <button class="rl-btn-primary" type="button" id="rlUseBtn">
              <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M8 5.5v13l11-6.5z"/></svg>
              Sử dụng báo cáo
            </button>
          </footer>
        </section>
      </section>
<div class="rl-tip" id="rlTip" role="tooltip" aria-hidden="true"></div>

      <!-- ============ TẠO ĐẶT PHÒNG ĐẦY ĐỦ (Reservation) — Figma 47216:315103 ============ -->
      <section class="card rs" id="rsCard" hidden aria-label="Tạo đặt phòng đầy đủ">

        <!-- ---------- Các bước ---------- -->
        <nav class="rs-steps" id="rsSteps" aria-label="Các bước tạo đặt phòng">
          <button class="rs-step is-cur" type="button" data-rsstep="1" aria-current="step">
            <span class="no">1</span><span class="tx">Tìm và báo giá phòng</span>
          </button>
          <span class="rs-sep" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6-6 6"/></svg></span>
          <button class="rs-step" type="button" data-rsstep="2" disabled>
            <span class="no">2</span><span class="tx">Thông tin đặt phòng</span>
          </button>
          <span class="rs-sep" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6-6 6"/></svg></span>
          <button class="rs-step" type="button" data-rsstep="3" disabled>
            <span class="no">3</span><span class="tx">Thanh toán</span>
          </button>
        </nav>

        <!-- ================= BƯỚC 1 ================= -->
        <div class="rs-pane" id="rsP1">
          <div class="rs-filter">
            <div class="rs-frow">
              <div class="rs-f rs-c3"><span class="lbl">Chi nhánh</span>
                <div class="qb-vs" data-qbsel="rsBranch" data-qblabel="Chi nhánh"></div>
              </div>
              <div class="rs-f rs-c2"><span class="lbl">Kênh bán</span>
                <div class="qb-vs" data-qbsel="rsChannel" data-qblabel="Kênh bán"></div>
              </div>
              <div class="rs-f rs-c2"><span class="lbl">Ngày đến<span class="req">*</span></span>
                <button class="rs-dt" type="button" id="rsIn" data-rsdate="in"><span class="v">—</span><span class="ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span></button>
              </div>
              <div class="rs-f rs-c2"><span class="lbl">Ngày đi<span class="req">*</span></span>
                <button class="rs-dt" type="button" id="rsOut" data-rsdate="out"><span class="v">—</span><span class="ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span></button>
              </div>
              <div class="rs-f rs-c1"><span class="lbl">Người lớn</span><input type="number" id="rsAd" min="1" max="30" value="2"></div>
              <div class="rs-f rs-c1"><span class="lbl">Trẻ em</span><input type="number" id="rsCh" min="0" max="30" value="0"></div>
              <div class="rs-f rs-c1"><span class="lbl">Em bé</span><input type="number" id="rsEb" min="0" max="30" value="0"></div>
            </div>

            <div class="rs-frow">
              <div class="rs-f rs-c3"><span class="lbl">Loại phòng</span>
                <div class="rs-chips" id="rsTypes" role="button" tabindex="0" aria-haspopup="listbox" aria-expanded="false" aria-label="Loại phòng"></div>
              </div>
              <div class="rs-f rs-c2"><span class="lbl">Công ty</span>
                <div class="qb-vs" data-qbsel="rsCompany" data-qblabel="Công ty"></div>
              </div>
              <div class="rs-f rs-c2"><span class="lbl">Nguồn</span>
                <div class="qb-vs" data-qbsel="rsSource" data-qblabel="Nguồn"></div>
              </div>
              <div class="rs-f rs-c2"><span class="lbl">Quỹ phòng</span>
                <div class="qb-vs" data-qbsel="rsPool" data-qblabel="Quỹ phòng"></div>
              </div>
              <div class="rs-f rs-c3"><span class="lbl">Chính sách giá</span>
                <div class="rs-chips" id="rsPolicy" role="button" tabindex="0" aria-haspopup="listbox" aria-expanded="false" aria-label="Chính sách giá"></div>
              </div>
            </div>

            <div class="rs-frow">
              <div class="rs-f rs-c2"><button class="tl-btn-primary" type="button" id="rsSearch">Tìm kiếm</button></div>
            </div>
          </div>

          <div class="rs-body">
            <div class="rs-main">
              <div class="rs-mhead">
                <h3>Danh sách phòng trống</h3>
                <label class="rs-heat"><input type="checkbox" id="rsHeat" checked>
                  <span class="box" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span>
                  <span class="t">Bật bản đồ nhiệt</span>
                </label>
                <div class="rs-seg" role="group" aria-label="Kiểu hiển thị giá">
                  <button type="button" data-rsprice="total" aria-pressed="true">Tổng giá</button>
                  <button type="button" data-rsprice="avg" aria-pressed="false">Trung bình giá</button>
                </div>
              </div>
              <div class="rs-tblwrap"><table class="rs-tbl" id="rsTbl"></table></div>
              <p class="rs-note"><b>*</b> Phòng thẳng chặng: Là phòng khách có thể ở qua đêm liên tục mà không phải chuyển phòng.</p>
            </div>

            <aside class="rs-cart" aria-label="Giỏ hàng">
              <div class="rs-carth">
                <h3 id="rsCartTitle">Giỏ hàng (0)</h3>
                <button class="rs-clear" type="button" id="rsClearAll" hidden>
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M4 7h16M10 11v6M14 11v6M6 7l1 13h10l1-13M9 7V4h6v3"/></svg>
                  Xoá tất cả
                </button>
              </div>
              <div id="rsCartBody"></div>
              <div class="rs-total" id="rsCartTotal" hidden></div>
            </aside>
          </div>

          <footer class="rs-foot">
            <span class="sp"></span>
            <button class="tl-btn-ghost" type="button" data-rsact="cancel">Huỷ</button>
            <button class="tl-btn-primary" type="button" id="rsNext1" data-rsact="next1" disabled>
              Tiếp tục
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
            </button>
          </footer>
        </div>

        <!-- ================= BƯỚC 2 ================= -->
        <div class="rs-pane" id="rsP2" hidden>
          <div class="rs-body">
            <div class="rs-main">
              <div class="rs-cols">
                <section class="rs-card">
                  <h4>Thông tin đặt phòng</h4>
                  <div class="rs-grid">
                    <div class="rs-f"><span class="lbl">Khách sạn</span><div class="rs-ro" id="rsHotel">—</div></div>
                    <div class="rs-f"><span class="lbl">Tên đoàn</span><input type="text" id="rsGroup" maxlength="80" placeholder="Nhập tên đoàn"></div>
                    <div class="rs-f"><span class="lbl">Kênh bán</span><div class="rs-ro" id="rsRoChannel">—</div></div>
                    <div class="rs-f"><span class="lbl">Nguồn</span><div class="rs-ro" id="rsRoSource">—</div></div>
                    <div class="rs-f"><span class="lbl">Công ty</span><div class="rs-ro" id="rsRoCompany">—</div></div>
                    <div class="rs-f"><span class="lbl">Thị trường</span>
                      <div class="qb-vs" data-qbsel="rsMarket" data-qblabel="Thị trường"></div>
                    </div>
                    <div class="rs-f"><span class="lbl">Ngày đến dự kiến</span><div class="rs-ro" id="rsRoIn">—</div></div>
                    <div class="rs-f"><span class="lbl">Ngày đi dự kiến</span><div class="rs-ro" id="rsRoOut">—</div></div>
                    <div class="rs-f"><span class="lbl">Trạng thái booking</span>
                      <div class="qb-vs" data-qbsel="rsStatus" data-qblabel="Trạng thái booking"></div>
                    </div>
                    <div class="rs-f"><span class="lbl">Ngày nhả phòng</span>
                      <div class="qb-vs" data-qbsel="rsRelease" data-qblabel="Ngày nhả phòng"></div>
                    </div>
                    <div class="rs-f"><span class="lbl">Số ngày trước ngày đến dự kiến</span><input type="number" id="rsRelDays" min="0" max="60" value="3"></div>
                    <div class="rs-f"><span class="lbl">Người tạo</span>
                      <div class="qb-vs" data-qbsel="rsStaff" data-qblabel="Người tạo"></div>
                    </div>
                  </div>
                  <div class="rs-grid one">
                    <div class="rs-f"><span class="lbl">Ghi chú</span><input type="text" id="rsNote" maxlength="200" placeholder="Nhập ghi chú"></div>
                    <div class="rs-f"><span class="lbl">Ghi chú nội bộ</span><input type="text" id="rsINote" maxlength="200" placeholder="Nhập ghi chú nội bộ"></div>
                  </div>
                </section>

                <section class="rs-card">
                  <h4>Thông tin khách hàng</h4>
                  <div class="rs-grid">
                    <div class="rs-f"><span class="lbl">Danh xưng</span>
                      <div class="qb-vs" data-qbsel="rsSalut" data-qblabel="Danh xưng"></div>
                    </div>
                    <div class="rs-f"><span class="lbl">Họ</span><input type="text" id="rsLast" maxlength="50" placeholder="Nhập họ"></div>
                    <div class="rs-f"><span class="lbl">Tên<span class="req">*</span></span><input type="text" id="rsFirst" maxlength="50" placeholder="Nhập tên"></div>
                    <div class="rs-f"><span class="lbl">Ngày sinh</span>
                      <button class="rs-dt" type="button" id="rsDob" data-rsdate="dob"><span class="v">—</span><span class="ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span></button>
                    </div>
                    <div class="rs-f"><span class="lbl">Giới tính</span>
                      <div class="qb-vs" data-qbsel="rsGender" data-qblabel="Giới tính"></div>
                    </div>
                    <div class="rs-f"><span class="lbl">Quốc tịch</span>
                      <div class="qb-vs" data-qbsel="rsNation" data-qblabel="Quốc tịch"></div>
                    </div>
                    <div class="rs-f"><span class="lbl">Số điện thoại<span class="req">*</span></span><input type="text" id="rsPhone" maxlength="20" placeholder="Nhập số điện thoại"></div>
                    <div class="rs-f"><span class="lbl">Email</span><input type="text" id="rsEmail" maxlength="80" placeholder="Nhập email"></div>
                    <div class="rs-f"><span class="lbl">Loại giấy tờ</span>
                      <div class="qb-vs" data-qbsel="rsDocType" data-qblabel="Loại giấy tờ"></div>
                    </div>
                    <div class="rs-f"><span class="lbl">Số giấy tờ</span><input type="text" id="rsDocNo" maxlength="20" placeholder="Nhập số giấy tờ"></div>
                    <div class="rs-f"><span class="lbl">Ngày hết hạn</span>
                      <button class="rs-dt" type="button" id="rsDocExp" data-rsdate="docexp"><span class="v">—</span><span class="ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span></button>
                    </div>
                  </div>
                  <h4>Khách gợi ý</h4>
                  <div class="rs-tblwrap"><table class="rs-tbl" id="rsSugg"></table></div>
                </section>
              </div>
            </div>

            <aside class="rs-cart" aria-label="Giỏ hàng">
              <h3 id="rsCartTitle2">Giỏ hàng (0)</h3>
              <div id="rsCartBody2"></div>
              <div class="rs-total" id="rsCartTotal2" hidden></div>
            </aside>
          </div>

          <footer class="rs-foot">
            <button class="tl-btn-outline" type="button" data-rsact="back2">Quay lại</button>
            <span class="sp"></span>
            <button class="tl-btn-ghost" type="button" data-rsact="cancel">Huỷ</button>
            <button class="tl-btn-outline" type="button" data-rsact="paylater">Đặt phòng và thanh toán sau</button>
            <button class="tl-btn-primary" type="button" data-rsact="next2">Tiến hành thanh toán</button>
          </footer>
        </div>

        <!-- ================= BƯỚC 3 ================= -->
        <div class="rs-pane" id="rsP3" hidden>
          <div class="rs-body">
            <div class="rs-main">
              <div class="rs-cols">
                <section class="rs-card rs-pay">
                  <h4>Thông tin thanh toán</h4>
                  <div class="rs-f"><span class="lbl">Chính sách giảm giá</span>
                    <div class="qb-vs" data-qbsel="rsPromo" data-qblabel="Chính sách giảm giá"></div>
                  </div>
                  <div class="r"><span>Tổng</span><span class="v" id="rsPTotal">0</span></div>
                  <div class="r"><span>Giảm giá</span><span class="v" id="rsPDisc">0</span></div>
                  <div class="r"><span>Phụ thu</span><span class="v" id="rsPSur">0</span></div>
                  <div class="r"><span>Thuế</span><span class="v" id="rsPTax">0</span></div>
                  <div class="r due"><span>Còn lại</span><span class="v" id="rsPDue">0</span></div>
                </section>

                <section class="rs-card">
                  <h4>Thanh toán</h4>
                  <div class="rs-methods" id="rsMethods" role="group" aria-label="Các lựa chọn thanh toán">
                    <button class="rs-method" type="button" data-rspm="Tiền mặt" aria-pressed="true">Tiền mặt</button>
                    <button class="rs-method" type="button" data-rspm="Chuyển khoản" aria-pressed="false">Chuyển khoản</button>
                    <button class="rs-method" type="button" data-rspm="Thẻ" aria-pressed="false">Thẻ</button>
                    <button class="rs-method" type="button" data-rspm="Mã QR" aria-pressed="false">Mã QR</button>
                  </div>
                  <div class="rs-grid">
                    <div class="rs-f"><span class="lbl">Hình thức thanh toán</span>
                      <div class="qb-vs" data-qbsel="rsPayType" data-qblabel="Hình thức thanh toán"></div>
                    </div>
                    <div class="rs-f"><span class="lbl">Tiền tệ</span>
                      <div class="qb-vs" data-qbsel="rsCurrency" data-qblabel="Tiền tệ"></div>
                    </div>
                    <div class="rs-f"><span class="lbl">Số tiền</span><input type="text" id="rsPayAmt" maxlength="15" placeholder="0"></div>
                    <div class="rs-f"><span class="lbl">Ghi chú</span><input type="text" id="rsPayNote" maxlength="120" placeholder="Nhập ghi chú"></div>
                  </div>
                </section>
              </div>
            </div>

            <aside class="rs-cart" aria-label="Giỏ hàng">
              <h3 id="rsCartTitle3">Giỏ hàng (0)</h3>
              <div id="rsCartBody3"></div>
              <div class="rs-total" id="rsCartTotal3" hidden></div>
            </aside>
          </div>

          <footer class="rs-foot">
            <button class="tl-btn-outline" type="button" data-rsact="back3">Quay lại</button>
            <span class="sp"></span>
            <button class="tl-btn-ghost" type="button" data-rsact="cancel">Huỷ</button>
            <button class="tl-btn-primary" type="button" data-rsact="finish">Hoàn tất</button>
          </footer>
        </div>
      </section>

<div class="rs-msmenu" id="rsMsMenu" role="listbox" aria-label="Chọn loại phòng"></div>

      <!-- ============ KIỂM TOÁN ĐÊM (FD01) — Figma 43747:47069 ============ -->
      <section class="card na" id="naCard" hidden aria-label="Kiểm toán đêm">

        <!-- ---------- Thanh đầu ---------- -->
        <div class="na-top">
          <span class="na-bd-lb">Ngày kinh doanh</span>
          <span class="na-bd-val" id="naBizDate">—</span>
          <button class="na-edit" type="button" id="naEditDate" aria-label="Chỉnh sửa ngày kinh doanh">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 20h9"/><path d="M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4z"/></svg>
          </button>
          <span class="sp"></span>
          <button class="tl-btn-primary" type="button" id="naRun">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 4l14 8-14 8z"/></svg>
            Thực hiện kiểm toán đêm
          </button>
          <button class="tl-btn-outline na-ib" type="button" id="naCfgBtn" aria-label="Cấu hình kiểm toán đêm">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="3.2"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 1 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.6 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 1 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.6a1.65 1.65 0 0 0 1-1.51V3a2 2 0 1 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9v0a1.65 1.65 0 0 0 1.51 1H21a2 2 0 1 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
          </button>
        </div>

        <!-- ---------- Quy trình ---------- -->
        <div class="na-card" id="naProc">
          <div class="na-cardhead">
            <h2 id="naProcTitle">Quy trình kiểm toán đêm</h2>
            <span class="na-chip" id="naChip">Chưa thực hiện</span>
          </div>
          <div class="na-steps" id="naSteps"></div>
          <div class="na-warn" id="naWarn" hidden>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><path d="M12 9v4M12 17h.01"/></svg>
            Vui lòng không thoát trang khi hệ thống đang thực hiện kiểm toán đêm để tránh làm sai lệch dữ liệu!
          </div>
        </div>

        <!-- ---------- Cấu hình kiểm toán đêm ---------- -->
        <div class="na-card na-cfg" id="naCfgPane" hidden>
          <div class="na-cardhead">
            <h2>Cấu hình kiểm toán đêm</h2>
          </div>

          <div class="na-cfgrow">
            <label class="na-sw"><input type="checkbox" id="naAuto">
              <span class="tr" aria-hidden="true"></span>
              <span class="k">Tự động kiểm toán đêm</span></label>
          </div>

          <div class="na-times" id="naTimes">
            <label class="na-f">
              <span class="k">Thời điểm thực hiện kiểm toán<i>*</i></span>
              <input type="time" id="naTimeRun" value="04:00">
            </label>
            <label class="na-f">
              <span class="k">Thời điểm hiển thị cảnh báo<i>*</i></span>
              <input type="time" id="naTimeWarn" value="06:00">
            </label>
          </div>

          <div>
            <div class="na-cardhead" style="margin-bottom:12px"><h2>Bước thực hiện</h2></div>
            <div class="na-cfglist" id="naCfgList"></div>
          </div>

          <div class="na-cfgfoot">
            <button class="tl-btn-ghost" type="button" id="naCfgBack">Quay lại</button>
            <span class="sp"></span>
            <button class="tl-btn-outline" type="button" id="naCfgReset">Đặt lại</button>
            <button class="tl-btn-primary" type="button" id="naCfgSave">Lưu</button>
          </div>
        </div>

      </section>

    </main>
  </div>
</div>

<!-- ===== Quick Setup: backdrop + panel ===== -->
<div class="qs-backdrop" id="qsBackdrop" hidden></div>
<section class="qs-panel" id="qsPanel" data-mode="popover" role="dialog" aria-modal="false"
         aria-labelledby="qsTitle" hidden>
  <header class="qs-head">
    <div class="qs-head-main">
      <h2 class="qs-title" id="qsTitle">Hoàn thành các thông tin bắt buộc</h2>
      <p class="qs-sub"><span>Đã hoàn thành <b id="qsHeadCount">0/0</b> bước thiết lập</span></p>
      <span class="qs-head-track" aria-hidden="true"><span class="qs-fill" data-qs-fill></span></span>
    </div>
    <button class="qs-close" type="button" id="qsClose" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M18 6l-12 12"/><path d="M6 6l12 12"/>
      </svg>
    </button>
  </header>

  <div class="qs-body" id="qsBody"><!-- render từ JS --></div>

  <footer class="qs-foot">
    <span class="qs-foot-note">Còn <b id="qsRemain">0</b> bước để sẵn sàng vận hành</span>
  </footer>
</section>

<!-- ===== Dialog chọn module — Figma "EzOne Products" (110:4547) ===== -->
<div class="prod-backdrop" id="prodBackdrop"></div>
<div class="prod-pop" id="prodPop" role="dialog" aria-modal="false" aria-labelledby="prodTitle">
  <div class="prod-head">
    <h2 class="prod-title" id="prodTitle">Hệ sinh thái ezOne</h2>
    <div class="prod-search">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M10 10m-7 0a7 7 0 1 0 14 0a7 7 0 1 0 -14 0"/><path d="M21 21l-6 -6"/>
      </svg>
      <input id="prodSearch" type="text" placeholder="Nhập tên sản phẩm" autocomplete="off" aria-label="Tìm sản phẩm">
    </div>
    <button class="prod-close" type="button" id="prodClose" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"><path d="M18 6l-12 12"/><path d="M6 6l12 12"/></svg>
    </button>
  </div>
  <div class="prod-body" id="prodBody"><!-- render từ JS --></div>
</div>

<script>
/* ===== Icon set (Tabler, inline SVG) ===== */
const ICONS = {
  'layout-grid': '<rect x="4" y="4" width="7" height="7" rx="1"/><rect x="13" y="4" width="7" height="7" rx="1"/><rect x="4" y="13" width="7" height="7" rx="1"/><rect x="13" y="13" width="7" height="7" rx="1"/>',
  'reservation': '<path d="M12 3.2a2.2 2.2 0 100 4.4a2.2 2.2 0 000 -4.4"/><path d="M12 7.6v3.2"/><path d="M6.5 10.8h11a1.5 1.5 0 011.5 1.5v1.2h-14v-1.2a1.5 1.5 0 011.5 -1.5"/><path d="M4 14.5h16v3.5h-16z"/><path d="M15.5 20.3h1.2M18 20.3h1.2"/>',
  'room-map': '<rect x="3" y="4" width="18" height="16" rx="2"/><path d="M3 9h18M9 9v11"/><path d="M12.5 13.5h5M12.5 16.5h3"/>',
  'calendar-plus': '<rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/><path d="M12 13v5M9.5 15.5h5"/>',
  'list-check': '<rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/><path d="M7.5 14h9M7.5 17h5"/>',
  'user-circle': '<circle cx="12" cy="12" r="9"/><circle cx="12" cy="10" r="2.6"/><path d="M6.6 18.4a6 6 0 0110.8 0"/>',
  'smart-home': '<path d="M5 12l-2 0l9 -9l9 9l-2 0"/><path d="M5 12v7a2 2 0 0 0 2 2h10a2 2 0 0 0 2 -2v-7"/><path d="M9 21v-6a2 2 0 0 1 2 -2h2a2 2 0 0 1 2 2v6"/>',
  'calendar-event': '<path d="M4 5m0 2a2 2 0 0 1 2 -2h12a2 2 0 0 1 2 2v12a2 2 0 0 1 -2 2h-12a2 2 0 0 1 -2 -2z"/><path d="M16 3v4"/><path d="M8 3v4"/><path d="M4 11h16"/><path d="M8 15h2v2h-2z"/>',
  'door-enter': '<path d="M13 12v.01"/><path d="M3 21h18"/><path d="M5 21v-16a2 2 0 0 1 2 -2h7.5"/><path d="M17 13.5v7.5"/><path d="M14 7h7"/><path d="M18 4l3 3l-3 3"/>',
  'bed': '<path d="M3 7v11"/><path d="M3 14h18"/><path d="M21 18v-8a2 2 0 0 0 -2 -2h-8v6"/><path d="M7 10m-1 0a1 1 0 1 0 2 0a1 1 0 1 0 -2 0"/>',
  'package': '<path d="M12 3l8 4.5v9l-8 4.5l-8 -4.5v-9l8 -4.5"/><path d="M12 12l8 -4.5"/><path d="M12 12v9"/><path d="M12 12l-8 -4.5"/>',
  'cash': '<path d="M7 9m0 2a2 2 0 0 1 2 -2h10a2 2 0 0 1 2 2v4a2 2 0 0 1 -2 2h-10a2 2 0 0 1 -2 -2z"/><path d="M14 13m-2 0a2 2 0 1 0 4 0a2 2 0 1 0 -4 0"/><path d="M17 9v-2a2 2 0 0 0 -2 -2h-10a2 2 0 0 0 -2 2v6a2 2 0 0 0 2 2h2"/>',
  'moon': '<path d="M12 3c.132 0 .263 0 .393 0a7.5 7.5 0 0 0 7.92 12.446a9 9 0 1 1 -8.313 -12.454z"/>',
  'chart-bar': '<path d="M3 13m0 1a1 1 0 0 1 1 -1h4a1 1 0 0 1 1 1v5a1 1 0 0 1 -1 1h-4a1 1 0 0 1 -1 -1z"/><path d="M9 9m0 1a1 1 0 0 1 1 -1h4a1 1 0 0 1 1 1v9a1 1 0 0 1 -1 1h-4a1 1 0 0 1 -1 -1z"/><path d="M15 5m0 1a1 1 0 0 1 1 -1h4a1 1 0 0 1 1 1v13a1 1 0 0 1 -1 1h-4a1 1 0 0 1 -1 -1z"/><path d="M4 20h14"/>',
  'settings': '<path d="M10.325 4.317c.426 -1.756 2.924 -1.756 3.35 0a1.724 1.724 0 0 0 2.573 1.066c1.543 -.94 3.31 .826 2.37 2.37a1.724 1.724 0 0 0 1.065 2.572c1.756 .426 1.756 2.924 0 3.35a1.724 1.724 0 0 0 -1.066 2.573c.94 1.543 -.826 3.31 -2.37 2.37a1.724 1.724 0 0 0 -2.572 1.065c-.426 1.756 -2.924 1.756 -3.35 0a1.724 1.724 0 0 0 -2.573 -1.066c-1.543 .94 -3.31 -.826 -2.37 -2.37a1.724 1.724 0 0 0 -1.065 -2.572c-1.756 -.426 -1.756 -2.924 0 -3.35a1.724 1.724 0 0 0 1.066 -2.573c-.94 -1.543 .826 -3.31 2.37 -2.37c1 .608 2.296 .07 2.572 -1.065z"/><path d="M12 12m-3 0a3 3 0 1 0 6 0a3 3 0 1 0 -6 0"/>'
};
const CHEVRON = '<path d="M6 9l6 6l6 -6"/>';
const BULLET  = '<circle cx="12" cy="12" r="5"/>';

/* ===== Dữ liệu menu ezOne.PMS ===== */
const MENU = [
  { type:'single', key:'dashboard', icon:'layout-grid', label:'Dashboard' },
  { type:'group', key:'booking', icon:'reservation', label:'Quản lý đặt phòng',
    children:[
      { key:'bk-map',  icon:'room-map',      label:'Sơ đồ phòng' },
      { key:'bk-new',  icon:'calendar-plus', label:'Tạo đặt phòng' },
      { key:'bk-list', icon:'list-check',    label:'Danh sách đặt phòng' }
    ] },
  { type:'single', key:'night-audit', icon:'moon', label:'Kiểm toán đêm' },
  { type:'single', key:'guest', icon:'user-circle', label:'Quản lý khách hàng' },
  { type:'single', key:'report', icon:'chart-bar', label:'Báo cáo' },
  { type:'group', key:'config', icon:'settings', label:'Cấu hình', dot:true,
    children:[
      { key:'cfg-new',    label:'Tạo đặt phòng' },
      { key:'cfg-map',    label:'Sơ đồ phòng' },
      { key:'cfg-detail', label:'Chi tiết đặt phòng' },
      { key:'cfg-guest',  label:'Hồ sơ khách hàng' },
      { key:'cfg-audit',  label:'Kiểm toán đêm' }
    ] }
];
const DEFAULT_SELECTED = 'bk-map';

/* ===== Render menu ===== */
function svgWrap(inner, cls){
  return '<svg class="'+cls+'" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">'+inner+'</svg>';
}
function renderMenu(){
  const root = document.getElementById('pagesScroll');
  let html = '';
  MENU.forEach(function(item){
    if (item.type === 'single'){
      html += '<button class="nav-item" type="button" data-nav="'+item.key+'">'
            +   svgWrap(ICONS[item.icon],'nav-icon')
            +   '<span class="nav-label">'+item.label+'</span>'
            + '</button>';
    } else {
      html += '<div class="nav-group is-closed">'
            +   '<div class="nav-item" role="button" tabindex="0">'
            +     svgWrap(ICONS[item.icon],'nav-icon')
            +     '<span class="nav-label">'+item.label+'</span>'
            +     '<button class="nav-caret" type="button" aria-expanded="false" aria-label="Mở rộng '+item.label+'">'
            +       '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">'+CHEVRON+'</svg>'
            +     '</button>'
            +   '</div>'
            +   '<div class="nav-children">';
      item.children.forEach(function(child){
        html += '<div class="nav-child'+(item.dot?' has-dot':'')+'" role="button" tabindex="0"'
              +   ' data-nav="'+child.key+'" data-navgroup="'+item.key+'">'
              +   (item.dot
                   ? '<svg class="nav-icon nav-dot" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="12" cy="12" r="3.2"/></svg>'
                   : svgWrap(ICONS[child.icon],'nav-icon'))
              +   '<span class="nav-label">'+child.label+'</span>'
              + '</div>';
      });
      html += '</div></div>';
    }
  });
  root.innerHTML = html;

  /* Mở nhóm chứa mục được chọn mặc định */
  Array.prototype.forEach.call(root.querySelectorAll('.nav-child'), function(el){
    if (el.getAttribute('data-nav') === DEFAULT_SELECTED){
      selectChild(el);
      const grp = el.closest('.nav-group');
      grp.classList.remove('is-closed');
      grp.querySelector('.nav-caret').setAttribute('aria-expanded','true');
    }
  });
}

function selectChild(el){
  const prev = document.querySelector('.nav-child.is-selected');
  if (prev){ prev.classList.remove('is-selected'); prev.removeAttribute('aria-current'); }
  el.classList.add('is-selected');
  el.setAttribute('aria-current','page');
}

/* ===== Hành vi ===== */
const app = document.getElementById('app');
const pinBtn = document.getElementById('pinBtn');
const logoBtn = document.getElementById('logoBtn');

pinBtn.addEventListener('click', function(){
  const collapsed = app.classList.toggle('is-collapsed');
  pinBtn.setAttribute('aria-label', collapsed ? 'Bung menu' : 'Thu gọn menu');
});

logoBtn.addEventListener('click', function(){
  if (app.classList.contains('is-collapsed')){
    app.classList.remove('is-collapsed');
    pinBtn.setAttribute('aria-label','Thu gọn menu');
  }
});

document.getElementById('pagesScroll').addEventListener('click', function(e){
  const caret = e.target.closest('.nav-caret');
  if (caret){
    e.stopPropagation();
    if (app.classList.contains('is-collapsed')) return;
    const grp = caret.closest('.nav-group');
    const closed = grp.classList.toggle('is-closed');
    caret.setAttribute('aria-expanded', closed ? 'false' : 'true');
    return;
  }
  const parent = e.target.closest('.nav-group > .nav-item');
  if (parent){
    if (app.classList.contains('is-collapsed')) return;
    const grp = parent.closest('.nav-group');
    const closed = grp.classList.toggle('is-closed');
    grp.querySelector('.nav-caret').setAttribute('aria-expanded', closed ? 'false' : 'true');
    return;
  }
  const child = e.target.closest('.nav-child');
  if (child) selectChild(child);
});

document.getElementById('pagesScroll').addEventListener('keydown', function(e){
  if (e.key !== 'Enter' && e.key !== ' ') return;
  const child = e.target.closest('.nav-child');
  if (child){ e.preventDefault(); selectChild(child); return; }
  const parent = e.target.closest('.nav-group > .nav-item');
  if (parent){
    e.preventDefault();
    const grp = parent.closest('.nav-group');
    const closed = grp.classList.toggle('is-closed');
    grp.querySelector('.nav-caret').setAttribute('aria-expanded', closed ? 'false' : 'true');
  }
});

renderMenu();

/* ============================================================
   QUICK SETUP — thanh tiến độ thiết lập khách sạn + panel
   Port từ EzOne_Admin_QuickSetup_v8.html (giữ PA1 "pill tròn",
   bỏ bộ chuyển phương án chỉ dùng cho demo).
   ============================================================ */
(function(){
  'use strict';
  /* ---------- DỮ LIỆU MẪU — sửa ở đây khi nối API/ezOne.Ims ---------- */
  var QS_DATA = [
    {
      name:'1. Khai báo dữ liệu chung',
      steps:[
        { name:'Thông tin chung', cond:'Thông tin này xuất hiện trên hóa đơn, booking confirmation và kênh bán của khách sạn', done:true },
        { name:'Thuế, phí',       cond:'Các loại thuế, phí mà khách sạn sử dụng', done:true },
        { name:'Mã giao dịch',    cond:'Mã giao dịch của khách sạn',              done:true },
        { name:'Loại phòng',      cond:'Các loại phòng mà khách sạn sử dụng',     done:true },
        { name:'Sản phẩm',        cond:'Sản phẩm và dịch vụ của khách sạn',       done:true }
      ]
    },
    {
      name:'2. Thông tin giá bán cho khách sạn',
      steps:[
        { name:'Cấu hình bán',           cond:'Cấu hình các loại giá và quy tắc bán phòng cho hệ thống.', done:false },
        { name:'Chính sách giá',         cond:'Thiết lập các thông tin về bán giá cho khách sạn của bạn', done:false },
        { name:'Công thức giá',          cond:'Cấu hình các loại giá và quy tắc bán phòng cho hệ thống.', done:false },
        { name:'Chính sách giá dịch vụ', cond:'Thiết lập chính sách giá cho các dịch vụ của khách sạn',   done:false }
      ]
    },
    {
      name:'3. Thông tin OTA',
      steps:[
        { name:'Kết nối OTA', cond:'Tạo kết nối để khách sạn của bạn hiển thị trên các kênh OTA', done:false }
      ]
    }
  ];

  var qs        = document.getElementById('qs');
  var trigger   = document.getElementById('qsTrigger');
  var panel     = document.getElementById('qsPanel');
  var bodyEl    = document.getElementById('qsBody');
  var closeBtn  = document.getElementById('qsClose');
  var ring      = document.getElementById('qsRing');
  var ringPct   = document.getElementById('qsRingPct');
  var countPill = document.getElementById('qsCountPill');
  var headCount = document.getElementById('qsHeadCount');
  var remainEl  = document.getElementById('qsRemain');
  var RING_LEN  = 2 * Math.PI * 15.5;   /* r = 15.5 */
  var isOpen    = false;

  function totals(){
    var a = 0, b = 0;
    QS_DATA.forEach(function(g){ g.steps.forEach(function(s){ b++; if (s.done) a++; }); });
    return { a:a, b:b, pct: b ? Math.round((a/b)*100) : 0 };
  }

  function renderProgress(){
    var t = totals();
    var text = t.a + '/' + t.b;
    countPill.textContent = text;
    headCount.textContent = text;
    remainEl.textContent = String(t.b - t.a);

    ring.setAttribute('stroke-dasharray', RING_LEN.toFixed(2));
    ring.setAttribute('stroke-dashoffset', (RING_LEN * (1 - t.pct/100)).toFixed(2));
    ringPct.textContent = t.pct + '%';

    Array.prototype.forEach.call(document.querySelectorAll('[data-qs-fill]'), function(el){
      el.style.width = t.pct + '%';
    });

    trigger.setAttribute('aria-label',
      'Thiết lập khách sạn — đã hoàn thành ' + text + ' bước (' + t.pct + '%)');
  }

  var ICON_CARET = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6l6 -6"/></svg>';
  var ICON_TICK  = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12l5 5l9 -9"/></svg>';
  var ICON_GO    = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6l-6 6"/></svg>';

  function renderGroups(){
    bodyEl.innerHTML = '';

    QS_DATA.forEach(function(g, gi){
      var x = g.steps.filter(function(s){ return s.done; }).length;
      var y = g.steps.length;
      var groupDone = x === y;

      var group = document.createElement('div');
      group.className = 'qs-group' + (groupDone ? ' is-done' : '') + (gi === 0 ? '' : ' is-closed');

      var head = document.createElement('button');
      head.type = 'button';
      head.className = 'qs-g-head';
      head.setAttribute('aria-expanded', String(gi === 0));
      head.innerHTML =
        '<span class="qs-g-caret" aria-hidden="true">' + ICON_CARET + '</span>' +
        '<span class="qs-g-name"></span>' +
        '<span class="qs-g-count">Đã hoàn thành: <b>' + x + '/' + y + '</b></span>';
      head.querySelector('.qs-g-name').textContent = g.name;
      head.addEventListener('click', function(){
        var closed = group.classList.toggle('is-closed');
        head.setAttribute('aria-expanded', String(!closed));
      });
      group.appendChild(head);

      var gBody = document.createElement('div');
      gBody.className = 'qs-g-body';

      g.steps.forEach(function(s, si){
        var step = document.createElement('div');
        step.className = 'qs-step' + (s.done ? ' is-done' : '');
        step.setAttribute('role','button');
        step.setAttribute('tabindex','0');
        /* Bước đã hoàn thành: không hiển thị dòng điều kiện hoàn thành */
        step.innerHTML =
          '<span class="qs-mark" aria-hidden="true">' + (s.done ? ICON_TICK : (si + 1)) + '</span>' +
          '<span class="qs-s-main">' +
            '<span class="qs-s-name"></span>' +
            (s.done ? '' : '<span class="qs-s-cond"></span>') +
          '</span>' +
          '<button class="qs-go" type="button" tabindex="-1"></button>';
        step.querySelector('.qs-s-name').textContent = s.name;
        if (!s.done) step.querySelector('.qs-s-cond').textContent = s.cond;
        step.setAttribute('aria-label', 'Đi đến màn hình khai báo: ' + s.name);

        var go = step.querySelector('.qs-go');
        go.innerHTML = ICON_GO;
        go.setAttribute('aria-label', 'Đi đến màn hình khai báo: ' + s.name);
        go.addEventListener('click', function(e){ e.stopPropagation(); step.click(); });

        step.addEventListener('click', function(){
          console.log('[Quick Setup] Điều hướng đến màn hình khai báo:', s.name);
        });
        step.addEventListener('keydown', function(e){
          if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); step.click(); }
        });

        gBody.appendChild(step);
      });

      group.appendChild(gBody);
      bodyEl.appendChild(group);
    });
  }

  function update(){ renderGroups(); renderProgress(); }

  /* API cập nhật trạng thái bước từ bên ngoài: qsSetStepDone(nhóm, bước, true/false) */
  window.qsSetStepDone = function(groupIndex, stepIndex, done){
    var g = QS_DATA[groupIndex];
    if (!g || !g.steps[stepIndex]) return false;
    g.steps[stepIndex].done = !!done;
    update();
    return true;
  };

  function positionPopover(){
    var r = trigger.getBoundingClientRect();
    var w = panel.offsetWidth;
    var left = Math.min(r.right - w, window.innerWidth - w - 8);
    panel.style.left = Math.max(8, left) + 'px';
    panel.style.top = (r.bottom + 8) + 'px';
    panel.style.right = 'auto';
    panel.style.bottom = 'auto';
  }

  function openPanel(){
    if (isOpen) return;
    isOpen = true;
    panel.hidden = false;
    panel.classList.add('is-open');
    trigger.classList.add('is-open');
    trigger.setAttribute('aria-expanded','true');
    positionPopover();
    closeBtn.focus();
  }

  function closePanel(){
    if (!isOpen) return;
    isOpen = false;
    panel.classList.remove('is-open');
    panel.hidden = true;
    trigger.classList.remove('is-open');
    trigger.setAttribute('aria-expanded','false');
    trigger.focus();
  }

  trigger.addEventListener('click', function(){ isOpen ? closePanel() : openPanel(); });
  closeBtn.addEventListener('click', closePanel);

  document.addEventListener('keydown', function(e){
    if (e.key === 'Escape' && isOpen) closePanel();
  });
  document.addEventListener('mousedown', function(e){
    if (!isOpen) return;
    if (panel.contains(e.target) || qs.contains(e.target)) return;
    closePanel();
  });
  window.addEventListener('resize', function(){ if (isOpen) positionPopover(); });
  window.addEventListener('scroll', function(){ if (isOpen) positionPopover(); }, true);

  update();
})();

/* ============================================================
   DIALOG CHỌN MODULE — popover neo dưới nút Ứng dụng
   ============================================================ */
(function(){
  'use strict';
  var $ = function(id){ return document.getElementById(id); };
  var esc = function(s){
    return String(s == null ? '' : s).replace(/[&<>"']/g, function(c){
      return ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[c];
    });
  };

  var CURRENT = 'pms';   /* module đang mở — không điều hướng lại chính nó */

  var LOGO_PMS = '<svg aria-hidden="true" viewBox="0 0 342 342" fill="none" xmlns="http://www.w3.org/2000/svg"> <path d="M170.879 315.47C250.734 315.47 315.469 250.735 315.469 170.88C315.469 91.0252 250.734 26.29 170.879 26.29C91.0242 26.29 26.2891 91.0252 26.2891 170.88C26.2891 250.735 91.0242 315.47 170.879 315.47Z" fill="#345B82"/> <path d="M206.148 107.14H135.608C126.915 107.14 119.868 114.184 119.868 122.875C119.868 131.565 126.915 138.61 135.608 138.61H206.148C214.841 138.61 221.888 131.565 221.888 122.875C221.888 114.184 214.841 107.14 206.148 107.14Z" fill="#8A9BB2"/> <path d="M220.098 156.65H121.648C112.955 156.65 105.908 163.694 105.908 172.385C105.908 181.075 112.955 188.12 121.648 188.12H220.098C228.791 188.12 235.838 181.075 235.838 172.385C235.838 163.694 228.791 156.65 220.098 156.65Z" fill="#BAC4D1"/> <path d="M233.278 206.17H108.478C99.7853 206.17 92.7383 213.214 92.7383 221.905C92.7383 230.595 99.7853 237.64 108.478 237.64H233.278C241.971 237.64 249.018 230.595 249.018 221.905C249.018 213.214 241.971 206.17 233.278 206.17Z" fill="white"/> </svg>';
  var LOGO_CRS = '<svg aria-hidden="true" viewBox="0 0 342 342" fill="none" xmlns="http://www.w3.org/2000/svg"> <path d="M170.88 318.09C252.32 318.09 318.34 252.07 318.34 170.63C318.34 89.19 252.32 23.1699 170.88 23.1699C89.44 23.1699 23.4199 89.19 23.4199 170.63C23.4199 252.07 89.44 318.09 170.88 318.09Z" fill="#00A8DA"/> <path d="M170.88 206.56C190.724 206.56 206.81 190.473 206.81 170.63C206.81 150.786 190.724 134.7 170.88 134.7C151.036 134.7 134.95 150.786 134.95 170.63C134.95 190.473 151.036 206.56 170.88 206.56Z" fill="white"/> <g opacity="0.5"> <path d="M170.881 280.18C183.025 280.18 192.871 270.335 192.871 258.19C192.871 246.045 183.025 236.2 170.881 236.2C158.736 236.2 148.891 246.045 148.891 258.19C148.891 270.335 158.736 280.18 170.881 280.18Z" fill="white"/> </g> <g opacity="0.5"> <path d="M246.711 148.83C258.855 148.83 268.701 138.984 268.701 126.84C268.701 114.695 258.855 104.85 246.711 104.85C234.566 104.85 224.721 114.695 224.721 126.84C224.721 138.984 234.566 148.83 246.711 148.83Z" fill="white"/> </g> <g opacity="0.5"> <path d="M95.0408 148.83C107.186 148.83 117.031 138.984 117.031 126.84C117.031 114.695 107.186 104.85 95.0408 104.85C82.896 104.85 73.0508 114.695 73.0508 126.84C73.0508 138.984 82.896 148.83 95.0408 148.83Z" fill="white"/> </g> </svg>';
  var LOGO_MESS = '<svg aria-hidden="true" viewBox="0 0 342 342" fill="none" xmlns="http://www.w3.org/2000/svg"> <path d="M170.879 314.969C250.32 314.969 314.719 250.57 314.719 171.129C314.719 91.6884 250.32 27.2891 170.879 27.2891C91.4384 27.2891 27.0391 91.6884 27.0391 171.129C27.0391 250.57 91.4384 314.969 170.879 314.969Z" fill="#00AA74"/> <g opacity="0.7"> <path d="M167.739 104.13H129.329C107.492 104.13 89.7891 121.831 89.7891 143.665C89.7891 165.5 107.492 183.2 129.329 183.2H167.739C189.576 183.2 207.279 165.5 207.279 143.665C207.279 121.831 189.576 104.13 167.739 104.13Z" fill="white"/> </g> <path d="M212.419 148.36H174.009C152.172 148.36 134.469 166.061 134.469 187.895C134.469 209.73 152.172 227.43 174.009 227.43H212.419C234.256 227.43 251.959 209.73 251.959 187.895C251.959 166.061 234.256 148.36 212.419 148.36Z" fill="white"/> <g opacity="0.7"> <path d="M229.619 129.59C239.98 129.59 248.379 121.191 248.379 110.83C248.379 100.469 239.98 92.0703 229.619 92.0703C219.258 92.0703 210.859 100.469 210.859 110.83C210.859 121.191 219.258 129.59 229.619 129.59Z" fill="white"/> </g> </svg>';
  var LOGO_ADMIN = '<svg aria-hidden="true" viewBox="0 0 342 342" fill="none" xmlns="http://www.w3.org/2000/svg"> <path d="M170.88 318.48C252.259 318.48 318.23 252.509 318.23 171.13C318.23 89.7511 252.259 23.7803 170.88 23.7803C89.5009 23.7803 23.53 89.7511 23.53 171.13C23.53 252.509 89.5009 318.48 170.88 318.48Z" fill="#758D9F"/> <path d="M248.62 153.83C248.39 152.77 248.12 151.72 247.85 150.68C247.76 150.35 247.67 150.03 247.58 149.7C247.38 148.98 247.17 148.26 246.95 147.55C246.84 147.18 246.72 146.82 246.6 146.46C246.38 145.77 246.14 145.09 245.9 144.41C245.78 144.07 245.66 143.73 245.53 143.39C245.24 142.61 244.94 141.84 244.62 141.07C244.53 140.85 244.45 140.63 244.36 140.42C243.94 139.43 243.51 138.44 243.06 137.47C242.99 137.31 242.91 137.16 242.83 137C242.45 136.19 242.05 135.39 241.64 134.6C241.5 134.32 241.35 134.05 241.2 133.78C240.84 133.1 240.47 132.42 240.08 131.75C239.92 131.47 239.76 131.19 239.6 130.92C239.19 130.22 238.77 129.53 238.34 128.85C238.24 128.7 238.15 128.54 238.06 128.38L252.66 107.62L243.51 98.4699L234.36 89.3199L213.6 103.92C213.45 103.83 213.3 103.74 213.16 103.65C212.46 103.21 211.76 102.79 211.05 102.37C210.78 102.21 210.51 102.06 210.24 101.9C209.56 101.51 208.88 101.14 208.19 100.77C207.92 100.63 207.66 100.49 207.39 100.35C206.59 99.9399 205.78 99.5399 204.96 99.1499C204.81 99.0799 204.67 98.9999 204.52 98.9299C203.54 98.4699 202.56 98.0399 201.56 97.6199C201.35 97.5299 201.14 97.4499 200.93 97.3699C200.15 97.0499 199.37 96.7499 198.59 96.4499C198.25 96.3299 197.91 96.2099 197.58 96.0899C196.9 95.8499 196.22 95.6099 195.53 95.3899C195.17 95.2699 194.81 95.1499 194.44 95.0399C193.73 94.8199 193.01 94.6099 192.29 94.4099C191.96 94.3199 191.64 94.2199 191.31 94.1399C190.27 93.8699 189.23 93.6099 188.18 93.3699C188.18 93.3699 188.18 93.3699 188.17 93.3699L183.82 68.3799H157.94L153.59 93.3699C152.11 93.6999 150.65 94.0799 149.2 94.4899C141.67 96.6199 134.6 99.8099 128.15 103.91L107.39 89.3099L98.24 98.4599L89.09 107.61L103.69 128.37C103.34 128.92 103.02 129.48 102.68 130.03C101.97 131.21 101.28 132.41 100.63 133.63C100.51 133.85 100.38 134.07 100.26 134.3C100.24 134.34 100.22 134.38 100.2 134.42C99.6 135.57 99.04 136.74 98.49 137.93C96.18 142.97 94.36 148.28 93.14 153.82L68.15 158.17V171.11V184.05L93.14 188.4C93.37 189.46 93.64 190.51 93.91 191.55C94 191.88 94.09 192.2 94.18 192.53C94.38 193.25 94.59 193.97 94.81 194.68C94.92 195.05 95.04 195.41 95.16 195.77C95.38 196.46 95.62 197.14 95.86 197.82C95.98 198.16 96.1 198.5 96.23 198.84C96.52 199.62 96.82 200.39 97.14 201.16C97.23 201.38 97.31 201.6 97.4 201.81C97.82 202.8 98.25 203.79 98.71 204.76C98.78 204.92 98.86 205.07 98.94 205.23C99.32 206.04 99.72 206.84 100.13 207.63C100.27 207.9 100.42 208.18 100.56 208.45C100.92 209.13 101.3 209.81 101.68 210.48C101.84 210.76 102 211.03 102.16 211.31C102.57 212.01 102.99 212.7 103.42 213.39C103.52 213.54 103.6 213.7 103.7 213.85L89.1 234.61L98.25 243.76L107.4 252.91L128.16 238.31C128.3 238.4 128.46 238.49 128.6 238.58C129.3 239.02 130 239.44 130.71 239.86C130.98 240.02 131.25 240.17 131.52 240.33C132.2 240.72 132.88 241.09 133.58 241.46C133.85 241.6 134.11 241.74 134.38 241.88C135.18 242.29 135.99 242.69 136.81 243.08C136.96 243.15 137.1 243.23 137.25 243.3C138.23 243.76 139.21 244.19 140.21 244.61C140.42 244.7 140.63 244.78 140.84 244.86C141.62 245.18 142.4 245.48 143.18 245.78C143.52 245.9 143.86 246.02 144.19 246.14C144.87 246.38 145.55 246.62 146.24 246.84C146.6 246.96 146.96 247.08 147.33 247.19C148.04 247.41 148.76 247.62 149.48 247.82C149.81 247.91 150.13 248.01 150.46 248.09C151.5 248.37 152.54 248.63 153.6 248.86L157.95 273.85H183.83L188.18 248.86C188.86 248.71 189.52 248.53 190.19 248.37C198.61 246.27 206.5 242.85 213.62 238.32L234.38 252.92L243.53 243.77L252.68 234.62L238.08 213.86C238.43 213.31 238.76 212.75 239.09 212.2C239.8 211.02 240.49 209.82 241.14 208.6C241.26 208.38 241.39 208.16 241.51 207.93C241.53 207.89 241.55 207.85 241.57 207.81C242.17 206.66 242.73 205.49 243.28 204.3C245.59 199.26 247.41 193.95 248.63 188.41L273.62 184.06V171.12V158.18L248.63 153.83H248.62Z" fill="white"/> <path d="M170.88 218.71C197.158 218.71 218.46 197.408 218.46 171.13C218.46 144.852 197.158 123.55 170.88 123.55C144.602 123.55 123.3 144.852 123.3 171.13C123.3 197.408 144.602 218.71 170.88 218.71Z" fill="#758D9F"/> <g opacity="0.7"> <path d="M170.88 186.19C179.197 186.19 185.94 179.448 185.94 171.13C185.94 162.813 179.197 156.07 170.88 156.07C162.563 156.07 155.82 162.813 155.82 171.13C155.82 179.448 162.563 186.19 170.88 186.19Z" fill="white"/> </g> </svg>';
  var LOGO_CRM = '<svg aria-hidden="true" viewBox="0 0 342 342" fill="none" xmlns="http://www.w3.org/2000/svg"> <path d="M170.88 317.701C252.105 317.701 317.95 251.855 317.95 170.631C317.95 89.406 252.105 23.5605 170.88 23.5605C89.6555 23.5605 23.81 89.406 23.81 170.631C23.81 251.855 89.6555 317.701 170.88 317.701Z" fill="#2E4A60"/> <path d="M170.88 167.181C191.077 167.181 207.45 150.808 207.45 130.611C207.45 110.414 191.077 94.041 170.88 94.041C150.683 94.041 134.31 110.414 134.31 130.611C134.31 150.808 150.683 167.181 170.88 167.181Z" fill="white"/> <g opacity="0.4"> <path d="M181.32 172.841H160.43C123.96 172.841 94.4 202.401 94.4 238.871H247.36C247.36 202.401 217.8 172.841 181.33 172.841H181.32Z" fill="white"/> </g> <g opacity="0.4"> <path d="M228.84 137.139C240.653 137.139 250.23 127.563 250.23 115.749C250.23 103.936 240.653 94.3594 228.84 94.3594C217.027 94.3594 207.45 103.936 207.45 115.749C207.45 127.563 217.027 137.139 228.84 137.139Z" fill="white"/> </g> </svg>';
  var IC_CHEVRON = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6l6 -6"/></svg>';

  var PRODUCT_GROUPS = [
    {
      name:'Vận hành', open:true,
      items:[
        { key:'pms',   name:'ezOne.Pms',     desc:'Property management system',       icon:LOGO_PMS },
        { key:'crs',   name:'ezOne.Crs',     desc:'Center reservation system',        icon:LOGO_CRS },
        { key:'msg',   name:'ezOne.Message', desc:'Guest messaging platforms',        icon:LOGO_MESS },
        { key:'admin', name:'ezOne.Admin',   desc:'Administration system',            icon:LOGO_ADMIN },
        { key:'crm',   name:'ezOne.Crm',     desc:'Customer Relationship Management', icon:LOGO_CRM }
      ]
    },
    { name:'Backoffice', open:false, items:[] },
    { name:'Module Placeholder', open:false, items:[] }
  ];

  function matches(it, q){
    return !q || it.name.toLowerCase().indexOf(q) > -1 || it.desc.toLowerCase().indexOf(q) > -1;
  }

  function renderProducts(query){
    var q = (query || '').trim().toLowerCase();
    var html = PRODUCT_GROUPS.map(function(g, gi){
      var items = g.items.filter(function(it){ return matches(it, q); });
      var closed = q ? items.length === 0 : !g.open;
      return '<section class="prod-group' + (closed ? ' is-closed' : '') + '" data-gi="' + gi + '">'
        + '<button class="prod-g-head" type="button" aria-expanded="' + (!closed) + '">'
        +   '<span class="prod-caret" aria-hidden="true">' + IC_CHEVRON + '</span>' + esc(g.name)
        + '</button>'
        + (items.length
            ? '<div class="prod-grid">' + items.map(function(it){
                var cur = it.key === CURRENT;
                return '<button class="prod-item' + (cur ? ' is-current' : '') + '" type="button" data-prod="' + it.key + '"'
                  + ' aria-label="' + (cur ? 'Đang ở ' : 'Mở ') + esc(it.name) + '"' + (cur ? ' aria-current="true"' : '') + '>'
                  + '<span class="prod-ico is-logo" aria-hidden="true">' + it.icon + '</span>'
                  + '<span class="prod-name">' + esc(it.name) + '</span>'
                  + '<span class="prod-desc">' + esc(it.desc) + '</span>'
                + '</button>';
              }).join('') + '</div>'
            : (q ? '' : '<div class="prod-grid"></div>'))
        + '</section>';
    }).join('');

    var any = PRODUCT_GROUPS.some(function(g){ return g.items.some(function(it){ return matches(it, q); }); });
    $('prodBody').innerHTML = any ? html : '<div class="prod-empty">Không có dữ liệu</div>';

    Array.prototype.forEach.call($('prodBody').querySelectorAll('.prod-g-head'), function(h){
      h.addEventListener('click', function(){
        var sec = h.closest('.prod-group');
        var closed = sec.classList.toggle('is-closed');
        h.setAttribute('aria-expanded', String(!closed));
        PRODUCT_GROUPS[Number(sec.getAttribute('data-gi'))].open = !closed;
      });
    });
    Array.prototype.forEach.call($('prodBody').querySelectorAll('[data-prod]'), function(b){
      b.addEventListener('click', function(){ pickProduct(b.getAttribute('data-prod')); });
    });
  }

  function pickProduct(key){
    closeProducts();
    if (key === CURRENT) return;                    /* đang ở ezOne.Pms */
    window.alert('Module này chưa được triển khai');
  }

  function positionProducts(){
    var pop = $('prodPop');
    if (!pop.classList.contains('is-open')) return;
    var r = $('btnApps').getBoundingClientRect();
    var w = pop.offsetWidth;
    var left = Math.min(r.right - w, window.innerWidth - w - 8);
    pop.style.left = Math.max(8, left) + 'px';
    pop.style.top = (r.bottom + 8) + 'px';
  }
  function openProducts(){
    $('prodSearch').value = '';
    renderProducts('');
    $('prodBackdrop').classList.add('is-open');
    $('prodPop').classList.add('is-open');
    $('btnApps').setAttribute('aria-expanded','true');
    positionProducts();
    $('prodSearch').focus();
  }
  function closeProducts(){
    $('prodPop').classList.remove('is-open');
    $('prodBackdrop').classList.remove('is-open');
    $('btnApps').setAttribute('aria-expanded','false');
  }

  $('btnApps').addEventListener('click', function(){
    $('prodPop').classList.contains('is-open') ? closeProducts() : openProducts();
  });
  $('prodClose').addEventListener('click', closeProducts);
  $('prodBackdrop').addEventListener('click', closeProducts);
  $('prodSearch').addEventListener('input', function(){ renderProducts(this.value); });
  document.addEventListener('keydown', function(e){
    if (e.key === 'Escape' && $('prodPop').classList.contains('is-open')) closeProducts();
  });
  window.addEventListener('resize', positionProducts);
  window.addEventListener('scroll', positionProducts, true);
})();
</script>

<!-- ===== CHI TIẾT ĐẶT PHÒNG (FOOR03) — slide-over 90% màn hình ===== -->
<div class="bd-backdrop" id="bdBackdrop"></div>

<aside class="bdw" id="bdCard" role="dialog" aria-modal="true" aria-label="Chi tiết đặt phòng" aria-hidden="true">

  <!-- ===== Header / Overview bar — SRS 3.1.4.2 ===== -->
  <header class="bd-head">
    <div class="bd-h1">
      <span class="bd-grpic" id="bdGrpIcon" hidden aria-hidden="true"></span>
      <span class="bd-name" id="bdName" role="button" tabindex="0" data-bded="gname"
            title="Bấm để sửa tên đoàn"></span>
      <button class="bd-code" type="button" id="bdCode" aria-label="Sao chép mã đặt phòng"></button>
      <button class="bd-chip" type="button" id="bdStatus" data-st="confirmed"
              aria-haspopup="listbox" aria-expanded="false"></button>
      <span class="sp"></span>
      <div class="bd-tools">
                <button class="bd-todo" type="button" id="bdTodo">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M7 8h10M7 12h10M7 16h6"/><path d="M4 4h16v16H4z"/></svg>
          <span>Trưởng đoàn thanh toán</span>
          <span class="badge">2</span>
        </button>
        <button class="bd-ib" type="button" id="bdMail" aria-label="Gửi email" title="Gửi email">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 7l9 6 9-6"/></svg>
        </button>
        <button class="bd-ib" type="button" id="bdPrint" aria-label="In hóa đơn" title="In hóa đơn">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9V3h12v6"/><rect x="4" y="9" width="16" height="7" rx="2"/><path d="M7 16h10v5H7z"/></svg>
        </button>
        <button class="bd-ib" type="button" id="bdMore" aria-label="Thêm hành động" title="Thêm hành động"
                aria-haspopup="menu" aria-expanded="false">
          <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="5" cy="12" r="2"/><circle cx="12" cy="12" r="2"/><circle cx="19" cy="12" r="2"/></svg>
        </button>
        <button class="bd-ib" type="button" id="bdCfg" aria-label="Cấu hình" title="Cấu hình">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M10.3 3.6a1.8 1.8 0 013.4 0l.2.7a1.8 1.8 0 002.5 1l.7-.3a1.8 1.8 0 012.3 2.4l-.3.6a1.8 1.8 0 001 2.6l.7.2a1.8 1.8 0 010 3.4l-.7.2a1.8 1.8 0 00-1 2.6l.3.6a1.8 1.8 0 01-2.3 2.4l-.7-.3a1.8 1.8 0 00-2.5 1l-.2.7a1.8 1.8 0 01-3.4 0l-.2-.7a1.8 1.8 0 00-2.5-1l-.7.3a1.8 1.8 0 01-2.3-2.4l.3-.6a1.8 1.8 0 00-1-2.6l-.7-.2a1.8 1.8 0 010-3.4l.7-.2a1.8 1.8 0 001-2.6l-.3-.6a1.8 1.8 0 012.3-2.4l.7.3a1.8 1.8 0 002.5-1z"/><circle cx="12" cy="12" r="3"/></svg>
        </button>
        <button class="bd-ib" type="button" id="bdClose" aria-label="Đóng" title="Đóng">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
        </button>
      </div>
    </div>

    <div class="bd-h2" id="bdMeta"></div>

    <nav class="bd-tabs" id="bdTabs" role="tablist" aria-label="Nội dung đặt phòng">
      <button class="bd-tab" type="button" role="tab" data-bdtab="general" aria-selected="false">Thông tin chung</button>
      <button class="bd-tab is-on" type="button" role="tab" data-bdtab="rooms" aria-selected="true" id="bdTabRooms">Danh sách phòng</button>
    </nav>
  </header>

  <!-- ===== Bulk action bar — Figma 26641:36076 ===== -->
  <div class="bd-bulk" id="bdBulk" role="group" aria-label="Thao tác nhanh với phòng đã chọn">
    <button class="x" type="button" id="bdBulkX" aria-label="Bỏ chọn tất cả">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
    <span class="lb" id="bdBulkTxt">Đang chọn (0):</span>
    <div id="bdBulkBtns" style="display:flex;align-items:center;gap:8px;flex-wrap:wrap"></div>
  </div>

  <!-- ===== Body ===== -->
  <div class="bd-body" id="bdBody">
    <aside class="bd-col-l" id="bdColL" aria-label="Danh sách phòng của đoàn">
      <div class="bd-lfilter">
        <label class="bd-tf">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><circle cx="11" cy="11" r="7"/><path d="M20 20l-3.5-3.5"/></svg>
          <input type="search" id="bdKw" maxlength="50" placeholder="Tên phòng, tên khách" aria-label="Tìm kiếm tên phòng, tên khách">
        </label>
        <div class="qb-vs" data-qbsel="bdGroupBy" data-qblabel="Nhóm phòng"></div>
      </div>
      <div class="bd-list" id="bdList"></div>
    </aside>

    <div class="bd-col-r" id="bdColR"></div>
  </div>
</aside>

<!-- ===== R01.22 · QUY TRÌNH NHẬN PHÒNG (CHECK-IN) — slide-over 85vw ===== -->
<div class="ci-bd" id="ciBd"></div>

<aside class="ci" id="ciCard" role="dialog" aria-modal="true" aria-labelledby="ciTitle">
  <header class="ci-head">
    <h2 id="ciTitle">Quy trình nhận phòng</h2>
    <button class="tl-btn-outline" type="button" id="ciReset">Cài lại mặc định</button>
    <button class="ci-x" type="button" id="ciClose" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>

  <nav class="ci-steps" aria-label="Các bước nhận phòng">
    <div class="ci-step is-cur" data-cistep="1" aria-current="step">
      <span class="no">1</span><span class="tx">Xác định thời gian nhận phòng</span>
    </div>
    <span class="ci-sep" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6-6 6"/></svg></span>
    <div class="ci-step" data-cistep="2">
      <span class="no">2</span><span class="tx">Tạo thẻ từ</span>
    </div>
  </nav>

  <!-- Thanh tab bước 2 -->
  <div class="ci-tools" id="ciTools" hidden>
    <button class="ci-tab" type="button" role="tab" data-citab="all" aria-selected="true">Tất cả (0)</button>
    <button class="ci-tab" type="button" role="tab" data-citab="none" aria-selected="false">Chưa tạo (0)</button>
    <span class="sp"></span>
  </div>
  <div class="ci-selbar" id="ciSelbar">
    <span class="lb" id="ciSelTxt">Đã chọn (0)</span>
    <span class="sp"></span>
    <button class="tl-btn-primary" type="button" id="ciSelMake">Tạo thẻ</button>
  </div>

  <div class="ci-body" id="ciBody">
    <div class="ci-tblwrap" id="ciWrap"><table class="ci-tbl" id="ciTbl"></table></div>
    <div class="ci-empty" id="ciEmpty" hidden>
      <svg viewBox="0 0 120 120" fill="none" stroke="currentColor" stroke-width="4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M30 58h60v34H30z"/><path d="M30 58l10-22h40l10 22"/><path d="M30 74h20l4 8h12l4-8h20"/>
        <path d="M60 18v10M42 26l5 8M78 26l-5 8"/>
      </svg>
      <p>Các phòng đều đã có thẻ</p>
    </div>
    <div class="ci-sum" id="ciSum" hidden></div>
  </div>

  <footer class="ci-foot" id="ciFoot">
    <button class="tl-btn-ghost" type="button" id="ciBack" hidden>
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 6l-6 6 6 6"/></svg>
      Quay lại
    </button>
    <span class="sp"></span>
    <button class="ci-btn-later" type="button" id="ciLater" hidden>Tạo sau</button>
    <button class="tl-btn-primary" type="button" id="ciOk">Xác nhận</button>
    <button class="tl-btn-primary" type="button" id="ciNext" hidden>
      Tạo thẻ từ
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 6l6 6-6 6"/></svg>
    </button>
    <button class="tl-btn-primary" type="button" id="ciBulk" hidden>
      Tạo thẻ hàng loạt
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 6l6 6-6 6"/></svg>
    </button>
    <button class="ci-btn-done" type="button" id="ciFinish" hidden>
      Hoàn thành
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M8 12.5l2.5 2.5L16 9.5"/></svg>
    </button>
  </footer>
</aside>

<!-- ===== Popup "Tạo thẻ phòng" (SRS 3.2.4) ===== -->
<div class="ci-cbd" id="ciCBd"></div>
<div class="ci-card" id="ciCPop" role="dialog" aria-modal="true" aria-labelledby="ciCTitle">
  <header class="ci-card-h">
    <h3 id="ciCTitle">Tạo thẻ phòng</h3>
    <button class="ci-x" type="button" id="ciCX" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>
  <div class="ci-card-b">
    <div class="ci-card-l"><table id="ciCTbl"></table></div>
    <div class="ci-card-r">
      <div class="t">Tạo thẻ phòng <b id="ciCRoom">—</b> <span class="ci-tag" data-t="none" id="ciCTag">Chưa tạo</span></div>
      <label class="fl" for="ciCQty">Tổng số thẻ</label>
      <div class="ci-num">
        <input type="number" id="ciCQty" min="0" step="1" value="2" aria-label="Tổng số thẻ">
        <span class="sp2">
          <button type="button" id="ciCUp" aria-label="Tăng"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M6 15l6-6 6 6"/></svg></button>
          <button type="button" id="ciCDn" aria-label="Giảm"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg></button>
        </span>
      </div>
      <div class="ci-info">
        <div><div class="k">Đã tạo/Tổng số thẻ</div><div class="v" id="ciCMade">0/2</div></div>
        <div><div class="k">Hiệu lực</div><div class="v" id="ciCValid">—</div></div>
      </div>
      <div class="ci-guide">
        <svg viewBox="0 0 200 110" fill="none" aria-hidden="true">
          <rect x="8" y="26" width="62" height="86" rx="8" transform="rotate(-8 8 26)" fill="#F1F1F3" stroke="#B9BAC2" stroke-width="2"/>
          <rect x="112" y="14" width="74" height="94" rx="12" fill="#F7F7F9" stroke="#8E9099" stroke-width="2"/>
          <path d="M141 48a14 14 0 0 1 0 20M149 42a22 22 0 0 1 0 32" stroke="#8E9099" stroke-width="2.2" stroke-linecap="round"/>
          <rect x="138" y="92" width="22" height="7" rx="3.5" fill="#28C76F"/>
          <path d="M84 60h20M100 56l5 4-5 4" stroke="#B9BAC2" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" stroke-dasharray="4 4"/>
        </svg>
        <p>Đặt thẻ lên máy đọc, sau đó nhấn <b>“Tạo thẻ phòng”</b><br>hoặc <b>Ctrl</b> trên bàn phím</p>
      </div>
      <button class="ci-write" type="button" id="ciCGo">Tạo thẻ phòng<kbd>Ctrl</kbd></button>
    </div>
  </div>
</div>

<!-- ===== SEARCH TỔNG (Header common — SRS 3.1) ===== -->
<div class="gs-bd" id="gsBd"></div>
<div class="gs" id="gsCard" role="dialog" aria-modal="true" aria-label="Tìm kiếm theo đặt phòng, chức năng">
  <div class="gs-head">
    <span class="ic" aria-hidden="true">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><circle cx="10.5" cy="10.5" r="7"/><path d="M21 21l-6-6"/></svg>
    </span>
    <input type="text" class="gs-q" id="gsQ" maxlength="255" autocomplete="off" spellcheck="false"
           placeholder="Tìm kiếm theo mã đặt phòng, tên khách, tên chức năng"
           aria-label="Nhập từ khoá tìm kiếm">
    <span class="gs-esc" aria-hidden="true">[esc]</span>
    <button class="gs-x" type="button" id="gsX" aria-label="Đóng tìm kiếm">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </div>

  <div class="gs-tabs">
    <div class="gs-seg" id="gsSeg" role="tablist" aria-label="Lọc kết quả tìm kiếm">
      <button type="button" role="tab" data-gstab="all" aria-selected="true">Tất cả</button>
      <button type="button" role="tab" data-gstab="booking" aria-selected="false">Đặt phòng</button>
      <button type="button" role="tab" data-gstab="function" aria-selected="false">Chức năng</button>
    </div>
  </div>

  <div class="gs-body" id="gsBody"></div>
</div>

<!-- ===== Timeline: tooltip / menu / dialog / toast ===== -->
<div class="tl-pop" id="tlPop" role="dialog" aria-label="Thông tin đặt phòng"></div>

<div class="tl-menu" id="tlMenu" role="menu" aria-label="Thao tác nhanh"></div>

<!-- Menu In nhanh (Hoá đơn tạm tính) — check-out -->
<div class="tl-menu co-pm" id="coPrintMenu" role="menu" aria-label="In nhanh (Hoá đơn tạm tính)"></div>

<div class="tl-tt" id="tlTip2" role="tooltip" data-dir="down"></div>

<!-- Date picker (Vuexy AppDateTimePicker / flatpickr) -->
<div class="tl-dp" id="tlDp" role="dialog" aria-modal="false" aria-label="Chọn ngày">
  <div class="tl-dp-head">
    <button class="tl-dp-nav" type="button" data-dp="prevMonth" aria-label="Tháng trước">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 6l-6 6 6 6"/></svg>
    </button>
    <div class="tl-dp-title">
      <select id="tlDpMonth" aria-label="Tháng"></select>
      <input id="tlDpYear" type="number" inputmode="numeric" min="1900" max="2999" aria-label="Năm">
    </div>
    <button class="tl-dp-nav" type="button" data-dp="nextMonth" aria-label="Tháng sau">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 6l6 6-6 6"/></svg>
    </button>
  </div>
  <div class="tl-dp-wd" id="tlDpWd" aria-hidden="true"></div>
  <div class="tl-dp-days" id="tlDpDays" role="grid" aria-label="Lịch"></div>
  <div class="tl-dp-foot">
    <button type="button" class="ghost" data-dp="close">Đóng</button>
    <button type="button" data-dp="today">Hôm nay</button>
  </div>
</div>

<div class="tl-backdrop" id="tlBackdrop"></div>

<!-- Popup Đổi trạng thái phòng (Figma) -->
<div class="tl-dialog" id="tlRoomDlg" role="dialog" aria-modal="true" aria-labelledby="tlRoomDlgTitle">
  <header>
    <h2 id="tlRoomDlgTitle">Đổi trạng thái phòng</h2>
    <button class="close" type="button" data-tl-close aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>
  <div class="bd">
    <div class="tl-switchrow">
      <button class="tl-switch" type="button" role="switch" aria-checked="false" id="tlSwDirty">
        <span class="track"></span>
        <span>Phòng bẩn</span>
      </button>
    </div>
  </div>
  <footer>
    <button class="tl-btn-ghost" type="button" data-tl-close>Huỷ</button>
    <button class="tl-btn-primary" type="button" id="tlRoomConfirm">Xác nhận</button>
  </footer>
</div>

<!-- Dialog dùng chung (danh sách đặt phòng / xác nhận) -->
<div class="tl-dialog" id="tlGenDlg" role="dialog" aria-modal="true" aria-labelledby="tlGenTitle">
  <header>
    <h2 id="tlGenTitle"></h2>
    <button class="close" type="button" data-tl-close aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>
  <div class="bd" id="tlGenBody"></div>
  <footer id="tlGenFoot"></footer>
</div>

<div class="bd-menu" id="bdMenu" role="menu" aria-label="Thao tác đặt phòng"></div>
<div class="tl-toasts" id="tlToasts" aria-live="polite"></div>

<!-- ===== SỬA PHÒNG (Thiết lập / Điều chỉnh / Xoá) — Figma 42502:133780 ===== -->
<div class="tl-dialog rp-dlg" id="rpDlg" role="dialog" aria-modal="true" aria-labelledby="rpTitle">
  <header>
    <h2 id="rpTitle">Thiết lập sửa phòng</h2>
    <button class="close" type="button" id="rpX" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>

  <div class="bd rp-bd">

    <!-- Loại sửa -->
    <div class="rp-f">
      <span class="rp-lb">Loại sửa</span>
      <div class="rp-toggle" role="group" aria-label="Loại sửa">
        <button type="button" data-rpkind="OOS" aria-pressed="true">Tạm ngừng phục vụ (OOS)</button>
        <button type="button" data-rpkind="OOO" aria-pressed="false">Phòng hỏng/bảo trì (OOO)</button>
      </div>
    </div>

    <!-- Ngày bắt đầu / kết thúc -->
    <div class="rp-row">
      <div class="rp-f">
        <span class="rp-lb">Ngày bắt đầu<i class="req">*</i></span>
        <button class="rp-dt" type="button" id="rpFrom" aria-label="Ngày bắt đầu">
          <span class="v" id="rpFromT">—</span>
          <span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span>
        </button>
      </div>
      <div class="rp-f">
        <span class="rp-lb">Ngày kết thúc<i class="req">*</i></span>
        <button class="rp-dt" type="button" id="rpTo" aria-label="Ngày kết thúc">
          <span class="v" id="rpToT">—</span>
          <span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span>
        </button>
      </div>
    </div>
    <span class="rp-msg" id="rpDateMsg" hidden>Ngày bắt đầu phải nhỏ hơn ngày kết thúc, vui lòng kiểm tra lại!</span>

    <!-- Vẫn mở bán phòng (chỉ hiện khi OOS) -->
    <label class="rp-ck" id="rpSellWrap">
      <input type="checkbox" id="rpSell">
      <span class="box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg></span>
      <span class="lb">Vẫn mở bán phòng</span>
    </label>

    <!-- Lý do -->
    <div class="rp-f">
      <span class="rp-lb">Lý do<i class="req">*</i></span>
      <div class="qb-vs" data-qbsel="rpReason" data-qblabel="Lý do sửa phòng"></div>
      <span class="rp-msg" id="rpReasonMsg" hidden>Vui lòng điền dữ liệu</span>
    </div>

    <!-- Lý do khác -->
    <div class="rp-f" id="rpOtherWrap" hidden>
      <span class="rp-lb">Lý do khác<i class="req">*</i></span>
      <textarea id="rpOther" maxlength="500" placeholder="Nhập lý do khác"></textarea>
      <span class="rp-msg" id="rpOtherMsg" hidden>Vui lòng điền dữ liệu</span>
    </div>

  </div>

  <footer class="rp-foot">
    <button class="rp-del" type="button" id="rpDel" hidden>Xoá sửa phòng</button>
    <span class="rp-sp"></span>
    <button class="tl-btn-ghost" type="button" id="rpCancel">Hủy</button>
    <button class="tl-btn-primary" type="button" id="rpSave">Lưu</button>
  </footer>
</div>

<!-- Popup Xoá sửa phòng -->
<div class="tl-dialog rp-dlg" id="rpDelDlg" role="dialog" aria-modal="true" aria-labelledby="rpDelTitle">
  <header>
    <h2 id="rpDelTitle">Xoá sửa phòng</h2>
    <button class="close" type="button" id="rpDelX" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>
  <div class="bd rp-bd">
    <dl class="rp-info" id="rpDelInfo"></dl>
    <hr class="rp-hr">
    <div class="rp-f">
      <span class="rp-lb">Lý do xoá sửa phòng<i class="req">*</i></span>
      <textarea id="rpDelReason" maxlength="150" placeholder="Nhập lý do"></textarea>
      <span class="rp-msg" id="rpDelMsg" hidden>Vui lòng nhập lý do, tối thiểu 5 ký tự.</span>
    </div>
  </div>
  <footer class="rp-foot">
    <span class="rp-sp"></span>
    <button class="tl-btn-ghost" type="button" id="rpDelCancel">Hủy</button>
    <button class="rp-danger" type="button" id="rpDelOk">Xác nhận</button>
  </footer>
</div>

<!-- ===== ĐỔI NGÀY Ở (R01.07) — Figma 50775:132643 ===== -->
<div class="cd-bd" id="cdBd"></div>

<div class="cd-dlg" id="cdDlg" role="dialog" aria-modal="true" aria-labelledby="cdTitle">
  <header class="cd-head">
    <h2 id="cdTitle">Đổi ngày ở</h2>
    <div class="cd-headacts">
      <button class="cd-btn-out" type="button" id="cdBulkAll">Đổi ngày ở đồng loạt</button>
      <button class="cd-btn-out" type="button" id="cdReset" disabled>Cài lại mặc định</button>
    </div>
    <button class="cd-x" type="button" id="cdX" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>

  <!-- Thanh chọn nhiều dòng -->
  <div class="cd-selbar" id="cdSelBar" hidden>
    <button class="cd-selx" type="button" id="cdSelClear" aria-label="Bỏ chọn tất cả">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
    <span id="cdSelCount">Đang chọn (0):</span>
    <button class="cd-btn-out" type="button" id="cdSelBulk">Đổi ngày ở</button>
  </div>

  <!-- Băng cảnh báo xung đột -->
  <div class="cd-errbar" id="cdErrBar" hidden>
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M9 9l6 6M15 9l-6 6"/></svg>
    <span id="cdErrText"></span>
  </div>

  <div class="cd-scroll" id="cdScroll">
    <table class="cd-tbl" id="cdTbl">
      <thead>
        <tr>
          <th class="ck"><label class="cd-ck"><input type="checkbox" id="cdCkAll"><span class="box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg></span></label></th>
          <th class="rm">Phòng</th>
          <th class="gt">Khách hàng</th>
          <th class="px">NL/TE/EB</th>
          <th class="dt">Ngày đến</th>
          <th class="dt">Ngày đi</th>
          <th class="nt">Số đêm</th>
          <th class="pa">Trả phòng<br>quá khứ</th>
          <th class="mo">Giá phòng</th>
          <th class="mo">Giá mới</th>
          <th class="mo">Chênh lệch</th>
          <th class="ed"></th>
        </tr>
      </thead>
      <tbody id="cdBody"></tbody>
    </table>
  </div>

  <div class="cd-sum">
    <span class="lb">Giá sẽ thay đổi</span>
    <span class="v" id="cdSumOld">0</span>
    <span class="ar" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M4 12h16M15 7l5 5-5 5"/></svg></span>
    <span class="v" id="cdSumNew">0</span>
    <span class="d" id="cdSumDiff">(+0)</span>
  </div>

  <div class="cd-pg" id="cdPg">
    <span class="l">Số bản ghi trên trang</span>
    <div class="qb-vs cd-vs" data-qbsel="cdPerPage" data-qblabel="Số bản ghi trên trang"></div>
    <span class="r" id="cdRange"></span>
    <div class="cd-pager" id="cdPager"></div>
  </div>

  <footer class="cd-foot">
    <button class="tl-btn-ghost" type="button" id="cdCancel">Huỷ</button>
    <button class="tl-btn-primary" type="button" id="cdSave">Lưu</button>
  </footer>
</div>

<!-- Popup Đổi đồng loạt / Đổi ngày ở đã chọn -->
<div class="cd-subbd" id="cdSubBd"></div>
<div class="cd-sub" id="cdBulkDlg" role="dialog" aria-modal="true" aria-labelledby="cdBulkTitle">
  <header>
    <h3 id="cdBulkTitle">Đổi đồng loạt</h3>
    <button class="cd-x" type="button" id="cdBX" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>
  <div class="cd-subbody">
    <div class="cd-frow">
      <span class="lb">Thời gian đến</span>
      <button class="cd-dt" type="button" id="cdBIn" aria-label="Thời gian đến">
        <span class="v" id="cdBInT">—</span>
        <span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span>
      </button>
    </div>
    <div class="cd-frow">
      <span class="lb">Thời gian đi</span>
      <button class="cd-dt" type="button" id="cdBOut" aria-label="Thời gian đi">
        <span class="v" id="cdBOutT">—</span>
        <span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span>
      </button>
    </div>
    <div class="cd-frow top">
      <span class="lb">Số đêm</span>
      <div class="cd-nwrap">
        <div class="cd-num"><input type="text" id="cdBN" inputmode="numeric" aria-label="Số đêm"><span class="sp"><button type="button" data-cdbn="1" aria-label="Tăng số đêm"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="M6 15l6-6 6 6"/></svg></button><button type="button" data-cdbn="-1" aria-label="Giảm số đêm"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg></button></span></div>
        <span class="cd-delta" id="cdBDelta"></span>
      </div>
    </div>
  </div>
  <footer>
    <button class="tl-btn-ghost" type="button" id="cdBCancel">Huỷ</button>
    <button class="tl-btn-primary" type="button" id="cdBOk">Xác nhận</button>
  </footer>
</div>

<!-- Popup Chi tiết thay đổi -->
<div class="cd-sub cd-dt-dlg" id="cdDtDlg" role="dialog" aria-modal="true" aria-labelledby="cdDtTitle">
  <header>
    <h3 id="cdDtTitle">Chi tiết thay đổi</h3>
    <span class="cd-guest" id="cdDtGuest"></span>
    <button class="cd-x" type="button" id="cdDtX" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>
  <div class="cd-subbody">
    <dl class="cd-dtinfo">
      <dt>Thời gian ở ban đầu:</dt><dd id="cdDtOld"></dd>
      <dt>Thời gian ở mới:</dt><dd class="strong" id="cdDtNew"></dd>
    </dl>
    <div class="cd-tabs" role="tablist" aria-label="Danh mục thay đổi">
      <button type="button" role="tab" data-cdtab="up" aria-selected="false">Danh mục tăng</button>
      <button type="button" role="tab" data-cdtab="down" aria-selected="true">Danh mục giảm</button>
    </div>
    <div class="cd-frow bulkrate">
      <span class="lb">Đổi chính sách giá hàng loạt</span>
      <div class="qb-vs cd-vs" data-qbsel="cdDtRate" data-qblabel="Đổi chính sách giá hàng loạt"></div>
    </div>
    <div class="cd-dtscroll">
      <table class="cd-dttbl">
        <thead><tr><th class="tm">Thời gian</th><th class="ct">Danh mục</th><th class="rt">Chính sách giá</th><th class="am">Thành tiền</th></tr></thead>
        <tbody id="cdDtBody"></tbody>
      </table>
    </div>
    <div class="cd-dtpg"><span id="cdDtPageInfo"></span></div>
    <dl class="cd-dtsum">
      <dt>Giá sẽ thay đổi (VND):</dt><dd id="cdDtSum"></dd>
      <dt>Tổng giá chênh lệch (VND):</dt><dd id="cdDtDiff"></dd>
    </dl>
  </div>
  <footer>
    <button class="tl-btn-ghost" type="button" id="cdDtCancel">Huỷ</button>
    <button class="tl-btn-primary" type="button" id="cdDtOk">Cập nhật</button>
  </footer>
</div>

<!-- ===== CHUYỂN PHÒNG (R01.08) — Figma 48831:92306 ===== -->
<div class="mv-bd" id="mvBd"></div>

<div class="mv-dlg" id="mvDlg" role="dialog" aria-modal="true" aria-labelledby="mvTitle">

  <!-- ---------- Cột trái: danh sách / cây phòng ---------- -->
  <section class="mv-left" id="mvLeft">
    <header class="mv-head">
      <h2 id="mvTitle">Chuyển phòng</h2>
      <button class="mv-x" type="button" id="mvX" aria-label="Đóng">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
      </button>
    </header>

    <div class="mv-scroll" id="mvScroll">
      <table class="mv-tbl" id="mvTbl">
        <thead>
          <tr>
            <th class="rm">Phòng</th>
            <th class="lk">Khoá</th>
            <th class="gt">Khách hàng</th>
            <th class="px">NL/TE/EB</th>
            <th class="dt">Ngày đến - đi</th>
            <th class="rt">Chính sách giá</th>
            <th class="mo">Giá cũ</th>
            <th class="mo">Giá mới</th>
            <th class="mo">Chênh lệch</th>
          </tr>
        </thead>
        <tbody id="mvBody"></tbody>
      </table>
      <div class="mv-tree" id="mvTree"></div>
    </div>

    <div class="mv-tot">
      <span class="lb">Tổng</span>
      <span class="mo" id="mvTotOld">0</span>
      <span class="mo" id="mvTotNew">0</span>
      <span class="mo" id="mvTotDiff">0</span>
    </div>
    <div class="mv-pg">
      <span class="l">Số bản ghi trên trang</span>
      <div class="qb-vs mv-vs-per" data-qbsel="mvPerPage" data-qblabel="Số bản ghi trên trang"></div>
      <span class="r" id="mvRange"></span>
      <div class="mv-pager" id="mvPager"></div>
    </div>
    <footer class="mv-foot" id="mvFoot">
      <button class="tl-btn-ghost" type="button" id="mvCancel">Huỷ</button>
      <button class="tl-btn-primary" type="button" id="mvSave">Lưu</button>
    </footer>
  </section>

  <!-- ---------- Cột giữa: thực hiện chuyển phòng ---------- -->
  <section class="mv-mid" id="mvMid">
    <header class="mv-head">
      <h2>Thực hiện chuyển phòng</h2>
    </header>
    <div class="mv-midbody" id="mvMidBody">
      <h3 class="mv-sect">Thông tin chung</h3>
      <div class="mv-banner">
        <div class="c"><span class="k">Phòng cũ</span><span class="v" id="mvOldRoom">—</span></div>
        <span class="ar" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M4 12h16M15 7l5 5-5 5"/></svg></span>
        <div class="c"><span class="k">Phòng mới <span class="lk" id="mvNewLock" hidden><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="4" y="10" width="16" height="11" rx="2"/><path d="M8 10V7a4 4 0 018 0"/></svg></span></span><span class="v none" id="mvNewRoom">Chưa chọn</span></div>
      </div>

      <dl class="mv-info" id="mvInfo"></dl>

      <div class="mv-f">
        <span class="mv-lb">Lý do chuyển phòng<i class="req">*</i></span>
        <div class="qb-vs" data-qbsel="mvReason" data-qblabel="Lý do chuyển phòng"></div>
        <span class="mv-msg" id="mvReasonMsg" hidden>Vui lòng chọn lý do chuyển phòng</span>
      </div>

      <!-- Tác vụ sau khi chuyển phòng thành công -->
      <div class="mv-tasks" id="mvTasks" hidden>
        <label class="mv-ck"><input type="checkbox" id="mvLock"><span class="box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg></span><span class="lb">Khoá phòng</span></label>
        <div class="mv-f" id="mvLockWrap" hidden>
          <span class="mv-lb">Lý do khoá phòng<i class="req">*</i></span>
          <textarea id="mvLockReason" maxlength="200" placeholder="Nhập lý do khoá phòng"></textarea>
          <span class="mv-msg" id="mvLockMsg" hidden>Vui lòng nhập lý do khoá phòng</span>
        </div>

        <label class="mv-ck" id="mvRateWrap" hidden><input type="checkbox" id="mvRateCk"><span class="box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg></span><span class="lb">Đổi chính sách giá</span></label>
        <div class="mv-rate" id="mvRateBox" hidden>
          <div class="mv-2col">
            <div class="mv-f">
              <span class="mv-lb">Chính sách giá</span>
              <div class="qb-vs" data-qbsel="mvRate" data-qblabel="Chính sách giá"></div>
            </div>
            <div class="mv-f">
              <span class="mv-lb">Phí chuyển phòng</span>
              <div class="mv-fee"><span class="u">VND</span><input type="text" id="mvFee" inputmode="numeric" value="0" aria-label="Phí chuyển phòng"></div>
            </div>
          </div>
          <dl class="mv-calc">
            <dt>Chính sách giá:</dt><dd id="mvCalcRate"></dd>
            <dt>Giá sẽ thay đổi (VND):</dt><dd id="mvCalcPrice"></dd>
            <dt>Tổng giá chênh lệch (VND):</dt><dd id="mvCalcDiff"></dd>
          </dl>
        </div>
      </div>
    </div>
    <footer class="mv-midfoot">
      <button class="tl-btn-ghost" type="button" id="mvPrev">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 12H4M9 7l-5 5 5 5"/></svg>
        Phòng trước
      </button>
      <button class="tl-btn-primary" type="button" id="mvNext">
        Phòng tiếp theo
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 12h16M15 7l5 5-5 5"/></svg>
      </button>
    </footer>
  </section>

  <!-- ---------- Cột phải: chọn phòng trống ---------- -->
  <section class="mv-right" id="mvRight">
    <header class="mv-head">
      <h2>Chọn phòng trống</h2>
      <button class="mv-x" type="button" id="mvX2" aria-label="Đóng">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
      </button>
    </header>

    <div class="mv-rightbody" id="mvRightBody">
      <div class="mv-filters">
        <div class="mv-f"><span class="mv-lb">Loại phòng</span><div class="qb-vs" data-qbsel="mvFType" data-qblabel="Loại phòng"></div></div>
        <div class="mv-f"><span class="mv-lb">Phòng</span><div class="qb-vs" data-qbsel="mvFRoom" data-qblabel="Phòng"></div></div>
        <div class="mv-f"><span class="mv-lb">Trạng thái</span><div class="qb-vs" data-qbsel="mvFHk" data-qblabel="Trạng thái"></div></div>
      </div>
      <div class="mv-fcks">
        <label class="mv-ck"><input type="checkbox" id="mvFAdj"><span class="box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg></span><span class="lb">Phòng thông nhau</span></label>
        <label class="mv-ck"><input type="checkbox" id="mvFOnlyOut"><span class="box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg></span><span class="lb">Chỉ phòng có khách sắp trả</span></label>
        <label class="mv-ck"><input type="checkbox" id="mvFExOut"><span class="box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg></span><span class="lb">Loại trừ phòng có khách sắp trả</span></label>
      </div>

      <div class="mv-rscroll">
        <table class="mv-rtbl">
          <thead><tr><th>Phòng</th><th>Trạng thái</th><th class="ac"></th></tr></thead>
          <tbody id="mvRoomBody"></tbody>
        </table>
      </div>
    </div>

    <!-- Trạng thái phòng đã bị khoá -->
    <div class="mv-locked" id="mvLocked" hidden>
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="4" y="10" width="16" height="11" rx="2"/><path d="M8 10V7a4 4 0 018 0"/><circle cx="12" cy="15.5" r="1.4"/></svg>
      <p class="t">Phòng đã bị khoá</p>
      <p class="s" id="mvLockedReason"></p>
      <p class="w" id="mvLockedBy"></p>
    </div>

    <div class="mv-rpg">
      <span class="l">Bản ghi/trang</span>
      <div class="qb-vs mv-vs-per" data-qbsel="mvRPerPage" data-qblabel="Bản ghi trên trang"></div>
      <div class="mv-pager" id="mvRPager"></div>
    </div>
  </section>
</div>

<!-- ===== KHAI BÁO CÔNG AN / HẢI QUAN (RR06) — Figma 35977:17486 ===== -->
<div class="ca-bd" id="caBd"></div>

<div class="ca" id="caCard" role="dialog" aria-modal="true" aria-labelledby="caA4Title">

  <!-- ---------- Thanh công cụ ---------- -->
  <header class="ca-nav">
    <div class="ca-tools">
      <button class="ca-ib" type="button" id="caUndo" aria-label="Hoàn tác" title="Hoàn tác">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 14l-5-5 5-5"/><path d="M4 9h9a6 6 0 010 12h-3"/></svg>
      </button>
      <button class="ca-ib" type="button" id="caRedo" aria-label="Làm lại" title="Làm lại">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 14l5-5-5-5"/><path d="M20 9h-9a6 6 0 000 12h3"/></svg>
      </button>
      <button class="ca-ib" type="button" id="caClear" aria-label="Xoá bộ lọc" title="Xoá bộ lọc">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
      </button>
      <button class="ca-ib" type="button" id="caReload" aria-label="Tải lại" title="Tải lại">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 11a8 8 0 10-2.3 6.3"/><path d="M20 5v6h-6"/></svg>
      </button>
      <button class="ca-ib" type="button" data-capg="first" aria-label="Trang đầu">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M17 6l-6 6 6 6M9 6l-6 6 6 6"/></svg>
      </button>
      <button class="ca-ib" type="button" data-capg="prev" aria-label="Trang trước">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 6l-6 6 6 6"/></svg>
      </button>
      <span class="ca-pgbox">
        <input type="text" id="caPage" inputmode="numeric" value="1" aria-label="Số trang">
        <span class="of" id="caPages">/1</span>
      </span>
      <button class="ca-ib" type="button" data-capg="next" aria-label="Trang sau">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 6l6 6-6 6"/></svg>
      </button>
      <button class="ca-ib" type="button" data-capg="last" aria-label="Trang cuối">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M7 6l6 6-6 6M15 6l6 6-6 6"/></svg>
      </button>
      <span class="ca-vr" aria-hidden="true"></span>
      <div class="ca-zoombox" role="group" aria-label="Thu phóng báo cáo">
        <button class="ca-ib" type="button" data-cazoom="out" aria-label="Thu nhỏ" title="Thu nhỏ (Ctrl -)">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="11" cy="11" r="7"/><path d="M8 11h6M20 20l-3.5-3.5"/></svg>
        </button>
        <button class="ca-zoomlb" type="button" id="caZoomLb" aria-label="Mức thu phóng, bấm để đặt lại 100%" title="Đặt lại 100%">100%</button>
        <button class="ca-ib" type="button" data-cazoom="in" aria-label="Phóng to" title="Phóng to (Ctrl +)">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="11" cy="11" r="7"/><path d="M8 11h6M11 8v6M20 20l-3.5-3.5"/></svg>
        </button>
        <button class="ca-ib" type="button" data-cazoom="fit" aria-label="Vừa bề ngang" title="Vừa bề ngang">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 9V5a2 2 0 012-2h4M21 9V5a2 2 0 00-2-2h-4M3 15v4a2 2 0 002 2h4M21 15v4a2 2 0 01-2 2h-4"/><path d="M7 12h10"/></svg>
        </button>
      </div>
    </div>

    <div class="ca-navr">
      <div class="ca-dl">
        <button class="ca-btn-out" type="button" id="caDlBtn" aria-haspopup="menu" aria-expanded="false">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3v12M8 11l4 4 4-4"/><path d="M4 19h16"/></svg>
          Tải xuống
          <svg class="cv" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>
        </button>
        <div class="ca-dlmenu" id="caDlMenu" role="menu">
          <button type="button" role="menuitem" data-cadl="XLSM">XLSM</button>
          <button type="button" role="menuitem" data-cadl="CSV">CSV</button>
          <button type="button" role="menuitem" data-cadl="XML">XML</button>
          <button type="button" role="menuitem" data-cadl="Mẫu bộ công an">Mẫu bộ công an</button>
        </div>
      </div>
      <button class="ca-btn-out" type="button" id="caPrint">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9V3h12v6"/><rect x="4" y="9" width="16" height="7" rx="2"/><path d="M8 16h8v5H8z"/></svg>
        In
      </button>
      <span class="ca-vr" aria-hidden="true"></span>
      <button class="ca-ib" type="button" id="caClose" aria-label="Đóng">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
      </button>
    </div>
  </header>

  <div class="ca-body">
    <!-- ---------- Trang A4 ---------- -->
    <div class="ca-viewer" id="caViewer">
      <div class="ca-zoom" id="caZoom">
      <article class="ca-a4" id="caA4">
        <header class="ca-a4-top">
          <span class="brand">ezOne</span>
          <span class="code" id="caA4Code">BC06</span>
        </header>
        <h2 class="ca-a4-title" id="caA4Title">Khai báo CA/Hải quan</h2>
        <p class="ca-a4-range" id="caA4Range"></p>
        <p class="ca-a4-cond" id="caA4Cond"></p>
        <div class="ca-a4-tblwrap">
          <table class="ca-a4-tbl" id="caTbl">
            <thead><tr id="caHead"></tr></thead>
            <tbody id="caBody2"></tbody>
          </table>
        </div>
        <footer class="ca-a4-foot">
          <span class="it">Powered by ezCloud.vn</span>
          <span class="it"><b>Thời gian in:</b> <span id="caPrintAt"></span></span>
          <span class="it"><b>Người in:</b> <span id="caPrintBy">Nguyễn Văn Thảo Nguyên</span></span>
          <span class="pg" id="caFootPg">1/1</span>
        </footer>
      </article>
      </div>
    </div>

    <!-- ---------- Panel lọc dữ liệu ---------- -->
    <aside class="ca-filter" aria-label="Lọc dữ liệu">
      <div class="ca-fhead"><h3>Lọc dữ liệu</h3></div>
      <div class="ca-fbody" id="caFBody">

        <div class="ca-f"><span class="ca-lb">Từ ngày</span>
          <button class="ca-dt" type="button" id="caFrom" aria-label="Từ ngày"><span class="v" id="caFromT">—</span><span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span></button>
        </div>
        <div class="ca-f"><span class="ca-lb">Đến ngày</span>
          <button class="ca-dt" type="button" id="caTo" aria-label="Đến ngày"><span class="v" id="caToT">—</span><span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span></button>
        </div>

        <div class="ca-frow">
          <div class="ca-f ca-f-last"><span class="ca-lb">Họ</span><input type="text" id="caLast" maxlength="50" placeholder="Nhập họ"></div>
          <div class="ca-f"><span class="ca-lb">Tên</span><input type="text" id="caFirst" maxlength="50" placeholder="Nhập tên"></div>
        </div>

        <div class="ca-f"><span class="ca-lb">Ngày sinh</span>
          <button class="ca-dt" type="button" id="caDob" aria-label="Ngày sinh"><span class="v ph" id="caDobT">Từ ngày - Đến ngày</span><span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span></button>
        </div>

        <div class="ca-f"><span class="ca-lb">Giới tính</span><div class="ca-ms" data-cams="gender" data-calabel="Giới tính"></div></div>
        <div class="ca-f"><span class="ca-lb">Số điện thoại</span><input type="text" id="caPhone" maxlength="20" placeholder="Nhập "></div>
        <div class="ca-f"><span class="ca-lb">Email</span><input type="text" id="caEmail" maxlength="80" placeholder="Nhập "></div>
        <div class="ca-f"><span class="ca-lb">Tỉnh/Thành phố</span><div class="ca-ms" data-cams="prov" data-calabel="Tỉnh/Thành phố"></div></div>
        <div class="ca-f"><span class="ca-lb">Quốc gia</span><div class="ca-ms" data-cams="country" data-calabel="Quốc gia"></div></div>
        <div class="ca-f"><span class="ca-lb">Quốc tịch</span><div class="ca-ms" data-cams="nation" data-calabel="Quốc tịch"></div></div>
        <div class="ca-f"><span class="ca-lb">Số CCCD</span><input type="text" id="caCccd" maxlength="20" placeholder="Nhập số CCCDD"></div>
        <div class="ca-f"><span class="ca-lb">Số hộ chiếu</span><input type="text" id="caPp" maxlength="20" placeholder="Nhập số hộ chiếu"></div>
        <div class="ca-f"><span class="ca-lb">Loại phòng</span><div class="ca-ms" data-cams="rtype" data-calabel="Loại phòng"></div></div>
        <div class="ca-f"><span class="ca-lb">Tên phòng</span><div class="ca-ms" data-cams="room" data-calabel="Tên phòng"></div></div>
      </div>
      <div class="ca-ffoot">
        <button class="ca-btn-primary" type="button" id="caApply">Áp dụng</button>
      </div>
    </aside>
  </div>
</div>

<!-- Menu multi-select dùng chung của màn báo cáo -->
<div class="ca-msmenu" id="caMsMenu" role="listbox" aria-label="Danh sách lựa chọn"></div>

<!-- ============ MÀN XEM BÁO CÁO DÙNG CHUNG (RR03 · RR04) — Figma 36378:125905 / 37738:14980 ============ -->
<div class="ca-bd" id="rvBd"></div>

<div class="ca rv" id="rvCard" role="dialog" aria-modal="true" aria-labelledby="rvA4Title">

  <!-- ---------- Thanh công cụ ---------- -->
  <header class="ca-nav">
    <div class="ca-tools">
      <button class="ca-ib" type="button" id="rvUndo" aria-label="Hoàn tác" title="Hoàn tác">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 14L4 9l5-5"/><path d="M4 9h11a5 5 0 010 10h-4"/></svg>
      </button>
      <button class="ca-ib" type="button" id="rvRedo" aria-label="Làm lại" title="Làm lại">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 14l5-5-5-5"/><path d="M20 9H9a5 5 0 000 10h4"/></svg>
      </button>
      <button class="ca-ib" type="button" id="rvClear" aria-label="Xoá bộ lọc" title="Xoá bộ lọc">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
      </button>
      <button class="ca-ib" type="button" id="rvReload" aria-label="Tải lại" title="Tải lại">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 12a8 8 0 11-2.3-5.7"/><path d="M20 4v5h-5"/></svg>
      </button>
      <button class="ca-ib" type="button" data-rvpg="first" aria-label="Trang đầu">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M17 6l-6 6 6 6M9 6l-6 6 6 6"/></svg>
      </button>
      <button class="ca-ib" type="button" data-rvpg="prev" aria-label="Trang trước">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 6l-6 6 6 6"/></svg>
      </button>
      <span class="ca-pgbox">
        <input type="text" id="rvPage" inputmode="numeric" value="1" aria-label="Số trang">
        <span class="of" id="rvPages">/1</span>
      </span>
      <button class="ca-ib" type="button" data-rvpg="next" aria-label="Trang sau">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 6l6 6-6 6"/></svg>
      </button>
      <button class="ca-ib" type="button" data-rvpg="last" aria-label="Trang cuối">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M7 6l6 6-6 6M15 6l6 6-6 6"/></svg>
      </button>
      <span class="ca-vr" aria-hidden="true"></span>
      <div class="ca-zoombox" role="group" aria-label="Thu phóng báo cáo">
        <button class="ca-ib" type="button" data-rvzoom="out" aria-label="Thu nhỏ" title="Thu nhỏ (Ctrl -)">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="11" cy="11" r="7"/><path d="M8 11h6M20 20l-3.5-3.5"/></svg>
        </button>
        <button class="ca-zoomlb" type="button" id="rvZoomLb" aria-label="Mức thu phóng, bấm để đặt lại 100%" title="Đặt lại 100%">100%</button>
        <button class="ca-ib" type="button" data-rvzoom="in" aria-label="Phóng to" title="Phóng to (Ctrl +)">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="11" cy="11" r="7"/><path d="M8 11h6M11 8v6M20 20l-3.5-3.5"/></svg>
        </button>
        <button class="ca-ib" type="button" data-rvzoom="fit" aria-label="Vừa bề ngang" title="Vừa bề ngang">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 9V5a2 2 0 012-2h4M21 9V5a2 2 0 00-2-2h-4M3 15v4a2 2 0 002 2h4M21 15v4a2 2 0 01-2 2h-4"/><path d="M7 12h10"/></svg>
        </button>
      </div>
    </div>

    <div class="ca-navr">
      <div class="ca-dl">
        <button class="ca-btn-out" type="button" id="rvDlBtn" aria-haspopup="menu" aria-expanded="false">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3v12"/><path d="M7 11l5 5 5-5"/><path d="M4 20h16"/></svg>
          Tải xuống
          <svg class="cv" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>
        </button>
        <div class="ca-dlmenu" id="rvDlMenu" role="menu">
          <button type="button" role="menuitem" data-rvdl="XLSM">XLSM</button>
          <button type="button" role="menuitem" data-rvdl="CSV">CSV</button>
          <button type="button" role="menuitem" data-rvdl="XML">XML</button>
        </div>
      </div>
      <button class="ca-btn-out" type="button" id="rvPrint">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9V3h12v6"/><rect x="4" y="9" width="16" height="7" rx="2"/><path d="M8 16h8v5H8z"/></svg>
        In
      </button>
      <span class="ca-vr" aria-hidden="true"></span>
      <button class="ca-ib" type="button" id="rvClose" aria-label="Đóng">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
      </button>
    </div>
  </header>

  <div class="ca-body">
    <!-- ---------- Trang A4 ---------- -->
    <div class="ca-viewer" id="rvViewer">
      <div class="ca-zoom" id="rvZoom"><article class="ca-a4" id="rvA4" aria-labelledby="rvA4Title"></article></div>
    </div>

    <!-- ---------- Bộ lọc ---------- -->
    <aside class="ca-filter" aria-label="Lọc dữ liệu">
      <div class="ca-fhead"><h3>Lọc dữ liệu</h3></div>
      <div class="ca-fbody" id="rvFBody"></div>
      <div class="ca-ffoot">
        <button class="ca-btn-primary" type="button" id="rvApply">Áp dụng</button>
      </div>
    </aside>
  </div>
</div>

<!-- ============ POPUP HỒ SƠ NGƯỜI DÙNG — Figma 22255:55976 ============ -->
<div class="pf-pop" id="pfPop" role="menu" aria-labelledby="pfName">

  <!-- Đầu: ảnh đại diện · tên · email -->
  <div class="pf-head">
    <span class="pf-ava">
      <img id="pfAva" src="data:image/svg+xml;utf8,%3Csvg xmlns='http://www.w3.org/2000/svg' width='80' height='80' viewBox='0 0 80 80'%3E%3Crect width='80' height='80' fill='%23EEEDF0'/%3E%3Ccircle cx='40' cy='31' r='14' fill='%23B7B4C0'/%3E%3Cpath d='M12 80c0-15.46 12.54-28 28-28s28 12.54 28 28z' fill='%23B7B4C0'/%3E%3C/svg%3E" alt="Ảnh đại diện người dùng">
      <span class="flag" aria-hidden="true">
        <svg viewBox="0 0 24 24"><rect width="24" height="24" fill="#DA251D"/><path d="M12 6.2l1.5 4.6h4.8l-3.9 2.8 1.5 4.6-3.9-2.9-3.9 2.9 1.5-4.6-3.9-2.8h4.8z" fill="#FFFF00"/></svg>
      </span>
    </span>
    <span class="pf-id">
      <span class="nm" id="pfName">Nguyễn Hoàng Long</span>
      <span class="em" id="pfEmail">Longhn@ezcloud.com</span>
    </span>
  </div>

  <div class="pf-div" role="separator"></div>

  <!-- Ngôn ngữ · Giao diện -->
  <div class="pf-grp">
    <button class="pf-row" type="button" role="menuitem" data-pf="lang">
      <span class="ic" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M4 5h7M9 3v2c0 4.418-2.239 8-5 8"/><path d="M5 9c0 2.144 2.952 3.908 6.7 4"/><path d="M12 20l4-9 4 9M19.1 18h-6.2"/></svg>
      </span>
      <span class="pf-lb">Ngôn ngữ</span>
      <span class="pf-opt">
        <span class="fg" aria-hidden="true">
          <svg viewBox="0 0 24 24"><rect y="4" width="24" height="16" fill="#DA251D"/><path d="M12 8.4l1.15 3.55h3.73l-3.02 2.19 1.16 3.55L12 15.49l-3.02 2.2 1.16-3.55-3.02-2.19h3.73z" fill="#FFFF00"/></svg>
        </span>
        <span class="v" id="pfLang">Tiếng Việt</span>
        <span class="cv" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg></span>
      </span>
    </button>

    <button class="pf-row" type="button" role="menuitem" data-pf="theme">
      <span class="ic" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 21a9 9 0 010-18c4.97 0 9 3.58 9 8 0 2.21-1.79 4-4 4h-2a2 2 0 00-1.6 3.2A2 2 0 0112 21z"/><circle cx="7.5" cy="10.5" r=".6" fill="currentColor"/><circle cx="12" cy="7.5" r=".6" fill="currentColor"/><circle cx="16.5" cy="10.5" r=".6" fill="currentColor"/></svg>
      </span>
      <span class="pf-lb">Giao diện</span>
      <span class="pf-opt">
        <span class="sun" aria-hidden="true">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M12 3v2M12 19v2M3 12h2M19 12h2M5.6 5.6l1.4 1.4M17 17l1.4 1.4M18.4 5.6L17 7M7 17l-1.4 1.4"/></svg>
        </span>
        <span class="v" id="pfTheme">Sáng</span>
        <span class="cv" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg></span>
      </span>
    </button>
  </div>

  <!-- Cài đặt tài khoản · Đổi mật khẩu -->
  <div class="pf-grp">
    <button class="pf-row" type="button" role="menuitem" data-pf="account">
      <span class="ic" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M10.3 4.3a1.8 1.8 0 013.4 0 1.7 1.7 0 002.6 1.1 1.8 1.8 0 012.3 2.3 1.7 1.7 0 001.1 2.6 1.8 1.8 0 010 3.4 1.7 1.7 0 00-1.1 2.6 1.8 1.8 0 01-2.3 2.3 1.7 1.7 0 00-2.6 1.1 1.8 1.8 0 01-3.4 0 1.7 1.7 0 00-2.6-1.1 1.8 1.8 0 01-2.3-2.3 1.7 1.7 0 00-1.1-2.6 1.8 1.8 0 010-3.4 1.7 1.7 0 001.1-2.6 1.8 1.8 0 012.3-2.3 1.7 1.7 0 002.6-1.1z"/><circle cx="12" cy="12" r="3"/></svg>
      </span>
      <span class="pf-lb">Cài đặt tài khoản</span>
    </button>

    <button class="pf-row" type="button" role="menuitem" data-pf="password">
      <span class="ic" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M4 8V6a2 2 0 012-2h2M16 4h2a2 2 0 012 2v2M20 16v2a2 2 0 01-2 2h-2M8 20H6a2 2 0 01-2-2v-2"/><rect x="8" y="11" width="8" height="6" rx="1"/><path d="M10 11V9.5a2 2 0 014 0V11"/></svg>
      </span>
      <span class="pf-lb">Đổi mật khẩu</span>
    </button>
  </div>

  <div class="pf-div" role="separator"></div>

  <!-- Hỗ trợ -->
  <div class="pf-grp">
    <button class="pf-row" type="button" role="menuitem" data-pf="docs">
      <span class="ic" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M3 5.5a9 9 0 019 1 9 9 0 019-1v12a9 9 0 00-9 1 9 9 0 00-9-1z"/><path d="M12 6.5v12"/></svg>
      </span>
      <span class="pf-lb">Tài liệu hướng dẫn</span>
    </button>

    <button class="pf-row" type="button" role="menuitem" data-pf="hotline">
      <span class="ic" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 4h4l2 5-2.5 1.5a11 11 0 005 5L15 13l5 2v4a2 2 0 01-2 2A16 16 0 013 6a2 2 0 012-2z"/></svg>
      </span>
      <span class="pf-lb">Hotline: <b>1900 6159</b></span>
    </button>

    <button class="pf-row" type="button" role="menuitem" data-pf="email">
      <span class="ic" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 7l9 6 9-6"/></svg>
      </span>
      <span class="pf-lb">hotro@ezCloud.vn</span>
    </button>

    <button class="pf-row" type="button" role="menuitem" data-pf="chat">
      <span class="ic" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M4 5h16a1 1 0 011 1v9a1 1 0 01-1 1h-9l-5 4v-4H4a1 1 0 01-1-1V6a1 1 0 011-1z"/><path d="M8 9h8M8 12h5"/></svg>
      </span>
      <span class="pf-lb">Chat với chúng tôi</span>
    </button>
  </div>

  <div class="pf-div" role="separator"></div>

  <div class="pf-act">
    <button class="pf-logout" type="button" role="menuitem" id="pfLogout">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14 8V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2h6a2 2 0 002-2v-2"/><path d="M20 12H10M17 9l3 3-3 3"/></svg>
      Đăng xuất
    </button>
  </div>
  <div class="pf-ver">v1.0.0</div>
</div>

<!-- Menu con của popup hồ sơ — Figma 19385:5067 (Giao diện) · 19385:5570 (Ngôn ngữ) -->
<div class="pf-menu" id="pfMenu" role="listbox" aria-label="Lựa chọn"></div>

<!-- ============ THÊM DỊCH VỤ (R01.14) — Figma 43142:300070 ============ -->
<div class="sv-bd" id="svBd"></div>

<div class="sv" id="svCard" role="dialog" aria-modal="true" aria-labelledby="svTitle">
  <header class="sv-head">
    <h2 id="svTitle">Thêm dịch vụ</h2>
    <button class="sv-x" type="button" id="svClose" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>

  <div class="sv-body">
    <!-- ---------- Trái: menu dịch vụ ---------- -->
    <div class="sv-left">
      <div class="sv-lhead">
        <div class="sv-lrow">
          <div class="qb-vs" data-qbsel="svRate" data-qblabel="Ratecode áp dụng"></div>
          <label class="sv-search">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" aria-hidden="true"><circle cx="11" cy="11" r="7"/><path d="M20 20l-3.5-3.5"/></svg>
            <input type="text" id="svQ" maxlength="80" placeholder="Tên, mã dịch vụ" aria-label="Tìm kiếm tên, mã dịch vụ">
          </label>
        </div>
        <div class="sv-tabs">
          <button class="sv-tarrow" type="button" data-svtnav="-1" aria-label="Danh mục trước">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 6l-6 6 6 6"/></svg>
          </button>
          <div class="sv-tstrip" id="svTabs" role="tablist" aria-label="Danh mục dịch vụ"></div>
          <button class="sv-tarrow" type="button" data-svtnav="1" aria-label="Danh mục sau">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 6l6 6-6 6"/></svg>
          </button>
        </div>
      </div>

      <div class="sv-grid" id="svGrid"></div>

      <div class="sv-page">
        <span class="lb">Dịch vụ/trang</span>
        <div class="qb-vs" data-qbsel="svPer" data-qblabel="Số dịch vụ mỗi trang" style="width:88px;flex:0 0 88px"></div>
        <div class="nav" id="svPager"></div>
      </div>
    </div>

    <!-- ---------- Phải: dịch vụ đã chọn ---------- -->
    <div class="sv-right">
      <div class="sv-rhead">
        <h3>Dịch vụ đã chọn</h3>
        <button class="sv-clear" type="button" id="svClear" hidden>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M4 7h16M10 11v6M14 11v6M6 7l1 13h10l1-13M9 7V4h6v3"/></svg>
          Xoá tất cả
        </button>
      </div>

      <div class="sv-tblwrap">
        <table class="sv-tbl">
          <thead><tr>
            <th id="svCount">Dịch vụ (0)</th>
            <th class="c">SL</th>
            <th>Đơn vị</th>
            <th class="num">Đơn giá</th>
            <th class="num">Thành tiền</th>
            <th></th>
          </tr></thead>
          <tbody id="svRows"></tbody>
        </table>
      </div>

      <div class="sv-sum">
        <div class="r"><span class="k">Tổng:</span><span class="v" id="svTotal">0</span></div>
        <div class="r"><span class="k">Giảm giá:</span><span class="v" id="svDisc">0</span></div>
        <div class="r big"><span class="k">Còn lại :</span><span class="v" id="svLeft">0</span></div>
      </div>
    </div>
  </div>

  <footer class="sv-foot">
    <button class="tl-btn-ghost" type="button" id="svCancel">Huỷ</button>
    <button class="tl-btn-primary" type="button" id="svSave">Cập nhật và thanh toán sau</button>
  </footer>
</div>

<!-- ---------- Popup Chi tiết dịch vụ ---------- -->
<div class="sv-dbd" id="svDBd"></div>
<div class="sv-dlg" id="svDlg" role="dialog" aria-modal="true" aria-labelledby="svDTitle">
  <header>
    <h3 id="svDTitle">Chi tiết dịch vụ</h3>
    <button class="sv-x" type="button" data-svd="close" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>

  <div class="sv-dbody">
    <div>
      <div class="sv-drow">
        <span class="k">Ngày sử dụng</span>
        <div class="f">
          <button class="rs-dt" type="button" id="svDDate"><span class="v">—</span><span class="ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span></button>
        </div>
      </div>
      <div class="sv-drow" style="margin-top:12px">
        <span class="k">Mã tham chiếu</span>
        <div class="f"><input type="text" id="svDRef" maxlength="20" placeholder="Nhập mã tham chiếu"></div>
      </div>
    </div>

    <div class="sv-tblwrap" style="max-height:280px">
      <table class="sv-tbl">
        <thead><tr>
          <th>Dịch vụ</th><th class="c">Số lượng</th><th>Đơn vị</th>
          <th class="num">Đơn giá</th><th class="num">Thành tiền</th>
        </tr></thead>
        <tbody id="svDRows"></tbody>
      </table>
    </div>

    <div class="sv-dck">
      <label class="sv-ck"><input type="checkbox" id="svDNet">
        <span class="box" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span>
        <span class="t">Giá net</span></label>
      <label class="sv-ck"><input type="checkbox" id="svDInc">
        <span class="box" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span>
        <span class="t">Dịch vụ đã bao gồm trong tiền phòng</span></label>
    </div>

    <div class="sv-sum" style="align-items:flex-end">
      <div class="r"><span class="k">Tổng:</span><span class="v" id="svDTotal">0</span></div>
      <div class="r"><span class="k">Giảm giá:</span><span class="v" id="svDDiscV">0</span></div>
    </div>

    <div class="sv-disc">
      <div class="sv-drow">
        <span class="k">Danh mục giảm giá</span>
        <div class="f" style="max-width:none"><div class="qb-vs" data-qbsel="svDCat" data-qblabel="Danh mục giảm giá"></div></div>
      </div>
      <div class="sv-drow">
        <span class="k">Mức giảm giá</span>
        <div class="f two" style="max-width:none;display:flex">
          <input type="number" id="svDPct" min="0" max="100" value="0" aria-label="Mức giảm giá phần trăm">
          <input type="number" id="svDAmt" min="0" value="0" aria-label="Số tiền giảm giá">
        </div>
      </div>
      <div class="sv-drow">
        <span class="k">Lý do giảm giá</span>
        <div class="f" style="max-width:none"><div class="qb-vs" data-qbsel="svDWhy" data-qblabel="Lý do giảm giá"></div></div>
      </div>
    </div>

    <div class="sv-sum" style="align-items:flex-end">
      <div class="r"><span class="k">Thành tiền:</span><span class="v" id="svDNetV">0</span></div>
    </div>

    <div>
      <div class="sv-drow" style="align-items:flex-start">
        <span class="k">Ghi chú</span>
        <div class="f" style="max-width:none"><input type="text" id="svDNote" maxlength="500" placeholder="Nhập"></div>
      </div>
    </div>
  </div>

  <footer class="sv-dfoot">
    <button class="tl-btn-ghost" type="button" data-svd="close">Huỷ</button>
    <button class="tl-btn-primary" type="button" data-svd="save">Lưu</button>
  </footer>
</div>

<!-- ============ QUY TRÌNH TRẢ PHÒNG (R01.21) — Figma 45799:283821 ============ -->
<div class="co-bd" id="coBd"></div>

<div class="co" id="coCard" role="dialog" aria-modal="true" aria-labelledby="coTitle">
  <header class="co-head">
    <h2 id="coTitle">Quy trình trả phòng</h2>
    <span class="co-chip" id="coChip">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M7 10l-3 3 3 3M4 13h12M17 14l3-3-3-3M20 11H8"/></svg>
      Trưởng đoàn thanh toán
      <span class="n" id="coChipN">2</span>
    </span>
    <button class="co-x" type="button" id="coClose" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>

  <nav class="co-steps" aria-label="Các bước trả phòng">
    <button class="co-step is-cur" type="button" data-costep="1" aria-current="step">
      <span class="no">1</span><span class="tx">Xác nhận thời gian trả phòng</span>
    </button>
    <span class="co-sep" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6-6 6"/></svg></span>
    <button class="co-step" type="button" data-costep="2" disabled>
      <span class="no">2</span><span class="tx">Thanh toán và trả phòng</span>
    </button>
    <span class="co-sep" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6-6 6"/></svg></span>
    <button class="co-step" type="button" data-costep="3" disabled>
      <span class="no">3</span><span class="tx">Xuất hoá đơn điện tử</span>
    </button>
  </nav>

  <!-- ---------- Thanh công cụ bước 1 ---------- -->
  <div class="co-tools" id="coTools1">
    <span class="co-hint">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M12 11v5M12 8h.01" stroke-linecap="round"/></svg>
      Cập nhật thời gian trả để tính toán tiền phòng
    </span>
    <button class="tl-btn-outline" type="button" id="coUpdTime">Cập nhật thời gian trả phòng</button>
    <button class="tl-btn-outline" type="button" id="coAddRoom">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M12 5v14M5 12h14"/></svg>
      Thêm phòng
    </button>
    <div class="co-print">
      <button class="tl-btn-outline" type="button" data-coprint="main">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9V3h12v6"/><rect x="4" y="9" width="16" height="7" rx="2"/><path d="M7 16h10v5H7z"/></svg>
        In hoá đơn
      </button>
      <button class="co-print-tg" type="button" data-coprint="menu" aria-label="In hoá đơn" aria-haspopup="menu" aria-expanded="false">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>
      </button>
    </div>
  </div>

  <!-- ---------- Thanh công cụ bước 2 ---------- -->
  <div class="co-tools" id="coTools2" hidden>
    <div class="co-tabs" role="tablist" aria-label="Lọc theo trạng thái thanh toán">
      <button class="co-tab" type="button" role="tab" data-cotab="all" aria-selected="true">Tất cả (0)</button>
      <button class="co-tab" type="button" role="tab" data-cotab="unpaid" aria-selected="false">Chưa thanh toán (0)</button>
      <button class="co-tab" type="button" role="tab" data-cotab="paid" aria-selected="false">Đã thanh toán (0)</button>
    </div>
    <div class="co-print">
      <button class="tl-btn-outline" type="button" data-coprint="main">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9V3h12v6"/><rect x="4" y="9" width="16" height="7" rx="2"/><path d="M7 16h10v5H7z"/></svg>
        In hoá đơn
      </button>
      <button class="co-print-tg" type="button" data-coprint="menu" aria-label="In hoá đơn" aria-haspopup="menu" aria-expanded="false">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>
      </button>
    </div>
  </div>

  <div class="co-body" id="coBody">
    <div class="co-tblwrap"><table class="co-tbl" id="coTbl"></table></div>
    <div class="co-sum" id="coSum"></div>
  </div>

  <!-- ---------- Bước 3 ---------- -->
  <div class="co-done" id="coDone" hidden>
    <span class="ic" aria-hidden="true">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M8 12.5l2.5 2.5L16 9.5"/></svg>
    </span>
    <h3>Trả phòng thành công</h3>
    <p>Hệ thống đã ghi nhận trả phòng và sẵn sàng phát hành hoá đơn điện tử cho khách.</p>
    <div class="co-inv" id="coInv"></div>
  </div>

  <footer class="co-foot" id="coFoot">
    <span class="co-note" id="coFootNote" hidden>
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M12 11v5M12 8h.01" stroke-linecap="round"/></svg>
      Đã dừng tính tiền phòng
    </span>
    <span class="sp"></span>
    <button class="tl-btn-ghost" type="button" id="coBack" hidden>
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M19 12H5M11 6l-6 6 6 6"/></svg>
      Quay lại
    </button>
    <button class="co-btn-all" type="button" id="coPayAll" hidden>Thanh toán toàn bộ: 0</button>
    <button class="tl-btn-primary" type="button" id="coNext">
      Đến bước thanh toán
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
    </button>
  </footer>
</div>

<!-- ---------- Popup Cập nhật thời gian trả phòng ---------- -->
<div class="co-dbd" id="coDBd"></div>
<div class="co-dlg" id="coDlg" role="dialog" aria-modal="true" aria-labelledby="coDTitle">
  <header>
    <h3 id="coDTitle">Cập nhật thời gian trả phòng</h3>
    <button class="co-x" type="button" data-cod="close" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>
  <div class="co-dbody">
    <div class="f">
      <span class="k">Thời gian trả phòng</span>
      <button class="rs-dt" type="button" id="coDTime"><span class="v">—</span><span class="ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span></button>
    </div>
    <label class="sv-ck"><input type="checkbox" id="coDAll" checked>
      <span class="box" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span>
      <span class="t">Đồng bộ cho tất cả phòng đang chọn</span></label>
  </div>
  <footer class="co-dfoot">
    <button class="tl-btn-ghost" type="button" data-cod="close">Huỷ</button>
    <button class="tl-btn-primary" type="button" data-cod="ok">Xác nhận</button>
  </footer>
</div>

<!-- ============ KIỂM TOÁN ĐÊM — CÁC LỚP PHỦ (FD01) ============ -->
<div class="na-bd" id="naBd"></div>

<!-- Danh sách phòng đến hạn trả / không đến — Figma 43747:114416 · 43747:136218 -->
<div class="na-dlg" id="naListDlg" role="dialog" aria-modal="true" aria-labelledby="naListTitle">
  <header class="na-dhead">
    <h3 id="naListTitle">Danh sách phòng đến hạn trả (0)</h3>
    <button class="na-x" type="button" data-na="listclose" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>
  <div class="na-dtools">
    <button class="tl-btn-outline" type="button" data-na="download">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3v12M7 11l5 5 5-5M4 20h16"/></svg>
      Tải danh sách
    </button>
    <button class="tl-btn-outline" type="button" data-na="print">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9V3h12v6"/><rect x="3" y="9" width="18" height="8" rx="2"/><path d="M6 17h12v4H6z"/></svg>
      In danh sách
    </button>
  </div>
  <div class="na-dbody"><table class="na-tbl" id="naListTbl"></table></div>
  <footer class="na-dfoot">
    <span class="na-dnote" id="naListHint">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M12 11v5M12 8h.01" stroke-linecap="round"/></svg>
      <span id="naListHintTx">Chọn nhiều để Trả phòng hoặc gia hạn thêm 1 đêm</span>
    </span>
    <span class="sp"></span>
    <button class="tl-btn-outline" type="button" id="naActB">Gia hạn thêm 1 đêm</button>
    <button class="tl-btn-primary" type="button" id="naActA">Trả phòng</button>
  </footer>
</div>

<!-- Dialog nhỏ: xác nhận kiểm toán / chuyển ngày kinh doanh -->
<div class="na-sdbd" id="naSdBd"></div>

<div class="na-sdlg" id="naConfirmDlg" role="dialog" aria-modal="true" aria-labelledby="naConfirmTitle">
  <header>
    <h3 id="naConfirmTitle">Xác nhận thực hiện kiểm toán đêm</h3>
    <button class="na-x" type="button" data-na="cfmclose" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>
  <div class="na-sdbody"><p id="naConfirmMsg" style="margin:0"></p></div>
  <footer class="na-sdfoot">
    <button class="tl-btn-ghost" type="button" data-na="cfmclose">Hủy</button>
    <button class="tl-btn-primary" type="button" id="naConfirmOk">Xác nhận</button>
  </footer>
</div>

<div class="na-sdlg" id="naDateDlg" role="dialog" aria-modal="true" aria-labelledby="naDateTitle">
  <header>
    <h3 id="naDateTitle">Chuyển ngày kinh doanh</h3>
    <button class="na-x" type="button" data-na="dateclose" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>
  <div class="na-sdbody">
    <div class="na-warn" id="naDateWarn"></div>
    <div class="na-sdf">
      <span class="k">Chuyển ngày kinh doanh</span>
      <button class="rs-dt" type="button" id="naDatePick"><span class="v">—</span><span class="ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span></button>
    </div>
  </div>
  <footer class="na-sdfoot">
    <button class="tl-btn-ghost" type="button" data-na="dateclose">Hủy</button>
    <button class="tl-btn-primary" type="button" id="naDateOk">Xác nhận</button>
  </footer>
</div>

<!-- ===== ĐẶT PHÒNG NHANH (Quick Booking) — Figma 46683:170162 / 46701:175379 ===== -->
<div class="qb-backdrop" id="qbBackdrop"></div>

<!-- Gợi ý khách đã có dữ liệu (Figma 47787:87196) — panel trái, sát mép panel đặt phòng -->
<aside class="qb-sugg" id="qbSugg" role="region" aria-label="Gợi ý khách đã có dữ liệu trong khách sạn">
  <div class="qb-sugg-head">Gợi ý khách đã có dữ liệu trong khách sạn</div>
  <div class="qb-sugg-list" id="qbSuggList" role="listbox" aria-label="Danh sách khách gợi ý"></div>
</aside>

<aside class="qb" id="qbPanel" role="dialog" aria-modal="true" aria-labelledby="qbTitle" hidden>

  <header class="qb-head">
    <h2 id="qbTitle">Đặt phòng nhanh</h2>
    <button class="qb-x" type="button" data-qb-close aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>

  <!-- Tabs: Đặt trước / Nhận phòng ngay -->
  <div class="qb-tabwrap">
    <div class="qb-tabs" role="tablist" aria-label="Hình thức đặt phòng">
      <button class="qb-tab" type="button" role="tab" data-qbtab="prebook" aria-selected="true" id="qbTabPre">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M3 10h18M8 3v4M16 3v4M9 15l2 2 4-4"/></svg>
        Đặt trước
      </button>
      <button class="qb-tab" type="button" role="tab" data-qbtab="checkin" aria-selected="false" id="qbTabIn">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 18v-8a2 2 0 012-2h9a4 4 0 014 4v6M3 14h18M3 18h18M7 11h2"/></svg>
        Nhận phòng ngay
      </button>
    </div>
  </div>

  <div class="qb-body" id="qbBody">

    <div class="qb-2col">

      <!-- ============ Nguồn đặt phòng ============ -->
      <section class="qb-card">
        <div class="qb-cardhead">
          <span class="qb-mark"></span>
          <h3>Nguồn đặt phòng</h3>
          <div class="qb-chips" role="group" aria-label="Trạng thái đặt phòng">
            <button class="qb-chip" type="button" data-qbstatus="definite" aria-pressed="true">Chắc chắn đặt</button>
            <button class="qb-chip" type="button" data-qbstatus="tentative" aria-pressed="false">Dự kiến</button>
          </div>
        </div>

        <div class="qb-grid">
          <div class="qb-f qb-c12">
            <span class="qb-lb">Công ty</span>
            <div class="qb-inline">
              <div class="qb-vs" data-qbsel="qbCompany" data-qblabel="Công ty"></div>
              <button class="qb-ibtn" type="button" id="qbCompanyEdit" aria-label="Sửa thông tin công ty">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 20h9M16.5 3.5a2.1 2.1 0 013 3L7 19l-4 1 1-4 12.5-12.5z"/></svg>
              </button>
            </div>
          </div>

          <div class="qb-f qb-c6">
            <span class="qb-lb">Thị trường</span>
            <div class="qb-vs" data-qbsel="qbMarket" data-qblabel="Thị trường"></div>
          </div>
          <div class="qb-f qb-c6">
            <span class="qb-lb">Nguồn</span>
            <div class="qb-vs" data-qbsel="qbSource" data-qblabel="Nguồn"></div>
          </div>

          <div class="qb-f qb-c12">
            <span class="qb-lb">Nhân viên</span>
            <div class="qb-vs" data-qbsel="qbStaff" data-qblabel="Nhân viên"></div>
          </div>

          <div class="qb-f qb-c12">
            <label class="qb-lb" for="qbNote">Ghi chú</label>
            <textarea class="qb-ctrl" id="qbNote" placeholder="Nhập ghi chú" maxlength="700"></textarea>
          </div>
        </div>
      </section>

      <!-- ============ Thông tin lưu trú ============ -->
      <section class="qb-card">
        <div class="qb-cardhead">
          <span class="qb-mark"></span>
          <h3>Thông tin lưu trú</h3>
        </div>

        <div class="qb-grid">
          <div class="qb-f qb-c12">
            <span class="qb-lb">Thời gian đặt phòng<i class="req">*</i></span>
            <div class="qb-stay">
              <button class="qb-dt" type="button" id="qbIn" aria-label="Ngày giờ đến">
                <span class="d" id="qbInD">—</span>
                <span class="t" id="qbInT">—</span>
              </button>
              <span class="qb-arrow" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M4 12h16M15 7l5 5-5 5"/></svg>
              </span>
              <button class="qb-dt" type="button" id="qbOut" aria-label="Ngày giờ đi">
                <span class="d" id="qbOutD">—</span>
                <span class="t" id="qbOutT">—</span>
              </button>
              <span class="qb-vr"></span>
              <div class="qb-step">
                <button type="button" id="qbNightMinus" aria-label="Giảm số đêm">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" aria-hidden="true"><path d="M5 12h14"/></svg>
                </button>
                <span class="qb-nights" id="qbNights" aria-live="polite">1 đêm</span>
                <button type="button" id="qbNightPlus" aria-label="Tăng số đêm">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" aria-hidden="true"><path d="M12 5v14M5 12h14"/></svg>
                </button>
              </div>
            </div>
          </div>

          <div class="qb-f qb-c6">
            <span class="qb-lb">Loại phòng<i class="req">*</i></span>
            <div class="qb-vs" data-qbsel="qbType" data-qblabel="Loại phòng"></div>
            <span class="qb-msg">Không được để trống</span>
          </div>
          <div class="qb-f qb-c6">
            <span class="qb-lb">Phòng<i class="req">*</i></span>
            <div class="qb-vs" data-qbsel="qbRoom" data-qblabel="Phòng"></div>
            <span class="qb-msg">Không được để trống</span>
          </div>

          <div class="qb-f qb-c6">
            <span class="qb-lb">Gói giá<i class="req">*</i></span>
            <div class="qb-vs" data-qbsel="qbRate" data-qblabel="Gói giá"></div>
            <span class="qb-msg">Không được để trống</span>
          </div>
          <div class="qb-f qb-c6">
            <span class="qb-lb">Tạm tính</span>
            <div class="qb-readout"><span></span><span><span class="v" id="qbSubtotal">0</span><span class="u"> VND</span></span></div>
          </div>

          <div class="qb-f qb-c4">
            <label class="qb-lb" for="qbAdults">Người lớn<i class="req">*</i></label>
            <div class="qb-paxbox">
              <span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="7" r="3.4"/><path d="M4.5 20a7.5 7.5 0 0115 0"/></svg></span>
              <input type="number" id="qbAdults" min="1" max="99" step="1" value="2">
              <span class="qb-spin">
                <button type="button" data-qbpax="qbAdults" data-qbdelta="1" aria-label="Tăng người lớn"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 15l6-6 6 6"/></svg></button>
                <button type="button" data-qbpax="qbAdults" data-qbdelta="-1" aria-label="Giảm người lớn"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg></button>
              </span>
            </div>
          </div>
          <div class="qb-f qb-c4">
            <label class="qb-lb" for="qbChildren">Trẻ em<i class="req">*</i></label>
            <div class="qb-paxbox">
              <span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="3"/><path d="M6.5 20a5.5 5.5 0 0111 0"/></svg></span>
              <input type="number" id="qbChildren" min="0" max="99" step="1" value="0">
              <span class="qb-spin">
                <button type="button" data-qbpax="qbChildren" data-qbdelta="1" aria-label="Tăng trẻ em"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 15l6-6 6 6"/></svg></button>
                <button type="button" data-qbpax="qbChildren" data-qbdelta="-1" aria-label="Giảm trẻ em"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg></button>
              </span>
            </div>
          </div>
          <div class="qb-f qb-c4">
            <label class="qb-lb" for="qbInfants">Em bé<i class="req">*</i></label>
            <div class="qb-paxbox">
              <span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="9" r="3.6"/><path d="M8 20c0-2.2 1.8-4 4-4s4 1.8 4 4"/></svg></span>
              <input type="number" id="qbInfants" min="0" max="99" step="1" value="0">
              <span class="qb-spin">
                <button type="button" data-qbpax="qbInfants" data-qbdelta="1" aria-label="Tăng em bé"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 15l6-6 6 6"/></svg></button>
                <button type="button" data-qbpax="qbInfants" data-qbdelta="-1" aria-label="Giảm em bé"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg></button>
              </span>
            </div>
          </div>
        </div>
      </section>
    </div>

    <!-- ============ Khách đại diện ============ -->
    <section class="qb-card">
      <div class="qb-cardhead">
        <span class="qb-mark"></span>
        <h3>Khách đại diện</h3>
      </div>

      <div class="qb-grid">
        <!-- Hàng 1 — Định danh -->
        <div class="qb-f qb-c2">
          <span class="qb-lb">Danh xưng</span>
          <div class="qb-vs" data-qbsel="qbSalut" data-qblabel="Danh xưng"></div>
        </div>
        <div class="qb-f qb-c3">
          <label class="qb-lb" for="qbLast">Họ<i class="req">*</i></label>
          <input class="qb-ctrl" id="qbLast" placeholder="Nhập họ" maxlength="60" autocomplete="off">
          <span class="qb-msg">Không được để trống</span>
        </div>
        <div class="qb-f qb-c4">
          <label class="qb-lb" for="qbFirst">Tên khách<i class="req">*</i></label>
          <input class="qb-ctrl" id="qbFirst" placeholder="Nhập tên khách" maxlength="60" autocomplete="off">
          <span class="qb-msg">Không được để trống</span>
        </div>
        <div class="qb-f qb-c3">
          <span class="qb-lb">Giới tính<i class="req">*</i></span>
          <div class="qb-vs" data-qbsel="qbGender" data-qblabel="Giới tính"></div>
          <span class="qb-msg">Không được để trống</span>
        </div>

        <!-- Hàng 2 — Liên hệ -->
        <div class="qb-f qb-c2">
          <label class="qb-lb" for="qbDob">Ngày sinh</label>
          <span class="qb-inwrap has-suffix">
            <input class="qb-ctrl" id="qbDob" placeholder="dd/mm/yyyy" maxlength="10" inputmode="numeric" autocomplete="off">
            <button class="qb-suffix" type="button" data-qbcal="qbDob" aria-label="Chọn ngày sinh">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M3 10h18M8 3v4M16 3v4"/></svg>
            </button>
          </span>
          <span class="qb-msg">Không được để trống</span>
        </div>
        <div class="qb-f qb-c3">
          <span class="qb-lb">Quốc tịch</span>
          <div class="qb-vs" data-qbsel="qbNation" data-qblabel="Quốc tịch"></div>
        </div>
        <div class="qb-f qb-c4">
          <label class="qb-lb" for="qbPhone">Số điện thoại</label>
          <div class="qb-inline" style="gap:0">
            <div class="qb-vs" data-qbsel="qbDial" data-qblabel="Mã vùng điện thoại" data-qbjoin="l" style="width:76px;flex:0 0 76px"></div>
            <input class="qb-ctrl" id="qbPhone" placeholder="Nhập số điện thoại" maxlength="20" inputmode="numeric" autocomplete="off" style="border-top-left-radius:0;border-bottom-left-radius:0;border-left:0">
          </div>
        </div>
        <div class="qb-f qb-c3">
          <label class="qb-lb" for="qbEmail">Email</label>
          <input class="qb-ctrl" id="qbEmail" placeholder="Nhập email" maxlength="120" autocomplete="off">
        </div>

        <!-- Hàng 3 — Giấy tờ -->
        <div class="qb-f qb-c3">
          <span class="qb-lb">Loại giấy tờ</span>
          <div class="qb-vs" data-qbsel="qbDocType" data-qblabel="Loại giấy tờ"></div>
        </div>
        <div class="qb-f qb-c6">
          <label class="qb-lb" for="qbDocNo">Số giấy tờ</label>
          <input class="qb-ctrl" id="qbDocNo" placeholder="Nhập số giấy tờ" maxlength="40" autocomplete="off">
        </div>
        <div class="qb-f qb-c3">
          <label class="qb-lb" for="qbDocExp">Ngày đến hạn</label>
          <span class="qb-inwrap has-suffix">
            <input class="qb-ctrl" id="qbDocExp" placeholder="dd/mm/yyyy" maxlength="10" inputmode="numeric" autocomplete="off">
            <button class="qb-suffix" type="button" data-qbcal="qbDocExp" aria-label="Chọn ngày đến hạn">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M3 10h18M8 3v4M16 3v4"/></svg>
            </button>
          </span>
        </div>
      </div>
    </section>

    <!-- ============ Thông tin thanh toán ============ -->
    <section class="qb-card">
      <div class="qb-cardhead">
        <span class="qb-mark"></span>
        <h3>Thông tin thanh toán</h3>
      </div>

      <div class="qb-grid">
        <div class="qb-f qb-c6">
          <span class="qb-lb">Phương thức thanh toán</span>
          <div class="qb-paymode" role="group" aria-label="Phương thức thanh toán">
            <button type="button" data-qbpaymode="pay" aria-pressed="true">Thanh toán</button>
            <button type="button" data-qbpaymode="deposit" aria-pressed="false">Đặt cọc</button>
          </div>
        </div>
        <div class="qb-f qb-c6">
          <span class="qb-lb">Tổng tiền phòng</span>
          <div class="qb-readout is-total"><span></span><span><span class="v" id="qbTotal">0</span><span class="u"> VND</span></span></div>
        </div>

        <div class="qb-f qb-c4">
          <span class="qb-lb">Hình thức thanh toán</span>
          <div class="qb-vs" data-qbsel="qbPayMethod" data-qblabel="Hình thức thanh toán"></div>
        </div>
        <div class="qb-f qb-c2">
          <span class="qb-lb">Tiền tệ</span>
          <div class="qb-vs" data-qbsel="qbCurrency" data-qblabel="Tiền tệ"></div>
        </div>
        <div class="qb-f qb-c6">
          <label class="qb-lb" for="qbAmount">Số tiền thanh toán</label>
          <input class="qb-ctrl" id="qbAmount" placeholder="Nhập số tiền thanh toán" inputmode="numeric" autocomplete="off" style="text-align:right">
          <span class="qb-msg" id="qbAmountMsg">Số tiền không hợp lệ. Vui lòng nhập lại!</span>
        </div>

        <div class="qb-f qb-c5" id="qbCompanyDebtWrap" hidden>
          <span class="qb-lb">Chọn công ty<i class="req">*</i></span>
          <div class="qb-vs" data-qbsel="qbDebtCompany" data-qblabel="Chọn công ty"></div>
          <span class="qb-msg">Không được để trống</span>
        </div>
        <div class="qb-c12" id="qbQuickWrap">
          <div class="qb-quick" role="group" aria-label="Chọn nhanh mức thanh toán">
            <span class="k">Chọn nhanh mức thanh toán:</span>
            <button type="button" data-qbpct="0" aria-pressed="false">0%</button>
            <button type="button" data-qbpct="30" aria-pressed="false">30%</button>
            <button type="button" data-qbpct="50" aria-pressed="false">50%</button>
            <button type="button" data-qbpct="100" aria-pressed="false">100%</button>
          </div>
        </div>

      </div>
    </section>
  </div>

  <footer class="qb-foot">
    <div class="qb-remain" id="qbRemain">
      <span class="k" id="qbRemainLabel">Tiền phòng còn lại</span>
      <span class="v"><span class="dot"></span><span class="amt" id="qbRemainAmt">0</span><span class="u">VND</span></span>
    </div>
    <div class="qb-actions">
      <button class="qb-cancel" type="button" data-qb-close>Hủy</button>
      <button class="qb-ok" type="button" id="qbSubmit">Hoàn tất đặt trước</button>
    </div>
  </footer>
</aside>

<!-- Menu nổi dùng chung cho mọi select kiểu Vuexy -->
<div class="qb-vsmenu" id="qbVsMenu" role="listbox" aria-label="Danh sách lựa chọn"></div>

<!-- Bộ lịch ngày + giờ (Figma 45163:286669) -->
<div class="qb-dtp" id="qbDtp" role="dialog" aria-label="Chọn ngày giờ">
  <div class="qb-dtp-head">
    <div class="qb-dtp-title">
      <select id="qbDtpMonth" aria-label="Tháng"></select>
      <select id="qbDtpYear" aria-label="Năm"></select>
    </div>
    <div class="qb-dtp-nav">
      <button type="button" data-qbdtp="prev" aria-label="Tháng trước">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M19 12H5M11 6l-6 6 6 6"/></svg>
      </button>
      <button type="button" data-qbdtp="next" aria-label="Tháng sau">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
      </button>
    </div>
  </div>
  <div class="qb-dtp-wd" id="qbDtpWd" aria-hidden="true"></div>
  <div class="qb-dtp-days" id="qbDtpDays" role="grid" aria-label="Lịch"></div>
  <div class="qb-dtp-time" id="qbDtpTime">
    <div class="cell h">
      <input type="number" id="qbDtpH" min="0" max="23" step="1" aria-label="Giờ">
      <span class="qb-dtp-spin">
        <button type="button" data-qbdtp="h+" aria-label="Tăng giờ"><svg viewBox="0 0 5 4" fill="currentColor" aria-hidden="true"><path d="M2.5 0L5 4H0z"/></svg></button>
        <button type="button" data-qbdtp="h-" aria-label="Giảm giờ"><svg viewBox="0 0 5 4" fill="currentColor" aria-hidden="true"><path d="M2.5 4L0 0h5z"/></svg></button>
      </span>
    </div>
    <div class="dots">:</div>
    <div class="cell">
      <input type="number" id="qbDtpM" min="0" max="59" step="1" aria-label="Phút">
      <span class="qb-dtp-spin">
        <button type="button" data-qbdtp="m+" aria-label="Tăng phút"><svg viewBox="0 0 5 4" fill="currentColor" aria-hidden="true"><path d="M2.5 0L5 4H0z"/></svg></button>
        <button type="button" data-qbdtp="m-" aria-label="Giảm phút"><svg viewBox="0 0 5 4" fill="currentColor" aria-hidden="true"><path d="M2.5 4L0 0h5z"/></svg></button>
      </span>
    </div>
  </div>
  <div class="qb-dtp-foot">
    <button type="button" class="ghost" data-qbdtp="cancel">Đóng</button>
    <button type="button" data-qbdtp="ok">Chọn</button>
  </div>
</div>

<!-- Loading khi hệ thống tạo đặt phòng (Figma 55583:60835) -->
<div class="qb-load" id="qbLoad" role="alertdialog" aria-live="assertive" aria-labelledby="qbLoadT1">
  <div class="box">
    <span class="qb-ring">
      <svg viewBox="0 0 76 76" aria-hidden="true">
        <circle class="trk" cx="38" cy="38" r="32" fill="none" stroke-width="6"></circle>
        <circle class="bar" id="qbRingBar" cx="38" cy="38" r="32" fill="none" stroke-width="6"
                stroke-dasharray="201.06" stroke-dashoffset="201.06"></circle>
      </svg>
      <span class="pct" id="qbLoadPct">0%</span>
    </span>
    <span class="t1" id="qbLoadT1">Hệ thống đang tạo đặt phòng</span>
    <span class="t2">Vui lòng đợi trong giây lát</span>
  </div>
</div>

<div class="qb-qrbd" id="qbQrBd"></div>

<!-- Popup Thanh toán QR (Figma 48500:303200) -->
<div class="qb-qrdlg" id="qbQrDlg" role="dialog" aria-modal="true" aria-labelledby="qbQrDlgTitle">
  <header>
    <h2 id="qbQrDlgTitle">Thanh toán QR</h2>
    <button class="qb-x" type="button" data-qbqr="close" aria-label="Đóng">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
  </header>
  <div class="bd">
    <div class="ok-bar">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M8.5 12.5l2.5 2.5 4.5-5"/></svg>
      <span id="qbQrBooking">Đã tạo booking</span>
    </div>

    <div class="qr-row">
      <div class="qr-box">
        <span class="brand" aria-label="VietQR">V<i>ietQR</i></span>
        <svg viewBox="0 0 96 96" fill="none" aria-hidden="true"><g fill="#091135"><rect x="4" y="4" width="24" height="24" rx="3" fill="none" stroke="#091135" stroke-width="5"/><rect x="68" y="4" width="24" height="24" rx="3" fill="none" stroke="#091135" stroke-width="5"/><rect x="4" y="68" width="24" height="24" rx="3" fill="none" stroke="#091135" stroke-width="5"/><rect x="12" y="12" width="8" height="8"/><rect x="76" y="12" width="8" height="8"/><rect x="12" y="76" width="8" height="8"/><rect x="36" y="4" width="6" height="6"/><rect x="36" y="16" width="6" height="6"/><rect x="48" y="10" width="6" height="6"/><rect x="36" y="28" width="6" height="6"/><rect x="58" y="4" width="6" height="6"/><rect x="58" y="22" width="6" height="6"/><rect x="4" y="36" width="6" height="6"/><rect x="16" y="36" width="6" height="6"/><rect x="28" y="36" width="6" height="6"/><rect x="40" y="40" width="6" height="6"/><rect x="52" y="36" width="6" height="6"/><rect x="64" y="40" width="6" height="6"/><rect x="76" y="36" width="6" height="6"/><rect x="88" y="36" width="6" height="6"/><rect x="4" y="52" width="6" height="6"/><rect x="22" y="48" width="6" height="6"/><rect x="34" y="52" width="6" height="6"/><rect x="46" y="52" width="6" height="6"/><rect x="58" y="48" width="6" height="6"/><rect x="70" y="52" width="6" height="6"/><rect x="82" y="52" width="6" height="6"/><rect x="36" y="58" width="6" height="6"/><rect x="48" y="64" width="6" height="6"/><rect x="60" y="58" width="6" height="6"/><rect x="72" y="64" width="6" height="6"/><rect x="84" y="66" width="6" height="6"/><rect x="36" y="70" width="6" height="6"/><rect x="48" y="82" width="6" height="6"/><rect x="60" y="76" width="6" height="6"/><rect x="72" y="82" width="6" height="6"/><rect x="84" y="82" width="6" height="6"/><rect x="36" y="86" width="6" height="6"/><rect x="60" y="88" width="6" height="6"/></g></svg>
      </div>
      <div class="qr-info">
        <div class="r"><span class="k">Ngân hàng:</span><span class="v" id="qbQrBank">Vietcombank</span></div>
        <div class="r"><span class="k">Số tài khoản:</span><span class="v" id="qbQrAcc">950493857843</span></div>
        <div class="r"><span class="k">Nội dung:</span><span class="v" id="qbQrDesc">—</span></div>
        <div class="r"><span class="k">Số tiền:</span><span class="v" id="qbQrMoney">0</span></div>
        <hr>
        <span class="note" id="qbQrTtl">Mã QR có hiệu lực trong 15:00 • VietQR</span>
      </div>
    </div>

    <p class="hint">Hệ thống sẽ tự động đối soát khi khách hàng thực hiện thanh toán thành công</p>
  </div>
  <footer>
    <button class="ghost" type="button" data-qbqr="print">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9V4h12v5"/><path d="M6 18H4a1 1 0 01-1-1v-6a1 1 0 011-1h16a1 1 0 011 1v6a1 1 0 01-1 1h-2"/><rect x="7" y="14" width="10" height="7" rx="1"/></svg>
      In hoá đơn
    </button>
    <button class="primary" type="button" data-qbqr="later">
      Thanh toán sau và xem booking
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
    </button>
  </footer>
</div>

<!-- Panel Cột hiển thị (Figma 33422:114094) -->
<div class="lb-bd" id="lbBd"></div>
<aside class="lb-drawer" id="lbColsDlg" role="dialog" aria-modal="true" aria-labelledby="lbColsTitle">
  <header>
    <div class="hd">
      <h2 id="lbColsTitle">Cột hiển thị</h2>
      <button class="qb-x" type="button" data-lbcols="close" aria-label="Đóng">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>
      </button>
    </div>
    <span class="lb-inwrap has-pre">
      <span class="pre" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"><circle cx="11" cy="11" r="7"/><path d="M20 20l-3.5-3.5"/></svg></span>
      <input class="lb-ctrl" id="lbColsSearch" placeholder="Nhập tên dữ liệu" autocomplete="off">
    </span>
  </header>
  <div class="bd" id="lbColsBody"></div>
  <footer>
    <button class="lb-btn" type="button" data-lbcols="close">Huỷ</button>
    <button class="lb-btn primary" type="button" data-lbcols="apply">Áp dụng</button>
  </footer>
</aside>

<!-- Menu chọn danh sách mẫu -->
<div class="lb-lists" id="lbLists" role="listbox" aria-label="Danh sách mẫu"></div>
<script id="tl-script">
/* =====================================================================
   Sơ đồ phòng — Timeline  (ezOne.PMS)
   Single-file, không phụ thuộc ngoài. Dữ liệu mẫu hard-code.
   ===================================================================== */
(function () {
  'use strict';

  /* ---------------- 0. Hằng số & tiện ích ---------------- */
  var LS_KEY = 'ezone.pms.timeline.v1';
  var CELL_W = 48;               // Figma: cột ngày 48px
  var ROOM_W = 180;              // cột phòng — đồng bộ với --tl-room-col (đọc lại từ CSS trong init)
  // Đồng hồ khách sạn = đồng hồ thật của máy. Cập nhật mỗi CLOCK_MS → thanh đỏ tự trôi.
  var HOTEL_NOW = new Date();
  var CLOCK_MS = 10000;

  var WD = ['CN', 'T2', 'T3', 'T4', 'T5', 'T6', 'T7'];

  // Nhãn lấy đúng theo web production (ezOne.Pms bản test) — "Có khách"
  var STATUS = {
    booked:     { label: 'Đã đặt',                     color: '#186FF4', rgb: '24,111,244'   },
    inhouse:    { label: 'Có khách',                   color: '#DA4343', rgb: '218,67,67'    },
    checkout:   { label: 'Đã trả',                     color: '#9C27B0', rgb: '156,39,176'   },
    repair:     { label: 'Sửa',                        color: '#2F2B3DB3', rgb: '47,43,61'   },
    dirty:      { label: 'Bẩn',                        color: '#B56613', rgb: '181,102,19'   }
  };

  function pad(n) { return n < 10 ? '0' + n : '' + n; }
  function ymd(d) { return d.getFullYear() + '-' + pad(d.getMonth() + 1) + '-' + pad(d.getDate()); }
  function dmy(d) { return pad(d.getDate()) + '/' + pad(d.getMonth() + 1) + '/' + d.getFullYear(); }
  function hm(d) { return pad(d.getHours()) + ':' + pad(d.getMinutes()); }
  function startOfDay(d) { return new Date(d.getFullYear(), d.getMonth(), d.getDate()); }
  function addDays(d, n) { var x = new Date(d); x.setDate(x.getDate() + n); return x; }
  function addMonths(d, n) { var x = new Date(d); x.setMonth(x.getMonth() + n); return x; }
  function sameDay(a, b) { return a.getFullYear() === b.getFullYear() && a.getMonth() === b.getMonth() && a.getDate() === b.getDate(); }
  function dayDiff(a, b) { return Math.round((startOfDay(a) - startOfDay(b)) / 86400000); }
  function money(n) { return n.toLocaleString('vi-VN'); }
  function esc(s) {
    return String(s == null ? '' : s).replace(/[&<>"']/g, function (c) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c];
    });
  }
  function $(id) { return document.getElementById(id); }

  /* ---------------- 1. Icon (inline SVG) ---------------- */
  var I = {
    caret: '<svg class="tl-grp-caret" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>',
    calcheck: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="17" rx="2"/><path d="M8 2v4M16 2v4M3 10h18"/><path d="M9 15.5l2 2 4-4"/></svg>',
    userplus: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="9" cy="8" r="3.5"/><path d="M2.5 20c0-3.3 2.9-5.5 6.5-5.5s6.5 2.2 6.5 5.5"/><path d="M18.5 8.5v6M21.5 11.5h-6"/></svg>',
    broom: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M19 4l-7 7M11 10l3 3-5 5H4v-5z"/><path d="M7 15l2 2"/></svg>',
    cone: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3l7 16H5z"/><path d="M8.5 12h7M3 21h18"/></svg>',
    users: '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="9" cy="8" r="3.2"/><path d="M3 19c0-3.3 2.7-5.5 6-5.5s6 2.2 6 5.5z"/><circle cx="17" cy="9" r="2.6" opacity=".75"/><path d="M15.4 14.2c2.9-.6 5.6 1.2 5.6 4.1h-4.4c0-1.6-.5-3-1.2-4.1z" opacity=".75"/></svg>',
    info: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M12 11v5M12 8h.01"/></svg>',
    login: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="17" rx="2"/><path d="M8 2v4M16 2v4M3 10h18"/><path d="M9.5 15.5l2 2 3.5-3.5"/></svg>',
    logout: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14 4H6a2 2 0 00-2 2v12a2 2 0 002 2h8"/><path d="M17 8l4 4-4 4M21 12H10"/></svg>',
    card: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="7" y="3" width="10" height="18" rx="2"/><path d="M3 12h6M6 9l-3 3 3 3"/></svg>',
    unassign: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="17" rx="2"/><path d="M8 2v4M16 2v4M3 10h18"/><path d="M10 14l4 4M14 14l-4 4"/></svg>',
    service: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="5" y="4" width="14" height="17" rx="2"/><path d="M9 4V3h6v1M12 10v6M9 13h6"/></svg>',
    swapdate: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="17" rx="2"/><path d="M8 2v4M16 2v4M3 10h18"/><path d="M10 16l-2-2 2-2M14 12l2 2-2 2"/></svg>',
    door: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="6" y="3" width="12" height="18" rx="1.5"/><circle cx="14.5" cy="12" r="1" fill="currentColor"/></svg>',
    swap: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 9h14l-3-3M20 15H6l3 3"/></svg>',
    copy: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="9" y="9" width="12" height="12" rx="2"/><path d="M5 15H4a1 1 0 01-1-1V4a1 1 0 011-1h10a1 1 0 011 1v1"/></svg>',
    undo: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="17" rx="2"/><path d="M8 2v4M16 2v4M3 10h18"/><path d="M9 15h5a2 2 0 110 4h-1M9 15l2-2M9 15l2 2"/></svg>',
    cancel: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="17" rx="2"/><path d="M8 2v4M16 2v4M3 10h18"/><path d="M10 14l4 4M14 14l-4 4"/></svg>',
    list: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" aria-hidden="true"><path d="M9 6h11M9 12h11M9 18h11M4 6h.01M4 12h.01M4 18h.01"/></svg>',
    phone: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 3h3l2 5-2 1a12 12 0 006 6l1-2 5 2v3a2 2 0 01-2 2A17 17 0 013 5a2 2 0 012-2z"/></svg>',
    building: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="4" y="3" width="10" height="18"/><path d="M14 8h6v13H4M7 7h1M11 7h1M7 11h1M11 11h1M7 15h1M11 15h1M17 12h1M17 16h1"/></svg>',
    monitor: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="12" rx="2"/><path d="M8 20h8M12 16v4"/></svg>',
    globe: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3a15 15 0 010 18M12 3a15 15 0 000 18"/></svg>',
    user: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="8" r="3.5"/><path d="M5 20c0-3.9 3.1-6.5 7-6.5s7 2.6 7 6.5"/></svg>',
    note: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 3h10l4 4v14H5z"/><path d="M15 3v4h4"/></svg>',
    noteLock: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 3h10l4 4v14H5z"/><path d="M15 3v4h4M10 13h4v4h-4zM11 13v-1.5a1 1 0 012 0V13"/></svg>',
    calRange: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="17" rx="2"/><path d="M8 2v4M16 2v4M3 10h18"/></svg>',
    bed: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 8v11M3 12h18v7M21 19v-7a3 3 0 00-3-3h-7v3"/><circle cx="7" cy="11" r="1.6"/></svg>',
    lock: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="5" y="10" width="14" height="10" rx="2"/><path d="M8 10V7a4 4 0 018 0v3"/></svg>',
    crown: '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M4 8l3.5 3L12 5l4.5 6L20 8l-1.5 10h-13z"/></svg>',
    check: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>',
    alert: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3l9 17H3z"/><path d="M12 9v5M12 17h.01"/></svg>',
    adult: '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="9" cy="7" r="2.6"/><path d="M4 19c0-3 2.2-5 5-5s5 2 5 5z"/><circle cx="17" cy="8" r="2.2" opacity=".7"/><path d="M14.6 13.4c2.6-.4 5.4 1.3 5.4 4.1h-3.7c0-1.6-.7-3.1-1.7-4.1z" opacity=".7"/></svg>',
    child: '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="12" cy="8" r="3"/><path d="M7 20c0-3 2.2-5.2 5-5.2s5 2.2 5 5.2z"/></svg>',
    baby: '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="12" cy="10" r="4.5"/><path d="M8 20c0-2 1.8-3.4 4-3.4s4 1.4 4 3.4z"/></svg>',
    calCheck: '<svg class="ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/><path d="M9.5 14.5l2 2 3.5-3.5"/></svg>',
    dots: '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="5" cy="12" r="1.6"/><circle cx="12" cy="12" r="1.6"/><circle cx="19" cy="12" r="1.6"/></svg>',
    pencil: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 20h4l10.5-10.5a2.1 2.1 0 10-3-3L5 17v3z"/><path d="M13.5 6.5l4 4"/></svg>',
    trash: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 7h16M10 11v6M14 11v6"/><path d="M6 7l1 13h10l1-13M9 7V4h6v3"/></svg>',
    close: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>',
    copy20: '<svg viewBox="0 0 24 24" fill="none" stroke="#186FF4" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="8" y="8" width="12" height="12" rx="2"/><path d="M16 8V6a2 2 0 00-2-2H6a2 2 0 00-2 2v8a2 2 0 002 2h2"/></svg>',
    users16: '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="9" cy="8" r="3.2"/><path d="M3 19c0-3.3 2.7-5.5 6-5.5s6 2.2 6 5.5z"/><circle cx="17" cy="9" r="2.6" opacity=".75"/><path d="M15.4 14.2c2.9-.6 5.6 1.2 5.6 4.1h-4.4c0-1.6-.5-3-1.2-4.1z" opacity=".75"/></svg>',
    cake: '<svg viewBox="0 0 24 24" fill="none" stroke="#EA5455" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 20h18v-6a3 3 0 00-3-3H6a3 3 0 00-3 3z"/><path d="M12 8V5M9 8V6M15 8V6M3 16c2 1.4 4 1.4 6 0s4-1.4 6 0 4 1.4 6 0"/></svg>',
    returning: '<svg viewBox="0 0 24 24" fill="none" stroke="#808390" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>'
  };
  I.copy16 = I.copy20;
  function flagSVG(cc) {
    var m = {
      VN: '<svg class="tl-flag" viewBox="0 0 30 20" aria-hidden="true"><rect width="30" height="20" fill="#DA251D"/><path d="M15 5l1.6 4.9h5.2l-4.2 3 1.6 4.9-4.2-3-4.2 3 1.6-4.9-4.2-3h5.2z" fill="#FF0"/></svg>',
      KR: '<svg class="tl-flag" viewBox="0 0 30 20" aria-hidden="true"><rect width="30" height="20" fill="#fff"/><circle cx="15" cy="10" r="5" fill="#CD2E3A"/><path d="M10 10a5 5 0 0110 0 2.5 2.5 0 01-5 0 2.5 2.5 0 00-5 0z" fill="#0047A0"/></svg>',
      JP: '<svg class="tl-flag" viewBox="0 0 30 20" aria-hidden="true"><rect width="30" height="20" fill="#fff"/><circle cx="15" cy="10" r="5.5" fill="#BC002D"/></svg>',
      GB: '<svg class="tl-flag" viewBox="0 0 30 20" aria-hidden="true"><rect width="30" height="20" fill="#012169"/><path d="M0 0l30 20M30 0L0 20" stroke="#fff" stroke-width="4"/><path d="M15 0v20M0 10h30" stroke="#fff" stroke-width="6"/><path d="M15 0v20M0 10h30" stroke="#C8102E" stroke-width="3"/></svg>'
    };
    return m[cc] || m.VN;
  }
  // Ảnh đại diện nguồn/công ty — production dùng logo thật trên nền #F2F2F2
  function avatarSVG(txt) {
    return '<svg viewBox="0 0 24 24" aria-hidden="true"><rect width="24" height="24" rx="12" fill="#F2F2F2"/>' +
      '<text x="12" y="16.5" font-size="11" font-family="Roboto,sans-serif" font-weight="500" fill="#2F2B3D" text-anchor="middle">' + esc(txt) + '</text></svg>';
  }

  /* ---------------- 2. Dữ liệu mẫu ---------------- */
  var TYPES = [
    { id: 'SGL', name: 'Phòng đơn' },
    { id: 'DOR', name: 'Phòng tập thể' },
    { id: 'TWN', name: 'Phòng đôi 2 giường' },
    { id: 'TRP', name: 'Phòng ba' },
    { id: 'DBL', name: 'Phòng đôi 1 giường' },
    { id: 'FAM', name: 'Phòng gia đình' },
    { id: 'STD', name: 'Phòng tiêu chuẩn' },
    { id: 'SUP', name: 'Phòng cao cấp' }
  ];

  var ROOMS = [
    /* SGL — tầng 1 */
    { id: '101', type: 'SGL', dirty: true, light: true  },
    { id: '102', type: 'SGL', dirty: true, light: false },
    { id: '103', type: 'SGL', dirty: false, light: false },
    { id: '104', type: 'SGL', dirty: false, light: false },
    { id: '105', type: 'SGL', dirty: false, light: false },
    { id: '106', type: 'SGL', dirty: false, light: true  },
    { id: '107', type: 'SGL', dirty: false, light: false },
    { id: '108', type: 'SGL', dirty: false, light: false },
    { id: '109', type: 'SGL', dirty: true, light: false },
    { id: '110', type: 'SGL', dirty: false, light: false },
    /* DOR — tầng 1 */
    { id: '111', type: 'DOR', dirty: false, light: false },
    { id: '112', type: 'DOR', dirty: true, light: false },
    { id: '113', type: 'DOR', dirty: false, light: false },
    { id: '114', type: 'DOR', dirty: false, light: false },
    { id: '115', type: 'DOR', dirty: false, light: true  },
    { id: '116', type: 'DOR', dirty: false, light: false },
    /* TWN — tầng 2 */
    { id: '201', type: 'TWN', dirty: false, light: false },
    { id: '202', type: 'TWN', dirty: true, light: false },
    { id: '203', type: 'TWN', dirty: false, light: false },
    { id: '204', type: 'TWN', dirty: false, light: true  },
    { id: '205', type: 'TWN', dirty: false, light: false },
    { id: '206', type: 'TWN', dirty: false, light: false },
    { id: '207', type: 'TWN', dirty: true, light: false },
    { id: '208', type: 'TWN', dirty: false, light: false },
    { id: '209', type: 'TWN', dirty: false, light: false },
    /* TRP — tầng 2 */
    { id: '210', type: 'TRP', dirty: false, light: false },
    { id: '211', type: 'TRP', dirty: false, light: false },
    { id: '212', type: 'TRP', dirty: true, light: false },
    { id: '213', type: 'TRP', dirty: false, light: false },
    { id: '214', type: 'TRP', dirty: false, light: false },
    { id: '215', type: 'TRP', dirty: false, light: true  },
    { id: '216', type: 'TRP', dirty: false, light: false },
    /* DBL — tầng 3 */
    { id: '301', type: 'DBL', dirty: false, light: false },
    { id: '302', type: 'DBL', dirty: false, light: false },
    { id: '303', type: 'DBL', dirty: true, light: false },
    { id: '304', type: 'DBL', dirty: false, light: false },
    { id: '305', type: 'DBL', dirty: false, light: true  },
    { id: '306', type: 'DBL', dirty: false, light: false },
    { id: '307', type: 'DBL', dirty: false, light: false },
    { id: '308', type: 'DBL', dirty: true, light: false },
    { id: '309', type: 'DBL', dirty: false, light: false },
    { id: '310', type: 'DBL', dirty: false, light: false },
    /* FAM — tầng 3 */
    { id: '311', type: 'FAM', dirty: false, light: true  },
    { id: '312', type: 'FAM', dirty: false, light: false },
    { id: '313', type: 'FAM', dirty: false, light: false },
    { id: '314', type: 'FAM', dirty: true, light: false },
    { id: '315', type: 'FAM', dirty: false, light: false },
    { id: '316', type: 'FAM', dirty: false, light: false },
    /* STD — tầng 4 */
    { id: '401', type: 'STD', dirty: false, light: false },
    { id: '402', type: 'STD', dirty: true, light: false },
    { id: '403', type: 'STD', dirty: false, light: false },
    { id: '404', type: 'STD', dirty: false, light: false },
    { id: '405', type: 'STD', dirty: false, light: true  },
    { id: '406', type: 'STD', dirty: false, light: false },
    { id: '407', type: 'STD', dirty: false, light: false },
    { id: '408', type: 'STD', dirty: false, light: false },
    /* SUP — tầng 4 */
    { id: '409', type: 'SUP', dirty: true, light: false },
    { id: '410', type: 'SUP', dirty: false, light: false },
    { id: '411', type: 'SUP', dirty: false, light: false },
    { id: '412', type: 'SUP', dirty: false, light: false },
    { id: '413', type: 'SUP', dirty: false, light: false },
    { id: '414', type: 'SUP', dirty: true, light: true  },
    { id: '415', type: 'SUP', dirty: false, light: false }
  ];

  // DD(offset, h, mi) — dữ liệu mẫu neo tương đối vào NGÀY HÔM NAY để demo luôn có dữ liệu
  var DATA_EPOCH = startOfDay(new Date());
  function DD(off, h, mi) {
    var d = new Date(DATA_EPOCH);
    d.setDate(d.getDate() + off);
    d.setHours(h || 0, mi || 0, 0, 0);
    return d;
  }

  // st: booked | inhouse | checkout | repair
  var BOOKINGS = [
    { id: 'BK01', room: '101', st: 'inhouse', name: 'Đào Hoàng Anh', code: '#938471', in: DD(-3,14,0), out: DD(6,12,0),
      group: null, pay: 'partial', total: 12500000, paid: 4000000, adults: 2, children: 1, infants: 0,
      src: 'Booking.com', ota: '#OTA118', cms: '#CMS042', company: 'Công ty CP Du lịch Sen Vàng', market: 'Nội địa', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: true, note: 'Khách yêu cầu phòng tầng cao, xa thang máy. Khách muốn yên tĩnh để nghỉ ngơi nên tránh làm phiền, lễ tân nhớ nhắc nhở các nhân viên nhé. Khách muốn yên tĩnh để nghỉ ngơi nên tránh làm phiền lễ tân nhớ nhắc nhở các nhân viên.',
      inote: 'Khách VIP — ưu tiên xử lý nhanh.',
      prev: { room: 'Phòng đôi 2 giường - 203', in: DD(-8, 12, 0), out: DD(-3, 14, 0) },
      next: { room: 'Phòng đôi 1 giường - 304', in: DD(7, 12, 0), out: DD(9, 14, 0) },
      guests: [ { n: 'Đào Hoàng Anh', p: '0123 456 789', cc: 'VN', lead: true }, { n: 'Trần Thu Hà', p: '0987 654 321', cc: 'VN' } ] },

    { id: 'BK02', room: '104', st: 'booked', name: 'Nguyễn Trọng Việt', code: '#938472', in: DD(4,14,0), out: DD(8,12,0),
      group: { gid: 'G1', name: 'Đoàn công ty ezCloud toàn cầu', color: '#00A2E1', rooms: 4, lead: 'Mr. Trần Minh Quân', leadPhone: '0123 456 789' },
      pay: 'none', total: 24000000, paid: 0, adults: 2, children: 0, infants: 0,
      src: 'Agoda.com', ota: '#OTA321', cms: '#CMS777', company: 'ezCloud Global JSC', market: 'Châu Á', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: true, note: 'Khách muốn yên tĩnh để nghỉ ngơi nên tránh làm phiền.', inote: '(Nội bộ) Nhắc lễ tân chuẩn bị nước suối miễn phí.',
      guests: [ { n: 'Mr. Trần Minh Quân', p: '0123 456 789', cc: 'VN', lead: true }, { n: 'Nguyễn Văn Thảo Nguyên', p: '0123 456 789', cc: 'VN' }, { n: 'Nguyễn Việt Quang', p: '0123 456 789', cc: 'VN' } ] },

    { id: 'BK03', room: '105', st: 'inhouse', name: 'Lê Minh Châu', code: '#938473', in: DD(-2,14,0), out: DD(0,12,0),
      group: null, pay: 'paid', total: 6800000, paid: 6800000, adults: 1, children: 0, infants: 0,
      src: 'Trực tiếp', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Phạm Thu Trang',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Lê Minh Châu', p: '0912 333 444', cc: 'VN', lead: true } ] },

    { id: 'BK04', room: '107', st: 'checkout', name: 'Hoàng Bảo Nam', code: '#938474', in: DD(-4,14,0), out: DD(-1,12,0),
      group: null, pay: 'paid', total: 9200000, paid: 9200000, adults: 2, children: 2, infants: 1,
      src: 'Expedia', ota: '#OTA556', cms: '', company: 'Công ty TNHH Hà Anh', market: 'Nội địa', staff: 'Phạm Thu Trang',
      confirmed: true, note: 'Cần thêm 1 giường phụ.', inote: '',
      guests: [ { n: 'Hoàng Bảo Nam', p: '0933 121 212', cc: 'VN', lead: true } ] },

    { id: 'BK05', room: '109', st: 'booked', name: 'Nguyễn Trần Minh Quân', code: '#938475', in: DD(5,14,0), out: DD(16,12,0),
      group: { gid: 'G1', name: 'Đoàn công ty ezCloud toàn cầu', color: '#00A2E1', rooms: 4, lead: 'Mr. Trần Minh Quân', leadPhone: '0123 456 789' },
      pay: 'partial', total: 24000000, paid: 500000, adults: 2, children: 1, infants: 0,
      src: 'Agoda.com', ota: '#OTA321', cms: '#CMS777', company: 'ezCloud Global JSC', market: 'Châu Á', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: true, note: 'Khách muốn yên tĩnh để nghỉ ngơi nên tránh làm phiền lễ tân nhớ nhắc nhở các nhân viên nhé.',
      inote: '(Nội bộ) Khách muốn yên tĩnh để nghỉ ngơi nên tránh làm phiền lễ tân nhớ nhắc nhở các nhân viên nhé.',
      prev: { room: 'Standard - 203', in: DD(1, 12, 0), out: DD(5, 14, 0) },
      next: { room: 'Standard - 304', in: DD(16, 12, 0), out: DD(18, 14, 0) },
      guests: [ { n: 'Mr. Trần Minh Quân', p: '0123 456 789', cc: 'VN', lead: true }, { n: 'Nguyễn Văn Thảo Nguyên', p: '0123 456 789', cc: 'VN' }, { n: 'Nguyễn Việt Quang', p: '0123 456 789', cc: 'VN' } ] },

    { id: 'BK06', room: '110', st: 'repair', repair: 'OOO', name: 'Sửa phòng', code: '', in: DD(-3,12,0), out: DD(2,14,0),
      group: null, pay: 'none', total: 0, paid: 0, adults: 0, children: 0, infants: 0, note: 'Phòng tạm ngừng phục vụ để kiểm tra lại thiết bị nội thất', inote: '' },

    { id: 'BK07', room: '102', st: 'repair', repair: 'OOS', sellable: true, name: 'Sửa phòng', code: '', in: DD(3,12,0), out: DD(7,14,0),
      group: null, pay: 'none', total: 0, paid: 0, adults: 0, children: 0, infants: 0, note: 'Phòng tạm ngừng phục vụ để kiểm tra lại thiết bị nội thất', inote: '' },

    { id: 'BK08', room: '103', st: 'booked', name: 'Vũ Khánh Linh', code: '#938476', in: DD(0,14,0), out: DD(3,12,0),
      group: null, pay: 'partial', total: 5400000, paid: 2000000, adults: 1, children: 0, infants: 0,
      src: 'Traveloka', ota: '#OTA901', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Thị Hiền',
      confirmed: false, note: '', inote: 'Chưa xác nhận — cần gọi lại.',
      guests: [ { n: 'Vũ Khánh Linh', p: '0977 888 999', cc: 'VN', lead: true } ] },

    { id: 'BK09', room: '106', st: 'inhouse', name: 'Kim Min Jun', code: '#938477', in: DD(-1,14,0), out: DD(4,12,0),
      group: null, pay: 'paid', total: 15600000, paid: 15600000, adults: 2, children: 0, infants: 0,
      src: 'Booking.com', ota: '#OTA221', cms: '', company: 'Samsung Vietnam', market: 'Hàn Quốc', staff: 'Nguyễn Thị Hiền',
      confirmed: true, note: 'Khách ăn chay.', inote: '',
      guests: [ { n: 'Kim Min Jun', p: '0906 555 111', cc: 'KR', lead: true } ] },

    { id: 'BK10', room: '108', st: 'booked', name: 'Trần Quốc Bảo', code: '#938478', in: DD(9,14,0), out: DD(12,12,0),
      group: null, pay: 'none', total: 7200000, paid: 0, adults: 2, children: 1, infants: 0,
      src: 'Trực tiếp', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Phạm Thu Trang',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Trần Quốc Bảo', p: '0944 222 333', cc: 'VN', lead: true } ] },

    { id: 'BK11', room: '201', st: 'inhouse', name: 'Sato Haruki', code: '#938479', in: DD(-1,14,0), out: DD(10,12,0),
      group: null, pay: 'partial', total: 31000000, paid: 10000000, adults: 2, children: 0, infants: 1,
      src: 'Booking.com', ota: '#OTA338', cms: '', company: 'Marubeni Corp', market: 'Nhật Bản', staff: 'Nguyễn Thị Hiền',
      confirmed: true, note: 'Yêu cầu nôi em bé.', inote: '',
      guests: [ { n: 'Sato Haruki', p: '0908 777 666', cc: 'JP', lead: true } ] },

    { id: 'BK12', room: '203', st: 'booked', name: 'Đoàn Sen Vàng', code: '#938480', in: DD(6,14,0), out: DD(11,12,0),
      group: { gid: 'G1', name: 'Đoàn công ty ezCloud toàn cầu', color: '#00A2E1', rooms: 4, lead: 'Mr. Trần Minh Quân', leadPhone: '0123 456 789' },
      pay: 'none', total: 18000000, paid: 0, adults: 3, children: 0, infants: 0,
      src: 'Agoda.com', ota: '#OTA321', cms: '#CMS777', company: 'ezCloud Global JSC', market: 'Châu Á', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Mr. Trần Minh Quân', p: '0123 456 789', cc: 'VN', lead: true } ] },

    { id: 'BK13', room: '204', st: 'checkout', name: 'Phạm Gia Hân', code: '#938481', in: DD(-5,14,0), out: DD(-2,12,0),
      group: null, pay: 'paid', total: 8800000, paid: 8800000, adults: 2, children: 0, infants: 0,
      src: 'Trực tiếp', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Phạm Thu Trang',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Phạm Gia Hân', p: '0966 111 222', cc: 'VN', lead: true } ] },

    { id: 'BK14', room: '206', st: 'booked', name: 'John Carter', code: '#938482', in: DD(13,14,0), out: DD(19,12,0),
      group: null, pay: 'partial', total: 27500000, paid: 5000000, adults: 2, children: 2, infants: 0,
      src: 'Expedia', ota: '#OTA610', cms: '', company: 'HSBC Vietnam', market: 'Châu Âu', staff: 'Nguyễn Thị Hiền',
      confirmed: true, note: 'Late check-out nếu còn phòng.', inote: '',
      guests: [ { n: 'John Carter', p: '0901 234 567', cc: 'GB', lead: true } ] },

    { id: 'BK15', room: '301', st: 'checkout', name: 'Ngô Thanh Tùng', code: '#938483', in: DD(-6,14,0), out: DD(-1,12,0),
      group: null, pay: 'paid', total: 11200000, paid: 11200000, adults: 2, children: 0, infants: 0,
      src: 'Trực tiếp', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Phạm Thu Trang',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Ngô Thanh Tùng', p: '0922 888 111', cc: 'VN', lead: true } ] },

    { id: 'BK16', room: '302', st: 'booked', name: 'Bùi Thảo My', code: '#938484', in: DD(0,14,0), out: DD(2,12,0),
      group: null, pay: 'none', total: 4600000, paid: 0, adults: 1, children: 0, infants: 0,
      src: 'Traveloka', ota: '#OTA455', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Thị Hiền',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Bùi Thảo My', p: '0955 444 333', cc: 'VN', lead: true } ] },

    { id: 'BK17', room: '304', st: 'inhouse', name: 'Đặng Hoài Phương', code: '#938485', in: DD(-3,14,0), out: DD(2,12,0),
      group: null, pay: 'paid', total: 13400000, paid: 13400000, adults: 2, children: 1, infants: 0,
      src: 'Booking.com', ota: '#OTA778', cms: '', company: '', market: 'Nội địa', staff: 'Phạm Thu Trang',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Đặng Hoài Phương', p: '0988 121 343', cc: 'VN', lead: true } ] },

    { id: 'BK18', room: '306', st: 'repair', repair: 'OOO', name: 'Sửa phòng', code: '', in: DD(8,12,0), out: DD(14,14,0),
      group: null, pay: 'none', total: 0, paid: 0, adults: 0, children: 0, infants: 0, note: 'Thay sàn gỗ.', inote: '' },

    { id: 'BK20', room: '111', st: 'booked', name: 'Trần Quang Huy', code: '#938720', in: DD(-4,14,0), out: DD(-1,12,0),
      group: null, pay: 'paid', total: 360000, paid: 360000, adults: 1, children: 0, infants: 0,
      src: 'Trực tiếp', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Hoàng Kiều Trinh',
      confirmed: false, note: '', inote: '',
      guests: [ { n: 'Trần Quang Huy', p: '0910 100 200', cc: 'VN', lead: true } ] },

    { id: 'BK21', room: '112', st: 'booked', name: 'Lê Thị Mai', code: '#938721', in: DD(-3,14,0), out: DD(1,12,0),
      group: null, pay: 'partial', total: 480000, paid: 240000, adults: 1, children: 0, infants: 0,
      src: 'Booking.com', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Lê Thị Mai', p: '0911 107 203', cc: 'VN', lead: true } ] },

    { id: 'BK22', room: '113', st: 'booked', name: 'Phạm Văn Đức', code: '#938722', in: DD(-2,14,0), out: DD(1,12,0),
      group: null, pay: 'none', total: 360000, paid: 0, adults: 1, children: 0, infants: 0,
      src: 'Agoda.com', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Phạm Thu Trang',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Phạm Văn Đức', p: '0912 114 206', cc: 'VN', lead: true } ] },

    { id: 'BK23', room: '114', st: 'booked', name: 'Nguyễn Thu Hằng', code: '#938723', in: DD(-1,14,0), out: DD(4,12,0),
      group: null, pay: 'paid', total: 600000, paid: 600000, adults: 1, children: 0, infants: 0,
      src: 'Traveloka', ota: '', cms: '', company: '', market: 'Châu Á', staff: 'Nguyễn Hoàng Kiều Trinh',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Nguyễn Thu Hằng', p: '0913 121 209', cc: 'VN', lead: true } ] },

    { id: 'BK24', room: '115', st: 'booked', name: 'Hoàng Minh Tuấn', code: '#938724', in: DD(-1,14,0), out: DD(1,12,0),
      group: null, pay: 'partial', total: 240000, paid: 120000, adults: 1, children: 0, infants: 0,
      src: 'Expedia', ota: '', cms: '', company: '', market: 'Quốc tế', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: false, note: '', inote: '',
      guests: [ { n: 'Hoàng Minh Tuấn', p: '0914 128 212', cc: 'VN', lead: true } ] },

    { id: 'BK25', room: '116', st: 'booked', name: 'Vũ Thị Nhung', code: '#938725', in: DD(0,14,0), out: DD(3,12,0),
      group: null, pay: 'none', total: 360000, paid: 0, adults: 1, children: 0, infants: 0,
      src: 'Điện thoại', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Phạm Thu Trang',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Vũ Thị Nhung', p: '0915 135 215', cc: 'VN', lead: true } ] },

    { id: 'BK26', room: '202', st: 'booked', name: 'Đỗ Anh Khoa', code: '#938726', in: DD(0,14,0), out: DD(4,12,0),
      group: null, pay: 'paid', total: 1400000, paid: 1400000, adults: 2, children: 0, infants: 0,
      src: 'Walk-in', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Hoàng Kiều Trinh',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Đỗ Anh Khoa', p: '0916 142 218', cc: 'VN', lead: true } ] },

    { id: 'BK27', room: '205', st: 'booked', name: 'Bùi Thanh Thảo', code: '#938727', in: DD(1,14,0), out: DD(3,12,0),
      group: null, pay: 'partial', total: 700000, paid: 350000, adults: 2, children: 0, infants: 0,
      src: 'Trực tiếp', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Bùi Thanh Thảo', p: '0917 149 221', cc: 'VN', lead: true } ] },

    { id: 'BK28', room: '207', st: 'booked', name: 'Đặng Quốc Cường', code: '#938728', in: DD(1,14,0), out: DD(4,12,0),
      group: null, pay: 'none', total: 1050000, paid: 0, adults: 2, children: 0, infants: 0,
      src: 'Booking.com', ota: '', cms: '', company: '', market: 'Châu Á', staff: 'Phạm Thu Trang',
      confirmed: false, note: '', inote: '',
      guests: [ { n: 'Đặng Quốc Cường', p: '0918 156 224', cc: 'VN', lead: true } ] },

    { id: 'BK29', room: '208', st: 'booked', name: 'Ngô Thị Kim Chi', code: '#938729', in: DD(2,14,0), out: DD(6,12,0),
      group: null, pay: 'paid', total: 1400000, paid: 1400000, adults: 2, children: 0, infants: 0,
      src: 'Agoda.com', ota: '', cms: '', company: '', market: 'Quốc tế', staff: 'Nguyễn Hoàng Kiều Trinh',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Ngô Thị Kim Chi', p: '0919 163 227', cc: 'VN', lead: true } ] },

    { id: 'BK30', room: '209', st: 'booked', name: 'Lý Văn Thành', code: '#938730', in: DD(2,14,0), out: DD(4,12,0),
      group: null, pay: 'partial', total: 700000, paid: 350000, adults: 2, children: 0, infants: 0,
      src: 'Traveloka', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Lý Văn Thành', p: '0920 170 230', cc: 'VN', lead: true } ] },

    { id: 'BK31', room: '210', st: 'booked', name: 'Tạ Hồng Nhung', code: '#938731', in: DD(3,14,0), out: DD(6,12,0),
      group: null, pay: 'none', total: 1350000, paid: 0, adults: 3, children: 1, infants: 0,
      src: 'Expedia', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Phạm Thu Trang',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Tạ Hồng Nhung', p: '0921 177 233', cc: 'VN', lead: true } ] },

    { id: 'BK32', room: '211', st: 'booked', name: 'Chen Wei', code: '#938732', in: DD(3,14,0), out: DD(8,12,0),
      group: null, pay: 'paid', total: 2250000, paid: 2250000, adults: 3, children: 1, infants: 0,
      src: 'Điện thoại', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Hoàng Kiều Trinh',
      confirmed: false, note: '', inote: '',
      guests: [ { n: 'Chen Wei', p: '0922 184 236', cc: 'VN', lead: true } ] },

    { id: 'BK33', room: '212', st: 'booked', name: 'Kim Soo Jin', code: '#938733', in: DD(4,14,0), out: DD(6,12,0),
      group: null, pay: 'partial', total: 900000, paid: 450000, adults: 3, children: 1, infants: 0,
      src: 'Walk-in', ota: '', cms: '', company: '', market: 'Châu Á', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Kim Soo Jin', p: '0923 191 239', cc: 'VN', lead: true } ] },

    { id: 'BK34', room: '213', st: 'booked', name: 'Yamada Kenji', code: '#938734', in: DD(4,14,0), out: DD(7,12,0),
      group: null, pay: 'none', total: 1350000, paid: 0, adults: 3, children: 1, infants: 0,
      src: 'Trực tiếp', ota: '', cms: '', company: '', market: 'Quốc tế', staff: 'Phạm Thu Trang',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Yamada Kenji', p: '0924 198 242', cc: 'VN', lead: true } ] },

    { id: 'BK35', room: '214', st: 'booked', name: 'Nguyễn Bá Lộc', code: '#938735', in: DD(5,14,0), out: DD(9,12,0),
      group: null, pay: 'paid', total: 1800000, paid: 1800000, adults: 3, children: 1, infants: 0,
      src: 'Booking.com', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Hoàng Kiều Trinh',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Nguyễn Bá Lộc', p: '0925 205 245', cc: 'VN', lead: true } ] },

    { id: 'BK36', room: '215', st: 'booked', name: 'Trịnh Thu Hà', code: '#938736', in: DD(5,14,0), out: DD(7,12,0),
      group: null, pay: 'partial', total: 900000, paid: 450000, adults: 3, children: 1, infants: 0,
      src: 'Agoda.com', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: false, note: '', inote: '',
      guests: [ { n: 'Trịnh Thu Hà', p: '0926 212 248', cc: 'VN', lead: true } ] },

    { id: 'BK37', room: '216', st: 'booked', name: 'Phan Văn Sơn', code: '#938737', in: DD(6,14,0), out: DD(9,12,0),
      group: null, pay: 'none', total: 1350000, paid: 0, adults: 3, children: 1, infants: 0,
      src: 'Traveloka', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Phạm Thu Trang',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Phan Văn Sơn', p: '0927 219 251', cc: 'VN', lead: true } ] },

    { id: 'BK38', room: '303', st: 'booked', name: 'Hồ Thị Lệ', code: '#938738', in: DD(7,14,0), out: DD(9,12,0),
      group: null, pay: 'paid', total: 640000, paid: 640000, adults: 2, children: 0, infants: 0,
      src: 'Expedia', ota: '', cms: '', company: '', market: 'Châu Á', staff: 'Nguyễn Hoàng Kiều Trinh',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Hồ Thị Lệ', p: '0928 226 254', cc: 'VN', lead: true } ] },

    { id: 'BK39', room: '305', st: 'booked', name: 'Dương Minh Nhật', code: '#938739', in: DD(7,14,0), out: DD(11,12,0),
      group: null, pay: 'partial', total: 1280000, paid: 640000, adults: 2, children: 0, infants: 0,
      src: 'Điện thoại', ota: '', cms: '', company: '', market: 'Quốc tế', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Dương Minh Nhật', p: '0929 233 257', cc: 'VN', lead: true } ] },

    { id: 'BK40', room: '307', st: 'booked', name: 'Cao Thị Yến', code: '#938740', in: DD(8,14,0), out: DD(11,12,0),
      group: null, pay: 'none', total: 960000, paid: 0, adults: 2, children: 0, infants: 0,
      src: 'Walk-in', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Phạm Thu Trang',
      confirmed: false, note: '', inote: '',
      guests: [ { n: 'Cao Thị Yến', p: '0930 240 260', cc: 'VN', lead: true } ] },

    { id: 'BK41', room: '308', st: 'booked', name: 'Lâm Quốc Vinh', code: '#938741', in: DD(9,14,0), out: DD(11,12,0),
      group: null, pay: 'paid', total: 640000, paid: 640000, adults: 2, children: 0, infants: 0,
      src: 'Trực tiếp', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Hoàng Kiều Trinh',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Lâm Quốc Vinh', p: '0931 247 263', cc: 'VN', lead: true } ] },

    { id: 'BK42', room: '309', st: 'booked', name: 'Đinh Thị Hoa', code: '#938742', in: DD(10,14,0), out: DD(13,12,0),
      group: null, pay: 'partial', total: 960000, paid: 480000, adults: 2, children: 0, infants: 0,
      src: 'Booking.com', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Đinh Thị Hoa', p: '0932 254 266', cc: 'VN', lead: true } ] },

    { id: 'BK43', room: '310', st: 'booked', name: 'Nguyễn Hữu Phước', code: '#938743', in: DD(11,14,0), out: DD(13,12,0),
      group: null, pay: 'none', total: 640000, paid: 0, adults: 2, children: 0, infants: 0,
      src: 'Agoda.com', ota: '', cms: '', company: '', market: 'Châu Á', staff: 'Phạm Thu Trang',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Nguyễn Hữu Phước', p: '0933 261 269', cc: 'VN', lead: true } ] },

    { id: 'BK44', room: '311', st: 'booked', name: 'Trương Mỹ Linh', code: '#938744', in: DD(12,14,0), out: DD(16,12,0),
      group: null, pay: 'paid', total: 2400000, paid: 2400000, adults: 4, children: 1, infants: 0,
      src: 'Traveloka', ota: '', cms: '', company: '', market: 'Quốc tế', staff: 'Nguyễn Hoàng Kiều Trinh',
      confirmed: false, note: '', inote: '',
      guests: [ { n: 'Trương Mỹ Linh', p: '0934 268 272', cc: 'VN', lead: true } ] },

    { id: 'BK45', room: '312', st: 'booked', name: 'Võ Thành Long', code: '#938745', in: DD(13,14,0), out: DD(16,12,0),
      group: null, pay: 'partial', total: 1800000, paid: 900000, adults: 4, children: 1, infants: 0,
      src: 'Expedia', ota: '', cms: '', company: '', market: 'Nội địa', staff: 'Nguyễn Văn Thảo Nguyên',
      confirmed: true, note: '', inote: '',
      guests: [ { n: 'Võ Thành Long', p: '0935 275 275', cc: 'VN', lead: true } ] }
  ];

  // Booking chưa gán phòng (không nằm trên grid — hiển thị qua badge tròn vàng)
  var UNASSIGNED = [
    { id: 'UA1', name: 'Lý Gia Bảo',      code: '#938601', in: DD(1,14,0), out: DD(3,12,0),  type: 'SGL', src: 'Booking.com',
      gid: 'G1', adults: 2, children: 0, infants: 0, total: 4000000, paid: 0, pay: 'none', rate: 'Rate code 1',
      guests: [ { n: 'Nguyễn Trần Minh Quân', p: '0912 345 678', cc: 'VN', lead: true } ] },
    { id: 'UA2', name: 'Nguyễn Hải Đăng', code: '#938602', in: DD(4,14,0), out: DD(6,12,0),  type: 'TWN', src: 'Agoda.com' },
    { id: 'UA3', name: 'Trịnh Mai Anh',   code: '#938603', in: DD(7,14,0), out: DD(10,12,0), type: 'DBL', src: 'Trực tiếp' },
    { id: 'UA4', name: 'Park Ji Sung',    code: '#938604', in: DD(11,14,0), out: DD(13,12,0), type: 'TRP', src: 'Expedia' },
    { id: 'UA5', name: 'Hồ Ngọc Diệp',    code: '#938605', in: DD(14,14,0), out: DD(17,12,0), type: 'FAM', src: 'Traveloka' }
  ];

  var HOLIDAYS = [{ date: DD(10), name: 'Giỗ tổ Hùng Vương' }];

  /* ---------------- 3. Trạng thái view ---------------- */
  /* ---------------- Ngày nghiệp vụ & quy tắc trạng thái ----------------
     Mốc quyết định trạng thái của MỌI booking trên cả 3 sơ đồ (Timeline /
     Chi tiết / Đơn giản):
       · ngày đi  TRƯỚC mốc                → Đã trả   (checkout)
       · ngày đến TRƯỚC mốc, ngày đi >= mốc → Có khách (inhouse)
       · ngày đến TỪ mốc trở đi             → Đã đặt   (booked)
     Phòng đang sửa (repair) không chịu quy tắc này.

     BIZ_DATE mặc định = HÔM NAY để prototype không bị lệch khi mở lại vào
     ngày khác. Muốn ghim cứng một ngày cụ thể (vd. 14/08/2026) thì đổi 1 dòng:
         var BIZ_DATE = new Date(2026, 7, 14);
     -------------------------------------------------------------------- */
  var BIZ_DATE = startOfDay(HOTEL_NOW);
  function normalizeSt(b) {
    // repair: không theo quy tắc. manual: booking do lễ tân vừa tạo trong phiên
    // (nhận phòng ngay / đặt trước) — giữ đúng thao tác của người dùng.
    if (!b || b.st === 'repair' || b.manual) return b;
    var din = startOfDay(b.in), dout = startOfDay(b.out);
    if (dout < BIZ_DATE) b.st = 'checkout';
    else if (din < BIZ_DATE) b.st = 'inhouse';
    else b.st = 'booked';
    return b;
  }
  function normalizeAll() {
    BIZ_DATE = startOfDay(HOTEL_NOW);
    for (var i = 0; i < BOOKINGS.length; i++) normalizeSt(BOOKINGS[i]);
  }

  var state = { anchor: startOfDay(HOTEL_NOW), range: 'week', group: 'type', chart: 'timeline', collapsed: {}, filter: null };
  // true = lần render kế tiếp cho booking hiện dần từ trên xuống.
  // Chỉ bật khi NGƯỜI DÙNG chuyển ngày / chế độ xem — không bật khi resize, kéo–thả hay đồng hồ tick.
  var animateNext = true;

  function loadPrefs() {
    try {
      var raw = window.localStorage.getItem(LS_KEY);
      if (!raw) return;                                     // lần đầu / trình duyệt mới → Tuần + Loại
      var p = JSON.parse(raw);
      if (p.range === 'day' || p.range === 'week' || p.range === 'month') state.range = p.range;
      if (p.group === 'type' || p.group === 'room') state.group = p.group;
      if (p.chart === 'timeline' || p.chart === 'detail' || p.chart === 'simple') state.chart = p.chart;
    } catch (e) { /* localStorage bị chặn → giữ mặc định */ }
  }
  function savePrefs() {
    try {
      window.localStorage.setItem(LS_KEY, JSON.stringify({ range: state.range, group: state.group, chart: state.chart }));
    } catch (e) { /* bỏ qua */ }
  }

  /* ---------------- 4. Tính cột ---------------- */
  function isHourMode() { return state.range === 'day'; }
  function colCount() { return isHourMode() ? 24 : 30; }
  function visibleCols() { return state.range === 'week' ? 7 : (isHourMode() ? 24 : 30); }
  function cellW() {
    var scroll = $('tlScroll');
    var avail = (scroll ? scroll.clientWidth : 1172) - ROOM_W;
    var w = Math.floor(avail / visibleCols());
    return Math.max(CELL_W, w);
  }
  function colDate(i) { return addDays(state.anchor, i); }

  // vị trí (đơn vị cột, số thực) của một mốc thời gian
  function colPos(dt) {
    if (isHourMode()) {
      var d0 = state.anchor;
      var diffH = (dt - d0) / 3600000;
      return diffH;
    }
    return dayDiff(dt, state.anchor) + 0.5;   // Figma: neo giữa ô
  }

  function isHoliday(d) {
    for (var i = 0; i < HOLIDAYS.length; i++) if (sameDay(HOLIDAYS[i].date, d)) return HOLIDAYS[i];
    return null;
  }
  function occupancyOn(d, typeId) {
    var rooms = typeId ? ROOMS.filter(function (r) { return r.type === typeId; }) : ROOMS;
    if (!rooms.length) return 0;
    var ids = {}; rooms.forEach(function (r) { ids[r.id] = 1; });
    var busy = 0;
    for (var i = 0; i < BOOKINGS.length; i++) {
      var b = BOOKINGS[i];
      if (!ids[b.room] || b.st === 'checkout') continue;
      if (startOfDay(b.in) <= startOfDay(d) && startOfDay(d) < startOfDay(b.out)) busy++;
    }
    return Math.round(busy / rooms.length * 100);
  }

  /* ---------------- 5. Render grid ---------------- */
  function roomsOfType(t) { return ROOMS.filter(function (r) { return r.type === t; }).filter(roomVisible); }

  /* ---- Bộ lọc trạng thái ----
     · booked / inhouse / checkout / repair : lọc theo trạng thái booking
     · dirty                                 : lọc theo cờ của PHÒNG */
  var BK_FILTERS = { booked: 1, inhouse: 1, checkout: 1, repair: 1 };
  function barVisible(b) { return !state.filter || !BK_FILTERS[state.filter] || b.st === state.filter; }
  function roomVisible(r) {
    var f = state.filter;
    if (!f) return true;
    if (f === 'dirty') return !!r.dirty;
    if (BK_FILTERS[f]) {
      for (var i = 0; i < BOOKINGS.length; i++) if (BOOKINGS[i].room === r.id && BOOKINGS[i].st === f) return true;
      return false;
    }
    return true;
  }
  function setFilter(k) {
    state.filter = (state.filter === k) ? null : k;
    animateNext = true;
    render();
  }

  function render() {
    var isTimeline = state.chart === 'timeline';
    $('tlScroll').hidden = !isTimeline;
    $('tlCards').hidden = isTimeline;
    $('tlEmpty').hidden = true;
    if (isTimeline) { renderHead(); renderBody(); renderNow(); }
    else { renderCards(); }
    syncControls();
    syncChrome();
  }

  function renderHead() {
    var w = cellW(), n = colCount(), h = [];
    var head = $('tlHead');
    if (isHourMode()) {
      var d = state.anchor;
      h.push('<tr>');
      h.push('<th class="rc" rowspan="2" style="height:60px"></th>');
      h.push('<th class="tl-daycell is-hour" colspan="' + n + '" style="height:30px">' +
        '<span class="sq"><span class="dw">' + WD[d.getDay()] + '</span><span class="dn">' + d.getDate() + '/' + pad(d.getMonth() + 1) + '</span></span></th>');
      h.push('</tr><tr>');
      for (var i = 0; i < n; i++) {
        h.push('<th class="tl-daycell" style="width:' + w + 'px;height:30px"><span class="sq"><span class="dn">' + pad(i) + ':00</span></span></th>');
      }
      h.push('</tr>');
    } else {
      h.push('<tr>');
      h.push('<th class="rc" style="height:var(--tl-head-h)"></th>');
      for (var j = 0; j < n; j++) {
        var dd = colDate(j);
        var wknd = (dd.getDay() === 0 || dd.getDay() === 6);
        var hol = isHoliday(dd);
        h.push('<th class="tl-daycell' + (wknd ? ' tl-weekend' : '') + (hol ? ' tl-holiday' : '') + (sameDay(dd, HOTEL_NOW) ? ' is-today' : '') +
          '" style="width:' + w + 'px;height:var(--tl-head-h)" data-col="' + j + '"' +
          (hol ? ' title="' + esc(hol.name) + '"' : '') + '><span class="sq">' +
          '<span class="dw">' + WD[dd.getDay()] + '</span>' +
          '<span class="dn">' + dd.getDate() + '</span>' +
          '</span></th>');
      }
      h.push('</tr>');
    }
    head.innerHTML = h.join('');
  }

  function sumTotalLabel() {
    var m = cfgDef('timeline', 'sumTotal');
    return m === 'Số phòng đã bán' ? 'Số phòng đã bán' : (m === 'Số phòng trống' ? 'Số phòng trống' : 'Công suất phòng');
  }

  function sumRow(cls, label, typeId, extraAttr) {
    var w = cellW(), n = colCount(), h = [];
    h.push('<tr class="' + cls + '"' + (extraAttr || '') + '>');
    h.push('<th class="rc">' + label + '</th>');
    for (var i = 0; i < n; i++) {
      var d = isHourMode() ? state.anchor : colDate(i);
      var wknd = !isHourMode() && (d.getDay() === 0 || d.getDay() === 6);
      h.push('<td class="' + (wknd ? 'tl-weekend' : '') + '" style="width:' + w + 'px">' + occupancyOn(d, typeId) + '%</td>');
    }
    h.push('</tr>');
    return h.join('');
  }

  /* Nhãn hiển thị trên thanh booking — theo cấu hình "Tên hiển thị" */
  function bkLabel(b) {
    var m = cfgDef('timeline', 'bkName');
    if (m === 'Mã đặt phòng') return b.id;
    if (m === 'Tên công ty') return b.company || b.name;
    return b.name;
  }

  function barHTML(b, w, n) {
    var p0 = colPos(b.in), p1 = colPos(b.out);
    if (p1 <= 0 || p0 >= n) return null;                 // ngoài vùng xem
    var clipL = p0 < 0, clipR = p1 > n;
    var s = Math.max(p0, 0), e = Math.min(p1, n);
    var td = Math.min(Math.max(Math.floor(s), 0), n - 1);
    var left = (s - td) * w, width = Math.max((e - s) * w, 24);

    var pay = { paid: 'var(--tl-pay-paid)', partial: 'var(--tl-pay-partial)', none: 'var(--tl-pay-none)' }[b.pay] || 'var(--tl-pay-none)';
    var inner;
    if (b.st === 'repair') {
      inner = '<span class="body"><span class="ava" style="background:transparent">' + I.cone + '</span>' +
        '<span class="label">' + esc(b.repair === 'OOS' ? (b.sellable ? 'OOS' : 'OOS') : 'OOO') + ' · Sửa phòng</span></span>';
    } else {
      var showKind = cfgOn('timeline', 'bkKind');
      inner = '<span class="body">' +
        (b.group && showKind ? '<span class="grp" style="background:' + esc(b.group.color) + '">' + I.users + '</span>' : '') +
        (showKind ? '<span class="ava">' + avatarSVG((b.src || 'B').charAt(0).toUpperCase()) + '</span>' : '') +
        '<span class="label">' + esc(bkLabel(b)) + '</span></span>' +
        (cfgOn('timeline', 'bkPay') ? '<span class="pay" style="background:' + pay + '"></span>' : '');
    }
    return {
      td: td,
      html: '<button class="tl-bar' + (clipL ? ' clip-l' : '') + (clipR ? ' clip-r' : '') + (width < 56 ? ' is-narrow' : '') + '" type="button" ' +
        'data-bk="' + b.id + '" data-st="' + b.st + '" ' +
        'style="left:' + left + 'px;width:' + width + 'px" ' +
        'aria-label="' + esc(b.name) + ' — ' + dmy(b.in) + ' đến ' + dmy(b.out) + '">' + inner + '</button>'
    };
  }

  function roomRow(r, w, n) {
    var bars = {}, i;
    var list = BOOKINGS.filter(function (b) { return b.room === r.id && barVisible(b); });
    for (i = 0; i < list.length; i++) {
      var res = barHTML(list[i], w, n);
      if (res) { (bars[res.td] = bars[res.td] || []).push(res.html); }
    }
    var h = ['<tr class="tl-row-room" data-room="' + r.id + '">'];
    h.push('<th class="rc"><button class="tl-roomcell" type="button" data-room="' + r.id + '" aria-label="Trạng thái phòng ' + r.id + '">' +
      '<span class="rn">' + esc(r.id) + '</span><span class="flags">' +
      (r.dirty ? '<span class="tl-flag-dirty" title="Phòng bẩn">' + I.broom + '</span>' : '') +
      '</span></button></th>');
    for (i = 0; i < n; i++) {
      var d = isHourMode() ? state.anchor : colDate(i);
      var wknd = !isHourMode() && (d.getDay() === 0 || d.getDay() === 6);
      h.push('<td class="dc' + (wknd ? ' tl-weekend' : '') + (bars[i] ? ' has-bar' : '') +
        '" style="width:' + w + 'px" data-room="' + r.id + '" data-col="' + i + '">' +
        (bars[i] ? bars[i].join('') : '') + '</td>');
    }
    h.push('</tr>');
    return h.join('');
  }

  function renderBody() {
    var w = cellW(), n = colCount(), h = [];
    var table = $('tlTable');
    table.style.width = (ROOM_W + w * n) + 'px';

    if (cfgOn('timeline', 'sumTotal')) h.push(sumRow('tl-row-sum', sumTotalLabel(), null));

    if (state.group === 'type') {
      TYPES.forEach(function (t) {
        if (!roomsOfType(t.id).length) return;      // lọc làm rỗng nhóm → ẩn cả hàng loại phòng
        var col = !!state.collapsed[t.id] || cfgDef('timeline', 'sumType') === 'Thu gọn mặc định';
        if (!cfgOn('timeline', 'sumType')) { roomsOfType(t.id).forEach(function (r) { h.push(roomRow(r, w, n)); }); return; }
        h.push(sumRow('tl-row-grp' + (col ? ' is-collapsed' : ''),
          '<span class="tl-roomcell" style="cursor:pointer" data-type="' + t.id + '">' + I.caret +
          '<span class="tl-grp-name" data-tip="' + esc(t.name) + '">' + esc(t.name) + '</span></span>',
          t.id, ' data-type="' + t.id + '"'));
        if (!col) roomsOfType(t.id).forEach(function (r) { h.push(roomRow(r, w, n)); });
      });
    } else {
      ROOMS.filter(roomVisible).forEach(function (r) { h.push(roomRow(r, w, n)); });
    }
    $('tlBody').innerHTML = h.join('');
    if (animateNext) applyStagger();
  }

  // Mỗi DÒNG CÓ BOOKING nhận một bậc delay → hiệu ứng chạy đều từ trên xuống,
  // dòng trống không "ăn" nhịp. Trần 700ms để lưới dài không phải chờ lâu.
  function applyStagger() {
    var tl = $('tl'), body = $('tlBody'), rows = body.rows, step = 0;
    tl.classList.remove('tl-anim');
    void body.offsetWidth;                       // ép reflow: animation mới chạy lại từ đầu
    tl.classList.add('tl-anim');
    for (var i = 0; i < rows.length; i++) {
      var bars = rows[i].querySelectorAll('.tl-bar');
      if (!bars.length) continue;
      var d = Math.min(step * 35, 700);
      step++;
      for (var k = 0; k < bars.length; k++) bars[k].style.setProperty('--tl-d', d + 'ms');
    }
    animateNext = false;
  }

  function renderNow() {
    var line = $('tlNowLine'), chip = $('tlNowChip'), hchip = $('tlHolidayChip');
    var w = cellW(), n = colCount(), pos = null;
    if (isHourMode()) {
      if (sameDay(state.anchor, HOTEL_NOW)) pos = HOTEL_NOW.getHours() + HOTEL_NOW.getMinutes() / 60;
    } else {
      var idx = dayDiff(HOTEL_NOW, state.anchor);
      if (idx >= 0 && idx < n) pos = idx + (HOTEL_NOW.getHours() + HOTEL_NOW.getMinutes() / 60) / 24;
    }
    if (pos === null || pos < 0 || pos > n) {
      line.hidden = true; chip.hidden = true;
    } else {
      var x = ROOM_W + pos * w;
      line.hidden = false;
      line.style.left = x + 'px';
      line.style.height = $('tlTable').offsetHeight + 'px';
      // chip nổi trên cột phòng sticky → ẩn khi mốc "bây giờ" đã trôi vào vùng cột phòng
      chip.hidden = x < ROOM_W;   // chỉ ẩn khi mốc hiện tại đã trôi hẳn vào vùng cột phòng
      chip.style.left = x + 'px';
      chip.style.top = (isHourMode() ? 60 : 60) + 'px';
      chip.textContent = hm(HOTEL_NOW);
    }
    // chip ngày lễ giờ hiện theo hover (xem handler mouseover) — vạch 4px trên ô header là chỉ báo thường trực
    hchip.hidden = true;
  }

  function syncControls() {
    document.querySelectorAll('[data-range]').forEach(function (b) { b.setAttribute('aria-pressed', String(b.dataset.range === state.range)); });
    document.querySelectorAll('[data-group]').forEach(function (b) { b.setAttribute('aria-pressed', String(b.dataset.group === state.group)); });
    document.querySelectorAll('[data-chart]').forEach(function (b) { b.setAttribute('aria-pressed', String(b.dataset.chart === state.chart)); });
    $('tlDateText').textContent = dmy(state.anchor);
    if (dpIsOpen()) { dpView = new Date(state.anchor.getFullYear(), state.anchor.getMonth(), 1); dpRender(); }
    // Đang ở đúng hôm nay → Active (outline primary), KHÔNG disable
    $('tlToday').setAttribute('aria-pressed', String(sameDay(state.anchor, HOTEL_NOW)));
    renderLegend();
  }

  function renderLegend() {
    var c = { booked: 0, inhouse: 0, checkout: 0, repair: 0, dirty: 0 };
    BOOKINGS.forEach(function (b) { if (c[b.st] !== undefined) c[b.st]++; });
    ROOMS.forEach(function (r) { if (r.dirty) c.dirty++; });
    var order = ['booked', 'inhouse', 'checkout', 'repair', 'dirty'];
    var icons = { repair: I.cone, dirty: I.broom };
    $('tlLegend').innerHTML = order.map(function (k) {
      var s = STATUS[k], on = state.filter === k;
      return '<button type="button" data-legend="' + k + '" aria-pressed="' + on + '" ' +
        'title="' + esc(on ? 'Bỏ lọc' : 'Lọc: ' + s.label) + '" ' +
        'style="color:' + s.color + ';--tl-legend-active:rgba(' + s.rgb + ',.12)">' +
        (icons[k] ? icons[k] : '<span class="dot" style="background:' + s.color + '"></span>') +
        '<span class="lbl">' + s.label + '</span><span class="cnt">(' + c[k] + ')</span></button>';
    }).join('');
    var note = $('tlFilterNote');
    if (state.filter) {
      note.classList.add('is-on');
      $('tlFilterNoteText').textContent = 'Đang lọc: ' + STATUS[state.filter].label;
    } else { note.classList.remove('is-on'); $('tlFilterNoteText').textContent = ''; }
  }

  /* ---------------- 6. Popup xem nhanh booking (Figma 49851:170574 / 49820:169614 / 49820:170449) ---------------- */
  var tipTimer = null, closeTimer = null;
  function bkById(id) { for (var i = 0; i < BOOKINGS.length; i++) if (BOOKINGS[i].id === id) return BOOKINGS[i]; return null; }
  function roomById(id) { for (var i = 0; i < ROOMS.length; i++) if (ROOMS[i].id === id) return ROOMS[i]; return null; }
  function typeName(id) { for (var i = 0; i < TYPES.length; i++) if (TYPES[i].id === id) return TYPES[i].name; return id; }
  function nightsOf(b) { return Math.max(1, dayDiff(b.out, b.in)); }
  function fullRoomName(b) { var r = roomById(b.room); return typeName(r ? r.type : '') + ' - ' + b.room; }

  function dtRange(a, z) {
    return '<div class="tl-pop-dates"><span>' + dmy(a) + ' ' + hm(a) + '</span><span class="sep">-</span>' +
      '<span>' + dmy(z) + ' ' + hm(z) + '</span>' +
      (cfgOn('card', 'nights') ? '<span class="n">(' + Math.max(1, dayDiff(z, a)) + ' đêm)</span>' : '') + '</div>';
  }
  function field(k, vHTML) { return '<div class="tl-pop-f"><div class="k">' + k + '</div><div class="v">' + vHTML + '</div></div>'; }
  function money2(n) { return '<div class="amt"><i>' + money(n) + '</i><u>VND</u></div>'; }

  /* --- Khối phòng dùng chung cho booking lẻ và từng phòng của đoàn --- */
  function popRoomCard(b) {
    var h = [];
    var acts = [];
    if (b.st === 'booked')       acts.push('<button class="tl-pop-btn checkin" type="button" data-act="checkin">' + I.login + 'Nhận phòng</button>');
    else if (b.st === 'inhouse') acts.push('<button class="tl-pop-btn checkout" type="button" data-act="checkout">' + I.logout + 'Trả phòng</button>');
    acts.push('<button class="tl-pop-btn2" type="button" data-act="detail">Chi tiết</button>');
    acts.push('<button class="tl-pop-ib" type="button" data-act="more" aria-haspopup="menu" aria-expanded="false" aria-label="Thao tác khác">' + I.dots + '</button>');

    h.push('<div class="tl-pop-room">' + I.calCheck +
      '<div class="meta">' +
        '<div class="tl-pop-roomname"><span>' + esc(fullRoomName(b)) + '</span>' +
          (b.st === 'inhouse' ? I.lock : '') + '</div>' +
        dtRange(b.in, b.out) +
      '</div>' +
      '<div class="tl-pop-acts">' + acts.join('') + '</div></div>');

    var f = [];
    if (cfgOn('card', 'pax')) f.push(field('Lượng khách:',
      '<div class="tl-pop-count">' +
        '<span>' + I.adult + b.adults + '</span>' +
        '<span>' + I.child + b.children + '</span>' +
        '<span>' + I.baby + b.infants + '</span></div>'));

    var lead = (b.guests || []).filter(function (g) { return g.lead; })[0] || (b.guests || [])[0];
    if (lead && cfgOn('card', 'leadGuest')) {
      f.push(field('Khách chính:',
        '<div class="tl-pop-guests">' + flagSVG(lead.cc) +
          '<span class="nm">' + esc(lead.n) + '</span>' + I.crown + I.cake + I.returning + '</div>' +
        '<div class="tl-pop-phone">' + I.phone + '<span>' + esc(lead.p) + '</span>' + I.copy16 + '</div>'));
    }
    if (cfgOn('card', 'guests')) {
      var others = (b.guests || []).filter(function (g) { return !g.lead; });
      if (others.length) f.push(field('Khách trong phòng:', others.map(function (g) {
        return '<div class="tl-pop-guests">' + flagSVG(g.cc) + '<span class="nm">' + esc(g.n) + '</span></div>';
      }).join('')));
    }

    if (cfgOn('card', 'note')) f.push(field('Ghi chú:', b.note
      ? '<div>' + esc(clip(b.note, 170)) + (b.note.length > 170 ? ' <button class="tl-pop-more" type="button" data-act="note">Xem thêm</button>' : '') + '</div>'
      : '<div class="empty">Không có ghi chú</div>'));
    if (cfgOn('card', 'inote')) f.push(field('Ghi chú (Nội bộ):', b.inote
      ? '<div>' + esc(clip(b.inote, 170)) + (b.inote.length > 170 ? ' <button class="tl-pop-more" type="button" data-act="inote">Xem thêm</button>' : '') + '</div>'
      : '<div class="empty">Không có ghi chú</div>'));

    var left = b.total - b.paid;
    var payColor = left === 0 ? 'var(--tl-pay-paid)' : (b.paid > 0 ? '#FFB269' : 'var(--tl-pay-none)');
    if (cfgOn('card', 'total') || cfgOn('card', 'paid') || cfgOn('card', 'left') || cfgOn('card', 'payStatus'))
      f.push('<div class="tl-pop-pay">' +
        (cfgOn('card', 'total') ? '<div class="r"><b>Tổng tiền:</b>' + money2(b.total) + '</div>' : '') +
        (cfgOn('card', 'paid') ? '<div class="r"><b>Đã thanh toán:</b>' + money2(b.paid) + '</div>' : '') +
        (cfgOn('card', 'left')
          ? '<div class="r left">' + (cfgOn('card', 'payStatus') ? '<span class="dot" style="background:' + payColor + '"></span>' : '') +
            '<b>Còn lại:</b><div class="amt"><i>' + money(left) + '</i><u>VND</u></div></div>'
          : (cfgOn('card', 'payStatus')
              ? '<div class="r left"><span class="dot" style="background:' + payColor + '"></span><b>' +
                (left === 0 ? 'Đã thanh toán' : (b.paid > 0 ? 'Thanh toán một phần' : 'Chưa thanh toán')) + '</b></div>'
              : '')) +
        '</div>');

    h.push('<div class="tl-pop-fields">' + f.join('') + '</div>');
    return '<div class="tl-pop-card">' + h.join('') + '</div>';
  }
  function clip(s, n) { return s.length > n ? s.slice(0, n) + '…' : s; }

  /* --- Cột trái: thông tin chung của đoàn --- */
  function popGroupSide(b) {
    var g = b.group, f = [];
    if (cfgOn('card', 'gRooms')) f.push(field('Số phòng:', '<div>' + g.rooms + ' phòng</div>'));
    if (cfgOn('card', 'gLead')) f.push(field('Trưởng đoàn:',
      '<div class="tl-pop-guests">' + flagSVG('VN') + '<span class="nm">' + esc(g.lead) + '</span>' + I.crown + I.cake + I.returning + '</div>' +
      '<div class="tl-pop-phone">' + I.phone + '<span class="link">' + esc(g.leadPhone) + '</span>' + I.copy16 + '</div>'));
    if (cfgOn('card', 'gCompany')) f.push(field('Công ty:', '<div>' + esc(b.company || '—') + '</div>'));
    if (cfgOn('card', 'gOta')) f.push(field('Mã OTA:', b.ota ? '<div class="link">' + esc(b.ota) + '</div>' : '<div class="empty">—</div>'));
    if (cfgOn('card', 'gCms')) f.push(field('Mã CMS:', '<div>' + esc(b.cms || 'Online') + '</div>'));
    if (cfgOn('card', 'gSrc')) f.push(field('Nguồn:', '<div>' + esc(b.src || '—') + '</div>'));
    if (cfgOn('card', 'gMarket')) f.push(field('Thị trường:', '<div>' + esc(b.market || '—') + '</div>'));
    if (cfgOn('card', 'gStaff')) f.push(field('Nhân viên:', '<div>' + esc(b.staff || '—') + '</div>'));
    if (cfgOn('card', 'gNote')) f.push(field('Ghi chú:', b.note
      ? '<div>' + esc(clip(b.note, 150)) + (b.note.length > 150 ? ' <button class="tl-pop-more" type="button" data-act="note">Xem thêm</button>' : '') + '</div>'
      : '<div class="empty">Không có ghi chú</div>'));
    return '<div class="tl-pop-card side"><h3 class="tl-pop-sect">Thông tin chung của đoàn</h3>' +
      '<div class="tl-pop-fields">' + f.join('') + '</div></div>';
  }

  /* --- Popup xem chi tiết sửa phòng (OOO / OOS) --- */
  function popRepair(b) {
    var sellable = b.repair === 'OOS' && b.sellable;
    var f = [];
    f.push(field('Loại sửa:', '<div>' + (b.repair === 'OOS' ? 'Tạm ngừng phục vụ (OOS)' : 'Ngừng khai thác (OOO)') + '</div>'));
    f.push(field('Thời gian:', '<div>' + dmy(b.in) + ' ' + hm(b.in) + ' - ' + dmy(b.out) + ' ' + hm(b.out) + '</div>'));
    f.push(field('Bán phòng:', '<div><span class="tl-pop-chip ' + (sellable ? 'green' : 'grey') + '">' +
      (sellable ? 'Vẫn mở bán' : 'Không mở bán') + '</span></div>'));
    f.push(field('Lý do:', '<div>' + esc(b.note || '—') + '</div>'));
    return '<div class="tl-pop-card"><div class="tl-pop-fields">' + f.join('') + '</div></div>';
  }

  function openTip(bar) {
    var b = bkById(bar.dataset.bk); if (!b) return;
    menuCtx.bk = b; menuCtx.room = b.room;      // để nút trong popup biết ngữ cảnh
    var pop = $('tlPop'), head, body, kind;

    if (b.st === 'repair') {
      kind = 'repair';
      head = '<div class="tl-pop-head"><div class="l"><div class="tl-pop-title"><span>Sửa phòng ' + esc(fullRoomName(b)) + '</span></div></div>' +
        '<div class="tl-pop-acts">' +
          '<button class="tl-pop-btn primary" type="button" data-act="repair">' + I.pencil + 'Chỉnh sửa</button>' +
          '<button class="tl-pop-ib danger" type="button" data-act="delrepair" aria-label="Xoá thiết lập sửa phòng">' + I.trash + '</button>' +
          '<button class="tl-pop-x" type="button" data-act="close" aria-label="Đóng">' + I.close + '</button>' +
        '</div></div>';
      body = '<div class="tl-pop-body">' + popRepair(b) + '</div>';
    } else if (b.group) {
      kind = 'group';
      head = '<div class="tl-pop-head"><div class="l">' +
          '<div class="tl-pop-title">' + I.users16 + '<span>' + esc(b.group.name) + '</span></div>' +
          '<button class="tl-pop-code" type="button" data-act="copycode">' + esc(b.code) + I.copy20 + '</button>' +
          (b.confirmed ? '<span class="tl-pop-chip ok">Đã xác nhận</span>' : '') +
        '</div><button class="tl-pop-x" type="button" data-act="close" aria-label="Đóng">' + I.close + '</button></div>';
      body = '<div class="tl-pop-body">' + popGroupSide(b) + popRoomCard(b) + '</div>';
    } else {
      kind = 'single';
      head = '<div class="tl-pop-head"><div class="l">' +
          '<button class="tl-pop-code" type="button" data-act="copycode">' + esc(b.code) + I.copy20 + '</button>' +
          (b.confirmed ? '<span class="tl-pop-chip ok">Đã xác nhận</span>' : '') +
        '</div><button class="tl-pop-x" type="button" data-act="close" aria-label="Đóng">' + I.close + '</button></div>';
      body = '<div class="tl-pop-body">' + popRoomCard(b) + '</div>';
    }

    pop.dataset.kind = kind;
    pop.innerHTML = head + body;
    pop.classList.add('is-open');
    placeFloating(pop, bar);
  }

  function closeTip() { clearTimeout(closeTimer); $('tlPop').classList.remove('is-open'); }
  function scheduleCloseTip() { clearTimeout(closeTimer); closeTimer = setTimeout(closeTip, 160); }
  function cancelCloseTip() { clearTimeout(closeTimer); }

  function placeFloating(el, anchorEl, gap) {
    gap = gap === undefined ? 8 : gap;
    var r = anchorEl.getBoundingClientRect();
    el.style.left = '0px'; el.style.top = '0px';
    var w = el.offsetWidth, h = el.offsetHeight;
    var left = Math.min(Math.max(8, r.left), window.innerWidth - w - 8);
    var top = r.bottom + gap;
    if (top + h > window.innerHeight - 8) top = Math.max(8, r.top - h - gap);
    el.style.left = left + 'px';
    el.style.top = top + 'px';
  }

  /* Menu mở từ nút "..." trong popup: bám sát nút, ƯU TIÊN bung LÊN TRÊN để không
     che phần thân popup mà lễ tân đang đọc (đúng như ảnh tham chiếu). */
  function placeBeside(el, anchorEl) {
    var r = anchorEl.getBoundingClientRect();
    el.style.left = '0px'; el.style.top = '0px';
    var w = el.offsetWidth, h = el.offsetHeight, gap = 6;
    var left = Math.min(Math.max(8, r.left), window.innerWidth - w - 8);
    var above = r.top - 8, below = window.innerHeight - r.bottom - 8;
    var top = (h <= above || above >= below)
      ? Math.max(8, r.top - h - gap)
      : Math.min(r.bottom + gap, window.innerHeight - h - 8);
    el.style.left = left + 'px';
    el.style.top = top + 'px';
  }

  /* ---------------- 6b. Tooltip (tooltip.md · Figma 216:135965) ---------------- */
  // Hover có delay 300ms; focus bàn phím hiện NGAY (không delay) — theo bảng States.
  var ttTimer = null, ttTrigger = null;
  function showTip2(el, text, instant) {
    clearTimeout(ttTimer);
    var run = function () {
      var tt = $('tlTip2');
      tt.textContent = text;
      tt.dataset.dir = 'down';                       // mặc định: khối nằm TRÊN, mũi tên chỉ xuống
      tt.classList.add('is-open');
      tt.style.left = '0px'; tt.style.top = '0px';
      var r = el.getBoundingClientRect(), w = tt.offsetWidth, h = tt.offsetHeight;
      var cx = r.left + r.width / 2;
      var top = r.top - h - 8;                       // gap 8px
      if (top < 8) { top = r.bottom + 8; tt.dataset.dir = 'up'; }
      var left = Math.min(Math.max(8, cx - w / 2), window.innerWidth - w - 8);
      tt.style.left = left + 'px';
      tt.style.top = top + 'px';
      tt.style.setProperty('--tt-ax', (cx - left) + 'px');   // mũi tên luôn trỏ đúng tâm trigger
      el.setAttribute('aria-describedby', 'tlTip2');
      ttTrigger = el;
    };
    if (instant) run(); else ttTimer = setTimeout(run, 300);
  }
  function hideTip2() {
    clearTimeout(ttTimer);
    $('tlTip2').classList.remove('is-open');
    if (ttTrigger) { ttTrigger.removeAttribute('aria-describedby'); ttTrigger = null; }
  }

  /* ---------------- 7. Menu thao tác nhanh ---------------- */
  function menuItems(b, room) {
    function it(icon, label, act, cls) {
      return '<button type="button" role="menuitem" data-act="' + act + '"' + (cls ? ' class="' + cls + '"' : '') + '>' + icon + label + '</button>';
    }
    var h = [];
    if (!b) {                                                   // phòng trống
      h.push(it(I.login, 'Nhận phòng nhanh', 'quickcheckin'));
      h.push('<hr>');
      h.push(it(I.swap, 'Đổi trạng thái phòng', 'roomstatus'));
      h.push(it(I.cone, 'Sửa phòng', 'repair'));
      h.push('<hr>');
      h.push(it(I.list, 'Danh sách đặt phòng', 'bklist'));
      return h.join('');
    }
    if (b.st === 'repair') {
      h.push(it(I.cone, 'Thiết lập sửa phòng', 'repair'));
      if (b.repair === 'OOS' && b.sellable) h.push(it(I.login, 'Nhận phòng nhanh', 'quickcheckin'));
      h.push('<hr>');
      h.push(it(I.swap, 'Đổi trạng thái phòng', 'roomstatus'));
      h.push(it(I.list, 'Danh sách đặt phòng', 'bklist'));
      return h.join('');
    }
    if (b.st === 'booked') {
      h.push(it(I.info, 'Chi tiết', 'detail'));
      h.push(it(I.login, 'Nhận phòng', 'checkin'));
      h.push(it(I.card, 'Tạo thẻ', 'makecard'));
      h.push(it(I.unassign, 'Bỏ gán phòng', 'unassign'));
      h.push('<hr>');
      h.push(it(I.service, 'Thêm dịch vụ', 'service'));
      h.push(it(I.swapdate, 'Đổi ngày ở', 'changedate'));
      h.push(it(I.door, 'Chuyển phòng', 'moveroom'));
      h.push('<hr>');
      h.push(it(I.swap, 'Đổi trạng thái phòng', 'roomstatus'));
      h.push(it(I.copy, 'Sao chép', 'copy'));
      h.push(it(I.cancel, 'Hủy đặt phòng', 'cancel', 'danger'));
      h.push(it(I.list, 'Danh sách đặt phòng', 'bklist'));
      return h.join('');
    }
    if (b.st === 'inhouse') {
      h.push(it(I.info, 'Chi tiết', 'detail'));
      h.push(it(I.logout, 'Trả phòng', 'checkout'));
      h.push(it(I.card, 'Tạo thẻ', 'makecard'));
      h.push('<hr>');
      h.push(it(I.service, 'Thêm dịch vụ', 'service'));
      h.push(it(I.swapdate, 'Đổi ngày ở', 'changedate'));
      h.push(it(I.door, 'Chuyển phòng', 'moveroom'));
      h.push('<hr>');
      h.push(it(I.swap, 'Đổi trạng thái phòng', 'roomstatus'));
      h.push(it(I.copy, 'Sao chép', 'copy'));
      h.push(it(I.undo, 'Hoàn tác nhận phòng', 'undocheckin'));
      h.push(it(I.list, 'Danh sách đặt phòng', 'bklist'));
      return h.join('');
    }
    // checkout
    h.push(it(I.info, 'Chi tiết', 'detail'));
    h.push(it(I.undo, 'Hoàn tác trả phòng', 'undocheckout'));
    h.push('<hr>');
    h.push(it(I.swap, 'Đổi trạng thái phòng', 'roomstatus'));
    h.push(it(I.list, 'Danh sách đặt phòng', 'bklist'));
    return h.join('');
  }

  var menuCtx = { bk: null, room: null, col: null };
  // keepPop = true → mở menu NGAY CẠNH nút "..." mà vẫn giữ popup thông tin.
  function openMenu(anchorEl, bkId, roomId, keepPop) {
    if (!keepPop) closeTip();
    menuCtx.bk = bkId ? bkById(bkId) : null;
    menuCtx.room = roomId || (menuCtx.bk ? menuCtx.bk.room : null);
    menuCtx.col = (anchorEl && anchorEl.dataset && anchorEl.dataset.col !== undefined)
      ? parseInt(anchorEl.dataset.col, 10) : null;
    var m = $('tlMenu');
    m.innerHTML = menuItems(menuCtx.bk, menuCtx.room);
    m.classList.add('is-open');
    m.classList.toggle('is-beside', !!keepPop);
    if (keepPop) { placeBeside(m, anchorEl); menuAnchor = anchorEl; }
    else placeFloating(m, anchorEl);
  }
  var menuAnchor = null;
  function closeMenu() {
    $('tlMenu').classList.remove('is-open', 'is-beside');
    if (menuAnchor && menuAnchor.setAttribute) menuAnchor.setAttribute('aria-expanded', 'false');
    menuAnchor = null;
  }
  function menuIsOpen() { return $('tlMenu').classList.contains('is-open'); }

  /* --- v24: Menu 3 lựa chọn sau khi kéo chọn khoảng ngày trên ô trống --- */
  var selCtx = null;
  function openSelMenu(s) {
    selCtx = { room: s.room, c0: Math.min(s.c0, s.c1), c1: Math.max(s.c0, s.c1) };
    var m = $('tlMenu');
    m.innerHTML =
      '<button type="button" role="menuitem" data-selact="quickcheckin">' + I.calcheck + 'Nhận phòng nhanh</button>' +
      '<button type="button" role="menuitem" data-selact="newbooking">' + I.userplus + 'Tạo đặt phòng</button>' +
      '<hr>' +
      '<button type="button" role="menuitem" data-selact="repair">' + I.cone + 'Sửa phòng</button>';
    m.classList.add('is-open');
    m.classList.remove('is-beside');
    var td = $('tlBody').querySelector('td.dc[data-room="' + cssq(selCtx.room) + '"][data-col="' + selCtx.c0 + '"]');
    if (td) placeFloating(m, td, 4); else m.classList.remove('is-open');
  }
  function cssq(v) { return String(v).replace(/"/g, '\\"'); }
  function doSelAction(act) {
    var s = selCtx; selCtx = null;
    closeMenu();
    if (!s) return;
    if (act === 'quickcheckin') { qbOpen(s.room, s.c0, s.c1, 'checkin'); return; }
    if (act === 'newbooking') { qbOpen(s.room, s.c0, s.c1, 'prebook'); return; }
    if (act === 'repair') { rpOpenNew(s.room, s.c0, s.c1); return; }
  }

  /* ---------------- 8. Dialog & toast ---------------- */
  function openDlg(el) { $('tlBackdrop').classList.add('is-open'); el.classList.add('is-open'); }
  function closeDlgs() {
    $('tlBackdrop').classList.remove('is-open');
    $('tlRoomDlg').classList.remove('is-open');
    $('tlGenDlg').classList.remove('is-open');
    var rd = $('rpDlg'), rdd = $('rpDelDlg');
    if (rd) rd.classList.remove('is-open');
    if (rdd) rdd.classList.remove('is-open');
  }
  function copyText(s) {
    try {
      if (navigator.clipboard && navigator.clipboard.writeText) navigator.clipboard.writeText(s);
      toast('Đã sao chép: ' + s);
    } catch (e) { toast('Không sao chép được', true); }
  }
  function toast(msg, isErr) {
    var t = document.createElement('div');
    t.className = 'tl-toast' + (isErr ? ' err' : '');
    t.innerHTML = (isErr ? I.alert : I.check) + '<span>' + esc(msg) + '</span>';
    $('tlToasts').appendChild(t);
    setTimeout(function () { t.remove(); }, 3200);
  }

  var roomDlgCtx = null;
  function openRoomStatus(roomId) {
    var r = roomById(roomId); if (!r) return;
    roomDlgCtx = r;
    $('tlRoomDlgTitle').textContent = 'Đổi trạng thái phòng — ' + r.id;
    $('tlSwDirty').setAttribute('aria-checked', String(!!r.dirty));
    openDlg($('tlRoomDlg'));
    setTimeout(function () { $('tlSwDirty').focus(); }, 0);
  }
  function swOn(el) { return el.getAttribute('aria-checked') === 'true'; }
  function swToggle(el) { el.setAttribute('aria-checked', String(!swOn(el))); }

  function openGenDlg(title, bodyHTML, footHTML) {
    $('tlGenTitle').textContent = title;
    $('tlGenBody').innerHTML = bodyHTML;
    $('tlGenFoot').innerHTML = footHTML || '<button class="tl-btn-ghost" type="button" data-tl-close>Đóng</button>';
    openDlg($('tlGenDlg'));
  }

  function openBookingListDlg(roomId) {
    var list = BOOKINGS.filter(function (b) { return b.room === roomId && b.st !== 'repair' && b.out >= HOTEL_NOW; })
      .sort(function (a, b) { return a.in - b.in; });
    var html = '<div class="tl-list">' + list.map(function (b) {
      return '<div class="tl-list-item"><div class="l"><b>' + esc(b.name) + ' <span class="tl-pop-code2">' + esc(b.code) + '</span></b>' +
        '<span>' + dmy(b.in) + ' - ' + dmy(b.out) + ' · ' + nightsOf(b) + ' đêm</span></div>' +
        '<span class="tl-badge-st" style="background:rgba(24,111,244,.12);color:' + STATUS[b.st].color + '">' + STATUS[b.st].label + '</span></div>';
    }).join('') + '</div>';
    openGenDlg('Danh sách đặt phòng — ' + roomId, list.length ? html : '<p style="color:var(--theme-text-secondary)">Chưa có đặt phòng sắp đến.</p>');
  }

  function confirmDlg(title, msg, okLabel, onOk) {
    openGenDlg(title, '<p style="margin:0">' + esc(msg) + '</p>',
      '<button class="tl-btn-ghost" type="button" data-tl-close>Huỷ</button>' +
      '<button class="tl-btn-primary" type="button" id="tlGenOk">' + esc(okLabel) + '</button>');
    $('tlGenOk').onclick = function () { closeDlgs(); onOk(); };
  }

  /* ---------------- 9. Xử lý hành động menu ---------------- */
  function doAction(act) {
    var b = menuCtx.bk, roomId = menuCtx.room;
    closeMenu(); closeTip();
    switch (act) {
      case 'roomstatus': openRoomStatus(roomId); return;
      case 'bklist': openBookingListDlg(roomId); return;
      case 'checkin':
        /* R01.22 — mở sidebar "Quy trình nhận phòng" thay cho hộp thoại xác nhận cũ */
        if (typeof ciOpen === 'function') { ciOpen(b.id); return; }
        confirmDlg('Nhận phòng', 'Xác nhận nhận phòng cho ' + b.name + ' (' + b.room + ')?', 'Nhận phòng', function () {
          b.st = 'inhouse'; render(); toast('Nhận phòng thành công!');
        }); return;
      case 'checkout':
        if (typeof coOpen === 'function') { coOpen(b.id); return; }
        confirmDlg('Trả phòng', 'Xác nhận trả phòng cho ' + b.name + ' (' + b.room + ')?', 'Trả phòng', function () {
          b.st = 'checkout'; var r = roomById(b.room); if (r) r.dirty = true; render(); toast('Trả phòng thành công!');
        }); return;
      case 'undocheckin':
        confirmDlg('Hoàn tác nhận phòng', 'Bạn có chắc chắn muốn hoàn tác nhận phòng ' + b.room + '?', 'Xác nhận', function () {
          b.st = 'booked'; render(); toast('Hoàn tác nhận phòng thành công!');
        }); return;
      case 'undocheckout':
        confirmDlg('Hoàn tác trả phòng', 'Bạn có chắc chắn muốn hoàn tác trả phòng ' + b.room + '?', 'Xác nhận', function () {
          b.st = 'inhouse'; render(); toast('Hoàn tác trả phòng thành công!');
        }); return;
      case 'cancel':
        confirmDlg('Hủy đặt phòng', 'Bạn có chắc chắn muốn hủy đặt phòng ' + b.code + '?', 'Hủy đặt phòng', function () {
          BOOKINGS.splice(BOOKINGS.indexOf(b), 1); render(); toast('Hủy đặt phòng thành công!');
        }); return;
      case 'unassign':
        confirmDlg('Bỏ gán phòng', 'Bạn có chắc chắn muốn bỏ gán phòng ' + b.room + ' khỏi đặt phòng ' + b.code + '?', 'Bỏ gán', function () {
          UNASSIGNED.push({ id: 'UA' + b.id, name: b.name, code: b.code, in: b.in, out: b.out, type: (roomById(b.room) || {}).type, src: b.src || 'Trực tiếp' });
          BOOKINGS.splice(BOOKINGS.indexOf(b), 1); render(); toast('Bỏ gán phòng thành công!');
        }); return;
      case 'quickcheckin': {
        var qc = (menuCtx.col !== null && !isNaN(menuCtx.col)) ? menuCtx.col : Math.max(0, Math.floor(colPos(HOTEL_NOW)));
        qbOpen(roomId, qc, qc, 'checkin');   // menu "Nhận phòng nhanh" → tab Nhận phòng ngay, giờ đến lấy theo ô đã chọn
        return;
      }
      case 'repair':       if (b && b.st === 'repair') rpOpenEdit(b); else rpOpenNew(roomId, menuCtx.col, menuCtx.col); return;
      case 'makecard':     toast('Mở popup Tạo thẻ — ' + roomId); return;
      case 'service':      svOpen(b ? b.id : null, roomId); return;
      case 'changedate':   if (b) cdOpen(b.id); return;
      case 'moveroom':     if (b) mvOpen(b.id); return;
      case 'copy':         toast('Mở popup Sao chép đặt phòng — ' + (b ? b.code : roomId)); return;
      case 'detail':       if (b) openBookingDetail(b.id, b.room); return;
      default: return;
    }
  }

  /* ---------------- 10. Kéo–thả booking bar / kéo chọn ô trống ---------------- */
  var drag = null;
  var gsel = null;              // vùng kéo chọn ô trống → Đặt phòng nhanh
  var gselJustDone = false;     // chặn click mở menu ngay sau khi thả chuột

  function gselTd(room, col) {
    return $('tlBody').querySelector('td.dc[data-room="' + room + '"][data-col="' + col + '"]');
  }
  function gselPaint() {
    $('tlBody').querySelectorAll('td.dc.is-selecting').forEach(function (td) { td.classList.remove('is-selecting'); });
    if (!gsel || !gsel.active) return;
    var a = Math.min(gsel.c0, gsel.c1), z = Math.max(gsel.c0, gsel.c1);
    var t0 = gselTd(gsel.room, a), t1 = gselTd(gsel.room, z);
    for (var i = a; i <= z; i++) { var td = gselTd(gsel.room, i); if (td) td.classList.add('is-selecting'); }
    if (!t0 || !t1 || !gsel.ghost) return;
    var sc = $('tlScroll'), sr = sc.getBoundingClientRect();
    var r0 = t0.getBoundingClientRect(), r1 = t1.getBoundingClientRect();
    gsel.ghost.style.left = (r0.left - sr.left + sc.scrollLeft) + 'px';
    gsel.ghost.style.top = (r0.top - sr.top + sc.scrollTop + 1) + 'px';
    gsel.ghost.style.width = Math.max(24, r1.right - r0.left) + 'px';
  }

  function onPointerDown(e) {
    var bar = e.target.closest ? e.target.closest('.tl-bar') : null;
    if (!bar && e.button === 0) {
      // ô trống (không có booking bar) → bắt đầu vùng chọn cho Đặt phòng nhanh
      var td0 = e.target.closest ? e.target.closest('td.dc') : null;
      if (td0 && !td0.querySelector('.tl-bar') && !qb.open) {
        gsel = { room: td0.dataset.room, c0: parseInt(td0.dataset.col, 10),
                 c1: parseInt(td0.dataset.col, 10), x0: e.clientX, y0: e.clientY, active: false, ghost: null };
      }
    }
    if (!bar || e.button !== 0) return;
    var b = bkById(bar.dataset.bk); if (!b || b.st === 'checkout') return;
    drag = { bar: bar, bk: b, x0: e.clientX, y0: e.clientY, active: false, ghost: null, target: null };
  }
  function onPointerMove(e) {
    if (gsel) {
      if (!gsel.active) {
        if (Math.abs(e.clientX - gsel.x0) < 4 && Math.abs(e.clientY - gsel.y0) < 4) return;
        gsel.active = true;
        closeTip(); closeMenu();
        var g = document.createElement('div');
        g.className = 'tl-sel-ghost';
        $('tlScroll').appendChild(g);
        gsel.ghost = g;
      }
      var el2 = document.elementFromPoint(e.clientX, e.clientY);
      var td2 = el2 && el2.closest ? el2.closest('td.dc') : null;
      if (td2 && td2.dataset.room === gsel.room) gsel.c1 = parseInt(td2.dataset.col, 10);
      gselPaint();
      return;
    }
    if (!drag) return;
    if (!drag.active) {
      if (Math.abs(e.clientX - drag.x0) < 4 && Math.abs(e.clientY - drag.y0) < 4) return;
      drag.active = true;
      drag.bar.classList.add('is-drag');
      closeTip(); closeMenu();
      var g = document.createElement('div');
      g.className = 'tl-bar-ghost';
      g.style.width = drag.bar.offsetWidth + 'px';
      $('tlScroll').appendChild(g);
      drag.ghost = g;
    }
    var el = document.elementFromPoint(e.clientX, e.clientY);
    var td = el && el.closest ? el.closest('td.dc') : null;
    if (td) {
      drag.target = { room: td.dataset.room, col: parseInt(td.dataset.col, 10) };
      var sc = $('tlScroll'), sr = sc.getBoundingClientRect(), tr = td.getBoundingClientRect();
      drag.ghost.style.left = (tr.left - sr.left + sc.scrollLeft) + 'px';
      drag.ghost.style.top = (tr.top - sr.top + sc.scrollTop + 1) + 'px';
    }
  }
  function onPointerUp() {
    if (gsel) {
      var s = gsel; gsel = null;
      if (s.ghost) s.ghost.remove();
      $('tlBody').querySelectorAll('td.dc.is-selecting').forEach(function (td) { td.classList.remove('is-selecting'); });
      if (s.active) {
        gselJustDone = true;
        setTimeout(function () { gselJustDone = false; }, 0);
        openSelMenu(s);          // v24: hiện 3 lựa chọn thay vì mở thẳng form đặt phòng nhanh
        return;
      }
    }
    if (!drag) return;
    var d = drag; drag = null;
    if (d.ghost) d.ghost.remove();
    d.bar.classList.remove('is-drag');
    if (!d.active || !d.target) return;
    var b = d.bk;
    if (d.target.room === b.room && (isHourMode() || d.target.col === Math.floor(colPos(b.in)))) return;
    var nights = nightsOf(b);
    var oldRoom = b.room;
    b.room = d.target.room;
    if (!isHourMode()) {
      var nin = colDate(d.target.col);
      b.in = new Date(nin.getFullYear(), nin.getMonth(), nin.getDate(), b.in.getHours(), b.in.getMinutes());
      b.out = addDays(startOfDay(b.in), nights);
      b.out.setHours(12, 0, 0, 0);
    }
    normalizeSt(b);                       // đổi ngày → trạng thái phải khớp lại mốc nghiệp vụ
    render();
    toast(oldRoom === b.room
      ? 'Đổi ngày ở thành công! ' + b.code + ' → ' + dmy(b.in)
      : 'Chuyển phòng thành công! ' + oldRoom + ' → ' + b.room);
  }

  /* ---------------- 11. Điều hướng ---------------- */
  function step(dir) {
    if (state.range === 'day') state.anchor = addDays(state.anchor, dir);
    else if (state.range === 'week') state.anchor = addDays(state.anchor, 7 * dir);
    else state.anchor = addMonths(state.anchor, dir);
    animateNext = true;
    render();
  }

  /* ---------------- 12. Gắn sự kiện ---------------- */
  function bind() {
    $('tlPrev').addEventListener('click', function () { step(-1); });
    $('tlNext').addEventListener('click', function () { step(1); });
    $('tlToday').addEventListener('click', function () {
      if (sameDay(state.anchor, HOTEL_NOW)) return;      // đã ở hôm nay → không đổi gì
      state.anchor = startOfDay(HOTEL_NOW); animateNext = true; render();
    });
    document.querySelectorAll('[data-range]').forEach(function (b) {
      b.addEventListener('click', function () { state.range = b.dataset.range; savePrefs(); animateNext = true; render(); syncGroupTabindex(); });
    });
    document.querySelectorAll('[data-group]').forEach(function (b) {
      b.addEventListener('click', function () { state.group = b.dataset.group; savePrefs(); animateNext = true; render(); syncGroupTabindex(); });
    });
    document.querySelectorAll('[data-chart]').forEach(function (b) {
      b.addEventListener('click', function () { state.chart = b.dataset.chart; savePrefs(); animateNext = true; render(); syncGroupTabindex(); });
    });

    // Bộ lọc trạng thái.
    $('tlLegend').addEventListener('click', function (e) {
      var b = e.target.closest('[data-legend]'); if (!b) return;
      setFilter(b.dataset.legend);
    });
    $('tlFilterClear').addEventListener('click', function () { setFilter(state.filter); });

    $('tlCardReader').addEventListener('click', function () { toast('Kích hoạt luồng đọc thẻ khoá từ…'); });
    // Bấm nút cấu hình → mở thẳng màn Cấu hình sơ đồ phòng, đúng tab của view đang xem
    $('tlConfig').addEventListener('click', function () {
      var tab = state.chart === 'detail' ? 'detail' : (state.chart === 'simple' ? 'simple' : 'timeline');
      gotoConfig(tab);
    });

    // grid: click
    $('tlScroll').addEventListener('click', function (e) {
      if (gselJustDone) { e.stopPropagation(); return; }
      var t = e.target;
      var grp = t.closest('[data-type]');
      if (grp && grp.classList.contains('tl-roomcell')) {
        var id = grp.dataset.type; state.collapsed[id] = !state.collapsed[id]; render(); return;
      }
      var bar = t.closest('.tl-bar');
      if (bar) { e.stopPropagation(); closeMenu(); openTip(bar); return; }   // chuột trái → popup thông tin
      var rc = t.closest('.tl-roomcell[data-room]');
      if (rc) { e.stopPropagation(); openRoomStatus(rc.dataset.room); return; }
      var td = t.closest('td.dc');
      if (td) { e.stopPropagation(); openMenu(td, null, td.dataset.room); return; }
    });

    // grid: CHUỘT PHẢI → menu thao tác nhanh (bar có booking, hoặc ô trống của phòng)
    $('tlScroll').addEventListener('contextmenu', function (e) {
      var bar = e.target.closest ? e.target.closest('.tl-bar') : null;
      if (bar) { e.preventDefault(); closeTip(); openMenu(bar, bar.dataset.bk, null); return; }
      var td = e.target.closest ? e.target.closest('td.dc') : null;
      if (td) { e.preventDefault(); closeTip(); openMenu(td, null, td.dataset.room); return; }
      var rc = e.target.closest ? e.target.closest('.tl-roomcell[data-room]') : null;
      if (rc) { e.preventDefault(); closeTip(); openMenu(rc, null, rc.dataset.room); }
    });

    // grid: hover tên loại → minitip
    $('tlScroll').addEventListener('mouseover', function (e) {
      // Popup thông tin KHÔNG mở bằng hover nữa — chuột trái mới mở (xem handler click).
      var hd = e.target.closest ? e.target.closest('th.tl-holiday') : null;
      if (hd) {
        var hcol = parseInt(hd.dataset.col, 10);
        var hh = isHoliday(colDate(hcol));
        if (hh) {
          var hc = $('tlHolidayChip'), hr = hd.getBoundingClientRect();
          hc.hidden = false; hc.textContent = hh.name;
          hc.style.left = (hr.left + hr.width / 2) + 'px';
          hc.style.top = (hr.top - 6) + 'px';
        }
      }
      var gn = e.target.closest ? e.target.closest('.tl-grp-name') : null;
      if (gn) showTip2(gn, gn.dataset.tip || gn.textContent);
    });
    $('tlScroll').addEventListener('mouseout', function (e) {
      // Popup mở bằng chuột trái → chỉ đóng bằng nút X, click ra ngoài hoặc Esc.
      if (e.target.closest && e.target.closest('.tl-grp-name')) hideTip2();
      if (e.target.closest && e.target.closest('th.tl-holiday')) $('tlHolidayChip').hidden = true;
    });
    $('tlScroll').addEventListener('scroll', function () { closeTip(); closeMenu(); hideTip2(); });
    document.addEventListener('click', function (e) {
      if (!$('tlPop').classList.contains('is-open')) return;
      if (e.target.closest && (e.target.closest('.tl-pop') || e.target.closest('.tl-menu') ||
          e.target.closest('.tl-bar') || e.target.closest('.tl-card') || e.target.closest('#lbBody'))) return;
      closeTip();
    }, true);
    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape' && $('tlPop').classList.contains('is-open')) { e.preventDefault(); closeTip(); }
    });

    // tooltip action buttons
    $('tlPop').addEventListener('click', function (e) {
      var t = e.target.closest('[data-act]'); if (!t) return;
      // Chặn nổi bọt: handler đóng menu ở document chạy sau sẽ đóng ngay menu mà
      // nút "Thao tác khác" vừa mở.
      e.stopPropagation();
      var act = t.dataset.act;
      if (act === 'close') { closeTip(); return; }
      if (act === 'copycode') { copyText(menuCtx.bk ? menuCtx.bk.code : ''); return; }
      // Nút "Thao tác khác": mở ĐÚNG menu của chuột phải, hiện NGAY CẠNH nút,
      // popup thông tin vẫn giữ nguyên (theo ảnh tham chiếu của PO).
      if (act === 'more') {
        if (menuIsOpen() && menuAnchor === t) { closeMenu(); return; }
        var mb = menuCtx.bk, mr = menuCtx.room;
        t.setAttribute('aria-expanded', 'true');
        openMenu(t, mb ? mb.id : null, mr, true);
        return;
      }
      if (act === 'note' || act === 'inote') {
        var bk = menuCtx.bk; if (!bk) return;
        openGenDlg(act === 'note' ? 'Ghi chú đặt phòng' : 'Ghi chú nội bộ',
          '<p style="margin:0;white-space:pre-wrap">' + esc((act === 'note' ? bk.note : bk.inote) || '') + '</p>');
        return;
      }
      if (act === 'delrepair') {
        var rb = menuCtx.bk; closeTip();
        rpOpenDelete(rb);
        return;
      }
      doAction(act);   // menuCtx đã được set khi mở popup
    });

    // menu
    $('tlMenu').addEventListener('click', function (e) {
      var sa = e.target.closest('[data-selact]');
      if (sa) { doSelAction(sa.dataset.selact); return; }
      var b = e.target.closest('[data-act]'); if (!b) return;
      closeMenu(); closeTip();                 // chọn xong đóng cả menu lẫn popup
      doAction(b.dataset.act);
    });

    // dialog
    document.addEventListener('click', function (e) {
      if (e.target.closest('[data-tl-close]')) { closeDlgs(); return; }
      if (!e.target.closest('.tl-menu')) closeMenu();
      if (!e.target.closest('.tl-pop') && !e.target.closest('.tl-menu') && !e.target.closest('.tl-bar') &&
          !e.target.closest('.tl-card') && !e.target.closest('#lbBody')) closeTip();
    });
    $('tlBackdrop').addEventListener('click', closeDlgs);
    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape') { closeDlgs(); closeMenu(); closeTip(); hideTip2(); if (dpIsOpen()) dpClose(true); }
    });

    $('tlSwDirty').addEventListener('click', function () { swToggle(this); });
    $('tlRoomConfirm').addEventListener('click', function () {
      if (roomDlgCtx) { roomDlgCtx.dirty = swOn($('tlSwDirty')); }
      closeDlgs(); render(); toast('Chuyển trạng thái phòng thành công!');
    });

    // kéo thả
    $('tlScroll').addEventListener('pointerdown', onPointerDown);
    window.addEventListener('pointermove', onPointerMove);
    window.addEventListener('pointerup', onPointerUp);

    window.addEventListener('resize', function () { render(); });
  }

  /* ---------------- 12a. Date picker (Vuexy AppDateTimePicker / flatpickr) ----------------
     Không nhúng flatpickr được (file phải self-contained, chỉ cho phép Roboto),
     nên dựng lại đúng hành vi & hình thức: mode single, dateFormat d/m/Y, locale vi. */
  var MONTHS_VI = ['Tháng 1','Tháng 2','Tháng 3','Tháng 4','Tháng 5','Tháng 6',
                   'Tháng 7','Tháng 8','Tháng 9','Tháng 10','Tháng 11','Tháng 12'];
  var dpView = null;               // tháng đang xem trong picker
  // dpTarget != null  → picker dang phuc vu 1 o ngay khac (vd. panel Dat phong nhanh)
  var dpTarget = null;
  function dpBase() { return dpTarget ? dpTarget.cur : state.anchor; }
  function dpAnchorEl() { return dpTarget ? dpTarget.el : $('tlDateBtn'); }
  function dpOpenFor(el, cur, cb) {
    dpTarget = { el: el, cur: startOfDay(cur), cb: cb };
    $('tlDp').classList.add('is-qb');
    dpOpen(true);
  }
  function dpOpen(keepTarget) {
    if (!keepTarget) { dpTarget = null; $('tlDp').classList.remove('is-qb'); }
    var base = dpBase();
    dpView = new Date(base.getFullYear(), base.getMonth(), 1);
    var sel = $('tlDpMonth');
    if (!sel.options.length) {
      sel.innerHTML = MONTHS_VI.map(function (m, i) { return '<option value="' + i + '">' + m + '</option>'; }).join('');
      $('tlDpWd').innerHTML = [1,2,3,4,5,6,0].map(function (d) {
        return '<span class="' + (d === 0 || d === 6 ? 'we' : '') + '">' + WD[d] + '</span>';
      }).join('');
    }
    dpRender();
    $('tlDp').classList.add('is-open');
    if (!dpTarget) $('tlDateBtn').setAttribute('aria-expanded', 'true');
    dpPlace();
    var f = $('tlDpDays').querySelector('button.sel') || $('tlDpDays').querySelector('button:not(.out)');
    if (f) f.focus();
  }
  function dpClose(refocus) {
    var back = dpAnchorEl();
    $('tlDp').classList.remove('is-open', 'is-qb');
    $('tlDateBtn').setAttribute('aria-expanded', 'false');
    dpTarget = null;
    if (refocus && back && back.focus) back.focus();
  }
  function dpIsOpen() { return $('tlDp').classList.contains('is-open'); }
  function dpPlace() {
    var el = $('tlDp'), r = dpAnchorEl().getBoundingClientRect();
    el.style.left = '0px'; el.style.top = '0px';
    var w = el.offsetWidth, h = el.offsetHeight;
    var top = r.bottom + 6;
    if (top + h > window.innerHeight - 8) top = Math.max(8, r.top - h - 6);
    el.style.left = Math.min(Math.max(8, r.left), window.innerWidth - w - 8) + 'px';
    el.style.top = top + 'px';
  }
  function dpRender() {
    $('tlDpMonth').value = String(dpView.getMonth());
    $('tlDpYear').value = String(dpView.getFullYear());
    var first = new Date(dpView.getFullYear(), dpView.getMonth(), 1);
    var lead = (first.getDay() + 6) % 7;                 // lưới bắt đầu từ Thứ 2
    var start = addDays(first, -lead);
    var h = [];
    for (var i = 0; i < 42; i++) {
      var d = addDays(start, i);
      var cls = [];
      if (d.getMonth() !== dpView.getMonth()) cls.push('out');
      if (d.getDay() === 0 || d.getDay() === 6) cls.push('we');
      if (sameDay(d, HOTEL_NOW)) cls.push('today');
      if (sameDay(d, dpBase())) cls.push('sel');
      h.push('<button type="button" role="gridcell" class="' + cls.join(' ') + '" data-d="' + ymd(d) + '" ' +
        'aria-label="' + dmy(d) + '"' + (cls.indexOf('sel') >= 0 ? ' aria-selected="true"' : '') + '>' + d.getDate() + '</button>');
    }
    $('tlDpDays').innerHTML = h.join('');
  }
  function dpPick(iso) {
    var p = iso.split('-').map(Number);
    var d = new Date(p[0], p[1] - 1, p[2]);
    if (isNaN(d.getTime())) return;                       // sai định dạng → không ghi nhận
    if (dpTarget) { var cb = dpTarget.cb; dpClose(true); cb(startOfDay(d)); return; }
    state.anchor = startOfDay(d);
    animateNext = true;
    dpClose(true);
    render();
  }
  function dpMove(days) {
    var cur = document.activeElement;
    if (!cur || !cur.dataset || !cur.dataset.d) return;
    var p = cur.dataset.d.split('-').map(Number);
    var d = addDays(new Date(p[0], p[1] - 1, p[2]), days);
    if (d.getMonth() !== dpView.getMonth() || d.getFullYear() !== dpView.getFullYear()) {
      dpView = new Date(d.getFullYear(), d.getMonth(), 1);
      dpRender();
    }
    var next = $('tlDpDays').querySelector('button[data-d="' + ymd(d) + '"]');
    if (next) next.focus();
  }
  function bindDatePicker() {
    $('tlDateBtn').addEventListener('click', function () { dpIsOpen() ? dpClose(true) : dpOpen(); });
    $('tlDp').addEventListener('click', function (e) {
      var act = e.target.closest('[data-dp]');
      if (act) {
        var a = act.dataset.dp;
        if (a === 'prevMonth') { dpView = addMonths(dpView, -1); dpRender(); }
        else if (a === 'nextMonth') { dpView = addMonths(dpView, 1); dpRender(); }
        else if (a === 'today') { dpPick(ymd(HOTEL_NOW)); }
        else if (a === 'close') { dpClose(true); }
        return;
      }
      var day = e.target.closest('button[data-d]');
      if (day) dpPick(day.dataset.d);
    });
    $('tlDpMonth').addEventListener('change', function () {
      dpView = new Date(dpView.getFullYear(), +this.value, 1); dpRender();
    });
    $('tlDpYear').addEventListener('change', function () {
      var y = parseInt(this.value, 10);
      if (!(y >= 1900 && y <= 2999)) { this.value = String(dpView.getFullYear()); return; }
      dpView = new Date(y, dpView.getMonth(), 1); dpRender();
    });
    $('tlDp').addEventListener('keydown', function (e) {
      var k = e.key, m = null;
      if (k === 'ArrowLeft') m = -1; else if (k === 'ArrowRight') m = 1;
      else if (k === 'ArrowUp') m = -7; else if (k === 'ArrowDown') m = 7;
      if (m !== null && document.activeElement.dataset.d) { e.preventDefault(); dpMove(m); return; }
      if (k === 'PageUp')   { e.preventDefault(); dpView = addMonths(dpView, -1); dpRender(); return; }
      if (k === 'PageDown') { e.preventDefault(); dpView = addMonths(dpView, 1); dpRender(); return; }
      if (k === 'Escape')   { e.preventDefault(); dpClose(true); }
    });
    // CAPTURE phase: các handler trong lưới có gọi stopPropagation(), nếu nghe ở bubble
    // thì click vào ô/booking sẽ không đóng được picker.
    document.addEventListener('click', function (e) {
      if (!dpIsOpen()) return;
      if (e.target.closest('#tlDp') || e.target.closest('#tlDateBtn')) return;
      if (dpTarget && dpTarget.el && e.target.closest && e.target.contains && dpTarget.el.contains(e.target)) return;
      dpClose(false);
    }, true);
    window.addEventListener('resize', function () { if (dpIsOpen()) dpPlace(); });
  }

  /* ---------------- 12b. Button Group: điều hướng bàn phím ---------------- */
  // buttongroup.md: Tab vào 1 điểm duy nhất trong group, ← → di chuyển giữa các nút (roving tabindex).
  // Chỉ áp cho toggle group thật (.tl-seg, .tl-viewswitch) — date navigator giữ Tab bình thường
  // vì nó chứa ô nhập ngày, roving sẽ làm ô đó không Tab tới được.
  function groupItems(g) { return Array.prototype.slice.call(g.querySelectorAll('button')); }
  function syncGroupTabindex() {
    document.querySelectorAll('.tl-seg, .tl-viewswitch').forEach(function (g) {
      var items = groupItems(g);
      var act = items.findIndex(function (b) { return b.getAttribute('aria-pressed') === 'true'; });
      if (act < 0) act = 0;
      items.forEach(function (b, i) { b.tabIndex = i === act ? 0 : -1; });
    });
  }
  function bindGroupKeyboard() {
    document.querySelectorAll('.tl-seg, .tl-viewswitch').forEach(function (g) {
      g.addEventListener('keydown', function (e) {
        var k = e.key;
        if (k !== 'ArrowLeft' && k !== 'ArrowRight' && k !== 'Home' && k !== 'End') return;
        var items = groupItems(g).filter(function (b) { return !b.disabled; });
        var i = items.indexOf(document.activeElement);
        if (i < 0) return;
        e.preventDefault();
        var n = k === 'ArrowLeft' ? (i - 1 + items.length) % items.length
              : k === 'ArrowRight' ? (i + 1) % items.length
              : k === 'Home' ? 0 : items.length - 1;
        items.forEach(function (b) { b.tabIndex = -1; });
        items[n].tabIndex = 0;
        items[n].focus();
      });
    });
    syncGroupTabindex();
  }

  /* ---------------- 13. Đồng hồ realtime ---------------- */
  // Thanh đỏ bám đồng hồ thật: mỗi CLOCK_MS cập nhật lại vị trí (CSS transition .6s linear
  // làm nó trôi mượt thay vì nhảy). Sang ngày mới thì vẽ lại cả lưới.
  var clockTimer = null;
  function tickClock() {
    var prev = HOTEL_NOW;
    HOTEL_NOW = new Date();
    if (!sameDay(prev, HOTEL_NOW)) {
      normalizeAll();                            // sang ngày mới → mốc nghiệp vụ đổi, trạng thái booking đổi theo
      render();                                  // qua nửa đêm: cột "hôm nay", công suất, nút Hôm nay đều đổi
    } else {
      renderNow();
      $('tlToday').setAttribute('aria-pressed', String(sameDay(state.anchor, HOTEL_NOW)));
    }
    qbClockTick();
  }
  function startClock() {
    if (clockTimer) clearInterval(clockTimer);
    clockTimer = setInterval(tickClock, CLOCK_MS);
    document.addEventListener('visibilitychange', function () {
      if (!document.hidden) tickClock();        // tab ẩn → timer bị throttle, quay lại phải đồng bộ ngay
    });
    window.addEventListener('focus', tickClock);
  }



  /* ================================================================
     15. ĐẶT PHÒNG NHANH (Quick Booking) — SRS B1
     Figma: 46683:170162 · 46701:175379 · 47924:231992
            47787:87196  (goi y khach da co du lieu)
            45163:286669 (ReactDatePicker + TimePicker)
            55583:60835  (Loading khi he thong tao dat phong)
     ----------------------------------------------------------------
     Nguyên tắc thời gian: KHÔNG hard-code bất kỳ ngày/giờ nào.
     Mọi mốc đều phái sinh từ HOTEL_NOW (đồng hồ thật) và vùng ô người
     dùng kéo chọn trên lưới (state.anchor + state.range → colDate()).
     ================================================================ */

  /* ---- Cấu hình giờ mặc định của khách sạn (SRS 3.1) ---- */
  var QB_CFG_IN_H = 14, QB_CFG_IN_M = 0;     // giờ nhận phòng cấu hình
  var QB_CFG_OUT_H = 12, QB_CFG_OUT_M = 0;   // giờ trả phòng cấu hình
  var QB_MAX_NIGHTS = 999;
  var QB_EMPTY = 'Không được để trống';

  function qbArrivalOn(day) {
    var d = startOfDay(day);
    d.setHours(QB_CFG_IN_H, QB_CFG_IN_M, 0, 0);
    if (sameDay(day, HOTEL_NOW) && HOTEL_NOW.getTime() > d.getTime()) return new Date(HOTEL_NOW.getTime());
    return d;
  }
  function qbDepartureOn(day) {
    var d = startOfDay(day);
    d.setHours(QB_CFG_OUT_H, QB_CFG_OUT_M, 0, 0);
    return d;
  }

  /* ---- Danh mục (mô phỏng nguồn ezOne.Admin / ezIms) ---- */
  var QB_FLAG_VN = '<svg viewBox="0 0 24 18" fill="none" aria-hidden="true"><rect width="24" height="18" rx="2" fill="#DA251D"/><path d="M12 4.6l1.1 3.3h3.5l-2.8 2 1.1 3.3-2.9-2-2.9 2 1.1-3.3-2.8-2h3.5z" fill="#FFCD00"/></svg>';
  var QB_LOGO_EZ = '<svg viewBox="0 0 24 18" fill="none" aria-hidden="true"><rect width="24" height="18" rx="2" fill="#186FF4" opacity=".12"/><path d="M5 12.5c1.6-3.4 4-5 7-5s5.4 1.6 7 5H5z" fill="#186FF4"/></svg>';

  var QB_COMPANIES = ['EzCloud', 'Công ty ABC', 'ezCloud Global JSC', 'Công ty CP Du lịch Sen Vàng'];
  var QB_MARKETS   = ['Trong nước', 'Quốc tế', 'Nội địa', 'Châu Á'];
  var QB_SOURCES   = ['Walk-in', 'Trực tiếp', 'Điện thoại', 'Booking.com', 'Agoda.com'];
  var QB_STAFFS    = ['Nguyễn Hoàng Kiều Trinh', 'Nguyễn Văn Thảo Nguyên', 'Phạm Thu Trang'];
  var QB_SALUTS    = ['Mr', 'Ms', 'Mrs', 'Miss', 'Dr', 'Professor', 'Sir', 'Madam', "Ma'am"];
  var QB_GENDERS   = ['Nam', 'Nữ', 'Khác'];
  var QB_NATIONS   = ['Việt Nam', 'Hoa Kỳ', 'Nhật Bản', 'Hàn Quốc', 'Trung Quốc', 'Pháp', 'Anh'];
  var QB_DIALS     = ['+84', '+1', '+81', '+82', '+86', '+33', '+44'];
  var QB_DOCTYPES  = ['Căn cước công dân', 'Hộ chiếu', 'VISA', 'Giấy phép lái xe'];
  var QB_METHODS   = ['Tiền mặt', 'Chuyển khoản', 'Thẻ', 'QR code', 'Công nợ'];
  var QB_CURRS     = ['VND', 'USD'];

  /* Bảng giá 8 loại phòng — mặt bằng khách sạn 1-2 sao */
  var QB_RATES = {
    SGL: [{ id: 'SGL-D', name: 'Giá ngày', price: 250000 }, { id: 'SGL-W', name: 'Giá cuối tuần', price: 300000 }, { id: 'SGL-H', name: 'Giá theo giờ', price: 80000 }],
    DOR: [{ id: 'DOR-D', name: 'Giá ngày', price: 120000 }, { id: 'DOR-W', name: 'Giá cuối tuần', price: 150000 }],
    TWN: [{ id: 'TWN-D', name: 'Giá ngày', price: 350000 }, { id: 'TWN-W', name: 'Giá cuối tuần', price: 420000 }, { id: 'TWN-H', name: 'Giá theo giờ', price: 100000 }],
    TRP: [{ id: 'TRP-D', name: 'Giá ngày', price: 450000 }, { id: 'TRP-W', name: 'Giá cuối tuần', price: 550000 }],
    DBL: [{ id: 'DBL-D', name: 'Giá ngày', price: 320000 }, { id: 'DBL-W', name: 'Giá cuối tuần', price: 400000 }, { id: 'DBL-H', name: 'Giá theo giờ', price: 90000 }],
    FAM: [{ id: 'FAM-D', name: 'Giá ngày', price: 600000 }, { id: 'FAM-W', name: 'Giá cuối tuần', price: 720000 }],
    STD: [{ id: 'STD-D', name: 'Giá ngày', price: 400000 }, { id: 'STD-W', name: 'Giá cuối tuần', price: 480000 }],
    SUP: [{ id: 'SUP-D', name: 'Giá ngày', price: 550000 }, { id: 'SUP-W', name: 'Giá cuối tuần', price: 650000 }]
  };
  var QB_OCC = {
    SGL: { a: 1, c: 0, i: 0 }, DOR: { a: 1, c: 0, i: 0 },
    TWN: { a: 2, c: 1, i: 1 }, TRP: { a: 3, c: 1, i: 1 },
    DBL: { a: 2, c: 1, i: 1 }, FAM: { a: 4, c: 2, i: 1 },
    STD: { a: 2, c: 1, i: 1 }, SUP: { a: 2, c: 1, i: 1 }
  };
  var QB_EXTRA = { a: 100000, c: 50000, i: 0 };

  /* Hồ sơ khách cũ (Figma 47787:87196) — tuổi tính động theo HOTEL_NOW */
  var QB_GUESTS = [
    { last: 'Nguyễn Trần', first: 'Minh Quân', dob: '10/10/1996', phone: '0912.345.678', email: 'quan.ntm@gmail.com', doc: '0010968274582', g: 'Nam' },
    { last: 'Lê Thị', first: 'Bích Ngọc', dob: '15/05/1994', phone: '0987.654.321', email: 'ngoc.ltb@gmail.com', doc: '0010968274583', g: 'Nữ' },
    { last: 'Trần Văn', first: 'Hưng', dob: '20/12/1991', phone: '0901.234.567', email: 'hung.tv@gmail.com', doc: '0010968274584', g: 'Nam' },
    { last: 'Phạm Hoàng', first: 'Anh', dob: '05/07/1998', phone: '0934.567.890', email: 'anh.ph@gmail.com', doc: '0010968274585', g: 'Nam' },
    { last: 'Bùi Thị', first: 'Lan', dob: '12/11/1999', phone: '0915.678.901', email: 'lan.bt@gmail.com', doc: '0010968274586', g: 'Nữ' },
    { last: 'Ngô Văn', first: 'Bình', dob: '17/03/1995', phone: '0905.432.109', email: 'binh.nv@gmail.com', doc: '0010968274587', g: 'Nam' },
    { last: 'Võ Minh', first: 'Tâm', dob: '22/08/1991', phone: '0978.901.234', email: 'tam.vm@gmail.com', doc: '0010968274588', g: 'Nam' }
  ];

  var qb = {
    open: false, busy: false, tab: 'prebook', status: 'definite',
    in: null, inPre: null, out: null, nights: 1, inFixed: false,
    paymode: 'pay', pct: null, paid: 0, linked: null, lastFocus: null
  };

  function qbEl(id) { return document.getElementById(id); }
  function qbNum(s) { var n = parseInt(String(s).replace(/[^\d]/g, ''), 10); return isNaN(n) ? 0 : n; }
  function qbDigits(s) { return String(s || '').replace(/[^\d]/g, ''); }
  function qbParseDMY(s) {
    var m = /^(\d{1,2})\/(\d{1,2})\/(\d{4})$/.exec(String(s || '').trim());
    if (!m) return null;
    var d = new Date(+m[3], +m[2] - 1, +m[1]);
    if (isNaN(d.getTime()) || d.getDate() !== +m[1] || d.getMonth() !== +m[2] - 1) return null;
    return d;
  }
  function qbAge(dobStr) {
    var d = qbParseDMY(dobStr); if (!d) return null;
    var a = HOTEL_NOW.getFullYear() - d.getFullYear();
    var m = HOTEL_NOW.getMonth() - d.getMonth();
    if (m < 0 || (m === 0 && HOTEL_NOW.getDate() < d.getDate())) a--;
    return a;
  }

  /* ================================================================
     15.1 Select kiểu Vuexy (AppSelect / VSelect — variant outlined)
     Menu render trong #qbVsMenu (position:fixed) nên không bị .qb-body
     cắt mất khi dropdown dài hơn vùng cuộn.
     ================================================================ */
  var QB_SEL = {};                 // id → { items, value, ph, el, btn }
  var vsCur = null, vsAct = -1;

  function vsDef(id, items, value, ph) {
    var host = document.querySelector('.qb-vs[data-qbsel="' + id + '"]');
    if (!host) return;
    var list = items.map(function (o) {
      return typeof o === 'string' ? { v: o, t: o } : o;
    });
    if (!QB_SEL[id]) {
      host.innerHTML =
        '<button class="qb-vs-btn" type="button" id="' + id + '" aria-haspopup="listbox" aria-expanded="false"' +
        ' aria-label="' + esc(host.dataset.qblabel || id) + '">' +
        '<span class="qb-vs-lead"></span><span class="qb-vs-val"></span></button>' +
        '<span class="qb-cv" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg></span>';
      QB_SEL[id] = { el: host, btn: host.querySelector('.qb-vs-btn') };
      QB_SEL[id].btn.addEventListener('click', function (e) { e.stopPropagation(); vsToggle(id); });
      QB_SEL[id].btn.addEventListener('keydown', function (e) { vsKey(id, e); });
    }
    QB_SEL[id].items = list;
    QB_SEL[id].ph = ph || '';
    vsSet(id, value !== undefined ? value : (list[0] ? list[0].v : ''), true);
  }
  function vsItem(id) {
    var s = QB_SEL[id]; if (!s) return null;
    for (var i = 0; i < s.items.length; i++) if (s.items[i].v === s.value) return s.items[i];
    return null;
  }
  function vsVal(id) { return QB_SEL[id] ? QB_SEL[id].value : ''; }
  function vsSet(id, v, silent) {
    var s = QB_SEL[id]; if (!s) return;
    s.value = v;
    var it = vsItem(id);
    var lead = s.btn.querySelector('.qb-vs-lead');
    lead.innerHTML = it && it.lead ? it.lead : '';
    lead.className = 'qb-vs-lead' + (it && it.lead ? ' qb-lead' : '');
    s.btn.querySelector('.qb-vs-val').textContent = it ? it.t : (s.ph || '');
    s.btn.classList.toggle('is-ph', !it);
    if (!silent && s.onChange) s.onChange(v);
  }
  function vsPlace() {
    if (!vsCur) return;
    var m = qbEl('qbVsMenu'), b = QB_SEL[vsCur].btn, r = b.getBoundingClientRect();
    m.style.left = '0px'; m.style.top = '0px'; m.style.minWidth = Math.max(120, r.width) + 'px';
    var h = m.offsetHeight, w = m.offsetWidth;
    var top = r.bottom + 4;
    if (top + h > window.innerHeight - 8) top = Math.max(8, r.top - h - 4);
    m.style.top = top + 'px';
    m.style.left = Math.min(Math.max(8, r.left), window.innerWidth - w - 8) + 'px';
  }
  function vsOpen(id) {
    vsClose();
    var s = QB_SEL[id]; if (!s) return;
    vsCur = id;
    var m = qbEl('qbVsMenu');
    m.innerHTML = s.items.map(function (o, i) {
      return '<button type="button" role="option" data-vsi="' + i + '"' +
        (o.v === s.value ? ' aria-selected="true"' : ' aria-selected="false"') + '>' +
        (o.lead || '') + '<span>' + esc(o.t) + '</span></button>';
    }).join('') || '<button type="button" disabled>Không có dữ liệu</button>';
    m.classList.add('is-open');
    s.el.classList.add('is-open');
    s.btn.setAttribute('aria-expanded', 'true');
    vsAct = s.items.findIndex(function (o) { return o.v === s.value; });
    vsPaintAct();
    vsPlace();
    var sel = m.querySelector('[aria-selected="true"]');
    if (sel) sel.scrollIntoView({ block: 'nearest' });
  }
  function vsPaintAct() {
    var bs = qbEl('qbVsMenu').querySelectorAll('button');
    bs.forEach(function (b, i) { b.classList.toggle('is-act', i === vsAct); });
    if (vsAct >= 0 && bs[vsAct]) bs[vsAct].scrollIntoView({ block: 'nearest' });
  }
  function vsClose(refocus) {
    var m = qbEl('qbVsMenu');
    if (!m || !vsCur) { if (m) m.classList.remove('is-open'); return; }
    var s = QB_SEL[vsCur];
    m.classList.remove('is-open');
    s.el.classList.remove('is-open');
    s.btn.setAttribute('aria-expanded', 'false');
    if (refocus) s.btn.focus();
    vsCur = null; vsAct = -1;
  }
  function vsToggle(id) { if (vsCur === id) vsClose(true); else vsOpen(id); }
  function vsPick(i) {
    if (!vsCur) return;
    var id = vsCur, s = QB_SEL[id];
    if (!s.items[i]) return;
    vsClose(true);
    vsSet(id, s.items[i].v);
    qbClearErr(id);
  }
  function vsKey(id, e) {
    var k = e.key, open = vsCur === id;
    if (k === 'Enter' || k === ' ' || k === 'ArrowDown' || k === 'ArrowUp') {
      if (!open) { e.preventDefault(); vsOpen(id); return; }
    }
    if (!open) return;
    var n = QB_SEL[id].items.length;
    if (k === 'ArrowDown') { e.preventDefault(); vsAct = (vsAct + 1 + n) % n; vsPaintAct(); }
    else if (k === 'ArrowUp') { e.preventDefault(); vsAct = (vsAct - 1 + n) % n; vsPaintAct(); }
    else if (k === 'Home') { e.preventDefault(); vsAct = 0; vsPaintAct(); }
    else if (k === 'End') { e.preventDefault(); vsAct = n - 1; vsPaintAct(); }
    else if (k === 'Enter' || k === ' ') { e.preventDefault(); vsPick(vsAct); }
    else if (k === 'Escape' || k === 'Tab') { vsClose(k === 'Escape'); }
  }

  /* ================================================================
     15.2 Bộ lịch ngày + giờ (Figma 45163:286669)
     ================================================================ */
  var QB_WD_EN = ['Su', 'Mo', 'Tu', 'We', 'Th', 'Fr', 'Sa'];
  var QB_MONTHS = ['Tháng 1', 'Tháng 2', 'Tháng 3', 'Tháng 4', 'Tháng 5', 'Tháng 6',
                   'Tháng 7', 'Tháng 8', 'Tháng 9', 'Tháng 10', 'Tháng 11', 'Tháng 12'];
  var dtp = { open: false, view: null, sel: null, h: 0, m: 0, time: false, anchor: null, cb: null };

  function dtpIsOpen() { return dtp.open; }
  function dtpOpen(anchorEl, cur, withTime, cb) {
    var base = cur instanceof Date && !isNaN(cur.getTime()) ? new Date(cur.getTime()) : new Date(HOTEL_NOW.getTime());
    dtp.open = true; dtp.anchor = anchorEl; dtp.cb = cb; dtp.time = !!withTime;
    dtp.sel = startOfDay(base);
    dtp.view = new Date(base.getFullYear(), base.getMonth(), 1);
    dtp.h = withTime ? base.getHours() : 0;
    dtp.m = withTime ? base.getMinutes() : 0;
    qbEl('qbDtpTime').hidden = !withTime;
    if (!qbEl('qbDtpWd').childNodes.length) {
      qbEl('qbDtpWd').innerHTML = QB_WD_EN.map(function (d) { return '<span>' + d + '</span>'; }).join('');
      qbEl('qbDtpMonth').innerHTML = QB_MONTHS.map(function (m, i) { return '<option value="' + i + '">' + m + '</option>'; }).join('');
    }
    var y0 = HOTEL_NOW.getFullYear();
    var ys = [];
    for (var y = y0 - 100; y <= y0 + 10; y++) ys.push('<option value="' + y + '">' + y + '</option>');
    qbEl('qbDtpYear').innerHTML = ys.join('');
    dtpRender();
    qbEl('qbDtp').classList.add('is-open');
    dtpPlace();
  }
  function dtpClose(refocus) {
    dtp.open = false;
    qbEl('qbDtp').classList.remove('is-open');
    if (refocus && dtp.anchor && dtp.anchor.focus) dtp.anchor.focus();
    dtp.anchor = null; dtp.cb = null;
  }
  function dtpPlace() {
    if (!dtp.open || !dtp.anchor) return;
    var el = qbEl('qbDtp'), r = dtp.anchor.getBoundingClientRect();
    el.style.left = '0px'; el.style.top = '0px';
    var w = el.offsetWidth, h = el.offsetHeight;
    var top = r.bottom + 6;
    if (top + h > window.innerHeight - 8) top = Math.max(8, r.top - h - 6);
    el.style.top = top + 'px';
    el.style.left = Math.min(Math.max(8, r.left), window.innerWidth - w - 8) + 'px';
  }
  function dtpRender() {
    qbEl('qbDtpMonth').value = String(dtp.view.getMonth());
    qbEl('qbDtpYear').value = String(dtp.view.getFullYear());
    var first = new Date(dtp.view.getFullYear(), dtp.view.getMonth(), 1);
    var start = addDays(first, -first.getDay());        // lưới bắt đầu Chủ nhật (Su) — theo Figma
    var h = [];
    for (var i = 0; i < 42; i++) {
      var d = addDays(start, i), cls = [];
      if (d.getMonth() !== dtp.view.getMonth()) cls.push('out');
      if (sameDay(d, HOTEL_NOW)) cls.push('today');
      if (sameDay(d, dtp.sel)) cls.push('sel');
      h.push('<button type="button" role="gridcell" class="' + cls.join(' ') + '" data-d="' + ymd(d) + '"' +
        ' aria-label="' + dmy(d) + '"' + (cls.indexOf('sel') >= 0 ? ' aria-selected="true"' : '') + '>' + d.getDate() + '</button>');
    }
    qbEl('qbDtpDays').innerHTML = h.join('');
    qbEl('qbDtpH').value = pad(dtp.h);
    qbEl('qbDtpM').value = pad(dtp.m);
  }
  function dtpResult() {
    var d = new Date(dtp.sel.getTime());
    d.setHours(dtp.time ? dtp.h : 0, dtp.time ? dtp.m : 0, 0, 0);
    return d;
  }
  function bindDtp() {
    qbEl('qbDtp').addEventListener('click', function (e) {
      var a = e.target.closest('[data-qbdtp]');
      if (a) {
        var k = a.dataset.qbdtp;
        if (k === 'prev') { dtp.view = addMonths(dtp.view, -1); dtpRender(); }
        else if (k === 'next') { dtp.view = addMonths(dtp.view, 1); dtpRender(); }
        else if (k === 'h+') { dtp.h = (dtp.h + 1) % 24; dtpRender(); }
        else if (k === 'h-') { dtp.h = (dtp.h + 23) % 24; dtpRender(); }
        else if (k === 'm+') { dtp.m = (dtp.m + 5) % 60; dtpRender(); }
        else if (k === 'm-') { dtp.m = (dtp.m + 55) % 60; dtpRender(); }
        else if (k === 'cancel') { dtpClose(true); }
        else if (k === 'ok') { var cb = dtp.cb, v = dtpResult(); dtpClose(true); if (cb) cb(v); }
        return;
      }
      var day = e.target.closest('button[data-d]');
      if (day) {
        var p = day.dataset.d.split('-').map(Number);
        dtp.sel = new Date(p[0], p[1] - 1, p[2]);
        if (!dtp.time) { var cb2 = dtp.cb, v2 = dtpResult(); dtpClose(true); if (cb2) cb2(v2); return; }
        dtpRender();
      }
    });
    qbEl('qbDtpMonth').addEventListener('change', function () {
      dtp.view = new Date(dtp.view.getFullYear(), +this.value, 1); dtpRender();
    });
    qbEl('qbDtpYear').addEventListener('change', function () {
      dtp.view = new Date(+this.value, dtp.view.getMonth(), 1); dtpRender();
    });
    qbEl('qbDtpH').addEventListener('input', function () {
      var v = qbNum(this.value); dtp.h = Math.max(0, Math.min(23, v));
    });
    qbEl('qbDtpM').addEventListener('input', function () {
      var v = qbNum(this.value); dtp.m = Math.max(0, Math.min(59, v));
    });
    qbEl('qbDtp').addEventListener('keydown', function (e) {
      if (e.key === 'Escape') { e.preventDefault(); dtpClose(true); }
    });
  }

  /* ================================================================
     15.3 Tính toán
     ================================================================ */
  function qbRoomFree(roomId, inD, outD) {
    for (var i = 0; i < BOOKINGS.length; i++) {
      var b = BOOKINGS[i];
      if (b.room !== roomId || b.st === 'checkout') continue;
      if (startOfDay(b.in) < startOfDay(outD) && startOfDay(inD) < startOfDay(b.out)) return false;
    }
    return true;
  }
  function qbFreeRooms(typeId, inD, outD) {
    return ROOMS.filter(function (r) { return r.type === typeId && qbRoomFree(r.id, inD, outD); });
  }
  function qbRateList() { return QB_RATES[vsVal('qbType')] || QB_RATES[TYPES[0].id]; }
  function qbRateObj() {
    var l = qbRateList(), v = vsVal('qbRate');
    for (var i = 0; i < l.length; i++) if (l[i].id === v) return l[i];
    return l[0];
  }
  function qbTotal() {
    var r = qbRateObj(), occ = QB_OCC[vsVal('qbType')] || { a: 2, c: 0, i: 0 };
    var a = Math.max(1, qbNum(qbEl('qbAdults').value));
    var c = Math.max(0, qbNum(qbEl('qbChildren').value));
    var i = Math.max(0, qbNum(qbEl('qbInfants').value));
    var extra = Math.max(0, a - occ.a) * QB_EXTRA.a + Math.max(0, c - occ.c) * QB_EXTRA.c + Math.max(0, i - occ.i) * QB_EXTRA.i;
    return (r.price + extra) * qb.nights;
  }

  /* ================================================================
     15.4 Validate
     ================================================================ */
  function qbFieldOf(id) {
    var e = qbEl(id) || document.querySelector('.qb-vs[data-qbsel="' + id + '"]');
    return e && e.closest ? e.closest('.qb-f') : null;
  }
  function qbSetErr(id, on, msg) {
    var f = qbFieldOf(id); if (!f) return;
    f.classList.toggle('is-err', !!on);
    if (msg) { var m = f.querySelector('.qb-msg'); if (m) m.textContent = msg; }
  }
  function qbClearErr(id) { qbSetErr(id, false); }
  function qbClearErrors() {
    document.querySelectorAll('#qbPanel .qb-f.is-err').forEach(function (f) { f.classList.remove('is-err'); });
  }

  /* ================================================================
     15.5 Mở / đóng panel
     ================================================================ */
  function qbBuildSelects() {
    vsDef('qbCompany', QB_COMPANIES.map(function (c, i) { return { v: c, t: c, lead: i === 0 ? QB_LOGO_EZ : '' }; }), QB_COMPANIES[0]);
    vsDef('qbMarket', QB_MARKETS, QB_MARKETS[0]);
    vsDef('qbSource', QB_SOURCES, QB_SOURCES[0]);
    vsDef('qbStaff', QB_STAFFS, QB_STAFFS[0]);
    vsDef('qbSalut', [{ v: '', t: 'Danh xưng' }].concat(QB_SALUTS), '', 'Danh xưng');
    vsDef('qbGender', QB_GENDERS, QB_GENDERS[0]);
    vsDef('qbNation', QB_NATIONS.map(function (n) { return { v: n, t: n, lead: n === 'Việt Nam' ? QB_FLAG_VN : '' }; }), QB_NATIONS[0]);
    vsDef('qbDial', QB_DIALS, QB_DIALS[0]);
    vsDef('qbDocType', QB_DOCTYPES, QB_DOCTYPES[0]);
    vsDef('qbPayMethod', QB_METHODS, QB_METHODS[0]);
    vsDef('qbCurrency', QB_CURRS, QB_CURRS[0]);
    vsDef('qbDebtCompany', [{ v: '', t: 'Chọn công ty' }].concat(QB_COMPANIES), '', 'Chọn công ty');
    QB_SEL.qbType.onChange = function (v) {
      vsDef('qbRate', qbRateList().map(function (r) { return { v: r.id, t: r.name }; }), qbRateList()[0].id);
      var occ = QB_OCC[v] || { a: 2, c: 0, i: 0 };
      qbEl('qbAdults').value = String(occ.a);
      qbEl('qbChildren').value = String(occ.c);
      qbEl('qbInfants').value = String(occ.i);
      qbSyncRooms(true); qbSyncMoney();
    };
    QB_SEL.qbRate.onChange = function () { qbSyncMoney(); };
    QB_SEL.qbPayMethod.onChange = function () { qbSyncMoney(); };
  }

  function qbOpen(roomId, c0, c1, tab) {
    if (qb.busy) return;
    var r = roomById(roomId);
    var a = Math.min(c0, c1), z = Math.max(c0, c1), d0, d1;
    if (isHourMode()) { d0 = startOfDay(state.anchor); d1 = startOfDay(state.anchor); }
    else { d0 = colDate(a); d1 = colDate(z); }

    qb.tab = tab === 'checkin' ? 'checkin' : 'prebook';
    qb.status = 'definite';
    // v24.1: ngày giờ đến = thời điểm BẮT ĐẦU KÉO trên sơ đồ (kể cả tab Nhận phòng ngay),
    // không lấy giờ hiện tại của hệ thống nữa.
    qb.inFixed = !(c0 === null || c0 === undefined || isNaN(c0));
    qb.in = qbArrivalOn(d0);
    qb.inPre = new Date(qb.in.getTime());
    qb.out = qbDepartureOn(addDays(d1, 1));
    if (qb.out.getTime() <= qb.in.getTime()) qb.out = qbDepartureOn(addDays(startOfDay(qb.in), 1));
    qb.nights = Math.max(1, dayDiff(qb.out, qb.in));
    qb.paymode = 'pay'; qb.pct = null; qb.paid = 0; qb.linked = null;

    vsDef('qbType', TYPES.map(function (t) { return { v: t.id, t: t.name }; }), r ? r.type : TYPES[0].id);
    vsDef('qbRate', qbRateList().map(function (x) { return { v: x.id, t: x.name }; }), qbRateList()[0].id);
    qbBuildSelects();

    ['qbLast', 'qbFirst', 'qbDob', 'qbPhone', 'qbEmail', 'qbDocNo', 'qbDocExp', 'qbNote', 'qbAmount'].forEach(function (id) {
      var e = qbEl(id); if (e) e.value = '';
    });
    var occ = QB_OCC[vsVal('qbType')] || { a: 2, c: 0, i: 0 };
    qbEl('qbAdults').value = String(occ.a);
    qbEl('qbChildren').value = String(occ.c);
    qbEl('qbInfants').value = String(occ.i);
    document.querySelectorAll('[data-qbstatus]').forEach(function (x) {
      x.setAttribute('aria-pressed', String(x.dataset.qbstatus === 'definite'));
    });
    document.querySelectorAll('[data-qbpaymode]').forEach(function (x) {
      x.setAttribute('aria-pressed', String(x.dataset.qbpaymode === 'pay'));
    });
    qbClearErrors();
    qbUnlink();
    qbSuggClose();

    qb.lastFocus = document.activeElement;
    qb.open = true;
    qbEl('qbPanel').hidden = false;
    void qbEl('qbPanel').offsetWidth;
    qbEl('qbPanel').classList.add('is-open');
    qbEl('qbBackdrop').classList.add('is-open');
    qbSyncAll();
    // phòng đã chọn nếu còn trống trong khoảng ngày (SRS 3.4)
    if (r && qbRoomFree(r.id, qb.in, qb.out)) vsSet('qbRoom', r.id, true);
    qbSyncRooms(false);
    setTimeout(function () { var f = qbEl('qbTabPre'); if (f) f.focus(); }, 0);
  }

  function qbClose() {
    if (!qb.open || qb.busy) return;
    qb.open = false;
    qbEl('qbPanel').classList.remove('is-open');
    qbEl('qbBackdrop').classList.remove('is-open');
    qbSuggClose();
    vsClose();
    if (dtpIsOpen()) dtpClose(false);
    setTimeout(function () { if (!qb.open) qbEl('qbPanel').hidden = true; }, 200);
    if (qb.lastFocus && qb.lastFocus.focus) { try { qb.lastFocus.focus(); } catch (e) {} }
  }

  /* ================================================================
     15.6 Đồng bộ giao diện
     ================================================================ */
  function qbSyncTabs() {
    document.querySelectorAll('[data-qbtab]').forEach(function (b) {
      b.setAttribute('aria-selected', String(b.dataset.qbtab === qb.tab));
    });
    qbEl('qbPanel').classList.toggle('is-checkin', qb.tab === 'checkin');
    qbEl('qbSubmit').textContent = qb.tab === 'checkin' ? 'Hoàn tất nhận phòng ngay' : 'Hoàn tất đặt trước';
    qbEl('qbIn').disabled = qb.tab === 'checkin' && !qb.inFixed;
  }
  function qbSyncStay() {
    if (qb.tab === 'checkin' && !qb.inFixed) {
      qb.in = new Date(HOTEL_NOW.getTime());
      qbEl('qbInD').textContent = 'Ngày giờ';
      qbEl('qbInT').textContent = 'hiện tại';
      qbEl('qbIn').setAttribute('title', dmy(qb.in) + ' ' + hm(qb.in));
    } else {
      qbEl('qbInD').textContent = dmy(qb.in);
      qbEl('qbInT').textContent = hm(qb.in);
      qbEl('qbIn').removeAttribute('title');
    }
    qb.out = qbDepartureOn(addDays(startOfDay(qb.in), qb.nights));
    qbEl('qbOutD').textContent = dmy(qb.out);
    qbEl('qbOutT').textContent = hm(qb.out);
    qbEl('qbNights').textContent = qb.nights + ' đêm';
    qbEl('qbNightMinus').disabled = qb.nights <= 1;
    qbEl('qbNightPlus').disabled = qb.nights >= QB_MAX_NIGHTS;
  }
  function qbSyncRooms(reset) {
    var free = qbFreeRooms(vsVal('qbType'), qb.in, qb.out);
    var keep = reset ? '' : vsVal('qbRoom');
    if (keep && free.every(function (r) { return r.id !== keep; })) keep = '';
    vsDef('qbRoom', [{ v: '', t: 'Chọn phòng' }].concat(free.map(function (r) { return { v: r.id, t: r.id }; })), keep, 'Chọn phòng');
  }
  function qbSyncMoney() {
    var total = qbTotal();
    qbEl('qbSubtotal').textContent = money(total);
    qbEl('qbTotal').textContent = money(total);
    var paid = Math.min(Math.max(0, qb.paid), total);
    var remain = total - paid;
    var box = qbEl('qbRemain');
    box.classList.remove('is-ok', 'is-warn');
    if (remain <= 0) box.classList.add('is-ok');
    else if (paid > 0) box.classList.add('is-warn');
    qbEl('qbRemainAmt').textContent = money(remain);
    var method = vsVal('qbPayMethod');
    var isQR = method === 'QR code';
    qbEl('qbRemainLabel').textContent = 'Tiền phòng còn lại' + (isQR && paid <= 0 ? ' (Chưa thanh toán)' : '');
    document.querySelectorAll('[data-qbpct]').forEach(function (b) {
      b.setAttribute('aria-pressed', String(qb.pct !== null && +b.dataset.qbpct === qb.pct));
    });
    var debt = method === 'Công nợ';
    qbEl('qbCompanyDebtWrap').hidden = !debt;
    qbEl('qbQuickWrap').className = debt ? 'qb-c7' : 'qb-c12';
    if (!debt) qbClearErr('qbDebtCompany');
  }
  function qbSyncAll() { qbSyncTabs(); qbSyncStay(); qbSyncMoney(); }

  /* ================================================================
     15.7 Gợi ý khách đã có dữ liệu (Figma 47787:87196)
     ================================================================ */
  var qbSuggList = [], qbSuggIdx = -1, qbSuggField = null;
  function qbHi(text, q) {
    var t = String(text), i = q ? t.toLowerCase().indexOf(q.toLowerCase()) : -1;
    if (i < 0 || !q) return esc(t);
    return esc(t.slice(0, i)) + '<mark>' + esc(t.slice(i, i + q.length)) + '</mark>' + esc(t.slice(i + q.length));
  }
  function qbSuggClose() {
    var s = qbEl('qbSugg');
    if (s) { s.classList.remove('is-open'); qbEl('qbSuggList').innerHTML = ''; }
    qbSuggList = []; qbSuggIdx = -1; qbSuggField = null;
  }
  function qbSuggSearch(fieldId) {
    var v = String(qbEl(fieldId).value || '').trim();
    if (v.length < 2) { qbSuggClose(); return; }
    var key = (fieldId === 'qbPhone' || fieldId === 'qbDocNo') ? qbDigits(v) : v.toLowerCase();
    if (!key) { qbSuggClose(); return; }
    qbSuggList = QB_GUESTS.filter(function (g) {
      if (fieldId === 'qbLast') return g.last.toLowerCase().indexOf(key) >= 0;
      if (fieldId === 'qbFirst') return (g.first + ' ' + g.last).toLowerCase().indexOf(key) >= 0 || g.first.toLowerCase().indexOf(key) >= 0;
      if (fieldId === 'qbPhone') return qbDigits(g.phone).indexOf(key) >= 0;
      if (fieldId === 'qbEmail') return g.email.toLowerCase().indexOf(key) >= 0;
      if (fieldId === 'qbDocNo') return g.doc.indexOf(key) >= 0;
      return false;
    });
    if (!qbSuggList.length) { qbSuggClose(); return; }
    qbSuggField = fieldId; qbSuggIdx = -1;
    var who = '<svg class="who" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><circle cx="12" cy="12" r="9"/><circle cx="12" cy="10" r="3"/><path d="M6.5 19a6 6 0 0111 0"/></svg>';
    var icAge = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><circle cx="12" cy="7" r="3.2"/><path d="M5 20a7 7 0 0114 0"/></svg>';
    var icCal = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M3 10h18M8 3v4M16 3v4"/></svg>';
    var icTel = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M5 4h4l2 5-2.5 1.5a12 12 0 005 5L15 13l5 2v4a1 1 0 01-1 1A16 16 0 014 5a1 1 0 011-1z"/></svg>';
    var nameQ = (fieldId === 'qbLast' || fieldId === 'qbFirst') ? v : '';
    qbEl('qbSuggList').innerHTML = qbSuggList.map(function (g, i) {
      var age = qbAge(g.dob);
      return '<button type="button" role="option" data-qbg="' + i + '">' +
        '<span class="n">' + QB_FLAG_VN + qbHi(g.last + ' ' + g.first, nameQ) + who + '</span>' +
        '<span class="m">' + icAge + (age === null ? '—' : age + ' tuổi') + '<i>|</i>' +
        icCal + esc(g.dob) + '<i>|</i>' +
        icTel + qbHi(g.phone, fieldId === 'qbPhone' ? v : '') + '<i>|</i>' +
        'CCCD: ' + qbHi(g.doc, fieldId === 'qbDocNo' ? v : '') + '</span></button>';
    }).join('');
    qbEl('qbSugg').classList.add('is-open');
  }
  function qbSuggMove(d) {
    if (!qbSuggList.length) return;
    qbSuggIdx = (qbSuggIdx + d + qbSuggList.length) % qbSuggList.length;
    var bs = qbEl('qbSuggList').querySelectorAll('button');
    bs.forEach(function (b, i) { b.classList.toggle('is-act', i === qbSuggIdx); });
    if (bs[qbSuggIdx]) bs[qbSuggIdx].scrollIntoView({ block: 'nearest' });
  }
  function qbLink(g) {
    qb.linked = g;
    qbEl('qbLast').value = g.last;
    qbEl('qbFirst').value = g.first;
    qbEl('qbDob').value = g.dob;
    qbEl('qbPhone').value = g.phone;
    qbEl('qbEmail').value = g.email;
    qbEl('qbDocNo').value = g.doc;
    vsSet('qbGender', g.g, true);
    vsSet('qbNation', 'Việt Nam', true);
    vsSet('qbDocType', QB_DOCTYPES[0], true);
    qbEl('qbFirst').classList.add('is-linked');
    var w = qbFieldOf('qbFirst');
    if (w && !w.querySelector('.qb-clear')) {
      var b = document.createElement('button');
      b.type = 'button'; b.className = 'qb-clear'; b.setAttribute('aria-label', 'Bỏ gán hồ sơ khách');
      b.innerHTML = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>';
      b.addEventListener('click', function () { qbUnlink(true); });
      w.appendChild(b);
    }
    qbSuggClose();
    qbClearErrors();
  }
  function qbUnlink(clearFields) {
    qb.linked = null;
    var f = qbEl('qbFirst'); if (!f) return;
    f.classList.remove('is-linked');
    var w = qbFieldOf('qbFirst'), b = w && w.querySelector('.qb-clear');
    if (b) b.remove();
    if (clearFields) {
      ['qbLast', 'qbFirst', 'qbDob', 'qbPhone', 'qbEmail', 'qbDocNo'].forEach(function (id) { qbEl(id).value = ''; });
      f.focus();
    }
  }

  /* ================================================================
     15.8 Loading + thông báo (Figma 55583:60835)
     ================================================================ */
  var QB_RING_C = 201.06;                    // 2πr, r = 32
  var qbLoadTimer = null;
  function qbLoadShow() {
    qbEl('qbLoadPct').textContent = '0%';
    qbEl('qbRingBar').setAttribute('stroke-dashoffset', String(QB_RING_C));
    qbEl('qbLoad').classList.add('is-open');
  }
  function qbLoadSet(p) {
    qbEl('qbLoadPct').textContent = Math.round(p) + '%';
    qbEl('qbRingBar').setAttribute('stroke-dashoffset', String(QB_RING_C * (1 - p / 100)));
  }
  function qbLoadHide() {
    if (qbLoadTimer) { clearInterval(qbLoadTimer); qbLoadTimer = null; }
    qbEl('qbLoad').classList.remove('is-open');
  }
  function qbLoadRun(done) {
    qbLoadShow();
    var p = 0, step = 100 / 12;              // ~1.1s: 12 nhịp x 90ms
    qbLoadTimer = setInterval(function () {
      p = Math.min(100, p + step);
      qbLoadSet(p);
      if (p >= 100) {
        clearInterval(qbLoadTimer); qbLoadTimer = null;
        setTimeout(function () { qbLoadHide(); done(); }, 220);
      }
    }, 90);
  }
  function qbToastOk(msg) {
    var wrap = $('tlToasts'); if (!wrap) return;
    var el = document.createElement('div');
    el.className = 'qb-toast';
    el.setAttribute('role', 'status');
    el.innerHTML =
      '<span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span>' +
      '<span class="tx">' + esc(msg) + '</span>' +
      '<button class="x" type="button" aria-label="Đóng thông báo"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg></button>';
    el.querySelector('.x').addEventListener('click', function () { el.remove(); });
    wrap.appendChild(el);
    setTimeout(function () { if (el.parentNode) el.remove(); }, 4000);
  }

  /* ================================================================
     15.9 Submit
     ================================================================ */
  function qbSubmit() {
    if (qb.busy) return;
    qbClearErrors();
    var bad = null;
    function need(id, ok) { if (!ok) { qbSetErr(id, true, QB_EMPTY); bad = bad || id; } }

    need('qbLast', !!qbEl('qbLast').value.trim());
    need('qbFirst', !!qbEl('qbFirst').value.trim());
    need('qbGender', !!vsVal('qbGender'));
    need('qbType', !!vsVal('qbType'));
    need('qbRate', !!vsVal('qbRate'));
    need('qbRoom', !!vsVal('qbRoom'));
    if (vsVal('qbPayMethod') === 'Công nợ') need('qbDebtCompany', !!vsVal('qbDebtCompany'));

    var total = qbTotal();
    if (qb.paid > total) {
      qbSetErr('qbAmount', true, 'Số tiền không hợp lệ. Vui lòng nhập lại!');
      bad = bad || 'qbAmount';
    }
    if (bad) {                                                   // A1
      toast('Vui lòng điền dữ liệu tại các trường bắt buộc!', true);
      var e = qbEl(bad); if (e && e.focus) e.focus();
      return;
    }
    var room = vsVal('qbRoom');
    if (!qbRoomFree(room, qb.in, qb.out)) {                      // E2
      toast('Phòng bị xung đột!', true);
      qbSetErr('qbRoom', true, 'Phòng bị xung đột! Vui lòng chọn lại phòng khác.');
      qbSyncRooms(true);
      return;
    }

    // B7 → B8: hệ thống đang tạo đặt phòng (QR thì đang tạo mã QR)
    var isQR = vsVal('qbPayMethod') === 'QR code';
    qbEl('qbLoadT1').textContent = isQR ? 'Hệ thống đang tạo QR' : 'Hệ thống đang tạo đặt phòng';
    qb.busy = true;
    qbEl('qbSubmit').disabled = true;
    var isCheckin = qb.tab === 'checkin';
    var paid = Math.min(Math.max(0, qb.paid), total);
    var last = qbEl('qbLast').value.trim(), first = qbEl('qbFirst').value.trim();
    var name = (last + ' ' + first).replace(/\s+/g, ' ').trim();
    var snapshot = {
      id: 'QB' + (BOOKINGS.length + 1) + '-' + Math.max(1, qb.nights),
      room: room,
      st: isCheckin ? 'inhouse' : 'booked',
      name: name,
      code: '#' + (938480 + BOOKINGS.length),
      in: new Date(qb.in.getTime()),
      out: new Date(qb.out.getTime()),
      group: null,
      pay: paid <= 0 ? 'none' : (paid >= total ? 'paid' : 'partial'),
      total: total, paid: paid,
      adults: Math.max(1, qbNum(qbEl('qbAdults').value)),
      children: Math.max(0, qbNum(qbEl('qbChildren').value)),
      infants: Math.max(0, qbNum(qbEl('qbInfants').value)),
      src: vsVal('qbSource'), ota: '', cms: '',
      company: vsVal('qbPayMethod') === 'Công nợ' ? vsVal('qbDebtCompany') : vsVal('qbCompany'),
      market: vsVal('qbMarket'), staff: vsVal('qbStaff'),
      manual: true,                                 // do lễ tân tạo — không bị quy tắc mốc nghiệp vụ ghi đè
      rate: (qbRateObj() || {}).name || 'Giá ngày',
      created: new Date(HOTEL_NOW.getTime()),       // thời gian tạo — dùng cho sort ở Danh sách đặt phòng
      confirmed: qb.status === 'definite',
      note: qbEl('qbNote').value.trim(), inote: '',
      guests: [{ n: name, p: vsVal('qbDial') + ' ' + qbEl('qbPhone').value, cc: 'VN', lead: true }]
    };

    qbLoadRun(function () {
      qb.busy = false;
      qbEl('qbSubmit').disabled = false;
      if (!qbRoomFree(snapshot.room, snapshot.in, snapshot.out)) {   // E2 — kiểm tra lại sau khi chờ
        toast('Phòng bị xung đột!', true);
        qbSetErr('qbRoom', true, 'Phòng bị xung đột! Vui lòng chọn lại phòng khác.');
        qbSyncRooms(true);
        return;
      }
      BOOKINGS.push(snapshot);
      qbClose();
      animateNext = true;
      render();
      // QR code: không hiện toast, mở popup Thanh toán QR với số tiền của chính booking
      if (isQR) qrOpen(snapshot, paid > 0 ? paid : total);
      else qbToastOk(isCheckin ? 'Nhận phòng thành công' : 'Đặt phòng thành công');
    });
  }

  function qbClockTick() {
    if (!qb.open) return;
    if (qb.tab === 'checkin') qbSyncStay();
  }

  /* ================================================================
     15.10 Gắn sự kiện
     ================================================================ */
  function qbMaskDate(el) {
    var d = qbDigits(el.value).slice(0, 8), out = d.slice(0, 2);
    if (d.length > 2) out += '/' + d.slice(2, 4);
    if (d.length > 4) out += '/' + d.slice(4, 8);
    el.value = out;
  }

  function bindQB() {
    if (!qbEl('qbPanel')) return;
    bindDtp();

    document.querySelectorAll('[data-qb-close]').forEach(function (b) {
      b.addEventListener('click', function () { qbClose(); });               // A2
    });
    qbEl('qbBackdrop').addEventListener('click', function () { qbClose(); });

    document.querySelectorAll('[data-qbtab]').forEach(function (b) {
      b.addEventListener('click', function () {
        qb.tab = b.dataset.qbtab;
        if (qb.tab === 'checkin' && !qb.inFixed) qb.in = new Date(HOTEL_NOW.getTime());
        else if (qb.inPre) qb.in = new Date(qb.inPre.getTime());
        qbSyncAll(); qbSyncRooms(false);
      });
    });
    document.querySelectorAll('[data-qbstatus]').forEach(function (b) {
      b.addEventListener('click', function () {
        qb.status = b.dataset.qbstatus;
        document.querySelectorAll('[data-qbstatus]').forEach(function (x) {
          x.setAttribute('aria-pressed', String(x.dataset.qbstatus === qb.status));
        });
      });
    });

    qbEl('qbNightMinus').addEventListener('click', function () {
      if (qb.nights <= 1) return; qb.nights--; qbSyncStay(); qbSyncRooms(false); qbSyncMoney();
    });
    qbEl('qbNightPlus').addEventListener('click', function () {
      if (qb.nights >= QB_MAX_NIGHTS) return; qb.nights++; qbSyncStay(); qbSyncRooms(false); qbSyncMoney();
    });

    // Ngày giờ đến / đi — bộ lịch có cả giờ
    qbEl('qbIn').addEventListener('click', function () {
      if (qb.tab === 'checkin' && !qb.inFixed) return;
      dtpOpen(qbEl('qbIn'), qb.in, true, function (d) {
        qb.in = d; qb.inPre = new Date(d.getTime());
        qbSyncStay(); qbSyncRooms(false); qbSyncMoney();
      });
    });
    qbEl('qbOut').addEventListener('click', function () {
      dtpOpen(qbEl('qbOut'), qb.out, true, function (d) {
        var n = dayDiff(startOfDay(d), startOfDay(qb.in));
        qb.nights = Math.min(QB_MAX_NIGHTS, Math.max(1, n));
        QB_CFG_OUT_H = d.getHours(); QB_CFG_OUT_M = d.getMinutes();
        qbSyncStay(); qbSyncRooms(false); qbSyncMoney();
      });
    });

    // Lịch cho Ngày sinh / Ngày đến hạn (chỉ ngày, không giờ)
    document.querySelectorAll('[data-qbcal]').forEach(function (b) {
      b.addEventListener('click', function (e) {
        e.stopPropagation();
        var id = b.dataset.qbcal, inp = qbEl(id);
        dtpOpen(inp, qbParseDMY(inp.value) || HOTEL_NOW, false, function (d) {
          inp.value = dmy(d); qbClearErr(id);
        });
      });
    });
    ['qbDob', 'qbDocExp'].forEach(function (id) {
      qbEl(id).addEventListener('input', function () { qbMaskDate(this); });
    });

    document.querySelectorAll('[data-qbpax]').forEach(function (b) {
      b.addEventListener('click', function () {
        var t = qbEl(b.dataset.qbpax), min = t.id === 'qbAdults' ? 1 : 0;
        t.value = String(Math.max(min, Math.min(99, qbNum(t.value) + parseInt(b.dataset.qbdelta, 10))));
        qbSyncMoney();
      });
    });
    ['qbAdults', 'qbChildren', 'qbInfants'].forEach(function (id) {
      qbEl(id).addEventListener('input', function () {
        var min = id === 'qbAdults' ? 1 : 0;
        if (this.value !== '') this.value = String(Math.max(min, Math.min(99, qbNum(this.value))));
        qbSyncMoney();
      });
    });

    document.querySelectorAll('[data-qbpaymode]').forEach(function (b) {
      b.addEventListener('click', function () {
        qb.paymode = b.dataset.qbpaymode;
        document.querySelectorAll('[data-qbpaymode]').forEach(function (x) {
          x.setAttribute('aria-pressed', String(x.dataset.qbpaymode === qb.paymode));
        });
      });
    });
    document.querySelectorAll('[data-qbpct]').forEach(function (b) {
      b.addEventListener('click', function () {
        qb.pct = parseInt(b.dataset.qbpct, 10);
        qb.paid = Math.round(qbTotal() * qb.pct / 100);
        qbEl('qbAmount').value = qb.paid ? money(qb.paid) : '';
        qbClearErr('qbAmount');
        qbSyncMoney();
      });
    });
    qbEl('qbAmount').addEventListener('input', function () {
      var n = qbNum(this.value);
      this.value = n ? money(n) : '';
      qb.paid = n; qb.pct = null;
      qbClearErr('qbAmount');
      qbSyncMoney();
    });

    // Gợi ý khách đã có dữ liệu
    ['qbLast', 'qbFirst', 'qbPhone', 'qbEmail', 'qbDocNo'].forEach(function (id) {
      var e = qbEl(id);
      e.addEventListener('input', function () { qbClearErr(id); qbSuggSearch(id); });
      e.addEventListener('keydown', function (ev) {
        if (!qbSuggList.length) return;
        if (ev.key === 'ArrowDown') { ev.preventDefault(); qbSuggMove(1); }
        else if (ev.key === 'ArrowUp') { ev.preventDefault(); qbSuggMove(-1); }
        else if (ev.key === 'Enter' && qbSuggIdx >= 0) { ev.preventDefault(); qbLink(qbSuggList[qbSuggIdx]); }
        else if (ev.key === 'Escape') { qbSuggClose(); }
      });
    });
    qbEl('qbSuggList').addEventListener('click', function (e) {
      var b = e.target.closest('[data-qbg]'); if (!b) return;
      qbLink(qbSuggList[parseInt(b.dataset.qbg, 10)]);
    });

    qbEl('qbCompanyEdit').addEventListener('click', function () {
      toast('Mở màn hình quản lý công ty — ' + vsVal('qbCompany'));
    });
    qbEl('qbSubmit').addEventListener('click', qbSubmit);

    // Menu select: chọn item
    qbEl('qbVsMenu').addEventListener('click', function (e) {
      var b = e.target.closest('[data-vsi]'); if (!b) return;
      vsPick(parseInt(b.dataset.vsi, 10));
    });

    // Esc: đóng theo thứ tự menu → lịch → gợi ý → panel
    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape') return;
      if (vsCur) { e.preventDefault(); vsClose(true); return; }
      if (dtpIsOpen()) { e.preventDefault(); dtpClose(true); return; }
      if (!qb.open) return;
      if (qbSuggList.length) { e.preventDefault(); qbSuggClose(); return; }
      e.preventDefault(); qbClose();
    });

    // CAPTURE: các handler trong lưới có stopPropagation() nên phải nghe ở pha capture
    document.addEventListener('click', function (e) {
      if (vsCur && !e.target.closest('#qbVsMenu') && !e.target.closest('.qb-vs')) vsClose(false);
      if (dtpIsOpen() && !e.target.closest('#qbDtp') &&
          !(dtp.anchor && dtp.anchor.contains(e.target)) && !e.target.closest('[data-qbcal]')) dtpClose(false);
      if (qb.open && qbSuggList.length && !e.target.closest('#qbSugg') &&
          !(qbSuggField && e.target === qbEl(qbSuggField))) qbSuggClose();
    }, true);

    window.addEventListener('resize', function () {
      if (vsCur) vsPlace();
      if (dtpIsOpen()) dtpPlace();
    });
    // Cuộn trong panel: menu/lịch là position:fixed nên phải bám lại vị trí nút,
    // KHÔNG đóng — click của trình duyệt tự cuộn nút vào tầm nhìn trước khi mở menu.
    qbEl('qbBody').addEventListener('scroll', function () {
      if (vsCur) vsPlace();
      if (dtpIsOpen()) dtpPlace();
    });
  }


  /* ================================================================
     16. SƠ ĐỒ CHI TIẾT / SƠ ĐỒ ĐƠN GIẢN
     Figma: 55588:67065 (chi tiết) · 55588:67066 (đơn giản)
            32146:44832 (Room card — 5 trạng thái × 2 biến thể)
     Cùng nguồn dữ liệu ROOMS/BOOKINGS với sơ đồ Timeline; ngày hiển thị
     lấy từ state.anchor (thanh chọn ngày) — không hard-code ngày nào.
     ================================================================ */
  var dvGroup = 'status';          // status | type | floor | room
  var dvQuery = '';

  function dvFloor(r) { var m = /(\d)/.exec(r.id); return m ? m[1] : '1'; }
  function dvDay() { return startOfDay(state.anchor); }

  /* Trạng thái phòng trong NGÀY đang xem (ưu tiên: sửa > có khách > đã đặt > đã trả) */
  var DV_RANK = { repair: 4, inhouse: 3, booked: 2, checkout: 1 };
  function dvStatusOf(room, d) {
    var best = null;
    for (var i = 0; i < BOOKINGS.length; i++) {
      var b = BOOKINGS[i];
      if (b.room !== room.id) continue;
      if (!(startOfDay(b.in) <= d && d <= startOfDay(b.out))) continue;
      if (!best || (DV_RANK[b.st] || 0) > (DV_RANK[best.st] || 0)) best = b;
    }
    return { st: best ? best.st : 'empty', b: best };
  }

  function dvVisible(room, info) {
    if (dvQuery && room.id.toLowerCase().indexOf(dvQuery) < 0) return false;
    var f = state.filter;
    if (!f) return true;
    if (f === 'dirty') return !!room.dirty;
    return info.st === f;
  }

  /* Nhãn nhóm theo Figma */
  function dvBucket(room, info, d) {
    if (dvGroup === 'type') return typeName(room.type);
    if (dvGroup === 'floor') return 'Tầng ' + dvFloor(room);
    if (dvGroup === 'room') return '';
    switch (info.st) {                                   // dvGroup === 'status'
      case 'empty': return 'Phòng trống';
      case 'booked': return 'Đã đặt';
      case 'repair': return 'Sửa phòng';
      case 'checkout': return 'Khách đã trả';
      case 'inhouse': return sameDay(startOfDay(info.b.out), d) ? 'Khách đi hôm nay' : 'Khách đi ở qua ngày';
      default: return 'Khác';
    }
  }
  var DV_ORDER = ['Phòng trống', 'Đã đặt', 'Khách đi hôm nay', 'Sửa phòng', 'Khách đi ở qua ngày', 'Khách đã trả', 'Khác'];

  /* ---------- Room card ---------- */
  var DV_ICONS = {
    users: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="9" cy="8" r="3"/><path d="M3 20a6 6 0 0112 0"/><path d="M16 5.5a3 3 0 010 5M18 20a6 6 0 00-2-4.4"/></svg>',
    bell: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 17V11a6 6 0 1112 0v6M4 17h16M10 20a2 2 0 004 0"/></svg>',
    send: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 3L3 10l7 3 3 7 8-17z"/></svg>',
    check: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>',
    warn: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 4l9 16H3l9-16z"/><path d="M12 10v4M12 17.4v.2"/></svg>',
    bulb: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 18h6M10 21h4"/><path d="M8.5 14a5.5 5.5 0 117 0c-.7.6-1 1.3-1 2.2H9.5c0-.9-.3-1.6-1-2.2z"/></svg>',
    broom: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14 4l6 6M12.5 8.5l3 3M4 20l7-7M4 20l-1-4 3-1M4 20l4 1 1-3"/></svg>',
    flagVN: '<svg viewBox="0 0 24 18" fill="none" aria-hidden="true"><rect width="24" height="18" rx="2" fill="#DA251D"/><path d="M12 4.6l1.1 3.3h3.5l-2.8 2 1.1 3.3-2.9-2-2.9 2 1.1-3.3-2.8-2h3.5z" fill="#FFCD00"/></svg>'
  };
  function dvShort(d) { return pad(d.getDate()) + '/' + pad(d.getMonth() + 1) + ' ' + hm(d); }
  function dvName(b) {
    var m = cfgDef('detail', 'name');
    if (m === 'Mã đặt phòng') return b.id;
    if (m === 'Tên công ty') return b.company || b.name;
    return b.name;
  }
  function dvLeadPhone(b) {
    var g = (b.guests || []).filter(function (x) { return x.lead; })[0] || (b.guests || [])[0];
    return g && g.p ? g.p : '—';
  }
  function payWord(p) { return p === 'paid' ? 'Đã thanh toán' : (p === 'partial' ? 'Thanh toán một phần' : 'Chưa thanh toán'); }

  function dvCard(room, info, simple) {
    var b = info.b, st = info.st;
    var pax = b ? (b.adults || 0) + (b.children || 0) : 0;
    var tab = simple ? 'simple' : 'detail';
    var tile = [];
    /* v42: bỏ số khách ở góc trái trên cùng thẻ theo yêu cầu */
    if (!simple || cfgOn('simple', 'roomType')) tile.push('<span class="tp">' + esc(room.type) + '</span>');
    tile.push('<span class="rn">' + esc(room.id) + '</span>');
    if (simple) {
      var fl = [];
      if (cfgOn('simple', 'groupBk') && b && b.group) fl.push('<span class="dv-fl grp" title="Booking đoàn">' + DV_ICONS.users + '</span>');
      if (cfgOn('simple', 'hk')) {
        if (st === 'empty') fl.push('<span class="ok">' + DV_ICONS.check + '</span>');
        else if (st === 'repair') fl.push('<span class="ok">' + (b && b.repair === 'OOO' ? DV_ICONS.warn : DV_ICONS.check) + '</span>');
        else if (room.dirty) fl.push('<span class="dv-fl dirty" title="Phòng bẩn">' + DV_ICONS.broom + '</span>');
      }
      if (fl.length) tile.push('<span class="ics">' + fl.join('') + '</span>');
    } else if (st === 'empty') tile.push('<span class="ok">' + DV_ICONS.check + '</span>');
    else if (st === 'repair') tile.push('<span class="ok">' + (b && b.repair === 'OOO' ? DV_ICONS.warn : DV_ICONS.check) + '</span>');
    else tile.push('<span class="ics">' + '<span>' + DV_ICONS.users + '</span><span>' + DV_ICONS.bell + '</span><span>' + DV_ICONS.send + '</span></span>');

    var bd = '';
    if (!simple) {
      if (st === 'empty') {
        bd = '<span class="bd"><span class="free">Phòng trống</span></span>';
      } else if (st === 'repair') {
        bd = '<span class="bd">' +
          '<span class="nm"><b>Sửa phòng (' + esc(b.repair || 'OOS') + ')</b></span>' +
          '<span class="dt">' + dvShort(b.in) + ' - ' + dvShort(b.out) + '</span>' +
          '<span class="sr">' + esc(b.note || 'Sửa chữa điều hoà') + '</span></span>';
      } else {
        var pay = { paid: 'var(--tl-pay-paid)', partial: 'var(--tl-pay-partial)', none: 'var(--tl-pay-none)' }[b.pay] || 'var(--tl-pay-none)';
        var amtMode = cfgDef('detail', 'amount');
        var amtVal = amtMode === 'Tổng tiền' ? (b.total || 0)
                   : (amtMode === 'Đã thanh toán' ? (b.paid || 0) : Math.max(0, (b.total || 0) - (b.paid || 0)));
        var amtLb = amtMode === 'Tổng tiền' ? 'Tổng tiền' : (amtMode === 'Đã thanh toán' ? 'Đã thanh toán' : 'Còn lại');
        var meta = [];
        if (cfgOn('detail', 'phone')) meta.push(esc(dvLeadPhone(b)));
        if (cfgOn('detail', 'company') && b.company) meta.push(esc(b.company));
        if (cfgOn('detail', 'src') && b.src) meta.push(esc(b.src));
        if (cfgOn('detail', 'market') && b.market) meta.push(esc(b.market));
        bd = '<span class="bd">' +
          '<span class="nm">' + (cfgOn('detail', 'nation') ? DV_ICONS.flagVN : '') + '<b>' + esc(dvName(b)) + '</b></span>' +
          '<span class="dt">' + dvShort(b.in) + ' - ' + dvShort(b.out) + '</span>' +
          (meta.length ? '<span class="sr">' + meta.join(' · ') + '</span>' : '') +
          (cfgOn('detail', 'amount') || cfgOn('detail', 'pay')
            ? '<span class="pay">' +
              (cfgOn('detail', 'pay') ? '<span class="dot" style="background:' + pay + '"></span>' : '') +
              (cfgOn('detail', 'amount') ? amtLb + ': ' + money(amtVal) + ' VND' : payWord(b.pay)) +
              '</span>' : '') +
          '</span>';
      }
    }
    var label = room.id + ' — ' + (st === 'empty' ? 'Phòng trống' : (st === 'repair' ? 'Sửa phòng' : (simple ? b.name : dvName(b))));
    return '<button class="tl-card" type="button" data-st="' + st + '" data-room="' + esc(room.id) + '" data-col="0"' +
      (b ? ' data-bk="' + esc(b.id) + '"' : '') +
      ' title="' + esc(label) + '" aria-label="' + esc(label) + '">' +
      '<span class="tile">' + tile.join('') + '</span>' + bd + '</button>';
  }

  function renderCards() {
    var d = dvDay(), simple = state.chart === 'simple';
    var wrap = $('tlCards');
    wrap.className = 'tl-cards ' + (simple ? 'is-simple' : 'is-detail');

    var buckets = {}, order = [];
    for (var i = 0; i < ROOMS.length; i++) {
      var room = ROOMS[i], info = dvStatusOf(room, d);
      if (!dvVisible(room, info)) continue;
      var key = dvBucket(room, info, d);
      if (!buckets[key]) { buckets[key] = []; order.push(key); }
      buckets[key].push(dvCard(room, info, simple));
    }
    if (!order.length) {
      wrap.innerHTML = '<div class="tl-cards-empty">' +
        '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="16" rx="2"/><path d="M3 10h18M9 10v10"/></svg>' +
        '<p style="margin:0">Không có phòng nào khớp bộ lọc</p></div>';
      return;
    }
    if (dvGroup === 'status') {
      order.sort(function (a, b2) {
        var x = DV_ORDER.indexOf(a), y = DV_ORDER.indexOf(b2);
        return (x < 0 ? 99 : x) - (y < 0 ? 99 : y);
      });
    } else if (dvGroup !== 'room') {
      order.sort();
    }
    wrap.innerHTML = order.map(function (k) {
      var body = '<div class="tl-cards-body">' + buckets[k].join('') + '</div>';
      if (!k) return '<div class="tl-cards-grp">' + body + '</div>';
      return '<section class="tl-cards-grp"><h3 class="tl-cards-h">' + esc(k) + ' (' + buckets[k].length + ')</h3>' + body + '</section>';
    }).join('');
  }

  /* Ẩn/hiện các control chỉ dành riêng cho từng loại sơ đồ */
  function syncChrome() {
    var isTimeline = state.chart === 'timeline';
    $('tlGroup4').hidden = isTimeline;
    $('tlSearchWrap').hidden = isTimeline;
    var segRange = document.querySelector('.tl-seg[aria-label="Chế độ hiển thị thời gian"]');
    var segGroup = document.querySelector('.tl-seg[aria-label="Chế độ hiển thị nhóm phòng"]');
    var chk = document.querySelector('.tl-check');
    if (segRange) segRange.hidden = !isTimeline;
    if (segGroup) segGroup.hidden = !isTimeline;
    if (chk) chk.hidden = !isTimeline;
    document.querySelectorAll('[data-group4]').forEach(function (b) {
      b.setAttribute('aria-pressed', String(b.dataset.group4 === dvGroup));
    });
  }

  function bindCards() {
    if (!$('tlCards')) return;
    document.querySelectorAll('[data-group4]').forEach(function (b) {
      b.addEventListener('click', function () { dvGroup = b.dataset.group4; render(); });
    });
    $('tlSearch').addEventListener('input', function () {
      dvQuery = this.value.trim().toLowerCase();
      renderCards();
    });
    // Chuột trái: có booking → popup thông tin; phòng trống → menu thao tác nhanh
    $('tlCards').addEventListener('click', function (e) {
      var card = e.target.closest('.tl-card'); if (!card) return;
      e.stopPropagation();
      closeMenu();
      if (card.dataset.bk) openTip(card);
      else openMenu(card, null, card.dataset.room);
    });
    // Chuột phải: luôn mở menu thao tác nhanh
    $('tlCards').addEventListener('contextmenu', function (e) {
      var card = e.target.closest('.tl-card'); if (!card) return;
      e.preventDefault(); closeTip();
      openMenu(card, card.dataset.bk || null, card.dataset.room);
    });
  }

  /* ================================================================
     17. Popup Thanh toán QR (Figma 48500:303200)
     Số tiền lấy từ chính booking vừa tạo; hiệu lực mã đếm ngược thật.
     ================================================================ */
  var QB_QR_TTL = 900;                       // 15:00 — giây
  var qrLeft = 0, qrTimer = null;
  function qrPaint() {
    var m = Math.floor(qrLeft / 60), s = qrLeft % 60;
    $('qbQrTtl').textContent = 'Mã QR có hiệu lực trong ' + pad(m) + ':' + pad(s) + ' • VietQR';
  }
  function qrOpen(bk, amount) {
    $('qbQrBooking').textContent = 'Đã tạo booking ' + bk.code;
    $('qbQrDesc').textContent = 'TT-' + bk.room + '-' + String(bk.code).replace(/\D/g, '');
    $('qbQrMoney').textContent = money(amount);
    qrLeft = QB_QR_TTL; qrPaint();
    if (qrTimer) clearInterval(qrTimer);
    qrTimer = setInterval(function () {
      qrLeft--;
      if (qrLeft <= 0) { qrLeft = 0; qrPaint(); clearInterval(qrTimer); qrTimer = null; return; }
      qrPaint();
    }, 1000);
    $('qbQrBd').classList.add('is-open');
    $('qbQrDlg').classList.add('is-open');
    var f = $('qbQrDlg').querySelector('.primary'); if (f) f.focus();
  }
  function qrClose() {
    if (qrTimer) { clearInterval(qrTimer); qrTimer = null; }
    $('qbQrDlg').classList.remove('is-open');
    $('qbQrBd').classList.remove('is-open');
  }
  function qrIsOpen() { return $('qbQrDlg') && $('qbQrDlg').classList.contains('is-open'); }
  function bindQr() {
    if (!$('qbQrDlg')) return;
    $('qbQrDlg').addEventListener('click', function (e) {
      var a = e.target.closest('[data-qbqr]'); if (!a) return;
      var k = a.dataset.qbqr;
      if (k === 'close') { qrClose(); return; }
      if (k === 'print') { toast('Đang gửi lệnh in hoá đơn…'); return; }
      if (k === 'later') { qrClose(); toast('Điều hướng tới màn hình chi tiết đặt phòng'); }
    });
    $('qbQrBd').addEventListener('click', qrClose);
    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape' && qrIsOpen()) { e.preventDefault(); qrClose(); }
    });
  }


  /* ================================================================
     18. DANH SÁCH ĐẶT PHÒNG (R01.03 — List booking)
     Figma: 31443:41077 · body 33311:18510 · popup Cột hiển thị 33422:114094
     SRS  : R01.03 — mặc định danh sách "Khách đang ở", sort theo thời gian
            tạo mới nhất → cũ nhất, click chuột trái vào bản ghi mở popup
            thao tác nhanh của Sơ đồ phòng Timeline.
     Dữ liệu: dùng CHUNG BOOKINGS + UNASSIGNED với sơ đồ Timeline.
     ================================================================ */

  var LB_LISTS = [
    { id: 'inhouse',    name: 'Khách đang ở',     desc: 'Danh sách khách đang lưu trú' },
    { id: 'arrival',    name: 'Khách sẽ đến',     desc: 'Danh sách đón khách hôm nay' },
    { id: 'departure',  name: 'Khách sẽ đi',      desc: 'Danh sách khách trả phòng hôm nay' },
    { id: 'checkout',   name: 'Khách đã trả',     desc: 'Danh sách khách đã trả phòng' },
    { id: 'booked',     name: 'Đặt phòng mới',    desc: 'Danh sách booking đã đặt, chưa nhận phòng' },
    { id: 'unassigned', name: 'Phòng chưa gán',   desc: 'Danh sách booking chưa được gán phòng' },
    { id: 'repair',     name: 'Phòng đang sửa',   desc: 'Danh sách phòng OOO / OOS' },
    { id: 'all',        name: 'Tất cả đặt phòng', desc: 'Toàn bộ booking trong hệ thống' }
  ];

  var LB_COLS = [
    { k: 'code',    t: 'Mã đặt phòng',    g: 'Thông tin đặt phòng', lock: true, on: true },
    { k: 'name',    t: 'Tên khách',       g: 'Thông tin khách',     lock: true, on: true },
    { k: 'pax',     t: 'NL/TE/EB',        g: 'Thông tin khách',     on: true },
    { k: 'phone',   t: 'Số điện thoại',   g: 'Thông tin khách',     on: false },
    { k: 'nation',  t: 'Quốc tịch',       g: 'Thông tin khách',     on: false },
    { k: 'src',     t: 'Nguồn',           g: 'Thông tin đặt phòng', on: false },
    { k: 'market',  t: 'Thị trường',      g: 'Thông tin đặt phòng', on: false },
    { k: 'company', t: 'Công ty',         g: 'Thông tin đặt phòng', on: false },
    { k: 'staff',   t: 'Nhân viên sale',  g: 'Thông tin đặt phòng', on: false },
    { k: 'st',      t: 'Trạng thái',      g: 'Thông tin đặt phòng', on: true },
    { k: 'type',    t: 'Loại phòng',      g: 'Thông tin phòng',     on: true },
    { k: 'room',    t: 'Tên phòng',       g: 'Thông tin phòng',     on: true },
    { k: 'in',      t: 'Ngày đến',        g: 'Thông tin phòng',     on: true },
    { k: 'out',     t: 'Ngày đi',         g: 'Thông tin phòng',     on: true },
    { k: 'nights',  t: 'Số đêm',          g: 'Thông tin phòng',     on: false, num: true },
    { k: 'rate',    t: 'Chính sách giá',  g: 'Thông tin phòng',     on: true },
    { k: 'total',   t: 'Tổng tiền phòng', g: 'Thông tin phòng',     on: true,  num: true },
    { k: 'paid',    t: 'Đã thanh toán',   g: 'Thông tin phòng',     on: false, num: true },
    { k: 'remain',  t: 'Số tiền còn lại', g: 'Thông tin phòng',     on: true,  num: true }
  ];
  var LB_GROUPS = ['Thông tin khách', 'Thông tin đặt phòng', 'Thông tin phòng'];
  var LB_ST_FILTER = [
    { v: '', t: 'Tất cả' }, { v: 'booked', t: 'Đã đặt' }, { v: 'inhouse', t: 'Có khách' },
    { v: 'checkout', t: 'Đã trả' }, { v: 'repair', t: 'Sửa phòng' }, { v: 'unassigned', t: 'Chưa gán phòng' }
  ];

  var lb = {
    list: 'inhouse', kw: '', st: '', from: null, to: null,
    sort: { k: 'created', dir: -1 },       // SRS: mặc định mới nhất → cũ nhất
    page: 1, size: 25, sel: {}, closed: {}, cols: null, colsDraft: null
  };
  function lbColsOn() {
    if (!lb.cols) { lb.cols = {}; LB_COLS.forEach(function (c) { lb.cols[c.k] = !!c.on; }); }
    return lb.cols;
  }
  function lbVisibleCols() { var on = lbColsOn(); return LB_COLS.filter(function (c) { return on[c.k]; }); }

  /* ---------- Nguồn dữ liệu — CHUNG với sơ đồ Timeline ---------- */
  function lbRows() {
    var rows = [];
    BOOKINGS.forEach(function (b) {
      var r = roomById(b.room);
      rows.push({
        id: b.id, bkId: b.id, room: b.room, st: b.st, code: b.code,
        name: b.st === 'repair' ? ('Sửa phòng (' + (b.repair || 'OOS') + ')') : b.name,
        pax: (b.adults || 0) + '/' + (b.children || 0) + '/' + (b.infants || 0),
        phone: b.guests && b.guests[0] ? b.guests[0].p : '',
        nation: b.guests && b.guests[0] && b.guests[0].cc === 'JP' ? 'Nhật Bản' : 'Việt Nam',
        src: b.src || '', market: b.market || '', company: b.company || '', staff: b.staff || '',
        type: r ? typeName(r.type) : '', in: b.in, out: b.out, nights: nightsOf(b),
        rate: b.rate || 'Giá ngày', total: b.total || 0, paid: b.paid || 0,
        remain: Math.max(0, (b.total || 0) - (b.paid || 0)),
        group: b.group || null, created: b.created || b.in
      });
    });
    UNASSIGNED.forEach(function (u) {
      rows.push({
        id: u.id, bkId: null, room: '—', st: 'unassigned', code: u.code, name: u.name,
        pax: '2/0/0', phone: '', nation: 'Việt Nam', src: u.src || '', market: '', company: '', staff: '',
        type: typeName(u.type), in: u.in, out: u.out, nights: Math.max(1, dayDiff(u.out, u.in)),
        rate: 'Giá ngày', total: 0, paid: 0, remain: 0, group: null, created: u.in
      });
    });
    return rows;
  }

  function lbMatchList(r) {
    var d = startOfDay(HOTEL_NOW);
    switch (lb.list) {
      case 'inhouse':    return r.st === 'inhouse';
      case 'arrival':    return r.st === 'booked' && sameDay(startOfDay(r.in), d);
      case 'departure':  return r.st === 'inhouse' && sameDay(startOfDay(r.out), d);
      case 'checkout':   return r.st === 'checkout';
      case 'booked':     return r.st === 'booked';
      case 'unassigned': return r.st === 'unassigned';
      case 'repair':     return r.st === 'repair';
      default:           return true;
    }
  }
  function lbFiltered() {
    var kw = lb.kw.toLowerCase();
    return lbRows().filter(function (r) {
      if (!lbMatchList(r)) return false;
      if (lb.st && r.st !== lb.st) return false;
      if (kw && (r.name + ' ' + r.code + ' ' + r.room).toLowerCase().indexOf(kw) < 0) return false;
      if (lb.from && startOfDay(r.in) < startOfDay(lb.from)) return false;
      if (lb.to && startOfDay(r.in) > startOfDay(lb.to)) return false;
      return true;
    }).sort(function (a, b) {
      var k = lb.sort.k, x = a[k], y = b[k];
      if (x instanceof Date) { x = x.getTime(); y = y.getTime(); }
      if (typeof x === 'number' && typeof y === 'number') return (x - y) * lb.sort.dir;
      return String(x).localeCompare(String(y), 'vi') * lb.sort.dir;
    });
  }

  /* ---------- Render ---------- */
  var LB_IC = {
    up: '<span class="sortic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M6 15l6-6 6 6"/></svg></span>',
    down: '<span class="sortic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg></span>',
    caret: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg>'
  };
  function lbCell(r, c) {
    switch (c.k) {
      case 'code': return '<span class="lb-code">' + esc(r.code) + '</span>';
      case 'in': case 'out': return dmy(r[c.k]) + ' ' + hm(r[c.k]);
      case 'total': case 'paid': case 'remain': return money(r[c.k]);
      case 'st': {
        var s = STATUS[r.st] || { label: r.st, color: '#2F2B3D', rgb: '47,43,61' };
        return '<span class="lb-chip" style="color:' + s.color + ';background:rgba(' + s.rgb + ',.12)">' +
          '<span class="dot"></span>' + esc(r.st === 'unassigned' ? 'Chưa gán phòng' : s.label) + '</span>';
      }
      default: return esc(String(r[c.k] === undefined || r[c.k] === null ? '' : r[c.k]));
    }
  }
  function lbRenderHead() {
    var cols = lbVisibleCols();
    var h = ['<tr><th style="width:40px"><input class="lb-ck" type="checkbox" id="lbCkAll" aria-label="Chọn tất cả"></th>'];
    cols.forEach(function (c) {
      var on = lb.sort.k === c.k;
      h.push('<th class="sortable' + (on ? ' is-sorted' : '') + (c.num ? ' num' : '') + '" data-lbsort="' + c.k + '"' +
        ' aria-sort="' + (on ? (lb.sort.dir > 0 ? 'ascending' : 'descending') : 'none') + '">' +
        esc(c.t) + (on ? (lb.sort.dir > 0 ? LB_IC.up : LB_IC.down) : LB_IC.down) + '</th>');
    });
    h.push('</tr>');
    $('lbHead').innerHTML = h.join('');
  }
  function lbRenderBody() {
    var cols = lbVisibleCols(), all = lbFiltered();
    var start = (lb.page - 1) * lb.size, pageRows = all.slice(start, start + lb.size);
    var h = [], seen = {};

    pageRows.forEach(function (r) {
      if (r.group) {
        var gk = r.group.name;
        if (!seen[gk]) {
          seen[gk] = true;
          var n = pageRows.filter(function (x) { return x.group && x.group.name === gk; }).length;
          h.push('<tr class="lb-grp' + (lb.closed[gk] ? ' is-closed' : '') + '" data-lbgrp="' + esc(gk) + '">' +
            '<td><input class="lb-ck" type="checkbox" aria-label="Chọn đoàn"></td>' +
            '<td colspan="' + cols.length + '"><button class="cr" type="button" aria-label="Đóng/mở danh sách phòng trong đoàn">' + LB_IC.caret + '</button> ' +
            esc('[' + r.code.replace('#', '') + ' ' + (r.src || '').toUpperCase() + '] - ' + r.group.name + ' ( ' + r.group.rooms + ' PHÒNG )') +
            '</td></tr>');
        }
        if (lb.closed[gk]) return;
      }
      var tag = r.group ? r.group.color : '';
      h.push('<tr class="lb-row' + (r.group ? ' in-grp' : '') + (lb.sel[r.id] ? ' is-sel' : '') + '"' +
        ' data-lbrow="' + esc(r.id) + '"' + (r.bkId ? ' data-bk="' + esc(r.bkId) + '"' : '') +
        ' data-room="' + esc(r.room) + '"' + (tag ? ' style="--lb-tag:' + esc(tag) + '"' : '') + '>' +
        '<td><input class="lb-ck" type="checkbox" data-lbck="' + esc(r.id) + '"' + (lb.sel[r.id] ? ' checked' : '') + ' aria-label="Chọn bản ghi ' + esc(r.code) + '"></td>' +
        cols.map(function (c) { return '<td' + (c.num ? ' class="num"' : '') + '>' + lbCell(r, c) + '</td>'; }).join('') +
        '</tr>');
    });

    $('lbBody').innerHTML = h.join('');
    $('lbEmpty').hidden = all.length > 0;

    var from = all.length ? start + 1 : 0, to = Math.min(start + lb.size, all.length);
    $('lbCount').textContent = 'Hiển thị ' + from + ' - ' + to + ' trên ' + all.length + ' bản ghi';
    lbRenderPager(Math.max(1, Math.ceil(all.length / lb.size)));
    lbSyncBulk();
  }
  function lbRenderPager(total) {
    if (lb.page > total) lb.page = total;
    var h = [], p = lb.page;
    function btn(label, page, dis, cur) {
      return '<button type="button" data-lbpage="' + page + '"' + (dis ? ' disabled' : '') +
        (cur ? ' aria-current="page"' : '') + ' aria-label="Trang ' + page + '">' + label + '</button>';
    }
    h.push(btn('«', 1, p === 1));
    h.push(btn('‹', p - 1, p === 1));
    var pages = [];
    for (var i = 1; i <= total; i++) {
      if (i === 1 || i === total || Math.abs(i - p) <= 1) pages.push(i);
      else if (pages[pages.length - 1] !== '…') pages.push('…');
    }
    pages.forEach(function (x) {
      if (x === '…') h.push('<button type="button" disabled aria-hidden="true">…</button>');
      else h.push(btn(String(x), x, false, x === p));
    });
    h.push(btn('›', p + 1, p === total));
    h.push(btn('»', total, p === total));
    $('lbPager').innerHTML = h.join('');
  }
  function lbSyncBulk() {
    var n = Object.keys(lb.sel).filter(function (k) { return lb.sel[k]; }).length;
    $('lbBulk').classList.toggle('is-on', n > 0);
    $('lbBulkTxt').textContent = 'Đã chọn ' + n;
    var ck = $('lbCkAll');
    if (ck) {
      var rows = $('lbBody').querySelectorAll('[data-lbck]');
      var on = rows.length > 0 && Array.prototype.every.call(rows, function (x) { return x.checked; });
      ck.checked = on;
      ck.indeterminate = !on && n > 0;
    }
  }
  function lbRender() {
    var L = LB_LISTS.filter(function (x) { return x.id === lb.list; })[0] || LB_LISTS[0];
    $('lbListName').textContent = L.name;
    $('lbListDesc').textContent = L.desc;
    lbRenderHead();
    lbRenderBody();
  }

  /* ---------- Panel Cột hiển thị ---------- */
  function lbColsRender(q) {
    q = (q || '').toLowerCase();
    var d = lb.colsDraft;
    $('lbColsBody').innerHTML = LB_GROUPS.map(function (g) {
      var items = LB_COLS.filter(function (c) { return c.g === g && (!q || c.t.toLowerCase().indexOf(q) >= 0); });
      if (!items.length) return '';
      return '<section class="grp"><h3>' + esc(g) + '</h3><div class="list">' + items.map(function (c) {
        return '<label class="opt' + (c.lock ? ' is-lock' : '') + '">' +
          '<input class="lb-ck" type="checkbox" data-lbcol="' + c.k + '"' + (d[c.k] ? ' checked' : '') + (c.lock ? ' disabled' : '') + '>' +
          '<span>' + esc(c.t) + '</span></label>';
      }).join('') + '</div></section>';
    }).join('');
  }
  function lbColsOpen() {
    lb.colsDraft = {};
    var on = lbColsOn();
    LB_COLS.forEach(function (c) { lb.colsDraft[c.k] = !!on[c.k]; });
    $('lbColsSearch').value = '';
    lbColsRender('');
    $('lbBd').classList.add('is-open');
    $('lbColsDlg').classList.add('is-open');
  }
  function lbColsClose() {
    $('lbColsDlg').classList.remove('is-open');
    $('lbBd').classList.remove('is-open');
  }

  /* ---------- Menu chọn danh sách mẫu ---------- */
  function lbListsOpen() {
    var m = $('lbLists');
    m.innerHTML = '<div class="h">Mẫu mặc định</div>' + LB_LISTS.map(function (x) {
      return '<button type="button" role="option" data-lblist="' + x.id + '"' +
        (x.id === lb.list ? ' aria-selected="true"' : ' aria-selected="false"') + '>' +
        '<b>' + esc(x.name) + '</b><span>' + esc(x.desc) + '</span></button>';
    }).join('');
    m.classList.add('is-open');
    var r = $('lbListBtn').getBoundingClientRect();
    m.style.left = '0px'; m.style.top = '0px';
    var w = m.offsetWidth, hh = m.offsetHeight;
    var top = r.bottom + 4;
    if (top + hh > window.innerHeight - 8) top = Math.max(8, r.top - hh - 4);
    m.style.top = top + 'px';
    m.style.left = Math.min(Math.max(8, r.left), window.innerWidth - w - 8) + 'px';
    $('lbListBtn').setAttribute('aria-expanded', 'true');
  }
  function lbListsClose() {
    $('lbLists').classList.remove('is-open');
    $('lbListBtn').setAttribute('aria-expanded', 'false');
  }

  /* ---------- Điều hướng giữa 2 màn ---------- */
  var lbScreen = 'timeline';
  function showScreen(which) {
    lbScreen = which;
    $('contentCard').hidden = which !== 'timeline';
    $('lbCard').hidden = which !== 'list';
    if ($('cfgCard')) $('cfgCard').hidden = which !== 'config';
    if ($('rlCard')) $('rlCard').hidden = which !== 'report';
    if ($('rsCard')) $('rsCard').hidden = which !== 'newres';
    if ($('naCard')) $('naCard').hidden = which !== 'audit';
    closeTip(); closeMenu();
    /* Bỏ đánh dấu menu cha đang chọn — mỗi màn tự bật lại của mình */
    var cur = document.querySelector('.nav-item.is-selected');
    if (cur) { cur.classList.remove('is-selected'); cur.removeAttribute('aria-current'); }
    if (which === 'list') lbRender();
    else if (which === 'config') cfgShow();
    else if (which === 'report') rlShow();
    else if (which === 'newres') rsShow();
    else if (which === 'audit') naShow();
    else render();
  }

  /* Ánh xạ (nhóm menu · mục con) → màn hình */
  var NAV_MAP = {
    'bk-map': 'timeline',
    'bk-list': 'list',
    'cfg-map': 'config',
    'bk-new': 'newres',
    'report': 'report',
    'night-audit': 'audit'
  };

  function bindLb() {
    if (!$('lbCard')) return;

    // Menu bar → chuyển màn
    document.addEventListener('click', function (e) {
      var el = e.target.closest && (e.target.closest('.nav-child') ||
        e.target.closest('.nav-item[data-nav]'));
      if (!el) return;
      var key = el.getAttribute('data-nav');
      var screen = NAV_MAP[key];
      if (screen) { showScreen(screen); return; }
      if (el.classList.contains('nav-item')) {
        toast('Màn ' + el.textContent.trim() + ' đang được xây dựng');
        return;
      }
      if (key && key.indexOf('cfg-') === 0) {
        toast('Màn ' + el.textContent.trim() + ' đang được xây dựng');
      }
    }, true);

    vsDef('lbStatus', LB_ST_FILTER, '');
    vsDef('lbPageSize', ['10', '25', '50', '100'], '25');
    QB_SEL.lbStatus.onChange = function (v) { lb.st = v; lb.page = 1; lbRenderBody(); };
    QB_SEL.lbPageSize.onChange = function (v) { lb.size = parseInt(v, 10) || 25; lb.page = 1; lbRenderBody(); };

    $('lbListBtn').addEventListener('click', function (e) {
      e.stopPropagation();
      $('lbLists').classList.contains('is-open') ? lbListsClose() : lbListsOpen();
    });
    $('lbLists').addEventListener('click', function (e) {
      var b = e.target.closest('[data-lblist]'); if (!b) return;
      lb.list = b.dataset.lblist; lb.page = 1; lb.sel = {};
      lbListsClose(); lbRender();
    });

    $('lbKeyword').addEventListener('input', function () { lb.kw = this.value.trim(); lb.page = 1; lbRenderBody(); });
    $('lbSearchBtn').addEventListener('click', function () { lb.page = 1; lbRender(); toast('Đã áp dụng bộ lọc'); });
    $('lbSaveTpl').addEventListener('click', function () { toast('Mở popup Lưu mẫu bộ lọc mới'); });
    $('lbAddFilter').addEventListener('click', function () { toast('Mở popup Danh sách bộ lọc'); });
    $('lbDownload').addEventListener('click', function () { toast('Tải danh sách — chọn định dạng PDF hoặc Excel'); });
    $('lbPrint').addEventListener('click', function () { toast('Mở chế độ In danh sách'); });

    // Khoảng ngày đến — chọn 2 bước bằng bộ lịch dùng chung
    function pickRange() {
      dtpOpen($('lbRange'), lb.from || HOTEL_NOW, false, function (d1) {
        lb.from = d1;
        setTimeout(function () {
          dtpOpen($('lbRange'), lb.to || d1, false, function (d2) {
            lb.to = d2 < d1 ? d1 : d2;
            $('lbRange').value = dmy(lb.from) + ' - ' + dmy(lb.to);
            lb.page = 1; lbRenderBody();
          });
        }, 0);
      });
    }
    $('lbRangeBtn').addEventListener('click', function (e) { e.stopPropagation(); pickRange(); });
    $('lbRange').addEventListener('click', pickRange);

    // Sắp xếp
    $('lbHead').addEventListener('click', function (e) {
      var th = e.target.closest('[data-lbsort]');
      if (th) {
        var k = th.dataset.lbsort;
        if (lb.sort.k === k) lb.sort.dir = -lb.sort.dir;
        else lb.sort = { k: k, dir: 1 };
        lbRender();
        return;
      }
      if (e.target.id === 'lbCkAll') {
        var on = e.target.checked;
        $('lbBody').querySelectorAll('[data-lbck]').forEach(function (x) { lb.sel[x.dataset.lbck] = on; });
        lbRenderBody();
      }
    });

    // Bảng: checkbox / mở/đóng đoàn / chuột trái = popup thông tin
    $('lbBody').addEventListener('click', function (e) {
      var ck = e.target.closest('[data-lbck]');
      if (ck) { e.stopPropagation(); lb.sel[ck.dataset.lbck] = ck.checked; lbSyncBulk(); return; }
      var cr = e.target.closest('[data-lbgrp]');
      if (cr && e.target.closest('.cr')) {
        var gk = cr.dataset.lbgrp; lb.closed[gk] = !lb.closed[gk]; lbRenderBody(); return;
      }
      var tr = e.target.closest('.lb-row'); if (!tr) return;
      e.stopPropagation();
      closeMenu();
      if (tr.dataset.bk) openTip(tr);                       // SRS: click trái → popup thông tin
      else toast('Booking chưa được gán phòng — ' + tr.dataset.lbrow);
    });
    // Chuột phải → menu thao tác nhanh (giống sơ đồ Timeline)
    // Double-click 1 dòng → mở màn Chi tiết đặt phòng (FOOR03 luồng A2)
    $('lbBody').addEventListener('dblclick', function (e) {
      var tr = e.target.closest('.lb-row'); if (!tr || !tr.dataset.bk) return;
      e.preventDefault();
      openBookingDetail(tr.dataset.bk, tr.dataset.room);
    });

    $('lbBody').addEventListener('contextmenu', function (e) {
      var tr = e.target.closest('.lb-row'); if (!tr) return;
      e.preventDefault(); closeTip();
      if (tr.dataset.bk) openMenu(tr, tr.dataset.bk, tr.dataset.room);
    });

    $('lbPager').addEventListener('click', function (e) {
      var b = e.target.closest('[data-lbpage]'); if (!b || b.disabled) return;
      lb.page = parseInt(b.dataset.lbpage, 10); lbRenderBody();
      $('lbCard').querySelector('.lb-scroll').scrollTop = 0;
    });
    $('lbBulk').addEventListener('click', function (e) {
      var b = e.target.closest('[data-lbbulk]'); if (!b) return;
      var n = Object.keys(lb.sel).filter(function (k) { return lb.sel[k]; }).length;
      toast('Thao tác hàng loạt: ' + b.textContent.trim() + ' — ' + n + ' bản ghi');
    });

    // Panel Cột hiển thị
    $('lbColsBtn').addEventListener('click', lbColsOpen);
    $('lbBd').addEventListener('click', lbColsClose);
    $('lbColsSearch').addEventListener('input', function () { lbColsRender(this.value.trim()); });
    $('lbColsBody').addEventListener('change', function (e) {
      var c = e.target.closest('[data-lbcol]'); if (!c) return;
      lb.colsDraft[c.dataset.lbcol] = c.checked;
    });
    $('lbColsDlg').addEventListener('click', function (e) {
      var a = e.target.closest('[data-lbcols]'); if (!a) return;
      if (a.dataset.lbcols === 'apply') { lb.cols = lb.colsDraft; lbColsClose(); lbRender(); toast('Đã cập nhật cột hiển thị'); }
      else lbColsClose();
    });

    document.addEventListener('click', function (e) {
      if ($('lbLists').classList.contains('is-open') &&
          !e.target.closest('#lbLists') && !e.target.closest('#lbListBtn')) lbListsClose();
    }, true);
    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape') return;
      if ($('lbLists').classList.contains('is-open')) { e.preventDefault(); lbListsClose(); return; }
      if ($('lbColsDlg').classList.contains('is-open')) { e.preventDefault(); lbColsClose(); }
    });
    window.addEventListener('resize', function () { if ($('lbLists').classList.contains('is-open')) lbListsOpen(); });
  }


  /* ================================================================
     17. MÀN CẤU HÌNH SƠ ĐỒ PHÒNG
     Figma: 41697:138310 · 41697:167156 · 41765:97267 (RowTable/Setting)
     Bật/tắt từng trường thông tin → áp dụng ngay cho 3 view sơ đồ
     (timeline · chi tiết · đơn giản) và thẻ xem nhanh (popup).
     ================================================================ */
  var CFG_NODEF = { card: true };   /* tab không có cột Giá trị mặc định (Figma 41834:47455) */
  var CFG_TABS = [
    { k: 'timeline', t: 'Sơ đồ timeline', title: 'Cấu hình sơ đồ timeline',
      sub: 'Chọn thông tin hiển thị trên sơ đồ timeline. Bỏ chọn để ẩn thông tin đó khỏi màn hình.' },
    { k: 'detail', t: 'Sơ đồ chi tiết', title: 'Cấu hình sơ đồ chi tiết',
      sub: 'Chọn thông tin hiển thị trên thẻ phòng của sơ đồ chi tiết.' },
    { k: 'simple', t: 'Sơ đồ đơn giản', title: 'Cấu hình sơ đồ đơn giản',
      sub: 'Chọn thông tin hiển thị trên thẻ phòng của sơ đồ đơn giản.' },
    { k: 'card', t: 'Thẻ xem nhanh', title: 'Cấu hình thẻ xem nhanh',
      sub: 'Chọn thông tin hiển thị trên popup xem nhanh khi bấm vào một booking.' }
  ];

  /* Đặc tả nhóm / trường theo Figma — lock: luôn hiển thị, def: giá trị mặc định */
  var CFG_SPEC = {
    timeline: [
      { g: 'Thông tin sơ đồ', fs: [
        { k: 'sumTotal', t: 'Tổng', def: ['Công suất phòng (%)', 'Số phòng đã bán', 'Số phòng trống'] },
        { k: 'sumType', t: 'Nhóm loại phòng', def: ['Hiển thị đầy đủ', 'Thu gọn mặc định'] }
      ] },
      { g: 'Thông tin đặt phòng', fs: [
        { k: 'bkKind', t: 'Phân loại booking', hint: 'Biểu tượng khách đoàn / khách lẻ trên thanh booking' },
        { k: 'bkName', t: 'Tên hiển thị', lock: true, def: ['Tên khách đại diện', 'Mã đặt phòng', 'Tên công ty'] },
        { k: 'bkPay', t: 'Trạng thái thanh toán', hint: 'Vạch màu ở cuối thanh booking' }
      ] }
    ],
    detail: [
      { g: 'Thông tin đặt phòng', fs: [
        { k: 'name', t: 'Tên hiển thị', lock: true, def: ['Tên khách đại diện', 'Mã đặt phòng', 'Tên công ty'] },
        { k: 'nation', t: 'Quốc tịch' },
        { k: 'phone', t: 'Số điện thoại khách đại diện' },
        { k: 'company', t: 'Công ty' },
        { k: 'src', t: 'Nguồn' },
        { k: 'market', t: 'Thị trường' }
      ] },
      { g: 'Thông tin thanh toán', fs: [
        { k: 'amount', t: 'Số tiền', def: ['Số tiền còn lại', 'Tổng tiền', 'Đã thanh toán'] },
        { k: 'pay', t: 'Trạng thái thanh toán' }
      ] }
    ],
    simple: [
      { g: 'Thông tin phòng', fs: [
        { k: 'roomType', t: 'Loại phòng' },
        { k: 'roomName', t: 'Tên phòng', lock: true }
      ] },
      { g: 'Trạng thái phòng', fs: [
        { k: 'groupBk', t: 'Booking đoàn' },
        { k: 'hk', t: 'Trạng thái buồng phòng', hint: 'Phòng trống · phòng sửa · phòng bẩn' }
      ] }
    ],
    card: [
      { g: 'Thông tin đặt phòng', fs: [
        { k: 'gName', t: 'Tên đoàn', lock: true },
        { k: 'gRooms', t: 'Tổng số phòng (đoàn)' },
        { k: 'gLead', t: 'Thông tin trưởng đoàn' },
        { k: 'gSrc', t: 'Nguồn' },
        { k: 'gOta', t: 'Mã OTA' },
        { k: 'gCms', t: 'Mã CMS' },
        { k: 'gCompany', t: 'Công ty' },
        { k: 'gMarket', t: 'Thị trường' },
        { k: 'gStaff', t: 'Nhân viên', off: true },
        { k: 'gNote', t: 'Ghi chú đoàn' }
      ] },
      { g: 'Thông tin phòng', fs: [
        { k: 'room', t: 'Mã loại phòng - Số phòng', lock: true },
        { k: 'stay', t: 'Thời gian đến - đi', lock: true },
        { k: 'nights', t: 'Số đêm/giờ' },
        { k: 'pax', t: 'Người lớn/ Trẻ em/ Em bé' },
        { k: 'leadGuest', t: 'Thông tin khách trưởng phòng' },
        { k: 'guests', t: 'Thông tin khách trong phòng' },
        { k: 'note', t: 'Ghi chú phòng' },
        { k: 'inote', t: 'Ghi chú phòng nội bộ' },
        { k: 'payStatus', t: 'Trạng thái thanh toán' },
        { k: 'total', t: 'Tổng tiền' },
        { k: 'paid', t: 'Số tiền đã thanh toán' },
        { k: 'left', t: 'Số tiền còn lại' }
      ] }
    ]
  };

  /* Giá trị khởi tạo: mọi trường bật, giá trị mặc định = lựa chọn đầu tiên */
  function cfgSeed() {
    var out = {};
    for (var tab in CFG_SPEC) {
      out[tab] = { on: {}, def: {} };
      CFG_SPEC[tab].forEach(function (grp) {
        grp.fs.forEach(function (f) {
          out[tab].on[f.k] = !f.off;
          if (f.def) out[tab].def[f.k] = f.def[0];
        });
      });
    }
    return out;
  }
  var CFG = cfgSeed();          // đang áp dụng
  var cfgDraft = cfgClone(CFG); // đang chỉnh trên form
  var cfgTab = 'timeline';
  var cfgFG = '', cfgFF = '';
  var cfgClosed = {};

  function cfgClone(o) { return JSON.parse(JSON.stringify(o)); }
  function cfgOn(tab, k) { return !!(CFG[tab] && CFG[tab].on[k]); }
  function cfgDef(tab, k) { return CFG[tab] ? CFG[tab].def[k] : ''; }

  var CFG_CK = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>';
  var CFG_CV = '<svg class="cv" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>';

  function cfgRender() {
    var spec = CFG_SPEC[cfgTab] || [];
    var meta = CFG_TABS.filter(function (x) { return x.k === cfgTab; })[0];
    $('cfgTitle').textContent = meta.title;
    $('cfgSub').textContent = meta.sub;
    $('cfgPrevTag').textContent = meta.t;
    document.querySelector('.cfg-prev').classList.toggle('is-wide', cfgTab === 'card');
    Array.prototype.forEach.call(document.querySelectorAll('#cfgTabs .cfg-tab'), function (b) {
      var on = b.dataset.cfgtab === cfgTab;
      b.classList.toggle('is-on', on);
      b.setAttribute('aria-selected', on ? 'true' : 'false');
    });

    var host = $('cfgGroups'), h = [], shown = 0;
    spec.forEach(function (grp, gi) {
      if (cfgFG && grp.g !== cfgFG) return;
      var fs = grp.fs.filter(function (f) { return !cfgFF || f.t === cfgFF; });
      if (!fs.length) return;
      shown += fs.length;
      var id = cfgTab + '|' + gi;
      var closed = !!cfgClosed[id];
      h.push('<section class="cfg-grp' + (closed ? ' is-closed' : '') + '" data-cfggrp="' + esc(id) + '">');
      h.push('<button class="cfg-grp-h" type="button" data-cfgtoggle="' + esc(id) + '" aria-expanded="' + (closed ? 'false' : 'true') + '">' +
        CFG_CV + '<span class="nm">' + esc(grp.g) + '</span><span class="cnt">' + fs.length + ' trường</span></button>');
      var noDef = !!CFG_NODEF[cfgTab];
      h.push('<div class="cfg-tbl-wrap"><table class="cfg-tbl"><thead><tr>' +
        '<th>Trường thông tin</th><th class="c-tog">Ẩn/hiện</th>' +
        (noDef ? '' : '<th class="c-def">Giá trị mặc định</th>') +
        '</tr></thead><tbody>');
      fs.forEach(function (f) {
        var on = !!cfgDraft[cfgTab].on[f.k];
        h.push('<tr>');
        h.push('<td><span class="fname">' + esc(f.t) +
          (f.lock ? '<span class="cfg-lock">Bắt buộc</span>' : '') + '</span>' +
          (f.hint ? '<span class="cfg-hint">' + esc(f.hint) + '</span>' : '') + '</td>');
        h.push('<td class="c-tog"><label class="cfg-ck"><input type="checkbox" data-cfgck="' + esc(f.k) + '"' +
          (on ? ' checked' : '') + (f.lock ? ' disabled' : '') +
          ' aria-label="Hiển thị ' + esc(f.t) + '"><span class="bx">' + CFG_CK + '</span></label></td>');
        if (noDef) {
          /* không render cột giá trị mặc định */
        } else if (f.def) {
          var cur = cfgDraft[cfgTab].def[f.k];
          h.push('<td class="c-def"><select class="cfg-sel" data-cfgdef="' + esc(f.k) + '"' + (on ? '' : ' disabled') +
            ' aria-label="Giá trị mặc định của ' + esc(f.t) + '">' +
            f.def.map(function (o) {
              return '<option value="' + esc(o) + '"' + (o === cur ? ' selected' : '') + '>' + esc(o) + '</option>';
            }).join('') + '</select></td>');
        } else {
          h.push('<td class="c-def"><span class="cfg-dash">—</span></td>');
        }
        h.push('</tr>');
      });
      h.push('</tbody></table></div></section>');
    });
    host.innerHTML = shown ? h.join('') : '<div class="cfg-empty">Không có trường thông tin nào khớp bộ lọc</div>';
    cfgPreview();
  }


  /* ---------- Xem trước sơ đồ timeline (Figma 41746:14713) ----------
     Dựng lại đúng cấu trúc lưới thật ở tỷ lệ thu nhỏ: hàng thứ/ngày,
     hàng Tổng, hàng nhóm loại phòng, hàng phòng kèm cờ trạng thái và
     thanh booking. Ngày lấy theo state.anchor — không fix cứng.       */
  var CFG_PV_COLS = 5;
  var CFG_PV_ROOMS = [
    { id: 'A1001', light: true, dirty: true },
    { id: 'A1002', light: false, dirty: true },
    { id: 'A1003', light: false, dirty: true },
    { id: 'A1004', light: false, dirty: true }
  ];
  var CFG_PV_BARS = [
    { room: 'A1001', st: 'inhouse', c0: 1, c1: 4, name: 'Đào Hoàng Anh', code: 'BK01', company: 'Công ty TNHH Sao Mai', src: 'B', group: true, pay: 'partial' },
    { room: 'A1002', st: 'booked', c0: 2, c1: 5, name: 'Nguyễn Trọng Việt', code: 'BK02', company: 'Vietravel', src: 'B', group: false, pay: 'paid' },
    { room: 'A1003', st: 'checkout', c0: 0, c1: 3, name: 'Nguyễn Trọng Việt', code: 'BK03', company: 'Saigontourist', src: 'B', group: false, pay: 'none' }
  ];
  var CFG_PV_SUM = [12, 22, 21, 13, 13];
  var CFG_PV_GRP = [12, 12, 12, 12, 12];
  var CFG_PV_I = {
    caret: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>',
    bulb: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 18h6M10 21h4"/><path d="M8.5 14a5.5 5.5 0 117 0c-.7.6-1 1.3-1 2.2H9.5c0-.9-.3-1.6-1-2.2z"/></svg>',
    broom: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14 4l6 6M12.5 8.5l3 3M4 20l7-7M4 20l-1-4 3-1M4 20l4 1 1-3"/></svg>',
    users: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="9" cy="8" r="3"/><path d="M3 20a6 6 0 0112 0"/><path d="M16 5.5a3 3 0 010 5M18 20a6 6 0 00-2-4.4"/></svg>'
  };
  var CFG_PV_WD = ['CN', 'T2', 'T3', 'T4', 'T5', 'T6', 'T7'];

  function cfgPvLabel(b, mode) {
    if (mode === 'Mã đặt phòng') return b.code;
    if (mode === 'Tên công ty') return b.company;
    return b.name;
  }
  function cfgPvSumLabel(mode) {
    return mode === 'Số phòng đã bán' ? 'Đã bán' : (mode === 'Số phòng trống' ? 'Còn trống' : 'Tổng');
  }
  function cfgPvSumVal(mode, pct, total) {
    if (mode === 'Số phòng đã bán') return Math.round(total * pct / 100);
    if (mode === 'Số phòng trống') return total - Math.round(total * pct / 100);
    return pct + '%';
  }

  function cfgMiniTimeline(d, dv) {
    var base = (typeof state === 'object' && state && state.anchor) ? startOfDay(state.anchor) : startOfDay(new Date());
    var i, h = [];
    var payColor = { paid: 'var(--tl-pay-paid)', partial: 'var(--tl-pay-partial)', none: 'var(--tl-pay-none)' };

    h.push('<div class="cfg-tl"><table role="presentation"><colgroup><col class="rc">');
    for (i = 0; i < CFG_PV_COLS; i++) h.push('<col class="dc">');
    h.push('</colgroup><tbody>');

    /* Hàng thứ + ngày */
    h.push('<tr class="hd"><th class="rc"></th>');
    for (i = 0; i < CFG_PV_COLS; i++) {
      var dd = addDays(base, i), wk = dd.getDay() === 0 || dd.getDay() === 6;
      h.push('<th class="' + (wk ? 'wknd' : '') + '">' +
        '<span class="wd">' + CFG_PV_WD[dd.getDay()] + '</span>' +
        '<span class="dn">' + dd.getDate() + '</span>' +
        '</th>');
    }
    h.push('</tr>');

    /* Hàng Tổng */
    if (d.sumTotal) {
      h.push('<tr class="sum"><th class="rc">' + esc(cfgPvSumLabel(dv.sumTotal)) + '</th>');
      for (i = 0; i < CFG_PV_COLS; i++) h.push('<td>' + cfgPvSumVal(dv.sumTotal, CFG_PV_SUM[i], 25) + '</td>');
      h.push('</tr>');
    }

    /* Hàng nhóm loại phòng */
    if (d.sumType) {
      var collapsed = dv.sumType === 'Thu gọn mặc định';
      h.push('<tr class="grp"><th class="rc"><span style="display:inline-flex' +
        (collapsed ? ';transform:rotate(-90deg)' : '') + '">' + CFG_PV_I.caret + '</span>Standard</th>');
      for (i = 0; i < CFG_PV_COLS; i++) h.push('<td>' + CFG_PV_GRP[i] + '%</td>');
      h.push('</tr>');
    }

    /* Hàng phòng + thanh booking */
    for (var r = 0; r < CFG_PV_ROOMS.length; r++) {
      var room = CFG_PV_ROOMS[r];
      h.push('<tr class="rm"><th class="rc"><span class="nm">' + esc(room.id) + '</span>' +
        (room.dirty ? '<span class="fl-dirty">' + CFG_PV_I.broom + '</span>' : '') + '</th>');
      var bar = null;
      for (i = 0; i < CFG_PV_BARS.length; i++) if (CFG_PV_BARS[i].room === room.id) bar = CFG_PV_BARS[i];
      for (i = 0; i < CFG_PV_COLS; i++) {
        var dd2 = addDays(base, i), wk2 = dd2.getDay() === 0 || dd2.getDay() === 6;
        var inner = '';
        if (bar && bar.c0 === i) {
          var span = bar.c1 - bar.c0;
          inner = '<span class="bar" data-st="' + bar.st + '"' +
            ' style="left:3px;width:calc(' + span + ' * var(--cfg-tl-col) - 6px)">' +
            (d.bkKind && bar.group ? '<span class="grp">' + CFG_PV_I.users + '</span>' : '') +
            (d.bkKind ? '<span class="ava">' + esc(bar.src) + '.</span>' : '') +
            '<span class="lbl">' + esc(cfgPvLabel(bar, dv.bkName)) + '</span>' +
            (d.bkPay ? '<span class="pay" style="background:' + payColor[bar.pay] + '"></span>' : '') +
            '</span>';
        }
        h.push('<td class="dc' + (wk2 ? ' wknd' : '') + '">' + inner + '</td>');
      }
      h.push('</tr>');
    }
    h.push('</tbody></table></div>');
    return h.join('');
  }


  /* ================= Xem trước SƠ ĐỒ CHI TIẾT (Figma 41789:95419) ================= */
  var CFG_PVI = {
    flagVN: '<svg class="fl" viewBox="0 0 24 18" aria-hidden="true"><rect width="24" height="18" rx="2" fill="#DA251D"/><path d="M12 4.6l1.1 3.3h3.5l-2.8 2 1.1 3.3-2.9-2-2.9 2 1.1-3.3-2.8-2h3.5z" fill="#FFCD00"/></svg>',
    users: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="9" cy="8" r="3"/><path d="M3 20a6 6 0 0112 0"/><path d="M16 5.5a3 3 0 010 5M18 20a6 6 0 00-2-4.4"/></svg>',
    bell: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 17V11a6 6 0 1112 0v6M4 17h16M10 20a2 2 0 004 0"/></svg>',
    send: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 3L3 10l7 3 3 7 8-17z"/></svg>',
    check: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>',
    phone: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6.5 3h3l1.5 4-2 1.4a12 12 0 006.6 6.6L17 13l4 1.5v3a2 2 0 01-2.2 2A17.5 17.5 0 013.5 5.2 2 2 0 015.5 3h1z"/></svg>',
    building: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="4" y="3" width="16" height="18" rx="2"/><path d="M8 7h2M8 11h2M8 15h2M14 7h2M14 11h2M14 15h2"/></svg>',
    door: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 21V4a1 1 0 011-1h10a1 1 0 011 1v17M4 21h16M14 12v.01"/></svg>',
    monitor: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="12" rx="2"/><path d="M8 20h8M12 16v4"/></svg>',
    pin: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 21s7-5.5 7-11a7 7 0 10-14 0c0 5.5 7 11 7 11z"/><circle cx="12" cy="10" r="2.6"/></svg>',
    user: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="8" r="3.4"/><path d="M5 20a7 7 0 0114 0"/></svg>',
    note: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 4h11l3 3v13H5z"/><path d="M8 10h8M8 14h5"/></svg>',
    noteLock: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 4h9v16H5z"/><path d="M8 10h4M8 14h3"/><rect x="16" y="9" width="6" height="5" rx="1"/><path d="M17.5 9V7.6a1.5 1.5 0 013 0V9"/></svg>',
    cal: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M8 3v4M16 3v4M3 10h18M9 15l2 2 4-4"/></svg>',
    lock: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="4.5" y="10.5" width="15" height="10" rx="2"/><path d="M8 10.5V7.5a4 4 0 018 0v3"/></svg>',
    adult: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="7" r="3"/><path d="M5.5 21a6.5 6.5 0 0113 0"/></svg>',
    child: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="8" r="3.2"/><path d="M7.5 21a4.5 4.5 0 019 0"/></svg>',
    baby: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="9" r="4"/><path d="M8.5 20a3.5 3.5 0 017 0"/></svg>',
    crown: '<svg viewBox="0 0 24 24" fill="#FFB400" aria-hidden="true"><path d="M4 18h16l1-9-5 3-4-6-4 6-5-3z"/></svg>',
    flag: '<svg viewBox="0 0 24 24" fill="none" stroke="#28C76F" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 21V4M5 5h11l-2 3.5L16 12H5"/></svg>',
    close: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>',
    grpHead: '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="9" cy="8" r="3.2"/><path d="M2.6 20a6.4 6.4 0 0112.8 0z"/><circle cx="17" cy="8.6" r="2.6"/><path d="M13.8 20a7.6 7.6 0 00-1.4-3.8A5 5 0 0121.6 20z"/></svg>'
  };

  var CFG_PV_DT = { inD: '24/02 13:00', outD: '11/03 14:00' };

  function cfgMiniDetail(d, dv) {
    var name = d.name ? cfgPvLabel({ name: 'Nguyễn Hoàng Thuỷ Linh', code: 'BK-938475', company: 'Công ty ezCloud' }, dv.name) : '';
    var amtMode = dv.amount;
    var amtLb = amtMode === 'Tổng tiền' ? 'Tổng tiền' : (amtMode === 'Đã thanh toán' ? 'Đã thanh toán' : 'Còn lại');
    var amtVal = amtMode === 'Tổng tiền' ? '2.400.000' : (amtMode === 'Đã thanh toán' ? '2.400.000' : '0');
    var meta = [];
    if (d.phone) meta.push('0123 456 789');
    if (d.company) meta.push('Công ty ezCloud');
    if (d.src) meta.push('Agoda.com');
    if (d.market) meta.push('Châu Á');

    return '<div class="cfg-dv"><span class="tile" data-st="booked">' +
        '<span class="tp">STD</span><span class="rn">1022</span>' +
        '<span class="ics"><span>' + CFG_PVI.users + '</span><span>' + CFG_PVI.bell + '</span><span>' + CFG_PVI.send + '</span></span>' +
      '</span>' +
      '<span class="bd">' +
        '<span class="nm">' + (d.nation ? CFG_PVI.flagVN : '') + '<b>' + esc(name) + '</b></span>' +
        '<span class="dt">' + CFG_PV_DT.inD + ' - ' + CFG_PV_DT.outD + '</span>' +
        (meta.length ? '<span class="sr">' + esc(meta.join(' · ')) + '</span>' : '') +
        ((d.amount || d.pay) ? '<span class="pay">' +
          (d.pay ? '<span class="dot"></span>' : '') +
          (d.amount ? esc(amtLb) + ': <b>' + amtVal + '</b> <u>VND</u>' : 'Đã thanh toán một phần') +
          '</span>' : '') +
      '</span></div>';
  }

  /* ================= Xem trước SƠ ĐỒ ĐƠN GIẢN (Figma 41789:98064) ================= */
  var CFG_PV_TILES = [
    { st: 'empty', ok: true, badge: true, group: false },
    { st: 'booked', ok: false, badge: true, group: true },
    { st: 'inhouse', ok: false, badge: true, group: true },
    { st: 'repair', ok: true, badge: false, group: false }
  ];
  function cfgMiniSimple(d) {
    var h = ['<div class="cfg-sv">'];
    for (var i = 0; i < CFG_PV_TILES.length; i++) {
      var t = CFG_PV_TILES[i], ics = [];
      if (d.groupBk && t.group) ics.push('<span>' + CFG_PVI.users + '</span>');
      if (d.hk) ics.push(t.ok ? '<span class="ok">' + CFG_PVI.check + '</span>' : '<span>' + CFG_PVI.send + '</span>');
      h.push('<span class="tile" data-st="' + t.st + '">' +
        (d.roomType ? '<span class="tp">STD</span>' : '') +
        '<span class="rn">1022</span>' +
        (ics.length ? '<span class="ics">' + ics.join('') + '</span>' : '') +
        '</span>');
    }
    h.push('</div>');
    return h.join('');
  }

  /* ================= Xem trước THẺ XEM NHANH (Figma 41834:47500) ================= */
  function cfgRow(icon, body, right) {
    return '<div class="r"><span class="ic">' + icon + '</span><span class="tx">' + body + '</span>' +
      (right ? '<span class="rt">' + right + '</span>' : '') + '</div>';
  }
  function cfgMiniCard(d) {
    var L = [], R = [], money = [];

    /* Cột trái — thông tin đoàn */
    if (d.gRooms) L.push(cfgRow(CFG_PVI.door, '5 phòng'));
    if (d.gLead) L.push(cfgRow(CFG_PVI.flagVN, 'Mr. Trần Minh Quân ' + CFG_PVI.flag,
      CFG_PVI.phone + '<span>0123 456 789</span>'));
    if (d.gSrc) L.push(cfgRow(CFG_PVI.building, 'Agoda.com', d.gOta ? '<a class="lk">#OTA321</a>' : ''));
    else if (d.gOta) L.push(cfgRow(CFG_PVI.building, '<a class="lk">#OTA321</a>'));
    if (d.gCms) L.push(cfgRow(CFG_PVI.monitor, 'Online'));
    if (d.gMarket) L.push(cfgRow(CFG_PVI.pin, 'Châu Á'));
    if (d.gCompany) L.push(cfgRow(CFG_PVI.building, 'Công ty ezCloud toàn cầu'));
    if (d.gStaff) L.push(cfgRow(CFG_PVI.user, 'Nguyễn Văn Thảo Nguyên'));
    if (d.gNote) L.push(cfgRow(CFG_PVI.note, 'Ghi chú của booking sẽ hiển thị ở đây'));

    /* Cột phải — thông tin phòng */
    R.push('<div class="rm">' + CFG_PVI.cal +
      '<div class="mt"><div class="t"><b>Deluxe mountain View - 101</b><span class="lk-red">' + CFG_PVI.lock + '</span></div>' +
      '<div class="s">02/05/2025 12:00 - 03/05/2025 14:00' +
      (d.nights ? ' <i>(1 đêm)</i>' : '') + '</div></div></div>');
    if (d.pax) R.push('<div class="pax"><span>' + CFG_PVI.adult + '2</span><span>' + CFG_PVI.child + '1</span><span>' + CFG_PVI.baby + '0</span></div>');
    if (d.leadGuest) R.push(cfgRow(CFG_PVI.flagVN, 'Mr. Trần Minh Quân ' + CFG_PVI.crown,
      CFG_PVI.phone + '<span>0123 456 789</span>'));
    if (d.guests) {
      R.push(cfgRow(CFG_PVI.flagVN, 'Nguyễn Văn Thảo Nguyên', CFG_PVI.phone + '<span>0123 456 789</span>'));
      R.push(cfgRow(CFG_PVI.flagVN, 'Nguyễn Việt Quang', CFG_PVI.phone + '<span>0123 456 789</span>'));
    }
    if (d.note) R.push(cfgRow(CFG_PVI.note, 'Ghi chú phòng sẽ hiển thị ở đây'));
    if (d.inote) R.push(cfgRow(CFG_PVI.noteLock, '(Nội bộ) Ghi chú sẽ hiển thị ở đây'));

    if (d.total) money.push('<div class="m"><b>Tổng tiền:</b><span>24,000,000 <u>VND</u></span></div>');
    if (d.paid) money.push('<div class="m"><b>Đã thanh toán:</b><span>500,000 <u>VND</u></span></div>');
    if (d.left) money.push('<div class="m left">' + (d.payStatus ? '<i class="dot"></i>' : '') +
      '<b>Còn lại:</b><span class="red">23,500,000 <u>VND</u></span></div>');
    if (!money.length && d.payStatus) money.push('<div class="m left"><i class="dot"></i><b>Thanh toán một phần</b></div>');

    return '<div class="cfg-qv">' +
      '<div class="hd"><span class="gi">' + CFG_PVI.grpHead + '</span>' +
      '<b class="nm">Đoàn công ty ezCloud toàn cầu</b><a class="lk">#938475</a>' +
      '<span class="chip">Đã xác nhận</span>' +
      '<span class="x">' + CFG_PVI.close + '</span></div>' +
      '<div class="bd"><div class="col cl">' + L.join('') + '</div>' +
      '<div class="col cr">' + R.join('') +
      (money.length ? '<div class="money">' + money.join('') + '</div>' : '') +
      '</div></div></div>';
  }

  /* ---------- Xem trước ---------- */
  function cfgPreview() {
    var d = cfgDraft[cfgTab].on, dv = cfgDraft[cfgTab].def, h = [];
    if (cfgTab === 'timeline') {
      h.push(cfgMiniTimeline(d, dv));
    } else if (cfgTab === 'detail') {
      h.push(cfgMiniDetail(d, dv));
    } else if (cfgTab === 'simple') {
      h.push(cfgMiniSimple(d));
    } else {
      h.push(cfgMiniCard(d));
    }
    $('cfgPrev').innerHTML = h.join('') || '<p class="cfg-pv-off">Không còn thông tin nào được hiển thị.</p>';
  }

  /* ---------- Bộ lọc ---------- */
  function cfgSyncFilters() {
    var spec = CFG_SPEC[cfgTab] || [];
    var groups = [{ v: '', t: 'Chọn nhóm thông tin' }].concat(spec.map(function (g) { return { v: g.g, t: g.g }; }));
    var fields = [{ v: '', t: 'Chọn trường thông tin' }];
    spec.forEach(function (g) {
      if (cfgFG && g.g !== cfgFG) return;
      g.fs.forEach(function (f) { fields.push({ v: f.t, t: f.t }); });
    });
    vsDef('cfgFGroup', groups, cfgFG, 'Chọn nhóm thông tin');
    vsDef('cfgFField', fields, cfgFF, 'Chọn trường thông tin');
  }

  function cfgApply() {
    CFG = cfgClone(cfgDraft);
    if (lbScreen === 'timeline') render();
    closeTip();
  }

  function bindCfg() {
    if (!$('cfgCard')) return;

    $('cfgTabs').addEventListener('click', function (e) {
      var b = e.target.closest('[data-cfgtab]'); if (!b) return;
      cfgTab = b.dataset.cfgtab; cfgFG = ''; cfgFF = '';
      cfgSyncFilters(); cfgRender();
    });

    $('cfgGroups').addEventListener('click', function (e) {
      var t = e.target.closest('[data-cfgtoggle]'); if (!t) return;
      var id = t.dataset.cfgtoggle;
      cfgClosed[id] = !cfgClosed[id];
      var sec = t.closest('.cfg-grp');
      sec.classList.toggle('is-closed', !!cfgClosed[id]);
      t.setAttribute('aria-expanded', cfgClosed[id] ? 'false' : 'true');
    });

    $('cfgGroups').addEventListener('change', function (e) {
      var ck = e.target.closest('[data-cfgck]');
      if (ck) {
        cfgDraft[cfgTab].on[ck.dataset.cfgck] = ck.checked;
        var row = ck.closest('tr'), sel = row && row.querySelector('[data-cfgdef]');
        if (sel) sel.disabled = !ck.checked;
        cfgPreview();
        return;
      }
      var df = e.target.closest('[data-cfgdef]');
      if (df) { cfgDraft[cfgTab].def[df.dataset.cfgdef] = df.value; cfgPreview(); }
    });

    $('cfgSave').addEventListener('click', function () {
      cfgApply();
      toast('Đã lưu cấu hình hiển thị');
    });
    $('cfgReset').addEventListener('click', function () {
      var seed = cfgSeed();
      cfgDraft[cfgTab] = seed[cfgTab];
      cfgRender();
      toast('Đã đặt lại cấu hình về mặc định');
    });

    cfgSyncFilters();
    QB_SEL.cfgFGroup.onChange = function (v) { cfgFG = v; cfgFF = ''; cfgSyncFilters(); cfgRender(); };
    QB_SEL.cfgFField.onChange = function (v) { cfgFF = v; cfgRender(); };
    cfgRender();
  }

  /* Mở màn Cấu hình từ nơi khác (nút bánh răng trên sơ đồ timeline) */
  function gotoConfig(tab) {
    cfgTab = tab || 'timeline';
    cfgFG = ''; cfgFF = '';
    showScreen('config');
    cfgSyncFilters();
    cfgRender();
    var nav = document.querySelector('.nav-child[data-nav="cfg-map"]');
    if (nav) {
      var grp = nav.closest('.nav-group');
      if (grp) {
        grp.classList.remove('is-closed');
        var cv = grp.querySelector('.nav-caret');
        if (cv) cv.setAttribute('aria-expanded', 'true');
      }
      if (typeof selectChild === 'function') selectChild(nav);
      else {
        var prev = document.querySelector('.nav-child.is-selected');
        if (prev) { prev.classList.remove('is-selected'); prev.removeAttribute('aria-current'); }
        nav.classList.add('is-selected'); nav.setAttribute('aria-current', 'page');
      }
    }
  }

  function cfgShow() {
    cfgDraft = cfgClone(CFG);
    cfgRender();
    /* Đánh dấu đúng mục menu đang mở (vào từ menu trái cũng phải sáng) */
    var nav = document.querySelector('.nav-child[data-nav="cfg-map"]');
    if (nav) {
      var prev = document.querySelector('.nav-child.is-selected');
      if (prev && prev !== nav) { prev.classList.remove('is-selected'); prev.removeAttribute('aria-current'); }
      nav.classList.add('is-selected'); nav.setAttribute('aria-current', 'page');
    }
  }


  /* ================================================================
     20. MÀN CHI TIẾT ĐẶT PHÒNG — FOOR03
     SRS: [FOOR03] Chi tiết đặt phòng v0.1
       · 2.5.1 Phân quyền chức năng (ma trận trạng thái × action)
       · 3.1.4.2 Header + Thông tin chung
       · 3.1.5.2 Danh sách phòng / Card phòng / Bulk action
     Figma: 26641:35144 (đoàn) · 34214:112087 (lẻ) · 26641:37082 (TT chung)
            26641:38147 (Card phòng) · 26641:36076 (Bulk action)
     Dữ liệu bám 100% BOOKINGS / UNASSIGNED / ROOMS.
     ================================================================ */

  /* ---------- Ma trận phân quyền — SRS 2.5.1 ----------
     Bản export SRS mất dấu tick nên bảng dưới suy luận theo nghiệp vụ PMS
     chuẩn; mọi thay đổi chỉ cần sửa đúng bảng này.                     */
  var BD_ACTIONS = [
    { k: 'assign',    t: 'Gán phòng',          st: ['unassigned'],                     kind: 'primary' },
    { k: 'unassign',  t: 'Bỏ gán phòng',       st: ['booked'] },
    { k: 'checkin',   t: 'Nhận phòng',         st: ['booked'],                         kind: 'primary' },
    { k: 'checkout',  t: 'Trả phòng',          st: ['inhouse'],                        kind: 'primary' },
    { k: 'pay',       t: 'Thanh toán',         st: ['unassigned', 'booked', 'inhouse'], kind: 'ok' },
    { k: 'dates',     t: 'Đổi ngày ở',         st: ['unassigned', 'booked', 'inhouse'] },
    { k: 'rate',      t: 'Đổi giá',            st: ['unassigned', 'booked'] },
    { k: 'move',      t: 'Chuyển phòng',       st: ['booked', 'inhouse'] },
    { k: 'card',      t: 'Tạo thẻ (Khoá từ)',  st: ['booked', 'inhouse'] },
    { k: 'copy',      t: 'Sao chép đặt phòng', st: ['unassigned', 'booked', 'inhouse', 'checkout'] },
    { k: 'ungroup',   t: 'Gỡ phòng khỏi đoàn', st: ['unassigned', 'booked'] },
    { k: 'noshow',    t: 'Noshow',             st: ['booked'],                         danger: true },
    { k: 'cancel',    t: 'Huỷ đặt phòng',      st: ['unassigned', 'booked'],           danger: true },
    { k: 'undoIn',    t: 'Hoàn tác nhận phòng', st: ['inhouse'] },
    { k: 'undoOut',   t: 'Hoàn tác trả phòng',  st: ['checkout'] },
    { k: 'undoNoshow', t: 'Hoàn tác Noshow',    st: ['noshow'] }
  ];
  function bdAllowed(sts) {
    return BD_ACTIONS.filter(function (a) {
      for (var i = 0; i < sts.length; i++) if (a.st.indexOf(sts[i]) < 0) return false;
      return sts.length > 0;
    });
  }

  /* ---------- Nhãn trạng thái phòng — SRS 3.1.5.2 mục 7 ---------- */
  var BD_ST_LB = { unassigned: 'Chưa gán', booked: 'Đã gán', inhouse: 'Đang ở', checkout: 'Đã trả', noshow: 'Noshow' };
  var BD_ST_ORDER = ['unassigned', 'booked', 'inhouse', 'checkout', 'noshow'];
  var BD_BK_ST = {
    confirmed: { t: 'Đã xác nhận', k: 'confirmed' },
    tentative: { t: 'Tạm thời', k: 'tentative' },
    cancelled: { t: 'Huỷ', k: 'cancelled' }
  };

  var BDI = {
    users: '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="9" cy="8" r="3.4"/><path d="M2.2 20.4a6.8 6.8 0 0113.6 0z"/><circle cx="17.2" cy="8.8" r="2.8"/><path d="M13.6 20.4a8 8 0 00-1.5-4A5.4 5.4 0 0122 20.4z"/></svg>',
    copy: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="9" y="9" width="11" height="11" rx="2"/><path d="M5 15V5a2 2 0 012-2h8"/></svg>',
    check: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>',
    caret: '<svg class="cv" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>',
    minus: '<svg class="mi" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" aria-hidden="true"><path d="M6 12h12"/></svg>',
    adult: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="7" r="3.2"/><path d="M5.5 21a6.5 6.5 0 0113 0"/></svg>',
    child: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="8" r="3"/><path d="M7.5 21a4.5 4.5 0 019 0"/></svg>',
    baby: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="9" r="3.6"/><path d="M8.6 20a3.4 3.4 0 016.8 0"/></svg>',
    cal: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M8 3v4M16 3v4M3 10h18M9 15l2 2 4-4"/></svg>',
    lockOpen: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="4.5" y="10.5" width="15" height="10" rx="2"/><path d="M8 10.5V7.5a4 4 0 017.5-2"/></svg>',
    lockClose: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="4.5" y="10.5" width="15" height="10" rx="2"/><path d="M8 10.5V7.5a4 4 0 018 0v3"/></svg>',
    flag: '<svg class="lead" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 21V4M5 5h11l-2 3.5L16 12H5"/></svg>',
    crown: '<svg class="mk" viewBox="0 0 24 24" fill="#FFB400" aria-hidden="true"><path d="M4 18h16l1-9-5 3-4-6-4 6-5-3z"/></svg>',
    cake: '<svg class="mk" viewBox="0 0 24 24" fill="none" stroke="#FF6B6B" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 20h16v-6H4z"/><path d="M4 14c2 0 2-2 4-2s2 2 4 2 2-2 4-2 2 2 4 2"/><path d="M12 4v4"/></svg>',
    ret: '<svg class="mk" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M12 8v4l3 2"/></svg>',
    phone: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6.5 3h3l1.5 4-2 1.4a12 12 0 006.6 6.6L17 13l4 1.5v3a2 2 0 01-2.2 2A17.5 17.5 0 013.5 5.2 2 2 0 015.5 3h1z"/></svg>',
    mail: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 7l9 6 9-6"/></svg>',
    dob: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M8 3v4M16 3v4M3 10h18"/></svg>',
    id: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="14" rx="2"/><circle cx="9" cy="11" r="2"/><path d="M14 10h4M14 14h4M6 16a3.5 3.5 0 016 0"/></svg>',
    pen: '<svg class="pen" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 20h9M16.5 3.5a2.1 2.1 0 013 3L7 19l-4 1 1-4z"/></svg>',
    dots: '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><circle cx="5" cy="12" r="2"/><circle cx="12" cy="12" r="2"/><circle cx="19" cy="12" r="2"/></svg>',
    plus: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"><path d="M12 5v14M5 12h14"/></svg>',
    note: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 4h11l3 3v13H5z"/><path d="M8 10h8M8 14h5"/></svg>',
    door: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 21V4a1 1 0 011-1h10a1 1 0 011 1v17M4 21h16M14 12v.01"/></svg>'
  };
  function bdRoomIcon() {
    return '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">' +
      '<rect x="3" y="5" width="18" height="16" rx="2"/><path d="M8 3v4M16 3v4M3 10h18"/><path d="M8 14h8"/></svg>';
  }

  /* ---------- Trạng thái màn ---------- */
  var BD = {
    bk: null, rooms: [], focus: null, tab: 'rooms', groupBy: 'Loại phòng',
    kw: '', sel: {}, closed: {}, from: 'timeline', isGroup: false
  };
  var BD_TAB_MEM = 'rooms';

  /* ---------- Gom phòng của 1 booking — SRS 3.1.5.2 mục 1 ---------- */
  function bdStOf(x) { return x.st === 'repair' ? 'booked' : (x.st || 'booked'); }
  function bdRoomRec(src, kind) {
    var isUA = kind === 'ua';
    var room = isUA ? null : roomById(src.room);
    return {
      key: src.id, kind: kind, ref: src,
      st: isUA ? 'unassigned' : bdStOf(src),
      roomId: isUA ? '' : src.room,
      typeName: isUA ? typeName(src.type) : (room ? typeName(room.type) : ''),
      title: isUA ? (typeName(src.type) + ' - N/A') : fullRoomName(src),
      in: src.in, out: src.out,
      guests: src.guests || [],
      adults: src.adults || 0, children: src.children || 0, infants: src.infants || 0,
      total: src.total || 0, paid: src.paid || 0,
      rate: src.rate || 'Rate code 1',
      note: src.note || '', inote: src.inote || '',
      locked: !!src.moveLocked
    };
  }
  function bdCollect(bk) {
    var out = [], i;
    var gid = bk.group && bk.group.gid;
    if (gid) {
      for (i = 0; i < BOOKINGS.length; i++)
        if (BOOKINGS[i].group && BOOKINGS[i].group.gid === gid) out.push(bdRoomRec(BOOKINGS[i], 'bk'));
      for (i = 0; i < UNASSIGNED.length; i++)
        if (UNASSIGNED[i].gid === gid) out.push(bdRoomRec(UNASSIGNED[i], 'ua'));
    } else {
      out.push(bdRoomRec(bk, 'bk'));
    }
    return out;
  }

  /* ---------- Mở màn ---------- */
  function openBookingDetail(bkId, focusRoomId) {
    var bk = bkById(bkId);
    if (!bk) { toast('Không tìm thấy đặt phòng ' + bkId, true); return; }
    BD.bk = bk;
    BD.rooms = bdCollect(bk);
    BD.isGroup = BD.rooms.length > 1;
    BD.from = (typeof lbScreen === 'string') ? lbScreen : 'timeline';
    BD.sel = {}; BD.closed = {}; BD.kw = '';
    BD.tab = BD_TAB_MEM;
    var f = null, i;
    for (i = 0; i < BD.rooms.length; i++)
      if (BD.rooms[i].key === bkId || (focusRoomId && BD.rooms[i].roomId === focusRoomId)) f = BD.rooms[i];
    BD.focus = f || BD.rooms[0];
    closeTip(); closeMenu();
    bdOpen();
  }

  /* ---------- Đóng/mở slide-over (chiếm 90% màn hình) ---------- */
  function bdIsOpen() { var e = $('bdCard'); return !!e && e.classList.contains('is-open'); }
  function bdOpen() {
    var el = $('bdCard');
    $('bdBackdrop').classList.add('is-open');
    el.hidden = false;
    el.classList.add('is-open');
    el.setAttribute('aria-hidden', 'false');
    document.body.classList.add('bd-open');
    bdRender();
    var f = el.querySelector('.bd-tab.is-on');
    if (f) f.focus();
  }
  function bdCloseDrawer() {
    var el = $('bdCard');
    bdMenuClose();
    el.classList.remove('is-open');
    el.setAttribute('aria-hidden', 'true');
    $('bdBackdrop').classList.remove('is-open');
    document.body.classList.remove('bd-open');
    setTimeout(function () { if (!bdIsOpen()) el.hidden = true; }, 200);
  }

  /* ---------- Header ---------- */
  function bdSumPax() {
    var a = 0, c = 0, b = 0;
    BD.rooms.forEach(function (r) { a += r.adults; c += r.children; b += r.infants; });
    return { a: a, c: c, b: b };
  }
  function bdSumLeft() {
    var s = 0;
    BD.rooms.forEach(function (r) { s += Math.max(0, r.total - r.paid); });
    return s;
  }
  function bdBkStatus() {
    var bk = BD.bk;
    if (bk.bkStatus) return bk.bkStatus;
    return bk.confirmed ? 'confirmed' : 'tentative';
  }
  function bdCanEditStatus() {
    /* SRS 1.4: chỉ được sửa trạng thái đặt khi trạng thái phòng là Booked */
    for (var i = 0; i < BD.rooms.length; i++) if (BD.rooms[i].st !== 'booked' && BD.rooms[i].st !== 'unassigned') return false;
    return true;
  }

  function bdRenderHead() {
    var bk = BD.bk, g = bk.group;
    $('bdGrpIcon').hidden = !BD.isGroup;
    if (BD.isGroup) $('bdGrpIcon').innerHTML = BDI.users;

    var nm = $('bdName');
    nm.textContent = BD.isGroup ? (g ? g.name : bk.name) : bk.name;
    nm.hidden = false;

    $('bdCode').innerHTML = esc(bk.code || bk.id) + BDI.copy;
    $('bdCode').classList.remove('is-copied');

    var stKey = bdBkStatus(), st = BD_BK_ST[stKey];
    var sb = $('bdStatus');
    sb.dataset.st = stKey;
    sb.textContent = st.t;
    sb.classList.toggle('is-editable', bdCanEditStatus());
    sb.setAttribute('aria-expanded', 'false');

    var p = bdSumPax(), left = bdSumLeft(), h = [];
    h.push('<span class="bd-pax">' +
      '<span title="Người lớn">' + BDI.adult + p.a + '</span>' +
      '<span title="Trẻ em">' + BDI.child + p.c + '</span>' +
      '<span title="Em bé">' + BDI.baby + p.b + '</span></span>');
    if (bk.ota) {
      h.push('<span class="bd-ota">' +
        '<button class="lk" type="button" id="bdOta" aria-label="Sao chép mã OTA">' + esc(bk.ota) + '</button>' +
        '<span class="nm" title="' + esc(bk.src || '') + '">' + esc(bk.src || '') + '</span></span>');
    }
    var lead = (bk.guests || []).filter(function (x) { return x.lead; })[0] || (bk.guests || [])[0];
    var leadName = (g && g.lead) ? g.lead : (lead ? lead.n : bk.name);
    h.push('<span class="bd-booker" title="' + esc(leadName) + (lead && lead.p ? ' · ' + esc(lead.p) : '') + '">' +
      flagSVG(lead ? lead.cc : 'VN') + '<span>' + esc(leadName) + '</span>' + BDI.flag + '</span>');
    h.push('<span class="bd-sep">|</span>');
    h.push('<span class="bd-left-amt">' + (BD.isGroup ? 'Còn lại của đoàn: ' : 'Còn lại: ') +
      '<b>' + money(left) + '</b> VND</span>');
    $('bdMeta').innerHTML = h.join('');

    $('bdTabRooms').textContent = BD.isGroup ? ('Danh sách phòng (' + BD.rooms.length + ')') : 'Phòng';
    Array.prototype.forEach.call(document.querySelectorAll('#bdTabs .bd-tab'), function (b) {
      var on = b.dataset.bdtab === BD.tab;
      b.classList.toggle('is-on', on);
      b.setAttribute('aria-selected', on ? 'true' : 'false');
    });
    $('bdCard').classList.toggle('is-single', !BD.isGroup);
    $('bdColL').hidden = !BD.isGroup || BD.tab !== 'rooms';
  }

  /* ---------- Cột trái: nhóm + card phòng — SRS 3.1.5.2 mục 2-7 ---------- */
  function bdFloor(r) { var m = /(\d)/.exec(r.roomId || ''); return m ? m[1] : '1'; }
  function bdBucket(r) {
    if (r.st === 'unassigned') return 'Chưa gán';
    if (BD.groupBy === 'Tầng') return 'Tầng ' + bdFloor(r);
    if (BD.groupBy === 'Trạng thái') return BD_ST_LB[r.st] || 'Khác';
    if (BD.groupBy === 'Phòng') return '';
    return r.typeName || 'Khác';
  }
  function bdVisible(r) {
    if (!BD.kw) return true;
    var k = BD.kw.toLowerCase();
    if ((r.title || '').toLowerCase().indexOf(k) >= 0) return true;
    for (var i = 0; i < r.guests.length; i++)
      if ((r.guests[i].n || '').toLowerCase().indexOf(k) >= 0) return true;
    return false;
  }
  function bdCkBox() {
    return '<span class="bx">' +
      BDI.check.replace('<svg ', '<svg class="ck" ') + BDI.minus + '</span>';
  }
  function bdCardHTML(r) {
    var lead = r.guests.filter(function (g) { return g.lead; })[0] || r.guests[0];
    return '<div class="bd-card' + (BD.focus && BD.focus.key === r.key ? ' is-focus' : '') +
      '" role="button" tabindex="0" data-bdroom="' + esc(r.key) + '" aria-label="' + esc(r.title) + '">' +
      '<label class="bd-ck" onclick="event.stopPropagation()">' +
        '<input type="checkbox" data-bdck="' + esc(r.key) + '"' + (BD.sel[r.key] ? ' checked' : '') +
        ' aria-label="Chọn ' + esc(r.title) + '">' + bdCkBox() + '</label>' +
      '<span class="sticon" style="color:var(--tl-st-' +
        (r.st === 'unassigned' ? 'unassign' : r.st) + ')" title="' + esc(BD_ST_LB[r.st]) + '">' + bdRoomIcon() + '</span>' +
      '<span class="bd-cbody">' +
        '<span class="rn">' + esc(r.title) + '</span>' +
        '<span class="tm"><span>' + dmy(r.in) + ' ' + hm(r.in) + '</span><span class="sep">-</span>' +
          '<span>' + dmy(r.out) + ' ' + hm(r.out) + '</span>' +
          '<span class="n">(' + Math.max(1, dayDiff(r.out, r.in)) + ' đêm)</span></span>' +
        (lead ? '<span class="gu">' + flagSVG(lead.cc) + '<span class="nm">' + esc(lead.n) + '</span>' +
          (lead.lead ? BDI.flag : '') + '</span>' : '') +
      '</span></div>';
  }

  function bdRenderList() {
    if (!BD.isGroup) { $('bdList').innerHTML = ''; return; }
    var vis = BD.rooms.filter(bdVisible);
    var selN = Object.keys(BD.sel).filter(function (k) { return BD.sel[k]; }).length;
    var h = [];

    h.push('<div class="bd-allrow"><label class="bd-ck">' +
      '<input type="checkbox" id="bdAll" aria-label="Chọn tất cả phòng">' + bdCkBox() + '</label>' +
      '<span>Chọn tất cả (' + BD.rooms.length + ')</span></div>');

    if (!vis.length) { h.push('<div class="bd-empty">Không tìm thấy phòng phù hợp</div>'); }
    else if (BD.groupBy === 'Phòng') {
      vis.forEach(function (r) { h.push(bdCardHTML(r)); });
    } else {
      var buckets = {}, order = [];
      vis.forEach(function (r) {
        var b = bdBucket(r);
        if (!buckets[b]) { buckets[b] = []; order.push(b); }
        buckets[b].push(r);
      });
      /* SRS: nhóm "Chưa gán" luôn đẩy lên đầu */
      order.sort(function (a, b) {
        if (a === 'Chưa gán') return -1;
        if (b === 'Chưa gán') return 1;
        if (BD.groupBy === 'Trạng thái') {
          var ia = BD_ST_ORDER.map(function (k) { return BD_ST_LB[k]; }).indexOf(a);
          var ib = BD_ST_ORDER.map(function (k) { return BD_ST_LB[k]; }).indexOf(b);
          return ia - ib;
        }
        return 0;
      });
      order.forEach(function (b) {
        var list = buckets[b], id = 'g|' + b, closed = !!BD.closed[id];
        var on = list.filter(function (r) { return BD.sel[r.key]; }).length;
        h.push('<section class="bd-grp' + (closed ? ' is-closed' : '') + '">');
        h.push('<div class="bd-grprow" data-bdgrp="' + esc(id) + '">' +
          '<label class="bd-ck" onclick="event.stopPropagation()">' +
          '<input type="checkbox" data-bdgck="' + esc(b) + '"' + (on === list.length ? ' checked' : '') +
          ' aria-label="Chọn nhóm ' + esc(b) + '">' + bdCkBox() + '</label>' +
          '<span class="nm">' + esc(b) + ' (' + list.length + ')</span>' + BDI.caret + '</div>');
        h.push('<div class="bd-grp-body">');
        list.forEach(function (r) { h.push(bdCardHTML(r)); });
        h.push('</div></section>');
      });
    }
    $('bdList').innerHTML = h.join('');

    /* checkbox 3 trạng thái */
    var all = $('bdAll');
    if (all) {
      all.checked = selN > 0 && selN === BD.rooms.length;
      all.indeterminate = selN > 0 && selN < BD.rooms.length;
    }
    Array.prototype.forEach.call(document.querySelectorAll('#bdList [data-bdgck]'), function (el) {
      var b = el.dataset.bdgck;
      var list = BD.rooms.filter(function (r) { return bdVisible(r) && bdBucket(r) === b; });
      var on = list.filter(function (r) { return BD.sel[r.key]; }).length;
      el.checked = on > 0 && on === list.length;
      el.indeterminate = on > 0 && on < list.length;
    });
    bdRenderBulk();
  }

  /* ---------- Bulk action bar — SRS 3.1.5.2 mục 37-49 ---------- */
  function bdRenderBulk() {
    var keys = Object.keys(BD.sel).filter(function (k) { return BD.sel[k]; });
    var bar = $('bdBulk');
    bar.classList.toggle('is-on', keys.length > 0);
    if (!keys.length) return;
    var sts = [];
    keys.forEach(function (k) {
      var r = BD.rooms.filter(function (x) { return x.key === k; })[0];
      if (r && sts.indexOf(r.st) < 0) sts.push(r.st);
    });
    $('bdBulkTxt').textContent = 'Đang chọn (' + keys.length + '):';
    var acts = bdAllowed(sts), h = [];
    acts.slice(0, 4).forEach(function (a) {
      var cls = a.kind === 'primary' ? 'tl-btn-primary' : (a.kind === 'ok' ? 'tl-btn-primary bd-btn-ok' : 'lb-btn');
      h.push('<button class="' + cls + '" type="button" data-bdact="' + a.k + '">' + esc(a.t) + '</button>');
    });
    if (acts.length > 4)
      h.push('<button class="lb-btn" type="button" id="bdBulkMore" aria-haspopup="menu" aria-expanded="false" aria-label="Thêm thao tác">' + BDI.dots + '</button>');
    if (!acts.length)
      h.push('<span style="font-size:13px;color:var(--theme-text-secondary)">Không có thao tác chung cho các phòng đang chọn</span>');
    $('bdBulkBtns').innerHTML = h.join('');
  }

  /* ---------- Cột phải ---------- */
  function bdEdit(key, text, ph) {
    return '<span class="bd-ed" role="button" tabindex="0" data-bded="' + esc(key) + '">' +
      '<span class="tx' + (text ? '' : ' muted') + '">' + esc(text || ph || 'Chưa có thông tin') + '</span>' + BDI.pen + '</span>';
  }
  function bdAge(dob) { return dob ? dob : '—'; }

  function bdRenderRoom() {
    var r = BD.focus;
    if (!r) { $('bdColR').innerHTML = '<div class="bd-empty">Chưa chọn phòng</div>'; return; }
    var acts = bdAllowed([r.st]);
    var main = acts.filter(function (a) { return a.kind === 'primary'; })[0];
    var h = [];

    h.push('<div class="bd-pane"><section class="bd-block">');
    h.push('<div class="bd-rhead"><span class="cal">' + BDI.cal + '</span>' +
      '<h2>' + esc(r.title) + '</h2>' +
      '<button class="bd-lock' + (r.locked ? ' is-locked' : '') + '" type="button" id="bdLock" ' +
        'aria-label="Khoá chuyển phòng" title="Khoá chuyển phòng">' +
        (r.locked ? BDI.lockClose : BDI.lockOpen) + '</button>' +
      '<span class="sp"></span>' +
      (main ? '<button class="tl-btn-primary" type="button" data-bdact="' + main.k + '">' + esc(main.t) + '</button>' : '') +
      '<button class="lb-btn" type="button" id="bdRoomMore" aria-haspopup="menu" aria-expanded="false" aria-label="Thao tác khác">' + BDI.dots + '</button>' +
      '</div>');

    h.push('<div class="bd-fields">');
    h.push('<div class="bd-f"><div class="k">Ngày đến - đi:</div><div class="v">' +
      dmy(r.in) + ' ' + hm(r.in) + '<br>' + dmy(r.out) + ' ' + hm(r.out) +
      ' <span class="muted">(' + Math.max(1, dayDiff(r.out, r.in)) + ' đêm)</span></div></div>');
    h.push('<div class="bd-f"><div class="k">Chính sách giá:</div><div class="v">' + esc(r.rate) + '</div></div>');
    h.push('<div class="bd-f"><div class="k">Tiền phòng:</div><div class="v">' + money(r.total) + ' VND</div></div>');
    h.push('<div class="bd-f"><div class="k">Số thẻ/pin:</div><div class="v">' +
      '<button class="lk" type="button" id="bdCards">' + (r.st === 'unassigned' ? 0 : 3) + ' thẻ/pin</button></div></div>');
    h.push('<div class="bd-f"><div class="k">Ghi chú phòng:</div><div class="v"><div class="bd-note">' +
      bdEdit('note', r.note, 'Chưa có ghi chú') + '</div></div></div>');
    h.push('<div class="bd-f"><div class="k">Ghi chú nội bộ:</div><div class="v">' +
      bdEdit('inote', r.inote, 'Chưa có ghi chú') + '</div></div>');
    h.push('</div>');

    /* Danh sách khách — SRS 3.1.5.2 mục 21-35 */
    h.push('<div class="bd-guests"><div class="bd-guests-h">' +
      '<h3>Thông tin khách</h3>' +
      '<span class="bd-pax" style="font-size:13px">' +
        '<span title="Người lớn">' + BDI.adult + r.adults + '</span>' +
        '<span title="Trẻ em">' + BDI.child + r.children + '</span>' +
        '<span title="Em bé">' + BDI.baby + r.infants + '</span></span>' +
      '<span class="sp"></span>' +
      '<button class="bd-addg" type="button" id="bdAddGuest">' + BDI.plus + 'Thêm khách</button></div>');
    if (!r.guests.length) h.push('<div class="bd-empty">Chưa có khách trong phòng</div>');
    r.guests.forEach(function (g, i) {
      h.push('<div class="bd-g"><span class="gi">' +
        '<span class="gn">' + flagSVG(g.cc) + esc(g.n) +
          (g.lead ? BDI.crown : '') + (i === 0 ? BDI.cake : '') + BDI.ret + '</span>' +
        '<span class="gm">' +
          '<span>' + BDI.adult + '21 tuổi</span><span>·</span>' +
          '<span>' + BDI.dob + '10/10/1996</span><span>·</span>' +
          '<span>' + BDI.phone + esc(g.p || '—') + '</span><span>·</span>' +
          '<span>' + BDI.id + 'CCCD: 0010968274582</span>' +
        '</span></span>' +
        '<span class="ga">' +
          '<button class="bd-gib" type="button" data-bdg="detail" aria-label="Chi tiết khách">' + BDI.note + '</button>' +
          '<button class="bd-gib" type="button" data-bdg="edit" aria-label="Sửa thông tin khách">' + BDI.pen.replace(' class="pen"', '') + '</button>' +
          '<button class="bd-gib" type="button" data-bdg="more" aria-label="Thao tác khác">' + BDI.dots + '</button>' +
        '</span></div>');
    });
    h.push('</div></section></div>');
    $('bdColR').innerHTML = h.join('');
  }

  /* ---------- Tab Thông tin chung — Figma 26641:37082 ---------- */
  function bdRenderGeneral() {
    var bk = BD.bk, g = bk.group;
    var lead = (bk.guests || []).filter(function (x) { return x.lead; })[0] || (bk.guests || [])[0];
    var stKey = bdBkStatus(), st = BD_BK_ST[stKey];
    var h = [];
    h.push('<div class="bd-gen"><section class="bd-block" style="padding:16px 20px">');
    h.push('<h2>Thông tin chung</h2>');
    h.push('<div class="bd-gen-grid">');

    h.push('<div class="bd-f full"><div class="k">Người đặt:</div><div class="v"><span class="bd-booker-line">' +
      '<span style="display:flex;align-items:center;gap:6px">' + flagSVG(lead ? lead.cc : 'VN') +
        bdEdit('booker', (g && g.lead) ? g.lead : (lead ? lead.n : bk.name)) + '</span>' +
      '<span class="sub">' + BDI.phone + esc(lead && lead.p ? lead.p : '—') +
        ' | Email: ' + esc(bk.email || 'quanntm@gmail.com') + '</span>' +
      '</span></div></div>');

    h.push('<div class="bd-f"><div class="k">Trạng thái đặt:</div><div class="v">' +
      '<button class="bd-chip' + (bdCanEditStatus() ? ' is-editable' : '') + '" type="button" id="bdStatus2" data-st="' + stKey + '" ' +
      'aria-haspopup="listbox" aria-expanded="false">' + esc(st.t) + '</button></div></div>');
    h.push('<div class="bd-f"><div class="k">Nguồn:</div><div class="v">' +
      '<span class="bd-ed" role="button" tabindex="0" data-bdsel="src"><span class="tx' + (bk.src ? '' : ' muted') + '">' +
      esc(bk.src || 'Chưa có thông tin') + '</span>' + BDI.caret.replace('class="cv"', 'class="pen"') + '</span></div></div>');

    h.push('<div class="bd-f"><div class="k">Công ty:</div><div class="v">' +
      '<span class="bd-ed" role="button" tabindex="0" data-bdsel="company"><span class="tx' + (bk.company ? '' : ' muted') + '">' +
      esc(bk.company || 'Chưa có thông tin') + '</span>' + BDI.caret.replace('class="cv"', 'class="pen"') + '</span></div></div>');
    h.push('<div class="bd-f"><div class="k">Thị trường:</div><div class="v">' +
      '<span class="bd-ed" role="button" tabindex="0" data-bdsel="market"><span class="tx' + (bk.market ? '' : ' muted') + '">' +
      esc(bk.market || 'Chưa có thông tin') + '</span>' + BDI.caret.replace('class="cv"', 'class="pen"') + '</span></div></div>');

    h.push('<div class="bd-f"><div class="k">Mã OTA:</div><div class="v">' +
      (bk.ota ? '<span style="color:var(--ez-primary);font-weight:500">' + esc(bk.ota) + '</span>' :
        '<span class="muted">Chưa có thông tin</span>') + '</div></div>');
    h.push('<div class="bd-f"><div class="k">NV kinh doanh:</div><div class="v">' +
      '<span class="bd-ed" role="button" tabindex="0" data-bdsel="staff"><span class="tx' + (bk.staff ? '' : ' muted') + '">' +
      esc(bk.staff || 'Chưa có thông tin') + '</span>' + BDI.caret.replace('class="cv"', 'class="pen"') + '</span></div></div>');

    h.push('<div class="bd-f"><div class="k">Mã CMS:</div><div class="v">' +
      (bk.cms ? esc(bk.cms) : '<span class="muted">Chưa có thông tin</span>') + '</div></div>');
    h.push('<div class="bd-f"><div class="k">Mã TA:</div><div class="v">' +
      '<span class="bd-ed" role="button" tabindex="0" data-bdsel="ta"><span class="tx' + (bk.ta ? '' : ' muted') + '">' +
      esc(bk.ta || 'Chưa có thông tin') + '</span>' + BDI.caret.replace('class="cv"', 'class="pen"') + '</span></div></div>');

    h.push('<div class="bd-f full"><div class="k">Ghi chú đặt phòng:</div><div class="v">' +
      bdEdit('bknote', bk.note, 'Chưa có ghi chú') + '</div></div>');
    h.push('<div class="bd-f full"><div class="k">Ghi chú đặt phòng nội bộ:</div><div class="v">' +
      bdEdit('bkinote', bk.inote, 'Chưa có ghi chú') + '</div></div>');

    h.push('</div></section></div>');
    $('bdColR').innerHTML = h.join('');
  }

  function bdRender() {
    if (!BD.bk) return;
    bdRenderHead();
    if (BD.tab === 'general') { $('bdBulk').classList.remove('is-on'); bdRenderGeneral(); }
    else { bdRenderList(); bdRenderRoom(); }
  }

  /* ---------- Edit inline — SRS "Common Edit Inline" ---------- */
  var BD_ED_FIELD = {
    gname:  { get: function () { return BD.bk.group ? BD.bk.group.name : BD.bk.name; },
              set: function (v) { if (BD.bk.group) BD.bk.group.name = v; else BD.bk.name = v; } },
    booker: { get: function () { return BD.bk.group ? BD.bk.group.lead : BD.bk.name; },
              set: function (v) { if (BD.bk.group) BD.bk.group.lead = v; else BD.bk.name = v; } },
    bknote: { get: function () { return BD.bk.note || ''; }, set: function (v) { BD.bk.note = v; }, area: true },
    bkinote: { get: function () { return BD.bk.inote || ''; }, set: function (v) { BD.bk.inote = v; }, area: true },
    note:   { get: function () { return BD.focus.ref.note || ''; },
              set: function (v) { BD.focus.ref.note = v; BD.focus.note = v; }, area: true },
    inote:  { get: function () { return BD.focus.ref.inote || ''; },
              set: function (v) { BD.focus.ref.inote = v; BD.focus.inote = v; }, area: true }
  };
  function bdStartEdit(host, key) {
    var f = BD_ED_FIELD[key]; if (!f || host.querySelector('input,textarea')) return;
    var cur = f.get() || '';
    var el = document.createElement(f.area ? 'textarea' : 'input');
    el.className = 'bd-edin'; el.value = cur;
    if (f.area) el.setAttribute('maxlength', '2000');
    var wrap = host.parentNode;
    host.hidden = true;
    wrap.insertBefore(el, host);
    el.focus(); el.select();
    var done = false;
    function commit(save) {
      if (done) return; done = true;
      if (save) f.set(el.value.trim());
      el.remove(); host.hidden = false;
      bdRender();
      if (save) toast('Đã cập nhật thông tin');
    }
    el.addEventListener('keydown', function (e) {
      if (e.key === 'Enter' && !f.area) { e.preventDefault(); commit(true); }
      else if (e.key === 'Escape') { e.preventDefault(); commit(false); }
    });
    el.addEventListener('blur', function () { commit(true); });
  }

  /* ---------- Menu dùng chung ---------- */
  function bdMenuOpen(anchor, items) {
    var m = $('bdMenu');
    m.innerHTML = items.map(function (it) {
      if (it === '-') return '<div class="sep"></div>';
      return '<button type="button" data-bdact="' + it.k + '"' + (it.danger ? ' class="danger"' : '') + '>' +
        esc(it.t) + '</button>';
    }).join('');
    m.classList.add('is-open');
    var r = anchor.getBoundingClientRect(), w = m.offsetWidth, hgt = m.offsetHeight;
    var top = r.bottom + 6;
    if (top + hgt > window.innerHeight - 8) top = Math.max(8, r.top - hgt - 6);
    m.style.top = top + 'px';
    m.style.left = Math.min(Math.max(8, r.right - w), window.innerWidth - w - 8) + 'px';
    anchor.setAttribute('aria-expanded', 'true');
    bdMenuAnchor = anchor;
  }
  var bdMenuAnchor = null;
  function bdMenuClose() {
    var m = $('bdMenu'); if (!m) return;
    m.classList.remove('is-open');
    if (bdMenuAnchor) bdMenuAnchor.setAttribute('aria-expanded', 'false');
    bdMenuAnchor = null;
  }

  /* ---------- Popup cảnh báo ratecode — SRS 3.2/3.9/3.10 ---------- */
  function bdWarnRate(what, apply) {
    openGenDlg('Cảnh báo thay đổi ' + what,
      '<p style="margin:0 0 8px;color:var(--theme-text-primary)">Người dùng cần chọn lại chính sách giá được áp dụng cho ' +
      esc(what) + ' vừa đổi.</p>' +
      '<p style="margin:0;color:var(--theme-text-secondary);font-size:13px">Huỷ: Không cập nhật, giữ lại thông tin ban đầu. ' +
      'Xác nhận: Cập nhật giá trị mới và chính sách giá mới.</p>',
      '<button class="tl-btn-ghost" type="button" data-tl-close>Huỷ</button>' +
      '<button class="tl-btn-primary" type="button" id="bdWarnOk">Xác nhận</button>');
    var ok = $('bdWarnOk');
    if (ok) ok.addEventListener('click', function () { closeDlgs(); apply(); });
  }

  /* ---------- Hành động ---------- */
  function bdDoAction(k) {
    var a = BD_ACTIONS.filter(function (x) { return x.k === k; })[0];
    var keys = Object.keys(BD.sel).filter(function (x) { return BD.sel[x]; });
    var targets = keys.length ? keys : (BD.focus ? [BD.focus.key] : []);
    if (!a || !targets.length) return;

    function each(fn) {
      targets.forEach(function (key) {
        var r = BD.rooms.filter(function (x) { return x.key === key; })[0];
        if (r) fn(r);
      });
    }
    if (k === 'checkin') { each(function (r) { if (r.kind === 'bk') { r.ref.st = 'inhouse'; r.ref.manual = true; } }); }
    else if (k === 'checkout') { each(function (r) { if (r.kind === 'bk') { r.ref.st = 'checkout'; r.ref.manual = true; } }); }
    else if (k === 'undoIn') { each(function (r) { if (r.kind === 'bk') { r.ref.st = 'booked'; r.ref.manual = true; } }); }
    else if (k === 'undoOut') { each(function (r) { if (r.kind === 'bk') { r.ref.st = 'inhouse'; r.ref.manual = true; } }); }
    else if (k === 'pay') { each(function (r) { r.ref.paid = r.ref.total; r.ref.pay = 'paid'; }); }
    else { toast('Thao tác: ' + a.t + ' (' + targets.length + ' phòng)'); }

    BD.rooms = bdCollect(BD.bk);
    if (BD.focus) BD.focus = BD.rooms.filter(function (x) { return x.key === BD.focus.key; })[0] || BD.rooms[0];
    BD.sel = {};
    bdMenuClose();
    bdRender();
    if (['checkin', 'checkout', 'undoIn', 'undoOut', 'pay'].indexOf(k) >= 0)
      toast('Đã thực hiện: ' + a.t + ' (' + targets.length + ' phòng)');
  }

  function bdCopy(el, text, cls) {
    var ok = function () {
      el.classList.add(cls || 'is-copied');
      toast('Đã sao chép ' + text);
      setTimeout(function () { el.classList.remove(cls || 'is-copied'); }, 1600);
    };
    if (navigator.clipboard && navigator.clipboard.writeText) navigator.clipboard.writeText(text).then(ok, ok);
    else ok();
  }

  /* ---------- Bind ---------- */
  function bindBd() {
    if (!$('bdCard')) return;

    vsDef('bdGroupBy', ['Loại phòng', 'Tầng', 'Trạng thái', 'Phòng'], 'Loại phòng');
    QB_SEL.bdGroupBy.onChange = function (v) { BD.groupBy = v; BD.closed = {}; bdRenderList(); };

    $('bdTabs').addEventListener('click', function (e) {
      var b = e.target.closest('[data-bdtab]'); if (!b) return;
      BD.tab = b.dataset.bdtab; BD_TAB_MEM = BD.tab;
      bdRender();
    });

    $('bdKw').addEventListener('input', function () {
      BD.kw = this.value.replace(/^\s+|\s+$/g, '');
      bdRenderList();
    });

    /* Cột trái */
    $('bdList').addEventListener('change', function (e) {
      var one = e.target.closest('[data-bdck]');
      if (one) { BD.sel[one.dataset.bdck] = one.checked; bdRenderList(); return; }
      var all = e.target.closest('#bdAll');
      if (all) {
        BD.rooms.forEach(function (r) { BD.sel[r.key] = all.checked; });
        bdRenderList(); return;
      }
      var grp = e.target.closest('[data-bdgck]');
      if (grp) {
        var b = grp.dataset.bdgck;
        BD.rooms.filter(function (r) { return bdVisible(r) && bdBucket(r) === b; })
          .forEach(function (r) { BD.sel[r.key] = grp.checked; });
        bdRenderList();
      }
    });
    $('bdList').addEventListener('click', function (e) {
      var g = e.target.closest('[data-bdgrp]');
      if (g && !e.target.closest('.bd-ck')) {
        BD.closed[g.dataset.bdgrp] = !BD.closed[g.dataset.bdgrp];
        bdRenderList(); return;
      }
      var c = e.target.closest('[data-bdroom]');
      if (c && !e.target.closest('.bd-ck')) {
        BD.focus = BD.rooms.filter(function (x) { return x.key === c.dataset.bdroom; })[0] || BD.focus;
        bdRenderList(); bdRenderRoom();
      }
    });
    $('bdList').addEventListener('keydown', function (e) {
      if (e.key !== 'Enter' && e.key !== ' ') return;
      var c = e.target.closest('[data-bdroom]'); if (!c) return;
      e.preventDefault(); c.click();
    });

    /* Bulk */
    $('bdBulkX').addEventListener('click', function () { BD.sel = {}; bdRenderList(); });
    $('bdBulk').addEventListener('click', function (e) {
      var m = e.target.closest('#bdBulkMore');
      if (m) {
        e.stopPropagation();
        var sts = [];
        Object.keys(BD.sel).filter(function (k) { return BD.sel[k]; }).forEach(function (k) {
          var r = BD.rooms.filter(function (x) { return x.key === k; })[0];
          if (r && sts.indexOf(r.st) < 0) sts.push(r.st);
        });
        bdMenuOpen(m, bdAllowed(sts).slice(4));
        return;
      }
      var b = e.target.closest('[data-bdact]');
      if (b) bdDoAction(b.dataset.bdact);
    });

    /* Cột phải */
    $('bdColR').addEventListener('click', function (e) {
      var more = e.target.closest('#bdRoomMore');
      if (more) { e.stopPropagation(); bdMenuOpen(more, bdAllowed([BD.focus.st])); return; }
      var lock = e.target.closest('#bdLock');
      if (lock) {
        var r = BD.focus;
        openGenDlg('Khoá chuyển phòng',
          '<p style="margin:0;color:var(--theme-text-primary)">' +
          (r.locked ? 'Bỏ khoá chuyển phòng cho ' : 'Khoá chuyển phòng cho ') + esc(r.title) + '?</p>' +
          '<p style="margin:8px 0 0;color:var(--theme-text-secondary);font-size:13px">Khi khoá, phòng này sẽ không thể bị chuyển sang phòng khác.</p>',
          '<button class="tl-btn-ghost" type="button" data-tl-close>Huỷ</button>' +
          '<button class="tl-btn-primary" type="button" id="bdLockOk">Xác nhận</button>');
        var lok = $('bdLockOk');
        if (lok) lok.addEventListener('click', function () {
          closeDlgs();
          r.ref.moveLocked = !r.ref.moveLocked; r.locked = !!r.ref.moveLocked;
          bdRenderRoom(); toast(r.locked ? 'Đã khoá chuyển phòng' : 'Đã bỏ khoá chuyển phòng');
        });
        return;
      }
      var act = e.target.closest('[data-bdact]');
      if (act) { bdDoAction(act.dataset.bdact); return; }
      var ed = e.target.closest('[data-bded]');
      if (ed) { bdStartEdit(ed, ed.dataset.bded); return; }
      var sel = e.target.closest('[data-bdsel]');
      if (sel) { bdOpenSel(sel, sel.dataset.bdsel); return; }
      if (e.target.closest('#bdAddGuest')) { toast('Mở popup Thêm khách'); return; }
      if (e.target.closest('#bdCards')) { toast('Mở Danh sách thẻ từ'); return; }
      var gb = e.target.closest('[data-bdg]');
      if (gb) toast({ detail: 'Mở Chi tiết khách', edit: 'Mở popup Sửa thông tin khách', more: 'Mở thao tác khác với khách' }[gb.dataset.bdg]);
    });
    $('bdColR').addEventListener('keydown', function (e) {
      if (e.key !== 'Enter' && e.key !== ' ') return;
      var ed = e.target.closest('[data-bded]'), sel = e.target.closest('[data-bdsel]');
      if (ed) { e.preventDefault(); bdStartEdit(ed, ed.dataset.bded); }
      else if (sel) { e.preventDefault(); bdOpenSel(sel, sel.dataset.bdsel); }
    });

    /* Header */
    $('bdName').addEventListener('click', function () { bdStartEdit(this, 'gname'); });
    $('bdName').addEventListener('keydown', function (e) {
      if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); bdStartEdit(this, 'gname'); }
    });
    $('bdCode').addEventListener('click', function () { bdCopy(this, BD.bk.code || BD.bk.id); });
    $('bdMeta').addEventListener('click', function (e) {
      var o = e.target.closest('#bdOta'); if (o) bdCopy(o, BD.bk.ota);
    });
    document.addEventListener('click', function (e) {
      var s = e.target.closest('#bdStatus, #bdStatus2');
      if (!s) return;
      if (!bdCanEditStatus()) { toast('Chỉ được đổi trạng thái đặt khi phòng đang ở trạng thái Đã gán', true); return; }
      e.stopPropagation();
      bdMenuOpen(s, [
        { k: 'st:confirmed', t: 'Đã xác nhận' },
        { k: 'st:tentative', t: 'Tạm thời' },
        { k: 'st:cancelled', t: 'Huỷ' }
      ]);
    }, true);

    $('bdMore').addEventListener('click', function (e) {
      e.stopPropagation();
      bdMenuOpen(this, [
        { k: 'ui:routing', t: 'Routing chia hóa đơn' },
        { k: 'ui:todo', t: 'Danh sách việc cần làm' },
        '-',
        { k: 'ui:print', t: 'In hóa đơn' },
        { k: 'ui:mail', t: 'Gửi email' }
      ]);
    });
    $('bdMail').addEventListener('click', function () { toast('Mở popup Gửi mail'); });
    $('bdPrint').addEventListener('click', function () { toast('Chuyển sang luồng In hóa đơn'); });
    $('bdTodo').addEventListener('click', function () { toast('Mở popup Việc cần làm'); });
    $('bdCfg').addEventListener('click', function () { bdCloseDrawer(); gotoConfig('card'); });
    $('bdClose').addEventListener('click', bdCloseDrawer);
    $('bdBackdrop').addEventListener('click', bdCloseDrawer);

    /* Ghi lại trạng thái các lớp con TRƯỚC khi handler của chúng chạy —
       nếu không, chúng tự đóng trước và Esc lọt xuống đóng luôn drawer. */
    var bdEscBusy = false;
    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape') return;
      var vs = $('qbVsMenu'), dtp = $('qbDtp');
      bdEscBusy = ($('bdMenu') && $('bdMenu').classList.contains('is-open')) ||
                  (vs && vs.classList.contains('is-open')) ||
                  (dtp && dtp.classList.contains('is-open')) ||
                  !!(e.target && e.target.classList && e.target.classList.contains('bd-edin'));
    }, true);

    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape' || !bdIsOpen()) return;
      if (bdEscBusy) { bdEscBusy = false; return; }                       // nhường cho lớp con
      if ($('tlGenDlg').classList.contains('is-open')) return;            // đang mở dialog thì nhường
      bdCloseDrawer();
    });

    /* Menu dùng chung */
    $('bdMenu').addEventListener('click', function (e) {
      var b = e.target.closest('[data-bdact]'); if (!b) return;
      var k = b.dataset.bdact;
      bdMenuClose();
      if (k.indexOf('st:') === 0) {
        BD.bk.bkStatus = k.slice(3);
        BD.bk.confirmed = BD.bk.bkStatus === 'confirmed';
        bdRender(); toast('Đã cập nhật trạng thái đặt phòng');
      } else if (k.indexOf('ui:') === 0) {
        toast('Mở ' + b.textContent.trim());
      } else bdDoAction(k);
    });
    document.addEventListener('click', function (e) {
      if (!e.target.closest('#bdMenu') && !e.target.closest('[aria-haspopup]')) bdMenuClose();
    }, true);
    document.addEventListener('keydown', function (e) { if (e.key === 'Escape') bdMenuClose(); });
  }

  /* Dropdown chọn cho các trường Nguồn / Công ty / Thị trường / NV / Mã TA */
  var BD_SEL_SRC = {
    src: { t: 'Nguồn', items: ['Trực tiếp', 'Booking.com', 'Agoda.com', 'Traveloka', 'Expedia', 'Online'], warn: true },
    company: { t: 'Công ty', items: ['Công ty CP Du lịch Sen Vàng', 'ezCloud Global JSC', 'Vietravel', 'Saigontourist'], warn: true },
    market: { t: 'Thị trường', items: ['Nội địa', 'Châu Á', 'Châu Âu', 'Quốc tế'], warn: true },
    staff: { t: 'NV kinh doanh', items: ['Nguyễn Văn Thảo Nguyên', 'Phạm Thu Trang', 'Nguyễn Trần Huyền Trang'] },
    ta: { t: 'Mã TA', items: ['#TA001', '#TA002', '#TA003'] }
  };
  function bdOpenSel(anchor, key) {
    var d = BD_SEL_SRC[key]; if (!d) return;
    bdMenuOpen(anchor, d.items.map(function (v) { return { k: 'set:' + key + ':' + v, t: v }; }));
    var m = $('bdMenu');
    m.onclick = function (e) {
      var b = e.target.closest('[data-bdact]'); if (!b) return;
      e.stopPropagation();
      var parts = b.dataset.bdact.split(':');
      if (parts[0] !== 'set') return;
      var v = parts.slice(2).join(':');
      bdMenuClose(); m.onclick = null;
      var apply = function () {
        BD.bk[key] = v; bdRender();
        toast('Đã cập nhật ' + d.t);
      };
      if (d.warn) bdWarnRate(d.t, apply); else apply();
    };
  }


  /* =====================================================================
     22. SỬA PHÒNG — Thiết lập (F1) / Điều chỉnh (F3) / Xoá (F2)
     SRS "Sửa phòng" v2.0 + Figma 42502:133780.
     Dùng lại: engine select Vuexy (vsDef/vsVal/vsSet), datetime-picker
     (dtpOpen) của Đặt phòng nhanh, backdrop + toast của Timeline.
     ===================================================================== */

  var RP_REASONS = [
    'Sửa chữa điện',
    'Sửa chữa điều hoà',
    'Sửa chữa hệ thống nước',
    'Thay thế nội thất',
    'Vệ sinh tổng thể',
    'Sơn sửa tường',
    'Khác'
  ];
  var RP_KIND_LB = { OOS: 'Tạm ngừng phục vụ (OOS)', OOO: 'Phòng hỏng/bảo trì (OOO)' };
  var rpLastKind = 'OOS';           // "Mặc định: loại sửa của phiên làm việc gần nhất"
  var rpSeq = 0;
  var rp = { open: false, mode: 'new', bk: null, room: null, kind: 'OOS', from: null, to: null, sell: false };
  var rpDelBk = null;

  function rpFmt(d) { return d ? dmy(d) + ' ' + hm(d) : '—'; }

  /* --- Đọc/ghi lý do: note lưu "<lý do>" hoặc "Khác: <lý do khác>" --- */
  function rpSplitReason(note) {
    var s = (note || '').trim();
    for (var i = 0; i < RP_REASONS.length - 1; i++) if (s === RP_REASONS[i]) return { r: s, o: '' };
    if (s.indexOf('Khác: ') === 0) return { r: 'Khác', o: s.slice(6) };
    return s ? { r: 'Khác', o: s } : { r: '', o: '' };
  }
  function rpJoinReason(r, o) { return r === 'Khác' ? 'Khác: ' + o : r; }

  /* --- Xung đột: có booking (không phải bản ghi sửa đang mở) trùng khoảng --- */
  function rpConflict(roomId, from, to, selfId) {
    for (var i = 0; i < BOOKINGS.length; i++) {
      var b = BOOKINGS[i];
      if (b.room !== roomId || b.id === selfId) continue;
      if (b.in < to && from < b.out) return b;
    }
    return null;
  }

  function rpSyncKind() {
    document.querySelectorAll('#rpDlg [data-rpkind]').forEach(function (x) {
      x.setAttribute('aria-pressed', String(x.dataset.rpkind === rp.kind));
    });
    $('rpSellWrap').hidden = rp.kind !== 'OOS';          // SRS 3.1.4-6: chỉ hiện với OOS
  }
  function rpSyncDates() {
    $('rpFromT').textContent = rpFmt(rp.from);
    $('rpToT').textContent = rpFmt(rp.to);
  }
  function rpSyncOther() {
    $('rpOtherWrap').hidden = vsVal('rpReason') !== 'Khác';
  }
  function rpClearErrors() {
    $('rpDlg').classList.remove('is-dateerr');
    $('rpDateMsg').hidden = true;
    $('rpReasonMsg').hidden = true;
    $('rpOtherMsg').hidden = true;
    $('rpOther').classList.remove('is-error');
  }

  function rpShow(roomId) {
    var r = roomById(roomId);
    $('rpTitle').textContent = 'Thiết lập sửa phòng ' + (r ? r.type + ' - ' + r.id : roomId);
    vsDef('rpReason', RP_REASONS.map(function (x) { return { v: x, t: x }; }),
      rp.reason || '', 'Chọn');
    if (QB_SEL.rpReason) QB_SEL.rpReason.onChange = function () { rpSyncOther(); rpClearErrors(); };
    vsSet('rpReason', rp.reason || '', true);
    $('rpOther').value = rp.other || '';
    $('rpSell').checked = !!rp.sell;
    rpSyncKind(); rpSyncDates(); rpSyncOther(); rpClearErrors();
    $('rpDel').hidden = rp.mode !== 'edit';
    rp.open = true;
    openDlg($('rpDlg'));
    setTimeout(function () { $('rpFrom').focus(); }, 0);
  }

  /* F1 — Thiết lập sửa phòng (kéo chọn khoảng ngày trên ô trống) */
  function rpOpenNew(roomId, c0, c1) {
    closeTip(); closeMenu();
    var a, z, from, to;
    if (c0 === null || c0 === undefined || isNaN(c0)) {
      from = new Date(HOTEL_NOW.getTime());
      to = addDays(startOfDay(from), 1); to.setHours(from.getHours(), from.getMinutes(), 0, 0);
    } else {
      a = Math.min(c0, c1); z = Math.max(c0, c1);
      from = colDate(a); from = new Date(from.getFullYear(), from.getMonth(), from.getDate(), 12, 0, 0, 0);
      to = colDate(z); to = new Date(to.getFullYear(), to.getMonth(), to.getDate(), 12, 0, 0, 0);
      to = addDays(to, 1);
    }
    rp = { open: false, mode: 'new', bk: null, room: roomId, kind: rpLastKind,
           from: from, to: to, sell: false, reason: '', other: '' };
    rpShow(roomId);
  }

  /* F3 — Điều chỉnh sửa phòng (mở từ bản ghi sửa phòng đang có) */
  function rpOpenEdit(b) {
    closeTip(); closeMenu();
    var sp = rpSplitReason(b.note);
    rp = { open: false, mode: 'edit', bk: b, room: b.room, kind: b.repair === 'OOO' ? 'OOO' : 'OOS',
           from: new Date(b.in.getTime()), to: new Date(b.out.getTime()),
           sell: !!b.sellable, reason: sp.r, other: sp.o };
    rpShow(b.room);
  }

  function rpCollect() {
    rp.sell = $('rpSell').checked;
    rp.reason = vsVal('rpReason');
    rp.other = $('rpOther').value.trim();
  }

  function rpValidate() {
    rpClearErrors();
    var ok = true;
    if (!rp.from || !rp.to || rp.to.getTime() <= rp.from.getTime()) {
      $('rpDlg').classList.add('is-dateerr'); $('rpDateMsg').hidden = false; ok = false;
    }
    if (!rp.reason) { $('rpReasonMsg').hidden = false; ok = false; }
    else if (rp.reason === 'Khác' && !rp.other) {
      $('rpOtherMsg').hidden = false; $('rpOther').classList.add('is-error'); ok = false;
    }
    return ok;
  }

  function rpConflictDlg(b) {
    openGenDlg('Xung đột booking',
      '<p style="margin:0">Phòng ' + esc(rp.room) + ' đã có đặt phòng ' +
      (b.code ? esc(b.code) + ' ' : '') + '(' + dmy(b.in) + ' - ' + dmy(b.out) +
      ') trong khoảng thời gian sửa. Vui lòng chọn lại thời gian!</p>',
      '<button class="tl-btn-ghost" type="button" data-tl-close>Đóng</button>');
  }

  function rpSave() {
    rpCollect();
    if (!rpValidate()) return;
    var cf = rpConflict(rp.room, rp.from, rp.to, rp.bk ? rp.bk.id : null);
    if (cf) { rpConflictDlg(cf); return; }
    var note = rpJoinReason(rp.reason, rp.other);
    rpLastKind = rp.kind;
    if (rp.mode === 'edit' && rp.bk) {
      var b = rp.bk;
      b.repair = rp.kind; b.sellable = rp.kind === 'OOS' ? rp.sell : false;
      b.in = rp.from; b.out = rp.to; b.note = note;
      closeDlgs(); rp.open = false; render();
      toast('Đã điều chỉnh sửa phòng!');
      return;
    }
    BOOKINGS.push({
      id: 'RP' + (++rpSeq), room: rp.room, st: 'repair', repair: rp.kind,
      sellable: rp.kind === 'OOS' ? rp.sell : false,
      name: 'Sửa phòng', code: '', in: rp.from, out: rp.to,
      group: null, pay: 'none', total: 0, paid: 0, adults: 0, children: 0, infants: 0,
      note: note, inote: '', manual: true
    });
    closeDlgs(); rp.open = false; render();
    toast('Đã thiết lập sửa phòng!');
  }

  /* F2 — Xoá / huỷ thiết lập sửa phòng */
  function rpOpenDelete(b) {
    if (!b) return;
    closeTip(); closeMenu();
    rpDelBk = b;
    var r = roomById(b.room);
    var sp = rpSplitReason(b.note);
    $('rpDelTitle').textContent = 'Xoá sửa phòng ' + (r ? r.type + ' - ' + r.id : b.room);
    var rows = [
      ['Loại sửa:', RP_KIND_LB[b.repair === 'OOO' ? 'OOO' : 'OOS']],
      ['Ngày bắt đầu:', rpFmt(b.in)],
      ['Ngày kết thúc:', rpFmt(b.out)],
      ['Lý do sửa:', sp.r || '—']
    ];
    if (sp.r === 'Khác') rows.push(['Lý do khác:', sp.o || '—']);
    $('rpDelInfo').innerHTML = rows.map(function (x) {
      return '<dt>' + esc(x[0]) + '</dt><dd>' + esc(x[1]) + '</dd>';
    }).join('');
    $('rpDelReason').value = '';
    $('rpDelReason').classList.remove('is-error');
    $('rpDelMsg').hidden = true;
    openDlg($('rpDelDlg'));
    setTimeout(function () { $('rpDelReason').focus(); }, 0);
  }
  function rpDelConfirm() {
    var v = $('rpDelReason').value.trim();
    if (v.length < 5) {
      $('rpDelMsg').hidden = false; $('rpDelReason').classList.add('is-error');
      $('rpDelReason').focus(); return;
    }
    var b = rpDelBk; rpDelBk = null;
    if (b) {
      var i = BOOKINGS.indexOf(b);
      if (i >= 0) BOOKINGS.splice(i, 1);
      var r = roomById(b.room);
      if (r) r.dirty = true;                 // SRS F2-B5: phòng trở về Vacant dirty
    }
    closeDlgs(); render();
    toast('Đã xoá sửa phòng thành công!');
  }

  function bindRp() {
    document.querySelectorAll('#rpDlg [data-rpkind]').forEach(function (btn) {
      btn.addEventListener('click', function () {
        rp.kind = btn.dataset.rpkind; rpSyncKind(); rpClearErrors();
      });
    });
    $('rpFrom').addEventListener('click', function () {
      dtpOpen($('rpFrom'), rp.from, true, function (d) {
        rp.from = d;
        if (rp.to && rp.to.getTime() <= rp.from.getTime()) {
          rp.to = addDays(startOfDay(rp.from), 1);
          rp.to.setHours(rp.from.getHours(), rp.from.getMinutes(), 0, 0);
        }
        rpSyncDates(); rpClearErrors();
      });
    });
    $('rpTo').addEventListener('click', function () {
      dtpOpen($('rpTo'), rp.to, true, function (d) { rp.to = d; rpSyncDates(); rpClearErrors(); });
    });
    $('rpSave').addEventListener('click', rpSave);
    $('rpDel').addEventListener('click', function () {
      var b = rp.bk; closeDlgs(); rp.open = false;
      rpOpenDelete(b);
    });
    $('rpCancel').addEventListener('click', function () { closeDlgs(); rp.open = false; });
    $('rpX').addEventListener('click', function () { closeDlgs(); rp.open = false; });
    $('rpDelCancel').addEventListener('click', function () { rpDelBk = null; closeDlgs(); });
    $('rpDelX').addEventListener('click', function () { rpDelBk = null; closeDlgs(); });
    $('rpDelOk').addEventListener('click', rpDelConfirm);
    $('rpOther').addEventListener('input', function () {
      this.classList.remove('is-error'); $('rpOtherMsg').hidden = true;
    });
    $('rpDelReason').addEventListener('input', function () {
      this.classList.remove('is-error'); $('rpDelMsg').hidden = true;
    });
  }


  /* =====================================================================
     23. ĐỔI NGÀY Ở (R01.07) — SRS R01.07 + Figma 50775:132643
     Mở từ doAction case 'changedate' (popup thông tin booking / menu chuột phải).
     Dùng lại: dtpOpen (datetime-picker), vsDef/vsVal/vsSet (select Vuexy),
     openGenDlg/confirmDlg (MSG_xx), toast, render() của Timeline.
     ===================================================================== */

  var CD_PER = [10, 25, 50, 100];
  var cd = {
    open: false, rows: [], collapsed: {}, page: 1, per: 25,
    bulkMode: 'all', dtRow: null, dtTab: 'down', srcBk: null
  };

  var CDI = {
    bed: '<svg class="bed" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 18v-8a2 2 0 012-2h9a4 4 0 014 4v6M3 14h18M3 18h18M7 11h2"/></svg>',
    caret: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>',
    cal: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg>',
    edit: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 20h9"/><path d="M16.5 3.5a2.1 2.1 0 013 3L7 19l-4 1 1-4z"/></svg>',
    warn: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M9 9l6 6M15 9l-6 6"/></svg>',
    arrow: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 12h16M15 7l5 5-5 5"/></svg>',
    up: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 15l6-6 6 6"/></svg>',
    dn: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>',
    ck: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>'
  };

  function cdFmt(d) { return dmy(d) + ' ' + hm(d); }
  function cdMoney(n) { return money(Math.round(n)); }
  function cdSigned(n) {
    if (!n) return '0';
    return (n > 0 ? '+' : '-') + money(Math.abs(Math.round(n)));
  }
  function cdRateList(typeId) {
    var l = (typeof QB_RATES !== 'undefined' && QB_RATES[typeId]) ? QB_RATES[typeId] : [];
    return l.map(function (x) { return { v: x.id, t: x.name, price: x.price }; });
  }

  /* ---- Dựng danh sách phòng: booking đoàn → cả đoàn, booking lẻ → 1 dòng ---- */
  function cdBuildRows(bk) {
    var list = bk.group
      ? BOOKINGS.filter(function (b) { return b.group && b.group.gid === bk.group.gid; })
      : [bk];
    return list.map(function (b) {
      var r = roomById(b.room) || {};
      var g = (b.guests || []).filter(function (x) { return x.lead; })[0] || (b.guests || [])[0] || { n: b.name, cc: 'VN' };
      var n0 = nightsOf(b);
      var rl = cdRateList(r.type);
      return {
        id: b.id, bk: b, room: b.room, type: r.type || '', st: b.st,
        name: g.n || b.name, cc: g.cc || 'VN',
        adults: b.adults || 0, children: b.children || 0, infants: b.infants || 0,
        in0: new Date(b.in.getTime()), out0: new Date(b.out.getTime()), n0: n0,
        in: new Date(b.in.getTime()), out: new Date(b.out.getTime()), n: n0,
        past: false, pastReason: '',
        price0: b.total || 0, price: b.total || 0,
        unit: n0 > 0 ? Math.round((b.total || 0) / n0) : 0,
        rate: rl[0] ? rl[0].v : '', rates: rl,
        sel: false, conflict: null, edited: false
      };
    });
  }

  function cdDirty() {
    return cd.rows.some(function (r) {
      return r.edited || r.in.getTime() !== r.in0.getTime() || r.out.getTime() !== r.out0.getTime();
    });
  }
  function cdChanged(r) {
    return r.in.getTime() !== r.in0.getTime() || r.out.getTime() !== r.out0.getTime() || r.edited;
  }

  /* ---- Tính lại giá + kiểm tra xung đột ---- */
  function cdRecalc(r) {
    r.n = Math.max(0, dayDiff(r.out, r.in));
    if (!r.edited) r.price = (r.n === r.n0 && r.in.getTime() === r.in0.getTime()) ? r.price0 : r.unit * r.n;
    var ids = {};
    cd.rows.forEach(function (x) { ids[x.id] = 1; });
    r.conflict = null;
    for (var i = 0; i < BOOKINGS.length; i++) {
      var b = BOOKINGS[i];
      if (ids[b.id] || b.room !== r.room) continue;
      if (b.in < r.out && r.in < b.out) { r.conflict = b; break; }
    }
  }
  function cdRecalcAll() { cd.rows.forEach(cdRecalc); }

  /* ---- Mở popup ---- */
  function cdOpen(bkId) {
    var bk = bkById(bkId); if (!bk) return;
    if (bk.st !== 'booked' && bk.st !== 'inhouse') {
      toast('Chỉ đổi ngày ở được với đặt phòng ở trạng thái Đã đặt hoặc Có khách', true);
      return;
    }
    closeTip(); closeMenu();
    cd.srcBk = bk;
    cd.rows = cdBuildRows(bk);
    cd.collapsed = {}; cd.page = 1; cd.per = 25;
    cdRecalcAll();
    vsDef('cdPerPage', CD_PER.map(function (n) { return { v: String(n), t: String(n) }; }), '25');
    if (QB_SEL.cdPerPage) QB_SEL.cdPerPage.onChange = function (v) { cd.per = parseInt(v, 10) || 25; cd.page = 1; cdRender(); };
    cd.open = true;
    $('cdBd').classList.add('is-open');
    $('cdDlg').classList.add('is-open');
    cdRender();
    setTimeout(function () { $('cdX').focus(); }, 0);
  }
  function cdClose() {
    cd.open = false;
    $('cdDlg').classList.remove('is-open');
    $('cdBd').classList.remove('is-open');
    cdSubClose();
  }
  function cdTryClose() {                       // MSG_02
    if (!cdDirty()) { cdClose(); return; }
    confirmDlg('Huỷ bỏ Đổi ngày ở', 'Tất cả dữ liệu bạn đã nhập sẽ bị xóa', 'Xác nhận', cdClose);
  }

  /* ---- Nhóm theo loại phòng ---- */
  function cdGroups() {
    var order = [], map = {};
    cd.rows.forEach(function (r) {
      if (!map[r.type]) { map[r.type] = []; order.push(r.type); }
      map[r.type].push(r);
    });
    return order.map(function (t) { return { type: t, rows: map[t] }; });
  }
  function cdVisibleRows() {
    var out = [];
    cdGroups().forEach(function (g) {
      if (!cd.collapsed[g.type]) out = out.concat(g.rows);
    });
    return out;
  }

  /* ---- Render ---- */
  function cdRender() {
    if (!cd.open) return;
    cdRecalcAll();
    $('cdTitle').textContent = 'Đổi ngày ở (' + cd.rows.length + ')';
    $('cdReset').disabled = !cdDirty();
    $('cdBulkAll').textContent = cdDirty() ? 'Đổi đồng loạt' : 'Đổi ngày ở đồng loạt';

    var sel = cd.rows.filter(function (r) { return r.sel; });
    $('cdSelBar').hidden = sel.length === 0;
    $('cdSelCount').textContent = 'Đang chọn (' + sel.length + '):';

    var conf = cd.rows.filter(function (r) { return r.conflict; });
    $('cdErrBar').hidden = conf.length === 0;
    if (conf.length) $('cdErrText').textContent = conf.length + ' phòng đang xung đột ngày ở. Vui lòng xử lý hoặc chọn ngày ở khác!';

    var all = $('cdCkAll');
    all.checked = cd.rows.length > 0 && sel.length === cd.rows.length;
    all.indeterminate = sel.length > 0 && sel.length < cd.rows.length;

    /* phân trang trên danh sách phòng (không tính hàng nhóm) */
    var total = cd.rows.length;
    var pages = Math.max(1, Math.ceil(total / cd.per));
    if (cd.page > pages) cd.page = pages;
    var from = (cd.page - 1) * cd.per, to = Math.min(total, from + cd.per);
    var pageIds = {};
    cd.rows.slice(from, to).forEach(function (r) { pageIds[r.id] = 1; });

    var h = [];
    cdGroups().forEach(function (g) {
      var rows = g.rows.filter(function (r) { return pageIds[r.id]; });
      if (!rows.length) return;
      var gSel = rows.every(function (r) { return r.sel; });
      h.push('<tr class="cd-grp"><td class="ck">' + cdCk('grp', g.type, gSel) + '</td>' +
        '<td colspan="11"><button class="cd-grpname' + (cd.collapsed[g.type] ? ' is-closed' : '') +
        '" type="button" data-cdgrp="' + esc(g.type) + '" aria-expanded="' + !cd.collapsed[g.type] + '">' +
        CDI.caret + esc(typeName(g.type)) + ' (' + g.rows.length + ')</button></td></tr>');
      if (cd.collapsed[g.type]) return;
      rows.forEach(function (r) { h.push(cdRowHTML(r)); });
    });
    $('cdBody').innerHTML = h.join('') || '<tr><td colspan="12" style="padding:24px;text-align:center;color:var(--theme-text-secondary)">Không có phòng nào.</td></tr>';

    var oldSum = 0, newSum = 0;
    cd.rows.forEach(function (r) { oldSum += r.price0; newSum += r.price; });
    $('cdSumOld').textContent = cdMoney(oldSum);
    $('cdSumNew').textContent = cdMoney(newSum);
    var d = newSum - oldSum;
    var dEl = $('cdSumDiff');
    dEl.textContent = '(' + cdSigned(d) + ')';
    dEl.className = 'd' + (d > 0 ? ' cd-up' : (d < 0 ? ' cd-down' : ''));

    $('cdRange').textContent = 'Hiển thị ' + (total ? from + 1 : 0) + ' - ' + to + ' trên ' + total + ' bản ghi';
    cdRenderPager(pages);
  }

  function cdCk(kind, key, on, disabled) {
    return '<label class="cd-ck"><input type="checkbox" data-cdck="' + kind + '" data-key="' + esc(key) + '"' +
      (on ? ' checked' : '') + (disabled ? ' disabled' : '') + '><span class="box">' + CDI.ck + '</span></label>';
  }

  function cdRowHTML(r) {
    var nd = r.n - r.n0;
    var diff = r.price - r.price0;
    var changed = cdChanged(r);
    var td = [];
    td.push('<td class="ck">' + cdCk('row', r.id, r.sel) + '</td>');
    td.push('<td><span class="cd-room">' + CDI.bed + '<span>' + esc(typeName(r.type)) + ' - ' + esc(r.room) + '</span></span></td>');
    td.push('<td><span class="cd-guestcell">' + flagSVG(r.cc) + '<span>' + esc(r.name) + '</span></span></td>');
    td.push('<td>' + r.adults + '/' + r.children + '/' + r.infants + '</td>');
    // Ngày đến — phòng đã check-in không cho sửa
    var lockIn = r.st === 'inhouse';
    td.push('<td><button class="cd-cell-dt" type="button" data-cddate="in" data-key="' + esc(r.id) + '"' +
      (lockIn ? ' disabled aria-disabled="true" title="Phòng đã nhận — không đổi được ngày đến"' : '') + '>' +
      '<span>' + cdFmt(r.in) + '</span>' + CDI.cal + '</button>' +
      (r.in.getTime() !== r.in0.getTime() ? '<span class="cd-orig">' + cdFmt(r.in0) + '</span>' : '') + '</td>');
    td.push('<td><button class="cd-cell-dt" type="button" data-cddate="out" data-key="' + esc(r.id) + '">' +
      '<span>' + cdFmt(r.out) + '</span>' + CDI.cal + '</button>' +
      (r.out.getTime() !== r.out0.getTime() ? '<span class="cd-orig">' + cdFmt(r.out0) + '</span>' : '') + '</td>');
    td.push('<td><div class="cd-num"><input type="text" inputmode="numeric" value="' + r.n +
      '" data-cdn="' + esc(r.id) + '" aria-label="Số đêm phòng ' + esc(r.room) + '">' +
      '<span class="sp"><button type="button" data-cdstep="1" data-key="' + esc(r.id) + '" aria-label="Tăng số đêm">' + CDI.up + '</button>' +
      '<button type="button" data-cdstep="-1" data-key="' + esc(r.id) + '" aria-label="Giảm số đêm">' + CDI.dn + '</button></span></div>' +
      (nd ? '<span class="cd-nd' + (nd > 0 ? ' cd-up' : '') + '">(' + (nd > 0 ? '+' : '') + nd + ' đêm)</span>' : '') + '</td>');

    if (r.conflict) {
      td.push('<td colspan="5"><div class="cd-conf">' + CDI.warn +
        '<span class="tx">' + (r.conflict.st === 'repair'
          ? 'Xung đột với lịch sửa phòng (' + dmy(r.conflict.in) + ' - ' + dmy(r.conflict.out) + ')'
          : 'Xung đột với booking ' + esc(r.conflict.code || r.conflict.id)) + '</span>' +
        '<button class="go" type="button" data-cdconf="' + esc(r.id) + '">Chi tiết' + CDI.arrow + '</button></div></td>');
      return '<tr class="cd-row is-conf' + (r.sel ? ' is-sel' : '') + '" data-cdrow="' + esc(r.id) + '">' + td.join('') + '</tr>';
    }

    td.push('<td class="pa">' + (r.st === 'inhouse'
      ? cdCk('past', r.id, r.past)
      : '<span class="cd-mute">—</span>') + '</td>');
    td.push('<td class="mo">' + cdMoney(r.price0) + '</td>');
    td.push('<td class="mo">' + (changed ? cdMoney(r.price) : '<span class="cd-mute">--</span>') + '</td>');
    td.push('<td class="mo">' + (changed
      ? '<span class="' + (diff > 0 ? 'cd-up' : (diff < 0 ? 'cd-down' : 'cd-mute')) + '">' + cdSigned(diff) + '</span>'
      : '<span class="cd-mute">--</span>') + '</td>');
    td.push('<td class="ed">' + (changed
      ? '<button class="cd-edit" type="button" data-cdedit="' + esc(r.id) + '" aria-label="Chi tiết thay đổi phòng ' + esc(r.room) + '">' + CDI.edit + '</button>'
      : '') + '</td>');
    return '<tr class="cd-row' + (r.sel ? ' is-sel' : '') + '" data-cdrow="' + esc(r.id) + '">' + td.join('') + '</tr>';
  }

  function cdRenderPager(pages) {
    var h = [], i;
    var nav = function (act, label, dis, icon) {
      return '<button type="button" data-cdpg="' + act + '"' + (dis ? ' disabled' : '') +
        ' aria-label="' + label + '">' + icon + '</button>';
    };
    var L = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 6l-6 6 6 6"/></svg>';
    var LL = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 6l-6 6 6 6M9 6l-6 6 6 6"/></svg>';
    var R = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6-6 6"/></svg>';
    var RR = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 6l6 6-6 6M15 6l6 6-6 6"/></svg>';
    h.push(nav('first', 'Trang đầu', cd.page === 1, LL));
    h.push(nav('prev', 'Trang trước', cd.page === 1, L));
    for (i = 1; i <= pages; i++) {
      if (pages > 7 && i > 2 && i < pages - 1 && Math.abs(i - cd.page) > 1) {
        if (h[h.length - 1] !== '<button type="button" disabled>…</button>') h.push('<button type="button" disabled>…</button>');
        continue;
      }
      h.push('<button type="button" data-cdpage="' + i + '"' + (i === cd.page ? ' aria-current="true"' : '') + '>' + i + '</button>');
    }
    h.push(nav('next', 'Trang sau', cd.page === pages, R));
    h.push(nav('last', 'Trang cuối', cd.page === pages, RR));
    $('cdPager').innerHTML = h.join('');
  }

  /* ---- Thao tác ngày / số đêm ---- */
  function cdRow(id) { for (var i = 0; i < cd.rows.length; i++) if (cd.rows[i].id === id) return cd.rows[i]; return null; }

  function cdSetIn(r, d) {
    if (startOfDay(d) < startOfDay(HOTEL_NOW)) { toast('Không được chọn thời gian trước ngày hiện tại', true); return; }
    r.in = d;
    if (r.out.getTime() <= r.in.getTime()) {
      r.out = addDays(startOfDay(r.in), 1);
      r.out.setHours(r.out0.getHours(), r.out0.getMinutes(), 0, 0);
    }
    r.edited = false; cdRender();
  }
  function cdSetOut(r, d) {
    if (startOfDay(d) < startOfDay(HOTEL_NOW)) { toast('Không được chọn thời gian trước ngày hiện tại', true); return; }
    if (d.getTime() <= r.in.getTime()) { toast('Ngày đi phải sau ngày đến ít nhất 1 đêm', true); return; }
    r.out = d; r.edited = false; cdRender();
  }
  function cdSetNights(r, n) {
    n = Math.max(0, Math.min(365, n));
    if (n < 1) n = 1;
    r.out = addDays(startOfDay(r.in), n);
    r.out.setHours(r.out0.getHours(), r.out0.getMinutes(), 0, 0);
    r.edited = false; cdRender();
  }

  /* ---- Popup con ---- */
  function cdSubOpen(el) { $('cdSubBd').classList.add('is-open'); el.classList.add('is-open'); }
  function cdSubClose() {
    $('cdSubBd').classList.remove('is-open');
    $('cdBulkDlg').classList.remove('is-open');
    $('cdDtDlg').classList.remove('is-open');
  }
  function cdSubIsOpen() {
    return $('cdBulkDlg').classList.contains('is-open') || $('cdDtDlg').classList.contains('is-open');
  }

  /* ---- Đổi đồng loạt / Đổi ngày ở đã chọn ---- */
  var cdBulk = { in: null, out: null, n: 1, n0: 1 };
  function cdBulkOpen(mode) {
    var targets = mode === 'sel' ? cd.rows.filter(function (r) { return r.sel; }) : cd.rows;
    if (!targets.length) return;
    cd.bulkMode = mode;
    var base = targets[0];
    cdBulk.in = new Date(base.in.getTime());
    cdBulk.out = new Date(base.out.getTime());
    cdBulk.n = Math.max(1, dayDiff(cdBulk.out, cdBulk.in));
    cdBulk.n0 = base.n0;
    $('cdBulkTitle').textContent = mode === 'sel'
      ? 'Đổi ngày ở đã chọn (' + targets.length + ')'
      : 'Đổi đồng loạt';
    cdBulkSync();
    cdSubOpen($('cdBulkDlg'));
    setTimeout(function () { $('cdBIn').focus(); }, 0);
  }
  function cdBulkSync() {
    $('cdBInT').textContent = cdFmt(cdBulk.in);
    $('cdBOutT').textContent = cdFmt(cdBulk.out);
    $('cdBN').value = String(cdBulk.n);
    var d = cdBulk.n - cdBulk.n0;
    var e = $('cdBDelta');
    e.textContent = d ? (d > 0 ? '+' : '') + d + ' đêm' : '';
    e.className = 'cd-delta' + (d > 0 ? ' up' : '');
  }
  function cdBulkApply() {
    var targets = cd.bulkMode === 'sel' ? cd.rows.filter(function (r) { return r.sel; }) : cd.rows;
    var n = Math.max(1, dayDiff(cdBulk.out, cdBulk.in));
    targets.forEach(function (r) {
      if (r.st !== 'inhouse') r.in = new Date(cdBulk.in.getTime());
      r.out = addDays(startOfDay(r.in), n);
      r.out.setHours(cdBulk.out.getHours(), cdBulk.out.getMinutes(), 0, 0);
      r.edited = false;
    });
    cdSubClose(); cdRender();
    toast('Áp dụng đổi đồng loại thành công');   // MSG_06
  }

  /* ---- Chi tiết thay đổi ---- */
  function cdBuildItems(r) {
    var up = [], down = [], i, d;
    var extra = (r.adults || 0) + (r.children || 0);
    var svc = 'Phụ thu ' + (r.adults || 0) + 'NL ' + (r.children || 0) + 'TE';
    var pkg = (r.type || 'STD') + '-B (bookable - breakdown - daily breakfast | ' + (r.adults || 0) + ' adults)';
    if (r.n > r.n0) {                                     // thêm đêm → danh mục tăng
      for (i = r.n0; i < r.n; i++) {
        d = addDays(startOfDay(r.in), i);
        up.push({ d: d, name: 'Tiền phòng ngày ' + dmy(d), rate: r.rate, amt: r.unit, editable: true });
        up.push({ d: null, name: svc, rate: null, amt: 50000, editable: false });
        up.push({ d: null, name: pkg, rate: null, amt: 50000, editable: false });
      }
    } else if (r.n < r.n0) {                              // bớt đêm → danh mục giảm
      for (i = r.n; i < r.n0; i++) {
        d = addDays(startOfDay(r.in0), i);
        down.push({ d: d, name: 'Tiền phòng ngày ' + dmy(d), rate: r.rate, amt: r.unit, editable: true });
        down.push({ d: null, name: svc, rate: null, amt: 50000, editable: false });
        down.push({ d: null, name: pkg, rate: null, amt: 50000, editable: false });
      }
    }
    return { up: up, down: down };
  }

  function cdDetailOpen(id) {
    var r = cdRow(id); if (!r) return;
    cd.dtRow = r;
    if (!r.items) r.items = cdBuildItems(r);
    cd.dtTab = r.items.down.length ? 'down' : 'up';    // mặc định focus danh mục giảm
    $('cdDtTitle').textContent = 'Chi tiết thay đổi';
    $('cdDtGuest').innerHTML = flagSVG(r.cc) + '<span>' + esc(r.name) + '</span>';
    $('cdDtOld').textContent = cdFmt(r.in0) + ' - ' + cdFmt(r.out0) + ' (' + r.n0 + ' đêm)';
    $('cdDtNew').textContent = cdFmt(r.in) + ' - ' + cdFmt(r.out) + ' (' + r.n + ' đêm)';
    vsDef('cdDtRate', [{ v: '', t: 'Chọn chính sách giá' }].concat(r.rates.map(function (x) { return { v: x.v, t: x.t }; })), '', 'Chọn chính sách giá');
    if (QB_SEL.cdDtRate) QB_SEL.cdDtRate.onChange = function (v) {
      if (!v) return;
      cd.dtRow.items[cd.dtTab].forEach(function (it) { if (it.editable) it.rate = v; });
      vsSet('cdDtRate', '', true);
      cdDetailRender();
    };
    cdDetailRender();
    cdSubOpen($('cdDtDlg'));
  }

  function cdDetailRender() {
    var r = cd.dtRow; if (!r) return;
    document.querySelectorAll('#cdDtDlg [data-cdtab]').forEach(function (b) {
      b.setAttribute('aria-selected', String(b.dataset.cdtab === cd.dtTab));
    });
    var items = r.items[cd.dtTab];
    var sign = cd.dtTab === 'up' ? 'cd-up' : 'cd-down';
    var h = items.map(function (it, i) {
      var rateCell = it.editable
        ? '<select class="cd-ratesel" data-cdit="' + i + '">' + r.rates.map(function (x) {
            return '<option value="' + esc(x.v) + '"' + (x.v === it.rate ? ' selected' : '') + '>' + esc(x.t) + '</option>';
          }).join('') + '</select>'
        : '<span class="cd-mute">-</span>';
      var amtCell = it.editable
        ? '<input type="text" inputmode="numeric" class="' + sign + '" data-cdamt="' + i + '" value="' + money(it.amt) + '" aria-label="Thành tiền">'
        : '<span class="' + sign + '">' + money(it.amt) + '</span>';
      return '<tr><td class="tm">' + (it.d ? dmy(it.d) : '') + '</td>' +
        '<td class="ct">' + esc(it.name) + '</td>' +
        '<td class="rt">' + rateCell + '</td>' +
        '<td class="am">' + amtCell + '</td></tr>';
    }).join('');
    $('cdDtBody').innerHTML = h || '<tr><td colspan="4" style="padding:20px;text-align:center;color:var(--theme-text-secondary)">Không có danh mục nào.</td></tr>';
    $('cdDtPageInfo').textContent = items.length + ' trong ' + (r.items.up.length + r.items.down.length) + ' kết quả';

    var delta = cdDetailDelta(r);
    var np = r.price0 + delta;
    $('cdDtSum').innerHTML = cdMoney(r.price0) + ' <span class="cd-mute">→</span> <span class="' +
      (delta > 0 ? 'cd-up' : (delta < 0 ? 'cd-down' : '')) + '">' + cdMoney(np) + '</span>';
    var dd = $('cdDtDiff');
    dd.textContent = cdSigned(delta);
    dd.className = delta > 0 ? 'cd-up' : (delta < 0 ? 'cd-down' : 'cd-mute');
  }
  function cdDetailDelta(r) {
    var up = 0, down = 0;
    r.items.up.forEach(function (it) { up += it.amt; });
    r.items.down.forEach(function (it) { down += it.amt; });
    return up - down;
  }

  /* ---- Lưu ---- */
  function cdSave() {
    var conf = cd.rows.filter(function (r) { return r.conflict; });
    if (conf.length) {                                   // MSG_04
      openGenDlg('Không còn phòng vật lý trống!',
        '<p style="margin:0">Không tìm thấy phòng vật lý trống phù hợp trong khoảng thời gian đã chọn. Vui lòng chọn sang ngày khác</p>',
        '<button class="tl-btn-ghost" type="button" data-tl-close>Đóng</button>');
      return;
    }
    var bad = cd.rows.filter(function (r) { return r.out.getTime() <= r.in.getTime(); });
    if (bad.length) {                                    // MSG_03
      openGenDlg('Không còn phòng trống!',
        '<p style="margin:0">Không tìm thấy phòng phù hợp trong khoảng thời gian đã chọn. Vui lòng chọn sang ngày khác</p>',
        '<button class="tl-btn-ghost" type="button" data-tl-close>Đóng</button>');
      return;
    }
    var zero = cd.rows.filter(function (r) { return r.n !== r.n0 && r.price === r.price0; });
    cd.rows.forEach(function (r) {
      r.bk.in = new Date(r.in.getTime());
      r.bk.out = new Date(r.out.getTime());
      r.bk.total = r.price;
      r.bk.manual = true;
      normalizeSt(r.bk);
    });
    cdClose(); render();
    if (zero.length) toast('Số đêm đã thay đổi nhưng số tiền chênh lệch = 0. Vui lòng kiểm tra lại giá phòng!', true);
    toast('Đổi ngày ở thành công');                       // MSG_05
  }

  function cdResetDefault() {                            // MSG_01
    confirmDlg('Xác nhận cài lại mặc định',
      'Thao tác này khôi phục toàn bộ phòng về trạng thái chưa gán phòng. Bạn có muốn tiếp tục không?',
      'Xác nhận', function () {
        cd.rows.forEach(function (r) {
          r.in = new Date(r.in0.getTime());
          r.out = new Date(r.out0.getTime());
          r.price = r.price0; r.edited = false; r.items = null;
          r.past = false; r.pastReason = '';
        });
        cdRender();
      });
  }

  /* ---- Bind ---- */
  function bindCd() {
    $('cdX').addEventListener('click', cdTryClose);
    $('cdCancel').addEventListener('click', cdTryClose);
    $('cdBd').addEventListener('click', cdTryClose);
    $('cdSave').addEventListener('click', cdSave);
    $('cdReset').addEventListener('click', cdResetDefault);
    $('cdBulkAll').addEventListener('click', function () { cdBulkOpen('all'); });
    $('cdSelBulk').addEventListener('click', function () { cdBulkOpen('sel'); });
    $('cdSelClear').addEventListener('click', function () {
      cd.rows.forEach(function (r) { r.sel = false; }); cdRender();
    });
    $('cdCkAll').addEventListener('change', function () {
      var on = this.checked;
      cd.rows.forEach(function (r) { r.sel = on; });
      cdRender();
    });

    $('cdBody').addEventListener('click', function (e) {
      var t = e.target;
      var grp = t.closest('[data-cdgrp]');
      if (grp) { var k = grp.dataset.cdgrp; cd.collapsed[k] = !cd.collapsed[k]; cdRender(); return; }
      var dt = t.closest('[data-cddate]');
      if (dt && !dt.disabled) {
        var r1 = cdRow(dt.dataset.key); if (!r1) return;
        var which = dt.dataset.cddate;
        dtpOpen(dt, which === 'in' ? r1.in : r1.out, true, function (d) {
          if (which === 'in') cdSetIn(r1, d); else cdSetOut(r1, d);
        });
        return;
      }
      var st = t.closest('[data-cdstep]');
      if (st) {
        var r2 = cdRow(st.dataset.key); if (!r2) return;
        cdSetNights(r2, r2.n + parseInt(st.dataset.cdstep, 10));
        return;
      }
      var ed = t.closest('[data-cdedit]');
      if (ed) { cdDetailOpen(ed.dataset.cdedit); return; }
      var cf = t.closest('[data-cdconf]');
      if (cf) {
        var r3 = cdRow(cf.dataset.cdconf);
        if (r3 && r3.conflict) {
          openGenDlg('Xung đột booking',
            '<p style="margin:0">Phòng ' + esc(r3.room) + ' đã có ' +
            (r3.conflict.st === 'repair' ? 'lịch sửa phòng' : 'đặt phòng ' + esc(r3.conflict.code || r3.conflict.id)) +
            ' (' + cdFmt(r3.conflict.in) + ' - ' + cdFmt(r3.conflict.out) + ') trùng khoảng thời gian mới. Vui lòng chọn sang ngày khác.</p>',
            '<button class="tl-btn-ghost" type="button" data-tl-close>Đóng</button>');
        }
        return;
      }
    });

    $('cdBody').addEventListener('change', function (e) {
      var ck = e.target.closest('[data-cdck]');
      if (ck) {
        var kind = ck.dataset.cdck, key = ck.dataset.key, on = ck.checked;
        if (kind === 'row') { var r = cdRow(key); if (r) r.sel = on; }
        else if (kind === 'grp') { cd.rows.forEach(function (x) { if (x.type === key) x.sel = on; }); }
        else if (kind === 'past') {
          var rp = cdRow(key);
          if (rp) {
            rp.past = on;
            if (on) {
              openGenDlg('Trả phòng quá khứ',
                '<label class="cd-frow top" style="align-items:flex-start"><span class="lb" style="width:120px">Lý do<span style="color:var(--ez-error)">*</span></span>' +
                '<textarea id="cdPastReason" maxlength="200" placeholder="Nhập lý do áp dụng trả phòng trong quá khứ" ' +
                'style="flex:1 1 auto;min-height:88px;padding:10px 14px;border:1px solid var(--ez-tf-border);border-radius:6px;font:inherit"></textarea></label>',
                '<button class="tl-btn-ghost" type="button" data-tl-close>Huỷ</button>' +
                '<button class="tl-btn-primary" type="button" id="cdPastOk">Xác nhận</button>');
              $('cdPastOk').onclick = function () {
                var v = ($('cdPastReason').value || '').trim();
                if (!v) { $('cdPastReason').focus(); return; }
                rp.pastReason = v; closeDlgs(); cdRender();
              };
            } else rp.pastReason = '';
          }
        }
        cdRender();
      }
    });

    $('cdBody').addEventListener('keydown', function (e) {
      var inp = e.target.closest('[data-cdn]');
      if (inp && e.key === 'Enter') {
        var r = cdRow(inp.dataset.cdn);
        if (r) cdSetNights(r, parseInt(inp.value, 10) || r.n0);
      }
    });
    $('cdBody').addEventListener('blur', function (e) {
      var inp = e.target.closest && e.target.closest('[data-cdn]');
      if (inp) {
        var r = cdRow(inp.dataset.cdn);
        if (r && (parseInt(inp.value, 10) || 0) !== r.n) cdSetNights(r, parseInt(inp.value, 10) || r.n0);
      }
    }, true);

    $('cdPager').addEventListener('click', function (e) {
      var b = e.target.closest('button'); if (!b || b.disabled) return;
      var pages = Math.max(1, Math.ceil(cd.rows.length / cd.per));
      if (b.dataset.cdpage) cd.page = parseInt(b.dataset.cdpage, 10);
      else if (b.dataset.cdpg === 'first') cd.page = 1;
      else if (b.dataset.cdpg === 'prev') cd.page = Math.max(1, cd.page - 1);
      else if (b.dataset.cdpg === 'next') cd.page = Math.min(pages, cd.page + 1);
      else if (b.dataset.cdpg === 'last') cd.page = pages;
      cdRender();
    });

    // ----- popup Đổi đồng loạt -----
    $('cdBX').addEventListener('click', cdSubClose);
    $('cdBCancel').addEventListener('click', cdSubClose);
    $('cdSubBd').addEventListener('click', cdSubClose);
    $('cdBOk').addEventListener('click', cdBulkApply);
    $('cdBIn').addEventListener('click', function () {
      dtpOpen($('cdBIn'), cdBulk.in, true, function (d) {
        if (startOfDay(d) < startOfDay(HOTEL_NOW)) { toast('Không được chọn thời gian trước ngày hiện tại', true); return; }
        cdBulk.in = d;
        if (cdBulk.out.getTime() <= cdBulk.in.getTime()) cdBulk.out = addDays(startOfDay(cdBulk.in), 1);
        cdBulk.n = Math.max(1, dayDiff(cdBulk.out, cdBulk.in));
        cdBulkSync();
      });
    });
    $('cdBOut').addEventListener('click', function () {
      dtpOpen($('cdBOut'), cdBulk.out, true, function (d) {
        if (d.getTime() <= cdBulk.in.getTime()) { toast('Ngày đi phải sau ngày đến ít nhất 1 đêm', true); return; }
        cdBulk.out = d;
        cdBulk.n = Math.max(1, dayDiff(cdBulk.out, cdBulk.in));
        cdBulkSync();
      });
    });
    document.querySelectorAll('#cdBulkDlg [data-cdbn]').forEach(function (b) {
      b.addEventListener('click', function () {
        cdBulk.n = Math.max(1, Math.min(365, cdBulk.n + parseInt(b.dataset.cdbn, 10)));
        cdBulk.out = addDays(startOfDay(cdBulk.in), cdBulk.n);
        cdBulk.out.setHours(12, 0, 0, 0);
        cdBulkSync();
      });
    });
    $('cdBN').addEventListener('change', function () {
      cdBulk.n = Math.max(1, Math.min(365, parseInt(this.value, 10) || 1));
      cdBulk.out = addDays(startOfDay(cdBulk.in), cdBulk.n);
      cdBulk.out.setHours(12, 0, 0, 0);
      cdBulkSync();
    });

    // ----- popup Chi tiết thay đổi -----
    $('cdDtX').addEventListener('click', cdSubClose);
    $('cdDtCancel').addEventListener('click', cdSubClose);
    document.querySelectorAll('#cdDtDlg [data-cdtab]').forEach(function (b) {
      b.addEventListener('click', function () { cd.dtTab = b.dataset.cdtab; cdDetailRender(); });
    });
    $('cdDtBody').addEventListener('change', function (e) {
      var r = cd.dtRow; if (!r) return;
      var sel = e.target.closest('[data-cdit]');
      if (sel) { r.items[cd.dtTab][parseInt(sel.dataset.cdit, 10)].rate = sel.value; cdDetailRender(); return; }
      var amt = e.target.closest('[data-cdamt]');
      if (amt) {
        var v = parseInt(String(amt.value).replace(/[^\d]/g, ''), 10) || 0;
        r.items[cd.dtTab][parseInt(amt.dataset.cdamt, 10)].amt = v;
        cdDetailRender();
      }
    });
    $('cdDtOk').addEventListener('click', function () {
      var r = cd.dtRow; if (!r) return;
      r.price = r.price0 + cdDetailDelta(r);
      r.edited = true;
      cdSubClose(); cdRender();
      toast('Cập nhật tiền phòng thành công');            // MSG_07
    });

    // Esc: đóng popup con trước, rồi mới tới popup chính
    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape' || !cd.open) return;
      if (typeof dtpIsOpen === 'function' && dtpIsOpen()) return;
      var vs = $('qbVsMenu');
      if (vs && vs.classList.contains('is-open')) return;
      if ($('tlGenDlg').classList.contains('is-open')) return;
      e.preventDefault();
      if (cdSubIsOpen()) cdSubClose(); else cdTryClose();
    });
  }


  /* =====================================================================
     24. CHUYỂN PHÒNG (R01.08) — SRS Chuyển phòng + Figma 48831:92306
     Mở từ doAction case 'moveroom'. Dùng lại vsDef/vsVal/vsSet, toast,
     confirmDlg/openGenDlg, render() của Timeline.
     ===================================================================== */

  var MV_REASONS = [
    'Khách yêu cầu chuyển phòng',
    'Nâng hạng phòng (upgrade)',
    'Sự cố kỹ thuật tại phòng',
    'Tối ưu vận hành buồng phòng',
    'Phòng không đúng yêu cầu đã đặt',
    'Khác'
  ];
  var MV_HK = [{ v: 'Sạch', t: 'Sạch' }, { v: 'Bẩn', t: 'Bẩn' }];
  var MV_PER = [10, 25, 50, 100];

  var mv = {
    open: false, side: false, rows: [], cur: null,
    collapsed: {}, page: 1, per: 10, rpage: 1, rper: 10,
    fAdj: false, fOnlyOut: false, fExOut: false
  };

  var MVI = {
    caret: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>',
    room: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="17" rx="2"/><path d="M8 2v4M16 2v4M3 10h18"/><path d="M9.5 15.5l2 2 3.5-3.5"/></svg>',
    lock: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="4" y="10" width="16" height="11" rx="2"/><path d="M8 10V7a4 4 0 018 0"/></svg>'
  };

  function mvFmt(d) { return dmy(d) + ' ' + hm(d); }
  function mvRateList(t) {
    var l = (typeof QB_RATES !== 'undefined' && QB_RATES[t]) ? QB_RATES[t] : [];
    return l.map(function (x) { return { v: x.id, t: x.name, price: x.price }; });
  }
  function mvRateName(t, id) {
    var l = mvRateList(t);
    for (var i = 0; i < l.length; i++) if (l[i].v === id) return l[i].t;
    return l[0] ? l[0].t : '—';
  }

  /* ---- Dựng danh sách phòng của booking (đoàn → cả đoàn) ---- */
  function mvBuildRows(bk) {
    var list = bk.group
      ? BOOKINGS.filter(function (b) { return b.group && b.group.gid === bk.group.gid; })
      : [bk];
    return list.map(function (b) {
      var r = roomById(b.room) || {};
      var g = (b.guests || []).filter(function (x) { return x.lead; })[0] || (b.guests || [])[0] || { n: b.name, cc: 'VN' };
      var rl = mvRateList(r.type);
      return {
        id: b.id, bk: b, room: b.room, type: r.type || '', st: b.st,
        name: g.n || b.name, cc: g.cc || 'VN',
        adults: b.adults || 0, children: b.children || 0, infants: b.infants || 0,
        note: b.note || '', inote: b.inote || '',
        rate: rl[0] ? rl[0].v : '', rates: rl,
        price0: b.total || 0, price: b.total || 0,
        newRoom: null, newType: null, reason: '',
        locked: false, lockReason: '', lockBy: '',
        changeRate: false, newRate: '', fee: 0, done: false
      };
    });
  }

  function mvDirty() { return mv.rows.some(function (r) { return r.done || r.newRoom; }); }
  function mvRow(id) { for (var i = 0; i < mv.rows.length; i++) if (mv.rows[i].id === id) return mv.rows[i]; return null; }

  /* ---- Mở / đóng ---- */
  function mvOpen(bkId) {
    var bk = bkById(bkId); if (!bk) return;
    if (bk.st !== 'booked' && bk.st !== 'inhouse') {
      toast('Chỉ chuyển phòng được với đặt phòng ở trạng thái Đã đặt hoặc Có khách', true); return;
    }
    if (!bk.room) { toast('Đặt phòng chưa được gán phòng — cần gán phòng trước khi chuyển', true); return; }
    closeTip(); closeMenu();
    mv.rows = mvBuildRows(bk);
    mv.cur = null; mv.side = false; mv.collapsed = {}; mv.page = 1; mv.per = 10;
    vsDef('mvPerPage', MV_PER.map(function (n) { return { v: String(n), t: String(n) }; }), '10');
    if (QB_SEL.mvPerPage) QB_SEL.mvPerPage.onChange = function (v) { mv.per = parseInt(v, 10) || 10; mv.page = 1; mvRender(); };
    vsDef('mvRPerPage', MV_PER.map(function (n) { return { v: String(n), t: String(n) }; }), '10');
    if (QB_SEL.mvRPerPage) QB_SEL.mvRPerPage.onChange = function (v) { mv.rper = parseInt(v, 10) || 10; mv.rpage = 1; mvRenderRooms(); };
    mv.open = true;
    $('mvBd').classList.add('is-open');
    $('mvDlg').classList.add('is-open');
    $('mvDlg').classList.remove('is-side');
    mvRender();
    setTimeout(function () { $('mvX').focus(); }, 0);
  }
  function mvClose() {
    mv.open = false;
    $('mvDlg').classList.remove('is-open', 'is-side');
    $('mvBd').classList.remove('is-open');
  }
  function mvTryClose() {
    if (!mvDirty()) { mvClose(); return; }
    confirmDlg('Huỷ chuyển phòng', 'Bạn có chắc chắn muốn huỷ chuyển phòng? Toàn bộ thao tác đã thực hiện sẽ không được lưu.', 'Xác nhận', mvClose);
  }

  /* ---- Nhóm ---- */
  function mvGroups() {
    var order = [], map = {};
    mv.rows.forEach(function (r) {
      if (!map[r.type]) { map[r.type] = []; order.push(r.type); }
      map[r.type].push(r);
    });
    return order.map(function (t) { return { type: t, rows: map[t] }; });
  }

  /* ---- Render danh sách + cây ---- */
  function mvRender() {
    if (!mv.open) return;
    $('mvDlg').classList.toggle('is-side', !!mv.side);

    var total = mv.rows.length;
    var pages = Math.max(1, Math.ceil(total / mv.per));
    if (mv.page > pages) mv.page = pages;
    var from = (mv.page - 1) * mv.per, to = Math.min(total, from + mv.per);
    var ids = {}; mv.rows.slice(from, to).forEach(function (r) { ids[r.id] = 1; });

    var h = [], tree = [];
    mvGroups().forEach(function (g) {
      var rows = g.rows.filter(function (r) { return ids[r.id]; });
      tree.push('<div class="g"><button type="button" data-mvgrp="' + esc(g.type) + '" aria-expanded="' +
        !mv.collapsed[g.type] + '">' + MVI.caret + esc(typeName(g.type)) + ' (' + g.rows.length + ')</button></div>');
      if (!mv.collapsed[g.type]) {
        g.rows.forEach(function (r) {
          tree.push('<button class="it' + (mv.cur === r.id ? ' is-cur' : '') + (r.done ? ' done' : '') +
            '" type="button" data-mvpick="' + esc(r.id) + '">' + MVI.room +
            '<span>' + esc(typeName(r.newType || r.type)) + ' - ' + esc(r.newRoom || r.room) + '</span></button>');
        });
      }
      if (!rows.length) return;
      h.push('<tr class="mv-grp"><td colspan="9"><button class="mv-grpname' + (mv.collapsed[g.type] ? ' is-closed' : '') +
        '" type="button" data-mvgrp="' + esc(g.type) + '" aria-expanded="' + !mv.collapsed[g.type] + '">' +
        MVI.caret + esc(typeName(g.type)) + ' (' + g.rows.length + ')</button></td></tr>');
      if (mv.collapsed[g.type]) return;
      rows.forEach(function (r) { h.push(mvRowHTML(r)); });
    });
    $('mvBody').innerHTML = h.join('') || '<tr><td colspan="9" style="padding:24px;text-align:center;color:var(--theme-text-secondary)">Không có phòng nào.</td></tr>';
    $('mvTree').innerHTML = tree.join('');

    var o = 0, n = 0;
    mv.rows.forEach(function (r) { o += r.price0; n += r.price; });
    $('mvTotOld').textContent = money(o);
    $('mvTotNew').textContent = money(n);
    var d = n - o;
    var de = $('mvTotDiff');
    de.textContent = d ? (d > 0 ? '+' : '-') + money(Math.abs(d)) : '0';
    de.className = 'mo' + (d > 0 ? ' mv-up' : (d < 0 ? ' mv-down' : ''));

    $('mvRange').textContent = 'Hiển thị ' + (total ? from + 1 : 0) + ' - ' + to + ' trên ' + total + ' bản ghi';
    mvPager($('mvPager'), mv.page, pages, 'mvpg');
    if (mv.side) mvRenderMid();
  }

  function mvRowHTML(r) {
    var diff = r.price - r.price0;
    var td = [];
    td.push('<td class="rm"><span class="mv-roomcell' + (r.done ? ' done' : '') + '">' + MVI.room +
      '<span>' + esc(typeName(r.newType || r.type)) + ' - ' + esc(r.newRoom || r.room) + '</span></span></td>');
    td.push('<td class="lk">' + (r.locked
      ? '<span class="mv-lockic" title="Lý do: ' + esc(r.lockReason) + ' — Người khoá: ' + esc(r.lockBy) + '">' + MVI.lock + '</span>'
      : '') + '</td>');
    td.push('<td><span class="mv-guest">' + flagSVG(r.cc) + '<span>' + esc(r.name) + '</span></span></td>');
    td.push('<td>' + r.adults + '/' + r.children + '/' + r.infants + '</td>');
    td.push('<td class="dt">' + mvFmt(r.bk.in) + '<br>' + mvFmt(r.bk.out) +
      ' <span class="mv-mute">(' + nightsOf(r.bk) + ' đêm)</span></td>');
    td.push('<td>' + esc(mvRateName(r.newType || r.type, r.changeRate ? r.newRate : r.rate)) + '</td>');
    td.push('<td class="mo">' + money(r.price0) + '</td>');
    td.push('<td class="mo">' + (r.done ? money(r.price) : '<span class="mv-mute">--</span>') + '</td>');
    td.push('<td class="mo">' + (r.done
      ? '<span class="' + (diff > 0 ? 'mv-up' : (diff < 0 ? 'mv-down' : 'mv-mute')) + '">' +
        (diff ? (diff > 0 ? '+' : '-') + money(Math.abs(diff)) : '0') + '</span>'
      : '<span class="mv-mute">--</span>') + '</td>');
    return '<tr class="mv-row' + (mv.cur === r.id ? ' is-cur' : '') + '" data-mvrow="' + esc(r.id) + '">' + td.join('') + '</tr>';
  }

  function mvPager(el, page, pages, key) {
    var L = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 6l-6 6 6 6"/></svg>';
    var LL = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 6l-6 6 6 6M9 6l-6 6 6 6"/></svg>';
    var R = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6-6 6"/></svg>';
    var RR = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 6l6 6-6 6M15 6l6 6-6 6"/></svg>';
    var h = [];
    h.push('<button type="button" data-' + key + '="first"' + (page === 1 ? ' disabled' : '') + ' aria-label="Trang đầu">' + LL + '</button>');
    h.push('<button type="button" data-' + key + '="prev"' + (page === 1 ? ' disabled' : '') + ' aria-label="Trang trước">' + L + '</button>');
    for (var i = 1; i <= pages; i++) {
      if (pages > 7 && i > 2 && i < pages - 1 && Math.abs(i - page) > 1) {
        if (h[h.length - 1].indexOf('…') < 0) h.push('<button type="button" disabled>…</button>');
        continue;
      }
      h.push('<button type="button" data-' + key + 'num="' + i + '"' + (i === page ? ' aria-current="true"' : '') + '>' + i + '</button>');
    }
    h.push('<button type="button" data-' + key + '="next"' + (page === pages ? ' disabled' : '') + ' aria-label="Trang sau">' + R + '</button>');
    h.push('<button type="button" data-' + key + '="last"' + (page === pages ? ' disabled' : '') + ' aria-label="Trang cuối">' + RR + '</button>');
    el.innerHTML = h.join('');
  }

  /* ---- Chọn 1 phòng → mở sidebar ---- */
  function mvPick(id) {
    var r = mvRow(id); if (!r) return;
    mv.cur = id; mv.side = true; mv.rpage = 1;
    vsDef('mvReason', [{ v: '', t: 'Chọn lý do' }].concat(MV_REASONS.map(function (x) { return { v: x, t: x }; })), r.reason || '', 'Chọn lý do');
    if (QB_SEL.mvReason) QB_SEL.mvReason.onChange = function (v) { r.reason = v; $('mvReasonMsg').hidden = true; };
    vsDef('mvFType', TYPES.map(function (t) { return { v: t.id, t: t.name }; }), r.newType || r.type);
    if (QB_SEL.mvFType) QB_SEL.mvFType.onChange = function () { mvSyncRoomFilter(true); mv.rpage = 1; mvRenderRooms(); };
    mvSyncRoomFilter(true);
    vsDef('mvFHk', MV_HK, 'Sạch');
    if (QB_SEL.mvFHk) QB_SEL.mvFHk.onChange = function () { mv.rpage = 1; mvRenderRooms(); };
    $('mvFAdj').checked = mv.fAdj; $('mvFOnlyOut').checked = mv.fOnlyOut; $('mvFExOut').checked = mv.fExOut;
    mvRender();
  }
  function mvSyncRoomFilter(reset) {
    var t = vsVal('mvFType');
    var rooms = ROOMS.filter(function (x) { return x.type === t; });
    vsDef('mvFRoom', [{ v: '', t: 'Tất cả' }].concat(rooms.map(function (x) { return { v: x.id, t: x.id }; })),
      reset ? '' : vsVal('mvFRoom'), 'Tất cả');
    if (QB_SEL.mvFRoom) QB_SEL.mvFRoom.onChange = function () { mv.rpage = 1; mvRenderRooms(); };
  }

  /* ---- Thông tin chung ---- */
  function mvRenderMid() {
    var r = mvRow(mv.cur); if (!r) return;
    $('mvOldRoom').textContent = typeName(r.type) + ' - ' + r.room;
    var nr = $('mvNewRoom');
    nr.textContent = r.newRoom ? (typeName(r.newType) + ' - ' + r.newRoom) : 'Chưa chọn';
    nr.classList.toggle('none', !r.newRoom);
    $('mvNewLock').hidden = !r.locked;

    var f = [];
    f.push('<dt>Khách hàng:</dt><dd>' + flagSVG(r.cc) + esc(r.name) + '</dd>');
    f.push('<dt>Ngày đến - đi:</dt><dd>' + mvFmt(r.bk.in) + '<br>' + mvFmt(r.bk.out) +
      ' <span class="sub">(' + nightsOf(r.bk) + ' đêm)</span></dd>');
    f.push('<dt>Phòng đã đặt:</dt><dd>' + esc(typeName(r.type)) + '</dd>');
    f.push('<dt>Lượng khách:</dt><dd>' + r.adults + 'NL/' + r.children + 'TE/' + r.infants + 'EB</dd>');
    f.push('<dt>Ghi chú phòng:</dt><dd>' + (r.note ? esc(r.note) : '<span class="sub">Không có ghi chú</span>') + '</dd>');
    f.push('<dt>Ghi chú nội bộ:</dt><dd>' + (r.inote ? esc(r.inote) : '<span class="sub">Không có ghi chú</span>') + '</dd>');
    $('mvInfo').innerHTML = f.join('');

    $('mvTasks').hidden = !r.done;
    $('mvLock').checked = r.locked;
    $('mvLockWrap').hidden = !r.locked;
    $('mvLockReason').value = r.lockReason;
    var diffType = !!r.newRoom && r.newType !== r.type;
    $('mvRateWrap').hidden = !diffType;
    $('mvRateCk').checked = r.changeRate;
    $('mvRateBox').hidden = !r.changeRate;
    if (r.changeRate) {
      vsDef('mvRate', mvRateList(r.newType).map(function (x) { return { v: x.v, t: x.t }; }), r.newRate);
      if (QB_SEL.mvRate) QB_SEL.mvRate.onChange = function (v) { r.newRate = v; mvSyncCalc(r); mvRender(); };
      $('mvFee').value = money(r.fee);
      mvSyncCalc(r);
    }
    var idx = mv.rows.map(function (x) { return x.id; }).indexOf(r.id);
    $('mvPrev').disabled = idx <= 0;
    $('mvNext').disabled = idx >= mv.rows.length - 1;

    $('mvLocked').hidden = !r.locked;
    $('mvRightBody').hidden = !!r.locked;
    $('mvLockedReason').textContent = r.lockReason;
    $('mvLockedBy').textContent = r.lockBy;
    if (!r.locked) mvRenderRooms();
  }

  function mvUnit(r) { return nightsOf(r.bk) > 0 ? Math.round(r.price0 / nightsOf(r.bk)) : 0; }
  function mvSyncCalc(r) {
    var list = mvRateList(r.newType);
    var pick = null;
    for (var i = 0; i < list.length; i++) if (list[i].v === r.newRate) pick = list[i];
    var nights = nightsOf(r.bk);
    var np = (pick ? pick.price * nights : r.price0) + (r.fee || 0);
    r.price = np;
    $('mvCalcRate').innerHTML = esc(mvRateName(r.type, r.rate)) + ' <span class="mv-mute">→</span> ' + esc(pick ? pick.t : '—');
    $('mvCalcPrice').innerHTML = money(r.price0) + ' <span class="mv-mute">→</span> ' + money(np);
    var d = np - r.price0;
    $('mvCalcDiff').innerHTML = '<span class="' + (d > 0 ? 'mv-up' : (d < 0 ? 'mv-down' : 'mv-mute')) + '">' +
      (d ? (d > 0 ? '+ ' : '- ') + money(Math.abs(d)) : '0') + '</span>';
  }

  /* ---- Danh sách phòng trống ---- */
  function mvFreeRooms(r) {
    var t = vsVal('mvFType'), only = vsVal('mvFRoom'), hk = vsVal('mvFHk');
    var taken = {};
    mv.rows.forEach(function (x) { if (x.newRoom && x.id !== r.id) taken[x.newRoom] = 1; });
    return ROOMS.filter(function (x) {
      if (x.type !== t) return false;
      if (only && x.id !== only) return false;
      if (hk === 'Sạch' && x.dirty) return false;
      if (hk === 'Bẩn' && !x.dirty) return false;
      if (x.id === r.room || taken[x.id]) return false;
      for (var i = 0; i < BOOKINGS.length; i++) {
        var b = BOOKINGS[i];
        if (b.room !== x.id || b.id === r.id) continue;
        if (b.in < r.bk.out && r.bk.in < b.out) return false;
      }
      if (mv.fAdj) {
        var num = parseInt(x.id, 10), cur = parseInt(r.room, 10);
        if (isNaN(num) || isNaN(cur) || Math.abs(num - cur) !== 1) return false;
      }
      var soonOut = BOOKINGS.some(function (b) {
        return b.room === x.id && b.st === 'inhouse' && sameDay(b.out, HOTEL_NOW);
      });
      if (mv.fOnlyOut && !soonOut) return false;
      if (mv.fExOut && soonOut) return false;
      return true;
    });
  }

  function mvRenderRooms() {
    var r = mvRow(mv.cur); if (!r) return;
    var list = mvFreeRooms(r);
    var pages = Math.max(1, Math.ceil(list.length / mv.rper));
    if (mv.rpage > pages) mv.rpage = pages;
    var from = (mv.rpage - 1) * mv.rper;
    var page = list.slice(from, from + mv.rper);
    $('mvRoomBody').innerHTML = page.map(function (x) {
      return '<tr><td>' + esc(typeName(x.type)) + ' - ' + esc(x.id) + '</td>' +
        '<td>' + (x.dirty ? 'Bẩn' : 'Sạch') + '</td>' +
        '<td class="ac"><button class="mv-godo" type="button" data-mvgo="' + esc(x.id) + '">Chuyển phòng</button></td></tr>';
    }).join('') || '<tr><td colspan="3" style="padding:20px;text-align:center;color:var(--theme-text-secondary)">Không có phòng trống phù hợp.</td></tr>';
    mvPager($('mvRPager'), mv.rpage, pages, 'mvrpg');
  }

  /* ---- Thực hiện chuyển ---- */
  function mvDoMove(roomId) {
    var r = mvRow(mv.cur); if (!r) return;
    if (!r.reason) { $('mvReasonMsg').hidden = false; return; }
    var nr = roomById(roomId); if (!nr) return;
    r.newRoom = roomId; r.newType = nr.type; r.done = true;
    if (r.newType !== r.type) {
      var l = mvRateList(r.newType);
      r.newRate = l[0] ? l[0].v : '';
    } else { r.changeRate = false; r.price = r.price0; }
    mvRender();
    toast('Đã chuyển phòng');
  }

  function mvStep(delta) {
    var idx = mv.rows.map(function (x) { return x.id; }).indexOf(mv.cur);
    var next = idx + delta;
    if (next < 0 || next >= mv.rows.length) return;
    mvPick(mv.rows[next].id);
  }

  function mvSave() {
    var pending = mv.rows.filter(function (r) { return r.newRoom; });
    if (!pending.length) { toast('Chưa có phòng nào được chuyển', true); return; }
    var miss = pending.filter(function (r) { return r.locked && !r.lockReason.trim(); });
    if (miss.length) {
      mvPick(miss[0].id);
      $('mvLockMsg').hidden = false;
      $('mvLockReason').classList.add('is-error');
      return;
    }
    pending.forEach(function (r) {
      r.bk.room = r.newRoom;
      if (r.changeRate) r.bk.total = r.price;
      r.bk.manual = true;
      normalizeSt(r.bk);
    });
    mvClose(); render();
    toast('Đã chuyển phòng');
  }

  /* ---- Bind ---- */
  function bindMv() {
    $('mvX').addEventListener('click', mvTryClose);
    $('mvX2').addEventListener('click', mvTryClose);
    $('mvCancel').addEventListener('click', mvTryClose);
    $('mvBd').addEventListener('click', mvTryClose);
    $('mvSave').addEventListener('click', mvSave);

    $('mvBody').addEventListener('click', function (e) {
      var g = e.target.closest('[data-mvgrp]');
      if (g) { var k = g.dataset.mvgrp; mv.collapsed[k] = !mv.collapsed[k]; mvRender(); return; }
      var tr = e.target.closest('[data-mvrow]');
      if (tr) mvPick(tr.dataset.mvrow);
    });
    $('mvTree').addEventListener('click', function (e) {
      var g = e.target.closest('[data-mvgrp]');
      if (g) { var k = g.dataset.mvgrp; mv.collapsed[k] = !mv.collapsed[k]; mvRender(); return; }
      var it = e.target.closest('[data-mvpick]');
      if (it) mvPick(it.dataset.mvpick);
    });
    $('mvRoomBody').addEventListener('click', function (e) {
      var b = e.target.closest('[data-mvgo]');
      if (b) mvDoMove(b.dataset.mvgo);
    });

    $('mvPrev').addEventListener('click', function () { mvStep(-1); });
    $('mvNext').addEventListener('click', function () { mvStep(1); });
    document.addEventListener('keydown', function (e) {
      if (!mv.open || !mv.side) return;
      if (e.target && /INPUT|TEXTAREA|SELECT/.test(e.target.tagName)) return;
      if ($('qbVsMenu').classList.contains('is-open')) return;
      if (e.key === 'ArrowLeft') { e.preventDefault(); mvStep(-1); }
      else if (e.key === 'ArrowRight') { e.preventDefault(); mvStep(1); }
    });

    $('mvLock').addEventListener('change', function () {
      var r = mvRow(mv.cur); if (!r) return;
      r.locked = this.checked;
      if (!r.locked) { r.lockReason = ''; r.lockBy = ''; }
      else r.lockBy = 'Nguyễn Hoàng Kiều Trinh';
      $('mvLockWrap').hidden = !r.locked;
      $('mvLockMsg').hidden = true;
      mvRender();
    });
    $('mvLockReason').addEventListener('input', function () {
      var r = mvRow(mv.cur); if (!r) return;
      r.lockReason = this.value;
      $('mvLockedReason').textContent = r.lockReason;
      this.classList.remove('is-error'); $('mvLockMsg').hidden = true;
    });
    $('mvRateCk').addEventListener('change', function () {
      var r = mvRow(mv.cur); if (!r) return;
      r.changeRate = this.checked;
      if (!r.changeRate) { r.price = r.price0; r.fee = 0; }
      mvRender();
    });
    $('mvFee').addEventListener('input', function () {
      var r = mvRow(mv.cur); if (!r) return;
      r.fee = parseInt(String(this.value).replace(/[^\d]/g, ''), 10) || 0;
      mvSyncCalc(r); mvRender();
    });

    [['mvFAdj', 'fAdj'], ['mvFOnlyOut', 'fOnlyOut'], ['mvFExOut', 'fExOut']].forEach(function (p) {
      $(p[0]).addEventListener('change', function () {
        mv[p[1]] = this.checked; mv.rpage = 1; mvRenderRooms();
      });
    });

    $('mvPager').addEventListener('click', function (e) {
      var b = e.target.closest('button'); if (!b || b.disabled) return;
      var pages = Math.max(1, Math.ceil(mv.rows.length / mv.per));
      if (b.dataset.mvpgnum) mv.page = parseInt(b.dataset.mvpgnum, 10);
      else if (b.dataset.mvpg === 'first') mv.page = 1;
      else if (b.dataset.mvpg === 'prev') mv.page = Math.max(1, mv.page - 1);
      else if (b.dataset.mvpg === 'next') mv.page = Math.min(pages, mv.page + 1);
      else if (b.dataset.mvpg === 'last') mv.page = pages;
      mvRender();
    });
    $('mvRPager').addEventListener('click', function (e) {
      var b = e.target.closest('button'); if (!b || b.disabled) return;
      var r = mvRow(mv.cur); if (!r) return;
      var pages = Math.max(1, Math.ceil(mvFreeRooms(r).length / mv.rper));
      if (b.dataset.mvrpgnum) mv.rpage = parseInt(b.dataset.mvrpgnum, 10);
      else if (b.dataset.mvrpg === 'first') mv.rpage = 1;
      else if (b.dataset.mvrpg === 'prev') mv.rpage = Math.max(1, mv.rpage - 1);
      else if (b.dataset.mvrpg === 'next') mv.rpage = Math.min(pages, mv.rpage + 1);
      else if (b.dataset.mvrpg === 'last') mv.rpage = pages;
      mvRenderRooms();
    });

    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape' || !mv.open) return;
      if (typeof dtpIsOpen === 'function' && dtpIsOpen()) return;
      if ($('qbVsMenu').classList.contains('is-open')) return;
      if ($('tlGenDlg').classList.contains('is-open')) return;
      e.preventDefault();
      if (mv.side) { mv.side = false; mv.cur = null; mvRender(); } else mvTryClose();
    });
  }


  /* =====================================================================
     25. DANH SÁCH BÁO CÁO (RR00) — SRS RR00 + Figma 35977:4589
     Màn toàn trang, mở từ mục "Báo cáo" trên menu trái (.nav-item).
     ===================================================================== */

  /* Mã + tên báo cáo lấy nguyên văn theo ảnh render Figma 35875:28101 / 36251:4296 */
  var REPORT_GROUPS = [
    { g: 'Báo cáo kiểm soát', items: [
      { id: 'BC001', name: 'Lịch sử thao tác' },
      { id: 'BC002', name: 'Báo cáo kiểm toán đêm' },
      { id: 'BC004', name: 'Nhật ký thao tác thẻ' },
      { id: 'BC005', name: 'Đối chiếu hóa đơn điện tử' }
    ] },
    { g: 'Báo cáo lễ tân', items: [
      { id: 'BC103', name: 'Công suất phòng', preview: 'rv', sheet: 'BC06' },
      { id: 'BC104', name: 'Báo cáo bữa ăn', preview: 'rv', sheet: 'BC06' },
      { id: 'BC105', name: 'Công nợ chi tiết', preview: 'rv', sheet: 'BC06' },
      { id: 'BC107', name: 'Khai báo CA/Hải quan', preview: 'ca', sheet: 'BC06' },
      { id: 'BC109', name: 'Thanh toán hàng ngày' },
      { id: 'BC111', name: 'Báo cáo giảm giá' },
      { id: 'BC120', name: 'Báo cáo doanh thu theo phòng' },
      { id: 'BC121', name: 'Báo cáo doanh thu theo booking' },
      { id: 'BC122', name: 'Báo cáo doanh thu theo công ty' },
      { id: 'BC123', name: 'Báo cáo doanh thu theo nguồn' },
      { id: 'BC124', name: 'Báo cáo doanh thu theo sale' },
      { id: 'BC125', name: 'Báo cáo doanh thu theo loại phòng' },
      { id: 'BC126', name: 'Báo cáo doanh thu theo giao dịch' }
    ] },
    { g: 'Báo cáo buồng phòng', items: [
      { id: 'BC201', name: 'Báo cáo buồng phòng' },
      { id: 'BC202', name: 'Báo cáo tổng hợp dịch vụ buồng phòng' }
    ] }
  ];

  var rl = { tab: 'all', q: '', sel: 'BC107', collapsed: {}, fav: {} };

  var RLI = {
    caret: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>',
    starOn: '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 3.4l2.6 5.3 5.8.8-4.2 4.1 1 5.8-5.2-2.7-5.2 2.7 1-5.8-4.2-4.1 5.8-.8z"/></svg>',
    starOff: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round" aria-hidden="true"><path d="M12 3.4l2.6 5.3 5.8.8-4.2 4.1 1 5.8-5.2-2.7-5.2 2.7 1-5.8-4.2-4.1 5.8-.8z"/></svg>'
  };

  /* Bỏ dấu tiếng Việt để tìm kiếm không phân biệt dấu */
  function rlNorm(s) {
    return String(s || '')
      .toLowerCase()
      .replace(/[àáạảãâầấậẩẫăằắặẳẵ]/g, 'a')
      .replace(/[èéẹẻẽêềếệểễ]/g, 'e')
      .replace(/[ìíịỉĩ]/g, 'i')
      .replace(/[òóọỏõôồốộổỗơờớợởỡ]/g, 'o')
      .replace(/[ùúụủũưừứựửữ]/g, 'u')
      .replace(/[ỳýỵỷỹ]/g, 'y')
      .replace(/đ/g, 'd');
  }

  function rlAll() {
    var out = [];
    REPORT_GROUPS.forEach(function (g) {
      g.items.forEach(function (it) { out.push(it); });
    });
    return out;
  }
  function rlById(id) {
    var l = rlAll();
    for (var i = 0; i < l.length; i++) if (l[i].id === id) return l[i];
    return null;
  }
  function rlIsFav(it) { return rl.fav[it.id] !== undefined ? rl.fav[it.id] : !!it.fav; }
  function rlMatch(it) {
    if (!rl.q) return true;
    var q = rlNorm(rl.q);
    return rlNorm(it.id).indexOf(q) >= 0 || rlNorm(it.name).indexOf(q) >= 0 ||
           rlNorm(it.id + ' - ' + it.name).indexOf(q) >= 0;
  }

  function rlRenderList() {
    var h = [], any = false;

    if (rl.tab === 'fav') {
      rlAll().forEach(function (it) {
        if (!rlIsFav(it) || !rlMatch(it)) return;
        any = true; h.push(rlItemHTML(it));
      });
    } else {
      REPORT_GROUPS.forEach(function (g) {
        var items = g.items.filter(rlMatch);
        if (!items.length) return;
        any = true;
        h.push('<button class="rl-grp' + (rl.collapsed[g.g] ? ' is-closed' : '') +
          '" type="button" data-rlgrp="' + esc(g.g) + '" aria-expanded="' + !rl.collapsed[g.g] + '">' +
          '<span>' + esc(g.g) + '</span>' + RLI.caret + '</button>');
        if (rl.collapsed[g.g]) return;
        items.forEach(function (it) { h.push(rlItemHTML(it)); });
      });
    }

    $('rlList').innerHTML = any ? h.join('')
      : '<p class="rl-noresult">Không có kết quả phù hợp</p>';
    rlTipHide();
  }

  function rlItemHTML(it) {
    var on = rlIsFav(it);
    return '<div class="rl-item' + (rl.sel === it.id ? ' is-sel' : '') + '" role="button" tabindex="0"' +
      ' data-rlrep="' + esc(it.id) + '" aria-current="' + (rl.sel === it.id ? 'true' : 'false') + '">' +
      '<span class="nm">' + esc(it.id) + ' - ' + esc(it.name) + '</span>' +
      '<button class="rl-star' + (on ? ' is-on' : '') + '" type="button" data-rlfav="' + esc(it.id) + '"' +
      ' aria-pressed="' + on + '" aria-label="' + (on ? 'Bỏ yêu thích ' : 'Đánh dấu yêu thích ') + esc(it.name) + '">' +
      (on ? RLI.starOn : RLI.starOff) + '</button></div>';
  }

  /* ---- Khung xem trước ---- */
  function rlStayRange() {
    var a = startOfDay(HOTEL_NOW);
    var z = startOfDay(HOTEL_NOW);
    return { from: a, to: z };
  }

  /* Danh sách khách lưu trú giao với khoảng [from, to] — dùng chung cho RR06 */
  function rlGuestRows(from, to) {
    var rows = [];
    BOOKINGS.forEach(function (b) {
      if (b.st === 'repair') return;
      if (!(b.in <= addDays(startOfDay(to), 1) && startOfDay(from) < b.out)) return;
      (b.guests || []).forEach(function (g) {
        rows.push({ bk: b, g: g });
      });
    });
    return rows;
  }

  /* Hồ sơ giấy tờ mô phỏng — phái sinh ổn định từ tên khách, KHÔNG hard-code theo dòng */
  function rlHash(s) {
    var h = 0;
    for (var i = 0; i < String(s).length; i++) h = (h * 31 + String(s).charCodeAt(i)) >>> 0;
    return h;
  }
  var RL_PROV = ['Hà Nội', 'TP Hồ Chí Minh', 'Hải Phòng', 'Đà Nẵng', 'Nha Trang', 'Cần Thơ', 'Đà Lạt', 'Bắc Ninh', 'Nam Định', 'Quảng Bình'];
  var RL_NATION = { VN: 'Việt Nam', KR: 'Hàn Quốc', JP: 'Nhật Bản', GB: 'Anh', US: 'Hoa Kỳ', CN: 'Trung Quốc', FR: 'Pháp' };
  function rlGuestInfo(g, bk) {
    var h = rlHash(g.n + '|' + bk.id);
    var vn = (g.cc || 'VN') === 'VN';
    var dob = new Date(1975 + (h % 30), h % 12, 1 + (h % 27));
    return {
      name: g.n,
      gender: (h % 2) ? 'Nam' : 'Nữ',
      dob: dob,
      phone: g.p || ('09' + String(10000000 + (h % 89999999)).slice(0, 8)),
      prov: RL_PROV[h % RL_PROV.length],
      cc: g.cc || 'VN',
      nation: RL_NATION[g.cc || 'VN'] || 'Việt Nam',
      cccd: vn ? String(1000 + (h % 8999)) + String(100000000 + (h % 899999999)) : '',
      passport: vn ? '' : ('K' + String(h % 9) + 'W' + String(10 + (h % 89)) + 'M' + String(10 + (h % 79))),
      email: '',
      room: bk.room,
      inAt: bk.in,
      outAt: bk.out
    };
  }

  function rlRenderPreview() {
    var it = rlById(rl.sel);
    var canPv = !!(it && RV[it.id]);
    $('rlSheet').hidden = !canPv;
    $('rlEmpty').hidden = canPv;
    if (!canPv) {
      $('rlEmptyTitle').textContent = it ? ('Chưa có mẫu xem trước cho ' + it.id + ' - ' + it.name) : 'Chưa chọn báo cáo';
      return;
    }
    $('rlPvPage').innerHTML = rvPageHTML(it.id, null);
    rlPvFit();
  }

  /* Trang xem trước:
       - bề ngang trang = 80% bề ngang vùng nội dung (tự co giãn theo cửa sổ)
       - nhưng không hẹp hơn khổ giấy 793px và không hẹp hơn bề ngang nội dung thật
       - phần dư so với khung thì thu nhỏ cả trang cho vừa, không cắt, không cuộn ngang */
  function rlPvFit() {
    var wrap = $('rlSheet'), sc = $('rlPvScale'), pg = $('rlPvPage');
    if (!wrap || !sc || wrap.hidden || !pg) return;

    /* 1. Bỏ thu nhỏ.
       Lỗi cũ: đo t.scrollWidth khi trang đang bị khoá 793px — bảng width:100%
       tự nén lại nên báo về đúng 793, w tính ra thiếu và trang bị cắt bên phải.
       Nay nới trang thật rộng và cho bảng tự giãn theo nội dung rồi mới đo. */
    sc.style.width = '';
    sc.style.height = '';
    pg.style.transform = 'none';

    var tbls = pg.querySelectorAll('table');
    pg.style.width = '6000px';
    Array.prototype.forEach.call(tbls, function (t) {
      t.style.width = 'max-content'; t.style.maxWidth = 'none';
    });
    var need = 0;
    Array.prototype.forEach.call(tbls, function (t) {
      need = Math.max(need, Math.ceil(t.getBoundingClientRect().width));
    });
    Array.prototype.forEach.call(tbls, function (t) {
      t.style.width = ''; t.style.maxWidth = '';
    });

    /* padding + viền trái/phải thật của trang (box-sizing: border-box) */
    var cs = getComputedStyle(pg);
    var pad = (parseFloat(cs.paddingLeft) || 0) + (parseFloat(cs.paddingRight) || 0)
            + (parseFloat(cs.borderLeftWidth) || 0) + (parseFloat(cs.borderRightWidth) || 0);
    var avail = Math.max(120, wrap.clientWidth - 32);

    /* 2. Bề ngang trang: 80% vùng nội dung, sàn là khổ A4 và bề ngang nội dung */
    var w = Math.max(793, Math.ceil(need + pad), Math.round(avail * 0.8));
    pg.style.width = w + 'px';

    /* 2b. Chốt chặn: còn tràn bao nhiêu thì nới đúng bấy nhiêu — không bao giờ cắt */
    for (var g = 0; g < 4; g++) {
      var over = 0;
      Array.prototype.forEach.call(tbls, function (t) {
        over = Math.max(over, t.scrollWidth - t.clientWidth, 0);
      });
      over = Math.max(over, pg.scrollWidth - pg.clientWidth, 0);
      if (over <= 0) break;
      w += over;
      pg.style.width = w + 'px';
    }

    /* 3. Nếu vẫn rộng hơn khung thì thu nhỏ TRANG (hộp bọc chỉ nhận kích thước sau khi
          thu nhỏ) — không cắt, không cuộn ngang */
    var k = Math.min(1, avail / w);
    var hh = pg.offsetHeight;
    if (k < 1) {
      pg.style.transform = 'scale(' + k.toFixed(4) + ')';
      sc.style.width = Math.ceil(w * k) + 'px';
      sc.style.height = Math.ceil(hh * k) + 'px';
    } else {
      pg.style.transform = 'none';
      sc.style.width = '';
      sc.style.height = '';
    }
  }

  function rlRender() {
    rlRenderList();
    rlRenderPreview();
  }

  /* ---- Mở màn ---- */
  function rlShow() {
    rlRender();
    var prev = document.querySelector('.nav-child.is-selected, .nav-item.is-selected');
    if (prev) { prev.classList.remove('is-selected'); prev.removeAttribute('aria-current'); }
    var el = document.querySelector('.nav-item[data-nav="report"]');
    if (el) { el.classList.add('is-selected'); el.setAttribute('aria-current', 'page'); }
  }

  function rlUse() {
    var it = rlById(rl.sel);
    if (!it) return;
    if (it.preview === 'ca' && typeof caOpen === 'function') { caOpen(it); return; }
    if (it.preview === 'rv' && typeof rvOpen === 'function') { rvOpen(it); return; }
    toast('Màn ' + it.id + ' - ' + it.name + ' đang được xây dựng');
  }

  /* ---- Tooltip cho tên báo cáo bị cắt (…) ---- */
  function rlTipHide() {
    var t = $('rlTip');
    if (!t) return;
    t.classList.remove('is-open');
    t.setAttribute('aria-hidden', 'true');
  }
  function rlTipShow(nm) {
    var t = $('rlTip');
    if (!t || !nm) return;
    if (nm.scrollWidth <= nm.clientWidth + 1) { rlTipHide(); return; }
    t.textContent = nm.textContent;
    t.classList.add('is-open');
    t.setAttribute('aria-hidden', 'false');
    var r = nm.getBoundingClientRect();
    var tr = t.getBoundingClientRect();
    var left = Math.round(r.left + r.width / 2 - tr.width / 2);
    left = Math.max(8, Math.min(left, window.innerWidth - tr.width - 8));
    var top = Math.round(r.top - tr.height - 8);
    if (top < 8) top = Math.round(r.bottom + 8);
    t.style.left = left + 'px';
    t.style.top = top + 'px';
  }
  function bindRlTip() {
    var list = $('rlList');
    if (!list) return;
    list.addEventListener('mouseover', function (e) {
      var it = e.target.closest && e.target.closest('.rl-item');
      if (!it) return;
      rlTipShow(it.querySelector('.nm'));
    });
    list.addEventListener('mouseout', function (e) {
      var it = e.target.closest && e.target.closest('.rl-item');
      if (!it) return;
      if (e.relatedTarget && it.contains(e.relatedTarget)) return;
      rlTipHide();
    });
    list.addEventListener('focusin', function (e) {
      var it = e.target.closest && e.target.closest('.rl-item');
      if (it) rlTipShow(it.querySelector('.nm'));
    });
    list.addEventListener('focusout', rlTipHide);
    list.addEventListener('scroll', rlTipHide, true);
    window.addEventListener('resize', rlTipHide);
  }

  function bindRl() {
    if (!$('rlCard')) return;

    $('rlSearch').addEventListener('input', function () {
      rl.q = this.value.trim();
      rlRenderList();
    });

    document.querySelectorAll('#rlCard [data-rltab]').forEach(function (b) {
      b.addEventListener('click', function () {
        rl.tab = b.dataset.rltab;
        document.querySelectorAll('#rlCard [data-rltab]').forEach(function (x) {
          x.setAttribute('aria-selected', String(x.dataset.rltab === rl.tab));
        });
        rlRenderList();
      });
    });

    $('rlList').addEventListener('click', function (e) {
      var fav = e.target.closest('[data-rlfav]');
      if (fav) {
        e.stopPropagation();
        var itf = rlById(fav.dataset.rlfav);
        if (itf) rl.fav[itf.id] = !rlIsFav(itf);
        rlRenderList();
        return;
      }
      var g = e.target.closest('[data-rlgrp]');
      if (g) { var k = g.dataset.rlgrp; rl.collapsed[k] = !rl.collapsed[k]; rlRenderList(); return; }
      var it = e.target.closest('[data-rlrep]');
      if (it) { rl.sel = it.dataset.rlrep; rlRender(); }
    });
    $('rlList').addEventListener('keydown', function (e) {
      if (e.key !== 'Enter' && e.key !== ' ') return;
      var it = e.target.closest('[data-rlrep]');
      if (it) { e.preventDefault(); rl.sel = it.dataset.rlrep; rlRender(); }
    });

    $('rlUseBtn').addEventListener('click', rlUse);
    bindRlTip();
    window.addEventListener('resize', rlPvFit);
  }


  /* ================= TẠO ĐẶT PHÒNG ĐẦY ĐỦ (Reservation) — SRS "Tạo đặt phòng đầy đủ" · Figma 47216:315103 =================
     3 bước: Tìm và báo giá phòng → Thông tin đặt phòng → Thanh toán
     ======================================================================================================================= */

  var RS_BRANCHES = ['Chi nhánh Hà Nội', 'Chi nhánh Đà Nẵng', 'Chi nhánh Nha Trang'];
  var RS_CHANNELS = ['Call Center', 'Trực tiếp', 'Website', 'OTA'];
  var RS_COMPANIES = ['Không có', 'Booking.com', 'Agoda.com', 'Expedia', 'Traveloka', 'Công ty CP Du lịch Sen Vàng'];
  var RS_SOURCES = ['Walk-in', 'Điện thoại', 'Email', 'Đại lý', 'Khách quen'];
  var RS_POOLS = ['Freesale', 'Allotment', 'Guarantee'];
  var RS_MARKETS = ['Nội địa', 'Quốc tế', 'Công vụ', 'Đoàn'];
  var RS_STATUS = ['Tạm giữ', 'Chắc chắn', 'Chờ xác nhận'];
  var RS_RELEASE = ['Không nhả phòng', 'Theo số ngày', 'Ngày chính xác'];
  var RS_STAFFS = ['Nguyễn Văn Thảo Nguyên', 'Trần Thu Hà', 'Lê Minh Châu'];
  var RS_SALUTS = ['Ông', 'Bà', 'Anh', 'Chị'];
  var RS_GENDERS = ['Nam', 'Nữ', 'Khác'];
  var RS_NATIONS = ['Việt Nam', 'Hàn Quốc', 'Nhật Bản', 'Anh', 'Hoa Kỳ', 'Trung Quốc', 'Pháp'];
  var RS_DOCS = ['CCCD', 'Hộ chiếu', 'Giấy phép lái xe'];
  var RS_PROMOS = ['Không áp dụng', 'Giảm 5% khách quen', 'Giảm 10% đoàn', 'Ưu đãi mùa thấp điểm'];
  var RS_PAYTYPES = ['Trả trước', 'Đặt cọc', 'Trả toàn bộ'];
  var RS_CURRENCIES = ['VND', 'USD', 'EUR'];
  var RS_RATECODES = ['Rate code 1', 'Rate code 2', 'Rate code 3', 'Rate code 4', 'Rate code 5'];
  var RS_TAX = 0.08;   /* Thuế VAT mô phỏng */

  var rs = {
    step: 1,
    f: null,
    types: [],           /* loại phòng đang lọc */
    policy: [],          /* chính sách giá (ratecode) đang lọc */
    price: 'total',      /* total | avg */
    heat: true,
    grid: null,          /* kết quả tìm kiếm */
    cart: [],            /* [{ in, out, rooms:[...] }] — gom theo CHẶNG (SRS mục 26·29) */
    pm: 'Tiền mặt',
    promo: 'Không áp dụng'
  };

  function rsRoomTypes() {
    var seen = {}, out = [];
    ROOMS.forEach(function (r) { if (!seen[r.type]) { seen[r.type] = 1; out.push(r.type); } });
    return out;
  }
  function rsDefaultFilter() {
    return {
      branch: RS_BRANCHES[0], channel: RS_CHANNELS[0],
      in: new Date(startOfDay(HOTEL_NOW).getTime() + 14 * 3600000),
      out: new Date(addDays(startOfDay(HOTEL_NOW), 1).getTime() + 12 * 3600000),
      ad: 2, ch: 0, eb: 0,
      company: RS_COMPANIES[0], source: RS_SOURCES[0], pool: RS_POOLS[0]
    };
  }
  function rsNights() {
    return Math.max(1, Math.round((startOfDay(rs.f.out) - startOfDay(rs.f.in)) / 86400000));
  }

  /* ---------- Bảng phòng trống ---------- */
  /* 4 trạng thái ô theo SRS mục 22-25:
       ok    — ratecode áp dụng, bán được
       na    — ratecode KHÔNG áp dụng cho loại phòng: ô trống, xám
       stop  — stop sell: vẫn có giá nhưng không bán ngày xem, xám
       inv0  — inventory loại phòng = 0: xám CẢ CỘT                                   */
  function rsBuildGrid() {
    var types = rs.types.length ? rs.types : rsRoomTypes();
    var codes = rs.policy.length ? rs.policy : RS_RATECODES;
    var n = rsNights();
    var cols = types.map(function (t) {
      var all = ROOMS.filter(function (r) { return r.type === t; });
      var h = rvHash(t + ymd(rs.f.in));
      var inv = (h % 13 === 0) ? 0 : Math.max(0, all.length - (h % 3));
      return {
        t: t, total: all.length, inv: inv, free: inv,
        straight: Math.max(0, inv - (h % 2)),
        sold: inv ? (h % (all.length + 1)) : 0
      };
    });
    var rows = codes.map(function (rc, ri) {
      return {
        rc: rc,
        minStay: 1 + (ri % 3),
        cells: cols.map(function (c, ci) {
          var h = rvHash(rc + c.t + ymd(rs.f.in) + ci);
          var unit = 300000 + (h % 18) * 100000;
          var st = c.inv === 0 ? 'inv0' : (h % 13 === 0 ? 'na' : (h % 11 === 0 ? 'stop' : 'ok'));
          return { st: st, unit: unit, total: unit * n };
        })
      };
    });
    return { cols: cols, rows: rows, nights: n };
  }
  function rsHeatClass(v, min, max) {
    if (!rs.heat || max <= min) return '';
    var q = (v - min) / (max - min);
    return ' h' + (q > .75 ? 4 : q > .5 ? 3 : q > .25 ? 2 : 1);
  }
  function rsRenderGrid() {
    var g = rs.grid, el = $('rsTbl');
    if (!el) return;
    if (!g) {
      el.innerHTML = '<tbody><tr><td class="rs-hint">Chọn điều kiện rồi bấm <b>Tìm kiếm</b> để xem danh sách phòng trống.</td></tr></tbody>';
      return;
    }
    var vals = [];
    g.rows.forEach(function (r) {
      r.cells.forEach(function (c) { if (c.st === 'ok') vals.push(rs.price === 'avg' ? c.unit : c.total); });
    });
    var min = vals.length ? Math.min.apply(null, vals) : 0;
    var max = vals.length ? Math.max.apply(null, vals) : 0;

    var h = '<thead><tr><th></th>' + g.cols.map(function (c) {
      return '<th' + (c.inv === 0 ? ' class="is-off"' : '') + ' title="' + esc(c.t) + '">' +
        '<span class="tname">' + esc(c.t) + '</span></th>';
    }).join('') + '</tr></thead><tbody>';

    h += '<tr class="rs-sum"><td>Phòng trống/<br>thẳng chặng<span class="low">*</span></td>' +
      g.cols.map(function (c) {
        return '<td' + (c.inv === 0 ? ' class="is-off"' : '') + '>' + c.free + '/' +
          '<span class="' + (c.straight < c.free ? 'low' : '') + '">' + c.straight + '</span></td>';
      }).join('') + '</tr>';
    h += '<tr class="rs-sum"><td>Phòng bán</td>' +
      g.cols.map(function (c) { return '<td' + (c.inv === 0 ? ' class="is-off"' : '') + '>' + c.sold + '</td>'; }).join('') + '</tr>';

    g.rows.forEach(function (r, ri) {
      h += '<tr><td><span class="rcname">' + esc(r.rc) +
        '<button class="i" type="button" data-rsrc="' + ri + '" aria-label="Mô tả ' + esc(r.rc) + '">' +
        '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><circle cx="12" cy="12" r="9"/><path d="M12 11v5M12 8h.01" stroke-linecap="round"/></svg>' +
        '</button></span></td>';
      r.cells.forEach(function (c, ci) {
        var col = g.cols[ci];
        var live = c.st === 'ok';
        var v = rs.price === 'avg' ? c.unit : c.total;
        var sel = rsInCart(r.rc, col.t);
        var cls = 'rate' + (live ? rsHeatClass(v, min, max) : ' is-off') + (sel ? ' is-sel' : '');
        var tip = c.st === 'na' ? 'Ratecode không áp dụng cho loại phòng này'
          : c.st === 'stop' ? 'Stop sell — không mở bán trong khoảng ngày đang xem'
          : c.st === 'inv0' ? 'Loại phòng đã hết phòng (inventory = 0)'
          : 'Bấm để thêm vào giỏ hàng';
        var warn = live && r.minStay > g.nights
          ? '<span class="warn" title="Ngày ở tối thiểu: ' + r.minStay + '">' +
            '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 4l9 16H3z"/><path d="M12 10v4M12 17h.01"/></svg></span>'
          : '';
        h += '<td class="' + cls + '"' +
          (live ? ' data-rsri="' + ri + '" data-rsci="' + ci + '" role="button" tabindex="0"' : '') +
          ' title="' + tip + '">' + (c.st === 'na' ? '' : rsMoneyShort(v) + warn) + '</td>';
      });
      h += '</tr>';
    });
    el.innerHTML = h + '</tbody>';
  }
  function rsInCart(rc, type) {
    return rs.cart.some(function (seg) {
      return seg.rooms.some(function (x) { return x.rc === rc && x.type === type; });
    });
  }
  function rsMoneyShort(v) {
    if (v >= 1000000) return (Math.round(v / 100000) / 10).toString().replace('.', ',') + 'M';
    if (v >= 1000) return Math.round(v / 1000) + 'K';
    return money(v);
  }

  /* ---------- Giỏ hàng: gom theo CHẶNG (SRS mục 26 · 29 · 30 · 31 · 32) ---------- */
  function rsSegKey(a, z) { return ymd(a) + 'T' + hm(a) + '|' + ymd(z) + 'T' + hm(z); }
  function rsSegNights(s) { return Math.max(1, Math.round((startOfDay(s.out) - startOfDay(s.in)) / 86400000)); }
  function rsCartTotals() {
    var rooms = 0, ad = 0, ch = 0, eb = 0, total = 0;
    rs.cart.forEach(function (seg) {
      var n = rsSegNights(seg);
      seg.rooms.forEach(function (x) {
        rooms += x.qty;
        ad += x.ad * x.qty; ch += x.ch * x.qty; eb += x.eb * x.qty;
        total += x.unit * n * x.qty + x.sur * x.qty;
      });
    });
    return { rooms: rooms, ad: ad, ch: ch, eb: eb, guests: ad + ch + eb, total: total };
  }
  function rsSegGuests(seg) {
    var ad = 0, ch = 0;
    seg.rooms.forEach(function (x) { ad += x.ad * x.qty; ch += x.ch * x.qty; });
    return ad + 'NL - ' + ch + 'TE';
  }
  function rsRenderCart() {
    var t = rsCartTotals();
    ['', '2', '3'].forEach(function (sfx) {
      var title = $('rsCartTitle' + sfx), body = $('rsCartBody' + sfx), tot = $('rsCartTotal' + sfx);
      if (!body) return;
      /* Tiêu đề đếm SỐ PHÒNG đã đặt — Sum(số phòng) theo SRS mục 27 */
      if (title) title.textContent = 'Giỏ hàng (' + t.rooms + ')';
      if (sfx === '' && $('rsClearAll')) $('rsClearAll').hidden = !rs.cart.length;

      if (!rs.cart.length) {
        body.innerHTML =
          '<div class="rs-empty"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">' +
          '<path d="M3 9h18l-1.5 10.5a2 2 0 01-2 1.5H6.5a2 2 0 01-2-1.5z"/><path d="M8 9V6a4 4 0 018 0v3"/></svg>' +
          '<p>Giỏ hàng trống</p></div>';
        if (tot) tot.hidden = true;
        return;
      }
      body.innerHTML = rs.cart.map(function (seg, si) {
        var n = rsSegNights(seg);
        return '<section class="rs-seg-card">' +
          '<header class="rs-seg-h">' +
            '<span class="dt">' + dmy(seg.in) + ' ' + hm(seg.in) + ' → ' + dmy(seg.out) + ' ' + hm(seg.out) +
            ' <span class="nt">(' + n + ' đêm)</span></span>' +
            '<span class="gs">' + rsSegGuests(seg) + '</span>' +
          '</header>' +
          seg.rooms.map(function (x, ri) {
            var line = x.unit * n * x.qty + x.sur * x.qty;
            return '<div class="rs-item">' +
              '<div class="top"><span class="nm">' + esc(x.type) + '<br><span class="rc">' + esc(x.rc) + '</span></span>' +
              (sfx === '' ? '<button class="rs-del" type="button" data-rsdel="' + si + '_' + ri + '" aria-label="Xoá ' + esc(x.type) + '">' +
                '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"><path d="M4 7h16M10 11v6M14 11v6M6 7l1 13h10l1-13M9 7V4h6v3"/></svg></button>' : '') +
              '</div>' +
              '<div class="rs-row"><span class="k">Số lượng khách</span>' +
                (sfx === '' ? rsSpin('ad', si, ri, x.ad, 1, 10) : '<span class="v">' + x.ad + ' NL</span>') + '</div>' +
              '<div class="rs-row"><span class="k">Số lượng phòng</span>' +
                (sfx === '' ? rsSpin('qty', si, ri, x.qty, 1, 20) : '<span class="v">' + x.qty + '</span>') + '</div>' +
              '<div class="rs-row"><span class="k">Phụ thu người lớn</span><span class="v">' + money(x.sur) + '</span></div>' +
              '<div class="rs-row"><span class="k">Giá phòng</span><span class="v">' + money(x.unit) + '</span></div>' +
              '<div class="rs-row"><span class="k">Tổng tiền</span><span class="v">' + money(line) + '</span></div>' +
              '</div>';
          }).join('') +
          '</section>';
      }).join('');
      if (tot) {
        tot.hidden = false;
        tot.innerHTML =
          '<div class="r"><span>Tổng số lượng khách</span><span class="v">' + t.guests + '</span></div>' +
          '<div class="r"><span>Tổng số phòng</span><span class="v">' + t.rooms + '</span></div>' +
          '<div class="r big"><span>Tổng tiền</span><span class="v">' + money(t.total) + '</span></div>';
      }
    });
    var nx = $('rsNext1');
    if (nx) nx.disabled = !rs.cart.length;
  }
  function rsSpin(kind, si, ri, v, min, max) {
    return '<span class="rs-spin">' +
      '<button type="button" data-rsspin="' + kind + '" data-rsi="' + si + '_' + ri + '" data-rsd="-1"' + (v <= min ? ' disabled' : '') + ' aria-label="Giảm">−</button>' +
      '<span class="n">' + v + '</span>' +
      '<button type="button" data-rsspin="' + kind + '" data-rsi="' + si + '_' + ri + '" data-rsd="1"' + (v >= max ? ' disabled' : '') + ' aria-label="Tăng">+</button>' +
      '</span>';
  }
  function rsAdd(ri, ci) {
    var g = rs.grid, row = g.rows[ri], col = g.cols[ci], cell = row.cells[ci];
    /* Mỗi khoảng ngày đến–ngày đi là MỘT CHẶNG; phòng cùng chặng gom chung */
    var key = rsSegKey(rs.f.in, rs.f.out);
    var seg = rs.cart.filter(function (s) { return rsSegKey(s.in, s.out) === key; })[0];
    if (!seg) { seg = { in: new Date(rs.f.in), out: new Date(rs.f.out), rooms: [] }; rs.cart.push(seg); }
    var ex = seg.rooms.filter(function (x) { return x.rc === row.rc && x.type === col.t; })[0];
    if (ex) ex.qty = Math.min(20, ex.qty + 1);
    else seg.rooms.push({
      type: col.t, rc: row.rc, unit: cell.unit,
      ad: rs.f.ad, ch: rs.f.ch, eb: rs.f.eb,
      sur: rs.f.ad > 2 ? (rs.f.ad - 2) * 150000 : 0, qty: 1
    });
    rsRenderGrid(); rsRenderCart();
    toast('Đã thêm ' + col.t + ' · ' + row.rc + ' vào giỏ hàng');
  }

  /* ---------- Bước ---------- */
  function rsGoStep(n) {
    if (n === 2 && !rs.cart.length) { toast('Vui lòng chọn ít nhất 1 phòng', true); return; }
    rs.step = n;
    [1, 2, 3].forEach(function (i) { $('rsP' + i).hidden = i !== n; });
    Array.prototype.forEach.call(document.querySelectorAll('#rsSteps .rs-step'), function (b) {
      var i = Number(b.dataset.rsstep);
      b.classList.toggle('is-cur', i === n);
      b.classList.toggle('is-done', i < n);
      b.disabled = i > n && !(i === 2 && rs.cart.length);
      if (i === n) b.setAttribute('aria-current', 'step'); else b.removeAttribute('aria-current');
      var no = b.querySelector('.no');
      no.innerHTML = i < n
        ? '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg>'
        : String(i);
    });
    if (n === 2) rsFillStep2();
    if (n === 3) rsFillStep3();
    rsRenderCart();
  }
  function rsFillStep2() {
    $('rsHotel').textContent = rs.f.branch;
    $('rsRoChannel').textContent = rs.f.channel;
    $('rsRoSource').textContent = rs.f.source;
    $('rsRoCompany').textContent = rs.f.company;
    $('rsRoIn').textContent = dmy(rs.f.in) + ' ' + hm(rs.f.in);
    $('rsRoOut').textContent = dmy(rs.f.out) + ' ' + hm(rs.f.out);
    /* Khách gợi ý — lấy từ danh sách khách đã lưu trú */
    var seen = {}, rows = [];
    BOOKINGS.forEach(function (b) {
      if (b.st === 'repair' || seen[b.name] || rows.length >= 5) return;
      seen[b.name] = 1;
      var h = rvHash(b.name);
      rows.push({ n: b.name, age: 22 + (h % 40), dob: new Date(1980 + (h % 25), h % 12, 1 + (h % 27)),
        p: b.guests && b.guests[0] && b.guests[0].p ? b.guests[0].p : '09' + String(10000000 + (h % 89999999)).slice(0, 8),
        cccd: String(1000 + (h % 8999)) + String(100000000 + (h % 899999999)) });
    });
    $('rsSugg').innerHTML =
      '<thead><tr><th>Họ tên khách</th><th>Tuổi</th><th>Ngày sinh</th><th>Số điện thoại</th><th>CCCD</th></tr></thead><tbody>' +
      rows.map(function (r) {
        return '<tr data-rssug="' + esc(r.n) + '" data-rsphone="' + esc(r.p) + '" style="cursor:pointer">' +
          '<td>' + esc(r.n) + '</td><td>' + r.age + '</td><td>' + dmy(r.dob) + '</td>' +
          '<td>' + esc(r.p) + '</td><td>' + esc(r.cccd) + '</td></tr>';
      }).join('') + '</tbody>';
  }
  function rsFillStep3() {
    var t = rsCartTotals();
    var disc = rs.promo === 'Giảm 5% khách quen' ? Math.round(t.total * .05)
      : rs.promo === 'Giảm 10% đoàn' ? Math.round(t.total * .10)
      : rs.promo === 'Ưu đãi mùa thấp điểm' ? Math.round(t.total * .15) : 0;
    var sur = rs.cart.reduce(function (s, x) { return s + x.sur * x.qty; }, 0);
    var tax = Math.round((t.total - disc) * RS_TAX);
    $('rsPTotal').textContent = money(t.total);
    $('rsPDisc').textContent = disc ? '-' + money(disc) : '0';
    $('rsPSur').textContent = money(sur);
    $('rsPTax').textContent = money(tax);
    $('rsPDue').textContent = money(t.total - disc + tax);
    var amt = $('rsPayAmt');
    if (amt && !amt.value) amt.value = money(t.total - disc + tax);
  }

  /* ---------- Loại phòng nhiều lựa chọn ---------- */
  function rsPaintChips(id, list, empty) {
    var el = $(id);
    if (!el) return;
    var show = list.slice(0, 3), extra = list.length - show.length;
    el.innerHTML = (list.length
      ? show.map(function (t) {
          return '<span class="rs-chip">' + esc(t) +
            '<button class="x" type="button" data-rsunopt="' + esc(t) + '" data-rsfor="' + id + '" aria-label="Bỏ ' + esc(t) + '">' +
            '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round"><path d="M6 6l12 12M18 6L6 18"/></svg></button></span>';
        }).join('') + (extra > 0 ? '<span class="more">+' + extra + '</span>' : '')
      : '<span class="more">' + esc(empty) + '</span>') +
      '<span class="cv" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg></span>';
  }
  function rsPaintTypes() {
    rsPaintChips('rsTypes', rs.types, 'Tất cả loại phòng');
    rsPaintChips('rsPolicy', rs.policy, 'Tất cả chính sách giá');
  }

  var rsMsKind = null;
  function rsMsOpen(kind) {
    var m = $('rsMsMenu'), host = $(kind === 'policy' ? 'rsPolicy' : 'rsTypes');
    if (!m || !host) return;
    if (m.classList.contains('is-open') && rsMsKind === kind) { rsMsClose(); return; }
    rsMsKind = kind;
    var items = kind === 'policy' ? RS_RATECODES : rsRoomTypes();
    var cur = kind === 'policy' ? rs.policy : rs.types;
    m.innerHTML = items.map(function (t) {
      return '<label><input type="checkbox" data-rsopt="' + esc(t) + '"' + (cur.indexOf(t) >= 0 ? ' checked' : '') + '>' +
        '<span class="box" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span>' +
        '<span>' + esc(t) + '</span></label>';
    }).join('');
    m.classList.add('is-open');
    host.setAttribute('aria-expanded', 'true');
    var r = host.getBoundingClientRect();
    m.style.left = Math.max(8, Math.min(r.left, window.innerWidth - m.offsetWidth - 8)) + 'px';
    m.style.top = Math.min(r.bottom + 4, window.innerHeight - m.offsetHeight - 8) + 'px';
    m.style.minWidth = r.width + 'px';
  }
  function rsMsClose() {
    var m = $('rsMsMenu');
    if (m) m.classList.remove('is-open');
    rsMsKind = null;
    ['rsTypes', 'rsPolicy'].forEach(function (id) {
      if ($(id)) $(id).setAttribute('aria-expanded', 'false');
    });
  }

  /* ---------- Mở màn ---------- */
  function rsShow() {
    if (!rs.f) rsInitOnce();
    rsRenderGrid(); rsRenderCart();
    var prev = document.querySelector('.nav-child.is-selected, .nav-item.is-selected');
    if (prev) { prev.classList.remove('is-selected'); prev.removeAttribute('aria-current'); }
    var el = document.querySelector('.nav-child[data-nav="bk-new"]');
    if (el) {
      var grp = el.closest('.nav-group');
      if (grp) grp.classList.remove('is-closed');
      el.classList.add('is-selected'); el.setAttribute('aria-current', 'page');
    }
  }
  function rsSyncDates() {
    $('rsIn').querySelector('.v').textContent = dmy(rs.f.in) + ' ' + hm(rs.f.in);
    $('rsOut').querySelector('.v').textContent = dmy(rs.f.out) + ' ' + hm(rs.f.out);
  }
  function rsInitOnce() {
    rs.f = rsDefaultFilter();
    vsDef('rsBranch', RS_BRANCHES, rs.f.branch);
    vsDef('rsChannel', RS_CHANNELS, rs.f.channel);
    vsDef('rsCompany', RS_COMPANIES, rs.f.company);
    vsDef('rsSource', RS_SOURCES, rs.f.source);
    vsDef('rsPool', RS_POOLS, rs.f.pool);
    vsDef('rsMarket', RS_MARKETS, RS_MARKETS[0]);
    vsDef('rsStatus', RS_STATUS, RS_STATUS[1]);
    vsDef('rsRelease', RS_RELEASE, RS_RELEASE[0]);
    vsDef('rsStaff', RS_STAFFS, RS_STAFFS[0]);
    vsDef('rsSalut', RS_SALUTS, RS_SALUTS[0]);
    vsDef('rsGender', RS_GENDERS, RS_GENDERS[0]);
    vsDef('rsNation', RS_NATIONS, RS_NATIONS[0]);
    vsDef('rsDocType', RS_DOCS, RS_DOCS[0]);
    vsDef('rsPromo', RS_PROMOS, RS_PROMOS[0]);
    vsDef('rsPayType', RS_PAYTYPES, RS_PAYTYPES[0]);
    vsDef('rsCurrency', RS_CURRENCIES, RS_CURRENCIES[0]);
    if (QB_SEL['rsPromo']) QB_SEL['rsPromo'].onChange = function (v) { rs.promo = v; rsFillStep3(); };
    rsPaintTypes();
    rsSyncDates();
  }

  function bindRs() {
    if (!$('rsCard')) return;

    $('rsSearch').addEventListener('click', function () {
      ['branch', 'channel', 'company', 'source', 'pool'].forEach(function (k) {
        var id = 'rs' + k.charAt(0).toUpperCase() + k.slice(1);
        var v = vsVal(id);
        if (v !== null && v !== undefined) rs.f[k] = v;
      });
      rs.f.ad = Math.max(1, Number($('rsAd').value) || 1);
      rs.f.ch = Math.max(0, Number($('rsCh').value) || 0);
      rs.f.eb = Math.max(0, Number($('rsEb').value) || 0);
      if (startOfDay(rs.f.in) >= startOfDay(rs.f.out)) { toast('Ngày đi phải sau ngày đến', true); return; }
      rs.grid = rsBuildGrid();
      rsRenderGrid();
      toast('Tìm thấy ' + rs.grid.cols.length + ' loại phòng trống cho ' + rs.grid.nights + ' đêm');
    });

    $('rsTbl').addEventListener('click', function (e) {
      var c = e.target.closest('[data-rsri]');
      if (c) rsAdd(Number(c.dataset.rsri), Number(c.dataset.rsci));
    });
    $('rsTbl').addEventListener('keydown', function (e) {
      if (e.key !== 'Enter' && e.key !== ' ') return;
      var c = e.target.closest('[data-rsri]');
      if (c) { e.preventDefault(); rsAdd(Number(c.dataset.rsri), Number(c.dataset.rsci)); }
    });

    $('rsHeat').addEventListener('change', function () { rs.heat = this.checked; rsRenderGrid(); });
    document.querySelectorAll('[data-rsprice]').forEach(function (b) {
      b.addEventListener('click', function () {
        rs.price = b.dataset.rsprice;
        document.querySelectorAll('[data-rsprice]').forEach(function (x) {
          x.setAttribute('aria-pressed', x === b ? 'true' : 'false');
        });
        rsRenderGrid();
      });
    });

    /* Giỏ hàng: xoá 1 phòng · spinner · xoá tất cả */
    var pick = function (v) { var a = String(v).split('_'); return { s: Number(a[0]), r: Number(a[1]) }; };
    $('rsCartBody').addEventListener('click', function (e) {
      var d = e.target.closest('[data-rsdel]');
      if (d) {
        var k = pick(d.dataset.rsdel), seg = rs.cart[k.s], it = seg.rooms[k.r];
        seg.rooms.splice(k.r, 1);
        if (!seg.rooms.length) rs.cart.splice(k.s, 1);   /* chặng rỗng thì bỏ luôn */
        rsRenderGrid(); rsRenderCart();
        toast('Đã xoá ' + it.type + ' khỏi giỏ hàng');
        return;
      }
      var s = e.target.closest('[data-rsspin]');
      if (s) {
        var kk = pick(s.dataset.rsi), x = rs.cart[kk.s].rooms[kk.r], dd = Number(s.dataset.rsd);
        if (s.dataset.rsspin === 'qty') x.qty = Math.min(20, Math.max(1, x.qty + dd));
        else { x.ad = Math.min(10, Math.max(1, x.ad + dd)); x.sur = x.ad > 2 ? (x.ad - 2) * 150000 : 0; }
        rsRenderCart();
      }
    });

    /* Xoá tất cả — có popup cảnh báo, KHÔNG xoá bộ lọc (SRS mục 28) */
    $('rsClearAll').addEventListener('click', function () {
      confirmDlg('Xoá tất cả phòng đã chọn',
        'Toàn bộ phòng trong giỏ hàng sẽ bị xoá. Bộ lọc đang chọn vẫn được giữ nguyên. Bạn có chắc chắn?',
        'Xoá tất cả', function () {
          rs.cart = [];
          rsRenderGrid(); rsRenderCart();
          toast('Đã xoá tất cả phòng khỏi giỏ hàng');
        });
    });

    /* Tooltip mô tả ratecode (SRS mục 20) */
    $('rsTbl').addEventListener('click', function (e) {
      var i = e.target.closest('[data-rsrc]');
      if (!i) return;
      e.stopPropagation();
      var row = rs.grid.rows[Number(i.dataset.rsrc)];
      toast(row.rc + ' — ngày ở tối thiểu ' + row.minStay + ' đêm, áp dụng cho khách lẻ và khách đoàn');
    });

    /* Chọn ngày */
    $('rsCard').addEventListener('click', function (e) {
      var d = e.target.closest('[data-rsdate]');
      if (!d) return;
      var k = d.dataset.rsdate;
      var cur = k === 'in' ? rs.f.in : k === 'out' ? rs.f.out : new Date(1995, 0, 1);
      dtpOpen(d, cur, k === 'in' || k === 'out', function (nd) {
        if (k === 'in') { rs.f.in = nd; rsSyncDates(); }
        else if (k === 'out') { rs.f.out = nd; rsSyncDates(); }
        else d.querySelector('.v').textContent = dmy(nd);
      });
    });

    /* Loại phòng · Chính sách giá — cùng một engine chọn nhiều */
    [['rsTypes', 'types'], ['rsPolicy', 'policy']].forEach(function (pair) {
      var id = pair[0], kind = pair[1];
      $(id).addEventListener('click', function (e) {
        var x = e.target.closest('[data-rsunopt]');
        if (x) {
          e.stopPropagation();
          rs[kind] = rs[kind].filter(function (t) { return t !== x.dataset.rsunopt; });
          rsPaintTypes();
          return;
        }
        e.stopPropagation();
        rsMsOpen(kind);
      });
      $(id).addEventListener('keydown', function (e) {
        if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); rsMsOpen(kind); }
      });
    });
    $('rsMsMenu').addEventListener('change', function (e) {
      var c = e.target.closest('[data-rsopt]');
      if (!c || !rsMsKind) return;
      var t = c.dataset.rsopt, arr = rs[rsMsKind];
      if (c.checked) { if (arr.indexOf(t) < 0) arr.push(t); }
      else rs[rsMsKind] = arr.filter(function (x) { return x !== t; });
      rsPaintTypes();
    });

    /* Khách gợi ý → điền nhanh */
    $('rsSugg').addEventListener('click', function (e) {
      var r = e.target.closest('[data-rssug]');
      if (!r) return;
      var nm = r.dataset.rssug.trim().split(' ');
      $('rsFirst').value = nm.pop();
      $('rsLast').value = nm.join(' ');
      $('rsPhone').value = r.dataset.rsphone;
      toast('Đã điền thông tin khách ' + r.dataset.rssug);
    });

    /* Hình thức thanh toán */
    $('rsMethods').addEventListener('click', function (e) {
      var b = e.target.closest('[data-rspm]');
      if (!b) return;
      rs.pm = b.dataset.rspm;
      Array.prototype.forEach.call($('rsMethods').children, function (x) {
        x.setAttribute('aria-pressed', x === b ? 'true' : 'false');
      });
    });

    /* Các nút điều hướng */
    $('rsCard').addEventListener('click', function (e) {
      var b = e.target.closest('[data-rsact]');
      if (!b) return;
      var a = b.dataset.rsact;
      if (a === 'next1') rsGoStep(2);
      else if (a === 'back2') rsGoStep(1);
      else if (a === 'back3') rsGoStep(2);
      else if (a === 'next2') {
        if (!$('rsFirst').value.trim()) { toast('Vui lòng nhập Tên khách', true); return; }
        if (!$('rsPhone').value.trim()) { toast('Vui lòng nhập Số điện thoại', true); return; }
        rsGoStep(3);
      } else if (a === 'paylater') {
        if (!$('rsFirst').value.trim()) { toast('Vui lòng nhập Tên khách', true); return; }
        rsFinish(false);
      } else if (a === 'finish') rsFinish(true);
      else if (a === 'cancel') {
        confirmDlg('Huỷ tạo đặt phòng', 'Toàn bộ thông tin đang nhập sẽ bị mất. Bạn có chắc chắn muốn huỷ?', 'Huỷ đặt phòng', function () {
          rs.cart = []; rs.grid = null; rs.f = rsDefaultFilter(); rs.types = []; rs.policy = [];
          rsPaintTypes(); rsSyncDates(); rsGoStep(1); rsRenderGrid(); rsRenderCart();
          toast('Đã huỷ tạo đặt phòng');
        });
      }
    });

    $('rsSteps').addEventListener('click', function (e) {
      var b = e.target.closest('[data-rsstep]');
      if (b && !b.disabled) rsGoStep(Number(b.dataset.rsstep));
    });

    document.addEventListener('click', function (e) {
      if (!e.target.closest('#rsMsMenu') && !e.target.closest('#rsTypes') && !e.target.closest('#rsPolicy')) rsMsClose();
    }, true);
    window.addEventListener('resize', rsMsClose);
  }

  function rsFinish(paid) {
    var t = rsCartTotals();
    var name = ($('rsLast').value + ' ' + $('rsFirst').value).trim();
    /* confirmDlg tự escape nội dung → truyền văn bản thuần, không kèm thẻ HTML */
    confirmDlg('Hoàn tất đặt phòng',
      'Tạo booking cho khách ' + (name || '—') + ' với ' + t.rooms + ' phòng, tổng tiền ' + money(t.total) + '?',
      'Hoàn tất', function () {
        toast('Đã tạo đặt phòng thành công' + (paid ? ' và ghi nhận thanh toán' : ', thanh toán sau'));
        rs.cart = []; rs.grid = null; rs.f = rsDefaultFilter(); rs.types = []; rs.policy = [];
        rsPaintTypes(); rsSyncDates(); rsGoStep(1); rsRenderGrid(); rsRenderCart();
      });
  }


  /* =====================================================================
     26. KHAI BÁO CÔNG AN / HẢI QUAN (RR06) — SRS RR06 + Figma 35977:17486
     Mở từ nút "Sử dụng báo cáo" của BC107 trên màn Danh sách báo cáo.
     Dùng lại: dtpOpen, toast, openGenDlg, dữ liệu khách của rl.js.
     ===================================================================== */

  /* --- Định nghĩa cột: 12 cột mặc định (def) luôn bật, còn lại tuỳ chọn --- */
  var CA_COLS = [
    /* Nhóm thông tin khách */
    { k: 'salut',    t: 'Danh xưng',            g: 'Nhóm thông tin khách' },
    { k: 'last',     t: 'Họ',                   g: 'Nhóm thông tin khách' },
    { k: 'first',    t: 'Tên',                  g: 'Nhóm thông tin khách' },
    { k: 'name',     t: 'Họ tên',               g: 'Nhóm thông tin khách', def: true },
    { k: 'gender',   t: 'Giới tính',            g: 'Nhóm thông tin khách', def: true },
    { k: 'dob',      t: 'Ngày sinh',            g: 'Nhóm thông tin khách', def: true },
    { k: 'phone',    t: 'Số điện thoại',        g: 'Nhóm thông tin khách', def: true },
    { k: 'ward',     t: 'Phường/ Xã/ Đặc khu',  g: 'Nhóm thông tin khách' },
    { k: 'district', t: 'Quận/ Huyện',          g: 'Nhóm thông tin khách' },
    { k: 'prov',     t: 'Tỉnh/Thành phố',       g: 'Nhóm thông tin khách', def: true },
    { k: 'country',  t: 'Quốc gia',             g: 'Nhóm thông tin khách' },
    { k: 'ccode',    t: 'Mã quốc gia',          g: 'Nhóm thông tin khách', def: true },
    { k: 'ncode',    t: 'Mã quốc tịch',         g: 'Nhóm thông tin khách', def: true },
    { k: 'addr',     t: 'Địa chỉ',              g: 'Nhóm thông tin khách' },
    { k: 'nation',   t: 'Quốc tịch',            g: 'Nhóm thông tin khách' },
    { k: 'postal',   t: 'Mã bưu điện',          g: 'Nhóm thông tin khách' },
    { k: 'ar',       t: 'Mã công nợ',           g: 'Nhóm thông tin khách' },
    { k: 'company',  t: 'Công ty trực thuộc',   g: 'Nhóm thông tin khách' },
    { k: 'note',     t: 'Ghi chú',              g: 'Nhóm thông tin khách' },
    { k: 'pref',     t: 'Sở thích',             g: 'Nhóm thông tin khách' },
    { k: 'cccd',     t: 'Số CCCD',              g: 'Nhóm thông tin khách', def: true },
    { k: 'cccdName', t: 'Họ và tên CCCD',       g: 'Nhóm thông tin khách' },
    { k: 'issued',   t: 'Ngày cấp',             g: 'Nhóm thông tin khách' },
    { k: 'issuedBy', t: 'Nơi cấp',              g: 'Nhóm thông tin khách' },
    { k: 'pp',       t: 'Số passport',          g: 'Nhóm thông tin khách', def: true },
    { k: 'ppIssued', t: 'Ngày cấp passport',    g: 'Nhóm thông tin khách' },
    { k: 'ppExp',    t: 'Ngày hết hạn passport', g: 'Nhóm thông tin khách' },
    { k: 'stays',    t: 'Số lần lưu trú',       g: 'Nhóm thông tin khách' },
    /* Nhóm thông tin booking */
    { k: 'ota',      t: 'Mã OTA',               g: 'Nhóm thông tin booking' },
    { k: 'cms',      t: 'Mã CMS',               g: 'Nhóm thông tin booking' },
    { k: 'code',     t: 'Mã booking',           g: 'Nhóm thông tin booking' },
    { k: 'src',      t: 'Cty/OTA',              g: 'Nhóm thông tin booking' },
    { k: 'bkst',     t: 'Trạng thái booking',   g: 'Nhóm thông tin booking' },
    { k: 'sale',     t: 'NV Sale',              g: 'Nhóm thông tin booking' },
    /* Nhóm thông tin phòng */
    { k: 'rtype',    t: 'Loại phòng',           g: 'Nhóm thông tin phòng' },
    { k: 'room',     t: 'Tên Phòng',            g: 'Nhóm thông tin phòng', def: true },
    { k: 'rateCode', t: 'Rate Code',            g: 'Nhóm thông tin phòng' },
    { k: 'rateName', t: 'Rate Name',            g: 'Nhóm thông tin phòng' },
    { k: 'rtc',      t: 'RTC',                  g: 'Nhóm thông tin phòng' },
    { k: 'pax',      t: 'SL khách phòng',       g: 'Nhóm thông tin phòng' },
    { k: 'inAt',     t: 'Thời gian đến',        g: 'Nhóm thông tin phòng', def: true },
    { k: 'outAt',    t: 'Thời gian đi',         g: 'Nhóm thông tin phòng', def: true }
  ];
  /* Thứ tự 12 cột mặc định theo SRS 3.1.4 mục 6 */
  var CA_DEF_ORDER = ['name', 'gender', 'dob', 'phone', 'prov', 'ccode', 'ncode', 'cccd', 'pp', 'room', 'inAt', 'outAt'];

  var CA_GENDERS = ['Nam', 'Nữ'];
  var CA_ST_LB = { booked: 'Đã đặt', inhouse: 'Đang ở', checkout: 'Đã trả' };

  var ca = {
    open: false, rep: null, cols: null, page: 1, perPage: 22,
    f: null, applied: null, ms: {}
  };

  function caDefaultFilter() {
    return {
      from: startOfDay(HOTEL_NOW), to: startOfDay(HOTEL_NOW),
      last: '', first: '', dobFrom: null, dobTo: null,
      gender: [], phone: '', email: '', prov: [], country: [], nation: [],
      cccd: '', pp: '', rtype: [], room: []
    };
  }
  function caClone(f) {
    var o = {};
    for (var k in f) if (Object.prototype.hasOwnProperty.call(f, k)) {
      o[k] = (f[k] instanceof Date) ? new Date(f[k].getTime()) : (f[k] instanceof Array ? f[k].slice() : f[k]);
    }
    return o;
  }

  /* ---- Nguồn dữ liệu: mọi khách trên mọi booking (lọc theo bộ lọc) ---- */
  function caAllRows() {
    var out = [];
    BOOKINGS.forEach(function (b) {
      if (b.st === 'repair') return;
      var r = roomById(b.room) || {};
      (b.guests || []).forEach(function (g) {
        var v = rlGuestInfo(g, b);
        var parts = String(v.name).trim().split(/\s+/);
        var first = parts.length > 1 ? parts[parts.length - 1] : parts[0];
        var last = parts.length > 1 ? parts.slice(0, -1).join(' ') : '';
        var rl2 = (typeof QB_RATES !== 'undefined' && QB_RATES[r.type]) ? QB_RATES[r.type][0] : null;
        out.push({
          salut: v.gender === 'Nam' ? 'Mr' : 'Ms',
          last: last, first: first, name: v.name, gender: v.gender, dob: v.dob,
          phone: v.phone, ward: '', district: '', prov: v.prov,
          country: v.nation, ccode: v.cc, ncode: v.cc, addr: '', nation: v.nation,
          postal: '', ar: '', company: b.company || '', note: b.note || '', pref: '',
          cccd: v.cccd, cccdName: v.cccd ? v.name : '', issued: '', issuedBy: '',
          pp: v.passport, ppIssued: '', ppExp: '', stays: 1,
          ota: b.ota || '', cms: b.cms || '', code: b.code || '', src: b.src || '',
          bkst: CA_ST_LB[b.st] || b.st, sale: b.staff || '',
          rtype: typeName(r.type), room: b.room,
          rateCode: rl2 ? rl2.id : '', rateName: rl2 ? rl2.name : '', rtc: r.type || '',
          pax: (b.adults || 0) + 'NL/' + (b.children || 0) + 'TE/' + (b.infants || 0) + 'EB',
          inAt: b.in, outAt: b.out,
          _in: b.in, _out: b.out
        });
      });
    });
    return out;
  }

  function caHas(list, v) { return !list.length || list.indexOf(v) >= 0; }
  function caFilterRows() {
    var f = ca.applied;
    var lo = startOfDay(f.from), hi = addDays(startOfDay(f.to), 1);
    return caAllRows().filter(function (r) {
      if (!(r._in < hi && lo < r._out)) return false;
      if (f.last && rlNorm(r.last).indexOf(rlNorm(f.last)) < 0) return false;
      if (f.first && rlNorm(r.first).indexOf(rlNorm(f.first)) < 0) return false;
      if (f.phone && String(r.phone).replace(/\D/g, '').indexOf(f.phone.replace(/\D/g, '')) < 0) return false;
      if (f.email && rlNorm(r.email || '').indexOf(rlNorm(f.email)) < 0) return false;
      if (f.cccd && String(r.cccd).indexOf(f.cccd) < 0) return false;
      if (f.pp && rlNorm(r.pp).indexOf(rlNorm(f.pp)) < 0) return false;
      if (!caHas(f.gender, r.gender)) return false;
      if (!caHas(f.prov, r.prov)) return false;
      if (!caHas(f.country, r.country)) return false;
      if (!caHas(f.nation, r.nation)) return false;
      if (!caHas(f.rtype, r.rtype)) return false;
      if (!caHas(f.room, r.room)) return false;
      if (f.dobFrom && startOfDay(r.dob) < startOfDay(f.dobFrom)) return false;
      if (f.dobTo && startOfDay(r.dob) > startOfDay(f.dobTo)) return false;
      return true;
    });
  }

  function caActiveCols() {
    return CA_COLS.filter(function (c) { return ca.cols[c.k]; })
      .sort(function (a, b) {
        var ia = CA_DEF_ORDER.indexOf(a.k), ib = CA_DEF_ORDER.indexOf(b.k);
        if (ia >= 0 && ib >= 0) return ia - ib;
        if (ia >= 0) return -1;
        if (ib >= 0) return 1;
        return CA_COLS.indexOf(a) - CA_COLS.indexOf(b);
      });
  }
  function caCell(r, k) {
    var v = r[k];
    if (v instanceof Date) return (k === 'inAt' || k === 'outAt') ? (dmy(v) + ' ' + hm(v)) : dmy(v);
    return v == null ? '' : String(v);
  }

  function caCondText() {
    var f = ca.applied;
    var lb = function (arr) { return arr.length ? arr.join(', ') : 'Tất cả'; };
    return '<b>Giới tính:</b> <i>' + esc(lb(f.gender)) + '</i> ; <b>Tỉnh/Thành phố:</b> <i>' + esc(lb(f.prov)) +
      '</i> ; <b>Quốc gia:</b> <i>' + esc(lb(f.country)) + '</i> ; <b>Quốc tịch:</b> <i>' + esc(lb(f.nation)) +
      '</i> ; <b>Loại phòng:</b> <i>' + esc(lb(f.rtype)) + '</i> ;<br><b>Tên phòng:</b> <i>' + esc(lb(f.room)) + '</i>';
  }

  function caRender() {
    if (!ca.open) return;
    var cols = caActiveCols();
    var rows = caFilterRows();
    var pages = Math.max(1, Math.ceil(rows.length / ca.perPage));
    if (ca.page > pages) ca.page = pages;
    var from = (ca.page - 1) * ca.perPage;
    var page = rows.slice(from, from + ca.perPage);

    $('caA4Code').textContent = ca.rep && ca.rep.sheet ? ca.rep.sheet : 'BC06';
    $('caA4Title').textContent = ca.rep ? ca.rep.name : 'Khai báo CA/Hải quan';
    $('caA4Range').textContent = dmy(ca.applied.from) + ' - ' + dmy(ca.applied.to);
    $('caA4Cond').innerHTML = caCondText();

    $('caHead').innerHTML = '<th class="stt">STT</th>' +
      cols.map(function (c) { return '<th>' + esc(c.t) + '</th>'; }).join('');
    $('caBody2').innerHTML = page.length
      ? page.map(function (r, i) {
          return '<tr><td class="stt">' + (from + i + 1) + '</td>' +
            cols.map(function (c) { return '<td>' + esc(caCell(r, c.k)) + '</td>'; }).join('') + '</tr>';
        }).join('')
      : '<tr><td class="stt"></td><td colspan="' + cols.length +
        '" style="text-align:center">Không có dữ liệu phù hợp với điều kiện lọc.</td></tr>';

    $('caPrintAt').textContent = dmy(HOTEL_NOW) + ' ' + hm(HOTEL_NOW);
    $('caPages').textContent = '/' + pages;
    $('caPage').value = String(ca.page);
    $('caFootPg').textContent = ca.page + '/' + pages;
    document.querySelectorAll('#caCard [data-capg]').forEach(function (b) {
      var k = b.dataset.capg;
      b.disabled = (k === 'first' || k === 'prev') ? ca.page === 1 : ca.page === pages;
    });
  }

  /* ---- Multi-select ---- */
  function caMsOptions(key) {
    var uniq = function (arr) {
      var seen = {}, out = [];
      arr.forEach(function (x) { if (x && !seen[x]) { seen[x] = 1; out.push(x); } });
      return out.sort();
    };
    var rows = caAllRows();
    if (key === 'gender') return CA_GENDERS.slice();
    if (key === 'prov') return uniq(rows.map(function (r) { return r.prov; }));
    if (key === 'country') return uniq(rows.map(function (r) { return r.country; }));
    if (key === 'nation') return uniq(rows.map(function (r) { return r.nation; }));
    if (key === 'rtype') return TYPES.map(function (t) { return t.name; });
    if (key === 'room') return ROOMS.map(function (r) { return r.id; });
    return [];
  }
  function caMsSync(key) {
    var host = document.querySelector('.ca-ms[data-cams="' + key + '"]');
    if (!host) return;
    var sel = ca.f[key] || [];
    var btn = host.querySelector('.ca-msbtn');
    var v = btn.querySelector('.v');
    v.textContent = sel.length ? (sel.length <= 2 ? sel.join(', ') : sel.length + ' đã chọn') : 'Tất cả';
    v.classList.toggle('ph', false);
  }
  function caMsBuild() {
    document.querySelectorAll('#caCard .ca-ms').forEach(function (host) {
      var key = host.dataset.cams;
      host.innerHTML = '<button class="ca-msbtn" type="button" aria-haspopup="listbox" aria-expanded="false"' +
        ' aria-label="' + esc(host.dataset.calabel || key) + '"><span class="v">Tất cả</span>' +
        '<span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg></span></button>';
      host.querySelector('.ca-msbtn').addEventListener('click', function (e) {
        e.stopPropagation(); caMsToggle(key, this);
      });
      caMsSync(key);
    });
  }
  var caMsCur = null;
  function caMsToggle(key, btn) {
    if (caMsCur === key) { caMsClose(); return; }
    caMsCur = key;
    btn.setAttribute('aria-expanded', 'true');
    caMsPaint(key, btn);
  }
  function caMsPaint(key, btn) {
    var opts = caMsOptions(key), sel = ca.f[key] || [];
    var m = $('caMsMenu');
    m.innerHTML = '<label class="' + (!sel.length ? 'is-on' : '') + '"><input type="checkbox" data-caall="1"' +
      (!sel.length ? ' checked' : '') + '><span class="box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span><span>Tất cả</span></label><hr>' +
      opts.map(function (o) {
        var on = sel.indexOf(o) >= 0;
        return '<label class="' + (on ? 'is-on' : '') + '"><input type="checkbox" data-caopt="' + esc(o) + '"' +
          (on ? ' checked' : '') + '><span class="box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span><span>' + esc(o) + '</span></label>';
      }).join('');
    m.classList.add('is-open');
    var r = btn.getBoundingClientRect();
    m.style.left = '0px'; m.style.top = '0px';
    m.style.minWidth = Math.max(200, r.width) + 'px';
    var w = m.offsetWidth, h = m.offsetHeight;
    var top = r.bottom + 4;
    if (top + h > window.innerHeight - 8) top = Math.max(8, r.top - h - 4);
    m.style.top = top + 'px';
    m.style.left = Math.min(Math.max(8, r.left), window.innerWidth - w - 8) + 'px';
  }
  function caMsClose() {
    caMsCur = null;
    $('caMsMenu').classList.remove('is-open');
    document.querySelectorAll('#caCard .ca-msbtn').forEach(function (b) { b.setAttribute('aria-expanded', 'false'); });
  }

  /* ---- Mở / đóng màn ---- */
  function caOpen(rep) {
    ca.rep = rep || { name: 'Khai báo CA/Hải quan', sheet: 'BC06' };
    if (!ca.cols) {
      ca.cols = {};
      CA_COLS.forEach(function (c) { ca.cols[c.k] = !!c.def; });
    }
    ca.f = caDefaultFilter();
    ca.applied = caClone(ca.f);
    ca.page = 1;
    ca.open = true;
    $('caBd').classList.add('is-open');
    $('caCard').classList.add('is-open');
    caMsBuild();
    caSyncFilterUI();
    caRender();
    if (typeof rvZoomSet === 'function') rvZoomSet('ca', 1);
    setTimeout(function () { $('caClose').focus(); }, 0);
  }
  function caClose() {
    ca.open = false;
    caMsClose();
    $('caCard').classList.remove('is-open');
    $('caBd').classList.remove('is-open');
  }

  function caSyncFilterUI() {
    $('caFromT').textContent = dmy(ca.f.from);
    $('caToT').textContent = dmy(ca.f.to);
    var dt = $('caDobT');
    if (ca.f.dobFrom && ca.f.dobTo) { dt.textContent = dmy(ca.f.dobFrom) + ' - ' + dmy(ca.f.dobTo); dt.classList.remove('ph'); }
    else { dt.textContent = 'Từ ngày - Đến ngày'; dt.classList.add('ph'); }
    $('caLast').value = ca.f.last; $('caFirst').value = ca.f.first;
    $('caPhone').value = ca.f.phone; $('caEmail').value = ca.f.email;
    $('caCccd').value = ca.f.cccd; $('caPp').value = ca.f.pp;
    ['gender', 'prov', 'country', 'nation', 'rtype', 'room'].forEach(caMsSync);
  }

  var CA_BAD_NAME = /[0-9!@#$%^&*()_+={}\[\]|\\:;"'<>,.?\/~`]/;
  function caApply() {
    if (CA_BAD_NAME.test($('caLast').value) || CA_BAD_NAME.test($('caFirst').value)) {
      var bad = CA_BAD_NAME.test($('caLast').value) ? $('caLast') : $('caFirst');
      bad.classList.add('is-error');
      bad.title = 'Không được nhập số và ký tự đặc biệt';
      toast('Họ và Tên không được chứa số hoặc ký tự đặc biệt', true);
      return;
    }
    $('caLast').classList.remove('is-error'); $('caFirst').classList.remove('is-error');
    if (startOfDay(ca.f.from) > startOfDay(ca.f.to)) {
      toast('Từ ngày phải nhỏ hơn hoặc bằng Đến ngày', true);
      return;
    }
    ca.f.last = $('caLast').value.trim();
    ca.f.first = $('caFirst').value.trim();
    ca.f.phone = $('caPhone').value.trim();
    ca.f.email = $('caEmail').value.trim();
    ca.f.cccd = $('caCccd').value.trim();
    ca.f.pp = $('caPp').value.trim();
    ca.applied = caClone(ca.f);
    ca.page = 1;
    caRender();
    toast('Đã áp dụng bộ lọc');
  }

  function caDownload(kind) {
    var cols = caActiveCols(), rows = caFilterRows();
    var head = ['STT'].concat(cols.map(function (c) { return c.t; }));
    var body = rows.map(function (r, i) {
      return [String(i + 1)].concat(cols.map(function (c) { return caCell(r, c.k); }));
    });
    var text, mime, ext;
    if (kind === 'XML') {
      text = '<?xml version="1.0" encoding="UTF-8"?>\n<KhaiBao>\n' + body.map(function (row) {
        return '  <Khach>\n' + row.map(function (v, i) {
          return '    <' + 'C' + i + '>' + esc(v) + '</' + 'C' + i + '>';
        }).join('\n') + '\n  </Khach>';
      }).join('\n') + '\n</KhaiBao>';
      mime = 'application/xml'; ext = 'xml';
    } else {
      text = [head].concat(body).map(function (row) {
        return row.map(function (v) { return '"' + String(v).replace(/"/g, '""') + '"'; }).join(',');
      }).join('\n');
      mime = 'text/csv'; ext = (kind === 'CSV') ? 'csv' : 'csv';
    }
    try {
      var a = document.createElement('a');
      a.href = URL.createObjectURL(new Blob(['﻿' + text], { type: mime + ';charset=utf-8' }));
      a.download = 'KhaiBao_CA_HaiQuan_' + dmy(ca.applied.from).replace(/\//g, '') + '.' + ext;
      document.body.appendChild(a); a.click();
      setTimeout(function () { URL.revokeObjectURL(a.href); a.remove(); }, 0);
      toast('Đã tải xuống báo cáo định dạng ' + kind);
    } catch (e) { toast('Không tải xuống được, vui lòng thử lại', true); }
  }

  function bindCa() {
    if (!$('caCard')) return;

    $('caClose').addEventListener('click', caClose);
    $('caBd').addEventListener('click', caClose);
    $('caApply').addEventListener('click', caApply);
    $('caReload').addEventListener('click', function () { caRender(); toast('Đã tải lại dữ liệu báo cáo'); });
    $('caClear').addEventListener('click', function () {
      ca.f = caDefaultFilter(); ca.applied = caClone(ca.f); ca.page = 1;
      caSyncFilterUI(); caRender(); toast('Đã xoá bộ lọc');
    });
    $('caUndo').addEventListener('click', function () { toast('Hoàn tác thao tác gần nhất'); });
    $('caRedo').addEventListener('click', function () { toast('Làm lại thao tác vừa hoàn tác'); });
    $('caPrint').addEventListener('click', function () { window.print(); });

    $('caDlBtn').addEventListener('click', function (e) {
      e.stopPropagation();
      var m = $('caDlMenu'), on = m.classList.toggle('is-open');
      this.setAttribute('aria-expanded', String(on));
    });
    $('caDlMenu').addEventListener('click', function (e) {
      var b = e.target.closest('[data-cadl]'); if (!b) return;
      $('caDlMenu').classList.remove('is-open');
      $('caDlBtn').setAttribute('aria-expanded', 'false');
      caDownload(b.dataset.cadl);
    });

    document.querySelectorAll('#caCard [data-capg]').forEach(function (b) {
      b.addEventListener('click', function () {
        var pages = Math.max(1, Math.ceil(caFilterRows().length / ca.perPage));
        var k = b.dataset.capg;
        if (k === 'first') ca.page = 1;
        else if (k === 'prev') ca.page = Math.max(1, ca.page - 1);
        else if (k === 'next') ca.page = Math.min(pages, ca.page + 1);
        else ca.page = pages;
        caRender();
      });
    });
    $('caPage').addEventListener('change', function () {
      var pages = Math.max(1, Math.ceil(caFilterRows().length / ca.perPage));
      ca.page = Math.min(pages, Math.max(1, parseInt(this.value, 10) || 1));
      caRender();
    });

    $('caFrom').addEventListener('click', function () {
      dtpOpen($('caFrom'), ca.f.from, false, function (d) { ca.f.from = d; caSyncFilterUI(); });
    });
    $('caTo').addEventListener('click', function () {
      dtpOpen($('caTo'), ca.f.to, false, function (d) { ca.f.to = d; caSyncFilterUI(); });
    });
    $('caDob').addEventListener('click', function () {
      dtpOpen($('caDob'), ca.f.dobFrom || new Date(1990, 0, 1), false, function (d) {
        ca.f.dobFrom = d;
        dtpOpen($('caDob'), ca.f.dobTo || d, false, function (d2) {
          ca.f.dobTo = d2 < d ? d : d2; caSyncFilterUI();
        });
      });
    });

    ['caLast', 'caFirst'].forEach(function (id) {
      $(id).addEventListener('input', function () {
        this.classList.toggle('is-error', CA_BAD_NAME.test(this.value));
        this.title = CA_BAD_NAME.test(this.value) ? 'Không được nhập số và ký tự đặc biệt' : '';
      });
    });
    ['caPhone', 'caCccd'].forEach(function (id) {
      $(id).addEventListener('input', function () { this.value = this.value.replace(/\D/g, ''); });
    });
    $('caFBody').addEventListener('keydown', function (e) {
      if (e.key === 'Enter') { e.preventDefault(); caApply(); }
    });

    $('caMsMenu').addEventListener('click', function (e) {
      var all = e.target.closest('[data-caall]');
      if (all) { ca.f[caMsCur] = []; caMsSync(caMsCur); caMsPaint(caMsCur, document.querySelector('.ca-ms[data-cams="' + caMsCur + '"] .ca-msbtn')); return; }
      var o = e.target.closest('[data-caopt]');
      if (o) {
        var key = caMsCur, v = o.dataset.caopt, arr = ca.f[key] || [];
        var i = arr.indexOf(v);
        if (i >= 0) arr.splice(i, 1); else arr.push(v);
        if (key === 'room' && arr.length > 10) { arr.pop(); toast('Chỉ được chọn tối đa 10 phòng', true); }
        ca.f[key] = arr;
        caMsSync(key);
        caMsPaint(key, document.querySelector('.ca-ms[data-cams="' + key + '"] .ca-msbtn'));
      }
    });
    document.addEventListener('click', function (e) {
      if (caMsCur && !e.target.closest('#caMsMenu') && !e.target.closest('.ca-ms')) caMsClose();
      if (!e.target.closest('.ca-dl')) {
        $('caDlMenu').classList.remove('is-open');
        $('caDlBtn').setAttribute('aria-expanded', 'false');
      }
    }, true);

    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape' || !ca.open) return;
      if (typeof dtpIsOpen === 'function' && dtpIsOpen()) return;
      e.preventDefault();
      if (caMsCur) { caMsClose(); return; }
      caClose();
    });
  }


  /* ================= BÁO CÁO DÙNG CHUNG — RR03 Bữa ăn (BC104) · RR04 Công nợ chi tiết (BC105) =================
     Một nơi duy nhất dựng TRANG BÁO CÁO A4, dùng chung cho:
       - khung xem trước ở màn Danh sách báo cáo (rl)
       - màn xem báo cáo dạng sidebar 80% (#rvCard)
     ============================================================================================================ */

  var RV_MEALS = [
    { k: 'sang', t: 'Bữa sáng', lb: 'Sáng' },
    { k: 'trua', t: 'Bữa trưa', lb: 'Trưa' },
    { k: 'toi', t: 'Bữa tối', lb: 'Tối' }
  ];
  var RV_NATION = { VN: 'Việt Nam', KR: 'Hàn Quốc', JP: 'Nhật Bản', GB: 'Anh', US: 'Hoa Kỳ', CN: 'Trung Quốc', FR: 'Pháp' };

  /* ---------- Dữ liệu mô phỏng: phái sinh ổn định từ booking, KHÔNG hard-code ---------- */
  function rvHash(s) {
    var h = 0, t = String(s);
    for (var i = 0; i < t.length; i++) h = (h * 31 + t.charCodeAt(i)) >>> 0;
    return h;
  }
  function rvBookingsIn(from, to) {
    return BOOKINGS.filter(function (b) {
      return b.st !== 'repair' && b.in <= addDays(startOfDay(to), 1) && startOfDay(from) < b.out;
    });
  }

  /* ===== RR03 — BÁO CÁO BỮA ĂN ===== */
  function rvMealData(f) {
    var list = rvBookingsIn(f.from, f.to);
    var meals = RV_MEALS.map(function (m, mi) {
      var rows = list.filter(function (b, bi) { return (rvHash(b.id) + bi) % 3 !== ((mi + 1) % 3) || mi === 0; })
        .map(function (b) {
          var h = rvHash(b.id + m.k);
          var ad = b.adults || 1, ch = b.children || 0, inf = b.infants || 0;
          var guests = (b.guests || []).map(function (g) {
            var gh = rvHash(g.n + b.id);
            return {
              name: g.n,
              gender: (gh % 2) ? 'Nam' : 'Nữ',
              inAt: b.in, outAt: b.out,
              nation: RV_NATION[g.cc || 'VN'] || 'Việt Nam'
            };
          });
          if (!guests.length) guests = [{ name: b.name, gender: (h % 2) ? 'Nam' : 'Nữ', inAt: b.in, outAt: b.out, nation: 'Việt Nam' }];
          return {
            code: b.code || ('#' + b.id),
            src: b.src || b.company || '',
            group: b.group || (b.company ? 'Đoàn ' + b.company : ''),
            rate: 'Rate code ' + (1 + (h % 3)),
            ad: (h % (ad + 1)), adT: ad,
            ch: (h % (ch + 1)), chT: ch,
            eb: (h % (inf + 1)), ebT: inf,
            note: b.note ? ('Ghi chú: ' + String(b.note).split('.')[0] + '.') : '',
            guests: guests
          };
        });
      return { m: m, rows: rows };
    });
    return meals;
  }
  function rvMealFilter(meals, f) {
    if (!f.meal || f.meal === 'Tất cả') return meals;
    return meals.filter(function (x) { return x.m.t === f.meal; });
  }
  function rvSumCell(n, d) {
    var p = d ? Math.round(n / d * 100) : 0;
    return '<td class="n">' + n + '</td><td class="p">' + p + '%</td>';
  }
  /* Bảng tổng kết dạng: nhãn | NL(số,%) | TE | EB | Tổng */
  function rvSumTable(head, rows, tot) {
    var h =
      '<table class="rv-tbl"><thead>' +
      '<tr><th class="l" rowspan="2">' + esc(head) + '</th><th colspan="8">Số suất</th></tr>' +
      '<tr><th colspan="2">NL</th><th colspan="2">TE</th><th colspan="2">EB</th><th colspan="2">Tổng</th></tr>' +
      '</thead><tbody>';
    rows.forEach(function (r) {
      h += '<tr><td>' + esc(r.k) + '</td>' +
        rvSumCell(r.ad, tot.ad) + rvSumCell(r.ch, tot.ch) + rvSumCell(r.eb, tot.eb) +
        rvSumCell(r.ad + r.ch + r.eb, tot.ad + tot.ch + tot.eb) + '</tr>';
    });
    h += '<tr class="rv-total"><td>Tổng</td>' +
      '<td class="n" colspan="2">' + tot.ad + '</td><td class="n" colspan="2">' + tot.ch + '</td>' +
      '<td class="n" colspan="2">' + tot.eb + '</td><td class="n" colspan="2">' + (tot.ad + tot.ch + tot.eb) + '</td></tr>';
    return h + '</tbody></table>';
  }
  function rvMealBody(f) {
    var meals = rvMealFilter(rvMealData(f), f);
    var h =
      '<table class="rv-tbl"><thead>' +
      '<tr><th class="l" rowspan="2">Mã Booking</th><th class="l" rowspan="2">Công ty/OTA</th>' +
      '<th class="l" rowspan="2">Tên đoàn</th><th class="l" rowspan="2">Rate code</th>' +
      '<th colspan="3">Số suất</th>' +
      '<th class="l" rowspan="2">Tên khách</th><th rowspan="2">Giới tính</th>' +
      '<th rowspan="2">Ngày đến</th><th rowspan="2">Ngày đi</th><th rowspan="2">Quốc tịch</th></tr>' +
      '<tr><th>NL</th><th>TE</th><th>EB</th></tr>' +
      '</thead><tbody>';

    var gt = { ad: 0, adT: 0, ch: 0, chT: 0, eb: 0, ebT: 0 };
    var byMeal = [], byGender = {}, byNation = {}, byRate = {};

    meals.forEach(function (mx) {
      var mt = { ad: 0, adT: 0, ch: 0, chT: 0, eb: 0, ebT: 0 };
      mx.rows.forEach(function (r) {
        mt.ad += r.ad; mt.adT += r.adT; mt.ch += r.ch; mt.chT += r.chT; mt.eb += r.eb; mt.ebT += r.ebT;
        byRate[r.rate] = byRate[r.rate] || { ad: 0, ch: 0, eb: 0 };
        byRate[r.rate].ad += r.ad; byRate[r.rate].ch += r.ch; byRate[r.rate].eb += r.eb;
        r.guests.forEach(function (g) {
          byGender[g.gender] = byGender[g.gender] || { ad: 0, ch: 0, eb: 0 };
          byNation[g.nation] = byNation[g.nation] || { ad: 0, ch: 0, eb: 0 };
        });
        var g0 = r.guests[0];
        if (g0) {
          byGender[g0.gender].ad += r.ad; byGender[g0.gender].ch += r.ch; byGender[g0.gender].eb += r.eb;
          byNation[g0.nation].ad += r.ad; byNation[g0.nation].ch += r.ch; byNation[g0.nation].eb += r.eb;
        }
      });
      byMeal.push({ k: mx.m.lb, ad: mt.ad, ch: mt.ch, eb: mt.eb });
      gt.ad += mt.ad; gt.adT += mt.adT; gt.ch += mt.ch; gt.chT += mt.chT; gt.eb += mt.eb; gt.ebT += mt.ebT;

      /* Dòng nhóm bữa ăn */
      h += '<tr class="rv-meal"><td colspan="4">' + esc(mx.m.t) + '</td>' +
        '<td class="c">' + mt.ad + '/' + mt.adT + '</td><td class="c">' + mt.ch + '/' + mt.chT + '</td>' +
        '<td class="c">' + mt.eb + '/' + mt.ebT + '</td><td colspan="5"></td></tr>';

      if (!mx.rows.length) {
        h += '<tr><td colspan="12" class="c">Không có suất ăn nào trong khoảng thời gian này.</td></tr>';
        return;
      }
      mx.rows.forEach(function (r) {
        var n = r.guests.length;
        r.guests.forEach(function (g, gi) {
          h += '<tr>';
          if (gi === 0) {
            h += '<td rowspan="' + n + '">' + esc(r.code) + '</td>' +
              '<td class="wrap" rowspan="' + n + '">' + esc(r.src) + '</td>' +
              '<td class="wrap" rowspan="' + n + '">' + esc(r.group) + '</td>' +
              '<td rowspan="' + n + '">' + esc(r.rate) + '</td>' +
              '<td class="c" rowspan="' + n + '">' + r.ad + '/' + r.adT + '</td>' +
              '<td class="c" rowspan="' + n + '">' + r.ch + '/' + r.chT + '</td>' +
              '<td class="c" rowspan="' + n + '">' + r.eb + '/' + r.ebT + '</td>';
          }
          h += '<td class="wrap">' + esc(g.name) + '</td><td class="c">' + esc(g.gender) + '</td>' +
            '<td class="c">' + dmy(g.inAt) + '</td><td class="c">' + dmy(g.outAt) + '</td>' +
            '<td class="c">' + esc(g.nation) + '</td></tr>';
        });
        if (r.note) h += '<tr class="rv-note"><td colspan="12">' + esc(r.note) + '</td></tr>';
      });
    });

    h += '<tr class="rv-total"><td colspan="4">Tổng</td>' +
      '<td class="c">' + gt.ad + '/' + gt.adT + '</td><td class="c">' + gt.ch + '/' + gt.chT + '</td>' +
      '<td class="c">' + gt.eb + '/' + gt.ebT + '</td><td colspan="5"></td></tr>';
    h += '</tbody></table>';

    var tot = { ad: gt.ad, ch: gt.ch, eb: gt.eb };
    var toRows = function (o) {
      return Object.keys(o).map(function (k) { return { k: k, ad: o[k].ad, ch: o[k].ch, eb: o[k].eb }; });
    };
    h += '<section class="rv-a4-sec"><h3>Tổng kết</h3><div class="rv-sum">' +
      rvSumTable('Bữa ăn', byMeal, tot) +
      rvSumTable('Giới tính', toRows(byGender), tot) +
      rvSumTable('Quốc tịch', toRows(byNation), tot) +
      rvSumTable('Ratecode', toRows(byRate), tot) +
      '</div></section>';
    return h;
  }
  function rvMealCond(f) {
    return '<b>Loại bữa ăn:</b> <i>' + esc(f.meal || 'Tất cả') + '</i> ; ' +
      '<b>Toà nhà:</b> <i>' + esc(f.building || 'Tất cả') + '</i> ; ' +
      '<b>Tầng:</b> <i>' + esc(f.floor || 'Tất cả') + '</i> ; ' +
      '<b>Quốc tịch:</b> <i>' + esc(f.nation || 'Tất cả') + '</i> ; ' +
      '<b>Giới tính:</b> <i>' + esc(f.gender || 'Tất cả') + '</i>' +
      (f.note ? ' ; <b>Ghi chú:</b> <i>' + esc(f.note) + '</i>' : '');
  }

  /* ===== RR04 — BÁO CÁO CÔNG NỢ CHI TIẾT ===== */
  function rvDebtData(f) {
    var list = rvBookingsIn(f.from, f.to);
    var map = {}, order = [];
    list.forEach(function (b) {
      var nm = b.company || ('Khách lẻ - ' + b.name);
      if (!map[nm]) {
        var h = rvHash(nm);
        map[nm] = { acc: '#' + String(100000 + (h % 899999)), open0: (h % 5) * 900000, name: nm, open: 0, rows: [] };
        order.push(nm);
      }
      var hh = rvHash(b.id + nm);
      var amount = b.total || 0;
      /* Trả trong kỳ không vượt quá giá trị giao dịch; khấu trừ chỉ áp phần còn lại.
         Nợ trong kỳ = toàn bộ giá trị phát sinh (theo công thức cân đối của SRS). */
      var paid = (b.pay === 'paid') ? amount : Math.min(b.paid || 0, amount);
      var ded = (paid < amount && hh % 3 === 0) ? Math.min(900000, amount - paid) : 0;
      map[nm].rows.push({
        at: b.in,
        slip: '#' + String(90000 + (hh % 9999)),
        bk: b.code || ('#' + b.id),
        who: b.name,
        amount: amount,
        cur: 'VND',
        paid: paid,
        ded: ded,
        debt: amount,
        note: b.inote ? ('Ghi chú: ' + b.inote) : ''
      });
    });
    var accs = order.map(function (k) { return map[k]; });
    if (f.acc && f.acc !== 'Tất cả') accs = accs.filter(function (a) { return a.name === f.acc; });
    accs.forEach(function (a) {
      /* Tài khoản mà mọi giao dịch đều đã thanh toán đủ thì không có nợ đầu kỳ */
      var settled = a.rows.every(function (r) { return r.paid + r.ded >= r.debt; });
      a.open = settled ? 0 : a.open0;
      var bal = a.open;
      /* Dư cuối kỳ = Nợ đầu kỳ - Trả trong kỳ - Khấu trừ nợ + Nợ trong kỳ */
      a.rows.forEach(function (r) { bal = bal - r.paid - r.ded + r.debt; r.bal = bal; });
      a.close = bal;
    });
    if (f.skipPaid) accs = accs.filter(function (a) { return a.close > 0; });
    return accs;
  }
  function rvDebtBody(f) {
    var accs = rvDebtData(f);
    var h =
      '<table class="rv-tbl"><thead><tr>' +
      '<th class="l">Ngày</th><th class="l">Phiếu</th><th class="l">Booking</th><th class="l">Đối tượng</th>' +
      '<th>Số tiền</th><th>Tiền Tệ</th><th>Trả trong kỳ</th><th>Khấu trừ nợ</th><th>Nợ trong kỳ</th><th>Dư cuối kỳ</th>' +
      '</tr></thead><tbody>';
    var T = { paid: 0, ded: 0, debt: 0, bal: 0 };
    if (!accs.length) {
      h += '<tr><td colspan="10" class="c">Không có công nợ phát sinh trong khoảng thời gian này.</td></tr>';
    }
    accs.forEach(function (a) {
      h += '<tr class="rv-grp"><td>' + esc(a.acc) + '</td><td colspan="5">' + esc(a.name) + '</td>' +
        '<td colspan="3">Nợ đầu kỳ</td><td class="r">' + money(a.open) + '</td></tr>';
      a.rows.forEach(function (r) {
        T.paid += r.paid; T.ded += r.ded; T.debt += r.debt;
        h += '<tr><td class="wrap">' + dmy(r.at) + '<br>' + dmy(r.at) + '</td>' +
          '<td>' + esc(r.slip) + '</td><td>' + esc(r.bk) + '</td><td class="wrap">' + esc(r.who) + '</td>' +
          '<td class="r">' + money(r.amount) + '</td><td class="c">' + esc(r.cur) + '</td>' +
          '<td class="r">' + money(r.paid) + '</td><td class="r">' + money(r.ded) + '</td>' +
          '<td class="r">' + money(r.debt) + '</td><td class="r">' + money(r.bal) + '</td></tr>';
        if (r.note) h += '<tr class="rv-note"><td colspan="10">' + esc(r.note) + '</td></tr>';
      });
      T.bal += a.close;
    });
    h += '<tr class="rv-total"><td colspan="6">TỔNG</td>' +
      '<td class="r">' + money(T.paid) + '</td><td class="r">' + money(T.ded) + '</td>' +
      '<td class="r">' + money(T.debt) + '</td><td class="r">' + money(T.bal) + '</td></tr>';
    return h + '</tbody></table>';
  }
  function rvDebtCond(f) {
    return '<b>Tài khoản nợ:</b> <i>' + esc(f.acc || 'Tất cả') + '</i> ; ' +
      '<b>Điều kiện:</b> <i>' + (f.skipPaid ? 'Bỏ qua các công ty hết nợ' : 'Hiển thị tất cả công ty') + '</i>';
  }

  /* ===== RR02 — BÁO CÁO CÔNG SUẤT PHÒNG (BC103) ===== */
  function rvOccData(f) {
    var rows = [], d = startOfDay(f.from), stop = startOfDay(f.to);
    var guard = 0;
    while (d <= stop && guard++ < 400) {
      var day = new Date(d.getTime());
      var h = rvHash(ymd(day));
      /* Số phòng sẵn sàng = số phòng vật lý - số phòng đang sửa (theo SRS mục 16) */
      var repair = BOOKINGS.filter(function (b) {
        return b.st === 'repair' && b.in <= addDays(day, 1) && day < b.out;
      }).length;
      var avail = ROOMS.length - repair;

      var inhouse = BOOKINGS.filter(function (b) {
        return b.st !== 'repair' && b.in <= addDays(day, 1) && day < b.out;
      });
      var night = inhouse.length;
      var rev = inhouse.reduce(function (s, b) { return s + (b.total || 0) / Math.max(1, nights(b.in, b.out)); }, 0);
      rev = Math.round(rev / 1000) * 1000;

      var hourCnt = h % 5;
      var hourRev = hourCnt * ((h % 7 + 2) * 120000);
      var dayCnt = h % 4;
      var dayRev = dayCnt * ((h % 5 + 2) * 150000);

      rows.push({
        d: day, avail: avail, night: night,
        occ: avail ? Math.round(night / avail * 100) : 0,
        adr: night ? Math.round(rev / night) : 0,
        revpar: avail ? Math.round(rev / avail) : 0,
        hourCnt: hourCnt, hourAdr: hourCnt ? Math.round(hourRev / hourCnt) : 0,
        dayCnt: dayCnt, dayAdr: dayCnt ? Math.round(dayRev / dayCnt) : 0
      });
      d = addDays(d, 1);
    }
    return rows;
  }
  function nights(a, z) {
    return Math.max(1, Math.round((startOfDay(z) - startOfDay(a)) / 86400000));
  }
  function rvOccBody(f) {
    var rows = rvOccData(f);
    var h =
      '<table class="rv-tbl"><thead>' +
      '<tr><th class="l" rowspan="2">Ngày</th><th rowspan="2">Tổng số phòng sẵn sàng</th>' +
      '<th colspan="4">Nghỉ qua đêm</th><th colspan="2">Nghỉ giờ</th><th colspan="2">Day use</th></tr>' +
      '<tr><th>Khách ở qua đêm</th><th>Công suất (%)</th><th>ADR</th><th>REV PAR</th>' +
      '<th>Lượt nghỉ giờ</th><th>ADR</th><th>Lượt day use</th><th>ADR</th></tr>' +
      '</thead><tbody>';

    var T = { avail: 0, night: 0, adr: 0, revpar: 0, hourCnt: 0, hourAdr: 0, dayCnt: 0, dayAdr: 0, occ: 0 };
    if (!rows.length) {
      h += '<tr><td colspan="10" class="c">Không có dữ liệu công suất trong khoảng thời gian này.</td></tr>';
    }
    rows.forEach(function (r) {
      T.avail += r.avail; T.night += r.night; T.adr += r.adr; T.revpar += r.revpar;
      T.hourCnt += r.hourCnt; T.hourAdr += r.hourAdr; T.dayCnt += r.dayCnt; T.dayAdr += r.dayAdr;
      h += '<tr><td class="c">' + dmy(r.d) + '</td><td class="n">' + r.avail + '</td>' +
        '<td class="n">' + r.night + '</td><td class="n">' + r.occ + '</td>' +
        '<td class="r">' + money(r.adr) + '</td><td class="r">' + money(r.revpar) + '</td>' +
        '<td class="n">' + r.hourCnt + '</td><td class="r">' + money(r.hourAdr) + '</td>' +
        '<td class="n">' + r.dayCnt + '</td><td class="r">' + money(r.dayAdr) + '</td></tr>';
    });
    /* Công suất của dòng Tổng tính trên tổng số phòng sẵn sàng (không cộng dồn %) */
    var occT = T.avail ? (T.night / T.avail * 100) : 0;
    h += '<tr class="rv-total"><td>Tổng</td><td class="n">' + T.avail + '</td>' +
      '<td class="n">' + T.night + '</td><td class="n">' + occT.toFixed(2).replace('.', ',') + '</td>' +
      '<td class="r">' + money(T.adr) + '</td><td class="r">' + money(T.revpar) + '</td>' +
      '<td class="n">' + T.hourCnt + '</td><td class="r">' + money(T.hourAdr) + '</td>' +
      '<td class="n">' + T.dayCnt + '</td><td class="r">' + money(T.dayAdr) + '</td></tr>';
    return h + '</tbody></table>';
  }
  function rvOccCond(f) {
    return '<b>Ngày công suất:</b> <i>' + dmy(f.from) + ' - ' + dmy(f.to) + '</i>';
  }

  /* ===== Đăng ký báo cáo ===== */
  var RV = {
    BC103: {
      id: 'BC103', sheet: 'BC06', title: 'Báo cáo công suất phòng',
      body: rvOccBody, cond: rvOccCond,
      fields: [
        { k: 'from', t: 'date', lb: 'Từ ngày' },
        { k: 'to', t: 'date', lb: 'Đến ngày' }
      ],
      def: function () {
        return { from: startOfDay(HOTEL_NOW), to: addDays(startOfDay(HOTEL_NOW), 13) };
      }
    },
    BC104: {
      id: 'BC104', sheet: 'BC06', title: 'Báo cáo bữa ăn',
      body: rvMealBody, cond: rvMealCond,
      fields: [
        { k: 'from', t: 'date', lb: 'Từ ngày' },
        { k: 'to', t: 'date', lb: 'Đến ngày' },
        { k: 'meal', t: 'sel', lb: 'Loại bữa ăn', opts: ['Tất cả', 'Bữa sáng', 'Bữa trưa', 'Bữa tối'] },
        { k: 'building', t: 'sel', lb: 'Toà nhà', opts: ['Tất cả', 'Toà A', 'Toà B'] },
        { k: 'floor', t: 'sel', lb: 'Tầng', opts: ['Tất cả', 'Tầng 1', 'Tầng 2', 'Tầng 3', 'Tầng 4'] },
        { k: 'nation', t: 'sel', lb: 'Quốc tịch', opts: ['Tất cả', 'Việt Nam', 'Hàn Quốc', 'Nhật Bản', 'Anh', 'Hoa Kỳ'] },
        { k: 'gender', t: 'sel', lb: 'Giới tính', opts: ['Tất cả', 'Nam', 'Nữ'] },
        { k: 'note', t: 'text', lb: 'Ghi chú', ph: 'Nhập từ khoá tìm kiếm', max: 100 }
      ],
      def: function () {
        return { from: startOfDay(HOTEL_NOW), to: startOfDay(HOTEL_NOW), meal: 'Tất cả', building: 'Tất cả',
          floor: 'Tất cả', nation: 'Tất cả', gender: 'Tất cả', note: '' };
      }
    },
    BC105: {
      id: 'BC105', sheet: 'BC06', title: 'Báo cáo công nợ chi tiết',
      body: rvDebtBody, cond: rvDebtCond,
      fields: [
        { k: 'from', t: 'date', lb: 'Từ ngày' },
        { k: 'to', t: 'date', lb: 'Đến ngày' },
        { k: 'acc', t: 'sel', lb: 'Tài khoản nợ', opts: null },
        { k: 'skipPaid', t: 'check', lb: 'Bỏ qua các công ty hết nợ' }
      ],
      def: function () {
        return { from: startOfDay(HOTEL_NOW), to: startOfDay(HOTEL_NOW), acc: 'Tất cả', skipPaid: false };
      }
    }
  };
  /* ===== RR06 — KHAI BÁO CA/HẢI QUAN (BC107): dùng lại engine của ca.js =====
     Chỉ dùng cho KHUNG XEM TRƯỚC; màn xem đầy đủ vẫn do caOpen() đảm nhiệm. */
  function rvCaBody(f) {
    if (!ca.cols) { ca.cols = {}; CA_COLS.forEach(function (c) { ca.cols[c.k] = !!c.def; }); }
    var saved = ca.applied;
    ca.applied = f;
    var cols = caActiveCols(), rows = caFilterRows();
    ca.applied = saved;
    var h = '<table class="ca-a4-tbl"><thead><tr><th class="stt">STT</th>' +
      cols.map(function (c) { return '<th>' + esc(c.t) + '</th>'; }).join('') + '</tr></thead><tbody>';
    h += rows.length
      ? rows.map(function (r, i) {
          return '<tr><td class="stt">' + (i + 1) + '</td>' +
            cols.map(function (c) { return '<td>' + esc(caCell(r, c.k)) + '</td>'; }).join('') + '</tr>';
        }).join('')
      : '<tr><td class="stt"></td><td colspan="' + cols.length +
        '" style="text-align:center">Không có dữ liệu phù hợp với điều kiện lọc.</td></tr>';
    return h + '</tbody></table>';
  }
  function rvCaCond(f) {
    var saved = ca.applied;
    ca.applied = f;
    var s = caCondText();
    ca.applied = saved;
    return s;
  }
  RV.BC107 = {
    id: 'BC107', sheet: 'BC06', title: 'Khai báo CA/Hải quan',
    body: rvCaBody, cond: rvCaCond, fields: [],
    def: function () { return caDefaultFilter(); }
  };

  function rvAccOptions() {
    var seen = { 'Tất cả': 1 }, out = ['Tất cả'];
    BOOKINGS.forEach(function (b) {
      if (b.st === 'repair') return;
      var nm = b.company || ('Khách lẻ - ' + b.name);
      if (!seen[nm]) { seen[nm] = 1; out.push(nm); }
    });
    return out;
  }

  /* ===== Dựng TRANG A4 (dùng chung cho preview và màn xem) ===== */
  function rvPageHTML(id, f) {
    var R = RV[id];
    if (!R) return '';
    var ff = f || R.def();
    return '' +
      '<header class="ca-a4-top"><span class="brand">ezOne</span><span class="code">' + esc(R.sheet) + '</span></header>' +
      '<h2 class="ca-a4-title">' + esc(R.title) + '</h2>' +
      '<p class="ca-a4-range">' + dmy(ff.from) + ' - ' + dmy(ff.to) + '</p>' +
      '<p class="ca-a4-cond">' + R.cond(ff) + '</p>' +
      '<div class="ca-a4-tblwrap">' + R.body(ff) + '</div>' +
      '<footer class="ca-a4-foot">' +
        '<span class="it">Powered by ezCloud.vn</span>' +
        '<span class="it"><b>Thời gian in:</b> ' + dmy(HOTEL_NOW) + ' ' + hm(HOTEL_NOW) + '</span>' +
        '<span class="it"><b>Người in:</b> Nguyễn Văn Thảo Nguyên</span>' +
        '<span class="pg">1/1</span>' +
      '</footer>';
  }

  /* ===== Thu phóng trang báo cáo — dùng chung cho #caCard và #rvCard ===== */
  var RV_ZOOM_MIN = 0.5, RV_ZOOM_MAX = 2, RV_ZOOM_STEP = 0.1;
  var rvZoomLv = { ca: 1, rv: 1 };

  function rvZoomApply(pfx) {
    var box = $(pfx + 'Zoom'), lb = $(pfx + 'ZoomLb'), page = $(pfx + 'A4');
    if (!box || !page) return;
    var z = rvZoomLv[pfx];
    box.style.transform = z === 1 ? 'none' : 'scale(' + z + ')';
    box.classList.toggle('is-zoomed', z !== 1);
    /* transform không đổi hộp layout → bù chiều cao để thanh cuộn dọc đúng.
       Bề ngang KHÔNG bù: thu nhỏ thì thừa chỗ (không cuộn), phóng to thì cuộn ngang tự nhiên. */
    box.style.marginBottom = z === 1 ? '' : (page.offsetHeight * (z - 1)) + 'px';
    if (lb) lb.textContent = Math.round(z * 100) + '%';
    var out = document.querySelector('[data-' + pfx + 'zoom="out"]');
    var inn = document.querySelector('[data-' + pfx + 'zoom="in"]');
    if (out) out.disabled = z <= RV_ZOOM_MIN + 0.001;
    if (inn) inn.disabled = z >= RV_ZOOM_MAX - 0.001;
  }
  function rvZoomSet(pfx, z) {
    rvZoomLv[pfx] = Math.min(RV_ZOOM_MAX, Math.max(RV_ZOOM_MIN, Math.round(z * 100) / 100));
    rvZoomApply(pfx);
  }
  function rvZoomAct(pfx, act) {
    if (act === 'in') rvZoomSet(pfx, rvZoomLv[pfx] + RV_ZOOM_STEP);
    else if (act === 'out') rvZoomSet(pfx, rvZoomLv[pfx] - RV_ZOOM_STEP);
    else rvZoomSet(pfx, 1);   /* fit / đặt lại: trang vốn đã giãn hết bề ngang */
  }
  function bindZoom(pfx) {
    var lb = $(pfx + 'ZoomLb');
    if (!lb) return;
    document.querySelectorAll('[data-' + pfx + 'zoom]').forEach(function (b) {
      b.addEventListener('click', function () { rvZoomAct(pfx, b.dataset[pfx + 'zoom']); });
    });
    lb.addEventListener('click', function () { rvZoomSet(pfx, 1); });
    rvZoomApply(pfx);
  }

  /* ===== Màn xem báo cáo (sidebar 80%) ===== */
  var rv = { open: false, id: null, f: null };

  function rvFieldHTML(fd, f) {
    var cal = '<span class="ic" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/></svg></span>';
    if (fd.t === 'date') {
      return '<div class="ca-f"><span class="ca-lb">' + esc(fd.lb) + '</span>' +
        '<button class="ca-dt" type="button" id="rvF_' + fd.k + '" data-rvdate="' + fd.k + '" aria-label="' + esc(fd.lb) + '">' +
        '<span class="v">' + dmy(f[fd.k]) + '</span>' + cal + '</button></div>';
    }
    if (fd.t === 'sel') {
      return '<div class="ca-f"><span class="ca-lb">' + esc(fd.lb) + '</span>' +
        '<div class="qb-vs" data-qbsel="rvS_' + fd.k + '" data-qblabel="' + esc(fd.lb) + '"></div></div>';
    }
    if (fd.t === 'check') {
      return '<label class="ca-f ca-f-ck"><input type="checkbox" data-rvck="' + fd.k + '"' + (f[fd.k] ? ' checked' : '') + '>' +
        '<span class="box" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span>' +
        '<span class="lb">' + esc(fd.lb) + '</span></label>';
    }
    return '<div class="ca-f"><span class="ca-lb">' + esc(fd.lb) + '</span>' +
      '<input type="text" data-rvtx="' + fd.k + '" maxlength="' + (fd.max || 50) + '" placeholder="' + esc(fd.ph || 'Nhập') + '" value="' + esc(f[fd.k] || '') + '"></div>';
  }

  function rvBuildFilter() {
    var R = RV[rv.id];
    $('rvFBody').innerHTML = R.fields.map(function (fd) { return rvFieldHTML(fd, rv.f); }).join('');
    R.fields.forEach(function (fd) {
      if (fd.t !== 'sel') return;
      var opts = fd.opts || (fd.k === 'acc' ? rvAccOptions() : ['Tất cả']);
      vsDef('rvS_' + fd.k, opts, rv.f[fd.k] || opts[0]);
      if (QB_SEL['rvS_' + fd.k]) {
        QB_SEL['rvS_' + fd.k].onChange = function (v) { rv.f[fd.k] = v; };
      }
    });
  }

  function rvRender() {
    if (!rv.id) return;
    $('rvA4').innerHTML = rvPageHTML(rv.id, rv.f);
    var t = $('rvA4').querySelector('.ca-a4-title');
    if (t) t.id = 'rvA4Title';
  }

  function rvOpen(it) {
    var id = it && it.id;
    if (!RV[id]) { toast('Màn ' + (it ? it.id + ' - ' + it.name : '') + ' đang được xây dựng'); return; }
    rv.id = id;
    rv.f = RV[id].def();
    rv.open = true;
    $('rvBd').classList.add('is-open');
    $('rvCard').classList.add('is-open');
    rvBuildFilter();
    rvRender();
    rvZoomSet('rv', 1);
    setTimeout(function () { $('rvClose').focus(); }, 0);
  }
  function rvClose() {
    rv.open = false;
    $('rvCard').classList.remove('is-open');
    $('rvBd').classList.remove('is-open');
  }

  function rvApply() {
    RV[rv.id].fields.forEach(function (fd) {
      if (fd.t === 'text') {
        var el = $('rvFBody').querySelector('[data-rvtx="' + fd.k + '"]');
        if (el) rv.f[fd.k] = el.value.trim();
      } else if (fd.t === 'check') {
        var ck = $('rvFBody').querySelector('[data-rvck="' + fd.k + '"]');
        if (ck) rv.f[fd.k] = ck.checked;
      } else if (fd.t === 'sel') {
        var v = vsVal('rvS_' + fd.k);
        if (v !== null && v !== undefined) rv.f[fd.k] = v;
      }
    });
    if (rv.f.from > rv.f.to) { toast('Từ ngày phải nhỏ hơn hoặc bằng Đến ngày', true); return; }
    rvRender();
    toast('Áp dụng thành công');
  }

  function rvDownload(kind) {
    toast('Đã tải xuống báo cáo định dạng ' + kind);
    $('rvDlMenu').classList.remove('is-open');
    $('rvDlBtn').setAttribute('aria-expanded', 'false');
  }

  function bindRv() {
    if (!$('rvCard')) return;
    bindZoom('rv');
    bindZoom('ca');

    $('rvClose').addEventListener('click', rvClose);
    $('rvBd').addEventListener('click', rvClose);
    $('rvApply').addEventListener('click', rvApply);
    $('rvReload').addEventListener('click', function () { rvRender(); toast('Đã tải lại dữ liệu báo cáo'); });
    $('rvClear').addEventListener('click', function () {
      rv.f = RV[rv.id].def(); rvBuildFilter(); rvRender(); toast('Đã xoá bộ lọc');
    });
    $('rvUndo').addEventListener('click', function () { toast('Hoàn tác thao tác gần nhất'); });
    $('rvRedo').addEventListener('click', function () { toast('Làm lại thao tác vừa hoàn tác'); });
    $('rvPrint').addEventListener('click', function () { window.print(); });

    $('rvDlBtn').addEventListener('click', function (e) {
      e.stopPropagation();
      var m = $('rvDlMenu'), on = m.classList.toggle('is-open');
      $('rvDlBtn').setAttribute('aria-expanded', on ? 'true' : 'false');
    });
    $('rvDlMenu').addEventListener('click', function (e) {
      var b = e.target.closest('[data-rvdl]');
      if (b) rvDownload(b.dataset.rvdl);
    });

    /* Chọn ngày */
    $('rvFBody').addEventListener('click', function (e) {
      var d = e.target.closest('[data-rvdate]');
      if (!d) return;
      var k = d.dataset.rvdate;
      dtpOpen(d, rv.f[k], false, function (nd) {
        rv.f[k] = nd;
        d.querySelector('.v').textContent = dmy(nd);
      });
    });

    /* Phân trang — báo cáo mô phỏng luôn 1 trang */
    document.addEventListener('click', function (e) {
      var p = e.target.closest('[data-rvpg]');
      if (p && rv.open) { $('rvPage').value = '1'; }
      if (!rv.open) return;
      if (!e.target.closest('.ca-dl')) {
        $('rvDlMenu').classList.remove('is-open');
        $('rvDlBtn').setAttribute('aria-expanded', 'false');
      }
    }, true);

    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape' || !rv.open) return;
      if (typeof dtpIsOpen === 'function' && dtpIsOpen()) return;
      e.preventDefault();
      rvClose();
    });
  }


  /* ================= POPUP HỒ SƠ NGƯỜI DÙNG — Figma 22255:55976 =================
     Gắn vào ảnh đại diện có sẵn trên thanh tiêu đề của shell (không sửa shell).
     ============================================================================= */

  var PF_LABEL = {
    account: 'Cài đặt tài khoản',
    password: 'Đổi mật khẩu',
    docs: 'Tài liệu hướng dẫn',
    hotline: 'Hotline: 1900 6159',
    email: 'hotro@ezCloud.vn',
    chat: 'Chat với chúng tôi'
  };
  var pfOpen = false;

  function pfAnchor() { return document.querySelector('.topbar .avatar, header .avatar, .avatar'); }

  function pfPlace() {
    var pop = $('pfPop'), a = pfAnchor();
    if (!pop || !a) return;
    var r = a.getBoundingClientRect();
    var w = pop.offsetWidth || 300;
    var left = Math.round(r.right - w);                 /* canh mép phải với ảnh đại diện */
    left = Math.max(8, Math.min(left, window.innerWidth - w - 8));
    var top = Math.round(r.bottom + 8);
    var h = pop.offsetHeight || 0;
    if (top + h > window.innerHeight - 8) top = Math.max(8, r.top - h - 8);
    pop.style.left = left + 'px';
    pop.style.top = top + 'px';
  }

  function pfShow() {
    var pop = $('pfPop'), a = pfAnchor();
    if (!pop || !a) return;
    pop.classList.add('is-open');
    pfOpen = true;
    a.setAttribute('aria-expanded', 'true');
    pfPlace();
  }
  function pfHide() {
    var pop = $('pfPop'), a = pfAnchor();
    if (!pop) return;
    pfMenuClose();
    pop.classList.remove('is-open');
    pfOpen = false;
    if (a) a.setAttribute('aria-expanded', 'false');
  }
  function pfToggle() { pfOpen ? pfHide() : pfShow(); }

  /* ================= NGÔN NGỮ (Figma 19385:5570) ================= */
  var FLAG_VN = '<svg viewBox="0 0 24 24" aria-hidden="true"><rect y="4" width="24" height="16" rx="2" fill="#DA251D"/><path d="M12 8.4l1.15 3.55h3.73l-3.02 2.19 1.16 3.55L12 15.49l-3.02 2.2 1.16-3.55-3.02-2.19h3.73z" fill="#FFFF00"/></svg>';
  var FLAG_GB = '<svg viewBox="0 0 24 24" aria-hidden="true"><rect y="4" width="24" height="16" rx="2" fill="#012169"/><path d="M0 4l24 16M24 4L0 20" stroke="#fff" stroke-width="3.2"/><path d="M0 4l24 16M24 4L0 20" stroke="#C8102E" stroke-width="1.8"/><path d="M12 4v16M0 12h24" stroke="#fff" stroke-width="5.4"/><path d="M12 4v16M0 12h24" stroke="#C8102E" stroke-width="3.2"/></svg>';
  var FLAG_LA = '<svg viewBox="0 0 24 24" aria-hidden="true"><rect y="4" width="24" height="16" rx="2" fill="#CE1126"/><rect y="8" width="24" height="8" fill="#002868"/><circle cx="12" cy="12" r="2.9" fill="#fff"/></svg>';
  var FLAG_KH = '<svg viewBox="0 0 24 24" aria-hidden="true"><rect y="4" width="24" height="16" rx="2" fill="#032EA1"/><rect y="8.8" width="24" height="6.4" fill="#E00025"/><path d="M9.6 13.6h4.8v1.6H9.6zM10.6 10.9h2.8v2.4h-2.8zM11.6 9.4h.8v1.5h-.8z" fill="#fff"/></svg>';

  var PF_LANGS = [
    { k: 'vi', t: 'Tiếng Việt', ic: FLAG_VN },
    { k: 'en', t: 'English', ic: FLAG_GB },
    { k: 'lo', t: 'ພາສາລາວ', ic: FLAG_LA },
    { k: 'km', t: 'ភាសាខ្មែរ', ic: FLAG_KH }
  ];
  var pfLang = 'vi';

  /* ================= GIAO DIỆN (Figma 19385:5067) ================= */
  var IC_SUN = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="3.6"/><path d="M12 3v2M12 19v2M3 12h2M19 12h2M5.6 5.6L7 7M17 17l1.4 1.4M18.4 5.6L17 7M7 17l-1.4 1.4"/></svg>';
  var IC_MOON = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 14.5A8.5 8.5 0 019.5 4a8.5 8.5 0 1010.5 10.5z"/></svg>';
  var IC_SYS = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="12" rx="2"/><path d="M8 20h8M12 16v4"/></svg>';

  var PF_THEMES = [
    { k: 'light', t: 'Sáng', ic: IC_SUN },
    { k: 'dark', t: 'Tối', ic: IC_MOON },
    { k: 'system', t: 'Hệ thống', ic: IC_SYS }
  ];
  var pfTheme = 'light';

  function pfSysDark() {
    return !!(window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches);
  }
  /* Áp giao diện lên <html> — mọi token màu đổi theo */
  function pfApplyTheme() {
    var dark = pfTheme === 'dark' || (pfTheme === 'system' && pfSysDark());
    if (dark) document.documentElement.setAttribute('data-theme', 'dark');
    else document.documentElement.removeAttribute('data-theme');
    var it = PF_THEMES.filter(function (x) { return x.k === pfTheme; })[0];
    if ($('pfTheme')) $('pfTheme').textContent = it ? it.t : 'Sáng';
    var ic = document.querySelector('#pfPop .pf-opt .sun');
    if (ic && it) ic.innerHTML = it.ic;
  }
  function pfApplyLang() {
    var it = PF_LANGS.filter(function (x) { return x.k === pfLang; })[0];
    if (!it) return;
    if ($('pfLang')) $('pfLang').textContent = it.t;
    var fg = document.querySelector('#pfPop .pf-opt .fg');
    if (fg) fg.innerHTML = it.ic;
    var av = document.querySelector('.pf-ava .flag');
    if (av) av.innerHTML = it.ic;
  }

  /* ---------- Menu con dùng chung ---------- */
  var pfMenuKind = null;
  function pfMenuClose() {
    var m = $('pfMenu');
    if (m) m.classList.remove('is-open');
    pfMenuKind = null;
    Array.prototype.forEach.call(document.querySelectorAll('#pfPop [data-pfsub]'), function (b) {
      b.setAttribute('aria-expanded', 'false');
    });
  }
  function pfMenuOpen(kind, anchorEl) {
    var m = $('pfMenu');
    if (!m) return;
    if (pfMenuKind === kind) { pfMenuClose(); return; }
    var items = kind === 'lang' ? PF_LANGS : PF_THEMES;
    var cur = kind === 'lang' ? pfLang : pfTheme;
    m.innerHTML = items.map(function (o) {
      return '<button class="pf-mi' + (o.k === cur ? ' is-sel' : '') + '" type="button" role="option"' +
        ' aria-selected="' + (o.k === cur) + '" data-pfval="' + o.k + '">' +
        '<span class="mi-ic">' + o.ic + '</span><span class="mi-tx">' + esc(o.t) + '</span></button>';
    }).join('');
    m.classList.add('is-open');
    pfMenuKind = kind;
    anchorEl.setAttribute('aria-expanded', 'true');

    var r = anchorEl.getBoundingClientRect();
    var w = m.offsetWidth || 150, h = m.offsetHeight || 0;
    var left = Math.round(r.right - w);
    left = Math.max(8, Math.min(left, window.innerWidth - w - 8));
    var top = Math.round(r.bottom + 4);
    if (top + h > window.innerHeight - 8) top = Math.max(8, r.top - h - 4);
    m.style.left = left + 'px';
    m.style.top = top + 'px';
  }
  function pfMenuPick(v) {
    if (pfMenuKind === 'lang') {
      pfLang = v;
      pfApplyLang();
      var l = PF_LANGS.filter(function (x) { return x.k === v; })[0];
      toast('Đã chuyển ngôn ngữ sang ' + (l ? l.t : v));
    } else if (pfMenuKind === 'theme') {
      pfTheme = v;
      pfApplyTheme();
      var t = PF_THEMES.filter(function (x) { return x.k === v; })[0];
      toast('Đã chuyển giao diện sang ' + (t ? t.t : v));
    }
    pfMenuClose();
  }

  function bindPf() {
    var pop = $('pfPop'), a = pfAnchor();
    if (!pop || !a) return;

    /* Ảnh đại diện của shell vốn là <div> — nâng thành nút bấm được */
    a.setAttribute('role', 'button');
    a.setAttribute('tabindex', '0');
    a.setAttribute('aria-haspopup', 'menu');
    a.setAttribute('aria-expanded', 'false');
    a.setAttribute('aria-label', 'Tài khoản người dùng');

    a.addEventListener('click', function (e) { e.stopPropagation(); pfToggle(); });
    a.addEventListener('keydown', function (e) {
      if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); pfToggle(); }
    });

    /* Hai dòng Ngôn ngữ / Giao diện mở menu con thay vì bắn toast */
    Array.prototype.forEach.call(pop.querySelectorAll('[data-pf="lang"],[data-pf="theme"]'), function (b) {
      b.setAttribute('data-pfsub', b.dataset.pf);
      b.setAttribute('aria-haspopup', 'listbox');
      b.setAttribute('aria-expanded', 'false');
    });
    $('pfMenu').addEventListener('click', function (e) {
      var b = e.target.closest('[data-pfval]');
      if (b) { e.stopPropagation(); pfMenuPick(b.dataset.pfval); }
    });

    pop.addEventListener('click', function (e) {
      var sub = e.target.closest('[data-pfsub]');
      if (sub) { e.stopPropagation(); pfMenuOpen(sub.dataset.pfsub, sub); return; }
      var b = e.target.closest('[data-pf]');
      if (b) {
        var k = b.dataset.pf;
        pfHide();
        toast(PF_LABEL[k] + ' đang được xây dựng');
        return;
      }
      if (e.target.closest('#pfLogout')) {
        pfHide();
        confirmDlg('Đăng xuất', 'Bạn có chắc chắn muốn đăng xuất khỏi hệ thống?', 'Đăng xuất', function () {
          toast('Đã đăng xuất khỏi hệ thống');
        });
      }
    });

    /* Bấm ra ngoài (capture) · Escape · cuộn · đổi kích thước */
    document.addEventListener('click', function (e) {
      if (!pfOpen) return;
      if (e.target.closest('#pfPop') || e.target.closest('#pfMenu') || e.target.closest('.avatar')) return;
      pfHide();
    }, true);
    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape' && pfOpen) { e.preventDefault(); pfHide(); }
    });
    pfApplyLang();
    pfApplyTheme();
    if (window.matchMedia) {
      var mq = window.matchMedia('(prefers-color-scheme: dark)');
      var onSys = function () { if (pfTheme === 'system') pfApplyTheme(); };
      if (mq.addEventListener) mq.addEventListener('change', onSys);
      else if (mq.addListener) mq.addListener(onSys);
    }

    window.addEventListener('resize', function () { if (pfOpen) { pfPlace(); pfMenuClose(); } });
    window.addEventListener('scroll', function () { if (pfOpen) pfPlace(); }, true);
  }


  /* ================= THÊM DỊCH VỤ (R01.14 Order dịch vụ) — Figma 43142:300070 ================= */

  var SV_RATES = ['Rate code 1', 'Rate code 2', 'Rate code 3'];
  var SV_CATS = ['Tất cả', 'Đồ uống', 'Đồ ăn', 'Thuê', 'Giặt là', 'Spa', 'Đưa đón', 'Khác'];
  var SV_PER = ['20', '50', '100'];
  var SV_DISC_CATS = ['Không áp dụng', 'Khuyến mãi mùa du lịch', 'Ưu đãi khách thân thiết', 'Giảm giá đoàn'];
  var SV_DISC_WHY = ['Ưu đãi khách hàng mới', 'Khách hàng thân thiết', 'Đền bù sự cố', 'Khác'];

  /* Danh mục sản phẩm mô phỏng — dịch vụ "thuê" là loại dùng theo KHOẢNG NGÀY (SRS 3.2 mục 1) */
  var SV_ITEMS = (function () {
    var seed = [
      ['Sữa tươi', 'Đồ uống', 'Hộp', 25000, 0],
      ['Coca', 'Đồ uống', 'Lon', 25000, 0],
      ['Nước cam', 'Đồ uống', 'Ly', 35000, 10],
      ['Cà phê sữa', 'Đồ uống', 'Ly', 45000, 0],
      ['Trà đào', 'Đồ uống', 'Ly', 40000, 10],
      ['Bia Hà Nội', 'Đồ uống', 'Lon', 30000, 0],
      ['Chuối', 'Đồ ăn', 'Đĩa', 25000, 0],
      ['Kem hộp', 'Đồ ăn', 'Hộp', 25000, 0],
      ['Bánh kem', 'Đồ ăn', 'Cái', 55000, 10],
      ['Snack', 'Đồ ăn', 'Gói', 25000, 0],
      ['Ngũ cốc trẻ em', 'Đồ ăn', 'Hộp', 45000, 0],
      ['Mì xào hải sản', 'Đồ ăn', 'Đĩa', 120000, 0],
      ['Set ăn sáng', 'Đồ ăn', 'Suất', 150000, 10],
      ['Thuê xe Airblade', 'Thuê', 'Chiếc', 250000, 10],
      ['Thuê xe máy số', 'Thuê', 'Chiếc', 180000, 0],
      ['Thuê xe đạp', 'Thuê', 'Chiếc', 80000, 0],
      ['Thuê xe ô tô 4 chỗ', 'Thuê', 'Chiếc', 1200000, 0],
      ['Giặt là thường', 'Giặt là', 'Kg', 60000, 0],
      ['Giặt là nhanh', 'Giặt là', 'Kg', 90000, 0],
      ['Massage toàn thân', 'Spa', 'Lượt', 450000, 10],
      ['Xông hơi', 'Spa', 'Lượt', 250000, 0],
      ['Đưa đón sân bay', 'Đưa đón', 'Lượt', 350000, 0],
      ['Đưa đón trong thành phố', 'Đưa đón', 'Lượt', 200000, 0],
      ['Thuê phòng họp', 'Thuê', 'Giờ', 500000, 0],
      ['Gói tắm khoáng', 'Spa', 'Lượt', 380000, 10],
      ['Nước suối', 'Đồ uống', 'Chai', 15000, 0],
      ['Trái cây theo mùa', 'Đồ ăn', 'Đĩa', 80000, 0],
      ['Thuê máy chiếu', 'Thuê', 'Ngày', 400000, 0]
    ];
    return seed.map(function (s, i) {
      return {
        id: 'SV' + String(1001 + i),
        code: String(10010091 + i),
        name: s[0], cat: s[1], unit: s[2], price: s[3], off: s[4],
        /* Dịch vụ thuê tính theo khoảng ngày → có dòng chi tiết theo ngày */
        byDay: s[1] === 'Thuê'
      };
    });
  })();

  var sv = {
    open: false, bk: null, room: null,
    rate: SV_RATES[0], cat: 'Tất cả', q: '', page: 1, per: 20,
    lines: [],          /* [{ it, qty, from, to, ref, net, inc, pct, amt, cat, why, note }] */
    editIdx: -1
  };

  function svUnitOf(it) { return it.off ? Math.round(it.price * (100 - it.off) / 100) : it.price; }
  function svDays(l) {
    if (!l.it.byDay) return 1;
    return Math.max(1, Math.round((startOfDay(l.to) - startOfDay(l.from)) / 86400000) + 1);
  }
  function svLineGross(l) { return svUnitOf(l.it) * l.qty * svDays(l); }
  function svLineDisc(l) {
    var g = svLineGross(l);
    return Math.min(g, Math.round(g * (l.pct || 0) / 100) + (l.amt || 0));
  }
  function svLineNet(l) { return svLineGross(l) - svLineDisc(l); }
  function svTotals() {
    var total = 0, disc = 0;
    sv.lines.forEach(function (l) { total += svLineGross(l); disc += svLineDisc(l); });
    return { total: total, disc: disc, left: total - disc };
  }

  /* ---------- Menu sản phẩm ---------- */
  function svFiltered() {
    var q = rlNorm(sv.q);
    return SV_ITEMS.filter(function (it) {
      if (sv.cat !== 'Tất cả' && it.cat !== sv.cat) return false;
      if (!q) return true;
      return rlNorm(it.name).indexOf(q) >= 0 || it.code.indexOf(sv.q.trim()) >= 0;
    });
  }
  function svRenderTabs() {
    $('svTabs').innerHTML = SV_CATS.map(function (c) {
      return '<button class="sv-tab" type="button" role="tab" data-svcat="' + esc(c) + '"' +
        ' aria-selected="' + (c === sv.cat) + '">' + esc(c) + '</button>';
    }).join('');
  }
  function svRenderGrid() {
    var list = svFiltered();
    var pages = Math.max(1, Math.ceil(list.length / sv.per));
    if (sv.page > pages) sv.page = pages;
    var from = (sv.page - 1) * sv.per;
    var page = list.slice(from, from + sv.per);

    $('svGrid').innerHTML = page.length
      ? page.map(function (it) {
          var unit = svUnitOf(it);
          return '<button class="sv-card" type="button" data-svadd="' + it.id + '" aria-label="Thêm ' + esc(it.name) + '">' +
            (it.off ? '<span class="off">-' + it.off + '%</span>' : '') +
            '<span class="img" aria-hidden="true">' +
              '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="16" rx="2"/><path d="M3 15l5-5 4 4 3-3 6 6"/><circle cx="9" cy="9" r="1.4"/></svg>' +
            '</span>' +
            '<span class="bd">' +
              '<span class="nm" title="' + esc(it.name) + '">' + esc(it.name) + '</span>' +
              '<span class="cd">' + esc(it.code) + '</span>' +
              '<span class="pr">' + (it.off ? '<span class="old">' + money(it.price) + '</span>' : '') + money(unit) + 'đ</span>' +
            '</span></button>';
        }).join('')
      : '<div class="sv-empty" style="grid-column:1/-1"><p>Không có kết quả phù hợp</p></div>';

    /* Phân trang */
    var h = '';
    var btn = function (lb, pg, dis, cur, aria) {
      return '<button class="sv-pbtn' + (cur ? ' is-cur' : '') + '" type="button" data-svpg="' + pg + '"' +
        (dis ? ' disabled' : '') + (aria ? ' aria-label="' + aria + '"' : '') + '>' + lb + '</button>';
    };
    var ic = function (d) {
      return '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">' + d + '</svg>';
    };
    h += btn(ic('<path d="M17 6l-6 6 6 6M9 6l-6 6 6 6"/>'), 1, sv.page === 1, false, 'Trang đầu');
    h += btn(ic('<path d="M15 6l-6 6 6 6"/>'), sv.page - 1, sv.page === 1, false, 'Trang trước');
    var win = [];
    for (var i = 1; i <= pages; i++) {
      if (i === 1 || i === pages || Math.abs(i - sv.page) <= 1) win.push(i);
      else if (win[win.length - 1] !== '…') win.push('…');
    }
    win.forEach(function (i) {
      h += i === '…' ? '<span class="sv-pbtn" style="border:0;background:none">…</span>' : btn(i, i, false, i === sv.page);
    });
    h += btn(ic('<path d="M9 6l6 6-6 6"/>'), sv.page + 1, sv.page === pages, false, 'Trang sau');
    h += btn(ic('<path d="M7 6l6 6-6 6M15 6l6 6-6 6"/>'), pages, sv.page === pages, false, 'Trang cuối');
    $('svPager').innerHTML = h;
  }

  /* ---------- Bảng dịch vụ đã chọn ---------- */
  function svRenderLines() {
    var t = svTotals();
    $('svCount').textContent = 'Dịch vụ (' + sv.lines.length + ')';
    $('svClear').hidden = !sv.lines.length;
    $('svTotal').textContent = money(t.total);
    $('svDisc').textContent = t.disc ? '-' + money(t.disc) : '0';
    $('svLeft').textContent = money(t.left);

    if (!sv.lines.length) {
      $('svRows').innerHTML =
        '<tr><td colspan="6"><div class="sv-empty">' +
        '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">' +
        '<path d="M3 8h18l-1.6 11.2a2 2 0 01-2 1.8H6.6a2 2 0 01-2-1.8z"/><path d="M8 8V6a4 4 0 018 0v2"/></svg>' +
        '<p>Chưa có dịch vụ nào</p><span class="s">Chọn dịch vụ ở danh sách bên trái để thêm vào đơn</span>' +
        '</div></td></tr>';
      return;
    }
    $('svRows').innerHTML = sv.lines.map(function (l, i) {
      var d = svLineDisc(l);
      var sub = '';
      if (l.it.byDay) {
        sub += '<span class="sub">Ngày sử dụng: ' + dmy(l.from) +
          (svDays(l) > 1 ? ' - ' + dmy(l.to) + ' (' + svDays(l) + ' ngày)' : '') + '</span>';
      }
      if (d) sub += '<span class="sub pro">Khuyến mãi: -' + money(d) + ' VND' +
        (l.pct ? ' (' + l.pct + '%' + (l.amt ? ' và ' + money(l.amt) + ' VND' : '') + ')' : '') + '</span>';
      return '<tr>' +
        '<td><span class="nm">' + esc(l.it.name) + '</span>' + sub + '</td>' +
        '<td class="c">' +
          '<span class="sv-spin">' +
            '<button type="button" data-svqty="' + i + '" data-svd="-1"' + (l.qty <= 1 ? ' disabled' : '') + ' aria-label="Giảm">−</button>' +
            '<span class="n">' + l.qty + '</span>' +
            '<button type="button" data-svqty="' + i + '" data-svd="1"' + (l.qty >= 99 ? ' disabled' : '') + ' aria-label="Tăng">+</button>' +
          '</span></td>' +
        '<td>' + esc(l.it.unit) + '</td>' +
        '<td class="num">' + money(svUnitOf(l.it)) + '</td>' +
        '<td class="num">' + money(svLineNet(l)) + '</td>' +
        '<td><span class="sv-acts">' +
          '<button class="sv-ib" type="button" data-svedit="' + i + '" aria-label="Chỉnh sửa ' + esc(l.it.name) + '">' +
            '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M4 20h4l10-10a2.8 2.8 0 10-4-4L4 16z"/></svg></button>' +
          '<button class="sv-ib danger" type="button" data-svdel="' + i + '" aria-label="Xoá ' + esc(l.it.name) + '">' +
            '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"><path d="M4 7h16M10 11v6M14 11v6M6 7l1 13h10l1-13M9 7V4h6v3"/></svg></button>' +
        '</span></td></tr>';
    }).join('');
  }

  function svAdd(id) {
    var it = SV_ITEMS.filter(function (x) { return x.id === id; })[0];
    if (!it) return;
    var ex = sv.lines.filter(function (l) { return l.it.id === id; })[0];
    if (ex) { ex.qty = Math.min(99, ex.qty + 1); }
    else {
      sv.lines.push({
        it: it, qty: 1,
        from: startOfDay(HOTEL_NOW), to: startOfDay(HOTEL_NOW),
        ref: '', net: false, inc: false,
        pct: it.off || 0, amt: 0, cat: SV_DISC_CATS[0], why: SV_DISC_WHY[0], note: ''
      });
    }
    svRenderLines();
    toast('Đã thêm ' + it.name + ' vào đơn dịch vụ');
  }

  /* ---------- Popup Chi tiết dịch vụ ---------- */
  function svDlgOpen(i) {
    var l = sv.lines[i];
    if (!l) return;
    sv.editIdx = i;
    $('svDDate').querySelector('.v').textContent = l.it.byDay
      ? dmy(l.from) + ' - ' + dmy(l.to)
      : dmy(l.from);
    $('svDRef').value = l.ref;
    $('svDNet').checked = l.net;
    $('svDInc').checked = l.inc;
    $('svDPct').value = l.pct;
    $('svDAmt').value = l.amt;
    $('svDNote').value = l.note;
    vsDef('svDCat', SV_DISC_CATS, l.cat);
    vsDef('svDWhy', SV_DISC_WHY, l.why);
    svDlgRows();
    $('svDBd').classList.add('is-open');
    $('svDlg').classList.add('is-open');
  }
  function svDlgRows() {
    var l = sv.lines[sv.editIdx];
    if (!l) return;
    var unit = svUnitOf(l.it), n = svDays(l);
    var h = '<tr><td><span class="nm">' + esc(l.it.name) + '</span></td>' +
      '<td class="c">' + (l.it.byDay ? '' : '<span class="sv-spin">' +
        '<button type="button" data-svdqty="-1"' + (l.qty <= 1 ? ' disabled' : '') + ' aria-label="Giảm">−</button>' +
        '<span class="n">' + l.qty + '</span>' +
        '<button type="button" data-svdqty="1" aria-label="Tăng">+</button></span>') + '</td>' +
      '<td>' + esc(l.it.unit) + '</td>' +
      '<td class="num">' + money(unit) + '</td>' +
      '<td class="num">' + money(unit * l.qty * n) + '</td></tr>';
    /* Dịch vụ theo khoảng ngày: thêm dòng chi tiết từng ngày (SRS 3.2 mục 10) */
    if (l.it.byDay) {
      for (var k = 0; k < n; k++) {
        var d = addDays(l.from, k);
        h += '<tr><td>' + dmy(d) + '</td>' +
          '<td class="c"><span class="sv-spin">' +
            '<button type="button" data-svdqty="-1"' + (l.qty <= 1 ? ' disabled' : '') + ' aria-label="Giảm">−</button>' +
            '<span class="n">' + l.qty + '</span>' +
            '<button type="button" data-svdqty="1" aria-label="Tăng">+</button></span></td>' +
          '<td>' + esc(l.it.unit) + '</td>' +
          '<td class="num">' + money(unit) + '</td>' +
          '<td class="num">' + money(unit * l.qty) + '</td></tr>';
      }
    }
    $('svDRows').innerHTML = h;
    var g = svLineGross(l), dd = svLineDisc(l);
    $('svDTotal').textContent = money(g);
    $('svDDiscV').textContent = money(dd);
    $('svDNetV').textContent = money(g - dd);
  }
  function svDlgClose() {
    $('svDBd').classList.remove('is-open');
    $('svDlg').classList.remove('is-open');
    sv.editIdx = -1;
  }
  function svDlgSave() {
    var l = sv.lines[sv.editIdx];
    if (!l) { svDlgClose(); return; }
    l.ref = $('svDRef').value.trim();
    l.net = $('svDNet').checked;
    l.inc = $('svDInc').checked;
    l.pct = Math.max(0, Math.min(100, Number($('svDPct').value) || 0));
    l.amt = Math.max(0, Number($('svDAmt').value) || 0);
    l.cat = vsVal('svDCat') || l.cat;
    l.why = vsVal('svDWhy') || l.why;
    l.note = $('svDNote').value.trim();
    svDlgClose();
    svRenderLines();
    toast('Lưu thành công chỉnh sửa trường thông tin');
  }

  /* ---------- Mở / đóng màn ---------- */
  function svOpen(bkId, roomId) {
    var b = bkId ? BOOKINGS.filter(function (x) { return x.id === bkId; })[0] : null;
    sv.bk = b; sv.room = roomId || (b && b.room) || '';
    sv.lines = []; sv.q = ''; sv.cat = 'Tất cả'; sv.page = 1; sv.open = true;
    $('svQ').value = '';
    vsDef('svRate', SV_RATES, sv.rate);
    vsDef('svPer', SV_PER, String(sv.per));
    $('svTitle').textContent = b ? 'Thêm dịch vụ — ' + b.code + ' · Phòng ' + b.room : 'Thêm dịch vụ';
    svRenderTabs(); svRenderGrid(); svRenderLines();
    $('svBd').classList.add('is-open');
    $('svCard').classList.add('is-open');
    setTimeout(function () { $('svClose').focus(); }, 0);
  }
  function svClose() {
    svDlgClose();
    sv.open = false;
    $('svCard').classList.remove('is-open');
    $('svBd').classList.remove('is-open');
  }
  /* MSG_01 — đóng khi đang có dịch vụ chưa lưu */
  function svTryClose() {
    if (!sv.lines.length) { svClose(); return; }
    confirmDlg('Huỷ thêm dịch vụ',
      'Các dịch vụ vừa chọn sẽ không được lưu. Bạn có chắc chắn muốn huỷ thêm dịch vụ?',
      'Huỷ thêm dịch vụ', function () { svClose(); toast('Đã huỷ thêm dịch vụ'); });
  }

  function bindSv() {
    if (!$('svCard')) return;

    $('svClose').addEventListener('click', svTryClose);
    $('svCancel').addEventListener('click', svTryClose);
    $('svBd').addEventListener('click', svTryClose);

    $('svSave').addEventListener('click', function () {
      if (!sv.lines.length) { toast('Chưa chọn dịch vụ nào', true); return; }
      var t = svTotals();
      svClose();
      toast('Đã cập nhật ' + sv.lines.length + ' dịch vụ · Còn lại ' + money(t.left) + ' VND, thanh toán sau');
    });

    /* Tìm kiếm · danh mục · phân trang */
    $('svQ').addEventListener('input', function () { sv.q = this.value; sv.page = 1; svRenderGrid(); });
    $('svTabs').addEventListener('click', function (e) {
      var b = e.target.closest('[data-svcat]');
      if (!b) return;
      sv.cat = b.dataset.svcat; sv.page = 1;
      svRenderTabs(); svRenderGrid();
    });
    document.querySelectorAll('#svCard [data-svtnav]').forEach(function (b) {
      b.addEventListener('click', function () {
        $('svTabs').scrollLeft += Number(b.dataset.svtnav) * 160;
      });
    });
    $('svPager').addEventListener('click', function (e) {
      var b = e.target.closest('[data-svpg]');
      if (!b || b.disabled) return;
      sv.page = Number(b.dataset.svpg);
      svRenderGrid();
    });
    $('svGrid').addEventListener('click', function (e) {
      var b = e.target.closest('[data-svadd]');
      if (b) svAdd(b.dataset.svadd);
    });

    /* Bảng đã chọn: spinner · sửa · xoá */
    $('svRows').addEventListener('click', function (e) {
      var q = e.target.closest('[data-svqty]');
      if (q) {
        var l = sv.lines[Number(q.dataset.svqty)];
        l.qty = Math.max(1, Math.min(99, l.qty + Number(q.dataset.svd)));
        svRenderLines();
        return;
      }
      var ed = e.target.closest('[data-svedit]');
      if (ed) { svDlgOpen(Number(ed.dataset.svedit)); return; }
      var dl = e.target.closest('[data-svdel]');
      if (dl) {
        var it = sv.lines[Number(dl.dataset.svdel)];
        sv.lines.splice(Number(dl.dataset.svdel), 1);
        svRenderLines();
        toast('Đã xoá ' + it.it.name + ' khỏi đơn dịch vụ');
      }
    });

    /* MSG_02 — xoá tất cả */
    $('svClear').addEventListener('click', function () {
      confirmDlg('Xoá tất cả dịch vụ',
        'Toàn bộ dịch vụ đã thêm sẽ bị xoá khỏi đơn. Bạn có chắc chắn?',
        'Xác nhận', function () {
          sv.lines = [];
          svRenderLines();
          toast('Đã xoá tất cả dịch vụ');
        });
    });

    /* Popup chi tiết */
    $('svDlg').addEventListener('click', function (e) {
      var b = e.target.closest('[data-svd]');
      if (b) { b.dataset.svd === 'save' ? svDlgSave() : svDlgClose(); return; }
      var q = e.target.closest('[data-svdqty]');
      if (q) {
        var l = sv.lines[sv.editIdx];
        l.qty = Math.max(1, Math.min(99, l.qty + Number(q.dataset.svdqty)));
        svDlgRows();
      }
    });
    $('svDBd').addEventListener('click', svDlgClose);
    ['svDPct', 'svDAmt'].forEach(function (id) {
      $(id).addEventListener('input', function () {
        var l = sv.lines[sv.editIdx];
        if (!l) return;
        l.pct = Math.max(0, Math.min(100, Number($('svDPct').value) || 0));
        l.amt = Math.max(0, Number($('svDAmt').value) || 0);
        svDlgRows();
      });
    });
    $('svDDate').addEventListener('click', function () {
      var l = sv.lines[sv.editIdx];
      if (!l) return;
      dtpOpen($('svDDate'), l.from, false, function (d) {
        l.from = d;
        if (l.to < l.from) l.to = addDays(d, 1);
        $('svDDate').querySelector('.v').textContent = l.it.byDay ? dmy(l.from) + ' - ' + dmy(l.to) : dmy(l.from);
        svDlgRows();
      });
    });

    if (QB_SEL['svPer']) {
      QB_SEL['svPer'].onChange = function (v) { sv.per = Number(v) || 20; sv.page = 1; svRenderGrid(); };
    }
    if (QB_SEL['svRate']) {
      QB_SEL['svRate'].onChange = function (v) { sv.rate = v; svRenderGrid(); };
    }

    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape' || !sv.open) return;
      if (typeof dtpIsOpen === 'function' && dtpIsOpen()) return;
      e.preventDefault();
      if ($('svDlg').classList.contains('is-open')) { svDlgClose(); return; }
      svTryClose();
    });
  }


  /* ================= QUY TRÌNH TRẢ PHÒNG (R01.21 Checkout) — Figma 45799:283821 / 45799:281120 ================= */

  var co = { open: false, step: 1, rows: [], tab: 'all', collapsed: {}, dlgRow: -1 };

  var CO_FLAG = '<svg viewBox="0 0 24 16" aria-hidden="true"><rect width="24" height="16" fill="#DA251D"/><path d="M12 3.2l1.1 3.4h3.6l-2.9 2.1 1.1 3.4L12 10l-2.9 2.1 1.1-3.4-2.9-2.1h3.6z" fill="#FFFF00"/></svg>';

  /* Gom các phòng cùng booking / cùng đoàn vào một luồng trả phòng */
  function coBuildRows(bkId) {
    var b = BOOKINGS.filter(function (x) { return x.id === bkId; })[0];
    if (!b) return [];
    var mates = BOOKINGS.filter(function (x) {
      if (x.st !== 'inhouse' && x.st !== 'booked') return false;
      if (x.id === b.id) return true;
      return !!(b.group && x.group === b.group) || !!(b.company && x.company === b.company);
    });
    return mates.map(function (x) {
      var n = Math.max(1, Math.round((startOfDay(x.out) - startOfDay(x.in)) / 86400000));
      var room = ROOMS.filter(function (r) { return r.id === x.room; })[0];
      var total = x.total || 0, paid = x.paid || 0;
      return {
        id: x.id, bk: x, type: room ? room.type : '—', room: x.room,
        guest: x.name, in: new Date(x.in), out: new Date(x.out), nights: n,
        rate: 'Rate code ' + (1 + (rvHash(x.id) % 3)),
        total: total, paid: paid, left: Math.max(0, total - paid),
        lock: true, sel: x.id === bkId, done: false, lead: !!x.group
      };
    });
  }
  function coGroups() {
    var map = {}, order = [];
    co.rows.forEach(function (r) {
      if (!map[r.type]) { map[r.type] = []; order.push(r.type); }
      map[r.type].push(r);
    });
    return order.map(function (k) { return { type: k, rows: map[k] }; });
  }
  function coVisible() {
    if (co.step !== 2 || co.tab === 'all') return co.rows;
    return co.rows.filter(function (r) { return co.tab === 'paid' ? r.left === 0 : r.left > 0; });
  }
  function coTotals(list) {
    var t = { total: 0, paid: 0, left: 0 };
    (list || co.rows).forEach(function (r) { t.total += r.total; t.paid += r.paid; t.left += r.left; });
    return t;
  }

  function coRender() {
    if (!co.open) return;
    var isP2 = co.step === 2;
    $('coTools1').hidden = co.step !== 1;
    $('coTools2').hidden = !isP2;
    $('coBody').hidden = co.step === 3;
    $('coDone').hidden = co.step !== 3;
    $('coFootNote').hidden = !isP2;
    $('coBack').hidden = co.step === 1;
    $('coPayAll').hidden = !isP2;
    $('coNext').textContent = '';

    document.querySelectorAll('#coCard [data-costep]').forEach(function (b) {
      var n = Number(b.dataset.costep);
      b.classList.toggle('is-cur', n === co.step);
      b.classList.toggle('is-done', n < co.step);
      b.disabled = n > co.step;
      if (n === co.step) b.setAttribute('aria-current', 'step'); else b.removeAttribute('aria-current');
    });

    var nxt = $('coNext');
    if (co.step === 1) {
      nxt.innerHTML = 'Đến bước thanh toán <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6"/></svg>';
      nxt.disabled = !co.rows.some(function (r) { return r.sel; });
    } else if (co.step === 2) {
      nxt.innerHTML = 'Tiếp tục <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6"/></svg>';
      nxt.disabled = co.rows.some(function (r) { return r.sel && !r.done; });
    } else {
      nxt.innerHTML = 'Hoàn tất';
      nxt.disabled = false;
    }

    if (co.step === 3) { coRenderDone(); return; }

    /* Tabs bước 2 */
    if (isP2) {
      var un = co.rows.filter(function (r) { return r.left > 0; }).length;
      var pd = co.rows.length - un;
      var lb = { all: 'Tất cả (' + co.rows.length + ')', unpaid: 'Chưa thanh toán (' + un + ')', paid: 'Đã thanh toán (' + pd + ')' };
      document.querySelectorAll('#coTools2 [data-cotab]').forEach(function (b) {
        b.textContent = lb[b.dataset.cotab];
        b.setAttribute('aria-selected', b.dataset.cotab === co.tab ? 'true' : 'false');
      });
      var t2 = coTotals(co.rows.filter(function (r) { return r.sel; }));
      $('coPayAll').textContent = 'Thanh toán toàn bộ: ' + money(t2.left);
      $('coPayAll').disabled = t2.left === 0;
    }

    /* Bảng */
    var vis = coVisible();
    var h = '<thead><tr>' +
      '<th style="width:44px"><label class="co-ck"><input type="checkbox" id="coAll"' +
        (vis.length && vis.every(function (r) { return r.sel; }) ? ' checked' : '') + ' aria-label="Chọn tất cả">' +
        '<span class="box" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span></label></th>' +
      '<th>Phòng (' + co.rows.length + ')</th><th>Khách hàng</th><th>Thời gian ở</th>' +
      '<th class="c">Số đêm</th><th>Chính sách giá</th>' +
      '<th class="num">Tổng</th><th class="num">Thanh toán</th><th class="num">Còn lại</th>' +
      (isP2 ? '<th></th>' : '') + '</tr></thead><tbody>';

    var ck = function (r) {
      return '<label class="co-ck"><input type="checkbox" data-cosel="' + r.id + '"' + (r.sel ? ' checked' : '') +
        ' aria-label="Chọn phòng ' + esc(r.room) + '">' +
        '<span class="box" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span></label>';
    };
    var cols = isP2 ? 10 : 9;

    coGroups().forEach(function (g) {
      var rows = g.rows.filter(function (r) { return vis.indexOf(r) >= 0; });
      if (!rows.length) return;
      var closed = !!co.collapsed[g.type];
      var gt = coTotals(rows);
      h += '<tr class="co-grp"><td>' + '' + '</td>' +
        '<td colspan="5"><button class="cr' + (closed ? ' is-closed' : '') + '" type="button" data-cogrp="' + esc(g.type) + '" aria-expanded="' + (!closed) + '" aria-label="Thu gọn nhóm ' + esc(g.type) + '">' +
          '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg></button> ' +
          esc(g.type) + ' (' + rows.length + ')</td>' +
        '<td class="num">' + money(gt.total) + '</td><td class="num">' + money(gt.paid) + '</td>' +
        '<td class="num' + (gt.left ? ' left0' : '') + '">' + money(gt.left) + '</td>' +
        (isP2 ? '<td></td>' : '') + '</tr>';
      if (closed) return;
      rows.forEach(function (r) {
        h += '<tr class="co-row"><td>' + ck(r) + '</td>' +
          '<td><span class="rm"><span class="ic" aria-hidden="true">' +
            '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M3 8v10M3 13h18M21 18v-6a2 2 0 00-2-2h-8v5"/><circle cx="7" cy="11" r="1.6"/></svg>' +
            '</span>' + esc(r.type) + ' - ' + esc(r.room) + '</span></td>' +
          '<td><span class="gu"><span class="fg" aria-hidden="true">' + CO_FLAG + '</span>' +
            '<span class="nm" title="' + esc(r.guest) + '">' + esc(r.guest) + '</span>' +
            (r.lead ? ' <span class="ic" aria-hidden="true" title="Trưởng đoàn"><svg viewBox="0 0 24 24" fill="#28C76F" width="12" height="12"><path d="M5 3v18l7-4 7 4V3z"/></svg></span>' : '') +
            '</span></td>' +
          '<td class="stay">' + dmy(r.in) + ' ' + hm(r.in) + '<br><span class="out">' + dmy(r.out) + ' ' + hm(r.out) + '</span></td>' +
          '<td class="c">' + r.nights + '</td><td>' + esc(r.rate) + '</td>' +
          '<td class="num"><span class="lock">' +
            (r.lock ? '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true"><rect x="5" y="11" width="14" height="9" rx="2"/><path d="M8 11V8a4 4 0 018 0v3"/></svg>' : '') +
            money(r.total) + '</span></td>' +
          '<td class="num">' + money(r.paid) + '</td>' +
          '<td class="num' + (r.left ? ' left0' : '') + '">' + money(r.left) + '</td>' +
          (isP2 ? '<td>' + (r.done
            ? '<span style="color:var(--color-palette-success-dark,#24B364);font-weight:500">Đã trả</span>'
            : '<button class="co-btn-pay" type="button" data-copay="' + r.id + '">Trả phòng</button>') + '</td>' : '') +
          '</tr>';
      });
    });
    if (!vis.length) h += '<tr><td colspan="' + cols + '" style="text-align:center;color:var(--theme-text-secondary)">Không có phòng phù hợp</td></tr>';
    $('coTbl').innerHTML = h + '</tbody>';

    var t = coTotals(vis);
    $('coSum').innerHTML = '<span class="lb">TỔNG CỘNG (VND)</span>' +
      '<span class="v">' + money(t.total) + '</span>' +
      '<span class="v">' + money(t.paid) + '</span>' +
      '<span class="v' + (t.left ? ' left0' : '') + '">' + money(t.left) + '</span>' +
      (co.step === 2 ? '<span class="v" style="width:100px"></span>' : '');
  }

  function coRenderDone() {
    var t = coTotals(co.rows.filter(function (r) { return r.sel; }));
    $('coInv').innerHTML =
      '<div class="r"><span class="k">Số phòng đã trả</span><span class="v">' + co.rows.filter(function (r) { return r.sel; }).length + '</span></div>' +
      '<div class="r"><span class="k">Tổng tiền</span><span class="v">' + money(t.total) + ' VND</span></div>' +
      '<div class="r"><span class="k">Đã thanh toán</span><span class="v">' + money(t.paid) + ' VND</span></div>' +
      '<div class="r big"><span class="k">Còn lại</span><span class="v">' + money(t.left) + ' VND</span></div>';
  }

  function coGo(step) {
    if (step === 2 && !co.rows.some(function (r) { return r.sel; })) {
      toast('Chọn ít nhất một phòng để tiếp tục', true); return;
    }
    co.step = step;
    coRender();
  }

  function coOpen(bkId) {
    co.rows = coBuildRows(bkId);
    if (!co.rows.length) { toast('Không tìm thấy phòng để trả', true); return; }
    co.step = 1; co.tab = 'all'; co.collapsed = {}; co.open = true;
    $('coChipN').textContent = String(co.rows.filter(function (r) { return r.lead; }).length || co.rows.length);
    $('coBd').classList.add('is-open');
    $('coCard').classList.add('is-open');
    coRender();
    setTimeout(function () { $('coClose').focus(); }, 0);
  }
  function coClose() {
    coDlgClose();
    co.open = false;
    $('coCard').classList.remove('is-open');
    $('coBd').classList.remove('is-open');
  }
  function coTryClose() {
    if (co.step === 3 || !co.rows.some(function (r) { return r.done; })) { coClose(); return; }
    confirmDlg('Thoát quy trình trả phòng',
      'Một số phòng đã được trả. Thoát bây giờ sẽ không xuất hoá đơn điện tử cho các phòng này. Bạn có chắc chắn?',
      'Thoát', function () { coClose(); });
  }

  /* ---------- Popup cập nhật thời gian trả ---------- */
  function coDlgOpen() {
    var sel = co.rows.filter(function (r) { return r.sel; });
    if (!sel.length) { toast('Chọn phòng cần cập nhật thời gian trả', true); return; }
    co.dlgRow = co.rows.indexOf(sel[0]);
    $('coDTime').querySelector('.v').textContent = dmy(sel[0].out) + ' ' + hm(sel[0].out);
    $('coDAll').checked = sel.length > 1;
    $('coDBd').classList.add('is-open');
    $('coDlg').classList.add('is-open');
  }
  function coDlgClose() {
    $('coDBd').classList.remove('is-open');
    $('coDlg').classList.remove('is-open');
  }
  function coDlgOk() {
    var base = co.rows[co.dlgRow];
    if (!base) { coDlgClose(); return; }
    var all = $('coDAll').checked;
    var targets = all ? co.rows.filter(function (r) { return r.sel; }) : [base];
    targets.forEach(function (r) {
      r.out = new Date(base.out.getTime());
      r.nights = Math.max(1, Math.round((startOfDay(r.out) - startOfDay(r.in)) / 86400000));
      /* Tiền phòng tính lại theo số đêm mới */
      var unit = Math.round((r.bk.total || 0) / Math.max(1, Math.round((startOfDay(r.bk.out) - startOfDay(r.bk.in)) / 86400000)));
      r.total = unit * r.nights;
      r.left = Math.max(0, r.total - r.paid);
      r.lock = false;
    });
    coDlgClose();
    coRender();
    toast('Đã cập nhật thời gian trả phòng cho ' + targets.length + ' phòng');
  }

  function bindCo() {
    if (!$('coCard')) return;

    $('coClose').addEventListener('click', coTryClose);
    $('coBd').addEventListener('click', coTryClose);

    document.querySelectorAll('#coCard [data-costep]').forEach(function (b) {
      b.addEventListener('click', function () {
        var n = Number(b.dataset.costep);
        if (n < co.step) coGo(n);
      });
    });
    $('coBack').addEventListener('click', function () { coGo(Math.max(1, co.step - 1)); });
    $('coNext').addEventListener('click', function () {
      if (co.step === 3) {
        var n = co.rows.filter(function (r) { return r.sel; }).length;
        coClose();
        toast('Đã hoàn tất trả phòng và phát hành hoá đơn điện tử cho ' + n + ' phòng');
        render();
        return;
      }
      coGo(co.step + 1);
    });

    /* Chọn phòng · thu gọn nhóm · trả từng phòng */
    $('coTbl').addEventListener('change', function (e) {
      var a = e.target.closest('#coAll');
      if (a) {
        var vis = coVisible();
        vis.forEach(function (r) { r.sel = a.checked; });
        coRender();
        return;
      }
      var c = e.target.closest('[data-cosel]');
      if (c) {
        var r = co.rows.filter(function (x) { return x.id === c.dataset.cosel; })[0];
        if (r) r.sel = c.checked;
        coRender();
      }
    });
    $('coTbl').addEventListener('click', function (e) {
      var g = e.target.closest('[data-cogrp]');
      if (g) { co.collapsed[g.dataset.cogrp] = !co.collapsed[g.dataset.cogrp]; coRender(); return; }
      var p = e.target.closest('[data-copay]');
      if (p) {
        var r = co.rows.filter(function (x) { return x.id === p.dataset.copay; })[0];
        if (!r) return;
        confirmDlg('Trả phòng ' + r.type + ' - ' + r.room,
          'Xác nhận thu ' + money(r.left) + ' VND và trả phòng cho khách ' + r.guest + '?',
          'Trả phòng', function () {
            r.paid = r.total; r.left = 0; r.done = true;
            coRender();
            toast('Đã trả phòng ' + r.type + ' - ' + r.room);
          });
      }
    });

    $('coTools2').addEventListener('click', function (e) {
      var b = e.target.closest('[data-cotab]');
      if (b) { co.tab = b.dataset.cotab; coRender(); }
    });

    $('coPayAll').addEventListener('click', function () {
      var sel = co.rows.filter(function (r) { return r.sel && !r.done; });
      if (!sel.length) return;
      var t = coTotals(sel);
      confirmDlg('Thanh toán toàn bộ',
        'Xác nhận thu ' + money(t.left) + ' VND cho ' + sel.length + ' phòng và trả phòng?',
        'Thanh toán', function () {
          sel.forEach(function (r) { r.paid = r.total; r.left = 0; r.done = true; });
          coRender();
          toast('Đã thanh toán và trả ' + sel.length + ' phòng');
        });
    });

    $('coUpdTime').addEventListener('click', coDlgOpen);
    $('coAddRoom').addEventListener('click', function () {
      toast('Thêm phòng vào luồng đang trả phòng đang được xây dựng');
    });
    /* ---- In hoá đơn: split button + menu "In nhanh (Hoá đơn tạm tính)" ----
       Nguồn: ezOnePms.html (.print-invoice-split) + ảnh tham chiếu menu. */
    var CO_PRINT_IC = {
      svc:  '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 6h.01M4 12h.01M4 18h.01"/><path d="M9 6h11M9 12h11M9 18h11"/></svg>',
      room: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 7v11"/><path d="M3 14h18"/><path d="M21 18v-8a2 2 0 0 0-2-2h-8v6"/><circle cx="7" cy="10" r="1"/></svg>',
      dsvc: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="7" height="7" rx="1.5"/><rect x="3" y="13" width="7" height="7" rx="1.5"/><path d="M13 6h8M13 10h5M13 15h8M13 19h5"/></svg>'
    };
    var CO_PRINT_ITEMS = [
      { k: 'summary-service', ic: 'svc',  t: 'Gom · theo dịch vụ' },
      { k: 'summary-room',    ic: 'room', t: 'Gom · theo phòng' },
      { k: 'detail-service',  ic: 'dsvc', t: 'Chi tiết · theo dịch vụ' },
      { k: 'detail-room',     ic: 'room', t: 'Chi tiết · theo phòng' }
    ];
    var coPrintAnchor = null;
    function coPrintClose() {
      $('coPrintMenu').classList.remove('is-open');
      if (coPrintAnchor) coPrintAnchor.setAttribute('aria-expanded', 'false');
      coPrintAnchor = null;
    }
    function coPrintOpen(anchor) {
      var m = $('coPrintMenu');
      m.innerHTML = '<div class="cap">In nhanh (Hoá đơn tạm tính)</div>' +
        CO_PRINT_ITEMS.map(function (it) {
          return '<button type="button" role="menuitem" data-coprintk="' + it.k + '">' +
                 CO_PRINT_IC[it.ic] + it.t + '</button>';
        }).join('');
      m.classList.add('is-open');
      coPrintAnchor = anchor;
      anchor.setAttribute('aria-expanded', 'true');
      placeFloating(m, anchor, 4);
    }
    document.querySelectorAll('#coCard [data-coprint]').forEach(function (b) {
      b.addEventListener('click', function (e) {
        e.stopPropagation();
        if (b.dataset.coprint === 'main') { coPrintClose(); toast('Chuyển sang luồng In hoá đơn'); return; }
        $('coPrintMenu').classList.contains('is-open') ? coPrintClose() : coPrintOpen(b);
      });
    });
    $('coPrintMenu').addEventListener('click', function (e) {
      var b = e.target.closest('[data-coprintk]');
      if (!b) return;
      var it = CO_PRINT_ITEMS.filter(function (x) { return x.k === b.dataset.coprintk; })[0];
      coPrintClose();
      toast('In nhanh (Hoá đơn tạm tính) — ' + (it ? it.t : b.dataset.coprintk));
    });
    document.addEventListener('click', function (e) {
      if (!$('coPrintMenu').classList.contains('is-open')) return;
      if (e.target.closest('#coPrintMenu') || e.target.closest('[data-coprint]')) return;
      coPrintClose();
    }, true);
    window.addEventListener('resize', coPrintClose);
    window.addEventListener('scroll', coPrintClose, true);

    /* Popup thời gian */
    $('coDlg').addEventListener('click', function (e) {
      var b = e.target.closest('[data-cod]');
      if (b) { b.dataset.cod === 'ok' ? coDlgOk() : coDlgClose(); }
    });
    $('coDBd').addEventListener('click', coDlgClose);
    $('coDTime').addEventListener('click', function () {
      var r = co.rows[co.dlgRow];
      if (!r) return;
      dtpOpen($('coDTime'), r.out, true, function (d) {
        r.out = d;
        $('coDTime').querySelector('.v').textContent = dmy(d) + ' ' + hm(d);
      });
    });

    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape' || !co.open) return;
      if (typeof dtpIsOpen === 'function' && dtpIsOpen()) return;
      e.preventDefault();
      if ($('coDlg').classList.contains('is-open')) { coDlgClose(); return; }
      coTryClose();
    });
  }


  /* ================= KIỂM TOÁN ĐÊM (FD01) — SRS FD01_01 · Figma 43747:47069 =================
     Quy trình chốt sổ cuối ngày: kiểm tra đặt phòng tới hạn trả / không đến, hạch toán
     tiền phòng, cập nhật trạng thái phòng + hồ sơ khách rồi chuyển ngày kinh doanh.
     ------------------------------------------------------------------------------------- */

  /* Danh sách bước — req = bước bắt buộc, người dùng không thể bỏ chọn khi cấu hình */
  var NA_STEPS = [
    { k: 'due',    n: 'Kiểm tra đặt phòng tới hạn trả',              req: false },
    { k: 'noshow', n: 'Kiểm tra các đặt phòng không đến',            req: false },
    { k: 'task',   n: 'Kiểm tra Công việc cần hoàn thành',           req: false },
    { k: 'charge', n: 'Kiểm tra thêm tiền phòng',                    req: true  },
    { k: 'rmst',   n: 'Cập nhật trạng thái phòng',                   req: true  },
    { k: 'guest',  n: 'Cập nhật thông tin vào hồ sơ khách',          req: true  },
    { k: 'stat',   n: 'Cập nhật dữ liệu thống kê',                   req: false },
    { k: 'rate',   n: 'Cập nhật giá phòng trong chi tiết đặt phòng', req: false },
    { k: 'shift',  n: 'Chuyển ngày kinh doanh',                      req: true  }
  ];

  /* MSG_03 → MSG_08 (Phần 4 — SRS) */
  var NA_MSG = {
    due:    'Đã kiểm tra đặt phòng tới hạn trả',
    noshow: 'Đã kiểm tra đặt phòng đến trong ngày',
    task:   'Đã kiểm tra công việc cần hoàn thành',
    charge: 'Đã kiểm tra thêm tiền phòng',
    rmst:   'Đã cập nhật trạng thái phòng',
    guest:  'Đã cập nhật thông tin vào hồ sơ khách.',
    stat:   'Đã cập nhật dữ liệu thống kê!',
    rate:   'Đã cập nhật giá phòng trong chi tiết đặt phòng',
    shift:  'Hoàn tất kiểm toán đêm và chuyển ngày kinh doanh thành công'
  };

  var NA_DEF = { due: 1, noshow: 1, task: 0, charge: 1, rmst: 1, guest: 1, stat: 0, rate: 1, shift: 1 };

  var na = {
    on: {}, saved: {},                 /* cấu hình bước bật/tắt (on = đang sửa, saved = đã lưu) */
    auto: false, tRun: '04:00', tWarn: '06:00',
    sAuto: false, sRun: '04:00', sWarn: '06:00',
    run: false, done: false, cur: -1,  /* trạng thái chạy */
    st: {}, ts: {},                    /* st[k] = idle|run|done|err · ts[k] = mốc thời gian */
    view: 'proc',                      /* proc | cfg */
    list: null, sel: {}, rows: [],     /* modal danh sách phòng */
    dPick: null, timer: null
  };
  (function () { for (var k in NA_DEF) { na.on[k] = !!NA_DEF[k]; na.saved[k] = !!NA_DEF[k]; na.st[k] = 'idle'; } })();

  /* ---------- Dữ liệu nghiệp vụ ---------- */

  /* Đặt phòng tới hạn trả: đang ở và ngày đi đã tới (hoặc qua) ngày kinh doanh */
  function naDue() {
    var r = [], i, b;
    for (i = 0; i < BOOKINGS.length; i++) {
      b = BOOKINGS[i];
      if (b.st === 'inhouse' && startOfDay(b.out) <= BIZ_DATE) r.push(b);
    }
    return r;
  }

  /* Đặt phòng không đến: đã đặt nhưng ngày đến đã tới (hoặc qua) mà chưa nhận phòng */
  function naNoShow() {
    var r = [], i, b;
    for (i = 0; i < BOOKINGS.length; i++) {
      b = BOOKINGS[i];
      if (b.st === 'booked' && startOfDay(b.in) <= BIZ_DATE) r.push(b);
    }
    return r;
  }

  function naSteps() {
    var r = [], i;
    for (i = 0; i < NA_STEPS.length; i++) if (na.saved[NA_STEPS[i].k]) r.push(NA_STEPS[i]);
    return r;
  }

  function naStepByKey(k) { for (var i = 0; i < NA_STEPS.length; i++) if (NA_STEPS[i].k === k) return NA_STEPS[i]; return null; }

  /* ---------- Kết xuất màn hình ---------- */

  function naRenderHead() {
    var el = $('naBizDate'); if (!el) return;
    el.textContent = dmy(BIZ_DATE);
    el.classList.toggle('is-off', na.run);
    $('naEditDate').disabled = na.run;
    $('naRun').disabled = na.run;
    $('naProcTitle').textContent = 'Quy trình kiểm toán đêm - ' + dmy(BIZ_DATE);
    var chip = $('naChip');
    chip.classList.remove('is-run', 'is-done');
    if (na.run) { chip.classList.add('is-run'); chip.textContent = 'Đang thực hiện'; }
    else if (na.done) { chip.classList.add('is-done'); chip.textContent = 'Hoàn thành'; }
    else chip.textContent = 'Chưa thực hiện';
    $('naWarn').hidden = !na.run;
  }

  function naRenderSteps() {
    var box = $('naSteps'); if (!box) return;
    var list = naSteps(), h = [], i, s, st, sub, cnt;
    for (i = 0; i < list.length; i++) {
      s = list[i]; st = na.st[s.k] || 'idle'; sub = '';
      if (st === 'done') sub = esc(na.ts[s.k] || '') + ' • <span class="ok">Đã xử lý</span>';
      else if (st === 'run') sub = '<span class="run">Đang tiến hành kiểm tra...</span>';
      else if (st === 'err') {
        cnt = s.k === 'due' ? naDue().length : naNoShow().length;
        sub = esc(na.ts[s.k] || '') + ' • Vui lòng xử lý <button class="na-link" type="button" data-nalist="' + s.k + '">' +
              (s.k === 'due' ? 'Các đặt phòng đến hạn trả' : 'Các đặt phòng không đến') + ' (' + cnt + ')</button> trước khi tiếp tục!';
      }
      h.push('<div class="na-step is-' + st + '">' +
               '<span class="na-no">' + (i + 1) + '</span>' +
               '<span class="na-txt">' +
                 '<span class="na-nm">' + esc(s.n) + '</span>' +
                 (sub ? '<span class="na-sub">' + sub + '</span>' : '') +
                 (st === 'err' ? '<button class="tl-btn-primary na-fix" type="button" data-nafix="' + s.k + '">Tôi đã xử lý xong</button>' : '') +
               '</span>' +
             '</div>');
    }
    box.innerHTML = h.join('');
  }

  function naRender() { naRenderHead(); naRenderSteps(); }

  function naShow() {
    na.view = 'proc';
    $('naProc').hidden = false;
    $('naCfgPane').hidden = true;
    naRender();
    var prev = document.querySelector('.nav-child.is-selected, .nav-item.is-selected');
    if (prev) { prev.classList.remove('is-selected'); prev.removeAttribute('aria-current'); }
    var el = document.querySelector('.nav-item[data-nav="night-audit"]');
    if (el) { el.classList.add('is-selected'); el.setAttribute('aria-current', 'page'); }
  }

  /* ---------- Chạy quy trình ---------- */

  function naStamp() { var d = new Date(); return dmy(d) + ' ' + hm(d); }

  function naStart() {
    var list = naSteps(), i;
    for (i = 0; i < list.length; i++) { na.st[list[i].k] = 'idle'; na.ts[list[i].k] = ''; }
    na.run = true; na.done = false; na.cur = -1;
    naRender();
    naNext();
  }

  function naNext() {
    var list = naSteps();
    na.cur++;
    if (na.cur >= list.length) { naFinish(); return; }
    var s = list[na.cur];
    na.st[s.k] = 'run';
    naRender();
    if (na.timer) clearTimeout(na.timer);
    na.timer = setTimeout(function () { naResolve(s); }, 700);
  }

  function naResolve(s) {
    if (s.k === 'due' && naDue().length) { naFail(s); return; }
    if (s.k === 'noshow' && naNoShow().length) { naFail(s); return; }
    na.st[s.k] = 'done'; na.ts[s.k] = naStamp();
    if (s.k === 'shift') { naShiftDate(); }
    naRender();
    toast(NA_MSG[s.k] || 'Đã xử lý');
    if (s.k !== 'shift') naNext(); else naFinish();
  }

  function naFail(s) {
    na.st[s.k] = 'err'; na.ts[s.k] = naStamp();
    naRender();
    naListOpen(s.k);
  }

  /* Bước “Tôi đã xử lý xong” — kiểm tra lại, còn tồn thì báo lỗi */
  function naRetry(k) {
    var left = k === 'due' ? naDue().length : naNoShow().length;
    if (left) {
      toast('Vẫn còn ' + left + ' đặt phòng chưa xử lý!', true);
      naRender(); return;
    }
    na.st[k] = 'done'; na.ts[k] = naStamp();
    naRender();
    toast(NA_MSG[k]);
    naNext();
  }

  function naShiftDate() {
    BIZ_DATE = addDays(BIZ_DATE, 1);
    /* Khách ở qua đêm → phòng chuyển bẩn để buồng phòng dọn cho ngày mới */
    for (var i = 0; i < BOOKINGS.length; i++) {
      var b = BOOKINGS[i];
      if (b.st === 'inhouse') { var r = roomById(b.room); if (r) r.dirty = true; }
    }
    if (typeof render === 'function') render();
  }

  function naFinish() {
    na.run = false; na.done = true; na.cur = -1;
    naRender();
  }

  /* ---------- Modal danh sách phòng ---------- */

  function naListOpen(kind) {
    na.list = kind; na.sel = {};
    na.rows = kind === 'due' ? naDue() : naNoShow();
    $('naListTitle').textContent = (kind === 'due' ? 'Danh sách phòng đến hạn trả (' : 'Danh sách phòng không đến (') + na.rows.length + ')';
    $('naListHintTx').textContent = kind === 'due'
      ? 'Chọn nhiều để Trả phòng hoặc gia hạn thêm 1 đêm'
      : 'Chọn nhiều để Nhận phòng hoặc hủy đặt phòng';
    $('naActA').textContent = kind === 'due' ? 'Trả phòng' : 'Nhận phòng';
    $('naActB').textContent = kind === 'due' ? 'Gia hạn thêm 1 đêm' : 'Hủy đặt phòng';
    naListRender();
    $('naBd').classList.add('is-open');
    $('naListDlg').classList.add('is-open');
  }

  function naListClose() {
    $('naBd').classList.remove('is-open');
    $('naListDlg').classList.remove('is-open');
    na.list = null;
  }

  var NA_GC = ['#28C76F', '#FF9F43', '#FF4C51', '#00A2E1', '#186FF4'];

  function naListRender() {
    var t = $('naListTbl'); if (!t) return;
    if (!na.rows.length) {
      t.innerHTML = '<tbody><tr><td colspan="9"><div class="na-empty">' +
        (na.list === 'due' ? 'Không còn phòng đến hạn trả' : 'Không còn phòng không đến') +
        '</div></td></tr></tbody>';
      naListSync(); return;
    }

    /* Gom nhóm: đặt phòng lẻ trước, sau đó theo từng đoàn */
    var solo = [], groups = {}, order = [], i, b, g;
    for (i = 0; i < na.rows.length; i++) {
      b = na.rows[i];
      if (b.group && b.group.gid) {
        g = b.group.gid;
        if (!groups[g]) { groups[g] = { name: b.group.name, rows: [] }; order.push(g); }
        groups[g].rows.push(b);
      } else solo.push(b);
    }

    var h = ['<thead><tr>' +
      '<th class="na-ck"><label class="sv-ck"><input type="checkbox" id="naCkAll">' +
        '<span class="box" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span></label></th>' +
      '<th>Mã đặt phòng</th><th>Tên khách</th><th>NL/TE/EB</th><th>Loại phòng</th>' +
      '<th>Ngày đến</th><th>Ngày đi</th><th>Chính sách giá</th><th class="num">Tổng tiền phòng</th>' +
      '</tr></thead><tbody>'];

    function row(b, gc) {
      var r = roomById(b.room), tp = r ? r.type : '';
      return '<tr data-nabk="' + esc(b.id) + '">' +
        '<td class="na-ck"><label class="sv-ck"><input type="checkbox" data-nack="' + esc(b.id) + '"' + (na.sel[b.id] ? ' checked' : '') + '>' +
          '<span class="box" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span></label></td>' +
        '<td' + (gc ? ' class="na-mk" style="--gc:' + gc + '"' : '') + '>' + esc(b.code || b.id) + '</td>' +
        '<td>' + esc(b.name) + '</td>' +
        '<td>' + (b.adults || 0) + '/' + (b.children || 0) + '/' + (b.infants || 0) + '</td>' +
        '<td>' + esc(tp + ' - ' + b.room) + '</td>' +
        '<td>' + dmy(b.in) + ' ' + hm(b.in) + '</td>' +
        '<td>' + dmy(b.out) + ' ' + hm(b.out) + '</td>' +
        '<td>' + esc(b.src || 'Rate code 1') + '</td>' +
        '<td class="num">' + money(b.total || 0) + '</td>' +
      '</tr>';
    }

    for (i = 0; i < solo.length; i++) h.push(row(solo[i], ''));
    for (i = 0; i < order.length; i++) {
      g = groups[order[i]];
      h.push('<tr class="na-grp"><td colspan="9">' + esc(g.name) + ' ( ' + g.rows.length + ' phòng )</td></tr>');
      for (var j = 0; j < g.rows.length; j++) h.push(row(g.rows[j], NA_GC[i % NA_GC.length]));
    }
    h.push('</tbody>');
    t.innerHTML = h.join('');
    naListSync();
  }

  function naSelIds() { var r = [], k; for (k in na.sel) if (na.sel[k]) r.push(k); return r; }

  function naListSync() {
    var n = naSelIds().length;
    $('naActA').disabled = !n;
    $('naActB').disabled = !n;
    var all = $('naCkAll');
    if (all) all.checked = na.rows.length > 0 && n === na.rows.length;
  }

  function naListAct(which) {
    var ids = naSelIds(); if (!ids.length) return;
    var i, b, r, n = ids.length;
    for (i = 0; i < ids.length; i++) {
      b = bkById(ids[i]); if (!b) continue;
      if (na.list === 'due') {
        if (which === 'a') { b.st = 'checkout'; r = roomById(b.room); if (r) r.dirty = true; }
        else { b.out = addDays(b.out, 1); }
      } else {
        if (which === 'a') { b.st = 'inhouse'; }
        else { BOOKINGS.splice(BOOKINGS.indexOf(b), 1); }
      }
    }
    na.sel = {};
    na.rows = na.list === 'due' ? naDue() : naNoShow();
    $('naListTitle').textContent = (na.list === 'due' ? 'Danh sách phòng đến hạn trả (' : 'Danh sách phòng không đến (') + na.rows.length + ')';
    naListRender();
    if (typeof render === 'function') render();
    toast(na.list === 'due'
      ? (which === 'a' ? 'Trả phòng thành công ' + n + ' phòng!' : 'Gia hạn thêm 1 đêm cho ' + n + ' phòng!')
      : (which === 'a' ? 'Nhận phòng thành công ' + n + ' phòng!' : 'Hủy ' + n + ' đặt phòng thành công!'));
    naRender();
  }

  /* ---------- Dialog nhỏ ---------- */

  function naSdOpen(id) { $('naSdBd').classList.add('is-open'); $(id).classList.add('is-open'); }
  function naSdClose() {
    $('naSdBd').classList.remove('is-open');
    $('naConfirmDlg').classList.remove('is-open');
    $('naDateDlg').classList.remove('is-open');
  }

  function naConfirmOpen() {
    $('naConfirmMsg').innerHTML = 'Hệ thống sẽ kiểm toán đêm <b>' + dmy(BIZ_DATE) +
      '</b> và chuyển sang ngày kinh doanh <b>' + dmy(addDays(BIZ_DATE, 1)) + '</b>!';
    naSdOpen('naConfirmDlg');
  }

  function naDateOpen() {
    na.dPick = addDays(BIZ_DATE, 1);
    $('naDateWarn').innerHTML =
      '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><path d="M12 9v4M12 17h.01"/></svg>' +
      '<span>Ngày kinh doanh <b>' + dmy(BIZ_DATE) + '</b> chưa được kiểm toán đêm! ' +
      'Việc chuyển ngày kinh doanh thủ công sẽ làm sai lệch số liệu và báo cáo của bạn.</span>';
    $('naDatePick').querySelector('.v').textContent = dmy(na.dPick);
    naSdOpen('naDateDlg');
  }

  /* ---------- Cấu hình ---------- */

  function naCfgShow() {
    na.view = 'cfg';
    na.auto = na.sAuto; na.tRun = na.sRun; na.tWarn = na.sWarn;
    for (var k in na.saved) na.on[k] = na.saved[k];
    $('naProc').hidden = true;
    $('naCfgPane').hidden = false;
    naCfgRender();
  }

  function naCfgRender() {
    $('naAuto').checked = na.auto;
    $('naTimes').hidden = !na.auto;
    $('naTimeRun').value = na.tRun;
    $('naTimeWarn').value = na.tWarn;
    var h = [], i, s;
    for (i = 0; i < NA_STEPS.length; i++) {
      s = NA_STEPS[i];
      h.push('<div class="na-cfgitem">' +
        '<span class="n">' + (i + 1) + '</span>' +
        '<label class="sv-ck t"><input type="checkbox" data-nacfg="' + s.k + '"' +
          (na.on[s.k] ? ' checked' : '') + (s.req ? ' disabled' : '') + '>' +
          '<span class="box" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span>' +
          '<span class="t">' + esc(s.n) + '</span></label>' +
        (s.req ? '<span class="req">Bước bắt buộc</span>' : '') +
      '</div>');
    }
    $('naCfgList').innerHTML = h.join('');
  }

  /* ---------- Gắn sự kiện ---------- */

  function bindNa() {
    if (!$('naCard')) return;

    $('naRun').addEventListener('click', naConfirmOpen);
    $('naEditDate').addEventListener('click', naDateOpen);
    $('naCfgBtn').addEventListener('click', naCfgShow);

    $('naSteps').addEventListener('click', function (e) {
      var l = e.target.closest('[data-nalist]');
      if (l) { naListOpen(l.getAttribute('data-nalist')); return; }
      var f = e.target.closest('[data-nafix]');
      if (f) { naRetry(f.getAttribute('data-nafix')); }
    });

    /* --- Modal danh sách --- */
    $('naBd').addEventListener('click', naListClose);
    $('naListDlg').addEventListener('click', function (e) {
      var c = e.target.closest('[data-na="listclose"]');
      if (c) { naListClose(); return; }
      var d = e.target.closest('[data-na="download"], [data-na="print"]');
      if (d) { toast(d.getAttribute('data-na') === 'print' ? 'Đang chuẩn bị bản in danh sách…' : 'Đang tải danh sách…'); return; }
      var all = e.target.closest('#naCkAll');
      if (all) {
        var on = all.checked, i;
        na.sel = {};
        if (on) for (i = 0; i < na.rows.length; i++) na.sel[na.rows[i].id] = true;
        naListRender(); return;
      }
      var ck = e.target.closest('[data-nack]');
      if (ck) {
        var id = ck.getAttribute('data-nack');
        if (ck.checked) na.sel[id] = true; else delete na.sel[id];
        naListSync();
      }
    });
    $('naActA').addEventListener('click', function () { naListAct('a'); });
    $('naActB').addEventListener('click', function () { naListAct('b'); });

    /* --- Dialog nhỏ --- */
    $('naSdBd').addEventListener('click', naSdClose);
    document.addEventListener('click', function (e) {
      var c = e.target.closest && e.target.closest('[data-na="cfmclose"], [data-na="dateclose"]');
      if (c) naSdClose();
    });
    $('naConfirmOk').addEventListener('click', function () { naSdClose(); naStart(); });
    $('naDatePick').addEventListener('click', function (e) {
      e.stopPropagation();
      dtpOpen(this, na.dPick || addDays(BIZ_DATE, 1), false, function (d) {
        na.dPick = startOfDay(d);
        $('naDatePick').querySelector('.v').textContent = dmy(na.dPick);
      });
    });
    $('naDateOk').addEventListener('click', function () {
      var d = na.dPick ? startOfDay(na.dPick) : addDays(BIZ_DATE, 1);
      /* Validate (SRS): chỉ chọn từ ngày kinh doanh hiện tại → ngày thực tế của hệ thống */
      if (d < BIZ_DATE || d > startOfDay(addDays(HOTEL_NOW, 365))) {
        toast('Ngày kinh doanh phải nằm trong khoảng từ ngày hiện tại đến ngày thực tế của hệ thống!', true);
        return;
      }
      BIZ_DATE = d;
      naSdClose(); naRender();
      if (typeof render === 'function') render();
      toast('Chuyển ngày kinh doanh thành công!');
    });

    /* --- Cấu hình --- */
    $('naAuto').addEventListener('change', function () { na.auto = this.checked; $('naTimes').hidden = !na.auto; });
    $('naTimeRun').addEventListener('change', function () { na.tRun = this.value; });
    $('naTimeWarn').addEventListener('change', function () { na.tWarn = this.value; });
    $('naCfgList').addEventListener('change', function (e) {
      var c = e.target.closest('[data-nacfg]'); if (!c) return;
      na.on[c.getAttribute('data-nacfg')] = c.checked;
    });
    $('naCfgReset').addEventListener('click', function () {
      for (var k in NA_DEF) na.on[k] = !!NA_DEF[k];
      na.auto = false; na.tRun = '04:00'; na.tWarn = '06:00';
      naCfgRender();
      toast('Khôi phục thành công');
    });
    $('naCfgSave').addEventListener('click', function () {
      for (var k in na.on) na.saved[k] = na.on[k];
      na.sAuto = na.auto; na.sRun = na.tRun; na.sWarn = na.tWarn;
      naShow();
      toast('Lưu thành công.');
    });
    $('naCfgBack').addEventListener('click', naShow);

    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape') return;
      if ($('naDateDlg').classList.contains('is-open') || $('naConfirmDlg').classList.contains('is-open')) { naSdClose(); return; }
      if ($('naListDlg').classList.contains('is-open')) naListClose();
    });

    naRender();
  }


  /* ==========================================================================
     SEARCH TỔNG — Header common, SRS 3.1 (Search tổng)
     Figma zjVU9MyBEO0VxQkdZFtWeU / 20470:2849
     - Tất cả / Đặt phòng / Chức năng
     - Mặc định: 5 đặt phòng mới nhất + 5 chức năng gần đây
     - Sau khi nhập: tối đa 10 kết quả mỗi nhóm, in đậm nền từ khoá khớp
     - Đặt phòng: tên khách = contains (bỏ dấu, bỏ hoa/thường)
                  mã đặt phòng = exact match (bỏ dấu, bỏ hoa/thường)
     - Chức năng: contains (bỏ dấu, bỏ hoa/thường)
     ====================================================================== */
  var gs = { open: false, tab: 'all', q: '', cur: -1, items: [], recentFn: [], recentBk: [] };

  function gsNorm(s) {
    return String(s == null ? '' : s)
      .normalize('NFD').replace(/[\u0300-\u036f]/g, '')
      .replace(/\u0111/g, 'd').replace(/\u0110/g, 'D')
      .toLowerCase().trim();
  }
  function gsEsc(s) {
    return String(s == null ? '' : s)
      .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
  }
  /* Tô nền từ khoá khớp — so khớp trên chuỗi đã bỏ dấu nhưng cắt trên chuỗi gốc
     để giữ nguyên tiếng Việt hiển thị. */
  function gsHl(text, q) {
    var raw = String(text == null ? '' : text);
    if (!q) return gsEsc(raw);
    var nq = gsNorm(q);
    if (!nq) return gsEsc(raw);
    var nr = gsNorm(raw);
    if (nr.length !== raw.length) return gsEsc(raw);   // độ dài lệch → không cắt được an toàn
    var out = '', i = 0;
    while (i < raw.length) {
      var k = nr.indexOf(nq, i);
      if (k < 0) { out += gsEsc(raw.slice(i)); break; }
      out += gsEsc(raw.slice(i, k)) + '<span class="gs-hl">' + gsEsc(raw.slice(k, k + nq.length)) + '</span>';
      i = k + nq.length;
    }
    return out;
  }

  var GS_ST_IC = {
    inhouse:  '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M3 7v11"/><path d="M3 14h18"/><path d="M21 18v-8a2 2 0 0 0-2-2h-8v6"/><circle cx="7" cy="10" r="1"/></svg>',
    booked:   '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/><path d="M8.5 15l2.5 2.5 4.5-4.5"/></svg>',
    checkout: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M13 3H6a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h7"/><path d="M10 12h11M17.5 8.5 21 12l-3.5 3.5"/></svg>',
    noshow:   '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><circle cx="10" cy="8" r="3.5"/><path d="M3 20c0-3.5 3-6 7-6"/><path d="M16 15l5 5M21 15l-5 5"/></svg>',
    cancel:   '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"><path d="M6 6l12 12M18 6L6 18"/></svg>'
  };
  function gsStIc(st) { return GS_ST_IC[st] || GS_ST_IC.booked; }

  /* Danh sách chức năng: lấy từ MENU (nav ezOne.PMS) — "Cha - Con (Module)" */
  function gsFunctions() {
    var out = [];
    try {
      (typeof MENU !== 'undefined' ? MENU : []).forEach(function (m) {
        if (m.type === 'single') {
          out.push({ key: m.key, label: m.label, icon: m.icon, mod: 'PMS' });
        } else if (m.children) {
          m.children.forEach(function (ch) {
            out.push({ key: ch.key, label: m.label + ' - ' + ch.label, icon: ch.icon || m.icon, mod: 'PMS' });
          });
        }
      });
    } catch (e) { }
    return out;
  }
  function gsFnIc(name) {
    var IC = (typeof ICONS !== 'undefined') ? ICONS : {};
    var inner = IC[name] || IC['layout-grid'] || '';
    return '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">' + inner + '</svg>';
  }

  /* Đặt phòng mới nhất: sắp theo ngày nhận gần hiện tại nhất */
  function gsRecentBookings() {
    if (gs.recentBk.length) {
      var byId = {};
      gs.recentBk.forEach(function (id) { var b = bkById(id); if (b) byId[id] = b; });
      var picked = gs.recentBk.map(function (id) { return byId[id]; }).filter(Boolean);
      if (picked.length >= 5) return picked.slice(0, 5);
      var rest = gsAllBookings().filter(function (b) { return gs.recentBk.indexOf(b.id) < 0; });
      return picked.concat(rest).slice(0, 5);
    }
    return gsAllBookings().slice(0, 5);
  }
  function gsAllBookings() {
    return BOOKINGS.filter(function (b) { return b.st !== 'repair'; })
      .slice().sort(function (a, b) { return b.in - a.in; });
  }
  function gsRecentFunctions() {
    var all = gsFunctions();
    if (!gs.recentFn.length) return all.slice(0, 5);
    var m = {}; all.forEach(function (f) { m[f.key] = f; });
    var picked = gs.recentFn.map(function (k) { return m[k]; }).filter(Boolean);
    var rest = all.filter(function (f) { return gs.recentFn.indexOf(f.key) < 0; });
    return picked.concat(rest).slice(0, 5);
  }

  function gsMatchBooking(b, q) {
    var nq = gsNorm(q);
    if (!nq) return true;
    /* mã đặt phòng — exact match */
    var code = gsNorm(String(b.code || '').replace(/^#/, ''));
    if (code === nq || gsNorm(b.code) === nq) return true;
    if (gsNorm(b.id) === nq) return true;
    if (b.ota && gsNorm(String(b.ota).replace(/^#/, '')) === nq) return true;
    /* tên khách — contains */
    if (gsNorm(b.name).indexOf(nq) >= 0) return true;
    var g = b.guests || [];
    for (var i = 0; i < g.length; i++) if (gsNorm(g[i].n).indexOf(nq) >= 0) return true;
    return false;
  }
  function gsGuestLine(b) {
    var g = (b.guests || []).map(function (x) { return x.n; });
    if (!g.length && b.name) g = [b.name];
    return g.join(', ');
  }
  function gsTimeLine(b) {
    var s = dmy(b.in) + ' ' + hm(b.in) + ' - ' + dmy(b.out) + ' ' + hm(b.out);
    var n = nightsOf(b);
    return n >= 1 ? s + ' (' + n + ' đêm)' : s;
  }

  function gsBkHtml(b, q, idx) {
    var ota = b.ota
      ? '<span class="ota"><span class="lg" style="background:' + (b.src && /agoda/i.test(b.src) ? '#F05A2A' : '#003580') + '">' +
        gsEsc(String(b.src || 'OTA').slice(0, 1).toUpperCase()) + '</span>' + gsHl(b.ota, q) + '</span>'
      : '';
    return '<button class="gs-bk" type="button" role="option" data-gsi="' + idx + '" data-gsbk="' + gsEsc(b.id) + '">' +
      '<span class="st" data-st="' + gsEsc(b.st) + '" aria-hidden="true">' + gsStIc(b.st) + '</span>' +
      '<span class="dt">' +
        '<span class="rm">' + gsHl(fullRoomName(b), q) + '</span>' +
        '<span class="gu">' + gsHl(gsGuestLine(b), q) + '</span>' +
        '<span class="tm">' + gsEsc(gsTimeLine(b)) + '</span>' +
      '</span>' +
      '<span class="id"><span class="bk">' + gsHl(b.code, q) + '</span>' + ota + '</span>' +
    '</button>';
  }
  function gsFnHtml(f, q, idx) {
    return '<button class="gs-fn" type="button" role="option" data-gsi="' + idx + '" data-gsfn="' + gsEsc(f.key) + '">' +
      gsFnIc(f.icon) + '<span class="tx">' + gsHl(f.label, q) + ' (' + gsEsc(f.mod) + ')</span></button>';
  }

  function gsRender() {
    var q = gs.q.trim();
    var body = $('gsBody');
    var bks = [], fns = [];
    if (gs.tab !== 'function') {
      bks = q ? gsAllBookings().filter(function (b) { return gsMatchBooking(b, q); }).slice(0, 10)
              : gsRecentBookings();
    }
    if (gs.tab !== 'booking') {
      fns = q ? gsFunctions().filter(function (f) { return gsNorm(f.label).indexOf(gsNorm(q)) >= 0; }).slice(0, 10)
              : gsRecentFunctions();
    }
    if (!bks.length && !fns.length) {
      body.innerHTML =
        '<div class="gs-empty">' +
          '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">' +
            '<path d="M14 3H7a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V8z"/><path d="M14 3v5h5"/><path d="M12 11v4M12 18h.01"/></svg>' +
          '<h4>Không có kết quả phù hợp</h4>' +
          '<p>Hãy thử tìm kiếm lại với từ khoá khác</p>' +
        '</div>';
      gs.items = []; gs.cur = -1;
      return;
    }
    var idx = 0, html = '';
    gs.items = [];
    if (bks.length) {
      html += '<section class="gs-sec"><h3 class="gs-sec-t">' + (q ? 'Đặt phòng' : 'Đặt phòng mới nhất') + '</h3><div class="gs-list">';
      bks.forEach(function (b) { html += gsBkHtml(b, q, idx); gs.items.push({ t: 'bk', id: b.id }); idx++; });
      html += '</div></section>';
    }
    if (fns.length) {
      html += '<section class="gs-sec"><h3 class="gs-sec-t">' + (q ? 'Chức năng' : 'Chức năng gần đây') + '</h3><div class="gs-list">';
      fns.forEach(function (f) { html += gsFnHtml(f, q, idx); gs.items.push({ t: 'fn', id: f.key, label: f.label }); idx++; });
      html += '</div></section>';
    }
    body.innerHTML = html;
    gs.cur = gs.items.length ? 0 : -1;
    gsMark();
  }
  function gsMark() {
    var body = $('gsBody');
    Array.prototype.forEach.call(body.querySelectorAll('[data-gsi]'), function (el) {
      var on = parseInt(el.dataset.gsi, 10) === gs.cur;
      el.classList.toggle('is-cur', on);
      if (on && el.scrollIntoView) el.scrollIntoView({ block: 'nearest' });
    });
  }
  function gsMove(d) {
    if (!gs.items.length) return;
    gs.cur = (gs.cur + d + gs.items.length) % gs.items.length;
    gsMark();
  }
  function gsPick(i) {
    var it = gs.items[i];
    if (!it) return;
    if (it.t === 'bk') {
      gs.recentBk = [it.id].concat(gs.recentBk.filter(function (x) { return x !== it.id; })).slice(0, 5);
      gsClose();
      openBookingDetail(it.id);
    } else {
      gs.recentFn = [it.id].concat(gs.recentFn.filter(function (x) { return x !== it.id; })).slice(0, 5);
      gsClose();
      toast('Điều hướng tới chức năng: ' + it.label);
    }
  }

  function gsOpen() {
    if (gs.open) return;
    gs.open = true; gs.q = ''; gs.tab = 'all';
    $('gsQ').value = '';
    Array.prototype.forEach.call($('gsSeg').querySelectorAll('[data-gstab]'), function (b) {
      b.setAttribute('aria-selected', b.dataset.gstab === 'all' ? 'true' : 'false');
    });
    $('gsBd').classList.add('is-open');
    $('gsCard').classList.add('is-open');
    $('gsTrigger').setAttribute('aria-expanded', 'true');
    gsRender();
    $('gsQ').focus();
  }
  function gsClose() {
    if (!gs.open) return;
    gs.open = false;
    $('gsBd').classList.remove('is-open');
    $('gsCard').classList.remove('is-open');
    $('gsTrigger').setAttribute('aria-expanded', 'false');
  }

  function bindGs() {
    if (!$('gsCard')) return;
    if (/Mac|iPhone|iPad/i.test(navigator.platform || navigator.userAgent)) $('gsKbd').textContent = '\u2318 K';
    $('gsTrigger').addEventListener('click', gsOpen);
    $('gsTrigger').addEventListener('keydown', function (e) {
      if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); gsOpen(); }
    });
    $('gsX').addEventListener('click', gsClose);
    $('gsBd').addEventListener('click', gsClose);
    $('gsQ').addEventListener('input', function () {
      if (this.value.length > 255) this.value = this.value.slice(0, 255);
      gs.q = this.value; gsRender();
    });
    $('gsSeg').addEventListener('click', function (e) {
      var b = e.target.closest('[data-gstab]');
      if (!b) return;
      gs.tab = b.dataset.gstab;
      Array.prototype.forEach.call(this.querySelectorAll('[data-gstab]'), function (x) {
        x.setAttribute('aria-selected', x === b ? 'true' : 'false');
      });
      gsRender();
      $('gsQ').focus();
    });
    $('gsBody').addEventListener('click', function (e) {
      var b = e.target.closest('[data-gsi]');
      if (b) gsPick(parseInt(b.dataset.gsi, 10));
    });
    $('gsBody').addEventListener('mousemove', function (e) {
      var b = e.target.closest('[data-gsi]');
      if (b) { gs.cur = parseInt(b.dataset.gsi, 10); gsMark(); }
    });
    document.addEventListener('keydown', function (e) {
      var k = (e.key || '').toLowerCase();
      if (k === 'k' && (e.ctrlKey || e.metaKey)) { e.preventDefault(); gs.open ? gsClose() : gsOpen(); return; }
      if (!gs.open) return;
      if (e.key === 'Escape') { e.preventDefault(); gsClose(); return; }
      if (e.key === 'ArrowDown') { e.preventDefault(); gsMove(1); return; }
      if (e.key === 'ArrowUp') { e.preventDefault(); gsMove(-1); return; }
      if (e.key === 'Enter') { e.preventDefault(); gsPick(gs.cur); return; }
    });
  }


  /* ==========================================================================
     R01.22 — QUY TRÌNH NHẬN PHÒNG (CHECK-IN)
     SRS: R01.22_Checkin_v2.0 | Figma: eaPQ9ueUW2YmRpjucaNEBo / 32496:70835
     Bước 1: Xác định thời gian nhận phòng · Bước 2: Tạo thẻ từ
     MSG_01 Cài lại mặc định · MSG_02 Nhận phòng thành công
     MSG_03 Xác nhận huỷ    · MSG_04 Tạo thẻ thành công
     ====================================================================== */
  var ci = { open: false, step: 1, rows: [], collapsed: {}, tab: 'all', done: false, cur: -1, writing: false };

  var CI_IC = {
    booked:  '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M8 2.5v4M16 2.5v4M3 10h18"/><path d="M8.5 15l2.5 2.5 4.5-4.5"/></svg>',
    inhouse: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M3 7v11"/><path d="M3 14h18"/><path d="M21 18v-8a2 2 0 0 0-2-2h-8v6"/><circle cx="7" cy="10" r="1"/></svg>',
    flagVN:  '<svg viewBox="0 0 30 20"><rect width="30" height="20" fill="#DA251D"/><path d="M15 4.4l1.6 4.9h5.2l-4.2 3 1.6 4.9-4.2-3-4.2 3 1.6-4.9-4.2-3h5.2z" fill="#FF0"/></svg>',
    lead:    '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M5 3v18h1.6v-7.4h9.9l-1.6-3.3 1.6-3.3H6.6V3z"/></svg>',
    tick:    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12.5l4.5 4.5L19 7"/></svg>'
  };

  function ciEsc(s) {
    return String(s == null ? '' : s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
  }
  function ciDT(d) { return dmy(d) + ' ' + hm(d); }
  function ciNights(a, b) { return Math.max(0, Math.round((startOfDay(b) - startOfDay(a)) / 86400000)); }

  /* ---- Dựng danh sách phòng cần nhận ----
     SRS 3.1.1 Pre-condition: chỉ booking trạng thái "Booked" và đã gán phòng.
     Gom cùng đoàn / cùng công ty giống luồng trả phòng để nhận theo lô. */
  function ciBuildRows(bkId) {
    var b = bkById(bkId);
    if (!b) return [];
    var mates = BOOKINGS.filter(function (x) {
      if (x.st !== 'booked' || !x.room) return false;
      if (x.id === b.id) return true;
      return !!(b.group && x.group && x.group.gid === b.group.gid) || !!(b.company && x.company === b.company);
    });
    if (!mates.length) mates = [b];
    return mates.map(function (x) {
      var room = roomById(x.room);
      var n0 = Math.max(1, ciNights(x.in, x.out));
      var rate = Math.round((x.total || 0) / n0);
      return {
        id: x.id, bk: x, type: room ? typeName(room.type) : '—', room: x.room,
        guest: x.name, lead: !!(x.group && x.group.lead === x.name) || !!x.group,
        adults: x.adults || 2, children: x.children || 0, infants: x.infants || 0,
        in: new Date(x.in), out: new Date(x.out),
        policy: 'VIP' + (1 + (rvHash(x.id) % 3)),
        bill: 'keep',                       // keep | now  (SRS mục 10)
        nights0: n0, rate: rate, old: x.total || 0,
        cards: 0, need: 2, madeAt: null, madeBy: null, sel: false
      };
    });
  }
  /* Số đêm hiện tại phụ thuộc lựa chọn thời điểm tính tiền (SRS mục 11) */
  function ciBillFrom(r) { return r.bill === 'now' ? HOTEL_NOW : r.in; }
  function ciNightsOf(r) { return r.bill === 'now' ? Math.max(0, ciNights(ciBillFrom(r), r.out)) : r.nights0; }
  function ciFinal(r) { return ciNightsOf(r) * r.rate; }
  function ciDiff(r) { return ciFinal(r) - r.old; }     // SRS mục 14: Chênh lệch

  function ciGroups() {
    var map = {}, order = [];
    ci.rows.forEach(function (r) {
      if (!map[r.type]) { map[r.type] = []; order.push(r.type); }
      map[r.type].push(r);
    });
    return order.map(function (k) { return { type: k, rows: map[k] }; });
  }
  function ciTotals(list) {
    var t = { old: 0, diff: 0, fin: 0 };
    (list || ci.rows).forEach(function (r) { t.old += r.old; t.diff += ciDiff(r); t.fin += ciFinal(r); });
    return t;
  }
  function ciMoneyDiff(v) {
    if (!v) return '0';
    return '<span class="ci-diff ' + (v > 0 ? 'up' : 'dn') + '">' + (v > 0 ? '+' : '-') + money(Math.abs(v)) + '</span>';
  }

  /* ---------------- Bước 1: bảng xác định thời gian ---------------- */
  function ciRenderStep1() {
    var h = '<thead><tr>' +
      '<th>Phòng (' + ci.rows.length + ')</th>' +
      '<th>Khách hàng</th>' +
      '<th>Thời gian ở dự kiến</th>' +
      '<th>Thời điểm tính tiền</th>' +
      '<th class="c">Số đêm</th>' +
      '<th>Chính sách giá</th>' +
      '<th class="num">Giá cũ</th>' +
      '<th class="num">Chênh lệch</th>' +
      '<th class="num">Giá cuối</th>' +
      '</tr></thead><tbody>';

    ciGroups().forEach(function (g) {
      var closed = !!ci.collapsed[g.type];
      var gt = ciTotals(g.rows);
      var gAll = g.rows.every(function (r) { return r.bill === 'now'; });
      var gKeep = g.rows.every(function (r) { return r.bill === 'keep'; });
      h += '<tr class="ci-grp' + (closed ? ' is-closed' : '') + '" data-cigrp="' + ciEsc(g.type) + '">' +
        '<td colspan="3"><span class="ci-grpn">' +
          '<svg class="gcar" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg>' +
          ciEsc(g.type) + ' (' + g.rows.length + ')</span></td>' +
        '<td>' + (ci.done ? '' : ciRadioGroup('g:' + g.type, gKeep, gAll)) + '</td>' +
        '<td class="c"></td><td></td>' +
        '<td class="num">' + money(gt.old) + '</td>' +
        '<td class="num">' + ciMoneyDiff(gt.diff) + '</td>' +
        '<td class="num">' + money(gt.fin) + '</td>' +
        '</tr>';
      if (closed) return;
      g.rows.forEach(function (r, i) {
        var idx = ci.rows.indexOf(r);
        var n = ciNightsOf(r), d = n - r.nights0;
        h += '<tr class="ci-row" data-cirow="' + idx + '">' +
          '<td><span class="ci-rm"><span class="ic" data-st="' + (ci.done ? 'inhouse' : 'booked') + '">' +
            (ci.done ? CI_IC.inhouse : CI_IC.booked) + '</span>' + ciEsc(r.type + ' - ' + r.room) + '</span></td>' +
          '<td><span class="ci-gu"><span class="fg">' + CI_IC.flagVN + '</span>' +
            '<span class="nm">' + ciEsc(r.guest) + '</span>' +
            (r.lead ? '<span class="lead">' + CI_IC.lead + '</span>' : '') + '</span></td>' +
          '<td class="ci-stay">' + ciDT(r.in) + '<br>' + ciDT(r.out) + '</td>' +
          '<td>' + (ci.done
            ? '<span class="ci-billlb">' + ciDT(ciBillFrom(r)) + '</span>'
            : ciRadioGroup('r:' + idx, r.bill === 'keep', r.bill === 'now')) + '</td>' +
          '<td class="c"><span class="ci-n">' + n + (d ? '<span class="d">(' + (d > 0 ? '+' : '') + d + ')</span>' : '') + '</span></td>' +
          '<td>' + ciEsc(r.policy) + '</td>' +
          '<td class="num">' + money(r.old) + '</td>' +
          '<td class="num">' + ciMoneyDiff(ciDiff(r)) + '</td>' +
          '<td class="num">' + money(ciFinal(r)) + '</td>' +
          '</tr>';
      });
    });
    h += '</tbody>';
    $('ciTbl').innerHTML = h;

    var t = ciTotals();
    $('ciSum').hidden = false;
    $('ciSum').innerHTML =
      '<span class="lb">Tổng cộng (VND)</span>' +
      '<span class="v">' + money(t.old) + '</span>' +
      '<span class="v">' + ciMoneyDiff(t.diff) + '</span>' +
      '<span class="v">' + money(t.fin) + '</span>';
  }
  function ciRadioGroup(key, keep, now) {
    return '<span class="ci-rg">' +
      '<label class="ci-rd"><input type="radio" name="cib_' + ciEsc(key) + '" data-cibill="' + ciEsc(key) + '" value="keep"' + (keep ? ' checked' : '') + '><span class="dot"></span>Giữ nguyên</label>' +
      '<label class="ci-rd"><input type="radio" name="cib_' + ciEsc(key) + '" data-cibill="' + ciEsc(key) + '" value="now"' + (now ? ' checked' : '') + '><span class="dot"></span>Ngày giờ hiện tại</label>' +
      '</span>';
  }

  /* ---------------- Bước 2: tạo thẻ từ ---------------- */
  function ciCardState(r) { return r.cards >= r.need && r.need > 0 ? 'done' : (r.cards > 0 ? 'part' : 'none'); }
  function ciTagHtml(r) {
    var s = ciCardState(r);
    return '<span class="ci-tag" data-t="' + s + '">' +
      (s === 'done' ? 'Đã tạo' : s === 'part' ? 'Đang tạo (' + r.cards + '/' + r.need + ')' : 'Chưa tạo') + '</span>';
  }
  function ciStep2Rows() {
    return ci.tab === 'none' ? ci.rows.filter(function (r) { return ciCardState(r) !== 'done'; }) : ci.rows;
  }
  function ciRenderStep2() {
    var list = ciStep2Rows();
    $('ciTools').hidden = false;
    $('ciSum').hidden = true;
    $('ciTools').querySelector('[data-citab="all"]').textContent = 'Tất cả (' + ci.rows.length + ')';
    $('ciTools').querySelector('[data-citab="none"]').textContent =
      'Chưa tạo (' + ci.rows.filter(function (r) { return ciCardState(r) !== 'done'; }).length + ')';
    Array.prototype.forEach.call($('ciTools').querySelectorAll('[data-citab]'), function (b) {
      b.setAttribute('aria-selected', b.dataset.citab === ci.tab ? 'true' : 'false');
    });

    var head = '<thead><tr>' +
      '<th style="width:44px"><label class="ci-ck"><input type="checkbox" id="ciAll"><span class="box">' + CI_IC.tick + '</span></label></th>' +
      '<th>Phòng</th><th>Khách hàng</th><th>Lượng khách</th><th class="c">Số thẻ</th><th>Trạng thái</th>' +
      '<th>Thời hạn</th><th>Ngày tạo</th><th>Người tạo</th><th></th>' +
      '</tr></thead><tbody>';

    if (!list.length) {
      $('ciTbl').innerHTML = head + '</tbody>';
      $('ciWrap').hidden = true;
      $('ciEmpty').hidden = false;
      $('ciSelbar').classList.remove('is-on');
      ciFootState();
      return;
    }
    $('ciWrap').hidden = false;
    $('ciEmpty').hidden = true;

    var h = head;
    var byType = {}, order = [];
    list.forEach(function (r) { if (!byType[r.type]) { byType[r.type] = []; order.push(r.type); } byType[r.type].push(r); });
    order.forEach(function (ty) {
      var rows = byType[ty], closed = !!ci.collapsed[ty];
      var allSel = rows.every(function (r) { return r.sel; });
      h += '<tr class="ci-grp' + (closed ? ' is-closed' : '') + '" data-cigrp="' + ciEsc(ty) + '">' +
        '<td><label class="ci-ck"><input type="checkbox" data-cigsel="' + ciEsc(ty) + '"' + (allSel ? ' checked' : '') + '><span class="box">' + CI_IC.tick + '</span></label></td>' +
        '<td colspan="9"><span class="ci-grpn">' +
        '<svg class="gcar" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg>' +
        ciEsc(ty) + ' (' + rows.length + ')</span></td></tr>';
      if (closed) return;
      rows.forEach(function (r) {
        var idx = ci.rows.indexOf(r);
        h += '<tr class="ci-row" data-cirow="' + idx + '">' +
          '<td><label class="ci-ck"><input type="checkbox" data-cisel="' + idx + '"' + (r.sel ? ' checked' : '') + '><span class="box">' + CI_IC.tick + '</span></label></td>' +
          '<td><span class="ci-rm"><span class="ic" data-st="inhouse">' + CI_IC.inhouse + '</span>' + ciEsc(r.type + ' - ' + r.room) + '</span></td>' +
          '<td><span class="ci-gu"><span class="fg">' + CI_IC.flagVN + '</span><span class="nm">' + ciEsc(r.guest) + '</span></span></td>' +
          '<td>' + r.adults + 'NL/' + r.children + 'TE/' + r.infants + 'EB</td>' +
          '<td class="c">' + r.cards + '/' + r.need + '</td>' +
          '<td>' + ciTagHtml(r) + '</td>' +
          '<td class="ci-stay">' + ciDT(ciBillFrom(r)) + '<br>' + ciDT(r.out) + '</td>' +
          '<td>' + (r.madeAt ? ciDT(r.madeAt) : '--') + '</td>' +
          '<td>' + (r.madeBy
              ? '<span class="ci-user"><span class="av">' + ciEsc(r.madeBy.slice(0, 1)) + '</span>' + ciEsc(r.madeBy) + '</span>'
              : '--') + '</td>' +
          '<td class="num"><button class="ci-linkbtn" type="button" data-cimake="' + idx + '"' + (r.sel ? ' disabled' : '') + '>' +
            (ciCardState(r) === 'done' ? 'Thêm thẻ' : 'Tạo thẻ') + '</button></td>' +
          '</tr>';
      });
    });
    h += '</tbody>';
    $('ciTbl').innerHTML = h;

    var sel = ci.rows.filter(function (r) { return r.sel; });
    $('ciSelbar').classList.toggle('is-on', sel.length > 0);
    $('ciSelTxt').textContent = 'Đã chọn (' + sel.length + ')';
    var all = $('ciAll');
    if (all) all.checked = list.length > 0 && list.every(function (r) { return r.sel; });
    ciFootState();
  }

  function ciFootState() {
    var s1 = ci.step === 1;
    $('ciBack').hidden = s1;
    $('ciLater').hidden = s1;
    $('ciOk').hidden = !(s1 && !ci.done);
    $('ciNext').hidden = !(s1 && ci.done);
    var allDone = ci.rows.length > 0 && ci.rows.every(function (r) { return ciCardState(r) === 'done'; });
    $('ciBulk').hidden = s1 || allDone;
    $('ciFinish').hidden = s1 || !allDone;
    $('ciReset').hidden = !s1;
  }
  function ciRender() {
    $('ciTools').hidden = ci.step === 1;
    if (ci.step === 1) {
      $('ciSelbar').classList.remove('is-on');
      $('ciWrap').hidden = false;
      $('ciEmpty').hidden = true;
      ciRenderStep1();
    } else {
      ciRenderStep2();
    }
    Array.prototype.forEach.call(document.querySelectorAll('#ciCard [data-cistep]'), function (el) {
      var n = parseInt(el.dataset.cistep, 10);
      el.classList.toggle('is-cur', n === ci.step);
      el.classList.toggle('is-done', n < ci.step);
    });
    ciFootState();
  }

  /* ---------------- Mở / đóng ---------------- */
  function ciOpen(bkId) {
    ci.rows = ciBuildRows(bkId);
    if (!ci.rows.length) { toast('Không tìm thấy phòng đủ điều kiện nhận phòng', true); return; }
    ci.step = 1; ci.done = false; ci.tab = 'all'; ci.collapsed = {}; ci.open = true;
    $('ciBd').classList.add('is-open');
    $('ciCard').classList.add('is-open');
    ciRender();
    setTimeout(function () { $('ciClose').focus(); }, 0);
  }
  function ciClose() {
    ciCPopClose();
    ci.open = false;
    $('ciCard').classList.remove('is-open');
    $('ciBd').classList.remove('is-open');
  }
  /* MSG_03 — Xác nhận huỷ */
  function ciTryClose() {
    if (!ci.done) { ciClose(); return; }
    confirmDlg('Xác nhận huỷ',
      'Bạn có chắc muốn hủy thao tác này? Thông tin đã nhập sẽ không được lưu.',
      'Đồng ý', function () { ciClose(); });
  }

  /* MSG_01 — Cài lại mặc định */
  function ciResetAll() {
    confirmDlg('Xác nhận cài lại mặc định',
      'Thao tác này khôi phục toàn bộ phòng về trạng thái chưa gán phòng. Bạn có muốn tiếp tục không?',
      'Xác nhận', function () {
        ci.rows.forEach(function (r) {
          r.bill = 'keep'; r.cards = 0; r.madeAt = null; r.madeBy = null; r.sel = false;
          if (ci.done && r.bk.st === 'inhouse') r.bk.st = 'booked';
        });
        ci.done = false; ci.step = 1;
        render();
        ciRender();
      });
  }

  /* Bước 1 → xác nhận check-in (SRS mục 19) */
  function ciConfirm() {
    var t = ciTotals();
    openGenDlg('Xác nhận khách nhận ' + ci.rows.length + ' phòng',
      '<p style="margin:0 0 16px">Hệ thống sẽ chuyển trạng thái phòng sang đã nhận phòng!</p>' +
      '<p style="margin:0">Tổng cộng: ' + money(t.fin) + 'đ</p>' +
      '<p style="margin:4px 0 0">Chênh lệch: ' +
        (t.diff ? '<span style="color:' + (t.diff > 0 ? '#28C76F' : '#FF4C51') + '">' + (t.diff > 0 ? '+' : '-') + money(Math.abs(t.diff)) + 'đ</span>' : '0đ') +
      '</p>',
      '<button class="tl-btn-ghost" type="button" data-tl-close>Đóng</button>' +
      '<button class="tl-btn-primary" type="button" id="ciConfirmOk">Xác nhận</button>');
    $('ciConfirmOk').onclick = function () {
      closeDlgs();
      (function () {
        ci.rows.forEach(function (r) {
          r.bk.st = 'inhouse';
          if (r.bill === 'now') r.bk.in = new Date(HOTEL_NOW);   // SRS: đổi Ngày đến dự kiến -> ngày đến thực tế
          r.bk.total = ciFinal(r);
        });
        ci.done = true;
        render();
        ciRender();
        toast('Nhận phòng thành công!');                          // MSG_02
      })();
    };
  }

  /* ---------------- Popup tạo thẻ ---------------- */
  function ciCPopOpen(list) {
    ci.pop = list.slice();
    if (!ci.pop.length) { toast('Chọn phòng cần tạo thẻ', true); return; }
    ci.cur = 0;
    $('ciCBd').classList.add('is-open');
    $('ciCPop').classList.add('is-open');
    ciCPopRender();
  }
  function ciCPopClose() {
    $('ciCBd').classList.remove('is-open');
    $('ciCPop').classList.remove('is-open');
    ci.pop = null;
  }
  function ciCPopRender() {
    if (!ci.pop) return;
    var h = '<thead><tr><th>Phòng</th><th>Lượng khách</th><th class="c">Số thẻ</th><th>Trạng thái</th></tr></thead><tbody>';
    ci.pop.forEach(function (r, i) {
      h += '<tr' + (i === ci.cur ? ' class="is-cur"' : '') + ' data-cipop="' + i + '">' +
        '<td><span class="ci-rm"><span class="ic" data-st="inhouse">' + CI_IC.inhouse + '</span>' + ciEsc(r.type + ' - ' + r.room) + '</span></td>' +
        '<td>' + r.adults + 'NL/' + r.children + 'TE/' + r.infants + 'EB</td>' +
        '<td class="c">' + r.cards + '/' + r.need + '</td>' +
        '<td>' + ciTagHtml(r) + '</td></tr>';
    });
    $('ciCTbl').innerHTML = h + '</tbody>';

    var r = ci.pop[ci.cur];
    if (!r) return;
    $('ciCRoom').textContent = r.type + ' ' + r.room;
    var st = ciCardState(r);
    $('ciCTag').dataset.t = st;
    $('ciCTag').textContent = st === 'done' ? 'Đã tạo' : st === 'part' ? 'Đang tạo (' + r.cards + '/' + r.need + ')' : 'Chưa tạo';
    $('ciCQty').value = r.need;
    $('ciCMade').textContent = r.cards + '/' + r.need;
    $('ciCValid').textContent = ciDT(ciBillFrom(r)) + ' - ' + ciDT(r.out);
    $('ciCDn').disabled = r.need <= 0;
    $('ciCGo').disabled = ci.writing;
    $('ciCGo').innerHTML = ci.writing ? 'Đang tạo ...' : 'Tạo thẻ phòng<kbd>Ctrl</kbd>';
  }
  function ciWrite() {
    if (ci.writing || !ci.pop) return;
    var r = ci.pop[ci.cur];
    if (!r) return;
    if (r.need <= 0) { toast('Tổng số thẻ phải lớn hơn 0', true); return; }
    if (r.cards >= r.need) { toast('Phòng này đã tạo đủ thẻ', true); return; }
    ci.writing = true; ciCPopRender();
    setTimeout(function () {
      ci.writing = false;
      r.cards += 1;
      r.madeAt = new Date(HOTEL_NOW);
      r.madeBy = 'User A';
      toast('Tạo thẻ thành công');                                // MSG_04
      /* Đủ thẻ -> tự nhảy sang phòng tiếp theo chưa xong */
      if (r.cards >= r.need) {
        var nx = -1;
        for (var i = 0; i < ci.pop.length; i++) {
          var k = (ci.cur + 1 + i) % ci.pop.length;
          if (ciCardState(ci.pop[k]) !== 'done') { nx = k; break; }
        }
        if (nx >= 0) ci.cur = nx;
      }
      ciCPopRender();
      ciRender();
    }, 600);
  }

  function bindCi() {
    if (!$('ciCard')) return;
    $('ciClose').addEventListener('click', ciTryClose);
    $('ciBd').addEventListener('click', ciTryClose);
    $('ciReset').addEventListener('click', ciResetAll);
    $('ciOk').addEventListener('click', ciConfirm);
    $('ciNext').addEventListener('click', function () { ci.step = 2; ci.tab = 'all'; ciRender(); });
    $('ciBack').addEventListener('click', function () { ci.step = 1; ciRender(); });
    $('ciLater').addEventListener('click', ciClose);
    $('ciFinish').addEventListener('click', function () { ciClose(); toast('Hoàn tất quy trình nhận phòng'); });
    $('ciBulk').addEventListener('click', function () {
      ciCPopOpen(ci.rows.filter(function (r) { return ciCardState(r) !== 'done'; }));
    });
    $('ciSelMake').addEventListener('click', function () {
      ciCPopOpen(ci.rows.filter(function (r) { return r.sel; }));
    });
    $('ciTools').addEventListener('click', function (e) {
      var b = e.target.closest('[data-citab]');
      if (b) { ci.tab = b.dataset.citab; ciRender(); }
    });

    $('ciBody').addEventListener('change', function (e) {
      var t = e.target;
      if (t.dataset.cibill !== undefined) {
        var key = t.dataset.cibill, v = t.value;
        if (key.indexOf('g:') === 0) {
          var ty = key.slice(2);
          ci.rows.forEach(function (r) { if (r.type === ty) r.bill = v; });
        } else {
          var r2 = ci.rows[parseInt(key.slice(2), 10)];
          if (r2) r2.bill = v;
        }
        ciRender(); return;
      }
      if (t.dataset.cisel !== undefined) {
        var r3 = ci.rows[parseInt(t.dataset.cisel, 10)];
        if (r3) r3.sel = t.checked;
        ciRenderStep2(); return;
      }
      if (t.dataset.cigsel !== undefined) {
        var ty2 = t.dataset.cigsel;
        ci.rows.forEach(function (r) { if (r.type === ty2) r.sel = t.checked; });
        ciRenderStep2(); return;
      }
      if (t.id === 'ciAll') {
        var vis = ciStep2Rows();
        vis.forEach(function (r) { r.sel = t.checked; });
        ciRenderStep2(); return;
      }
    });
    $('ciBody').addEventListener('click', function (e) {
      var mk = e.target.closest('[data-cimake]');
      if (mk) { ciCPopOpen([ci.rows[parseInt(mk.dataset.cimake, 10)]]); return; }
      if (e.target.closest('.ci-ck') || e.target.closest('.ci-rd')) return;
      var g = e.target.closest('[data-cigrp]');
      if (g) { var k = g.dataset.cigrp; ci.collapsed[k] = !ci.collapsed[k]; ciRender(); }
    });

    /* Popup tạo thẻ */
    $('ciCX').addEventListener('click', ciCPopClose);
    $('ciCBd').addEventListener('click', ciCPopClose);
    $('ciCTbl').addEventListener('click', function (e) {
      var tr = e.target.closest('[data-cipop]');
      if (tr) { ci.cur = parseInt(tr.dataset.cipop, 10); ciCPopRender(); }
    });
    $('ciCUp').addEventListener('click', function () { ciQty(1); });
    $('ciCDn').addEventListener('click', function () { ciQty(-1); });
    $('ciCQty').addEventListener('input', function () {
      var r = ci.pop && ci.pop[ci.cur];
      if (!r) return;
      var v = parseInt(this.value, 10);
      r.need = isNaN(v) || v < 0 ? 0 : v;                          // SRS mục 9: 0 <= Tổng số thẻ
      $('ciCMade').textContent = r.cards + '/' + r.need;
      $('ciCDn').disabled = r.need <= 0;
    });
    $('ciCGo').addEventListener('click', ciWrite);
    document.addEventListener('keydown', function (e) {
      if (!$('ciCPop').classList.contains('is-open')) return;
      if (e.key === 'Escape') { e.preventDefault(); ciCPopClose(); return; }
      if (e.key === 'Control' || e.key === 'Enter') { e.preventDefault(); ciWrite(); }
    });
  }
  function ciQty(d) {
    var r = ci.pop && ci.pop[ci.cur];
    if (!r) return;
    r.need = Math.max(0, r.need + d);
    ciCPopRender();
    ciRender();
  }

  /* ---------------- 14. Khởi tạo ---------------- */
  function init() {
    if (!$('tl')) return;
    // Nguồn sự thật duy nhất cho bề rộng cột phòng là CSS var — tránh JS và CSS trôi lệch nhau
    // (lệch 1px là đường giờ hiện tại vẽ sai chỗ, lệch nhiều là nó chui vào dưới cột sticky).
    var cssRoomW = parseInt(getComputedStyle(document.documentElement).getPropertyValue('--tl-room-col'), 10);
    if (cssRoomW > 0) ROOM_W = cssRoomW;
    state.anchor = startOfDay(HOTEL_NOW);
    normalizeAll();                              // dữ liệu mẫu phải khớp quy tắc trạng thái trước khi vẽ
    loadPrefs();
    bind();
    render();
    bindGroupKeyboard();
    bindDatePicker();
    bindQB();
    bindCards();
    bindQr();
    bindLb();
    bindCfg();
    bindBd();
    bindRp();
    bindCd();
    bindMv();
    bindRl();
    bindCa();
    bindRv();
    bindPf();
    bindSv();
    bindCo();
    bindNa();
    bindRs();
    bindGs();
    bindCi();
    startClock();
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init);
  else init();
})();
</script>
</body>
</html>
